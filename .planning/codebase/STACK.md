# Technology Stack

**Analysis Date:** 2026-05-30

## Languages

**Primary:**
- Dart 3.11.5+ - Mobile app logic and UI in Flutter
- Java/Kotlin - Android platform layer (minimal, auto-generated by Flutter)

**Secondary:**
- Python - Backend services and Cloud Functions (planned, per CLAUDE.md)
- JavaScript/TypeScript - Web dashboard or parent-facing interfaces (if needed, not yet in codebase)

## Runtime

**Environment:**
- Flutter 3.18.0+ (channel: stable, revision 00b0c91f)
- Dart 3.11.5 - 3.x

**Package Manager:**
- Pub (Dart package manager)
- Lockfile: `pubspec.lock` present

**Android Target:**
- Minimum SDK: Android 5.0+ (typical Flutter default, confirmed for RTL support)
- Build system: Gradle
- Java compatibility: Android 7.0+ implied by Flutter constraints

## Frameworks

**Core:**
- Flutter 3.18.0+ - Cross-platform UI framework; Android-only for now per product strategy
- Material Design - UI design system (configured in `pubspec.yaml`, `uses-material-design: true`)

**Testing:**
- flutter_test (included in SDK) - Widget testing framework
- No additional test frameworks configured yet

**Build/Dev:**
- Flutter build system (Gradle for Android)
- Android Gradle Plugin
- Dart Analyzer - Static analysis (configured via `analysis_options.yaml`)

## Key Dependencies

**Critical:**
- `flutter` (SDK) - Core framework for UI, state, platform channels
- `cupertino_icons` 1.0.8+ - iOS-style icons (included by default, though iOS is deferred)

**Infrastructure:**
- `flutter_lints` 6.0.0 - Lint rules for Flutter best practices (development only)

**Planned (per CLAUDE.md, not yet added):**
- Firebase Admin SDK (Python) - Cloud Functions runtime for tutor backend
- Google ML Kit Digital Ink Recognition - Handwriting recognition on Android (validated, on-device)
- Firestore (Dart SDK) - Database and offline persistence
- Firebase Auth (Dart SDK) - User authentication
- Firebase Cloud Functions - Tutor AI backend (Python runtime)

## Configuration

**Environment:**
- No `.env` files currently present
- Configuration is minimal (Flutter defaults)
- Firebase configuration will require `google-services.json` (not yet in repo)

**Build:**
- `analysis_options.yaml` - Dart analyzer rules (Flutter lints enabled)
- `.metadata` - Flutter project metadata (version 00b0c91f, stable channel)
- `android/gradle.properties` - Android Gradle configuration
  - JVM args: `-Xmx8G -XX:MaxMetaspaceSize=4G`
  - AndroidX enabled
- `build.gradle` (Android) - Not yet present in app directory (auto-generated on build)

**Android Manifest:**
- `android/app/src/main/AndroidManifest.xml` - App declaration, MainActivity, minimal permissions
- Supports RTL layout via `android:layoutDirection`

## Platform Requirements

**Development:**
- Dart SDK 3.11.5+
- Flutter SDK 3.18.0+
- Android SDK tools
- For RTL rendering: Dart/Flutter RTL support (builtin)

**Production:**
- Android 5.0+ device with stylus support (tablet target)
- Network connectivity for Firebase services (offline mode via Firestore planned)
- Google Cloud Project (for Firebase, ML Kit API keys)

## Architecture Pattern

**Client-side:**
- Single-platform (Android tablet) with Flutter
- On-device ML Kit for handwriting recognition (no network round-trip for stroke scoring)
- Firestore for session data and offline persistence

**Backend:**
- Firebase Cloud Functions (Python) for tutor AI response generation
- No custom backend infrastructure at this stage

## Known Constraints

- **Android-only** until iOS port is explicitly approved
- **No Firebase integrations added yet** — decision made, implementation pending
- **No handwriting recognition library integrated** — ML Kit Digital Ink validated, not yet wired
- **Minimal dependencies** by design — fresh Flutter project with only essentials

---

*Stack analysis: 2026-05-30*
