---
phase: quick-260720-up4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/curriculum/graphs/baa.json
  - assets/curriculum/curriculum_graph.json
  - tools/content/validate.py
  - test/curriculum/learned_letters_lint_test.dart
  - lib/tutor/exercise_selector_provider.dart
  - test/tutor/l3_learned_letters_parity_test.dart
  - tools/firebase/test_seed_curriculum_v2.py
  - server/app/curriculum_data/curriculum_graph.json
  - server/app/curriculum_data/baa_authored_ids.json
  - lib/features/letter_unit/letter_unit_controller.dart
  - test/features/letter_unit/baa_live_path_mastery_test.dart
autonomous: true
requirements: [D-06, D-14]        # D-06 mastery invariant (enrichment never gates the star), D-14 graph byte-parity
user_setup: []

must_haves:
  truths:
    - "The 4 baa reach-ahead grammar cards (fillBlank.adjective, transformWord.dual/plural/opposite) no longer appear as live nodes in either baa graph asset"
    - "A child who completes baa's presented essential clean-reps has selection TERMINATE (route to Mastery) even when unpassable enrichment tail nodes remain — the star is no longer blocked forever (D-06 at routing level)"
    - "After baa mastery, progression advances to taa"
    - "L0 content gate, L1 learned-letters lint, and the graph byte-parity guard all pass with the 14-node baa graph"
    - "A live-path regression test drives the REAL bundled baa graph through the REAL selection path to the star without calling recordMasteryIfMet directly"
  artifacts:
    - path: "assets/curriculum/graphs/baa.json"
      provides: "14-node baa graph (4 reach-ahead nodes removed; enrichment competency declarations retained)"
      contains: "\"nodes\""
    - path: "assets/curriculum/curriculum_graph.json"
      provides: "byte-parity canonical baa graph (14 nodes)"
      contains: "\"nodes\""
    - path: "lib/features/letter_unit/letter_unit_controller.dart"
      provides: "selection terminates on presented-essential mastery-met (shared predicate with recordMasteryIfMet)"
      contains: "_selectNext"
    - path: "test/features/letter_unit/baa_live_path_mastery_test.dart"
      provides: "live-path regression: real graph + real selection → star → progression to taa"
      min_lines: 40
  key_links:
    - from: "lib/features/letter_unit/letter_unit_controller.dart#_selectNext"
      to: "isMasteryMetForPresented/isMasteryMet"
      via: "shared _isMasteryMetNow(graph) helper — returns null (terminate) when met"
      pattern: "_isMasteryMetNow|isMasteryMetForPresented"
    - from: "lib/features/letter_unit/letter_unit_screen.dart#_advanceSelection"
      to: "recordMasteryIfMet"
      via: "null pick → route to Mastery section → post-frame recordMasteryIfMet writes the LetterMastery row"
      pattern: "recordMasteryIfMet"
---

<objective>
Fix "baa unit never advances." Three coupled deliverables:

1. Make the 4 baa reach-ahead grammar cards DORMANT — remove their nodes from both baa
   graph assets (byte-parity) and shrink every allowlist/parity layer to the 18 taa/thaa
   ids. Exercise CONFIGS stay in exercises.json (fully reversible, like buildSentence).
2. Fix the mastery-route bug (D-06 invariant at routing level): selection must TERMINATE
   (return null → route to Mastery) once the presented-essential mastery condition is met,
   so unpassable enrichment tail nodes can never block the star forever.
3. Close the harness gap with a live-path regression test that drives the REAL bundled baa
   graph through the REAL selection path to the star — without calling recordMasteryIfMet
   directly.

Purpose: A child who genuinely completes baa's essential clean-reps earns the star and the
journey map advances to taa. Today the walk never exhausts (`_legalForward` re-offers every
legal node after the cursor), so `recordMasteryIfMet` — which fires only when selection
returns null = walk exhausted — never runs.

Output: 14-node baa graph (both assets), shrunk allowlists (L0/L1/L3 + 2 pinned-set
fixtures), regenerated server data, a one-seam routing fix, and a green live-path test.

PROVENANCE (record in SUMMARY): making the 4 baa cards dormant is the owner's decision of
2026-07-20, which REVERSES the mother's F1 verdict pending her re-confirmation packet. The
Cloud Run redeploy of the regenerated server data remains owner-gated — NOT this plan.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The routing seam + mastery predicate (already exist — reuse, do not restructure)
@lib/features/letter_unit/letter_unit_controller.dart
@lib/curriculum/mastery_condition.dart
@lib/features/letter_unit/letter_unit_screen.dart

# The graph parser (confirmed tolerant of node-less competencies — see fromJson)
@lib/curriculum/curriculum_graph.dart

# The four allowlist/parity layers (exact anchors are in the task actions)
@tools/content/validate.py
@test/curriculum/learned_letters_lint_test.dart
@lib/tutor/exercise_selector_provider.dart

# The test pattern to model the new live-path test on (dual-drain _awaitPumping, one case/file)
@test/features/letter_unit/mastery_progression_harness.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the 4 baa reach-ahead nodes dormant + shrink every allowlist/parity layer to 18</name>
  <files>assets/curriculum/graphs/baa.json, assets/curriculum/curriculum_graph.json, tools/content/validate.py, test/curriculum/learned_letters_lint_test.dart, lib/tutor/exercise_selector_provider.dart, test/tutor/l3_learned_letters_parity_test.dart, tools/firebase/test_seed_curriculum_v2.py, server/app/curriculum_data/curriculum_graph.json, server/app/curriculum_data/baa_authored_ids.json</files>
  <action>
Remove the 4 nodes `baa.fillBlank.adjective`, `baa.transformWord.dual`, `baa.transformWord.plural`,
`baa.transformWord.opposite` from the live `nodes[]` array in BOTH `assets/curriculum/graphs/baa.json`
AND `assets/curriculum/curriculum_graph.json`. Result: baa goes 18→14 nodes. The two files MUST stay
byte-parity (deep-equal) — `test/curriculum/graph_asset_parity_test.dart` guards this (D-14). KEEP the
now-empty `wordBuilding` and `grammarTransform` competency declarations in `competencies[]`: the parser
`CurriculumGraph.fromJson` (lib/curriculum/curriculum_graph.dart) builds `nodes` and `competencies`
independently and derives `essentialNodes` from `nodes` only, so node-less enrichment competencies are
inert and tolerated — verified. Do NOT touch `exercises.json`: the 4 exercise configs STAY dormant there
(fully reversible, mirroring `baa.buildSentence.*`).

Shrink ALL allowlist layers so the union drops from 22 (4 baa + 18 taa/thaa) to 18 (taa/thaa only):
- `tools/content/validate.py` ~L72-78 (L0): empty out `_BAA_D09_EXCEPTIONS` to `frozenset()`. Leave
  `_TAA_THAA_D16_EXCEPTIONS` (L91-114, 18 ids) and `OWNER_APPROVED_EXCEPTIONS = _BAA_D09_EXCEPTIONS |
  _TAA_THAA_D16_EXCEPTIONS` (L118) intact — the union now yields 18. Verify the message strings that
  print `len(_BAA_D09_EXCEPTIONS)` (~L519-520, L588-589) still render (they will read "0 baa") — no
  assertion breaks.
- `test/curriculum/learned_letters_lint_test.dart` ~L135-139: set `baaOwnerApprovedExceptions` to an
  empty set `<String>{}`; keep the `'baa': baaOwnerApprovedExceptions` per-unit map entry (L175, now
  empty). This edit MUST land together with the node removal above: the non-rot check (~L235-238)
  asserts every allowlisted id is a LIVE node, so removing nodes while leaving them allowlisted FAILS
  the lint — emptying the baa allowlist is what keeps it green.
- `lib/tutor/exercise_selector_provider.dart` ~L113-116 (L3): drop the 4 `baa.*` ids from
  `kApprovedReachAheadExceptions`. Keep the 18 taa/thaa ids.
- `test/tutor/l3_learned_letters_parity_test.dart`: this file PINS the set. Remove the 4 baa ids
  (~L63-66 and ~L87-90), change `expect(kApprovedReachAheadExceptions, hasLength(22))` (~L111) to
  `hasLength(18)`, and update the test-name string (~L79-80) from "22 owner-approved ids (4 baa D-09 +
  18 taa/thaa D-16)" to "18 owner-approved ids (18 taa/thaa D-16)".
- `tools/firebase/test_seed_curriculum_v2.py` ~L55-58: remove the 4 baa ids from the local list that
  the membership assertion (~L197 `assert ex_id in OWNER_APPROVED_EXCEPTIONS`) iterates — otherwise it
  fails once the ids leave the union. `tools/firebase/seed_curriculum_v2.py` imports
  `OWNER_APPROVED_EXCEPTIONS` from validate.py (verified — no local copy), so it inherits the shrink.

Regenerate the server copy from the canonical asset (never hand-edit the server JSON): run
`cd server && uv run python -m app.curriculum_data.generate` (entrypoint
`server/app/curriculum_data/generate.py` derives `curriculum_graph.json` + `baa_authored_ids.json`,
filtered to `baa.*`, from `assets/curriculum/curriculum_graph.json`). Commit the regenerated
`server/app/curriculum_data/*.json`. Do NOT redeploy Cloud Run — that stays owner-gated.
  </action>
  <verify>
    <automated>python3 -m tools.content.validate --gate && flutter test test/curriculum/learned_letters_lint_test.dart test/curriculum/graph_asset_parity_test.dart test/tutor/l3_learned_letters_parity_test.dart</automated>
  </verify>
  <done>baa graph is 14 nodes in both assets (byte-parity holds); the 4 baa reach-ahead ids are gone from L0/L1/L3 + both pinned-set fixtures (union = 18); server JSON regenerated and committed; L0 gate, L1 lint, parity guard, and L3 parity test all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminate selection on presented-essential mastery-met (D-06 routing invariant, one seam)</name>
  <files>lib/features/letter_unit/letter_unit_controller.dart</files>
  <behavior>
    - When every presented essential node has met its clean-reps, `_selectNext` returns null so the
      shell routes to Mastery (where `recordMasteryIfMet` writes the star) — even if legal enrichment
      tail nodes remain to be offered.
    - The termination check runs on BOTH pass and fail moments (a fail on an enrichment node must not
      block a star that is already earned — enrichment never gates, D-06).
    - When mastery is NOT yet met, `_selectNext` behaves exactly as today (normal pick, arc advance,
      remediation on fail) — no behavior change on the not-yet-met path.
    - The routing termination check and `recordMasteryIfMet`'s recording check use the IDENTICAL
      predicate, so selection never terminates to Mastery only for `recordMasteryIfMet` to decline
      (which would create a NEW dead-end: Mastery reached, no star).
  </behavior>
  <action>
Extract a private helper on `LetterUnitController`, e.g. `Future<bool> _isMasteryMetNow(CurriculumGraph
graph)`, that mirrors the exact logic already inside `recordMasteryIfMet` (:698-701): read the child's
Drift clean-reps via `exerciseCleanRepsFor(_letterId, childProfileId: _childProfileId)`, compute
`presented = await _presentedExerciseIds(graph)`, then return `presented.isNotEmpty ?
isMasteryMetForPresented(graph, reps, presented) : isMasteryMet(graph, reps)`. The helper is a PURE
read + predicate — it must NOT write, must NOT set `masteryRecorded`, and must NOT flip
`masteryEvaluationFailed` (leave the write + flags owned by `recordMasteryIfMet`). On a read/graph
failure the helper returns false (fail-safe: never terminate off a guess).

Refactor `recordMasteryIfMet` (:674-733) to call this shared helper for its met/not-met decision so
the two code paths cannot drift apart. Keep `recordMasteryIfMet`'s existing write, `masteryRecorded`
idempotency, `_essentialFloor` cleanReps value, and loud-degrade logging unchanged.

In `_selectNext` (:505-571), immediately AFTER the graph loads (just after the try/catch at :507-519,
before the `current`/`selFacts`/pick logic at :520+): `if (await _isMasteryMetNow(graph)) { state =
state.copyWith(selectionActive: true); return null; }`. Setting `selectionActive: true` and returning
null makes `nextReady()` resolve to null, so `_advanceSelection` (screen :330-345) routes to the
Mastery section, whose post-frame `_recordMasteryIfMet` persists the row. This is the SAME
"graph exhausted → route to Mastery" shape already used at :566-568 — you are adding a mastery-met
early exit alongside the existing exhausted-walk exit.

Keep the change MINIMAL and at this ONE seam. Do NOT restructure `SelectionPolicy`, the walker, or
`_legalForward`. Note the timing property for the SUMMARY: the check reads the freshest Drift reps; the
live-path test (Task 3) seeds reps at threshold before the final scored moment so termination is
deterministic. In production, on the exact final rep a fire-and-forget increment race could at worst
re-present the terminal essential once more before terminating — still advancing, and self-healing on
re-entry (strictly better than today's never-terminating walk).
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/ test/curriculum/ test/tutor/</automated>
  </verify>
  <done>`_selectNext` returns null (routing to Mastery) whenever presented-essential mastery is met, on pass and fail; `recordMasteryIfMet` and the routing check share one predicate; the not-yet-met path is behavior-preserving; existing letter_unit/curriculum/tutor suites stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live-path regression test — real baa graph + real selection → star → progression to taa</name>
  <files>test/features/letter_unit/baa_live_path_mastery_test.dart</files>
  <behavior>
    - Drives the REAL bundled baa graph (now 14 nodes) through the REAL selection path — NOT a
      single-node graph (unlike live_selection_shell_test.dart) and WITHOUT calling
      `controller.recordMasteryIfMet()` directly (unlike mastery_progression_harness.dart :304-307).
    - Seeds Drift clean-reps at threshold for baa's PRESENTED essential nodes (or drives the last rep
      through the live scored path per the harness pattern), then triggers the live advance.
    - Asserts: (a) selection TERMINATES — the Mastery section is reached via `_advanceSelection`
      (nextReady resolves null); (b) the `LetterMastery` row IS written for baa; (c) progression
      advances to taa (assert via the same progression snapshot the harness already reads).
  </behavior>
  <action>
Create `test/features/letter_unit/baa_live_path_mastery_test.dart` as ONE test case in its own file
(the rootBundle deadlock rule: any testWidgets after the first in a process, or a >50KB isolate decode,
stalls — see mastery_progression_harness.dart :35/:107 and the documented deadlock modes). Model it on
`mastery_progression_harness.dart`: reuse its dual-drain `_awaitPumping` helper (:118) for every await
that crosses the flutter/assets channel (graph load, Drift reads/writes, provider reads); reuse its
Drift/provider harness setup and its progression-snapshot read (~:328/:352). Prefer factoring the shared
scaffolding rather than duplicating it, but keep this as a standalone one-case file.

The distinguishing move from the existing harness: do NOT call `recordMasteryIfMet()` directly. Instead,
seed the child's clean-reps to threshold for baa's presented essential set (use
`incrementExerciseCleanReps` / the same Drift accessors `recordMasteryIfMet` reads, so the seeded reps
satisfy `isMasteryMetForPresented`), pump the controller through the LIVE selection path (mount the unit
screen / drive `_advanceSelection` via `nextReady`, as the shell does), and assert WITHOUT touching
`recordMasteryIfMet`: (a) the live advance routes to Mastery (selection returned null / Mastery section
index reached), (b) the `LetterMastery` row for baa exists in Drift, (c) the progression snapshot
advances to `taa`.

PREREQ on a fresh checkout/worktree: run `flutter gen-l10n` before the widget test compiles
(lib/l10n/app_localizations*.dart is gitignored). This test is the regression proof for BOTH Task 1
(14-node graph reaches the star) and Task 2 (selection terminates on mastery-met).
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/baa_live_path_mastery_test.dart</automated>
  </verify>
  <done>The new one-case file drives the real 14-node baa graph through the real selection path, seeds reps at threshold, and asserts (a) Mastery reached via _advanceSelection, (b) LetterMastery row written, (c) progression advances to taa — all green, with recordMasteryIfMet never called directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change touches authored curriculum DATA (no user input), local Dart routing logic, and test code. No new external surface, no new packages, no new network/auth path. The server JSON is regenerated locally and its deploy stays owner-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-up4-01 | Tampering | Graph/allowlist divergence (asset vs server vs allowlists) | mitigate | Byte-parity guard (graph_asset_parity_test), L0 gate, L1 lint non-rot check, and L3 pinned-set parity test all run in Task 1's verify — divergence fails the gates. |
| T-up4-02 | Denial of Service | Child stuck forever at the baa unit (the very bug) | mitigate | Task 2 gives selection a termination condition; Task 3's live-path test proves the star + progression fire. |
</threat_model>

<verification>
All gates must pass (run after Task 3):
- `python3 -m tools.content.validate --gate` (L0 content gate)
- `flutter test test/curriculum/learned_letters_lint_test.dart` (L1)
- `flutter test test/curriculum/graph_asset_parity_test.dart` (byte-parity, D-14)
- `flutter test test/tutor/l3_learned_letters_parity_test.dart` (L3 pinned set = 18)
- `flutter test test/features/letter_unit/ test/curriculum/ test/tutor/`
- `flutter test test/features/letter_unit/baa_live_path_mastery_test.dart` (new live-path test)

Server-side sanity: `server/app/curriculum_data/curriculum_graph.json` regenerated from the asset
(baa-only, 14 nodes) and committed. If the server has a self-check test, run it; the Cloud Run
redeploy is explicitly out of scope (owner-gated).
</verification>

<success_criteria>
- The 4 baa reach-ahead grammar nodes are gone from both baa graph assets (14 nodes, byte-parity); their exercise configs remain dormant in exercises.json (reversible).
- Every allowlist/parity layer (L0 validate.py, L1 lint, L3 selector + l3 parity test, seed test fixture) shrinks from 22 to 18 ids; the two enrichment competency declarations are retained (parser-tolerated).
- `_selectNext` terminates selection (routes to Mastery) once presented-essential mastery is met, on pass and fail, sharing ONE predicate with `recordMasteryIfMet`; the not-yet-met path is behavior-preserving.
- A one-case live-path test drives the real 14-node baa graph through real selection to the star and asserts progression to taa, without calling recordMasteryIfMet directly.
- All gates listed in <verification> are green.
</success_criteria>

<output>
Create `.planning/quick/260720-up4-fix-baa-never-advances-dormant-4-reach-a/260720-up4-SUMMARY.md` when done.

The SUMMARY MUST record:
- PROVENANCE: the 4 baa cards are made dormant per the owner's 2026-07-20 decision, which REVERSES
  the mother's F1 verdict pending her re-confirmation packet.
- The server data was regenerated but the Cloud Run redeploy remains owner-gated (NOT done here).
- KNOWN LIMITATION (do NOT fix here): taa/thaa remain unfinishable — their 18 reach-ahead word cards
  include ESSENTIAL competency nodes, and their letter-form rework (F2) is the mother's Phase-27
  authoring. This task fixed baa + the D-06 routing invariant only.
- The production timing note from Task 2 (fresh-reps read; worst-case one extra terminal-essential
  re-present on the exact final rep; self-heals; strictly better than the never-terminating walk).
</output>
