---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 05
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - CLAUDE.md
  - docs/legal/RELEASE-COMPLIANCE-2.0.1.md
autonomous: false
requirements: [D-09b, D-09c, PLAT-01]
user_setup:
  - service: google-play-console
    why: "Publish the data-safety + app-access declarations that match the account-first code (D-03, blocking release work for a children's product)"
    dashboard_config:
      - task: "Update the Data safety form to declare parent-email collection + account requirement; update the App access declaration to state login is required (provide test credentials); publish the privacy/legal page matching the same wording"
        location: "Play Console -> App content -> Data safety / App access; hosted privacy page"

must_haves:
  truths:
    - "The CLAUDE.md Decided section carries an EXPLICIT, DATED amendment: the account is now the FRONT DOOR (superseding 'reachable ONLY from behind the PIN-gated parent area') and the account GATES all use (superseding 'does not yet gate ... any data') — D-02."
    - "The child-safety core is untouched by the amendment: children never log in (D-09b) and sign-up links the boot anonymous identity (D-09c); only the GATING clause changed, not the SYNC clause (sync stays future scope)."
    - "A repository compliance artifact states, word-for-word to be transcribed, the Play data-safety + app-access + legal-page declarations: account required, parent EMAIL collected, no child PII collected/shared, data not synced across devices — matching the code (D-03)."
    - "The owner has ratified the amendment wording and transcribed the declarations into Play Console + the hosted legal page (SC1: legal pages and Play forms agree with the code word for word)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Dated D-02 amendment to the Decided/parent-accounts block"
      contains: "AMENDED"
    - path: "docs/legal/RELEASE-COMPLIANCE-2.0.1.md"
      provides: "The exact Play data-safety + app-access + legal-page declaration text matching the account-first code"
  key_links:
    - from: "lib/services/auth_service.dart (parent email collection)"
      to: "docs/legal/RELEASE-COMPLIANCE-2.0.1.md (data-safety declaration)"
      via: "declaration mirrors the code's actual data collection word-for-word"
      pattern: "email"
---

<objective>
Ratify the account-first entry model in the two governance surfaces that currently
contradict the shipped code (D-01), and align the Play/legal declarations to it (D-03):
(1) formally AMEND the CLAUDE.md Decided section (D-02) — an explicit, dated amendment
of the two superseded clauses, keeping the child-safety core (D-09b/D-09c) intact; and
(2) author the exact Play data-safety form + app-access declaration + legal-page wording
(D-03) that states account-gating + parent-email collection, matching the code
word-for-word, then have the owner transcribe it into Play Console + the hosted page.

Purpose: SC1 — "a recorded owner decision names the entry model ... legal pages and Play
forms agree with the code word for word." Closes D-01 (ratification half), D-02, D-03.
This is a compliance statement for a children's product — blocking release work.
Output: the dated CLAUDE.md amendment, a repository compliance artifact, and owner sign-off
+ transcription. autonomous:false (owner ratifies the amendment; owner does the Play/page).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@CLAUDE.md
@lib/services/auth_service.dart
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Owner ratifies the CLAUDE.md Decided-section amendment wording (D-01/D-02)</name>
  <read_first>
    - CLAUDE.md (the Decided/parent-accounts-LIVE block, lines 84-91: "reachable ONLY from behind the PIN-gated parent area" and "Foundation scope — the account does not yet gate or sync any data")
    - .planning/phases/26-.../26-CONTEXT.md (D-01 account-first ratify; D-01a children never log in; D-02 the exact two clauses superseded; D-02 note "only the GATING clause changes" — sync stays future)
  </read_first>
  <action>Present the two proposed dated amendment lines (front-door supersedes the PIN-gated clause; gating supersedes the "does not yet gate" clause; D-09b/D-09c + the sync clause preserved) to the owner and pause for ratification. Do NOT write CLAUDE.md until approved — this is a decision gate; no files change in this task.</action>
  <decision>Confirm the exact dated amendment text for the two superseded CLAUDE.md Decided clauses under the account-first ratification (D-01/D-02).</decision>
  <context>
    The Decided section still says accounts are "reachable ONLY from behind the PIN-gated
    parent area" and the account "does not yet gate ... any data". D-01 makes the account
    the FRONT DOOR and it now GATES all use. D-02 requires an explicit, dated amendment
    (not a silent rewrite). The child-safety core is NON-negotiable and stays: children
    never log in (D-09b/D-01a); sign-up links the boot anonymous identity (D-09c); the
    account still does NOT SYNC data (only the GATING clause changes). Present the proposed
    amendment lines for approval BEFORE writing them.
  </context>
  <options>
    <option id="approve-as-proposed">
      <name>Approve the proposed dated amendment (front-door + gating superseded; child-safety + sync clauses preserved)</name>
      <pros>Governance matches shipped code; child-safety core explicitly preserved; sync stays future scope</pros>
      <cons>None — reflects the owner's D-01 decision</cons>
    </option>
    <option id="revise-wording">
      <name>Owner revises the amendment wording</name>
      <pros>Exact phrasing is the owner's call</pros>
      <cons>Round-trip before Task 2 can write it</cons>
    </option>
  </options>
  <resume-signal>Approve the amendment wording, or provide the exact revised text.</resume-signal>
  <acceptance_criteria>
    - The owner has approved (or supplied) the exact amendment text for both superseded clauses.
    - The approved text keeps D-09b (children never log in) and D-09c (anonymous-boot linking) explicitly intact and changes ONLY the gating clause (not sync).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply the dated amendment to CLAUDE.md and author the Play/legal compliance declaration (D-02/D-03)</name>
  <files>CLAUDE.md, docs/legal/RELEASE-COMPLIANCE-2.0.1.md</files>
  <read_first>
    - CLAUDE.md (Decided block lines 84-91 — the two clauses to amend)
    - lib/services/auth_service.dart (what data is actually collected: parent email via signUpWithEmail/signInWithEmail; Google parent sign-in; children never authenticate; no child PII; no sync)
    - Project memory: "Firestore rules DEPLOYED ... NO child-data collection match exists (D-11, zero child PII surface in Firestore)" and "Parent accounts LIVE"
    - .planning/phases/26-.../26-CONTEXT.md (D-03: declarations must match the code word-for-word — account required + parent-email collection)
  </read_first>
  <action>
    Apply the Task-1-approved amendment to the CLAUDE.md Decided/parent-accounts block:
    add an explicit dated line (e.g. "AMENDED 2026-07-20 (D-01/D-02, Phase 26):") that
    supersedes (a) "reachable ONLY from behind the PIN-gated parent area" -> the account
    is now the mandatory FRONT DOOR (every launch starts at /auth), and (b) "does not yet
    gate ... any data" -> the account now GATES all use. Keep the original lines visible
    as superseded (strike-through phrasing or an explicit "superseded:" prefix) so the
    history is honest. Explicitly restate that D-09b (children never log in) and D-09c
    (anonymous-boot linking on sign-up) are UNCHANGED, and that the account still does NOT
    SYNC data (sync stays future scope — only the gating clause changed). Then write
    docs/legal/RELEASE-COMPLIANCE-2.0.1.md holding the EXACT declaration text for the
    owner to transcribe: the Play DATA SAFETY answers (collected: parent email + account
    identifier for account management/auth; NOT collected: any child PII; NOT shared;
    NOT synced across devices; encrypted in transit), the APP ACCESS declaration (all/most
    of the app is behind a required parent login — provide reviewer test credentials), and
    the LEGAL/PRIVACY PAGE wording (account + parent email required to use the app;
    children do not have accounts; minimum child data, private by default, parent-controlled).
    Every claim must mirror the code — do NOT declare data the app does not collect and do
    NOT omit the parent email it does.
  </action>
  <acceptance_criteria>
    - CLAUDE.md contains a dated amendment marker (grep for "AMENDED" + "2026-07-20" in the Decided block) superseding both clauses.
    - The amendment explicitly preserves D-09b and D-09c (grep confirms both IDs present in the amended block) and states sync stays future scope.
    - docs/legal/RELEASE-COMPLIANCE-2.0.1.md exists with three sections: Data safety, App access, Privacy/legal page — each stating account-gating + parent-email collection + no child PII + no sync.
    - Source-vs-declaration check: the declaration's "collected" list matches auth_service.dart (parent email) and the "not collected" list matches the zero-child-PII Firestore posture (recorded in the SUMMARY with the citing lines).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "AMENDED" CLAUDE.md && grep -q "D-09b" CLAUDE.md && grep -q "D-09c" CLAUDE.md && test -s docs/legal/RELEASE-COMPLIANCE-2.0.1.md</automated>
  </verify>
  <done>The Decided section is formally amended (child-safety intact) and the exact Play/legal declaration text exists, matching the code.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Owner transcribes the declarations into Play Console + the hosted legal page (D-03)</name>
  <read_first>
    - docs/legal/RELEASE-COMPLIANCE-2.0.1.md (the exact text authored in Task 2)
    - .planning/phases/26-.../26-CONTEXT.md (D-03: blocking release work; Success Criterion 1 — Play forms + legal pages agree with the code word for word)
  </read_first>
  <action>Owner-only step (no CLI path): the owner transcribes docs/legal/RELEASE-COMPLIANCE-2.0.1.md verbatim into the Play Console Data safety form + App access declaration and publishes the matching hosted privacy/legal page, then confirms each surface reads word-for-word as the artifact. Any wording that must differ is reported back so the artifact is reconciled.</action>
  <what-built>The exact Data safety + App access + privacy-page declaration text (docs/legal/RELEASE-COMPLIANCE-2.0.1.md), matching the account-first code word-for-word.</what-built>
  <how-to-verify>
    1. In Play Console -> App content -> Data safety, enter the declaration exactly as authored (parent email collected for account management/auth; no child PII; not shared; not synced; encrypted in transit).
    2. In Play Console -> App content -> App access, declare login is required and provide reviewer test credentials.
    3. Publish/update the hosted privacy/legal page with the matching wording.
    4. Confirm each published surface reads word-for-word as the artifact (no drift).
  </how-to-verify>
  <resume-signal>Type "declarations published" (with the Play form + page states), or list any wording that had to differ so the artifact can be reconciled.</resume-signal>
  <acceptance_criteria>
    - Owner confirms the Data safety form, App access declaration, and hosted legal page are published and match docs/legal/RELEASE-COMPLIANCE-2.0.1.md word-for-word.
    - Any unavoidable wording difference is reconciled back into the artifact so code, artifact, and published surfaces agree.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent -> FirebaseAuth | Parent email + password/Google credential collected here — the ONLY PII the app gathers; the declaration must state it accurately |
| child -> app | Children never authenticate (D-09b); no child PII is collected — the declaration must state the absence accurately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Repudiation/Compliance | mismatched Play data-safety vs code | mitigate | The declaration is authored FROM the code (auth_service.dart) and owner-verified word-for-word against the published surfaces (Task 3); a children's-product compliance requirement |
| T-26-05-02 | Information Disclosure | over-declaring child data | mitigate | Declaration asserts zero child PII, matching the deployed Firestore rules (no child-data collection match, D-11); reviewed against source in Task 2 |
| T-26-05-03 | Elevation of Privilege | amendment weakening child-safety | mitigate | Task 1 owner-gate + Task 2 grep-assert that D-09b/D-09c stay intact; only the gating clause changes, sync stays future |
| T-26-05-SC | Tampering | package installs | accept | No package installs (docs only); supply-chain surface unchanged |
</threat_model>

<verification>
- CLAUDE.md carries a dated D-02 amendment preserving D-09b/D-09c and the sync clause.
- The compliance artifact states account-gating + parent-email collection matching code.
- Owner ratified the amendment and published the Play/legal declarations word-for-word.
</verification>

<success_criteria>
Governance and compliance match the shipped account-first code: the Decided section is
formally amended (child-safety intact), and the Play data-safety + app-access + legal page
state account-gating and parent-email collection word-for-word. (Phase 26 Success
Criterion 1 — governance/compliance half.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-05-SUMMARY.md` when done.
</output>
