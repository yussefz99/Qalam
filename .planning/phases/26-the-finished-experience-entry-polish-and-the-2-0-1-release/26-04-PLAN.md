---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/prompts.py
  - server/tests/test_eval/test_variety.py
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - test/features/letter_unit/exercise_scaffold_agent_line_test.dart
autonomous: true
requirements: [FEEDBACK-DEBT]

must_haves:
  truths:
    - "Repeated attempts at the same mistake produce NON-verbatim coaching — the coach no longer echoes a GOLD EXEMPLAR line word-for-word, so feedback stops reading static (D-06a)."
    - "The bottom feedback bar (and the tutor bubble) render the AGENT's line on the baa agent path — never the authored floor line — proven via the live scored-attempt path (D-06b)."
    - "Grounding is untouched: only non-PII facts cross the network (GROUND-02) and the scorer still owns pass/fail (the coach only supplies words)."
  artifacts:
    - path: "server/app/prompts.py"
      provides: "COACH_PROMPT with the GOLD EXEMPLARS restructured so there is nothing to copy verbatim"
    - path: "test/features/letter_unit/exercise_scaffold_agent_line_test.dart"
      provides: "Live-path widget regression: foot + bubble show the agent line on a baa scored attempt"
      min_lines: 40
    - path: "server/tests/test_eval/test_variety.py"
      provides: "Regression: variety_report over the (updated) exemplar set flags 0 verbatim hits for the new prompt shape"
  key_links:
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart _foot"
      to: "tutorLineProvider (agentLine)"
      via: "footLine = _isAgentPath ? (agentLine ?? '') : state.line"
      pattern: "agentLine"
    - from: "server/app/prompts.py COACH_PROMPT"
      to: "server/tests/test_eval/test_variety.py"
      via: "variety_report(lines, exemplars) verbatim_exemplar_hits guard"
      pattern: "verbatim_exemplar"
---

<objective>
Close the two standing tutor-feedback debts (Phase-14/17), per D-06:
(a) the coach prompt's GOLD EXEMPLARS get copied VERBATIM, so feedback reads static;
(b) the bottom feedback bar shows the AUTHORED floor line instead of the agent's line
(`lib/features/letter_unit/widgets/exercise_scaffold.dart`).
Both must be closed so the on-screen line IS the agent's line and repeated attempts
produce non-verbatim coaching.

Purpose: SC3 — "the tutor's on-screen line is the agent's line and repeated attempts
produce non-verbatim coaching." Closes D-06.
Output: a restructured COACH_PROMPT that leaves nothing copyable + a variety regression;
and a LIVE-PATH widget test proving the foot/bubble render the agent line, plus any fix
needed in exercise_scaffold. Live model confirmation (make eval / on-device) folds into
26-06 and the owner-gated server re-deploy.

Note: this plan spans two subsystems (Python tutor server + Dart client) because D-06 is
two coupled feedback bugs; each is a single focused task with no file overlap.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@server/app/prompts.py
@lib/features/letter_unit/widgets/exercise_scaffold.dart
@lib/tutor/authored_fallback_brain.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the coach GOLD EXEMPLARS so nothing is copyable, and guard it with the variety report (D-06a)</name>
  <files>server/app/prompts.py, server/tests/test_eval/test_variety.py</files>
  <read_first>
    - server/app/prompts.py (COACH_PROMPT lines 45-63 — the GOLD EXEMPLARS block with the three LITERAL child-facing say-lines at 46-49, plus the HOW-TO-USE anti-copy rules; COACH_STROKE_ADDENDUM 74-101; note the tutor's-voice register from CLAUDE.md)
    - server/tests/test_eval/test_variety.py (variety_report(lines, exemplars) -> {distinct_ratio, verbatim_exemplar_hits, duplicate_pairs}; existing tests at 53-84, incl. test_a_verbatim_exemplar_echo_is_flagged)
    - CLAUDE.md (the tutor's-voice spec — warm, calm, specific, names the exact fix; "Never: Oops, try again!")
    - .planning/phases/26-.../26-CONTEXT.md (D-06a: exemplars copied verbatim -> feedback reads static; the fix lives in the tutor prompt)
  </read_first>
  <action>
    The root cause: COACH_PROMPT embeds three LITERAL, ready-to-speak child-facing lines
    (prompts.py 46-49) that the model echoes verbatim despite the existing "never copy"
    rules — there is copyable text present. Restructure the GOLD EXEMPLARS block so it
    conveys REGISTER (warmth, short length, named-fix specificity) WITHOUT any quotable
    child-facing sentence the model can lift: replace the three literal say-lines with an
    abstract/schematic description of the register (what a good line DOES — names this
    attempt's own geometry: dot side, which side of the bowl is flat, how shallow), keep
    the "NEVER: Oops, try again! / Great job! on a fail / a wall of text" anti-patterns,
    and keep the compose-every-line-fresh + vary-on-repeat rules. Do NOT weaken the
    GROUNDING RULE or the ACTION RULE. Update test_variety.py's exemplar fixture to the
    new prompt shape and add/adjust a regression asserting that a set of repeated-attempt
    coach lines drawn against THIS prompt's exemplars yields verbatim_exemplar_hits == 0
    and a high distinct_ratio (the presentation-not-parroting contract). The LIVE model
    confirmation via `make eval` needs Vertex ADC (owner-gated) and the server re-deploy
    (owner-authorized) — record both as 26-06 / release follow-ups; this task delivers the
    prompt change + the deterministic guard.
  </action>
  <acceptance_criteria>
    - Source assertion: COACH_PROMPT no longer contains the three literal exemplar say-strings as quotable lines — `grep -F "needs a deeper curve at the bottom — try again, slower this time" server/app/prompts.py` returns nothing (the copyable child-facing sentence is gone).
    - COACH_PROMPT still names the register requirement (warm/short/specific) and still forbids "Oops, try again!" (grep for the anti-pattern retained).
    - `python -m pytest server/tests/test_eval/test_variety.py` passes, including a guard that repeated-attempt lines against the updated exemplars produce 0 verbatim hits.
    - The GROUNDING RULE and ACTION RULE blocks are unchanged (grep confirms "GROUNDING RULE" and "ACTION RULE" still present).
    - SUMMARY records: server re-deploy (Cloud Run qalam-tutor) + `make eval` variety leg are owner-gated follow-ups verified in 26-06.
  </acceptance_criteria>
  <verify>
    <automated>cd server && python -m pytest tests/test_eval/test_variety.py -q</automated>
  </verify>
  <done>The coach prompt has no copyable child-facing exemplar line; the variety regression guards non-verbatim coaching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove (and fix if needed) the feedback bar renders the AGENT line, not the authored floor, on the live baa path (D-06b)</name>
  <files>test/features/letter_unit/exercise_scaffold_agent_line_test.dart, lib/features/letter_unit/widgets/exercise_scaffold.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (_isAgentPath line 439 (baa-only); _onResult 573-741 (the live scored-attempt path that routes the brain's line into tutorLineProvider); _foot 1117-1159 (footLine = _isAgentPath ? (agentLine ?? '') : state.line, line 1148); _TutorColumn._bubbleText 1315-1327; resolveFloorLine 172-183)
    - lib/tutor/authored_fallback_brain.dart (the authored FLOOR line the scaffold mirrors — the line that must NOT leak into the agent-path foot/bubble)
    - test/features/letter_unit/write_surface_test.dart (an existing letter_unit widget-test harness to model the fake-brain injection after)
    - .planning/phases/26-.../26-CONTEXT.md (D-06b: bottom feedback bar shows the authored line instead of the agent line; the fix is swapping which line the bar renders)
    - Project memory: "live-path widget tests mandatory" and "bottom feedback bar shows authored line not the agent line"
  </read_first>
  <behavior>
    - On the baa (agent) path, after a scored attempt whose brain returns a DISTINCTIVE agent line, the bottom feedback bar (FeedbackPanelV2 `line`) renders exactly that agent line — not the exercise's authored feedback/floor line.
    - The tutor bubble (_TutorColumn) renders the same agent line on the verdict.
    - Before the agent line resolves, the foot words are EMPTY (the verdict FACE/star still renders instantly from the scorer) — the authored floor never flashes in on the agent path.
    - A NON-agent letter (e.g. a fixture letter id != 'baa') still shows its own authored `state.line` in the foot (the floor is correct there).
  </behavior>
  <action>
    Write test/features/letter_unit/exercise_scaffold_agent_line_test.dart driving the
    REAL _onResult path (per project memory "live-path widget tests mandatory" — not a
    stubbed widget). Override tutorBrainFactoryProvider with a fake brain that returns a
    distinctive agent line (e.g. "AGENT_LINE_SENTINEL") for a baa exercise, feed a scored
    CheckResult, pump, and assert the FeedbackPanelV2 in the foot shows the sentinel
    (not the exercise's authored feedback line) AND the bubble shows the sentinel.
    Assert the empty-until-resolved behavior and the non-agent fallback to state.line.
    If HEAD already renders correctly (footLine at 1148 already reads agentLine for the
    agent path), the test PINS the invariant as a permanent regression and the SUMMARY
    records "verified correct at HEAD, guarded". If any path still leaks the authored
    floor into the agent-path foot or bubble (e.g. an error/timeout branch), fix it in
    exercise_scaffold.dart so the agent line is the single source of feedback words on the
    baa path. Do NOT change the verdict/star ownership (GROUND-01 — the scorer owns
    pass/fail; only the WORDS are the agent's).
  </action>
  <acceptance_criteria>
    - `flutter test test/features/letter_unit/exercise_scaffold_agent_line_test.dart` passes and drives the real _onResult path (the fake brain is injected via tutorBrainFactoryProvider, not a hand-built widget).
    - The foot FeedbackPanelV2 and the bubble both render the injected agent sentinel; the authored floor string is asserted ABSENT from the foot on the agent path.
    - The non-agent-letter case asserts the foot shows `state.line` (authored floor is correct there).
    - Source assertion: the agent-path foot line reads from `agentLine`/`tutorLineProvider`, not `resolveFloorLine`/`state.line` (grep confirms `_isAgentPath ? (agentLine` in _foot).
    - GROUND-01 unchanged: the verdict/star path (`applyResult`) is not modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/letter_unit/exercise_scaffold_agent_line_test.dart</automated>
  </verify>
  <done>A live-path regression proves the agent line is the on-screen feedback on the baa path; any authored-floor leak is fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device -> tutor server (/coach) | Only derived non-PII facts cross (mistakeId, strokeDiff summary, letterId) — GROUND-02; raw strokes/PII never leave |
| tutor server -> device | The coach returns a display-only WORDS line; it can never override the local scorer verdict (GROUND-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | coach prompt restructure | mitigate | GROUNDING RULE + ACTION RULE blocks left byte-intact; the change only removes copyable exemplar lines; grep-asserted |
| T-26-04-02 | Information Disclosure | agent line on screen | accept | The agent line carries only non-PII coaching words; GROUND-02 payload guard (existing) is unchanged by this plan |
| T-26-04-03 | Spoofing | agent line overriding verdict | mitigate | Task 2 leaves applyResult/GROUND-01 untouched — the scorer still owns pass/fail; only the WORDS channel is asserted |
| T-26-04-SC | Tampering | package installs | accept | No package installs (server + client edits only); supply-chain surface unchanged |
</threat_model>

<verification>
- COACH_PROMPT has no copyable child-facing exemplar; variety regression green.
- Live-path widget test proves the agent line is the on-screen feedback on baa.
- Server re-deploy + make-eval variety leg recorded as owner-gated 26-06/release follow-ups.
</verification>

<success_criteria>
The coach cannot parrot an exemplar verbatim (repeated attempts vary), and the on-screen
feedback line on the baa agent path is the agent's line, proven on the live path.
(Phase 26 Success Criterion 3 — tutor half; live model + device confirmation in 26-06.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-04-SUMMARY.md` when done.
</output>
