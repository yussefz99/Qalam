---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 07
type: execute
wave: 3
depends_on: [26-05, 26-06]
files_modified:
  - pubspec.yaml
  - .planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-RELEASE.md
autonomous: false
requirements: [PLAT-01]
user_setup:
  - service: google-play-console
    why: "Upload the 2.0.1+4 AAB to production and the APK to the webcourse in lockstep (release is gated ONLY after grading)"
    dashboard_config:
      - task: "Create the 2.0.1+4 production release with the signed AAB; upload the matching APK to the webcourse; archive the APK"
        location: "Play Console -> Production; the webcourse upload surface"

must_haves:
  truths:
    - "The release is cut ONLY after grading — the grading gate is an explicit owner authorization, never bypassed (D-01/ROADMAP)."
    - "The version is 2.0.1+4 (bumped from 2.0.0+3), and the signed AAB + APK are built TOGETHER from the same commit (no rebuild drift — finalization memory: NO rebuilds unless BOTH re-submitted)."
    - "Play (production AAB) and the webcourse (APK) move in LOCKSTEP or not at all; the matching APK is archived (SC5)."
    - "The release ships ONLY after the device pass (26-06) is green and the Play/legal declarations (26-05) match the code."
  artifacts:
    - path: "pubspec.yaml"
      provides: "version: 2.0.1+4"
      contains: "2.0.1+4"
    - path: ".planning/phases/26-.../26-RELEASE.md"
      provides: "The release record: commit SHA, AAB/APK checksums, Play + webcourse upload confirmation, archived-APK location"
  key_links:
    - from: "the 2.0.1+4 signed AAB"
      to: "Play production + webcourse APK"
      via: "lockstep owner upload after the grading gate"
      pattern: "2.0.1"
---

<objective>
Cut the 2.0.1+4 release — but ONLY after grading, with Play (production AAB) and the
webcourse (APK) moving in lockstep or not at all (D-01/ROADMAP + the finalization memory:
release/2.0 is frozen, v2.0.0+3 in review, 2.0.1+4 is the next cut; the AAB and APK are
built together and NOT rebuilt unless BOTH are re-submitted). This is the terminal plan:
it depends on the device pass (26-06) being green and the Play/legal declarations (26-05)
matching the code, plus the external grading gate.

Purpose: SC5 — "2.0.1+4 is live on Play production with the matching APK archived."
Output: the version bump, the signed AAB+APK built together from one commit, and the
owner-gated lockstep upload + archive. autonomous:false — the grading gate and the Play
upload are owner-only.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@.planning/finalization/README.md
@pubspec.yaml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Grading gate — owner authorizes the release cut (release ONLY after grading)</name>
  <read_first>
    - .planning/ROADMAP.md §Phase 26 ("Ships as 2.0.1+4 — cut ONLY after grading; Play and webcourse artifacts move in lockstep or not at all")
    - .planning/phases/26-.../26-CONTEXT.md (D-01 release posture; "Specific Ideas": 2.0.1+4, cut ONLY after grading; lockstep)
    - Project memory: finalization-workspace (release/2.0 frozen+pushed; v2.0.0+3 in Play review — do NOT disturb the in-review artifact unless re-submitting both)
  </read_first>
  <action>Owner-only gate: confirm grading is complete (or the owner explicitly authorizes cutting 2.0.1+4 now), confirm the lockstep intent (Play + webcourse together) and that 26-06 (device pass) + 26-05 (Play/legal) preconditions are met. Nothing is built or version-bumped until this authorization is recorded; on "hold" the plan pauses here.</action>
  <what-built>Nothing yet — this gate confirms the external precondition (grading complete) before any version bump or build.</what-built>
  <how-to-verify>
    1. Confirm grading is complete (or the owner explicitly authorizes cutting 2.0.1+4 now).
    2. Confirm this is intended to REPLACE/supersede v2.0.0+3 (or is a fresh production track), and that BOTH Play + webcourse will be updated in lockstep.
    3. Confirm the device pass (26-06) is green and the Play/legal declarations (26-05) are published and match the code.
  </how-to-verify>
  <resume-signal>Type "grading done — cut 2.0.1+4" (with the lockstep confirmation), or "hold" to defer the release.</resume-signal>
  <acceptance_criteria>
    - Explicit owner authorization to cut 2.0.1+4 AFTER grading is recorded.
    - 26-06 (device pass) green and 26-05 (Play/legal) published are both confirmed as preconditions met.
    - If "hold", the plan pauses here with nothing built — no premature cut.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bump to 2.0.1+4 and build the signed AAB + APK together from one commit</name>
  <files>pubspec.yaml, .planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-RELEASE.md</files>
  <read_first>
    - pubspec.yaml (line 19: `version: 2.0.0+3` -> `2.0.1+4`)
    - Project memory: play-upload-keystore (upload key at ~/qalam-upload-keystore.jks, creds in gitignored android/key.properties; keytool only via Android Studio's bundled JDK); finalization-workspace (AAB+APK built TOGETHER, no rebuilds unless both re-submitted); l10n-generated-gitignored (run flutter gen-l10n after a fresh checkout); phase-work-needs-own-branch
    - .planning/phases/26-.../26-06-SUMMARY.md (confirm the device pass passed on this same code)
  </read_first>
  <action>
    Bump pubspec.yaml line 19 `version: 2.0.0+3` -> `version: 2.0.1+4`. Ensure l10n is
    generated (`flutter gen-l10n`) and the analyzer/tests are clean on the release commit.
    Build the signed AAB and the APK TOGETHER from the SAME commit so they never drift
    (finalization memory): `flutter build appbundle --release` and `flutter build apk
    --release`, both signed via the upload keystore (~/qalam-upload-keystore.jks +
    android/key.properties — the signing config the release build already reads). Record
    in 26-RELEASE.md: the release commit SHA, the AAB + APK file paths + checksums
    (sha256), the build timestamp, and the archived-APK location. Do NOT rebuild after
    this (the AAB and APK must remain the matched pair); if a rebuild is ever needed, BOTH
    must be rebuilt and re-submitted together. If the keystore password is not available
    to this session, stop and hand the signing step to the owner (checkpoint), producing
    the unsigned/build-ready state and the exact commands.
  </action>
  <acceptance_criteria>
    - `grep -n "^version: 2.0.1+4" pubspec.yaml` matches.
    - The AAB and APK are built from the SAME commit SHA (recorded in 26-RELEASE.md with both sha256 checksums).
    - Both artifacts are signed with the upload keystore (or the signing step is explicitly handed to the owner with the exact commands, if the password is unavailable here).
    - 26-RELEASE.md records commit SHA, artifact paths, checksums, and the archived-APK location.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^version: 2.0.1+4" pubspec.yaml && test -s .planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-RELEASE.md</automated>
  </verify>
  <done>Version is 2.0.1+4 and the signed AAB+APK are built together from one recorded commit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Owner uploads to Play production + webcourse in lockstep and archives the APK (SC5)</name>
  <read_first>
    - .planning/phases/26-.../26-RELEASE.md (the built AAB/APK paths + checksums from Task 2)
    - Project memory: play-upload-keystore; shares-work-via-main (friends run it by pulling main); finalization-workspace (Play + webcourse lockstep)
  </read_first>
  <action>Owner-only step (no CLI path for the store upload): upload the 2.0.1+4 AAB to Play production and the matching APK to the webcourse in lockstep, archive the matching APK and record its location, and confirm the 26-05 Play declarations are still present on the listing — recording all of it in 26-RELEASE.md.</action>
  <what-built>The signed 2.0.1+4 AAB + matching APK, built together from one commit (Task 2), with the Play/legal declarations already aligned (26-05).</what-built>
  <how-to-verify>
    1. Upload the 2.0.1+4 AAB to Play Console -> Production, create the release, and roll it out.
    2. Upload the matching APK to the webcourse in LOCKSTEP (same commit/pair) — Play and webcourse move together or not at all.
    3. Archive the matching APK (record its location) so the exact production binary is retrievable.
    4. Confirm the Play data-safety + app-access declarations (26-05) are still in place on the production listing.
  </how-to-verify>
  <resume-signal>Type "2.0.1+4 live" (with the Play release state + webcourse + archived-APK location), or list any blocker.</resume-signal>
  <acceptance_criteria>
    - 2.0.1+4 is live on Play production (owner confirms the release state) and the matching APK is on the webcourse — in lockstep.
    - The matching APK is archived and its location is recorded in 26-RELEASE.md.
    - The Play declarations (26-05) are confirmed present on the production listing.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload keystore -> signed artifact | The signing key (~/qalam-upload-keystore.jks) authenticates the app to Play; losing/leaking it breaks update authority |
| build commit -> shipped binary | Play AAB + webcourse APK must be the SAME pair; a mismatch ships two different apps under one version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering | AAB/APK drift | mitigate | Both built from the same commit in one pass (Task 2), checksummed in 26-RELEASE.md; no rebuild unless BOTH re-submitted |
| T-26-07-02 | Spoofing/Integrity | upload key handling | mitigate | Keystore stays outside the repo (~/qalam-upload-keystore.jks); key.properties gitignored; owner performs signing/upload |
| T-26-07-03 | Repudiation | premature release before grading | mitigate | Task 1 grading gate blocks any build/upload until owner authorization |
| T-26-07-SC | Tampering | package installs | accept | No package installs (version bump + build only); supply-chain surface unchanged |
</threat_model>

<verification>
- Grading gate authorized before any build.
- Version 2.0.1+4; AAB+APK built together from one commit, checksummed.
- 2.0.1+4 live on Play production + webcourse in lockstep; matching APK archived.
</verification>

<success_criteria>
2.0.1+4 is live on Play production with the matching APK archived, cut only after grading,
with Play and webcourse in lockstep. (Phase 26 Success Criterion 5.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-07-SUMMARY.md` when done (with 26-RELEASE.md holding the release record).
</output>
