---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 06
type: execute
wave: 2
depends_on: [26-01, 26-02, 26-03, 26-04]
files_modified:
  - .planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-DEVICE-PASS.md
autonomous: false
requirements: [PLAT-01, D-09b, D-09c, FEEDBACK-DEBT]

must_haves:
  truths:
    - "On a REAL Android device running the REAL build (NO --dart-define=DEMO=true), fresh install routes to /auth (account-first) and sign-out routes cleanly to /auth and stays (D-01b / SC1)."
    - "The launcher tile is the Qalam mark on parchment — no Flutter default (D-05 / SC2)."
    - "The alif->thaa walk works incl. fail->retry; the scorer feel at the reverted tcc/tcw 0.10/0.15 is acceptable, OR the widened 0.12/0.16 is re-affirmed WITH a recorded device reason (D-04 / SC3-scorer)."
    - "The tutor's on-screen line is the agent's line and repeated attempts produce non-verbatim coaching (D-06 / SC3-tutor) — with the server re-deployed so the new prompt is live."
    - "Two-profile isolation holds (one child's progress never bleeds into another) and a v1.x->2.x update opens WITHOUT crashing (SC4)."
  artifacts:
    - path: ".planning/phases/26-.../26-DEVICE-PASS.md"
      provides: "Recorded device-pass results incl. the D-04 scorer verdict (originals held vs widened re-affirmed with reason)"
  key_links:
    - from: "real APK on device"
      to: "the 5 Success Criteria"
      via: "human walkthrough (no DEMO flag)"
      pattern: "device"
---

<objective>
Run the Android device pass the submission skipped, on a REAL physical device with the
REAL build (no DEMO flag — the DEMO flag boots the mocked /demo/* walkthrough, not the
live features, per the demo-flag memory). This is where all Wave-1 fixes and the icon are
verified end-to-end, and where the D-04 on-device scorer verdict is recorded (originals
held, or widened re-affirmed WITH the observed reason).

Purpose: SC4 — "the Android pass is green: 4-letter walk incl. fail->retry, profile
isolation, and v1->v2 update opens without crashing"; also the on-device confirmation legs
of SC1 (fresh-install + sign-out), SC2 (icon), and SC3 (scorer feel + agent-line/non-verbatim
coaching). Depends on 26-01/02/03/04 landing.
Output: a recorded device-pass result (26-DEVICE-PASS.md) with the D-04 verdict.
autonomous:false — a human on a physical device with a stylus; no CLI can trace letters.

Server prerequisite: the D-06a prompt change (26-04) only affects on-device coaching once
the Cloud Run qalam-tutor server is re-deployed — an owner-authorized step (each prod
deploy needs fresh explicit owner wording, per the tutor-server memory). Do the re-deploy
BEFORE the coaching-quality leg, or scope that leg to the deployed revision and note it.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-01-PLAN.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-03-PLAN.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Build the REAL APK, install on a physical Android device, verify entry + icon</name>
  <read_first>
    - .planning/phases/26-.../26-CONTEXT.md ("Established Patterns": the device pass must run the REAL build, NO --dart-define=DEMO=true, per demo-flag-is-mocked-walkthrough)
    - .planning/phases/26-.../26-01-PLAN.md (the sign-out routing fix being confirmed on device — D-01b)
    - .planning/phases/26-.../26-02-PLAN.md (the launcher icon being confirmed visually — D-05)
    - Project memory: demo-flag-is-mocked-walkthrough; ipad-co-demo-device (a co-equal test device is available)
  </read_first>
  <action>Human-on-device step (no CLI can trace a letter): build the REAL release APK with NO DEMO flag, sideload onto a physical Android device, and walk the entry + icon checks below, recording the outcomes in 26-DEVICE-PASS.md. Any failure is logged with an exact repro for a gap-closure plan.</action>
  <what-built>The account-first router with the sign-out routing fix (26-01) and the real Qalam adaptive launcher icon (26-02), in a real (non-DEMO) build.</what-built>
  <how-to-verify>
    1. Build the real APK: `flutter build apk --release` (NO --dart-define=DEMO=true). Sideload/install on a physical Android device (source the device early per the finalization memory).
    2. Home screen: confirm the launcher tile is the Qalam mark on parchment — no blue Flutter default (D-05 / SC2).
    3. Fresh install (or cleared app data): the app opens at /auth (account-first front door) — not straight into content (D-01 / SC1).
    4. Sign in (parent), reach Home (or onboarding then Home). Then SIGN OUT from Settings: confirm it lands on /auth and STAYS — no loop, no stranded blank/anonymous state (D-01b / SC1). Repeat sign-out from the parent account screen.
  </how-to-verify>
  <resume-signal>Type "entry+icon pass" with what you saw, or list any strand/loop/wrong-icon issue for a gap-closure fix.</resume-signal>
  <acceptance_criteria>
    - Recorded: launcher tile = Qalam mark on parchment (no Flutter default).
    - Recorded: fresh install opens at /auth; sign-out from BOTH surfaces ends at /auth and stays (no loop/strand).
    - Any failure is logged with the exact repro for a gap-closure plan.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Walk alif->thaa incl. fail->retry; record the D-04 scorer verdict and the coaching-quality check</name>
  <read_first>
    - .planning/phases/26-.../26-03-PLAN.md (D-04: originals 0.10/0.15 restored; the on-device FALLBACK — re-affirm widened 0.12/0.16 WITH a recorded reason only if real clean strokes false-fail)
    - .planning/phases/26-.../26-04-PLAN.md (D-06: agent line on screen; repeated attempts non-verbatim — needs the server re-deploy live)
    - Project memory: tutor-server-deployed (each prod deploy needs fresh explicit owner wording; /health not /healthz; demo warm-up min-instances); stroke-aware-coaching-decision (scorer owns pass/fail for all letters incl. baa)
  </read_first>
  <action>Human-on-device step: (owner-authorize the Cloud Run qalam-tutor re-deploy so the 26-04 prompt is live, or scope the coaching leg to the current revision), then trace alif->baa->taa->thaa with a stylus incl. a deliberate fail->retry, judge the scorer feel at 0.10/0.15, and check the coaching quality — recording the D-04 verdict (originals held vs widened re-affirmed + reason) and the coaching result in 26-DEVICE-PASS.md.</action>
  <what-built>The re-tightened scorer (26-03) and the closed tutor-feedback bugs (26-04), verified live on a real device (server re-deployed for the coaching leg).</what-built>
  <how-to-verify>
    1. Ensure the Cloud Run qalam-tutor server is re-deployed with the 26-04 prompt change (owner-authorized; confirm /health is up). If not re-deployed, scope the coaching leg to the current revision and record that.
    2. Walk the letters alif -> baa -> taa -> thaa. For at least one letter, deliberately FAIL a stroke then RETRY and confirm the retry path works and advances on a clean rep (SC4 4-letter walk incl. fail->retry).
    3. Scorer feel (D-04): with the reverted 0.10/0.15, write REAL clean strokes. If genuinely clean writing passes comfortably -> record "originals HELD". If real clean strokes false-fail -> record the specific observation and RE-AFFIRM the widened 0.12/0.16 WITH that device reason (the D-04 fallback), which becomes a small follow-up revert to 26-03's constants.
    4. Coaching quality (D-06): on baa, submit the SAME mistake twice and confirm the on-screen feedback line (bottom bar) is the AGENT's line and the two lines are NOT identical/verbatim-exemplar (non-static).
  </how-to-verify>
  <resume-signal>Type "coaching+scorer pass" with the D-04 verdict (originals held / widened re-affirmed + reason), or list issues.</resume-signal>
  <acceptance_criteria>
    - Recorded in 26-DEVICE-PASS.md: the alif->thaa walk incl. fail->retry outcome.
    - Recorded: the D-04 device verdict — originals 0.10/0.15 held, OR widened 0.12/0.16 re-affirmed WITH the observed real-stroke reason (which triggers a 26-03 constant re-affirm follow-up).
    - Recorded: the bottom-bar line is the agent's line and repeated same-mistake attempts are non-verbatim (D-06 live).
    - Server re-deploy state (deployed revision / owner authorization) recorded.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Two-profile isolation spot-check and the v1.x -> 2.x update-must-not-crash check</name>
  <read_first>
    - Project memory: Quick 260622-pal ("AppDatabase.forAccount(uid) opens a sha256(uid)-named DB per account — two parents on one tablet never share progress"); finalization-workspace (v2.0.0+3 in Play review, the prior v1.x build exists)
    - .planning/ROADMAP.md §Phase 26 SC4 (profile isolation + v1->v2 update opens without crashing)
  </read_first>
  <action>Human-on-device step: exercise two child profiles/accounts and confirm no progress bleed, then install a prior v1.x build, write local progress, and install the new build OVER it (in-place update) to confirm it opens without crashing and preserves progress — recording both outcomes in 26-DEVICE-PASS.md. Any bleed/crash is logged with an exact repro for a gap-closure plan.</action>
  <what-built>Account/profile data isolation and forward-compatibility with data written by the prior shipped version.</what-built>
  <how-to-verify>
    1. Profile isolation: create/use two DIFFERENT child profiles (and/or two parent accounts on one device). Advance one child's progress; confirm the other child's journey/mastery is UNCHANGED (no bleed) — a spot-check of the forAccount(uid) isolation.
    2. Update-must-not-crash: install a prior v1.x build (the course/webcourse build already in the field), let it write some local progress, THEN install the new build OVER it (an in-place update, not a fresh install). Confirm the app OPENS without crashing and prior local progress survives (the Drift migration path holds).
  </how-to-verify>
  <resume-signal>Type "isolation+update pass" with what you saw, or list any bleed/crash for a gap-closure fix.</resume-signal>
  <acceptance_criteria>
    - Recorded: two profiles/accounts stay isolated (one child's progress does not appear for the other).
    - Recorded: an in-place v1.x -> 2.x update opens without crashing and preserves local progress.
    - Any failure is logged with the exact repro for a gap-closure plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two accounts on one device | Verifies the forAccount(uid) DB isolation boundary holds under real use |
| prior-version data -> new binary | The Drift migration boundary; a bad migration would corrupt/crash on update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Information Disclosure | cross-profile progress bleed | mitigate | Task 3 spot-checks two-profile isolation on device; a bleed blocks release |
| T-26-06-02 | Denial of Service | update crash on old data | mitigate | Task 3 runs the real in-place v1.x->2.x update; a crash blocks release |
| T-26-06-03 | Tampering | grounding regression on device | mitigate | Task 2 confirms the scorer still owns pass/fail and the agent line never overturns the verdict (GROUND-01) |
| T-26-06-SC | Tampering | server re-deploy | accept | Owner-authorized Cloud Run deploy with explicit wording (tutor-server memory); no client package installs in this plan |
</threat_model>

<verification>
- Real (non-DEMO) build on a physical device passes entry+icon, alif->thaa incl. fail->retry, profile isolation, and the v1->v2 update.
- The D-04 device verdict is recorded (originals held / widened re-affirmed with reason).
- The D-06 coaching is agent-line + non-verbatim live (server re-deployed).
</verification>

<success_criteria>
The Android device pass is green on a real build: fresh-install + clean sign-out (SC1),
the Qalam launcher icon (SC2), the alif->thaa walk incl. fail->retry with an acceptable
scorer feel and agent-line non-verbatim coaching (SC3), and profile isolation + a
non-crashing v1->v2 update (SC4). (Phase 26 Success Criteria 1-4, device confirmation.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-06-SUMMARY.md` when done (with 26-DEVICE-PASS.md holding the recorded results).
</output>
