---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/auth_service.dart
  - lib/providers/auth_providers.dart
  - lib/router/app_router.dart
  - lib/screens/settings_screen.dart
  - lib/screens/parent_auth_screen.dart
  - test/router/sign_out_routing_test.dart
autonomous: true
requirements: [D-09b, D-09c, PLAT-01]

must_haves:
  truths:
    - "Signing out from Settings lands the app on /auth and STAYS there — no redirect loop, no stranded blank/anonymous state (D-01b)."
    - "Signing out from the parent account screen (parent_auth_screen) lands on /auth with the sign-in form usable to sign back in (D-01b)."
    - "The anonymous boot identity is still restored after sign-out for offline curriculum reads (D-09c) but never counts as signedIn (D-01a — children never log in)."
    - "A returning parent can sign in again after signing out and reach Home (or /onboarding if no profile) — the account front door still works both directions."
  artifacts:
    - path: "test/router/sign_out_routing_test.dart"
      provides: "Live-path router regression: sign-out ends at /auth with a usable form, never a loop/strand"
      min_lines: 40
    - path: "lib/services/auth_service.dart"
      provides: "signOut() that restores anonymous (D-09c) without stranding the router"
    - path: "lib/providers/auth_providers.dart"
      provides: "AuthGate that flips signedIn=false on the anonymous restore so the redirect fires to /auth"
  key_links:
    - from: "lib/screens/settings_screen.dart _signOut"
      to: "/auth"
      via: "AuthService.signOut -> AuthGate flip -> router redirect (or explicit context.go fallback)"
      pattern: "signOut|/auth"
    - from: "lib/router/app_router.dart redirect"
      to: "/auth"
      via: "if (!authGate.signedIn) return onAuth ? null : '/auth'"
      pattern: "authGate\\.signedIn"
---

<objective>
Fix the sign-out strand that triggered this phase: under the ratified account-first
model (D-01), signing out must route CLEANLY to `/auth` and stay there, never bounce
into a loop or a stranded state. The router is already account-first — this is a
sign-out ROUTING fix, NOT a rebuild (D-01: "ship exactly as-built" is not an option
because the strand is a bug even under the ratified model).

Purpose: SC1 — "router + sign-out implement exactly [the entry model]". Closes D-01b.
Preserves the non-negotiable child-safety core: children never log in (D-01a / D-09b)
and sign-up still links the boot anonymous identity (D-09c).
Output: a live-path router regression test proving the clean route, plus the minimal
routing fix across AuthService / AuthGate / the two sign-out call sites.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@lib/router/app_router.dart
@lib/services/auth_service.dart
@lib/providers/auth_providers.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the strand with a live-path router test, then fix sign-out routing</name>
  <files>test/router/sign_out_routing_test.dart, lib/services/auth_service.dart, lib/providers/auth_providers.dart, lib/router/app_router.dart</files>
  <read_first>
    - lib/router/app_router.dart (redirect lines 60-74: `if (!authGate.signedIn) return onAuth ? null : '/auth'`; refreshListenable merge at lines 52-56)
    - lib/services/auth_service.dart (signOut() lines 200-210: `_auth.signOut()` then `ensureSignedIn()` restores anonymous)
    - lib/providers/auth_providers.dart (AuthGate lines 28-56: `_signedIn = user != null && !user.isAnonymous`, flips + notifyListeners)
    - lib/main.dart line 8 comment (account-scoped `appDatabaseProvider` rebuilds on signed-in UID — confirm the rebuild does not leave the router mid-redirect)
    - .planning/phases/26-.../26-CONTEXT.md (D-01, D-01a, D-01b + "Established Patterns": synchronous redirect, two-rule loop guard)
  </read_first>
  <behavior>
    - Test: starting signed-in (non-anonymous) on `/settings`, invoking sign-out drives the router to `/auth` and the final matchedLocation is `/auth` (not `/settings`, not `/`, not `/onboarding`).
    - Test: after sign-out the AuthGate reports `signedIn == false` even though an anonymous identity was restored (D-09c restore did not re-strand).
    - Test: the redirect settles (no loop) — pumping to steady state leaves exactly one location, `/auth`, with the ParentAuthScreen sign-in form present.
    - Test: signing back in (non-anonymous) from `/auth` routes forward (to `/` when a profile exists, `/onboarding` when not) — the front door works both directions.
  </behavior>
  <action>
    Write test/router/sign_out_routing_test.dart as a LIVE-PATH test (drive the REAL
    appRouterProvider with a fake FirebaseAuth via firebase_auth_mocks, per the 06.1-01
    anon-boot test precedent) — NOT a hand-rolled router. Assert the four behaviors
    above by driving `AuthService.signOut()` through the real AuthGate/redirect, per the
    project memory "live-path widget tests mandatory" (Phase-15 dead-wire lesson).
    Then make the strand impossible: confirm AuthGate (auth_providers.dart:28-56) flips
    `_signedIn` to false when signOut restores the anonymous user, and that the router's
    merged refreshListenable re-runs the redirect to `/auth`. If the redirect alone does
    not relocate a screen OUTSIDE the router's own redirect (e.g. Settings, whose
    matchedLocation was `/settings` when the gate flipped), add an explicit
    post-signOut relocation in the fix path so the user always lands on `/auth`.
    Preserve BOTH router invariants (CONTEXT Established Patterns): the redirect stays
    SYNCHRONOUS (never await Drift) and keeps the two-rule loop guard. Keep
    AuthService.signOut()'s `ensureSignedIn()` anonymous restore intact (D-09c) — do NOT
    remove it; the fix is routing, not identity. Do NOT touch signUpWithEmail's
    anonymous-link path (D-09c) or the child-login ban (D-01a): no child-facing auth.
  </action>
  <acceptance_criteria>
    - `flutter test test/router/sign_out_routing_test.dart` passes; all four behaviors above are asserted against the real appRouter.
    - Source assertion: lib/services/auth_service.dart signOut() still calls `ensureSignedIn()` after `_auth.signOut()` (grep confirms the D-09c restore is intact).
    - Source assertion: lib/providers/auth_providers.dart AuthGate still computes `signedIn` as `currentUser != null && !currentUser.isAnonymous` (anonymous never counts — D-01a).
    - Source assertion: lib/router/app_router.dart redirect remains synchronous (no `await` inside the redirect closure) and retains both loop-guard rules.
    - No new child-facing login surface: `grep -rn "child.*[Ll]ogin\|childSignIn" lib/` returns nothing new.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/router/sign_out_routing_test.dart</automated>
  </verify>
  <done>Sign-out from a signed-in session deterministically ends at /auth with a usable form; the anonymous restore (D-09c) and child-login ban (D-01a) are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Make both sign-out call sites route consistently and prove the full suite is green</name>
  <files>lib/screens/settings_screen.dart, lib/screens/parent_auth_screen.dart</files>
  <read_first>
    - lib/screens/settings_screen.dart (_signOut lines 33-41 — awaits signOut(), no navigation)
    - lib/screens/parent_auth_screen.dart (_signOut lines 153-160 — awaits signOut(), no navigation; signed-in card at lines 198-205)
    - test/router/sign_out_routing_test.dart (the Task 1 regression — reuse its harness shape)
  </read_first>
  <action>
    Reconcile the two sign-out call sites with the Task 1 fix. In settings_screen.dart
    `_signOut` (33-41) and parent_auth_screen.dart `_signOut` (153-160), ensure that once
    `AuthService.signOut()` completes the user is on `/auth` — either by relying on the
    router redirect landed in Task 1, or (if the fix chose an explicit relocation) by
    adding a guarded `context.go('/auth')` after the await (`if (!mounted) return;`
    first). Do NOT double-navigate (a redirect PLUS an explicit go that fights it). Keep
    the `_submitting`/`_signingOut` busy-state handling. parent_auth_screen's signed-in
    card must return to its form after sign-out (its `authStateProvider` watch already
    recomputes `signedIn` — confirm it shows `_buildForm` post-sign-out). No behavior
    change for sign-IN paths.
  </action>
  <acceptance_criteria>
    - Both `_signOut` methods leave the app on `/auth` (asserted by extending test/router/sign_out_routing_test.dart to cover the parent_auth_screen call site, or a sibling test).
    - No double-navigation: at most one relocation mechanism per call site (redirect OR explicit go, not both fighting).
    - `flutter analyze` is clean (exit 0) for the two changed screens.
    - Full suite: `flutter test` shows no NEW failures vs the pre-existing known-failing baseline (alif_reference + goldens font-drift, per project memory).
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/router/ && flutter analyze lib/screens/settings_screen.dart lib/screens/parent_auth_screen.dart</automated>
  </verify>
  <done>Both sign-out surfaces route cleanly to /auth; no regressions in the wider suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device UI -> FirebaseAuth | Parent credentials + the session token cross here; sign-out must fully drop the permanent session |
| anonymous identity -> Firestore | The restored anonymous boot identity reads curriculum only (rules gate on `request.auth != null`); it must NOT be treated as a signed-in parent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Elevation of Privilege | AuthGate.signedIn after sign-out | mitigate | AuthGate computes `signedIn` as non-null AND non-anonymous; the anonymous restore keeps `signedIn == false` (asserted in Task 1) so a signed-out session cannot reach gated content |
| T-26-01-02 | Information Disclosure | account-scoped Drift DB on UID swap | mitigate | signOut restores anonymous -> appDatabaseProvider rebuilds to the anonymous UID DB; the signed-in parent's rows are not read by the anonymous identity (existing forAccount(uid) isolation, confirmed unbroken) |
| T-26-01-03 | Repudiation/Session | stale permanent session after sign-out | mitigate | GoogleSignIn.signOut() + FirebaseAuth.signOut() both fire before the anonymous restore (auth_service.dart:202-209); Task 1 asserts signedIn flips false |
| T-26-01-SC | Tampering | package installs | accept | No new package installs in this plan; supply-chain surface unchanged |
</threat_model>

<verification>
- The live-path router test lands sign-out at /auth for BOTH call sites.
- D-09c (anonymous restore) and D-01a (children never log in) source invariants intact.
- `flutter analyze` clean on the changed files; no new suite failures.
</verification>

<success_criteria>
Sign-out is deterministic and never strands: from any signed-in surface, `signOut()`
ends on `/auth` with a usable sign-in form, the anonymous boot identity is restored for
offline reads but never counts as signed in, and a parent can sign back in and route
forward. (Contributes to Phase 26 Success Criterion 1; device confirmation happens in
26-06.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-01-SUMMARY.md` when done.
</output>
