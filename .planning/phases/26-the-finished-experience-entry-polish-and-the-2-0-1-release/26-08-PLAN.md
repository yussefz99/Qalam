---
wave: 1
depends_on: []
autonomous: true
requirements: [CUR-01]
files_modified:
  - assets/curriculum/graphs/baa.json
  - assets/curriculum/curriculum_graph.json
---

<objective>
Restore baa's writing & tracing clean-reps from 1 back to 3 — the mother's Phase-25
verdict A1 (2026-07-20 live walkthrough). She signed baa at "writing/tracing = 3, lighter
= 1" on 2026-06-28; the demo build dropped everything to 1. This restores the 3 on the two
heavy skills ONLY; every lighter baa skill stays at 1. alif/taa/thaa are untouched (they
stay at 1 per her A2 verdict).

This plan makes the DATA edit only. Two owner-gated deploys are DEFERRED follow-ups, NOT in
this plan: (a) the Firestore re-seed that carries the change to a device (curriculum is
Firestore-first), and (b) the `qalam-tutor` Cloud Run redeploy so the tutor's baa copy
matches — batch that redeploy with Phase 26's coach-prompt fix (26-04) into ONE authorized
deploy.
</objective>

<threat_model>
No new attack surface. Curriculum-content data edit only; no auth, network, input-handling,
or child-data path changes. The values written (minCleanReps=3) are bounded integers already
used elsewhere in the schema.
</threat_model>

<must_haves>
- baa's WRITING and TRACING skill nodes have minCleanReps = 3 in BOTH assets/curriculum/graphs/baa.json AND assets/curriculum/curriculum_graph.json.
- Every OTHER baa skill node stays at minCleanReps = 1.
- alif, taa, thaa nodes are unchanged (still 1).
- graphs/baa.json and the baa subtree of curriculum_graph.json stay byte-parity-consistent (graph_asset_parity_test green).
- The seen-letters wall stays green: `python -m tools.content.validate --gate` exit 0; the learned-letters lint passes.
</must_haves>

<tasks>

<task id="1" type="execute">
<title>Bump baa writing/tracing minCleanReps 1 → 3 in both graph files</title>
<read_first>
- assets/curriculum/graphs/baa.json (the baa letter graph — identify which nodes are the writing and tracing skills by their `skill`/`type`/`id`; there are lighter skills that MUST stay at 1)
- assets/curriculum/curriculum_graph.json (the combined graph — the baa subtree must match graphs/baa.json byte-for-byte on the baa nodes, D-14 parity)
- test/curriculum/graph_asset_parity_test.dart (the byte-parity guard you must keep green)
- .planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-HUMAN-UAT.md (row A1 — the mother's verdict and its ingestion effect, the source of truth for this edit)
</read_first>
<action>
In BOTH assets/curriculum/graphs/baa.json and assets/curriculum/curriculum_graph.json, set minCleanReps from 1 to 3 on baa's writing-skill node and tracing-skill node ONLY (the "write the letter" and "trace the letter" nodes — the ones she signed at 3 on 2026-06-28). Leave every other baa node at 1, and leave all alif/taa/thaa nodes untouched. Keep the two files' baa subtrees identical (byte-parity per D-14) — the same nodes get the same value in both. Preserve all other formatting/indentation exactly; the diff must be only the changed minCleanReps integers.
If any existing test pins baa writing/tracing at reps=1 (grep the test/ tree for baa + minCleanReps / clean-rep assertions), update that expectation to 3 so it matches her signed spec — do not weaken the test, just correct the expected number.
</action>
<acceptance_criteria>
- `python3 -c "import json; d=json.load(open('assets/curriculum/graphs/baa.json')); print('ok')"` parses clean; same for curriculum_graph.json.
- A grep/inspection shows baa's writing + tracing nodes = minCleanReps 3 in both files, and all other baa nodes still = 1.
- `flutter test test/curriculum/graph_asset_parity_test.dart` passes (byte-parity held).
- `flutter test test/curriculum/learned_letters_lint_test.dart` passes.
- `python -m tools.content.validate --gate` exits 0.
- No alif/taa/thaa node changed (git diff shows only baa writing/tracing minCleanReps values, plus any corrected test expectation).
</acceptance_criteria>
<verify>
Run the three test/gate commands above; confirm all green and the git diff is limited to the baa writing/tracing reps (+ any corrected test expectation).
</verify>
<done>
Both graph files have baa writing/tracing at 3 reps, byte-parity + lint + gate green, diff scoped to baa. Commit: `feat(26-08): restore baa writing/tracing to 3 clean reps (mother A1)`.
</done>
</task>

</tasks>

<verification>
- baa writing/tracing = 3 in both files; other baa skills = 1; alif/taa/thaa unchanged.
- parity + lint + gate all green.
- SUMMARY notes the two DEFERRED owner-gated deploys (Firestore re-seed; qalam-tutor redeploy — batch with 26-04's coach-prompt fix).
</verification>
