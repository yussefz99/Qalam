---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/scoring/shape_match.dart
  - lib/core/scoring/tolerances.dart
  - test/core/scoring/tolerances_test.dart
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "The shape soft-band is back at the ORIGINAL tighter thresholds tcc=0.10 / tcw=0.15 (the 0.12/0.16 widening is undone) — D-04."
    - "The revert is safe: the painter-stretch bug that the widening worked around is fixed (commit 972427e), and the Dart calibration harness still separates good from named-bad at the tighter band."
    - "The decision is recorded in-source against the calibration data, including the on-device FALLBACK clause (re-affirm the widened values WITH a recorded reason only if real clean strokes false-fail on device) — D-04."
  artifacts:
    - path: "lib/core/scoring/shape_match.dart"
      provides: "SoftBand.shapeDefault reverted to tcc:0.10, tcw:0.15"
      contains: "tcc: 0.10"
    - path: "lib/core/scoring/tolerances.dart"
      provides: "Tolerances default shapeTcc=0.10, shapeTcw=0.15"
      contains: "shapeTcc = 0.10"
    - path: "test/core/scoring/calibration_harness_test.dart"
      provides: "Regression guard: FN==0 for good seeds, FP==0 for named-bad, F5 form-confusion zero passes at the reverted band"
  key_links:
    - from: "lib/core/scoring/tolerances.dart shapeTcc/shapeTcw"
      to: "lib/core/scoring/shape_match.dart SoftBand.shapeDefault"
      via: "single-source mirror (tolerances_test asserts equality)"
      pattern: "shapeDefault"
---

<objective>
Revert the shape soft-band thresholds tcc/tcw from the pre-demo widened values
(0.12/0.16) back to the ORIGINAL tighter values (0.10/0.15) per D-04. The widening
existed ONLY to work around the painter-stretch bug that is now fixed (commit 972427e),
so restoring the tighter feel is safe. The Dart calibration harness is the regression
guard; on-device feel is confirmed in the device pass (26-06), where the D-04 FALLBACK
lives (re-affirm the widened values WITH a recorded reason only if the originals
demonstrably false-fail real clean strokes on device).

Purpose: SC3 — "tcc/tcw re-tightened (or re-affirmed) with the decision recorded against
calibration data." Closes D-04's code half. Explicitly NOT gated on a mother-labelled
calibration set (deferred — she is the Phase-25 bottleneck).
Output: the two default constants reverted, the widening comments rewritten to record
the D-04 revert + reason + fallback clause, and the regression guards green at 0.10/0.15.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@lib/core/scoring/shape_match.dart
@lib/core/scoring/tolerances.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revert the two soft-band defaults to the original 0.10 / 0.15 and record the D-04 decision</name>
  <files>lib/core/scoring/shape_match.dart, lib/core/scoring/tolerances.dart</files>
  <read_first>
    - lib/core/scoring/shape_match.dart (SoftBand.shapeDefault line 75: `SoftBand(tcc: 0.12, tcw: 0.16)`; the WIDENED-2026-07-07 comment lines 64-74 documenting the original 0.10/0.15)
    - lib/core/scoring/tolerances.dart (default ctor lines 83-84: `this.shapeTcc = 0.12, this.shapeTcw = 0.16`; the PROVISIONAL/WIDENED comments lines 41-60)
    - lib/core/scoring/geometric_stroke_scorer.dart (line 105: `SoftBand(tcc: tolerances.shapeTcc, tcw: tolerances.shapeTcw)` — confirms the two defaults are the single source the scorer reads)
    - .planning/phases/26-.../26-CONTEXT.md (D-04 in full, incl. the on-device fallback clause and the rejected mother-labelled-set dependency)
  </read_first>
  <behavior>
    - `SoftBand.shapeDefault` returns tcc==0.10 and tcw==0.15 after the change.
    - A default `Tolerances()` (and `Tolerances.normal`) returns shapeTcc==0.10 and shapeTcw==0.15.
    - The single-source invariant holds: `Tolerances.normal.shapeTcc == SoftBand.shapeDefault.tcc` and likewise for tcw.
    - The `fromJson` override path is unchanged (a letters.json tolerance override still wins).
  </behavior>
  <action>
    In lib/core/scoring/shape_match.dart change `static const SoftBand shapeDefault =
    SoftBand(tcc: 0.12, tcw: 0.16);` (line 75) to `SoftBand(tcc: 0.10, tcw: 0.15)`. In
    lib/core/scoring/tolerances.dart change the default ctor args `this.shapeTcc = 0.12`
    and `this.shapeTcw = 0.16` (lines 83-84) to `0.10` and `0.15`. Rewrite the WIDENED
    comments (shape_match.dart 64-74; tolerances.dart 41-60) to a dated D-04 REVERT note:
    state that the 2026-07-07 widen to 0.12/0.16 was a work-around for the painter-stretch
    bug fixed in commit 972427e, that D-04 (2026-07-20, owner) reverts to the original
    0.10/0.15, that the Dart calibration harness is the regression guard, and record the
    FALLBACK: if the originals false-fail real clean strokes during the 26-06 device pass,
    re-affirm 0.12/0.16 WITH the observed-device reason logged here. Do NOT change the
    directionCc/directionCw defaults, `maxCurvature`, or the `fromJson` override parsing.
    Keep the F5 form-confusion separation note (the flat-bowl at d~0.1626 and the F5 trap
    at d~0.2838 both remain certainly-wrong at tcw=0.15 — a tighter tcw only strengthens
    the separation).
  </action>
  <acceptance_criteria>
    - `grep -n "tcc: 0.10, tcw: 0.15" lib/core/scoring/shape_match.dart` matches (shapeDefault reverted).
    - `grep -n "shapeTcc = 0.10" lib/core/scoring/tolerances.dart` and `grep -n "shapeTcw = 0.15" lib/core/scoring/tolerances.dart` both match.
    - The rewritten comments cite D-04, commit 972427e, and the on-device fallback clause (grep for "D-04" and "972427e" in both files, excluding is not needed — these are intentional citations).
    - No change to directionCc/directionCw/maxCurvature defaults (grep confirms `directionCc = 0.3`, `directionCw = -0.3` unchanged).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "tcc: 0.10, tcw: 0.15" lib/core/scoring/shape_match.dart && grep -q "shapeTcc = 0.10" lib/core/scoring/tolerances.dart && grep -q "shapeTcw = 0.15" lib/core/scoring/tolerances.dart</automated>
  </verify>
  <done>The two soft-band defaults are reverted to 0.10/0.15 and the D-04 decision (reason + fallback) is recorded in-source.</done>
</task>

<task type="auto">
  <name>Task 2: Update the pinned-value test and prove the calibration harness is green at 0.10 / 0.15</name>
  <files>test/core/scoring/tolerances_test.dart</files>
  <read_first>
    - test/core/scoring/tolerances_test.dart (lines 81-82 pin `shapeTcc == 0.12` / `shapeTcw == 0.16`; line 88-90 the single-source equality test; line 108 another 0.12 pin)
    - test/core/scoring/calibration_harness_test.dart (the confusion-table guard: FN==0 good, FP==0 named-bad, F5 form-confusion zero passes; the THRESHOLD-FIT report is PROVISIONAL and must not mutate production values)
    - test/core/scoring/calibration_fixtures/README.md (the good/named-bad synthetic seed contract; FN-over-FP tuning priority)
  </read_first>
  <action>
    Update tolerances_test.dart to pin the reverted defaults: change the `equals(0.12)` /
    `equals(0.16)` expectations (lines 81-82) to `equals(0.10)` / `equals(0.15)`, and the
    line-108 `equals(0.12)` to `equals(0.10)`. Leave the single-source equality test
    (89-90) and the fromJson-override test (93-99, which uses 0.08/0.20) untouched — the
    override path is unchanged. Then RUN the calibration harness as the regression guard.
    If any SYNTHETIC good seed now false-fails purely because the band tightened, that is
    NOT the D-04 device-fallback trigger (the fallback is about REAL clean strokes on
    device, 26-06) — surface it in the SUMMARY, but the good seeds were passing before the
    widen so they must pass at 0.10/0.15. Confirm the F5 form-confusion cell stays zero
    passes and every named-bad seed still rejects with its expected MistakeId.
  </action>
  <acceptance_criteria>
    - `flutter test test/core/scoring/tolerances_test.dart` passes with the reverted 0.10/0.15 pins.
    - `flutter test test/core/scoring/calibration_harness_test.dart` passes: FN==0 for good seeds, FP==0 for named-bad seeds, F5 form-confusion zero passes at the reverted band.
    - `flutter test test/core/scoring/` (the whole scoring suite) shows no new failures.
    - The THRESHOLD-FIT report remains PROVISIONAL (no production value mutated by the harness — source unchanged in calibration_harness_test.dart).
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/core/scoring/tolerances_test.dart test/core/scoring/calibration_harness_test.dart</automated>
  </verify>
  <done>The pinned-value test tracks the reverted defaults and the calibration harness proves clean good/bad separation at 0.10/0.15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child stroke -> on-device scorer | Purely local; the scorer never leaves the device and no network input reaches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Denial of Service (UX) | over-tight band false-fails real children | mitigate | The Dart calibration harness guards synthetic good seeds (FN==0); the device pass (26-06) is the real-child arbiter with the D-04 fallback (re-affirm widened WITH recorded reason) |
| T-26-03-SC | Tampering | package installs | accept | No package installs; scorer is local Dart. Supply-chain surface unchanged |
</threat_model>

<verification>
- shapeDefault and Tolerances defaults are 0.10/0.15.
- tolerances_test + calibration_harness_test green at the reverted band.
- The D-04 decision, reason, and fallback clause are recorded in-source.
</verification>

<success_criteria>
The scorer is re-tightened to the original 0.10/0.15 with the calibration harness proving
clean good-vs-bad separation, the decision recorded in-source with its device fallback.
(Phase 26 Success Criterion 3 — scorer half; on-device feel confirmed in 26-06.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-03-SUMMARY.md` when done.
</output>
