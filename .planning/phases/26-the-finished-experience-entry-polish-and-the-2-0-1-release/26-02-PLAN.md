---
phase: 26-the-finished-experience-entry-polish-and-the-2-0-1-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - flutter_launcher_icons.yaml
  - assets/branding/qalam_icon_foreground.png
  - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - android/app/src/main/res/mipmap-hdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-mdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
  - android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
  - android/app/src/main/res/values/colors.xml
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "The Android on-device launcher icon is the real Qalam brand mark (from logo.svg), not the stock Flutter default (D-05)."
    - "The Android icon is ADAPTIVE (foreground + parchment background), matching the Play listing art (D-05)."
    - "The iOS AppIcon.appiconset (which already ships the Qalam mark) is confirmed present and unbroken (no Flutter default on iOS either — SC2)."
  artifacts:
    - path: "android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      provides: "Adaptive-icon XML referencing foreground drawable + parchment background"
      contains: "adaptive-icon"
    - path: "flutter_launcher_icons.yaml"
      provides: "Icon-generation config (adaptive_icon_background parchment + foreground from brand art)"
    - path: "assets/branding/qalam_icon_foreground.png"
      provides: "Rendered brand foreground (from docs/design/kit logo.svg) at adaptive safe-zone scale"
  key_links:
    - from: "flutter_launcher_icons.yaml"
      to: "android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      via: "flutter pub run flutter_launcher_icons generation"
      pattern: "adaptive_icon"
---

<objective>
Replace the stock Flutter default Android on-device launcher icon (git-confirmed
untouched since the initial shell, commit e9fc86c — no adaptive-icon XML, no
flutter_launcher_icons config) with the REAL Qalam adaptive icon derived from the brand
art (`docs/design/kit/project/assets/logo.svg` + ICONOGRAPHY.md), a parchment background
matching the Play listing (D-05). The Play STORE mark and the iOS AppIcon.appiconset
already exist — the confirmed gap is the Android on-device launcher.

Purpose: SC2 — "The Qalam mark is the launcher icon on both platforms — no Flutter
default anywhere." Closes D-05.
Output: an adaptive Android launcher (foreground + parchment background across all
mipmap densities + anydpi-v26 XML), generated from brand art; iOS iconset confirmed.
Visual on-device confirmation is deferred to the device pass (26-06).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-CONTEXT.md
@docs/design/kit/project/assets/ICONOGRAPHY.md
@docs/design/kit/project/assets/logo.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the brand foreground + parchment background and write the icon-generation config</name>
  <files>assets/branding/qalam_icon_foreground.png, flutter_launcher_icons.yaml, android/app/src/main/res/values/colors.xml, pubspec.yaml</files>
  <read_first>
    - docs/design/kit/project/assets/logo.svg (the stacked Arabic+EN wordmark — قلم / QALAM)
    - docs/design/kit/project/assets/ICONOGRAPHY.md (brand stroke style; emoji/unicode banned; the reed-nib mark `assets/icons/qalam-nib.svg` is the small brand mark for sub-80px sizes)
    - docs/design/kit/colors_and_type.css (the parchment/ink token — the Play listing background colour to match)
    - pubspec.yaml (dev_dependencies block; no flutter_launcher_icons today; flutter_svg already present)
    - .planning/phases/26-.../26-CONTEXT.md (D-05: adaptive icon, parchment background to match the listing; tooling is planner/executor discretion)
  </read_first>
  <action>
    Discretion per D-05: use `flutter_launcher_icons` (pub.dev, publisher fluttercommunity/
    mrrhak — a well-established build-time dev tool; verify on pub.dev before adding).
    Add it under dev_dependencies in pubspec.yaml. Produce a square foreground PNG
    `assets/branding/qalam_icon_foreground.png` (1024x1024, transparent) rendered from
    the brand mark — prefer the reed-nib pictogram `docs/design/kit/project/assets/icons/qalam-nib.svg`
    or a centered لقم/QALAM wordmark from logo.svg — sized inside the adaptive-icon SAFE
    ZONE (keep the mark within the inner ~66% so Android's circular/rounded masks never
    clip it). Define the parchment background colour as a named colour in
    android/app/src/main/res/values/colors.xml (e.g. `ic_launcher_background`), value
    taken from the design-system parchment token so it MATCHES the Play listing art.
    Write flutter_launcher_icons.yaml with: android=true, ios=true (regenerate to keep
    the existing iconset in sync, or `remove_alpha_ios: true`), image_path pointing at the
    foreground PNG, adaptive_icon_background referencing the parchment colour, and
    adaptive_icon_foreground pointing at the foreground PNG. Do NOT invent a new brand
    colour — cite the design-system token. Emoji/unicode pseudo-icons are forbidden
    (ICONOGRAPHY.md) — the mark is the reed-pen/wordmark art only.
  </action>
  <acceptance_criteria>
    - `flutter_launcher_icons` appears under dev_dependencies in pubspec.yaml at a pinned/caret version.
    - assets/branding/qalam_icon_foreground.png exists, is 1024x1024, and is non-empty (`file` / image-size check).
    - flutter_launcher_icons.yaml exists with `adaptive_icon_background`, `adaptive_icon_foreground`, and `image_path` keys set (grep confirms all three).
    - android/app/src/main/res/values/colors.xml defines the parchment `ic_launcher_background` colour with the design-system hex.
    - Source assertion: the foreground is derived from brand art, not a Flutter logo — the config's image_path is under assets/branding/, not a default asset.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "flutter_launcher_icons" pubspec.yaml && grep -q "adaptive_icon_foreground" flutter_launcher_icons.yaml && grep -q "ic_launcher_background" android/app/src/main/res/values/colors.xml && test -s assets/branding/qalam_icon_foreground.png</automated>
  </verify>
  <done>The icon source art, parchment background colour, and generation config exist and reference the brand mark.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the adaptive Android launcher, replace the Flutter default, confirm iOS iconset</name>
  <files>android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml, android/app/src/main/res/mipmap-hdpi/ic_launcher.png, android/app/src/main/res/mipmap-mdpi/ic_launcher.png, android/app/src/main/res/mipmap-xhdpi/ic_launcher.png, android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png, android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png</files>
  <read_first>
    - flutter_launcher_icons.yaml (the config from Task 1)
    - android/app/src/main/res/mipmap-hdpi/ic_launcher.png (the stock Flutter default being replaced)
    - ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json (the existing iOS iconset — confirm it already carries the Qalam mark, D-05)
  </read_first>
  <action>
    Run the generator (`dart run flutter_launcher_icons` or `flutter pub run
    flutter_launcher_icons`). It must WRITE: a new mipmap-anydpi-v26/ic_launcher.xml
    (`<adaptive-icon>` with `<background>`=parchment colour and `<foreground>`=the brand
    drawable), the per-density foreground/background drawables, and overwrite the stock
    ic_launcher.png in every mipmap-*dpi bucket. Verify AndroidManifest's
    `android:icon="@mipmap/ic_launcher"` still points at the generated icon (it already
    does by convention — confirm, do not rename). For iOS, the generator refreshes
    AppIcon.appiconset from the same source; confirm the 1024 marketing icon and the app
    icon are the Qalam mark (not the Flutter default) and Contents.json is intact.
    Do NOT hand-edit the generated PNGs. The mascot must NOT be used as the icon
    (ICONOGRAPHY.md: he is illustration, not iconography, and not for sub-80px sizes) —
    the icon is the nib pictogram / wordmark.
  </action>
  <acceptance_criteria>
    - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml exists and contains `<adaptive-icon>` with a `<background>` and `<foreground>` (grep confirms the tag).
    - All five mipmap-*dpi/ic_launcher.png differ from the committed stock default (`git diff --stat` shows them modified) — the Flutter default is gone.
    - `flutter build apk --debug` exits 0 (the generated resources compile / link).
    - Source assertion: `grep -c "adaptive-icon" android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` >= 1.
    - iOS: ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json still lists the full icon set and the 1024 icon is present (no Flutter default) — recorded in the SUMMARY.
    - Human-check note (verified on device in 26-06): the launcher tile shows the Qalam mark on parchment.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "adaptive-icon" android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml && flutter build apk --debug</automated>
    <human-check>On-device (deferred to 26-06): the Android home-screen launcher tile is the Qalam mark on the parchment background, no blue Flutter default anywhere.</human-check>
  </verify>
  <done>The adaptive Qalam launcher icon replaces the Flutter default across all Android densities; the debug APK builds; iOS iconset confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build assets -> APK | Icon drawables are compiled into the app; no runtime input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-SC | Tampering | flutter_launcher_icons dev-dependency | mitigate | Dev-only build tool (never shipped in the app binary); verify legitimacy on pub.dev (publisher fluttercommunity/mrrhak, millions of installs) before adding; pin the version |
| T-26-02-01 | Spoofing | brand mark | accept | Public brand art only; no sensitive data in an app icon |
</threat_model>

<verification>
- Adaptive-icon XML + regenerated mipmaps replace the stock Flutter default.
- Debug APK builds with the new resources.
- iOS iconset confirmed present with the Qalam mark.
- On-device visual confirmation folds into 26-06.
</verification>

<success_criteria>
The Qalam brand mark is the Android on-device launcher icon (adaptive, parchment
background matching the listing), the Flutter default is fully removed, and the iOS
iconset is confirmed. (Phase 26 Success Criterion 2; on-device visual confirm in 26-06.)
</success_criteria>

<output>
Create `.planning/phases/26-the-finished-experience-entry-polish-and-the-2-0-1-release/26-02-SUMMARY.md` when done.
</output>
