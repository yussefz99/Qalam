---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 10
type: execute
wave: 6
depends_on: [18-07]
files_modified:
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - lib/features/letter_unit/widgets/teacher_margin_panel.dart
  - lib/features/letter_unit/widgets/spotlight_overlay.dart
  - lib/features/letter_unit/widgets/write_surface.dart
  - test/features/letter_unit/teacher_margin_test.dart
  - test/features/letter_unit/spotlight_overlay_test.dart
autonomous: true
requirements: [SPEC-18-R1, SPEC-18-R3, SPEC-18-R4]
must_haves:
  truths:
    - "A child-facing Teacher's Margin panel narrates the remediation arc alongside the canvas and carries the WHY line (D-01), reusing the 17.2 TutorInsight publish mechanism + the AuthoredFallback degradation axis (LLM online, template offline)."
    - "During a micro-drill the Spotlight overlay lights the target criterion's zone and dims the rest; the child still WRITES on the existing WriteSurface/scorer path (D-05) — no new interaction paradigm."
    - "No new reward surface is added — no counter, streak, badge, or points (anti-gamification); the panel reads like a teacher's pencil notes."
  artifacts:
    - path: "lib/features/letter_unit/widgets/teacher_margin_panel.dart"
      provides: "child-facing arc narration + WHY line panel (D-01)"
      contains: "TeacherMargin"
    - path: "lib/features/letter_unit/widgets/spotlight_overlay.dart"
      provides: "criterion-zone spotlight/dim over the WriteSurface (D-05)"
      contains: "Spotlight"
  key_links:
    - from: "teacher_margin_panel.dart"
      to: "TutorInsight (criterion + pick + rationale)"
      via: "the existing insight-publish plumbing in exercise_scaffold.dart"
      pattern: "TutorInsight|tutorInsight"
    - from: "spotlight_overlay.dart"
      to: "microDrill.spotlightZone"
      via: "the drill's authored lit-region string"
      pattern: "spotlightZone"
---

<objective>
Make the intelligence VISIBLE and warm: the Teacher's Margin panel (D-01, Variant C) narrates the
remediation arc beside the canvas and carries the WHY line in the tutor's voice, and the Spotlight
overlay (D-05, Variant B) lights the failing criterion's zone during a micro-drill while the child
keeps writing on the same canvas. Both reuse shipped plumbing — the 17.2 TutorInsight publish
mechanism + the AuthoredFallback degradation axis — and add NO reward surface (anti-gamification).

Purpose: the arc + drill FEEL like a patient teacher's guidance, not a mechanical rule.
Output: teacher_margin_panel.dart + spotlight_overlay.dart + scaffold/write-surface wiring + widget tests.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
@.planning/sketches/001-remediation-arc/index.html
@.planning/sketches/002-just-this-part/index.html
@docs/design/kit/colors_and_type.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teacher's Margin panel — arc narration + WHY line (D-01)</name>
  <files>lib/features/letter_unit/widgets/teacher_margin_panel.dart, lib/features/letter_unit/widgets/exercise_scaffold.dart, test/features/letter_unit/teacher_margin_test.dart</files>
  <behavior>
    - the panel renders the WHY line naming the targeted criterion (from TutorInsight), in the tutor's warm register
    - during an arc it shows the named step-down ("let's practice just the dot for a moment — then we'll come back") — the D-03 register, provisional copy signed:false
    - offline (no coach line) it shows the authored-template WHY line (D-10) — same panel, degraded source
    - NO counter / streak / badge / points appears in the panel (anti-gamification)
  </behavior>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the _teacherEye() strip lines 379-456; the TutorInsight model + tutorInsightProvider lines 80-86; the insight-publish mechanism lines 262-267,311-322; the AuthoredFallbackBrain degradation lines 295-327,465-472)
    - .planning/sketches/001-remediation-arc/index.html (Variant C "The Teacher's Margin" — the target layout)
    - docs/design/kit/colors_and_type.css (parchment/ink tokens — reuse, no new palette)
    - lib/curriculum/arc_state.dart (ArcStep — the arc phase the panel narrates)
  </read_first>
  <action>
    Author `TeacherMarginPanel` (child-facing, unlike the 17.2 demo-only Teacher's Eye strip): a
    parchment-margin panel beside the canvas that renders the WHY line + arc narration from the
    existing `TutorInsight` (criterion + pick + rationale, already published at verdict/coach time).
    Source the line the SAME way coaching degrades — the coach LLM line online, the authored template
    offline (D-10, reuse AuthoredFallback). During an arc, narrate the named step-down/rebuild/retry
    (D-03 register — warm, names the move, never fake cheer; provisional copy signed:false). Wire it
    into exercise_scaffold.dart beside the canvas (reuse the _teacherEye publish plumbing; do not add a
    second insight source). STRICTLY anti-gamification: no counter/streak/badge/points/"+N" — reuse
    the parchment/ink tokens only. Add teacher_margin_test.dart asserting the WHY line + criterion name
    render and that no forbidden reward token ("streak"/"points"/"+"/"badge") appears.
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/teacher_margin_test.dart</automated>
  </verify>
  <done>The Teacher's Margin panel renders the WHY line + arc narration from TutorInsight (online LLM / offline template), in the warm register, beside the canvas, with zero reward surface; the widget test passes.</done>
  <acceptance_criteria>
    - `flutter test test/features/letter_unit/teacher_margin_test.dart` passes.
    - The panel reads the line from `TutorInsight` (grep) — no new insight/publish source added.
    - The test asserts NO "streak"/"points"/"badge"/"+N" token renders (anti-gamification guard).
    - Provisional arc copy is documented signed:false (mother-signed at 18-11).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spotlight overlay — light the criterion zone during a micro-drill (D-05)</name>
  <files>lib/features/letter_unit/widgets/spotlight_overlay.dart, lib/features/letter_unit/widgets/write_surface.dart, test/features/letter_unit/spotlight_overlay_test.dart</files>
  <behavior>
    - during a type==microDrill exercise, the full letter stays visible; the target criterion's zone (from spotlightZone) is lit and everything else dims
    - the child still WRITES on the existing WriteSurface/StrokeCanvas/scorer path — the overlay is presentational only (no new interaction paradigm)
    - a non-drill exercise renders with NO spotlight (the overlay is inert)
  </behavior>
  <read_first>
    - lib/features/letter_unit/widgets/write_surface.dart (the existing WriteSurface wrapping StrokeCanvas — where the presentational overlay layers without changing capture/scoring)
    - .planning/sketches/002-just-this-part/index.html (Variant B "Spotlight" — the lit-zone/dim target)
    - assets/curriculum/exercises.json (the microDrill spotlightZone strings from 18-02: dot/bowl/start)
    - lib/features/letter_unit/exercise_spec_adapter.dart (how an exercise's type/fields reach the surface)
  </read_first>
  <action>
    Author `SpotlightOverlay` — a presentational Stack layer over the WriteSurface that, for a
    `type == "microDrill"` exercise, lights the `spotlightZone` region (dot / bowl / start) and dims the
    rest of the letter, keeping the whole letter visible (D-05). The child's stroke capture + scoring
    path is UNCHANGED — the overlay adds no gesture handling, no capture change (the existing
    StrokeCanvas still owns pointer events; D-05 "the child still writes"). Layer it in write_surface.dart
    ONLY when the exercise is a micro-drill (inert otherwise). Map the spotlightZone string → a region
    of the letter reference (reuse the reference-path geometry the trace guide already draws). Add
    spotlight_overlay_test.dart asserting: a drill exercise renders the overlay with the correct lit
    zone; a non-drill exercise renders no overlay; stroke capture is not intercepted.
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/spotlight_overlay_test.dart test/features/ 2>&1 | tail -6</automated>
  </verify>
  <done>The Spotlight overlay lights the target criterion's zone during a micro-drill and dims the rest, over the unchanged WriteSurface/scorer path; a non-drill renders no overlay; the widget test passes and the letter-unit suite has no regression.</done>
  <acceptance_criteria>
    - `flutter test test/features/letter_unit/spotlight_overlay_test.dart` passes.
    - The overlay renders only when the exercise `type == "microDrill"` and reads `spotlightZone` (grep).
    - The overlay adds no GestureDetector/pointer handling — stroke capture stays with StrokeCanvas (the test asserts capture is not intercepted).
    - `flutter test test/features/` shows no regression in the write/trace flow.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach line → child-facing panel | the line is words only; it must never present a verdict/mastery claim (ADR-014) or a reward surface |
| overlay → capture path | the spotlight is presentational; it must not touch stroke capture/scoring (ADR-017) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10-01 | Spoofing (UX) | reward-surface creep | mitigate | the panel/overlay add no counter/streak/badge/points; a test asserts no reward token renders (CLAUDE.md anti-gamification) |
| T-18-10-02 | Tampering (verdict) | overlay alters scoring | mitigate | the Spotlight is presentational only; stroke capture/scoring stays in StrokeCanvas (the test asserts capture is not intercepted) |
| T-18-10-03 | Spoofing (pedagogy) | unsigned arc/step-down copy | mitigate | provisional framing copy ships signed:false; the mother signs it at 18-11 |
</threat_model>

<verification>
- `flutter test test/features/letter_unit/teacher_margin_test.dart test/features/letter_unit/spotlight_overlay_test.dart test/features/` — green, no regression.
- Anti-gamification token guard green in both new widget tests.
- Visual fidelity to sketches 001/002 is confirmed at the 18-11 end-of-phase device UAT (human-check).
</verification>

<success_criteria>
The arc and the micro-drill are visible and warm — the Teacher's Margin narrates the WHY, the
Spotlight isolates the failing criterion — with no reward surface and no change to the write/score
path.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-10-SUMMARY.md` when done.
</output>
