---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 16
type: execute
wave: 3
depends_on: [18-12, 18-15]
files_modified:
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - lib/features/letter_unit/widgets/teacher_margin_panel.dart
  - lib/features/letter_unit/letter_unit_controller.dart
autonomous: true
gap_closure: true
requirements: [UAT-18-T6, UAT-18-T5, SPEC-18-R1]

must_haves:
  truths:
    - "A single, distinct Teacher's Margin note sits beside the writing canvas — not buried among duplicate feedback strips — so the child/owner can recognize it as a feature."
    - "During a remediation arc step-down (micro-drills parked), the margin narrates the step-down ('let's practice just the bowl for a moment — then we'll come back'), driven by the real arc state, not dead micro-drill-only code."
    - "The margin's WHY line varies per attempt: it shows the coach's real rationale when present, and on a clean pass shows a pass-appropriate line — not the same static 'deeper bowl' fallback every time."
    - "No points, streaks, badges, '+N', or score language appears anywhere on the margin or the exercise screen."
  artifacts:
    - path: "lib/features/letter_unit/widgets/teacher_margin_panel.dart"
      provides: "arc narration driven by insight.arcStep (not pick.microDrill); rationale-first WHY with a pass-appropriate line replacing the criterion-skewed static fallback"
      contains: "arcStep"
    - path: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      provides: "TutorInsight carries arcStep/whyFacts published at verdict; the panel sits beside the canvas; the demo Teacher's Eye strip is gated out of non-demo builds"
      contains: "arcStep"
    - path: "lib/features/letter_unit/letter_unit_controller.dart"
      provides: "an accessor exposing the current moment's policy arcStep + whyFacts (from the cached _pendingNarrow) so the scaffold can thread them into TutorInsight"
      contains: "arcStep"
  key_links:
    - from: "lib/features/letter_unit/letter_unit_controller.dart"
      to: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      via: "pending policy arcStep/whyFacts exposed and read at verdict time into TutorInsight"
      pattern: "arcStep|whyFacts"
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      to: "lib/features/letter_unit/widgets/teacher_margin_panel.dart"
      via: "TutorInsight.arcStep drives the step-down narration; TutorInsight.rationale drives the WHY"
      pattern: "TutorInsight\\("
---

<objective>
Close the Teacher's-Margin half of the blocker (UAT test 6 "I never understood it at all") and the client presentation half of the static-feedback gap (UAT test 5). The debug proved the panel DID render (the owner quoted its `_authoredWhy('shape')` string verbatim), but it has no perceivable identity: it pops in simultaneously with 4 other feedback surfaces, is buried in the 258px tutor column visually identical to the demo "Teacher's Eye" strip right below it, duplicates that strip's content, and its ONE signature behavior — the arc step-down narration — is dead code (keyed on `pick.contains('microDrill')`, but micro-drills are parked out of the live graph, and the policy's `arcStep`/`whyFacts` are never threaded into `TutorInsight`). Separately, on a clean pass `insight.rationale` is null (structurally, until 18-14 lands + deploys) so `_targetedCriterion` skews to 'shape' and the same static "deeper bowl" line fires every time.

Purpose: Make the Teacher's Margin a recognizable, singular, warm note beside the canvas that actually narrates the remediation arc and varies its WHY per attempt — the pedagogical surface the living tutor was built to show.

Output: The panel is relocated beside the writing canvas with a distinct persistent presence; the duplicate demo Teacher's Eye strip is gated out of non-demo builds; `TutorInsight` carries the real `arcStep`/`whyFacts` (via a new controller accessor) so the step-down narration fires without micro-drills; and the WHY prefers the coach `rationale` (now populated by 18-14) and shows a pass-appropriate line instead of the criterion-skewed static fallback on a clean pass.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/app-stuck-and-teacher-margin-not-understood.md
@.planning/debug/coach-feedback-feels-static.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-10-SUMMARY.md
@lib/features/letter_unit/widgets/teacher_margin_panel.dart
@lib/features/letter_unit/widgets/exercise_scaffold.dart
@lib/features/letter_unit/letter_unit_controller.dart
@lib/tutor/exercise_selector_provider.dart
@lib/curriculum/selection_policy.dart
@test/features/letter_unit/teacher_margin_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread the real arcStep/whyFacts into TutorInsight and fire arc narration from it</name>
  <files>lib/features/letter_unit/letter_unit_controller.dart, lib/features/letter_unit/widgets/exercise_scaffold.dart, lib/features/letter_unit/widgets/teacher_margin_panel.dart, test/features/letter_unit/teacher_margin_test.dart</files>
  <behavior>
    Write/extend teacher_margin_test.dart FIRST (RED), then implement to green:
    - When the current feedback moment's policy outcome has arcStep == 'entry'/'stepDown' (a step-down into a floor-trace, micro-drills parked), ASSERT the panel renders the step-down narration ("let's practice just <part> for a moment — then we'll come back") naming the targeted part — WITHOUT any pick containing 'microDrill'. (Today this line is unreachable because it keys on pick.contains('microDrill').)
    - When arcStep is null (no arc), ASSERT no step-down line renders.
  </behavior>
  <action>
    In letter_unit_controller.dart, add a small read-only accessor exposing the CURRENT moment's policy signal from the already-cached `_pendingNarrow` (set in `beginSelection`, before `selectNext` consumes it): e.g. `String? pendingArcStep() => _pendingNarrow?.arcStep;` and `List<String> pendingWhyFacts() => _pendingNarrow?.whyFacts ?? const [];`. `PolicyOutcome` already exposes `arcStep` (a convenience view of `nextArc?.step`) and `whyFacts` (verified in selection_policy.dart) — do not recompute the policy, just expose the cached outcome.

    In exercise_scaffold.dart, add `arcStep` (String?) and `whyFacts` (List<String>?) fields to the `TutorInsight` class and its `TutorInsightNotifier` usage. In `_onResult`, right AFTER `controller.beginSelection(...)` returns (so `_pendingNarrow` is populated) and while publishing the verdict-time insight (the `tutorInsightProvider.set(TutorInsight(criteria:…, diffSummary:…))` at ~line 320-323), also pass `arcStep: controller.pendingArcStep()` and `whyFacts: controller.pendingWhyFacts()`. When the coach rationale merges later (~line 402-410), preserve the arcStep/whyFacts already on the insight (carry them through the merge, like criteria/diffSummary are carried).

    In teacher_margin_panel.dart, REPLACE `_arcStepDownLine(insight.pick)` (which requires `pick.contains('microDrill')` — dead in the parked-drill graph) with a version driven by `insight.arcStep`: fire the named step-down when arcStep is 'entry' or 'stepDown', phrasing it from the targeted criterion. Reuse the existing `authoredWhyLine(whyFacts)` from exercise_selector_provider.dart (it already turns `criterion:*`/`arcStep:*` whyFacts into a warm, criterion-naming line) OR build the "let's practice just <friendlyCriterion> for a moment — then we'll come back" line from the targeted criterion — keep the D-03 register (a detour that comes right back, never a step BACK, never framed as failure). Keep the copy as named provisional strings (mother-signs at a later gate), not scattered literals.
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/teacher_margin_test.dart</automated>
  </verify>
  <done>The margin narrates the arc step-down from the real arcStep signal (no micro-drill required); TutorInsight carries arcStep/whyFacts through both publish points; the controller exposes the cached policy signal. Test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Give the margin a distinct, singular presence beside the canvas</name>
  <files>lib/features/letter_unit/widgets/exercise_scaffold.dart, lib/features/letter_unit/widgets/teacher_margin_panel.dart</files>
  <action>
    Make the Teacher's Margin recognizable as a distinct feature, addressing the four compounding causes in the debug:
    1. Relocate it from the 258px left tutor column to BESIDE the writing canvas (the main column, adjacent to the center WriteSurface) so it matches the UAT truth "a note near the writing canvas." Adjust `build()`/`_mainColumn` in exercise_scaffold.dart accordingly; keep it agent-path-only (baa).
    2. De-duplicate: gate the demo "Teacher's Eye" strip (`_teacherEye()`, `if (_isAgentPath) _teacherEye()`) so it renders ONLY in demo builds. Reuse the project's existing demo flag if one is defined in lib/ (grep for a `bool.fromEnvironment('DEMO')` constant); otherwise define `const bool kDemoMode = bool.fromEnvironment('DEMO');` in a shared spot and gate the strip behind it. Real UAT builds omit `--dart-define=DEMO=true`, so the strip disappears from the child-facing build, leaving the Teacher's Margin as the SINGLE margin surface (removes the duplicate content + the visual twin directly beneath it).
    3. Give the panel a distinct persistent presence rather than appearing only in the verdict blast alongside 4 other surfaces: pass the current `Exercise` (or its targeted focus) into TeacherMarginPanel so it can show a calm resting note naming the current question's focus BEFORE the first verdict (e.g. "Working on · the bowl"), then enrich with the WHY/arc line at verdict. If a resting focus is not cleanly derivable for a given exercise, keep the verdict-time appearance but ensure its heading + coral margin edge make it visually distinct from any neighbor. Keep it strictly anti-gamification (no counter/streak/badge/points).
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/exercise_scaffold_test.dart test/features/letter_unit/teacher_margin_test.dart</automated>
  </verify>
  <done>The Teacher's Margin renders beside the canvas as the single margin surface in a non-demo build (the demo Teacher's Eye strip is gated out); it has a distinct, recognizable presence. Tests pass; a grep confirms no reward/score language on the panel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vary the WHY per attempt and stop the static 'deeper bowl' skew</name>
  <files>lib/features/letter_unit/widgets/teacher_margin_panel.dart, test/features/letter_unit/teacher_margin_test.dart</files>
  <behavior>
    Extend teacher_margin_test.dart FIRST (RED), then implement to green:
    - When insight.rationale is present (non-empty), ASSERT the panel shows the rationale verbatim (the coach's per-attempt WHY, now populated on the clean-pass path by 18-14).
    - On a CLEAN PASS with no rationale and all criteria NOT failing (a genuinely good attempt), ASSERT the panel does NOT show the criterion-skewed static "Your baa wants a deeper bowl…" line — it shows a warm pass-appropriate line instead (or names what the child did well), so the WHY is not identical after every correct attempt.
    - On a genuine FAIL of a specific criterion with no rationale, ASSERT the authored floor line for THAT failed criterion still shows (the offline floor is preserved).
  </behavior>
  <action>
    In teacher_margin_panel.dart, adjust the WHY resolution so the static `_authoredWhy` table is a genuine last-resort offline FLOOR, not the majority-path output:
    - Prefer `insight.rationale` when present (unchanged), which 18-14 now populates on the clean-pass path via the schema-backed tool args.
    - Fix `_targetedCriterion` skew: on a PASS, do NOT treat a merely-not-certainlyCorrect soft-band criterion (e.g. 'shape' under the provisional uncalibrated bands) as "the part being worked on." Only surface a targeted criterion when the attempt actually FAILED it (or the scorer marked it wrong). On a clean pass with no failed criterion, show a warm pass-appropriate line (a named provisional string) rather than routing to the fixed 'shape' fallback — this is the exact fix for the owner's "feels static, always the deeper-bowl line" report.
    - Keep the per-failed-criterion `_authoredWhy` entries as the offline floor for genuine misses (they are correct there); keep them as named provisional strings for the mother's later sign-off.
    Do not reach into the server or the coach line channel (that is a separate surface); this task only changes what THIS panel selects for its WHY line.
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/teacher_margin_test.dart</automated>
  </verify>
  <done>The margin shows the coach rationale when present, a pass-appropriate line on a clean pass (not the static 'deeper bowl'), and the correct authored floor on a genuine criterion fail. Tests pass; `flutter test test/features/letter_unit/` shows no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy → UI | arcStep/whyFacts are pure on-device policy signals (non-PII, no verdict claim); the panel only narrates them. |
| demo chrome → child build | The demo Teacher's Eye strip is gated to demo builds so child-facing builds show only the warm margin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16-01 | Information disclosure | margin WHY / arc line | mitigate | Reads only non-PII policy whyFacts + the coach rationale (already grounded, no verdict/mastery/star claim, ADR-014). No reward/score surface (anti-gamification grep guard). |
| T-18-16-02 | Tampering | pub/pip installs | accept | No new dependencies. |
</threat_model>

<verification>
- `flutter test test/features/letter_unit/` passes (teacher_margin_test.dart + exercise_scaffold_test.dart + existing suite).
- A grep confirms no points/streaks/badges/"+N"/score language on the margin or exercise screen.
- `flutter analyze` exits 0.
</verification>

<success_criteria>
- One distinct, recognizable Teacher's Margin beside the canvas (demo Teacher's Eye gated out of non-demo builds) — UAT T6 margin half closed.
- Arc step-down narration fires from the real arc state (micro-drills parked).
- WHY varies per attempt; clean passes no longer show the static 'deeper bowl' line — UAT T5 client half closed.
- No gamification language anywhere on screen.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-16-SUMMARY.md` when done.
</output>
