---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 08
type: execute
wave: 4
depends_on: [18-02, 18-05]
files_modified:
  - server/app/nodes/plan.py
  - server/tests/test_eval/run_eval.py
  - server/tests/test_eval/selection_gold_set.jsonl
  - server/tests/test_eval/test_selection_dimension.py
autonomous: true
requirements: [SPEC-18-R1, SPEC-18-R9]
must_haves:
  truths:
    - "The coach node picks among the policy-narrowed legalNextExerciseIds and grounds its WHY line (Plan.rationale) in weakestCriterion + whether the pick is a micro-drill (D-10, online justification)."
    - "The picks stay railed by the unchanged G4/G5/G6 guards — an id outside legalNextExerciseIds is stripped, never forwarded."
    - "run_eval.py gains a `selection_policy` dimension ('would a teacher make this pick?') scored by the Vertex judge over a mother-signed gold scenario set."
    - "The selection gold set covers fail-streak, returning-child, and boredom-trap scenarios and ships signed:false until the mother signs the threshold + scenarios (Req 9)."
  artifacts:
    - path: "server/app/nodes/plan.py"
      provides: "coach picks among candidates + grounds the WHY rationale"
      contains: "rationale"
    - path: "server/tests/test_eval/run_eval.py"
      provides: "selection_policy dimension in DIMENSIONS + judge wiring"
      contains: "selection_policy"
    - path: "server/tests/test_eval/selection_gold_set.jsonl"
      provides: "signed:false fail-streak / returning-child / boredom-trap scenarios"
      contains: "boredom"
  key_links:
    - from: "plan.py coach pick"
      to: "facts.legalNextExerciseIds"
      via: "rail any proposal against the policy-narrowed candidates (unchanged G4/G5/G6)"
      pattern: "legalNextExerciseIds"
    - from: "run_eval.py DIMENSIONS"
      to: "selection_gold_set.jsonl"
      via: "judge-gated selection dimension scored over the gold scenarios"
      pattern: "selection_policy"
---

<objective>
Complete the server selection intelligence: the coach node grounds its WHY line (D-10 online
justification) in the policy facts already on the wire (weakestCriterion + the micro-drill-ness of
its pick), still railed by the unchanged G4/G5/G6 guards; and the 16-03 eval harness gains a
`selection_policy` dimension ("would a teacher make this pick?") scored over a mother-signed gold
scenario set (Req 9). The judged dimension complements the deterministic rails property tests (R5,
18-07).

Purpose: the tutor line SAYS WHY it picked (R1), and the pick quality is measurable and gated.
Output: plan.py WHY grounding, the 5th eval dimension + signed:false gold set + its test.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-RESEARCH.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coach picks among candidates + grounds the WHY line (D-10)</name>
  <files>server/app/nodes/plan.py</files>
  <behavior>
    - the coach's next_exercise_id is chosen from facts.legalNextExerciseIds (the policy-narrowed set); an id outside it is stripped (unchanged G4/G5/G6)
    - Plan.rationale names the targeted criterion (weakestCriterion) and, when the pick is a micro-drill, frames it as a warm named step-down ("let's practice just the dot for a moment")
    - no verdict/mastery claim ever appears in the rationale (ADR-014 — agent owns words + sequence, never pass/fail)
  </behavior>
  <read_first>
    - server/app/nodes/plan.py (the G4/G5/G6 guards lines 92-139; the fail-closed guard lines 93-100; Plan.rationale field lines 62-65)
    - server/app/schema.py (facts.legalNextExerciseIds lines 195-199; weakestCriterion lines 175-178 — the WHY inputs)
    - server/app/curriculum.py (tier_of/reachable_tiers/prerequisites_met — the rail the pick honors)
  </read_first>
  <action>
    Extend the coach/plan node so its `next_exercise_id` is selected from `facts.legalNextExerciseIds`
    (the policy-narrowed candidates — 18-07 sets this on the wire) and its `Plan.rationale` (the WHY
    line, D-10) is grounded in the policy facts already present: it NAMES the targeted criterion
    (`weakestCriterion`) and, when the chosen id is a `baa.microDrill.*`, frames a warm NAMED step-down
    (the D-03 register — never fake cheer, never "too hard for you"). Keep the G4/G5/G6 guard ladder
    BYTE-UNCHANGED: any proposed id outside `legalNextExerciseIds` is stripped/fails-closed to the
    AuthoredFallback (the rail is authoritative). The rationale must carry NO pass/fail or mastery
    claim (ADR-014 grounding — the scorer owns the verdict). Warm/specific tutor voice (CLAUDE.md).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_plan_graph.py -m code -q 2>&1 | tail -5 && uv run pytest -m code -q 2>&1 | tail -3</automated>
  </verify>
  <done>The coach picks from the policy-narrowed candidates and grounds a warm criterion-naming WHY line; G4/G5/G6 unchanged; no verdict/mastery claim in the rationale; the server model-free suite stays green.</done>
  <acceptance_criteria>
    - `cd server && uv run pytest -m code` stays green (the existing plan/graph rail tests unchanged).
    - The pick is drawn from `facts.legalNextExerciseIds`; an id outside it is stripped (the G4/G5/G6 guards are unchanged — grep shows no edit to the guard bodies).
    - Plan.rationale references `weakestCriterion` and branches on a `microDrill` pick for the step-down framing.
    - No `passed`/`grounded`/mastery mutation is added to the rationale path (ADR-014).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: selection_policy eval dimension + signed:false gold set (Req 9)</name>
  <files>server/tests/test_eval/run_eval.py, server/tests/test_eval/selection_gold_set.jsonl, server/tests/test_eval/test_selection_dimension.py</files>
  <behavior>
    - run_eval registers a `selection_policy` dimension in DIMENSIONS + JUDGE_GATED_DIMENSIONS
    - score_eval_set scores the selection dimension via the Vertex judge over selection_gold_set.jsonl and returns a rate ≥ SELECTION_THRESHOLD (provisional, signed:false)
    - the gold set has fail-streak, returning-child, and boredom-trap scenarios, each signed:false
  </behavior>
  <read_first>
    - server/tests/test_eval/run_eval.py (DIMENSIONS registry line 55; JUDGE_GATED_DIMENSIONS line 72; score_eval_set line 253 + the judge loop ~296; the existing _score_judge_dimension wiring)
    - server/tests/test_eval/gold_set.jsonl (the JSONL scenario shape to mirror)
    - server/tests/test_eval/test_selection_dimension.py (the RED contract from 18-01)
    - .planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-SPEC.md (Req 9 acceptance — scenarios + mother-agreed threshold)
  </read_first>
  <action>
    Add a `selection_policy` entry to `DIMENSIONS` and `JUDGE_GATED_DIMENSIONS`; wire a
    `_score_judge_dimension`-style leg in `score_eval_set` with a judge prompt that scores "would a
    teacher make THIS pick given the child's history, and does the WHY line name the right criterion?"
    against the expected-pick property in each gold scenario. Introduce `SELECTION_THRESHOLD` as a
    NAMED provisional constant documented `signed:false` (mother-agreed at 18-11, Req 9). Grow
    selection_gold_set.jsonl (started RED in 18-01) into the real scenario set: fail-streak (two
    same-criterion fails → a different targeted pick), returning-child (a stored struggle referenced),
    boredom-trap (never the identical 3rd repeat) — every line `"signed": false`. Turn
    test_selection_dimension.py GREEN with zero test edits (the model-free legs; the judge leg is
    deferred to `make eval` at the 18-11 gate, mirroring 17-04's judge-defer pattern).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_eval/test_selection_dimension.py -m code -q && make -n eval 2>&1 | tail -3</automated>
  </verify>
  <done>run_eval has a selection_policy judge-gated dimension with a provisional signed:false threshold; the gold set covers the three scenario families (all signed:false); the model-free selection test passes; `make eval` structure includes the dimension (judge leg deferred to 18-11).</done>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/test_eval/test_selection_dimension.py -m code` passes with zero test edits.
    - `grep -q selection_policy server/tests/test_eval/run_eval.py` in both DIMENSIONS and JUDGE_GATED_DIMENSIONS.
    - `SELECTION_THRESHOLD` is a named constant documented `signed:false`.
    - `grep -c '"signed": false' server/tests/test_eval/selection_gold_set.jsonl` equals the line count, and the file contains fail-streak / returning-child / boredom-trap scenarios.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent pick + rationale | untrusted; the rail (G4/G5/G6) decides legality and the scorer decides pass/fail; the rationale carries only words |
| eval gold content | pedagogy content; the threshold + scenarios are the mother's to sign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08-01 | Tampering | agent proposes off-graph pick | mitigate | G4/G5/G6 unchanged; the pick is drawn from legalNextExerciseIds; an out-of-set id is stripped/fails-closed |
| T-18-08-02 | Spoofing (verdict via words) | rationale claims pass/fail | mitigate | ADR-014 — the rationale carries no verdict/mastery claim; the scorer owns pass/fail; grounding guards unchanged |
| T-18-08-03 | Spoofing (pedagogy) | unsigned eval threshold/scenarios | mitigate | SELECTION_THRESHOLD + gold set ship signed:false; the mother signs the bar + scenarios at 18-11 (HUMAN-UAT) |
| T-18-08-SC | Tampering | package installs | accept | No new package — reuses langgraph/langchain-google-vertexai + the existing eval harness |
</threat_model>

<verification>
- `cd server && uv run pytest -m code` — plan/graph rails + the model-free selection dimension green.
- `make -n eval` — the selection_policy dimension is in the eval plan (judge leg runs at 18-11 with ADC).
- grep confirms signed:false threshold + gold scenarios.
</verification>

<success_criteria>
The coach voices WHY it picked (grounded in the policy facts, rails unchanged) and the pick quality
is a measurable, mother-gated eval dimension — the judged complement to the deterministic rails.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-08-SUMMARY.md` when done.
</output>
