---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - lib/core/scoring/criterion_ema.dart
  - server/app/criterion_ema.py
  - lib/data/app_database.dart
  - lib/data/app_database.g.dart
autonomous: true
requirements: [SPEC-18-R8]
must_haves:
  truths:
    - "A pure per-criterion EMA (updateEma) exists in Dart AND Python with byte-identical logic and agrees on the same fixtures (D-15)."
    - "The provisional α / thresholds are NAMED constants carrying signed:false (never magic numbers)."
    - "Drift schemaVersion bumps 5→6 with three additive, version-guarded tables (evidence accrual, arc state, profile mirror) and no touch to existing rows."
    - "Every new Drift table stores ONLY ids / counts / timestamps / JSON-encoded id-lists — never a stroke point or PII."
  artifacts:
    - path: "lib/core/scoring/criterion_ema.dart"
      provides: "pure Dart per-criterion EMA (no Flutter/Firebase import)"
      contains: "updateEma"
    - path: "server/app/criterion_ema.py"
      provides: "byte-identical Python EMA mirror"
      contains: "update_ema"
    - path: "lib/data/app_database.dart"
      provides: "schemaVersion 6 + LetterCriterionEvidence / ArcStateRows / ChildProfileMirror tables + primitive accessors"
      contains: "schemaVersion => 6"
  key_links:
    - from: "lib/core/scoring/criterion_ema.dart"
      to: "server/app/criterion_ema.py"
      via: "identical formula ema = alpha*outcome + (1-alpha)*prior"
      pattern: "1.0 - alpha|1 - alpha"
    - from: "app_database.dart migration if (from < 6)"
      to: "createTable(letterCriterionEvidence/arcStateRows/childProfileMirror)"
      via: "additive version-guarded onUpgrade block"
      pattern: "from < 6"
---

<objective>
Build the two lowest-level child-model foundations: the pure per-criterion EMA (mirrored
Dart↔Python, D-15) and the Drift schema bump 5→6 that adds the three persistence tables
(offline evidence accrual for the D-14 digest, arc-state resume for D-12, and the profile mirror
for D-16). Both are pure/additive and unblock the policy, the repositories, and the nightly
compiler.

Purpose: the on-device within-session estimate and the nightly compile must use the SAME EMA;
the resume cursor pattern (LetterGraphPosition v5) extends to arc state + evidence + profile.
Output: criterion_ema.dart + criterion_ema.py (green parity tests), app_database.dart at v6.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-RESEARCH.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: per-criterion EMA — Dart + Python mirror (D-15)</name>
  <files>lib/core/scoring/criterion_ema.dart, server/app/criterion_ema.py</files>
  <behavior>
    - updateEma(0.5, passed:true, 0.4) == 0.7 ; updateEma(0.5, passed:false, 0.4) == 0.3
    - a 3-step chain of the same inputs produces the same trajectory in Dart and Python
    - a criterion is "unknown" (neither strength nor struggle) until it has >= kMinAttempts (mirror the _deriveStruggleTags ">=2 occurrences" rule)
  </behavior>
  <read_first>
    - test/core/scoring/criterion_ema_test.dart (the RED fixtures authored in 18-01 — the exact expected values)
    - server/tests/test_criterion_ema.py (the mirrored Python fixtures)
    - lib/core/scoring/tolerances.dart (the pure-scorer-helper file-header + no-Flutter-import convention to copy)
    - lib/tutor/tutor_facts_builder.dart (the existing `_deriveStruggleTags` ">=2 occurrences" idiom to mirror for kMinAttempts)
  </read_first>
  <action>
    Author `updateEma(double prior, bool passed, double alpha)` in lib/core/scoring/criterion_ema.dart
    returning `alpha * (passed ? 1.0 : 0.0) + (1.0 - alpha) * prior`; pure Dart, NO Flutter/Firebase
    import (durable-layer citizen — lib/core is already scanned by durable_layers_no_agent_imports_test).
    Declare provisional NAMED constants `kEmaAlpha` (0.4), `kEmaStrengthHi` (0.75), `kEmaStruggleLo`
    (0.35), `kEmaMinAttempts` (2) — each documented `signed:false` (mother-signed at 18-11, D-15/A4) —
    plus a helper `classifyCriterion(ema, attempts)` returning strength/struggle/unknown using the
    thresholds+min-count gate (Pitfall 4: sparse data is "unknown", never a false struggle).
    Author `server/app/criterion_ema.py` `update_ema(prior, passed, alpha)` with the BYTE-IDENTICAL
    formula and the same provisional constants (module-level ALPHA/HI/LO/MIN). Turn the 18-01 EMA
    parity tests GREEN with zero test edits.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/criterion_ema_test.dart && cd server && uv run pytest tests/test_criterion_ema.py -m code -q</automated>
  </verify>
  <done>Both EMA functions exist with identical logic; both parity tests pass; α + thresholds are named signed:false constants; the Dart file carries no Flutter/Firebase import.</done>
  <acceptance_criteria>
    - `flutter test test/core/scoring/criterion_ema_test.dart` passes with zero edits to the RED test.
    - `cd server && uv run pytest tests/test_criterion_ema.py -m code` passes.
    - `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` stays green (criterion_ema.dart under lib/core carries no forbidden import).
    - criterion_ema.dart declares `kEmaAlpha`, `kEmaMinAttempts` and documents them `signed:false`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Drift schemaVersion 5→6 — evidence / arc-state / profile-mirror tables</name>
  <files>lib/data/app_database.dart, lib/data/app_database.g.dart</files>
  <read_first>
    - lib/data/app_database.dart (LetterGraphPosition table lines 82-107; LetterExerciseReps lines 109-130; @DriftDatabase table list lines 132-139; schemaVersion => 5 line 165; onUpgrade migration lines 168-195; the JSON-encoded-list-column idiom in clearedTiers/clearedCompetencies)
  </read_first>
  <action>
    Add three additive Drift tables mirroring the LetterGraphPosition shape (ids/counts/timestamps
    only, JSON-encode any id-list into a text column — Drift has no native list column):
    - `LetterCriterionEvidence` (offline accrual for the D-14 digest): columns letterId, criterion,
      passed (bool), source (text: "letter"|"word"), createdAt; auto-increment id PK.
    - `ArcStateRows` (D-12 resume): letterId PK, active (bool), step (text: entry/stepDown/rebuild/
      retryOriginal), targetCriterion (text nullable), exerciseToRetry (text nullable), updatedAt.
    - `ChildProfileMirror` (D-16 boot mirror): uid PK, strengths (text JSON List<String>), struggles
      (text JSON List<String>), perCriterion (text JSON Map<String,double>), updatedAt.
    Register all three in `@DriftDatabase(tables: [...])`; bump `schemaVersion` 5→6; add a
    version-guarded `if (from < 6) { await m.createTable(letterCriterionEvidence); await
    m.createTable(arcStateRows); await m.createTable(childProfileMirror); }` block (no data rewrite,
    no touch to existing rows). Add PRIMITIVE accessors (return Drift rows / maps / primitives, NOT
    lib/curriculum pure types — keep the DB layer type-cycle-free, 15-04 precedent): appendEvidence,
    unsyncedEvidence (returns rows), clearEvidence(ids) (rollup cap on tablet growth), getArcStateRow/
    setArcStateRow (JSON encode/decode like getPosition/setPosition lines 355-378), getProfileMirror/
    setProfileMirror. Run build_runner so app_database.g.dart regenerates; commit the .g.dart (tracked,
    not gitignored — 15-04 precedent).
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -3 && flutter test test/data/ 2>&1 | tail -5</automated>
  </verify>
  <done>schemaVersion is 6; three new tables registered + created under a `from < 6` guard; primitive accessors exist; existing migration blocks (from<2..from<5) untouched; app_database.g.dart regenerated and committed; existing DB tests green.</done>
  <acceptance_criteria>
    - `grep -q 'schemaVersion => 6' lib/data/app_database.dart`.
    - `grep -q 'from < 6' lib/data/app_database.dart` and the block calls createTable for the three new tables.
    - The three tables appear in the `@DriftDatabase(tables: [...])` list.
    - `dart run build_runner build` completes with no errors and updates app_database.g.dart.
    - Existing app_database tests (v2→v5 migrations, LetterMastery/LetterReps) still pass — no rewrite of existing migration blocks.
    - No new column holds an Offset/point/name — every column is an id, a count, a bool, a timestamp, or a JSON id-list.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-device Drift store | New tables must persist only derived non-PII ids/counts (child-safety) |
| Dart↔Python EMA | The two implementations must agree or the nightly compile disagrees with the on-device estimate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information disclosure | new Drift tables | mitigate | Columns are ids/counts/timestamps/JSON-id-lists only; the acceptance criteria forbid any point/Offset/name column; per-uid file isolation (forAccount) inherited for free |
| T-18-03-02 | Tampering (correctness) | EMA drift Dart↔Python | mitigate | Parity tests over identical fixtures on both sides; byte-identical formula |
| T-18-03-03 | Denial of service (storage) | on-device evidence growth | mitigate | clearEvidence(ids) rollup accessor caps tablet growth after the digest syncs (Claude's-discretion retention/rollup) |
</threat_model>

<verification>
- `flutter test test/core/scoring/criterion_ema_test.dart` + `uv run pytest tests/test_criterion_ema.py -m code` — parity green.
- `dart run build_runner build` clean; `flutter test test/data/` green (migration idempotent).
- `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` green (EMA stays pure).
</verification>

<success_criteria>
The EMA is one α, mirrored and agreeing on both sides; the Drift store is at v6 with three
additive, non-PII, resume-pattern tables ready for the repositories (18-06) and compiler (18-09).
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-03-SUMMARY.md` when done.
</output>
