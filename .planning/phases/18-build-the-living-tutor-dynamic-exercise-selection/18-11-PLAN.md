---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 11
type: execute
wave: 7
depends_on: [18-02, 18-05, 18-06, 18-07, 18-08, 18-09, 18-10]
files_modified:
  - assets/curriculum/exercises.json
  - assets/curriculum/curriculum_graph.json
  - server/app/curriculum_data/curriculum_graph.json
  - server/tests/test_eval/selection_gold_set.jsonl
  - lib/core/scoring/criterion_ema.dart
  - lib/curriculum/arc_state.dart
  - docs/architecture/COST-LATENCY-CLOSURE.md
  - .planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-HUMAN-UAT.md
autonomous: false
requirements: [SPEC-18-R2, SPEC-18-R3, SPEC-18-R4, SPEC-18-R7, SPEC-18-R8, SPEC-18-R9]
must_haves:
  truths:
    - "The server re-deploys carrying the new TutorFactsIn fields BEFORE any client build sends them (422 lockstep) — server FIRST; the child_models owner-read rule + the Cloud Run Job + Cloud Scheduler cron are created."
    - "The cost/latency open research question is CLOSED with MEASURED numbers (calls/session, cached-token %, stroke-up→feedback→next-pick wall-clock) documented on-device."
    - "The mother signs the provisional values + content (drill set, arc-N, α/EMA thresholds, eval threshold + gold set); signed:false flips to signed:true as the ONLY content change; recorded as HUMAN-UAT."
    - "`make eval` (with the Vertex judge) passes ≥ the mother-signed selection threshold on the signed gold set."
  artifacts:
    - path: "docs/architecture/COST-LATENCY-CLOSURE.md"
      provides: "the CLOSED cost/latency numbers (the open research question)"
      min_lines: 20
    - path: ".planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-HUMAN-UAT.md"
      provides: "the mother sign-off record + deploy-gate record (17-10 pattern)"
      min_lines: 15
  key_links:
    - from: "server re-deploy (new wire fields)"
      to: "client build that sends profile/evidenceDigest"
      via: "server FIRST — no 422 window"
      pattern: "422"
    - from: "mother sign-off"
      to: "signedOff:false → true flip"
      via: "the only content change behind the HUMAN-UAT gate"
      pattern: "signed"
---

<objective>
Close the phase: the ordered deploy gates (server re-deploy FIRST for the new wire fields, the
child_models owner-read rule, the Cloud Run Job + Cloud Scheduler cron), the CLOSED cost/latency
research question (measured on-device numbers), and the mother's sign-off of every provisional
pedagogy value + content (drill set, arc-N, α, eval threshold + gold set) — flipping signed:false to
signed:true as the only content change, then re-deriving + re-deploying the signed content. This is
the 17-10 human-gate pattern: the code is autonomous; these steps are genuinely human/infra.

Purpose: make the living tutor live, grounded, measured, and pedagogically signed.
Output: deployed server/Job/rule, COST-LATENCY-CLOSURE.md, signed content, 18-HUMAN-UAT.md.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-RESEARCH.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-VALIDATION.md
@docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: ordered deploy gates — server FIRST, rules, Cloud Run Job + Scheduler</name>
  <files>18-HUMAN-UAT.md (deploy record)</files>
  <action>
    Human-run infra deploy, in the 422-lockstep order: (1) re-deploy the /coach server carrying the
    new TutorFactsIn fields FIRST; (2) deploy the firestore.rules child_models owner-read block; (3)
    create the Cloud Run Job (compile command on the existing image) + the Cloud Scheduler nightly
    cron, with a Firestore-read/write runtime SA. Record every command + outcome in 18-HUMAN-UAT.md; a
    classifier-blocked command is recorded as a pending gate (17-10 pattern), never faked.
  </action>
  <what-built>
    The server (18-05) carries the additive TutorFactsIn fields + the /coach evidence append; the
    firestore.rules (18-05) has the child_models owner-read block; the compiler (18-09) is a runnable
    Cloud Run Job command. All are code-complete and model-free-tested. This gate performs the
    production deploys the auto-mode classifier blocks (17-10 precedent).
  </what-built>
  <how-to-verify>
    Run, IN THIS ORDER (the 422 lockstep — server ships FIRST, before any client build that sends the
    new fields):
    1. Re-deploy the /coach server carrying the new wire fields: `gcloud run deploy qalam-tutor
       --source server --project qalam-app-bd7d0 --region us-central1` (or the project's existing
       deploy command). Confirm `/health` 200 and that a live /coach with a `profile`/`evidenceDigest`
       payload returns 200 (not 422).
    2. Deploy the Firestore rule: `firebase deploy --only firestore:rules --project qalam-app-bd7d0`.
       In the Rules Playground confirm account A cannot read account B's child_models/{uid} and all
       client writes are denied.
    3. Create the nightly Cloud Run Job on the SAME image with the compile command:
       `gcloud run jobs create qalam-compile-profiles --image <the qalam-tutor image> --command python
       --args -m,app.jobs.compile_profiles --project qalam-app-bd7d0 --region us-central1`, ensure the
       Job runtime SA has Firestore read/write (roles/datastore.user), then create the Cloud Scheduler
       cron: `gcloud scheduler jobs create http qalam-nightly-compile --schedule "0 3 * * *" ...`
       targeting the Job. Run the Job once manually and confirm a child_models/{uid} doc is written.
    Record each command + outcome in 18-HUMAN-UAT.md. If the classifier blocks a command, record it as
    a pending gate (17-10 pattern) — do NOT fake it.
  </how-to-verify>
  <resume-signal>Type "deployed" (server 200 + rule live + Job+cron created + one compile run confirmed) or describe what blocked.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CLOSE the cost/latency research question (measured device numbers)</name>
  <files>docs/architecture/COST-LATENCY-CLOSURE.md</files>
  <action>
    Human-run a full baa session on a Pixel Tablet / iPad against the freshly deployed server and
    MEASURE the three numbers (calls/session, cached-token %, stroke-up→feedback→next-pick wall-clock),
    then write them — with device + build + date — into docs/architecture/COST-LATENCY-CLOSURE.md and
    mark the open research question CLOSED. Confirm selection adds no perceptible latency (the pick
    rides the same /coach round-trip the feedback moment masks, Pitfall 5).
  </action>
  <what-built>
    The selection pick rides the existing /coach round-trip (no new call, Pitfall 5) and implicit
    Vertex caching is on by default. The framework + expected values are in RESEARCH §Cost + Latency
    Closure; only a device+server run produces the signed numbers.
  </what-built>
  <how-to-verify>
    On a Pixel Tablet / iPad against the freshly deployed server, run a full baa session and MEASURE:
    (1) calls/session — count POST /coach in the run; (2) cached-token % — from the /coach logs /
      Vertex token accounting (input tokens served from the implicit cache vs total);
    (3) stroke-up → feedback → next-pick wall-clock — the perceived latency for a scored attempt.
    Write the measured numbers (with device + build + date) into docs/architecture/COST-LATENCY-CLOSURE.md
    and state the question CLOSED. Confirm selection adds no perceptible latency (the pick is in the
    same round-trip the feedback moment masks).
  </how-to-verify>
  <resume-signal>Type "measured" with the three numbers, or describe what blocked (e.g., no device).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: mother sign-off of provisional values + content → flip signed:false → true</name>
  <files>assets/curriculum/exercises.json, assets/curriculum/curriculum_graph.json, server/app/curriculum_data/curriculum_graph.json, server/tests/test_eval/selection_gold_set.jsonl, lib/core/scoring/criterion_ema.dart, lib/curriculum/arc_state.dart, 18-HUMAN-UAT.md</files>
  <decision>
    Approve (or correct) every provisional pedagogy value/content, then flip signed:false → true as
    the ONLY content change (15-07/17-10 pattern), re-derive the server copy, and re-deploy the signed
    content.
  </decision>
  <context>
    All of these shipped provisional (`signed:false`) and are the mother's domain — curriculum content
    and pedagogy parameters. Present each with its one-sentence rationale:
    - the baa micro-drill set (dot / bowl / start — assets/curriculum/exercises.json + curriculum_graph.json, D-07)
    - the step-down / arc framing copy (D-03 register)
    - arc-N: kArcEntryFailStreak + kArcMaxAttempts (lib/curriculum/arc_state.dart, D-02/D-04 — "recent fails enter the arc; a clean win within N")
    - the EMA α + thresholds: kEmaAlpha / kEmaStrengthHi / kEmaStruggleLo / kEmaMinAttempts (lib/core/scoring/criterion_ema.dart + server/app/criterion_ema.py, D-15 — "recent attempts count more")
    - the selection eval threshold + gold scenarios: SELECTION_THRESHOLD + selection_gold_set.jsonl (Req 9)
  </context>
  <options>
    <option id="approve-as-drafted">
      <name>Approve the provisional values as drafted</name>
      <pros>Fastest; values are research-grounded defaults</pros>
      <cons>May not match the mother's exact pedagogy</cons>
    </option>
    <option id="mother-adjusts">
      <name>The mother gives her numbers / edits the copy + drills</name>
      <pros>Pedagogically authoritative (her domain)</pros>
      <cons>Requires her session; then re-run generate.py + make eval + re-deploy</cons>
    </option>
  </options>
  <action>
    After the decision: update the constants/content to the signed values; flip `signedOff: false` →
    `true` on the signed micro-drill exercises + graph nodes and `"signed": false` → `true` on the gold
    scenarios; re-run `python -m app.curriculum_data.generate` to re-derive the server curriculum copy
    from the SIGNED asset; run `cd server && make eval` (with the Vertex judge / ADC) and confirm the
    selection dimension passes ≥ the signed SELECTION_THRESHOLD on the signed gold set; re-deploy the
    server so the signed graph + eval threshold go live. Record the sign-off (who/when/what) in
    18-HUMAN-UAT.md (15-07 pattern). The flip is the ONLY content change.
  </action>
  <resume-signal>Select: approve-as-drafted OR mother-adjusts (with her values); then confirm the flip + generate.py + make eval + re-deploy are done.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (deploy order) | server must carry the new wire fields before the client sends them (422) |
| client → Firestore (rules) | the owner-read rule must be live before the client's D-16 .get() succeeds |
| unsigned pedagogy → child | provisional values/content must not ship as authoritative without the mother's sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11-01 | Tampering (422 window) | deploy order | mitigate | Task 1 re-deploys the server FIRST and verifies a live profile/digest payload returns 200 before the client build ships |
| T-18-11-02 | Elevation of privilege | child_models cross-account read | mitigate | Task 1 verifies the owner-read rule in the Rules Playground (A cannot read B; all client writes denied) |
| T-18-11-03 | Elevation of privilege | Cloud Run Job SA scope | mitigate | Task 1 confirms the Job runtime SA holds only Firestore read/write (roles/datastore.user) |
| T-18-11-04 | Spoofing (pedagogy) | unsigned drills/thresholds go live | mitigate | Task 3 gates every provisional value/content behind the mother's sign-off; the flip is the only content change; recorded HUMAN-UAT |
| T-18-11-05 | Information disclosure | cost/latency measurement leaks PII | accept | The measurement counts calls/tokens/wall-clock only — no child PII captured; numbers are aggregates |
</threat_model>

<verification>
- Server `/health` 200 + a live /coach with profile/evidenceDigest returns 200 (server-first, no 422).
- Rules Playground: owner-only child_models read; all client writes denied.
- Cloud Run Job runs once → a child_models/{uid} doc appears.
- `cd server && make eval` passes ≥ the signed SELECTION_THRESHOLD on the signed gold set.
- docs/architecture/COST-LATENCY-CLOSURE.md states the three measured numbers + CLOSED.
- 18-HUMAN-UAT.md records the deploy gates + the mother sign-off (who/when/what).
</verification>

<success_criteria>
The living tutor is live (server-first, rules + Job deployed), measured (cost/latency CLOSED), and
signed (mother-approved values/content flipped signed:true, make eval green) — the phase's human/infra
gates closed on the 17-10 pattern.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-11-SUMMARY.md` when done.
</output>
