---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/letter_unit/widgets/prompt_header.dart
autonomous: true
gap_closure: true
requirements: [UAT-18-T2]

must_haves:
  truths:
    - "For a picture-prompt exercise, the stimulus image renders large and prominent — it grows to use the available header width on a real tablet, not a fixed small box."
    - "The picture caption reads left-to-right with its trailing punctuation in place ('what does it start with?', never '?what does it start with')."
  artifacts:
    - path: "lib/features/letter_unit/widgets/prompt_header.dart"
      provides: "_ImagePart sizes the stimulus responsively against available layout width, and the caption Text pins textDirection: TextDirection.ltr"
      contains: "TextDirection.ltr"
  key_links:
    - from: "lib/features/letter_unit/widgets/prompt_header.dart"
      to: "the exercise header row available width"
      via: "responsive sizing (LayoutBuilder/AspectRatio/flex) instead of a hardcoded pixel box"
      pattern: "LayoutBuilder|AspectRatio|FractionallySizedBox"
---

<objective>
Close the cosmetic stimulus-picture gap (UAT test 2). The image renders too small because `_ImagePart` in `prompt_header.dart` uses a hardcoded fixed-size box (260x176 — just doubled from 128x84 in the 2026-07-12 fix); the sizing STRATEGY was never fixed, only the constant. On a real tablet the main column is 700–1500+ logical px wide, so the fixed box reads as a small island in a wide row. Separately, the caption Text has no explicit `textDirection`, so it inherits the ambient RTL `Directionality` from ExerciseScaffold and the trailing "?" bidi-resolves to the front.

Purpose: The child reads the question FROM the picture — it must be big and clearly readable, and the English caption must not scramble its punctuation.

Output: `_ImagePart` sizes the stimulus responsively against the actual available header width, and the caption is pinned LTR. The correct-fix precedent already exists in `feedback_panel_v2.dart` (an English string given `textDirection: TextDirection.ltr` "so the trailing period doesn't jump left") — apply the same to the caption.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/stimulus-picture-too-small.md
@lib/features/letter_unit/widgets/prompt_header.dart
@lib/features/letter_unit/widgets/feedback_panel_v2.dart
@lib/features/letter_unit/widgets/exercise_scaffold.dart
@test/features/letter_unit/prompt_header_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Size the stimulus image responsively and pin the caption LTR</name>
  <files>lib/features/letter_unit/widgets/prompt_header.dart, test/features/letter_unit/prompt_header_image_test.dart</files>
  <behavior>
    Write these widget tests FIRST (they fail against the current hardcoded 260x176 box + RTL-inherited caption), then implement to green:
    - Responsiveness: pump `_ImagePart` (via PromptHeader with an image-only prompt matching baa.writeLetter.fromPicture — prompt = [say, image], no TextPart sibling) inside a WIDE constrained parent (e.g. 900px main column). ASSERT the rendered image box width scales up with the available width (materially larger than the old 260px cap — e.g. at least ~55% of the available header width), and re-pump at a NARROW width (e.g. 360px) and ASSERT it shrinks to fit (never overflows). Use a real bundled asset id that resolves, or the hatched-stub fallback branch — either must size the same way.
    - Caption bidi: ASSERT the caption Text is rendered with `textDirection: TextDirection.ltr` (find the caption Text widget and check its textDirection), so a trailing "?" stays at the end under the ambient RTL Directionality.
  </behavior>
  <action>
    Replace the fixed `width: 260, height: 176` Container/Image in `_ImagePart` with responsive sizing that grows against the available header-row width instead of a pixel constant. Wrap the image box in a `LayoutBuilder` and size the box to a sensible fraction of `constraints.maxWidth` (clamped to a readable min and a not-absurd max), preserving the picture's aspect ratio with an `AspectRatio` (keep the ~260:176 ≈ 1.48 ratio the art was authored for). The `ClipRRect(borderRadius: 14)` + `Image.asset(fit: BoxFit.cover, errorBuilder: _hatchedStub)` silent-degrade posture must be preserved (an unmapped id or a failed load still shows the hatched stub, never throws). Because this exercise type has no TextPart sibling to Expanded and fill the row (`PromptHeader._partFlex` only wraps TextPart), the image itself must now claim the row width — verify against exercise_scaffold.dart's layout where `_mainColumn` is `Expanded` after the fixed 258px mascot column + 24px gap + 26px×2 scaffold padding (that is the real space to size against).

    Add `textDirection: TextDirection.ltr` to the caption `Text` in `_ImagePart` (currently ~lines 226-234, no textDirection). This mirrors the already-shipped fix in feedback_panel_v2.dart for the identical trailing-punctuation-under-RTL bug class. Do not otherwise change the caption's style or the source caption string (the source data is correct — it is a render bug).
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/prompt_header_image_test.dart test/features/letter_unit/prompt_header_test.dart</automated>
  </verify>
  <done>The stimulus image grows with available width (large on a wide tablet column, shrinks to fit a narrow one) and preserves its aspect ratio + silent-degrade fallback; the caption renders LTR with trailing punctuation in place. New + existing prompt_header tests pass; `flutter analyze` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none-new | Pure presentation change (image sizing + caption text direction). No input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13-01 | Tampering | npm/pip/cargo installs | accept | No new dependencies; no install step. |
</threat_model>

<verification>
- `flutter test test/features/letter_unit/prompt_header_image_test.dart` and the existing `prompt_header_test.dart` pass.
- `flutter analyze` exits 0.
</verification>

<success_criteria>
- Picture prompts render large and readable, scaling with the tablet's available width (UAT T2 closed).
- Caption reads LTR with punctuation in the correct place.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-13-SUMMARY.md` when done.
</output>
