---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - lib/curriculum/selection_policy.dart
  - lib/curriculum/arc_state.dart
  - lib/curriculum/child_model_snapshot.dart
  - lib/curriculum/curriculum_graph.dart
autonomous: true
requirements: [SPEC-18-R1, SPEC-18-R3, SPEC-18-R4]
must_haves:
  truths:
    - "A pure-Dart SelectionPolicy narrows the graph-legal set (anti-boredom filter + micro-drill injection + arc-step constraints) and returns (candidates, arcStep, targetCriterion, whyFacts) (D-09/D-11)."
    - "Two consecutive fails on the SAME weakestCriterion both forbid an identical 3rd repeat (R1) AND enter the arc targeting that criterion (R4) — ONE counter drives both (D-02)."
    - "A dominant failing criterion injects its microDrill.<criterion> into candidates (R3)."
    - "The arc is a state machine entry→stepDown→rebuild→retryOriginal exiting on a clean win on the ORIGINAL exercise, with a floor guard to a guaranteed-doable success (never an endless loop, D-04)."
    - "Every candidate the policy emits still passes CurriculumGraph.isLegalSelection — the policy narrows, never replaces, the rail (D-09)."
    - "selection_policy.dart lives in lib/curriculum/ and imports nothing from Riverpod/Firebase/drift/render — covered by the existing strict lib/curriculum durable-layer ban with ZERO guard change."
  artifacts:
    - path: "lib/curriculum/selection_policy.dart"
      provides: "pure arc SM + anti-boredom + drill injection + candidate narrowing"
      contains: "class SelectionPolicy"
    - path: "lib/curriculum/arc_state.dart"
      provides: "pure ArcState value type + provisional arc constants (signed:false)"
      contains: "kArcEntryFailStreak"
  key_links:
    - from: "SelectionPolicy.narrow"
      to: "CurriculumGraph.isLegalSelection"
      via: "every emitted candidate re-checked against the rail"
      pattern: "isLegalSelection"
    - from: "dominant weakestCriterion"
      to: "microDrill.<criterion> node"
      via: "graph node.criterion tag lookup"
      pattern: "microDrill"
---

<objective>
Build the pure-Dart brain — `SelectionPolicy` — the heart of the phase (D-09/D-11). It reads the
two-timescale signal (session `trajectory`+`weakestCriterion` and the compiled `ChildModelSnapshot`)
plus the durable `GraphPosition`+`ArcState`, and returns the narrowed legal candidate set + the arc
step + target criterion + the WHY facts. Both the online router and the offline walker consume the
SAME output, so offline parity (R6) is guaranteed by construction. One counter (two same-criterion
fails, D-02) drives BOTH anti-boredom (R1) and arc entry (R4); a dominant failing criterion injects
its micro-drill (R3).

Placement decision (delegated to planner): `lib/curriculum/` — the existing strict lib/curriculum
durable-layer ban (durable_layers_no_agent_imports_test.dart lines 138-196) already forbids
cloud/Firebase/drift/riverpod/flutter/agent/network, so the policy is guaranteed pure with ZERO
guard change, and it sits beside the walker/graph it composes. Profile + arc state reach it as PURE
VALUE TYPES (parameters), exactly as GraphPosition already reaches the walker.

Purpose: deterministic, testable selection intelligence that never leaves the rails.
Output: selection_policy.dart + arc_state.dart + child_model_snapshot.dart; graph.dart gains the
microDrill criterion-tag parse.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-CONTEXT.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
@.planning/sketches/001-remediation-arc/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pure ArcState + ChildModelSnapshot value types + graph criterion-tag parse</name>
  <files>lib/curriculum/arc_state.dart, lib/curriculum/child_model_snapshot.dart, lib/curriculum/curriculum_graph.dart</files>
  <behavior>
    - ArcState has {active, step, targetCriterion, exerciseToRetry} and a stepDown/rebuild/retry transition helper
    - kArcEntryFailStreak and kArcMaxAttempts are named provisional constants (signed:false)
    - CurriculumGraph exposes microDrill nodes by their `criterion` tag (drillForCriterion(letter, criterion) → nodeId)
  </behavior>
  <read_first>
    - lib/curriculum/curriculum_graph.dart (GraphNode model lines 66-91; _nodeFromJson parse lines 150-162; essentialNodes lines 164-168; isLegalSelection lines 288-302)
    - lib/curriculum/curriculum_graph_walker.dart (the pure-layer file-header contract lines 1-16 to copy)
    - test/tutor/remediation_arc_test.dart (RED — the exact ArcState step names + constant names it references)
    - assets/curriculum/curriculum_graph.json (the microDrill nodes + criterion tags authored in 18-02)
  </read_first>
  <action>
    Author `lib/curriculum/arc_state.dart`: a pure immutable `ArcState` {bool active, ArcStep step,
    String? targetCriterion, String? exerciseToRetry} with an `ArcStep` enum
    (entry/stepDown/rebuild/retryOriginal) + named provisional constants `kArcEntryFailStreak` (2) and
    `kArcMaxAttempts` (5), each documented `signed:false` (mother-signed at 18-11, D-02/D-04). No
    Flutter/Firebase/drift import (copy the walker's pure-layer header).
    Author `lib/curriculum/child_model_snapshot.dart`: a pure immutable `ChildModelSnapshot`
    {List<String> strengths, List<String> struggles, Map<String,double> perCriterion} + a
    `ChildModelSnapshot.empty()` neutral factory (offline-first-run / no profile yet). Pure.
    Extend `curriculum_graph.dart`: parse the microDrill node `criterion` tag into GraphNode (nullable
    `criterion`) and add `String? drillForCriterion(String letterId, String criterion)` returning the
    matching `microDrill.<criterion>` node id (or null). No change to isLegalSelection/essentialNodes
    semantics — microDrills stay enrichment.
  </action>
  <verify>
    <automated>flutter test test/tutor/durable_layers_no_agent_imports_test.dart && flutter analyze lib/curriculum/arc_state.dart lib/curriculum/child_model_snapshot.dart lib/curriculum/curriculum_graph.dart 2>&1 | tail -3</automated>
  </verify>
  <done>ArcState + ChildModelSnapshot pure types exist with the named provisional constants; graph.dart exposes drillForCriterion; the strict lib/curriculum ban stays green (zero guard change).</done>
  <acceptance_criteria>
    - `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` passes (the new lib/curriculum files carry no forbidden import).
    - arc_state.dart declares `kArcEntryFailStreak`, `kArcMaxAttempts`, documents both `signed:false`, and defines `ArcStep` with entry/stepDown/rebuild/retryOriginal.
    - `curriculum_graph.dart` has `drillForCriterion(` and GraphNode carries a nullable `criterion`.
    - `flutter analyze` reports no errors for the three files.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SelectionPolicy — narrow() (anti-boredom + drill injection + arc SM)</name>
  <files>lib/curriculum/selection_policy.dart</files>
  <behavior>
    - Two consecutive same-criterion fails on exercise X → X excluded from candidates (R1) AND arc entered targeting that criterion (R4)
    - A dominant failing criterion → its microDrill.<criterion> injected into candidates (R3)
    - Arc walks entry→stepDown(drill)→rebuild→retryOriginal; a clean win on the ORIGINAL exercise exits; floor guard lands a guaranteed-doable success and ends warm within kArcMaxAttempts (D-04)
    - whyFacts names the targeted criterion + the arc step (feeds the WHY line, D-10)
    - EVERY emitted candidate passes CurriculumGraph.isLegalSelection
  </behavior>
  <read_first>
    - lib/curriculum/curriculum_graph_walker.dart (the offline selector this sits in front of — fail path lines 90-103, _nextReachableForward 108-123)
    - lib/curriculum/curriculum_graph.dart (isLegalSelection lines 288-302; drillForCriterion from Task 1)
    - lib/tutor/tutor_facts.dart (TutorFacts.trajectory / weakestCriterion the policy reads, lines 114-154)
    - lib/tutor/tutor_facts_builder.dart (the `_deriveStruggleTags` ">=2 occurrences" idiom to reuse, lines 83-96)
    - test/tutor/selection_policy_test.dart + test/tutor/microdrill_selection_test.dart (RED contracts — narrow() signature + exact assertions)
  </read_first>
  <action>
    Author `class SelectionPolicy` in lib/curriculum/selection_policy.dart (pure; imports only
    curriculum_graph.dart, arc_state.dart, child_model_snapshot.dart, and ../tutor/tutor_facts.dart —
    all pure). Method `PolicyDecision narrow(TutorFacts facts, GraphPosition position,
    ChildModelSnapshot profile, ArcState arc)` returning a pure `PolicyDecision` {List<String>
    candidates, ArcState nextArc, String? targetCriterion, Map<String,Object?> whyFacts}. Logic:
    (1) Start from the graph-legal forward+remediation set the walker would consider (compose the
    walker's reachable-forward + remediateOneTier), filtered through isLegalSelection.
    (2) Anti-boredom (R1/D-02): count consecutive same-`weakestCriterion` fails on the same exercise
    off `facts.trajectory` (reuse the ">=2 occurrences" idiom); at `kArcEntryFailStreak`, EXCLUDE that
    exact exercise from candidates.
    (3) Drill injection (R3): when a criterion dominates the recent fail history, add
    `graph.drillForCriterion(letterId, criterion)` to candidates (enrichment — never gates the star).
    (4) Arc SM (R4/D-04): at the fail streak, transition `arc` entry→stepDown(target=the criterion,
    exerciseToRetry=the failed exercise); stepDown's candidate is the micro-drill; on a drill pass →
    rebuild; on rebuild pass → retryOriginal (candidates = [exerciseToRetry]); a clean win on the
    original exits (nextArc.active=false); a floor-fail lands a guaranteed-doable success (a trace of
    the letter) and ends warm; enforce the kArcMaxAttempts ceiling so it never loops.
    (5) whyFacts: {criterion, arcStep, reason} — pure non-PII strings for the WHY line (D-10).
    Re-check EVERY candidate with isLegalSelection before returning (defense in depth — the policy
    narrows, never widens, the rail). Turn selection_policy_test + microdrill_selection_test +
    remediation_arc_test GREEN with zero test edits.
  </action>
  <verify>
    <automated>flutter test test/tutor/selection_policy_test.dart test/tutor/microdrill_selection_test.dart test/tutor/remediation_arc_test.dart test/tutor/durable_layers_no_agent_imports_test.dart</automated>
  </verify>
  <done>SelectionPolicy.narrow returns the narrowed legal candidates + arc step + target criterion + whyFacts; the three RED tests pass with zero edits; the policy re-checks isLegalSelection on every candidate; the strict lib/curriculum ban stays green.</done>
  <acceptance_criteria>
    - `flutter test test/tutor/selection_policy_test.dart test/tutor/microdrill_selection_test.dart test/tutor/remediation_arc_test.dart` all pass with zero test edits.
    - `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` passes (selection_policy.dart pure).
    - `grep -q 'isLegalSelection' lib/curriculum/selection_policy.dart` (every candidate re-checked).
    - The narrow() return type carries candidates, nextArc, targetCriterion, and whyFacts.
    - No literal fail-streak/attempt number in the body — `kArcEntryFailStreak`/`kArcMaxAttempts` are referenced by name.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy → rail | The policy proposes candidates; the graph rail (isLegalSelection) is the authority. The policy must never emit an illegal candidate. |
| pure layer purity | selection_policy.dart is the offline floor — it must carry zero agent/cloud/render/persistence import so the star/selection are never granted off a server response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Tampering | candidate set | mitigate | Every candidate re-checked with isLegalSelection inside narrow(); the seeded-random rails property (18-07) proves 100% legality downstream |
| T-18-04-02 | Elevation of privilege (offline floor) | policy purity | mitigate | Placed in lib/curriculum → the strict durable-layer ban forbids cloud/Firebase/drift/riverpod/render/agent/network with zero guard change; a self-test proves the ban is non-vacuous |
| T-18-04-03 | Denial of service (UX) | arc endless loop | mitigate | kArcMaxAttempts ceiling + the D-04 floor guard land a guaranteed-doable success and end the arc warm — never a loop |
</threat_model>

<verification>
- `flutter test test/tutor/selection_policy_test.dart test/tutor/microdrill_selection_test.dart test/tutor/remediation_arc_test.dart` — GREEN.
- `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` — the strict lib/curriculum ban still green (purity preserved, zero guard change).
- `flutter analyze lib/curriculum/` — no errors.
</verification>

<success_criteria>
The deterministic selection brain exists, pure and rail-bounded: anti-boredom + micro-drill
injection + the confidence-rebuilding arc, all off the two-timescale signal, all provisional-value
gated, consumed identically online and offline.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-04-SUMMARY.md` when done.
</output>
