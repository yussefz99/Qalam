---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/tutor/selection_policy_test.dart
  - test/tutor/remediation_arc_test.dart
  - test/tutor/microdrill_selection_test.dart
  - test/tutor/selection_rails_property_test.dart
  - test/tutor/offline_floor_test.dart
  - test/tutor/across_session_memory_test.dart
  - test/core/scoring/criterion_ema_test.dart
  - test/tutor/payload_nonpii_test.dart
  - server/tests/test_criterion_ema.py
  - server/tests/test_schema_forbid.py
  - server/tests/test_evidence.py
  - server/tests/test_compile_profiles.py
  - server/tests/test_eval/test_selection_dimension.py
  - server/tests/test_eval/selection_gold_set.jsonl
autonomous: true
requirements: [SPEC-18-R1, SPEC-18-R2, SPEC-18-R3, SPEC-18-R4, SPEC-18-R5, SPEC-18-R6, SPEC-18-R7, SPEC-18-R8, SPEC-18-R9]
must_haves:
  truths:
    - "Every Phase-18 requirement (R1..R9) has an executable, failing automated test naming its exact behavior before any implementation (Nyquist)."
    - "Each RED test fails by MISSING SYMBOL (SelectionPolicy, updateEma, evidence.append_evidence, compile_profiles, TutorFactsIn.profile), never by a fabricated assertion."
    - "The EMA parity contract is pinned by two tests (Dart + Python) reading the SAME fixtures."
  artifacts:
    - path: "test/tutor/selection_policy_test.dart"
      provides: "RED contract for Req 1 anti-boredom + WHY line"
      min_lines: 20
    - path: "test/tutor/selection_rails_property_test.dart"
      provides: "RED seeded-random rails property (Req 5)"
      min_lines: 20
    - path: "server/tests/test_evidence.py"
      provides: "RED contract for Req 7 word→per-letter×criterion evidence"
      min_lines: 15
    - path: "server/tests/test_compile_profiles.py"
      provides: "RED contract for Req 8 compiler + second-letter + PII guard"
      min_lines: 15
    - path: "server/tests/test_eval/selection_gold_set.jsonl"
      provides: "signed:false selection gold scenarios (fail-streak / returning-child / boredom-trap)"
      min_lines: 3
  key_links:
    - from: "test/core/scoring/criterion_ema_test.dart"
      to: "server/tests/test_criterion_ema.py"
      via: "identical EMA fixtures (same prior/outcome/alpha → same result)"
      pattern: "updateEma|update_ema"
---

<objective>
Author the complete Wave-0 RED contract for Phase 18: one failing automated test per
requirement (R1..R9) plus the EMA Dart↔Python parity tests and the extended non-PII / 422
guards. Every test fails by MISSING SYMBOL so the downstream plans have an executable target
to turn GREEN — the Nyquist discipline established by 15-01 / 17-01.

Purpose: no requirement ships without a falsifiable test that existed BEFORE its code; no 3
consecutive downstream tasks lack an automated verify (Dimension 8).
Output: 13 new/extended test files + 1 signed:false gold-set fixture, all RED.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-SPEC.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-VALIDATION.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client selection RED tests (Req 1,3,4,5,6)</name>
  <files>test/tutor/selection_policy_test.dart, test/tutor/remediation_arc_test.dart, test/tutor/microdrill_selection_test.dart, test/tutor/selection_rails_property_test.dart, test/tutor/offline_floor_test.dart, test/tutor/across_session_memory_test.dart</files>
  <read_first>
    - test/core/scoring/calibration_harness_test.dart (the parametrized-loop + per-case-assert shape to mirror, lines ~320-367)
    - test/tutor/payload_nonpii_test.dart (existing tutor-test conventions + imports)
    - lib/tutor/tutor_facts.dart (TutorFacts + AttemptFact shapes the tests construct)
    - lib/tutor/exercise_selector_provider.dart (RouterExerciseSelector.selectNext + the accept-if-legal seam under test)
    - lib/curriculum/curriculum_graph.dart (isLegalSelection signature; GraphPosition; nodes)
  </read_first>
  <action>
    Author six RED flutter_test files, each RED by referencing a not-yet-existing symbol
    (`SelectionPolicy` in `lib/curriculum/selection_policy.dart`, its `narrow(...)` returning
    `(candidates, arcStep, targetCriterion, whyFacts)`, and `ChildModelSnapshot`/`ArcState` pure
    types). Name the behavior precisely per SPEC acceptance criteria:
    - selection_policy_test.dart (R1): construct a trajectory with TWO consecutive fails on the
      SAME `weakestCriterion` for the same exercise; assert `SelectionPolicy.narrow` excludes that
      exact exercise from `candidates` (no identical 3rd repeat) AND emits a `targetCriterion` equal
      to the failed criterion AND a non-empty `whyFacts` naming that criterion.
    - remediation_arc_test.dart (R4): simulate a same-criterion fail streak reaching
      `kArcEntryFailStreak`; assert the policy returns observable arc steps
      entry→stepDown→rebuild→retryOriginal and that a struggling run reaches a clean win within
      `kArcMaxAttempts`; assert the floor guard lands on a guaranteed-doable success when even the
      drill fails (D-04) — never an endless loop. Reference `kArcEntryFailStreak`/`kArcMaxAttempts`
      as named provisional constants (signed:false), not literals.
    - microdrill_selection_test.dart (R3): calibration-harness-style per letter×form loop — when a
      criterion DOMINATES the fail history, assert the policy's `candidates` include that criterion's
      `microDrill.<criterion>` id (dot / bowl / start for baa).
    - selection_rails_property_test.dart (R5): a `Random(fixedSeed)` loop (≥200 iterations)
      generating arbitrary agent proposals + histories; assert EVERY accepted pick is graph-legal
      (`isLegalSelection` true) AND every illegal proposal degrades to the walker's pick. Plain
      flutter_test seeded-random — NO glados (analyzer-9 conflict, RESEARCH §Don't Hand-Roll).
    - offline_floor_test.dart (R6): drive a multi-exercise session with the brain unavailable
      (no `TutorDecision`); assert the walker completes it with no null-dead-end and that no
      selection call awaits a network future.
    - across_session_memory_test.dart (R2): seed a `ChildModelSnapshot` with a stored struggle;
      assert the outgoing `TutorFacts.profile` carries it and the first pick/`whyFacts` references it.
    Each file MUST fail with a compile/symbol error today. Do NOT stub the missing symbols.
  </action>
  <verify>
    <automated>flutter test test/tutor/selection_policy_test.dart test/tutor/remediation_arc_test.dart test/tutor/microdrill_selection_test.dart test/tutor/selection_rails_property_test.dart test/tutor/offline_floor_test.dart test/tutor/across_session_memory_test.dart 2>&1 | grep -Eiq 'compilation failed|Error:|was not found|isn.t defined' && echo RED-OK</automated>
  </verify>
  <done>All six files exist and fail by missing symbol (SelectionPolicy / ChildModelSnapshot / ArcState / provisional constants); none pass; none contain a fabricated always-true assertion.</done>
  <acceptance_criteria>
    - `flutter test <the six files>` fails to compile/execute because the referenced symbols do not exist yet.
    - selection_rails_property_test.dart uses `Random(<fixed seed>)` and contains no `glados`/`flutter_glados` import.
    - remediation_arc_test.dart references `kArcEntryFailStreak` and `kArcMaxAttempts` by name (not integer literals).
    - Each file's test names quote the SPEC acceptance-criterion behavior (grep the file for "criterion", "arc", "legal", "walker", "profile" as applicable).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: EMA parity + non-PII / 422 guard RED tests (D-14, R8)</name>
  <files>test/core/scoring/criterion_ema_test.dart, test/tutor/payload_nonpii_test.dart, server/tests/test_criterion_ema.py, server/tests/test_schema_forbid.py</files>
  <read_first>
    - test/tutor/payload_nonpii_test.dart (existing `_whitelist` lines ~27-66 + nested key sets `_criteriaKeys`/`_strokeDiffKeys` lines ~73-94 + `_forbiddenKey` token scan lines ~107-110)
    - server/app/schema.py (TutorFactsIn + CriterionIn extra="forbid" pattern the new fields must follow)
    - lib/tutor/tutor_facts.dart (toMap omit-when-null shape)
    - server/pyproject.toml ([tool.pytest.ini_options] markers = ["code"])
  </read_first>
  <action>
    Author the parity + guard RED tests:
    - criterion_ema_test.dart: assert `updateEma(prior, passed, alpha)` (missing —
      `lib/core/scoring/criterion_ema.dart`) equals fixed expected values for a fixture table
      (e.g. prior 0.5, pass, alpha 0.4 → 0.7; prior 0.5, fail, alpha 0.4 → 0.3; a 3-step chain).
      Record the SAME fixture rows as literals so the Python mirror can reuse them byte-for-byte.
    - server/tests/test_criterion_ema.py (mark `code`): assert `update_ema` (missing —
      `server/app/criterion_ema.py`) returns the IDENTICAL results for the identical fixture rows.
    - Extend payload_nonpii_test.dart: add `profile` and `evidenceDigest` to `_whitelist`; add
      nested-key sets `_profileKeys` = {strengths, struggles, perCriterion, schemaVersion} and
      `_evidenceDigestKeys` = {letter, criterion, pass, fail}; keep the `_forbiddenKey` token scan
      (x/y/points/strokes/offset/childName/nickname) green. This EXTENSION is RED now because
      TutorFacts does not yet emit those keys — assert a built TutorFacts with a profile+digest
      serializes exactly those whitelisted keys (fails: fields don't exist yet).
    - server/tests/test_schema_forbid.py (mark `code`): assert TutorFactsIn REJECTS (422 /
      ValidationError) a `profile`/`evidenceDigest` payload carrying a stray coordinate key
      (`x`/`points`) nested inside, and ACCEPTS the fixed-vocabulary shape. RED now because the
      fields aren't declared on TutorFactsIn yet.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/criterion_ema_test.dart test/tutor/payload_nonpii_test.dart 2>&1 | grep -Eiq 'Error|not found|isn.t defined|Expected' && cd server && uv run pytest tests/test_criterion_ema.py tests/test_schema_forbid.py -m code -q 2>&1 | grep -Eiq 'error|failed|no attribute|ImportError' && echo RED-OK</automated>
  </verify>
  <done>The Dart + Python EMA tests reference the SAME fixture rows; both fail by missing symbol. The extended payload guard + schema-forbid test fail because the new fields/keys don't exist yet.</done>
  <acceptance_criteria>
    - criterion_ema_test.dart and test_criterion_ema.py contain byte-identical fixture rows (same prior/outcome/alpha/expected values).
    - test_schema_forbid.py is decorated `@pytest.mark.code` and constructs a `profile`/`evidenceDigest` payload with a nested `x`/`points` key expecting rejection.
    - payload_nonpii_test.dart references `profile`, `evidenceDigest`, `_profileKeys`, `_evidenceDigestKeys`.
    - `uv run pytest -m code` collects the two new server tests (no collection error other than the intended missing-symbol failures).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Server evidence/compiler/eval RED tests + gold-set fixture (R7,R8,R9)</name>
  <files>server/tests/test_evidence.py, server/tests/test_compile_profiles.py, server/tests/test_eval/test_selection_dimension.py, server/tests/test_eval/selection_gold_set.jsonl</files>
  <read_first>
    - server/tests/test_eval/run_eval.py (DIMENSIONS registry ~lines 55-65, JUDGE_GATED_DIMENSIONS ~72-78, score_eval_set ~253-278)
    - server/tests/test_eval/gold_set.jsonl (JSONL scenario shape to mirror)
    - server/app/schema.py (TutorFactsIn — the source of the facts the evidence deriver reads)
    - server/app/curriculum_data/generate.py (derive-from-assets entrypoint the compiler mirrors)
    - .planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-RESEARCH.md (§Firestore Evidence + Profile Layout; §Cross-letter Evidence; §Nightly Job)
  </read_first>
  <action>
    Author the server RED tests + the signed:false selection gold set:
    - test_evidence.py (mark `code`): assert `append_evidence`/`evidence_rows_from_facts` (missing —
      `server/app/evidence.py`) turns a `writeWord` fact for باب into per-letter×criterion rows for
      EVERY letter it touches (baa AND alif), each row `{letter, criterion, passed, source, ts}` with
      `source == "word"` for the coarse per-letter signal (present/correct/dot), and turns an
      isolated-letter fact into 5-criteria rows with `source == "letter"`. Use `fake`/in-memory
      Firestore or assert the derived rows directly (no live network). RED by missing module.
    - test_compile_profiles.py (mark `code`): assert `compile_profiles.compile_child` (missing —
      `server/app/jobs/compile_profiles.py`) over a MULTI-LETTER evidence fixture emits derived-only
      `{strengths[], struggles[], perCriterion{}, schemaVersion}`; a SECOND-letter fixture proves a
      newly-signed letter needs ZERO schema change (same code path, new key `alif/dot`); and a
      PII/token guard asserts the profile doc keys are only fixed-vocabulary (no x/y/points/childName).
    - test_eval/test_selection_dimension.py (mark `code`): assert `run_eval` exposes a
      `selection_policy` dimension in DIMENSIONS and that `score_eval_set` over selection_gold_set.jsonl
      returns a selection score ≥ `SELECTION_THRESHOLD` (provisional, signed:false). RED because the
      dimension is not registered yet.
    - selection_gold_set.jsonl: ≥3 scenarios (fail-streak, returning-child, boredom-trap), each a JSON
      line mirroring gold_set.jsonl with `"signed": false`, the facts, the expected-pick property, and
      a teacher-justification target. Content is signed:false pending mother sign-off (18-11).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_evidence.py tests/test_compile_profiles.py tests/test_eval/test_selection_dimension.py -m code -q 2>&1 | grep -Eiq 'error|failed|no module|ImportError|no attribute' && test -s tests/test_eval/selection_gold_set.jsonl && echo RED-OK</automated>
  </verify>
  <done>The three server tests fail by missing module/attribute; selection_gold_set.jsonl exists with ≥3 signed:false scenarios covering fail-streak / returning-child / boredom-trap.</done>
  <acceptance_criteria>
    - Each server test is decorated `@pytest.mark.code` and fails by missing `server/app/evidence.py` or `server/app/jobs/compile_profiles.py` or the unregistered `selection_policy` dimension.
    - test_evidence.py asserts BOTH the coarse word-signal (`source == "word"`, present/correct/dot) AND the 5-criteria isolated-letter rows (`source == "letter"`).
    - test_compile_profiles.py includes a second-letter (alif) fixture asserting zero-schema-change.
    - `grep -c signed selection_gold_set.jsonl` ≥ 3 and every line contains `"signed": false`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test authoring | No runtime boundary crossed; tests are the executable contract for later boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Information disclosure | non-PII wire + profile doc | mitigate | payload_nonpii + test_schema_forbid RED tests pin the fixed-vocabulary key sets and the `extra=forbid`/coordinate-key rejection BEFORE any field ships |
| T-18-01-02 | Tampering | rails / agent picks | mitigate | selection_rails_property_test seeds the "100% legal, illegal→walker" contract before the policy exists |
| T-18-01-SC | Tampering | package installs | accept | Phase 18 adds NO packages (RESEARCH §Package Legitimacy Audit); glados rejected on analyzer-9 grounds — plain flutter_test only. No install task in this plan. |
</threat_model>

<verification>
- `flutter test test/tutor/ test/core/scoring/criterion_ema_test.dart` — all Phase-18 client tests present and RED.
- `cd server && uv run pytest -m code -q` — new server tests collected and RED; the pre-existing suite otherwise unchanged.
- No fabricated always-true assertions (each failure is a missing symbol / unmet expectation).
</verification>

<success_criteria>
Every R1..R9 requirement plus the EMA-parity and D-14 guard has a RED automated test that
downstream plans will turn GREEN with zero test edits (the 15-01/17-01 "zero test edits" contract).
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-01-SUMMARY.md` when done.
</output>
