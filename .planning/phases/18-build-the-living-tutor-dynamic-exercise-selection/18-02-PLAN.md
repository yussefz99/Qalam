---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - assets/curriculum/exercises.json
  - assets/curriculum/curriculum_graph.json
  - server/app/curriculum_data/generate.py
  - server/app/curriculum_data/curriculum_graph.json
  - server/app/curriculum_data/exercises.json
autonomous: true
requirements: [SPEC-18-R3, SPEC-18-R7]
must_haves:
  truths:
    - "Every exercise in exercises.json carries a `letters` list and a `criteria` list (the cross-letter DATA model, R7)."
    - "baa's three criteria each have a mother-authorable `microDrill` exercise (dot / bowl / start), signedOff:false, enrichment (never gates the star)."
    - "Each microDrill graph node is a `microDrill` competency, essential:false, and passes isLegalSelection/G5/G6 with zero rail change (D-06)."
    - "The server curriculum copy is DERIVED from the assets via generate.py — never hand-edited — and carries the new labels + microDrill nodes."
  artifacts:
    - path: "assets/curriculum/exercises.json"
      provides: "letters+criteria labels on every exercise + baa microDrill exercises"
      contains: "microDrill"
    - path: "assets/curriculum/curriculum_graph.json"
      provides: "criterion-tagged microDrill enrichment nodes (signedOff:false)"
      contains: "microDrill"
    - path: "server/app/curriculum_data/curriculum_graph.json"
      provides: "server copy re-derived from the signed asset (baa.* + microDrill.* only)"
      contains: "microDrill"
  key_links:
    - from: "assets/curriculum/exercises.json"
      to: "server/app/curriculum_data/exercises.json"
      via: "generate.py derive-from-assets (no hand-edit)"
      pattern: "microDrill"
    - from: "microDrill node.criterion"
      to: "scorer criterion names (strokeCount/strokeOrder/shape/direction/dot)"
      via: "criterion-tag == scorer criterion name"
      pattern: "dot|shape|direction"
---

<objective>
Author the cross-letter DATA model and the micro-drill CONTENT — the one pedagogy ask,
scheduled EARLY (D-07). Add `letters`+`criteria` labels to every exercise (R7), author baa's
3-criterion micro-drill set (dot / bowl / start) as REAL enrichment graph nodes (D-06/R3), and
re-derive the server copy from the signed asset via generate.py. All new content ships
`signedOff:false`; the mother's sign-off is a HUMAN-UAT gate batched at 18-11 — never a
mid-phase blocker.

Purpose: give the selection policy real drill nodes to inject and give the compiler real
per-letter×criterion labels; keep the schema all-letters (a newly signed letter needs zero
schema change).
Output: labelled exercises.json, criterion-tagged microDrill nodes, re-derived server copy.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-CONTEXT.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-RESEARCH.md
@docs/curriculum/national-curriculum-grade1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: letters+criteria labels + microDrill exercises (signedOff:false)</name>
  <files>assets/curriculum/exercises.json</files>
  <read_first>
    - assets/curriculum/exercises.json (existing baa exercise shape — id/type/skill/prompt/surface/expected/check/feedback/signedOff, lines ~9-42; the per-exercise signedOff at lines ~5,41)
    - server/app/schema.py (CriterionIn — the five criterion names: strokeCount/strokeOrder/shape/direction/dot — the exact vocabulary `criteria` labels must use)
    - .planning/sketches/002-just-this-part/index.html (the Spotlight micro-drill target experience, D-05)
  </read_first>
  <action>
    (a) Add two label arrays to EVERY exercise object: `letters` (the letter ids the exercise
    touches — e.g. `["baa"]` for isolated baa; `["baa","alif"]` for a word like باب) and `criteria`
    (the scorer criteria it exercises, drawn ONLY from the fixed set strokeCount/strokeOrder/shape/
    direction/dot). Isolated-letter exercises list the criteria they score; word/sequence exercises
    list the coarser signal they can honestly produce (present/correct/dot per Pitfall 3 — do NOT
    fabricate the 5 geometric criteria for a word). These are DATA labels; no behavior yet.
    (b) Author baa's micro-drill set: 3-5 new exercise objects of a NEW `type: "microDrill"`, one per
    named criterion (dot placement, bowl depth/shape, start point). Each carries: `id`
    (`baa.microDrill.dot`, `baa.microDrill.bowl`, `baa.microDrill.start`), `letters: ["baa"]`,
    `criteria` = the single target criterion, a `spotlightZone` field naming the lit region
    (fixed-vocabulary string == the criterion: "dot"/"bowl"/"start"), the child still WRITES on the
    existing canvas/scorer surface (D-05 — reuse the WriteSurface `surface`, no new interaction), warm
    provisional `prompt`/`feedback` copy in the tutor's voice, and `signedOff: false`. The framing copy
    is provisional (D-03) — named step-down, never fake cheer.
    Keep JSON valid; do not touch any signed:true reference-stroke data.
  </action>
  <verify>
    <automated>python3 -c "import json;d=json.load(open('assets/curriculum/exercises.json'));ex=d if isinstance(d,list) else d.get('exercises',d);import sys;items=ex.values() if isinstance(ex,dict) else ex;items=list(items);assert all('letters' in e and 'criteria' in e for e in items),'missing labels';md=[e for e in items if e.get('type')=='microDrill'];assert len(md)>=3,'need >=3 microDrill';assert all(e.get('signedOff') is False for e in md),'microDrill must be signedOff:false';assert all('spotlightZone' in e for e in md),'microDrill needs spotlightZone';print('OK',len(md),'drills')"</automated>
  </verify>
  <done>Every exercise has letters+criteria; ≥3 baa microDrill exercises exist with a spotlightZone and signedOff:false; JSON parses; no signed reference data altered.</done>
  <acceptance_criteria>
    - `python3 -m json.tool assets/curriculum/exercises.json` succeeds (valid JSON).
    - Every exercise object contains `letters` (list) and `criteria` (list drawn from strokeCount/strokeOrder/shape/direction/dot).
    - There are ≥3 objects with `"type": "microDrill"`, ids `baa.microDrill.{dot,bowl,start}`, each `"signedOff": false` and carrying a `spotlightZone`.
    - No word/sequence exercise lists the 5 geometric criteria (it uses the coarse present/correct/dot signal).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: criterion-tagged microDrill enrichment nodes (D-06)</name>
  <files>assets/curriculum/curriculum_graph.json</files>
  <read_first>
    - assets/curriculum/curriculum_graph.json (node shape exerciseId/competency/tier/minCleanReps ~lines 44-49; the enrichment competency examples wordBuilding/grammarTransform ~lines 31-40; the top-level signedOff ~line 9)
    - lib/curriculum/curriculum_graph.dart (essentialNodes derivation — enrichment competencies with essential:false are excluded from the 70/30 star core; isLegalSelection covers arbitrary nodes)
  </read_first>
  <action>
    Add one graph node per microDrill exercise from Task 1. Each node: `exerciseId`
    (`baa.microDrill.dot` etc.), a NEW `microDrill` competency, `tier: null` (not on the إملاء writing
    ramp — mirrors recognize/recall nodes), `minCleanReps: 1` (provisional), a `criterion` tag equal
    to the target criterion (the field the policy reads to map a dominant failing criterion → its
    drill), and the node is ENRICHMENT (`essential: false` OR a competency `essentialNodes` excludes —
    mirror wordBuilding/grammarTransform) so it NEVER gates the one quiet star. Keep the file-level
    `signedOff: false` until the mother signs (18-11). Do not change any existing baa node, tier, or
    prerequisite — microDrills are ADDITIVE nodes only (zero rail change, D-06).
  </action>
  <verify>
    <automated>python3 -c "import json;g=json.load(open('assets/curriculum/curriculum_graph.json'));nodes=g.get('nodes',g if isinstance(g,list) else []);md=[n for n in nodes if n.get('competency')=='microDrill'];assert len(md)>=3,'need >=3 microDrill nodes';assert all(n.get('criterion') for n in md),'node needs criterion tag';assert all(n.get('essential') is False or n.get('tier') is None for n in md),'microDrill must be enrichment';print('OK',[n['exerciseId'] for n in md])"</automated>
  </verify>
  <done>≥3 microDrill nodes exist, each criterion-tagged, enrichment (never essential), additive-only; existing nodes untouched; file stays signedOff:false.</done>
  <acceptance_criteria>
    - `python3 -m json.tool assets/curriculum/curriculum_graph.json` succeeds.
    - Every `microDrill`-competency node has a `criterion` field equal to one of dot/bowl(shape)/start(strokeOrder) and is enrichment (essential:false / tier:null).
    - `git diff assets/curriculum/curriculum_graph.json` shows only ADDED nodes (no edits to existing baa node lines).
    - The top-level `signedOff` remains `false`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: re-derive the server curriculum copy (generate.py)</name>
  <files>server/app/curriculum_data/generate.py, server/app/curriculum_data/curriculum_graph.json, server/app/curriculum_data/exercises.json</files>
  <read_first>
    - server/app/curriculum_data/generate.py (the derive-from-assets entrypoint — _regenerate_graph ~lines 72-85, authored-id/label derivation ~lines 48-66, the baa-filter `startswith("baa.")` ~line 80, module __main__ ~lines 88-97)
    - server/app/curriculum.py (how the server loads CURRICULUM_GRAPH + tier_of/reachable_tiers/prerequisites_met — the microDrill nodes must load cleanly)
  </read_first>
  <action>
    Extend generate.py so the derived server copy carries the new content: (a) the baa-filter that
    selects node ids must also admit `baa.microDrill.*` ids; (b) the derivation must preserve the new
    `criterion` tag on microDrill nodes and the `letters`+`criteria` labels on exercises. Keep the
    invariant: the server copy is DERIVED, never hand-edited (the file header). Run
    `python -m app.curriculum_data.generate` (or the module's regenerate entrypoint) to regenerate
    `curriculum_graph.json` + `exercises.json` under server/app/curriculum_data/. Confirm baa-only +
    microDrill ids present; no non-baa letters leak (D-11).
  </action>
  <verify>
    <automated>cd server && python -m app.curriculum_data.generate 2>&1 | tail -2 && python3 -c "import json;g=json.load(open('app/curriculum_data/curriculum_graph.json'));nodes=g.get('nodes',g if isinstance(g,list) else []);md=[n for n in nodes if n.get('competency')=='microDrill'];assert len(md)>=3,'server copy missing microDrill nodes';assert all(n.get('criterion') for n in md);print('OK server copy',len(md))"</automated>
  </verify>
  <done>generate.py regenerates the server copy from the signed asset; the server curriculum_graph.json + exercises.json carry the microDrill nodes + labels; baa-only preserved; no hand-edit to the derived files.</done>
  <acceptance_criteria>
    - Running the generate entrypoint exits 0 and rewrites server/app/curriculum_data/{curriculum_graph,exercises}.json.
    - The derived curriculum_graph.json contains the same ≥3 microDrill nodes with criterion tags as the asset.
    - No node id outside `baa.` / `baa.microDrill.` appears in the derived server graph (D-11 baa-only).
    - `cd server && uv run pytest -m code -q` shows the curriculum still loads (no regression in server graph tests).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored asset → derived server copy | generate.py is the only writer of the server copy; hand-edits are the tampering risk |
| curriculum content → child | unsigned content must never gate the star or ship as authoritative pedagogy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | server curriculum copy | mitigate | Server copy is DERIVED via generate.py (never hand-edited); the derive step + baa-only assertion catch drift |
| T-18-02-02 | Spoofing (pedagogy) | unsigned drill content | mitigate | All microDrill content ships `signedOff:false`; enrichment (essential:false) so it can never gate the one quiet star; mother sign-off is the HUMAN-UAT flip at 18-11 |
| T-18-02-03 | Information disclosure | criteria/letters labels | mitigate | Labels are fixed-vocabulary curriculum constants (letter ids + criterion names) — non-PII by construction |
</threat_model>

<verification>
- `python3 -m json.tool` on both assets — valid JSON.
- The generate entrypoint re-derives the server copy; `uv run pytest -m code` server graph tests stay green.
- `git diff` on curriculum_graph.json shows additive-only node changes.
</verification>

<success_criteria>
The cross-letter DATA model (labels) and the baa micro-drill set (real enrichment nodes,
signedOff:false) exist in the asset and are faithfully derived to the server — ready for the
policy (18-04) to inject drills and the compiler (18-09) to aggregate per-letter×criterion.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-02-SUMMARY.md` when done.
</output>
