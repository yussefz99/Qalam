---
phase: 18-build-the-living-tutor-dynamic-exercise-selection
plan: 06
type: execute
wave: 4
depends_on: [18-03, 18-04, 18-05]
files_modified:
  - lib/tutor/tutor_facts.dart
  - lib/tutor/tutor_facts_builder.dart
  - lib/data/child_model_repository.dart
  - lib/data/arc_state_repository.dart
  - lib/data/evidence_repository.dart
  - lib/tutor/child_model_providers.dart
  - test/tutor/payload_nonpii_test.dart
autonomous: true
requirements: [SPEC-18-R2, SPEC-18-R6]
must_haves:
  truths:
    - "TutorFacts gains `profile` + `evidenceDigest`, emitted omit-when-null, key names byte-for-byte matching server/app/schema.py (422 lockstep, client SECOND)."
    - "The non-PII payload guard is EXTENDED to whitelist profile + evidenceDigest and their nested keys, and still trips on a coordinate/PII key."
    - "ChildModelRepository reads the profile Firestore-first with a Drift-mirror fallback via a one-shot .get() — and NEVER blocks the practice path (Req 6/D-16): boot reads the mirror synchronously, the refresh is fire-and-forget."
    - "The profile-mirror provider is a hand-written FutureProvider (not @riverpod codegen) — Drift-returning codegen throws InvalidTypeException (Pitfall 6)."
    - "Arc state + offline evidence bridge from the Drift primitives (18-03) to the pure value types (18-04) via thin repositories."
  artifacts:
    - path: "lib/data/child_model_repository.dart"
      provides: "Firestore-first + Drift-mirror profile read (D-16), non-blocking"
      contains: "ChildModelSnapshot"
    - path: "lib/tutor/tutor_facts.dart"
      provides: "profile + evidenceDigest wire fields (omit-when-null)"
      contains: "evidenceDigest"
  key_links:
    - from: "lib/tutor/tutor_facts.dart toMap()"
      to: "server/app/schema.py TutorFactsIn"
      via: "byte-for-byte key names, omit-when-null, extra=forbid mirror"
      pattern: "evidenceDigest|profile"
    - from: "ChildModelRepository.get()"
      to: "childProfileMirror (Drift) + child_models/{uid} (Firestore)"
      via: "one-shot .get() write-through, fire-and-forget refresh"
      pattern: "child_models"
---

<objective>
Ship the CLIENT half of the wire (422 lockstep, SECOND) and the profile-read plumbing: add
`profile`+`evidenceDigest` to `TutorFacts` (mirroring the server fields byte-for-byte), extend the
non-PII guard, and build the `ChildModelRepository` (Firestore-first with a Drift-mirror fallback,
D-16) plus the thin arc-state / evidence repositories that bridge the Drift primitives (18-03) to
the pure value types (18-04). The practice path NEVER blocks on the model (Req 6).

Purpose: the returning-child memory (Req 2) becomes real — the compiled profile mirrors into Drift
at boot and rides the next session's facts — while the boot read stays instant and offline-safe.
Output: enlarged TutorFacts + guard, ChildModelRepository + repos + hand-written providers.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-RESEARCH.md
@.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TutorFacts +profile +evidenceDigest + non-PII guard extend (D-14)</name>
  <files>lib/tutor/tutor_facts.dart, lib/tutor/tutor_facts_builder.dart, test/tutor/payload_nonpii_test.dart</files>
  <behavior>
    - a built TutorFacts with a profile + digest serializes exactly {strengths,struggles,perCriterion,schemaVersion} + [{letter,criterion,pass,fail}] and no coordinate/PII key
    - an unchanged (no profile/digest) payload byte-matches the prior shape (omit-when-null)
    - the payload guard still trips on x/y/points/strokes/offset/childName/nickname
  </behavior>
  <read_first>
    - lib/tutor/tutor_facts.dart (the field-add + omit-when-null toMap pattern lines 139-149,197-213; the strokeDiff/criteria/weakestCriterion precedent)
    - server/app/schema.py (ChildProfileIn / EvidenceDigestRowIn key names from 18-05 — mirror byte-for-byte)
    - lib/tutor/tutor_facts_builder.dart (the derive-in-the-chokepoint pattern lines 40-77; clearedTiers/clearedCompetencies passed-through lines 46-47)
    - test/tutor/payload_nonpii_test.dart (the extended _whitelist / _profileKeys / _evidenceDigestKeys from 18-01)
    - lib/curriculum/child_model_snapshot.dart (the pure type the profile field carries)
  </read_first>
  <action>
    Add to `TutorFacts`: `final Map<String,Object?>? profile;` (a fixed-vocabulary map
    {strengths,struggles,perCriterion,schemaVersion}) and `final List<Map<String,Object?>>?
    evidenceDigest;` (rows {letter,criterion,pass,fail}). Emit BOTH omit-when-null in toMap(), key
    names byte-for-byte matching server/app/schema.py (`profile`, `evidenceDigest`). Thread them in
    `buildTutorFacts` as pass-through pure values (from the ChildModelSnapshot mirror + the Drift
    evidence digest) — NO new stroke/Offset parameter (the signature stays the non-PII guard). Turn the
    extended payload_nonpii_test GREEN with zero test edits (the whitelist + nested-key sets already
    added in 18-01). The 422 lockstep: these keys match the server fields shipped in 18-05 — client
    ships SECOND; the single live re-deploy is 18-11.
  </action>
  <verify>
    <automated>flutter test test/tutor/payload_nonpii_test.dart</automated>
  </verify>
  <done>TutorFacts carries profile + evidenceDigest (omit-when-null, server-mirrored keys); the extended non-PII guard passes; the builder threads them as pure pass-through with no new PII-capable parameter.</done>
  <acceptance_criteria>
    - `flutter test test/tutor/payload_nonpii_test.dart` passes with zero test edits.
    - `grep -q "'evidenceDigest'" lib/tutor/tutor_facts.dart` and `grep -q "'profile'" lib/tutor/tutor_facts.dart`, both guarded by `if (... != null)` (omit-when-null).
    - `buildTutorFacts` signature gained NO stroke/Offset/point parameter (grep the signature).
    - The emitted key names + digest row keys byte-match server/app/schema.py (profile/evidenceDigest/letter/criterion/pass/fail).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChildModelRepository (Firestore-first + Drift mirror, D-16) + arc/evidence repos</name>
  <files>lib/data/child_model_repository.dart, lib/data/arc_state_repository.dart, lib/data/evidence_repository.dart, lib/tutor/child_model_providers.dart</files>
  <behavior>
    - boot reads the Drift mirror synchronously (last-known profile, offline-safe) → ChildModelSnapshot
    - a background one-shot Firestore .get() on child_models/{uid} refreshes + write-throughs the mirror; a permission-denied/offline error keeps the last-known mirror (no throw, no block)
    - arc_state_repository maps the Drift ArcStateRow ↔ the pure ArcState (18-04)
    - evidence_repository builds the fixed-vocabulary evidenceDigest {letter,criterion,pass,fail} from unsynced Drift evidence, and clears synced rows (rollup cap)
  </behavior>
  <read_first>
    - lib/data/curriculum_repository.dart (the Firestore-first one-shot .get() with fallback idiom lines 68-93,151-182,358-389 — the fallback here is the Drift mirror, not the bundle)
    - lib/data/child_profile_repository.dart (the thin repo + keepAlive provider shape lines 17-49)
    - lib/data/app_database.dart (the getProfileMirror/setProfileMirror/getArcStateRow/setArcStateRow/unsyncedEvidence/clearEvidence primitive accessors from 18-03)
    - lib/curriculum/child_model_snapshot.dart + lib/curriculum/arc_state.dart (the pure types the repos map to)
    - lib/data/graph_position_repository.dart (the Drift-primitive ↔ pure value-type bridge precedent, 15-04)
  </read_first>
  <action>
    Author `ChildModelRepository` (Firestore-first, D-16): `Future<ChildModelSnapshot> get(String
    uid)` reads the Drift mirror and returns it immediately; `Future<void> refresh(String uid)` does a
    ONE-SHOT `child_models/{uid}.get()`, maps a non-empty doc into a ChildModelSnapshot + write-throughs
    the Drift mirror, and on any throw/permission-denied/offline keeps the last-known mirror (never
    throws, never blocks). Inject FirebaseFirestore via a `.withFirestore` seam (fake_cloud_firestore
    tests, curriculum_repository precedent). CRITICAL: no `.get()` on the selection/practice path —
    boot reads the mirror; refresh is fire-and-forget.
    Author `ArcStateRepository` (maps ArcStateRow ↔ pure ArcState) and `EvidenceRepository`
    (unsyncedEvidence → the {letter,criterion,pass,fail} digest; clearEvidence after the digest sends).
    Author `lib/tutor/child_model_providers.dart`: a HAND-WRITTEN `FutureProvider` for the profile
    mirror (childModelProvider) + keepAlive `Provider`s for the repos — NOT @riverpod codegen (a
    Drift-data-class-returning codegen provider throws InvalidTypeException, Pitfall 6 / Phase-05
    childProfileProvider precedent). Never a bare StreamProvider.future (Riverpod-3 hang).
  </action>
  <verify>
    <automated>flutter test test/tutor/across_session_memory_test.dart test/data/ 2>&1 | tail -8</automated>
  </verify>
  <done>ChildModelRepository reads Firestore-first with a Drift-mirror fallback and never blocks; arc/evidence repos bridge to the pure types; the profile provider is a hand-written FutureProvider; across_session_memory_test passes (the profile threads into the facts).</done>
  <acceptance_criteria>
    - `flutter test test/tutor/across_session_memory_test.dart` passes (seeded profile → outgoing facts carry it).
    - child_model_repository.dart does a single `.get()` (grep) and its refresh path is not awaited on the selection path (the provider does not `await refresh` before returning the mirror).
    - child_model_providers.dart uses `FutureProvider`/`Provider` (grep) — no `@riverpod` on a Drift-returning provider, no `StreamProvider.future`.
    - `flutter test test/data/` stays green; a `.withFirestore` injection seam exists for fake_cloud_firestore.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /coach wire | the enlarged payload must carry only fixed-vocabulary non-PII keys (extra=forbid on the server 422s the rest) |
| Firestore profile read | the client reads only its OWN profile doc (owner-scoped rule from 18-05) |
| practice path | must never block on a Firestore round-trip (Req 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Information disclosure | new wire fields | mitigate | profile/evidenceDigest emitted omit-when-null with server-mirrored fixed-vocab keys; the extended payload_nonpii guard trips on any coordinate/PII key; extra=forbid is the server teeth |
| T-18-06-02 | Denial of service (UX) | profile .get() on the practice path | mitigate | boot reads the Drift mirror synchronously; refresh is fire-and-forget; a permission-denied/offline error keeps the last-known mirror (no throw, no block) |
| T-18-06-03 | Tampering (422 window) | client ships field before server | mitigate | server shipped FIRST in 18-05; the client mirror here is byte-for-byte; the single re-deploy is gated at 18-11 |
| T-18-06-04 | Elevation of privilege | reading another account's profile | mitigate | the owner-scoped read rule (18-05) + per-uid Drift file (forAccount) — the repo only ever queries child_models/{currentUid} |
</threat_model>

<verification>
- `flutter test test/tutor/payload_nonpii_test.dart test/tutor/across_session_memory_test.dart test/data/` — green.
- `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` — lib/data may use Firebase/drift (allowed); the pure lib/curriculum types stay pure.
- grep confirms omit-when-null wire emission, single-.get() refresh, hand-written providers.
</verification>

<success_criteria>
The client carries the final wire shape (422-safe) and reads the compiled profile Firestore-first
with a Drift-mirror fallback that never blocks the practice path — returning-child memory is wired
end-to-end on the client side, ready for the selector (18-07) to consume.
</success_criteria>

<output>
Create `.planning/phases/18-build-the-living-tutor-dynamic-exercise-selection/18-06-SUMMARY.md` when done.
</output>
