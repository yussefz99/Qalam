---
phase: 09-parent-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/services/pin_service_test.dart
  - test/router/parent_gate_test.dart
  - test/screens/parent_dashboard_test.dart
  - test/data/app_database_test.dart
  - lib/l10n/app_en.arb
autonomous: true
requirements: [S1-11]
must_haves:
  truths:
    - "A failing test exists for every Phase-9 security and UI behavior before any implementation (Nyquist RED contract)"
    - "PIN hash/verify, persisted cooldown, route gate, and read-only dashboard each have an executable assertion that is RED today"
    - "All Phase-9 ARB copy keys exist so downstream screens compile against real l10n, not hardcoded strings"
  artifacts:
    - path: "test/services/pin_service_test.dart"
      provides: "RED PinService hash/verify/salt-randomness/constant-time + persisted-cooldown contract (S1-11, T-09-01/T-09-02)"
    - path: "test/router/parent_gate_test.dart"
      provides: "RED /parent gate: locked-by-default, reachable after PIN, per-entry relock, obscured field (T-09-03)"
    - path: "test/screens/parent_dashboard_test.dart"
      provides: "RED dashboard: summary 'N of M', per-letter rows, empty state, no edit/delete (T-09-04)"
    - path: "test/data/app_database_test.dart"
      provides: "Extended with allMastered()/allInProgress() read-accessor assertions"
    - path: "lib/l10n/app_en.arb"
      provides: "All Phase-9 copy keys (parentTitle, parentPin*, parentSummary, parentRow*, parentEmpty*, commonDone)"
  key_links:
    - from: "test/services/pin_service_test.dart"
      to: "lib/features/parent/pin_service.dart"
      via: "import of the not-yet-created PinService (RED by missing symbol)"
      pattern: "import.*features/parent/pin_service"
    - from: "test/router/parent_gate_test.dart"
      to: "lib/providers/parent_providers.dart"
      via: "ParentGate provider override (RED by missing symbol)"
      pattern: "parentGateProvider|ParentGate"
---

<objective>
Lay the Phase-9 RED contract: write the failing tests for every PIN, cooldown, route-gate,
and read-only-dashboard behavior BEFORE any implementation (Nyquist), and add every
user-facing ARB copy key so downstream screens compile against real l10n. This is the
Wave-0 failing-test slice mandated by 09-VALIDATION.md, mirroring the Phase 05 RED-first
structure (05-01).

Purpose: Make the goal observable as executable assertions first — every subsequent plan
turns these RED tests GREEN. No production logic ships in this plan.
Output: 3 new test files + 1 extended test file (all RED) + ~17 new ARB keys.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-parent-dashboard/09-VALIDATION.md
@.planning/phases/09-parent-dashboard/09-RESEARCH.md
@.planning/phases/09-parent-dashboard/09-UI-SPEC.md
@.planning/phases/09-parent-dashboard/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test files for PIN service, route gate, and dashboard</name>
  <files>test/services/pin_service_test.dart, test/router/parent_gate_test.dart, test/screens/parent_dashboard_test.dart</files>
  <read_first>
    - test/router/onboarding_gate_test.dart — the gate/route widget-test analog (router + ProviderScope override + refreshListenable assertions); mirror its structure for parent_gate_test.dart
    - test/providers/progression_providers_test.dart — Drift-typed provider test analog (in-memory AppDatabase, overrides); mirror for dashboard provider seeding
    - test/data/app_database_test.dart — the in-memory-AppDatabase "simulated restart" shape (D-09: a new AppDatabase over the same injected executor) — reuse for the persisted-cooldown test
    - .planning/phases/09-parent-dashboard/09-RESEARCH.md lines 156-233 — PinService Pattern 1 (PBKDF2 hash/verify/salt/constant-time) and Pattern 2 (persisted cooldown) the tests must pin
    - .planning/phases/09-parent-dashboard/09-VALIDATION.md lines 43-62 — Per-Task Verification Map + Wave-0 Requirements (exact behaviors and file names)
    - .planning/phases/09-parent-dashboard/09-UI-SPEC.md lines 190-235 — exact copy + states the dashboard/gate tests assert against
    - lib/providers/profile_providers.dart — note: tests must `import 'package:drift/drift.dart' hide isNull, isNotNull;` alongside flutter_test (STATE.md matcher-collision rule)
  </read_first>
  <action>
    Write three RED test files importing the not-yet-existing symbols (compile-RED by missing symbol is acceptable for Wave 0; mark each with a top comment "// RED — implemented in 09-02/09-03").

    pin_service_test.dart (T-09-01 / T-09-02, S1-11): assert against `PinService` from lib/features/parent/pin_service.dart over an in-memory AppDatabase —
    (a) setPin then verify with the same 4-digit PIN returns true; verify with a wrong PIN returns false;
    (b) the stored value at AppSettings key `parentPinHash` is NEVER equal to the plaintext PIN (hashed);
    (c) two setPin calls with the SAME PIN produce DIFFERENT stored hashes (random per-install salt at key `parentPinSalt`);
    (d) isPinSet returns false before setPin, true after;
    (e) PERSISTED COOLDOWN: 5 consecutive failed verifies set a non-null `parentPinLockUntil`, and that lockUntil SURVIVES a simulated restart — construct a second AppDatabase over the SAME injected executor (the D-09 shape) and assert remainingCooldown is still > Duration.zero (this is the single most important security assertion).
    Name the cooldown helpers per research: registerFailure, registerSuccess, remainingCooldown.

    parent_gate_test.dart (T-09-03, S1-11): pump the app router (mirror onboarding_gate_test.dart) with a fresh in-memory AppDatabase and a ParentGate seeded locked —
    (a) navigating to '/parent' while `parentGateProvider` is locked does NOT render the dashboard (renders the PIN flow / does not show the summary line);
    (b) after a correct PIN flips the gate unlocked, '/parent' renders the dashboard;
    (c) the "Done" control relocks (parentGate.unlocked == false) and a second '/parent' entry re-prompts the PIN (per-entry, D-07);
    (d) the PIN entry field is obscured (find a TextField with obscureText == true) and numeric.

    parent_dashboard_test.dart (T-09-04, S1-11): pump ParentDashboardScreen with `parentProgressProvider` overridden to a seeded ParentProgress —
    (a) the summary renders "{mastered} of {total} letters mastered" matching seeded counts (assert the ARB-resolved string, do NOT hardcode 28);
    (b) one mastered + one in-progress letter each render a row with the correct status label and clean-reps;
    (c) with empty rows, the empty-state copy (parentEmptyTitle / parentEmptyBody) shows, never a spinner or error;
    (d) READ-ONLY: assert there is NO edit/delete/reset affordance (no IconButton/TextButton matching delete/edit/reset semantics).
  </action>
  <verify>
    <automated>C:\Users\yusse\.vscode\flutter\bin\flutter.bat test test/services/pin_service_test.dart test/router/parent_gate_test.dart test/screens/parent_dashboard_test.dart 2>&1 | grep -qiE "compilation error|Error: .*pin_service|some tests failed|-1: " && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and reference PinService, parentGateProvider/ParentGate, ParentDashboardScreen, parentProgressProvider, and AppSettings keys parentPinHash/parentPinSalt/parentPinFailCount/parentPinLockUntil by their exact research names.
    - The suite is RED (fails to compile or fails assertions) because the implementation does not yet exist — verified by the command above reporting failure/compile-error.
    - The persisted-cooldown test constructs a SECOND AppDatabase over the SAME injected executor (simulated restart) and asserts the cooldown survives.
    - No production lib/ files (other than the ARB in Task 2) are created or modified in this plan.
  </acceptance_criteria>
  <done>Three RED test files exist asserting every S1-11 security and UI behavior; the suite fails by missing symbols / assertions, proving the contract is not yet satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Extend app_database_test + add all Phase-9 ARB copy keys</name>
  <files>test/data/app_database_test.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - test/data/app_database_test.dart — existing in-memory AppDatabase test patterns (recordMastery/cleanRepsFor/setCleanReps); add the new aggregate-accessor assertions in the same style
    - lib/data/app_database.dart lines 137-243 — existing accessor block (recordMastery, cleanRepsFor, setCleanReps) the new allMastered()/allInProgress() will mirror; the tests pin their return shape
    - lib/l10n/app_en.arb lines 34-47 (plain-string shape) and lines 263-334 (placeholder shape) — copy these exact JSON shapes for the new keys
    - .planning/phases/09-parent-dashboard/09-UI-SPEC.md lines 190-223 — the exact draft copy strings and placeholder names for every key
    - .planning/phases/09-parent-dashboard/09-PATTERNS.md lines 292-319 — the enumerated key list and placeholder types
  </read_first>
  <action>
    Extend test/data/app_database_test.dart with RED assertions for two new read-only accessors over an in-memory AppDatabase: `allMastered()` returns the seeded LetterMastery rows (with cleanReps + masteredAt) ordered by masteredAt; `allInProgress()` returns only LetterReps rows with cleanReps > 0. These are RED until 09-02 adds the accessors. Verify the Drift data-class names against app_database.g.dart (research used LetterMasteryData / LetterRepData).

    Add these keys to lib/l10n/app_en.arb with @-metadata, copy verbatim from UI-SPEC §Copywriting (owner-owned drafts, per A-04):
    parentTitle ("Parent Area"), parentPinCreatePrompt ("Create a PIN"), parentPinCreateHelp ("Choose a 4-digit PIN to protect this area."), parentPinConfirmPrompt ("Re-enter your PIN to confirm."), parentPinMismatch ("Those didn't match. Try once more."), parentPinNoRecovery (the honest no-recovery line, D-02), parentPinConfirm ("Confirm"), parentPinEnterPrompt ("Enter your PIN"), parentPinWrong ("Incorrect PIN. Try again."), parentPinCooldown ("Too many tries. You can try again in {seconds}s." — int placeholder seconds), parentSummary ("{mastered} of {total} letters mastered" — int placeholders mastered/total), parentRowMastered ("Mastered · {reps} clean reps · {date}" — int reps + String date), parentRowInProgress ("In progress · {reps} clean reps" — int reps), parentEmptyTitle ("No lessons completed yet."), parentEmptyBody ("When your child finishes a letter, it will appear here."), commonContinue ("Continue"), commonDone ("Done"). Reuse commonContinue/commonDone if an equivalent key already exists — grep first; do not duplicate. Run gen-l10n so AppLocalizations regenerates.
  </action>
  <verify>
    <automated>C:\Users\yusse\.vscode\flutter\bin\flutter.bat gen-l10n 2>&1 | grep -viE "warning" ; grep -c "parentTitle\|parentSummary\|parentEmptyTitle\|parentPinNoRecovery\|parentPinCooldown" "c:/Users/yusse/OneDrive/Desktop/Qalam/lib/l10n/app_en.arb"</automated>
  </verify>
  <acceptance_criteria>
    - All 17 enumerated keys (minus any pre-existing common* reused) are present in app_en.arb with correct placeholder types (seconds:int, mastered:int, total:int, reps:int, date:String).
    - gen-l10n regenerates AppLocalizations with no errors; the new getters (l10n.parentTitle, l10n.parentSummary(mastered,total), etc.) exist.
    - app_database_test.dart contains RED assertions for allMastered() and allInProgress() using exact Drift data-class names verified against app_database.g.dart.
    - parentSummary uses {mastered}/{total} placeholders — the denominator is NOT hardcoded to 28 anywhere (Pitfall 5).
  </acceptance_criteria>
  <done>All Phase-9 copy exists as l10n keys with correct placeholders, gen-l10n is clean, and the data-accessor RED assertions are in place — downstream plans compile against real l10n.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child (untrusted) → /parent route | A child attempts to reach the parent area; the PIN gate is the only barrier |
| test fixtures → AppSettings store | Tests seed PIN material; must never assert on or print plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | PIN storage (AppSettings parentPinHash) | mitigate | This plan PINS the contract via RED tests: stored hash ≠ plaintext, per-install random salt → different hashes for same PIN. Implementation in 09-02. |
| T-09-02 | Tampering | brute-force cooldown counter | mitigate | RED test asserts the cooldown PERSISTS across a simulated restart (second AppDatabase over same executor) — encodes the force-quit-bypass defense before any code exists. |
| T-09-03 | Elevation of Privilege | /parent route access | mitigate | RED gate test asserts default-deny: '/parent' does not render the dashboard while locked; reachable only after correct PIN; relocks on exit (D-07). |
| T-09-07 | Information Disclosure | test/log output | mitigate | Tests assert on booleans and hash-inequality only; never print/log the PIN, salt, or hash (no-log convention extended to the test suite). |
| T-09-SC | Tampering | pub add crypto (09-02) | accept | No package install in THIS plan. The crypto package legitimacy is pub.dev first-party (dart.dev team) per 09-RESEARCH Package Legitimacy Audit — verified, OK; install + its checkpoint live in 09-02. |
</threat_model>

<verification>
- `flutter test test/services/ test/router/ test/screens/` is RED (implementation absent) — expected and correct for Wave 0.
- `flutter gen-l10n` succeeds; AppLocalizations exposes all new getters.
- No production lib/ logic added (only the ARB + generated l10n).
</verification>

<success_criteria>
- Every S1-11 behavior (PIN hash/verify, persisted cooldown, route gate, read-only dashboard, empty state) has an executable RED assertion.
- All Phase-9 copy lives in l10n; no hardcoded user strings remain for downstream plans.
- The "N of M" denominator is parameterized via placeholders, never hardcoded.
</success_criteria>

<output>
Create `.planning/phases/09-parent-dashboard/09-01-SUMMARY.md` when done.
</output>
