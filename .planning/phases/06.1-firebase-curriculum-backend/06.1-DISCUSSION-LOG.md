# Phase 06.1: Firebase Curriculum Backend - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-14
**Phase:** 06.1-firebase-curriculum-backend
**Areas discussed:** Runtime data source, Firestore document model, Auth & security, Tooling & authoring path

---

## Runtime data source (offline contract)

| Option | Description | Selected |
|--------|-------------|----------|
| Bundle-only | App reads bundled letters.json at runtime; Firestore author-only + export to bundle. Guarantees offline, zero runtime network. (Claude recommended) | |
| Live Firestore + cache | App reads curriculum live from Firestore with offline persistence + bundled seed for first run. Content updates without app release; adds runtime cloud dependency. | ✓ |

**User's choice:** Live Firestore + cache.
**Notes:** Claude flagged the offline implication — a fresh, no-network install has no curriculum until first sync. Captured as a HARD CONSTRAINT (CONTEXT D-02/D-03): ship a bundled seed snapshot as cold-start fallback AND enable Firestore offline persistence, so PLAT-01 (Phase 10) still holds.

---

## Firestore document model

| Option | Description | Selected |
|--------|-------------|----------|
| Doc-per-letter, nested | One doc per letter; strokes/mistakes/tolerances nested; mirrors letters.json 1:1. (Recommended) | ✓ |
| Letter + subcollections | Strokes/mistakes as Firestore subcollections; more native/queryable but complex round-trip. | |

**User's choice:** Doc-per-letter, nested.
**Notes:** Simplest lossless round-trip; a `lessons` collection mirrors lessons.json.

---

## Auth & security

| Option | Description | Selected |
|--------|-------------|----------|
| Admin-SDK writes, client locked, no end-user Auth | Writes via service account only; rules deny client access; no Firebase Auth in v1. (Claude recommended) | |
| Add Firebase Auth now | Stand up Firebase Auth this phase to gate reads/writes; groundwork for v2 tutor. | ✓ |

**User's choice:** Add Firebase Auth now → after discussion, escalated to **"do the full setup, make Firebase ready for the rest of the project and v2, make auth fully."**
**Notes:** Claude first proposed anonymous-only; user questioned it ("are you sure"), Claude explained anonymous auth is a light gate (not real access control) and that admin-only write rules + App Check are what actually protect content/abuse. User then chose the **full, v2-ready auth foundation**. Resolved (CONTEXT D-09..D-10a) with a HARD child-safety guardrail: **children never log in / no child accounts or PII**. "Full" = real providers enabled (Anonymous + Email/Password + Google), v1 runtime uses anonymous, anonymous→permanent **account linking** architected for v2 upgrade, role-ready (custom-claim) security rules, App Check deferred to Phase 10. Content writes remain admin-only.

---

## Tooling & authoring path

| Option | Description | Selected |
|--------|-------------|----------|
| Firebase Console + Python seed | Python firebase-admin seeds alif + lessons once; ongoing edits in the console. (Recommended) | ✓ |
| Python seed/export scripts only | Seed + snapshot export; edits by re-running scripts from source JSON. | |
| Custom in-app/admin authoring tool | Polished authoring UI; real scope, deferred. | |

**User's choice:** Firebase Console + Python seed.
**Notes:** User questioned "why python scripts — set it up correctly." Clarified the two layers: app wiring uses the official **FlutterFire CLI** (`flutterfire configure`) — the canonical setup, not a script; only the data seed uses a script, and Python `firebase-admin` is mandated by CLAUDE.md ("Python over TypeScript for all tooling") and is the standard bulk-seed path (CONTEXT D-12/D-13/D-14). A refreshable snapshot export keeps the bundled offline fallback current (D-15).

## Claude's Discretion

- Firestore database location/region (permanent — pick low-latency for owner's locale; reuse existing project location if already set; flag before creating).
- `defaultToleranceRamp` / config-doc layout.
- Cache vs seed-fallback ordering in the repository (as long as D-02/D-03 hold).
- Android minSdk/Gradle bumps required by Firebase plugins.

## Deferred Ideas

- Authoring the remaining 27 letters + grammar/sentence questions — Phases 7–8 (owner's-mother sign-off).
- Custom in-app/web authoring tool — Phase 7+.
- Cloud Functions + AI tutor (session history, nightly profile compiler) — v2.
- Crashlytics / monitoring — once Firebase is in.
- Migrating child progress/profile data to Firestore — out of scope; stays local Drift.
