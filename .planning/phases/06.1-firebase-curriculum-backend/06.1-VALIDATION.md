---
phase: "06.1"
slug: firebase-curriculum-backend
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-14
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 06.1-RESEARCH.md §Validation Architecture + §Security Domain.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `flutter_test` (existing) + `fake_cloud_firestore` (new dev dep) + `mocktail` (existing, fallback fake) |
| **Config file** | `test/flutter_test_config.dart` (exists — loads bundled TTFs for goldens) |
| **Quick run command** | `flutter test test/data/curriculum_repository_test.dart` |
| **Full suite command** | `flutter test` |
| **Estimated runtime** | quick ~5s / full ~60s |

---

## Sampling Rate

- **After every task commit:** `flutter test test/data/curriculum_repository_test.dart`
- **After every plan wave:** `flutter test` (full suite)
- **Before `/gsd:verify-work`:** Full suite green
- **Phase gate (manual/device):** seed→export round-trip diff clean + Firestore rules manual/emulator check + **device airplane-mode cold-install smoke (PLAT-01)**.

---

## Per-Task Verification Map

| Req | Wave | Behavior | Test Type | Automated Command | File |
|-----|------|----------|-----------|-------------------|------|
| D-01 | 0/1 | Repo reads letters/lessons from Firestore when present | unit | `flutter test test/data/curriculum_repository_test.dart` (fake_cloud_firestore seeded) | extend existing |
| D-02 | 0/1 | Cold no-network → falls back to bundled JSON | unit | empty/throwing fake → assert bundle letters returned | same |
| D-03 | 1 | Scoring/practice path never blocks on network (one-shot `.get()` cache-served) | unit | offline fake `.get()` returns cached docs, no hang | same |
| D-05 | 0/1 | `validateReferenceStrokes` runs on Firestore source | unit | seed fake with closed-loop stroke → assert `StateError` (mirror `_loopLetterJson`) | same |
| D-06/D-08 | 0/1 | Round-trip parity incl. points `{x,y}`→`[x,y]`, signedOff, empty strokes, ramp | unit | encode alif + 1 skeleton to fake → read → deep-equal vs bundle parse | same |
| D-07 | 1 | `defaultToleranceRamp` survives via `meta/toleranceRamp` doc | unit | seed meta doc → `getDefaultToleranceRamp()` returns it, not default | same |
| D-09b | 1 | Anonymous sign-in completes before first read | widget/unit | auth fake (`firebase_auth_mocks` or mocktail) → assert `currentUser != null` post-boot | new boot test |
| D-10 | manual | Rules: read needs auth, writes denied | manual/emulator | `firebase emulators:exec` rules test OR console check (server-enforced; fakes do NOT evaluate rules) | firestore.rules |
| D-13/D-15 | manual | Python seed + export idempotent, lossless | integration | Python: seed → export → diff against source JSON exits 0 | `tools/firebase/test_roundtrip.py` |

---

## Wave 0 Gaps (must be closed first)

- [ ] Add a `CurriculumRepository.withFirestore(FirebaseFirestore)` injection seam (keep `.fromStrings` for bundle tests).
- [ ] Test fixtures: alif doc (points as `{x,y}` maps) + one skeleton letter (empty strokes) + a `meta/toleranceRamp` doc.
- [ ] Install `fake_cloud_firestore` dev dep (verify compat with resolved `cloud_firestore`; else mocktail fake).
- [ ] Auth fake for boot test — evaluate `firebase_auth_mocks` vs mocktail.
- [ ] Python `test_roundtrip.py` proving seed→export == source JSON.

---

## Justified Manual-Only Items

- **D-10 (security rules):** server-enforced; `fake_cloud_firestore` does not evaluate rules. Validate via the Firebase emulator rules-test or a documented console check.
- **D-13/D-15 (Python tooling):** off-device backend tooling; validated by a Python round-trip diff assertion, not flutter_test.
- **PLAT-01 cold-install airplane-mode smoke:** requires a real device/emulator install with no network — human/device checkpoint.
