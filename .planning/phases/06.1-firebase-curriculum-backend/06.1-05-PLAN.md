---
phase: 06.1-firebase-curriculum-backend
plan: 05
type: execute
wave: 3
depends_on: ["06.1-01", "06.1-03"]
files_modified:
  - firestore.rules
  - firebase.json
  - .firebaserc
  - test/firestore/rules.test.md
autonomous: false
requirements: [CUR-01]
user_setup:
  - service: firebase
    why: "Deploy + verify security rules against qalam-app-bd7d0 (D-10)"
    env_vars: []
    dashboard_config:
      - task: "Confirm deployed rules in the console Rules tab; run the Rules Playground read (auth on/off) + write (denied) checks"
        location: "Firebase Console → qalam-app-bd7d0 → Firestore Database → Rules"

must_haves:
  truths:
    - "Firestore security rules allow reads of letters/lessons/meta only when request.auth != null (anonymous identity OK)"
    - "All client writes to every collection are denied (writes happen only via the admin-SDK tooling)"
    - "An unauthenticated client cannot read curriculum; a signed-in anonymous client can"
    - "The rules carry a documented custom-claim seam for v2 roles (parent/admin/tutor) and do not conflict with adding App Check later"
    - "No child-data collection exists in the rules (no child PII surface)"
  artifacts:
    - path: "firestore.rules"
      provides: "Read-requires-auth, all-client-writes-denied rules with a v2 custom-claim seam"
      contains: "request.auth != null"
    - path: "firebase.json"
      provides: "firestore.rules deploy target"
      contains: "firestore"
    - path: "test/firestore/rules.test.md"
      provides: "Documented manual/emulator rules verification steps (server-enforced; fakes do not evaluate rules)"
      contains: "Rules Playground"
  key_links:
    - from: "firebase.json"
      to: "firestore.rules"
      via: "firestore.rules path target for firebase deploy --only firestore:rules"
      pattern: "firestore.rules"
    - from: "firestore.rules"
      to: "Plan 01 anonymous identity"
      via: "request.auth != null gates curriculum reads to the signed-in anonymous user"
      pattern: "allow read: if request.auth != null"
---

<objective>
Lock the Firestore curriculum content with security rules: any signed-in identity (including the
anonymous one from Plan 01) may READ `letters`/`lessons`/`meta`; ALL client writes are denied
(content is written only via the Plan 03 admin-SDK tooling). The rules carry a documented
custom-claim seam so v2 can tighten by parent/admin/tutor roles, and a deny-by-default catch-all.
Because rules are server-enforced and `fake_cloud_firestore` does not evaluate them, verification
is via the Firebase Console Rules Playground (or the emulator) — captured as a documented manual
checklist plus the actual deploy.

Purpose: Delivers ROADMAP SC#5 (curriculum locked read-only for clients; writes via tooling only;
no child PII introduced) and the access-control half of the phase. Implements LOCKED decisions
D-10 (read-requires-auth, client-writes-denied, custom-claim seam), D-10a (App Check deferred but
rules must not conflict), and D-11 (no child-data collection — no child PII surface).

Output: `firestore.rules`, `firebase.json` + `.firebaserc` deploy targets, a deploy, and a
documented manual/emulator rules-verification checklist.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-firebase-curriculum-backend/06.1-CONTEXT.md
@.planning/phases/06.1-firebase-curriculum-backend/06.1-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author firestore.rules + firebase.json deploy target (D-10, D-10a, D-11)</name>
  <read_first>
    - .planning/phases/06.1-firebase-curriculum-backend/06.1-RESEARCH.md (§Security Domain — recommended rules skeleton; D-10/D-10a; STRIDE table)
    - .planning/phases/06.1-firebase-curriculum-backend/06.1-CONTEXT.md (D-10 custom-claim seam, D-11 no child data)
    - firebase.json (create if absent — declares firestore.rules as the deploy target)
  </read_first>
  <action>
    Create `firestore.rules` (rules_version '2'): under
    `match /databases/{database}/documents` add per-collection matches —
    `match /letters/{letterId}` and `match /lessons/{lessonId}` and `match /meta/{doc}` each with
    `allow read: if request.auth != null;` and `allow write: if false;` (read requires any
    signed-in identity incl. anonymous; no client writes — D-10). Add a COMMENTED v2 custom-claim
    seam directly above the write rules, e.g. a commented
    `// allow write: if request.auth.token.role == 'admin';` so the role-tightening path is
    obvious (D-10). End with a deny-by-default catch-all
    `match /{document=**} { allow read, write: if false; }`. Do NOT add any child-data collection
    match (D-11 — there is no child PII surface). Do NOT add App-Check-specific clauses now, but
    keep the structure App-Check-compatible (no constructs that would block adding it — D-10a).
    Create `firebase.json` declaring `{ "firestore": { "rules": "firestore.rules" } }` and a
    `.firebaserc` defaulting the project to `qalam-app-bd7d0` (D-16). If a firebase.json already
    exists from the Data Connect spike, MERGE the firestore.rules target in without enabling Data
    Connect / billing (D-17).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && grep -q "request.auth != null" firestore.rules && grep -q "allow write: if false" firestore.rules && grep -q "document=\*\*" firestore.rules && grep -q "firestore.rules" firebase.json && echo RULES_FILES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `firestore.rules` allows read on letters/lessons/meta only when `request.auth != null` and denies all client writes (`allow write: if false`).
    - `firestore.rules` contains a deny-by-default `match /{document=**}` catch-all and a commented custom-claim seam.
    - `firestore.rules` contains NO child-data collection match (no child PII surface — D-11).
    - `firebase.json` declares the `firestore.rules` deploy target; `.firebaserc` defaults to `qalam-app-bd7d0`.
  </acceptance_criteria>
  <done>Rules and deploy targets exist: read-requires-auth, all-client-writes-denied, deny-by-default, v2 seam, no child-data surface.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Deploy rules + verify read-auth/write-deny in Rules Playground (D-10, SC#5)</name>
  <read_first>
    - firestore.rules, firebase.json (Task 1)
    - .planning/phases/06.1-firebase-curriculum-backend/06.1-VALIDATION.md (§Justified Manual-Only Items — D-10 server-enforced)
    - .planning/phases/06.1-firebase-curriculum-backend/06.1-RESEARCH.md (§Security Domain deploy command)
  </read_first>
  <what-built>
    Task 1 authored the rules. The agent runs `firebase deploy --only firestore:rules
    --project qalam-app-bd7d0`. If `firebase` returns an auth error, raise an auth gate (owner runs
    `firebase login --reauth`), then retry. Rules are SERVER-ENFORCED — `fake_cloud_firestore` does
    not evaluate them — so the owner verifies behavior in the console Rules Playground (or the agent
    runs the emulator rules-test if available). The agent ALSO writes `test/firestore/rules.test.md`
    documenting the exact Playground/emulator steps and expected outcomes so the check is repeatable.
  </what-built>
  <how-to-verify>
    In Firebase Console → Firestore Database → Rules → Rules Playground (against the deployed rules):
    1. Simulate a READ of `/letters/alif` with Authentication = ON (anonymous) → ALLOWED.
    2. Simulate a READ of `/letters/alif` with Authentication = OFF (unauthenticated) → DENIED.
    3. Simulate a WRITE (set) to `/letters/alif` with Authentication = ON → DENIED.
    4. Simulate a READ of any non-curriculum path (e.g. `/secret/x`) with auth ON → DENIED (catch-all).
    5. Confirm the deployed Rules tab matches firestore.rules and no child-data collection is referenced.
  </how-to-verify>
  <acceptance_criteria>
    - `firebase deploy --only firestore:rules` succeeds (rules live on qalam-app-bd7d0).
    - Playground: authed read ALLOWED; unauthed read DENIED; authed write DENIED; catch-all path DENIED.
    - `test/firestore/rules.test.md` exists documenting the five checks + expected outcomes (repeatable verification).
    - Owner confirms no child-data collection is readable/writable (D-11).
  </acceptance_criteria>
  <resume-signal>Type "rules deployed + playground verified" or describe what blocked you.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | Untrusted client requests (read/write) cross into the database; rules are the server-enforced gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-13 | Information Disclosure | Unauthorized read of curriculum / future content | mitigate | `allow read: if request.auth != null` on letters/lessons/meta; deny-by-default catch-all; verified in Rules Playground (Task 2) |
| T-06.1-14 | Tampering | Malicious client writes / content tampering | mitigate | `allow write: if false` on all collections; writes only via admin-SDK tooling (Plan 03); Playground confirms authed write DENIED |
| T-06.1-15 | Spoofing | Spoofed app hitting Firestore without App Check | accept | App Check deferred to Phase 10 (D-10a); rules kept App-Check-compatible; anonymous-auth + write-deny limit blast radius until then |
| T-06.1-16 | Information Disclosure | Child PII accidentally synced to Firestore | mitigate | No child-data collection match exists; deny-by-default blocks any unlisted path; D-11 keeps child data in local Drift only |
| T-06.1-17 | Elevation of Privilege | A future role tightening introduced unsafely | mitigate | Documented custom-claim seam (`request.auth.token.role == 'admin'`) commented in place; v2 tightens deliberately, not ad hoc |
</threat_model>

<verification>
- Rules-file gates green (request.auth != null read, write:false, catch-all, firebase.json target).
- Manual/Playground (Task 2): authed read allowed, unauthed read denied, authed write denied, catch-all denied.
- `firebase deploy --only firestore:rules` succeeds.
</verification>

<success_criteria>
- Curriculum locked read-only for clients; writes via tooling only (ROADMAP SC#5, D-10).
- No child PII surface in the rules (D-11); v2 custom-claim seam present (D-10); App-Check-compatible (D-10a).
</success_criteria>

<output>
Create `.planning/phases/06.1-firebase-curriculum-backend/06.1-05-SUMMARY.md` when done.
</output>
