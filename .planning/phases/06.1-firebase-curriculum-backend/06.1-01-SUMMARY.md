---
phase: 06.1-firebase-curriculum-backend
plan: 01
subsystem: auth
tags: [firebase, flutterfire, firebase_auth, cloud_firestore, firebase_core, anonymous-auth, android]

# Dependency graph
requires:
  - phase: 05-profiles-onboarding
    provides: "main.dart pre-runApp boot gate (hasProfile read + ProviderScope overrides) that the Firebase init + anonymous sign-in sequence into"
provides:
  - "FlutterFire wired to qalam-app-bd7d0 (firebase_options.dart + google-services.json), debug APK builds on Android"
  - "AuthService: idempotent anonymous boot sign-in (D-09b) + linkWithCredential upgrade seam (D-09c, defined/unused in v1)"
  - "Anonymous identity exists before the first curriculum read (FirebaseAuth.instance.currentUser non-null + isAnonymous at boot)"
  - "firebase_core / cloud_firestore / firebase_auth dependency floor for the rest of Phase 06.1 (Firestore repo, rules, seed)"
affects: [06.1-02 curriculum-repository-firestore, 06.1-03 python-seed-export, 06.1-04 security-rules, v2-tutor, account-linking]

# Tech tracking
tech-stack:
  added: [firebase_core ^4.10.0, cloud_firestore ^6.5.0, firebase_auth ^6.5.2, firebase_auth_mocks ^0.15.2 (dev)]
  patterns: ["Constructor-injected FirebaseAuth (defaults to .instance) so boot uses the singleton and tests pass an in-memory fake", "Anonymous-at-boot identity sequenced before the first curriculum read in the existing pre-runApp gate (Pattern 3)", "account-linking seam defined now, called in v2 (linkWithCredential)"]

key-files:
  created: [lib/services/auth_service.dart, test/services/auth_service_test.dart, lib/firebase_options.dart, android/app/google-services.json, firebase.json]
  modified: [lib/main.dart, pubspec.yaml, pubspec.lock, android/app/build.gradle.kts, android/settings.gradle.kts, .gitignore]

key-decisions:
  - "FlutterFire deps added via `flutter pub add` (let pub resolve the lockstep generation: firebase_core 4.x / cloud_firestore 6.x / firebase_auth 6.x) — no hand-pinned mismatched majors (D-12)"
  - "firebase_auth_mocks chosen for the anonymous boot test (A5 resolved — resolves clean against firebase_auth 6.5.2); mocktail used only to verify linkWithCredential delegation"
  - "google-services.json + firebase_options.dart committed (Firebase guidance: not secrets, research Q2); only the admin-SDK service-account key path is gitignored (T-06.1-02)"
  - "No Android minSdk bump needed — debug APK built clean, so Flutter's default minSdk already satisfies the Firebase floor of 23 (A2 confirmed)"

patterns-established:
  - "Pattern: injectable FirebaseAuth in a thin service — boot uses FirebaseAuth.instance, tests pass a fake; zero live-device/network dependency in unit tests"
  - "Pattern: idempotent ensureSignedIn() (guarded on currentUser == null) safe to call on every launch without minting a second anonymous user"

requirements-completed: [CUR-01]

# Metrics
duration: ~22min
completed: 2026-06-14
---

# Phase 06.1 Plan 01: Firebase Auth Foundation Summary

**FlutterFire wired to qalam-app-bd7d0 with anonymous-at-boot sign-in before the first curriculum read and a v2-ready linkWithCredential account-linking seam — zero child PII, no login UI.**

## Performance

- **Duration:** ~22 min
- **Completed:** 2026-06-14
- **Tasks:** 3 (Task 1 human-gate pre-resolved; Tasks 2–3 executed)
- **Files modified:** 11 (5 created, 6 modified)

## Accomplishments
- App wired to the existing `qalam-app-bd7d0` Firebase project (Android); `flutter build apk --debug` exits 0, proving the `com.google.gms.google-services` Gradle plugin links and minSdk satisfies the Firebase floor (ROADMAP SC#1).
- `AuthService` stands up the v2-ready auth foundation: idempotent anonymous sign-in (D-09b, zero PII) plus a `linkToPermanent(AuthCredential)` seam that delegates to `currentUser.linkWithCredential` (D-09c, defined but unused in v1).
- `main.dart` boot now runs `Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)` then `AuthService().ensureSignedIn()` **before** the existing `db.hasProfile()` read, so an anonymous identity exists before the first curriculum surface (Pattern 3).
- Child-safety invariant held: no child login UI, no child PII to Firebase — the `signInWithEmail|LoginScreen|child.*password` grep guard returns empty (T-06.1-01).
- Admin-SDK service-account key path gitignored (T-06.1-02); generated config files committed per Firebase non-secret guidance.

## Task Commits

1. **Task 2: FlutterFire CLI wiring + Gradle + .gitignore policy** - `31154fc` (feat)
2. **Task 3 (RED): failing AuthService test** - `afbacad` (test)
3. **Task 3 (GREEN): AuthService + main.dart boot wiring** - `1e4146a` (feat)

_Task 1 (console: enable the 3 auth providers + confirm project reuse) was a human-action gate resolved before this execution — `flutterfire configure --project=qalam-app-bd7d0` had already generated the config files; enabling the Anonymous/Email-Password/Google providers in the console remains a runtime prerequisite (see User Setup)._

_No REFACTOR commit — the GREEN implementation was already clean (no behavior-preserving cleanup warranted)._

## Files Created/Modified
- `lib/services/auth_service.dart` (created) - `AuthService` wrapping an injectable `FirebaseAuth`; `ensureSignedIn()` (idempotent anonymous sign-in) + `linkToPermanent()` (v2 account-linking seam).
- `test/services/auth_service_test.dart` (created) - 3 behaviors: anon sign-in when null, no-op when already signed in, link delegation.
- `lib/firebase_options.dart` (created, generated) - `DefaultFirebaseOptions` for qalam-app-bd7d0 (Android).
- `android/app/google-services.json` (created, generated) - Android Firebase config (`project_id: qalam-app-bd7d0`).
- `firebase.json` (created, generated) - FlutterFire config target.
- `lib/main.dart` (modified) - Firebase init + anonymous sign-in sequenced before the boot read.
- `pubspec.yaml` / `pubspec.lock` (modified) - firebase_core/cloud_firestore/firebase_auth + firebase_auth_mocks dev dep.
- `android/app/build.gradle.kts` / `android/settings.gradle.kts` (modified, by flutterfire) - google-services plugin declared + applied; signingConfigs + `com.technion.qalam` applicationId/namespace preserved.
- `.gitignore` (modified) - excludes `tools/firebase|curriculum/*serviceAccount*.json` and `*.key.json` (service-account key never committed).

## Decisions Made
- **Deps via `flutter pub add`, not hand-pinned** — pub resolved the compatible lockstep generation (firebase_core 4.10.0 / cloud_firestore 6.5.0 / firebase_auth 6.5.2), exactly as research predicted (D-12, anti-pattern avoided).
- **`firebase_auth_mocks` for the boot test** — resolved cleanly against firebase_auth 6.5.2 (research assumption A5 confirmed); used `MockFirebaseAuth`/`MockUser` for the anonymous paths and mocktail only to `verify` the `linkWithCredential` delegation (a call firebase_auth_mocks does not record).
- **No minSdk bump** — the debug APK built with no minSdkVersion error (research A2 confirmed; Flutter default ≥ 23).
- **Generated config committed, key gitignored** — per Firebase non-secret guidance (research Q2) and T-06.1-02.

## Deviations from Plan

None - plan executed exactly as written. (Task 1's console/FlutterFire-CLI portion was pre-resolved by the human before execution per the orchestrator's instruction; this is not a deviation — the generated artifacts were verified on disk and committed in Task 2.)

## Issues Encountered
None. `flutter pub add` resolved all FlutterFire plugins and `firebase_auth_mocks` on the first attempt; the debug APK built clean; all three auth behaviors went GREEN on the first implementation.

## User Setup Required

**One runtime prerequisite remains (does NOT block the build or unit tests, only on-device sign-in):**

- **Firebase Console → qalam-app-bd7d0 → Authentication → Sign-in method:** enable **Anonymous** (required for v1 runtime), and provision **Email/Password** + **Google** (v2 infrastructure, D-09a). Keep the project on the **Spark (free) tier** — do **not** enable billing or Data Connect (D-16 / D-17).
- Anonymous sign-in at boot will fail on a real device until the **Anonymous** provider is enabled in the console.

## Next Phase Readiness
- `cloud_firestore` is installed and the app is initialized against qalam-app-bd7d0 — **Plan 06.1-02** can now extend `CurriculumRepository` to read live from Firestore behind its unchanged async-getter seam.
- The anonymous identity + write-deny rules seam is ready for **Plan 06.1-04** (security rules) to gate curriculum reads on `request.auth != null`.
- The admin-SDK key gitignore stanza is in place for **Plan 06.1-03** (Python firebase-admin seed/export).
- **Pre-existing, out-of-scope test failures (unchanged by this plan, do not block):** 2 alif reference tests + 1 alif reference-overlay golden (`alif_reference_test.dart`, `reference_overlay_golden_test.dart` — owner's unsigned draft strokes) and 1 `mastery_celebration_golden_test.dart` (golden font-drift). Full suite: 381 passed, 4 failed (all four are these known pre-existing failures).

## Self-Check: PASSED

- Files verified on disk: `lib/services/auth_service.dart`, `test/services/auth_service_test.dart`, `lib/firebase_options.dart`, `android/app/google-services.json`, `firebase.json`, `lib/main.dart`, `06.1-01-SUMMARY.md` — all FOUND.
- Commits verified in git: `31154fc` (Task 2 feat), `afbacad` (Task 3 RED test), `1e4146a` (Task 3 GREEN feat) — all FOUND.

---
*Phase: 06.1-firebase-curriculum-backend*
*Completed: 2026-06-14*
