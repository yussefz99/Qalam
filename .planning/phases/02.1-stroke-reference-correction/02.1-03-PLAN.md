---
phase: 02.1-stroke-reference-correction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/extract_reference_paths.py
  - tools/authoring_hints.json
  - tools/test_authoring_hints.py
autonomous: true

requirements: [CUR-01]

must_haves:
  truths:
    - "The Python tool can NEVER again emit a body outline as a referenceStrokes path"
    - "The tool emits only dot centroids + glyph bounding box as authoring HINTS to its new output file tools/authoring_hints.json"
    - "The tool carries a loud deprecation header pointing to STROKE-REFERENCE.md and stating outlines are NOT teaching strokes"
    - "A Python check asserts the hints output contains no body-outline polyline (only centroids + bbox)"
  artifacts:
    - path: "tools/extract_reference_paths.py"
      provides: "Repurposed extractor: dot-centroid + bbox authoring hints only, with deprecation header (D-05)"
      contains: "STROKE-REFERENCE.md"
    - path: "tools/authoring_hints.json"
      provides: "The new hints output (dot centroids + glyph bbox), replacing candidate_paths.json"
    - path: "tools/test_authoring_hints.py"
      provides: "Python check that the tool emits no body outlines as strokes"
  key_links:
    - from: "tools/extract_reference_paths.py"
      to: "tools/authoring_hints.json"
      via: "writes dot centroids + bbox (NOT body outline polylines)"
      pattern: "authoring_hints.json"
---

<objective>
Repurpose `tools/extract_reference_paths.py` (D-05, STROKE-REFERENCE.md §7.1) so it can NEVER again emit a glyph body outline as a `referenceStrokes` teaching path. Keep only its genuinely-useful signal — dot-vs-body separation (dot centroids) and the glyph bounding box — as authoring HINTS, write them to a renamed output `tools/authoring_hints.json`, and prepend a loud deprecation header pointing to STROKE-REFERENCE.md.

Purpose: The original bug came from this tool's body outlines being pasted into curriculum. Removing that capability at the source (and renaming the output so no one pastes outlines again) is the tooling half of the fix. This is Python tooling (CLAUDE.md: Python for tooling). It runs in parallel with the schema/validator plan — no Dart file overlap.

Output: A repurposed, clearly-deprecated extractor that emits hints only, plus a Python check pinning that no body outline is emitted.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STROKE-REFERENCE.md
@.planning/phases/02.1-stroke-reference-correction/02.1-CONTEXT.md
@tools/extract_reference_paths.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repurpose the extractor to emit dot-centroid + bbox authoring hints only (D-05)</name>
  <files>tools/extract_reference_paths.py, tools/authoring_hints.json</files>
  <read_first>
    - tools/extract_reference_paths.py (the current outline-extraction pipeline: get_simple_contours, normalize_contours, the contours_out body-outline emission, OUTPUT_PATH = candidate_paths.json)
    - .planning/research/STROKE-REFERENCE.md §7.1 (repurpose, don't trust for bodies — keep dot centroids + bbox as hints; rename output; loud header) and §4 (the font-contour dot separation already works: baa=2, taa=3, sheen=4 contours; body is always one contour)
  </read_first>
  <action>
    Repurpose `tools/extract_reference_paths.py` so its output is authoring HINTS, never teaching strokes. Change `OUTPUT_PATH` to `tools/authoring_hints.json`. Keep the dot-vs-body separation logic (contour count + which contours are the small dot blobs vs the single large body contour) but emit, per letter, ONLY: (a) the dot centroids (one [x,y] per dot contour, normalized 0..1 in the shared per-letter bbox) and (b) the glyph bounding box — and explicitly DO NOT emit any body-outline polyline. Add a prominent deprecation header comment at the top of the file stating: outlines are NOT teaching strokes; the body centerline is authored by the owner in the in-app trace screen; see `.planning/research/STROKE-REFERENCE.md`. Update the module docstring and the `Usage` text accordingly. The script must still run without crashing if `fonttools` is absent (keep the existing graceful import error). Do not delete `tools/candidate_paths.json` here, but the tool must no longer write it.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python3 -c "import ast,sys; src=open('tools/extract_reference_paths.py').read(); assert 'authoring_hints.json' in src, 'output not renamed'; assert 'STROKE-REFERENCE.md' in src, 'deprecation header missing'; b=[l for l in src.splitlines() if l.strip().startswith('OUTPUT_PATH')]; assert any('authoring_hints' in l for l in b), 'OUTPUT_PATH not repointed'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/extract_reference_paths.py` `OUTPUT_PATH` points to `tools/authoring_hints.json`.
    - The file contains a deprecation header referencing `.planning/research/STROKE-REFERENCE.md` and stating outlines are not teaching strokes.
    - The emitted per-letter structure contains dot centroids + glyph bbox and NO body-outline polyline key.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>The extractor emits dot-centroid + bbox hints to authoring_hints.json, with a loud deprecation header; it can no longer emit body outlines.</done>
</task>

<task type="auto">
  <name>Task 2: Add a Python check pinning "no body outline emitted" + regenerate hints if fonttools available</name>
  <files>tools/test_authoring_hints.py, tools/authoring_hints.json</files>
  <read_first>
    - tools/extract_reference_paths.py (repurposed in Task 1 — the hint-emission structure)
    - .planning/research/STROKE-REFERENCE.md §7.1 (the never-emit-outline invariant this check pins)
  </read_first>
  <action>
    Create `tools/test_authoring_hints.py` — a plain Python check (runnable as `python3 tools/test_authoring_hints.py`, exit 0 on pass) that imports/inspects the repurposed extractor and asserts the emitted-hints structure for a letter contains dot-centroid and bbox keys but NO key holding a multi-point body-outline polyline (e.g. assert no per-letter value is a long closed polyline). Drive it off a small synthetic in-memory contour set (do not require fonttools or the TTF — keep it dependency-free so it runs in any environment), or skip-with-clear-message if the tool's pure logic cannot be exercised without the font, while still asserting the static guarantees (output path, no outline emission code path). If `fonttools` happens to be installed, also run the extractor to regenerate `tools/authoring_hints.json` so the committed hints file reflects the repurposed output; otherwise leave a stub `tools/authoring_hints.json` documenting that it is regenerated when fonttools is present.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python3 tools/test_authoring_hints.py</automated>
  </verify>
  <acceptance_criteria>
    - `tools/test_authoring_hints.py` exists and exits 0.
    - It asserts the hints structure has dot centroids + bbox and NO body-outline polyline.
    - It runs without requiring fonttools/the TTF (dependency-free or clean skip).
    - `tools/authoring_hints.json` exists (regenerated if fonttools present, else a documented stub).
  </acceptance_criteria>
  <done>A Python check pins the never-emit-outline invariant; the hints file is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| font file → tool → hints JSON | The tool reads a bundled TTF and writes a local hints file. No network, no child input, no app runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-05 | Tampering | extraction tool re-introducing outlines as strokes | mitigate | The tool is repurposed to emit hints only (Task 1) and a Python check pins "no body outline emitted" (Task 2); the renamed output prevents anyone pasting outlines into curriculum again. |
| T-02.1-02 | Information disclosure | child / stroke data | accept | This is offline font tooling — no child data, no capture, no network. No point-data logging of any child input (there is none). |
| T-02.1-SC | Tampering | pip installs | accept | No new pip dependencies introduced; the tool already documents `pip install fonttools` as an optional pre-existing dependency and degrades gracefully without it. No new package legitimacy gate needed. |
</threat_model>

<verification>
- `python3 tools/test_authoring_hints.py` exits 0.
- The static check command in Task 1 exits 0 (output renamed, header present).
- Inspection: no code path in the tool writes a body-outline polyline into the hints output.
</verification>

<success_criteria>
- The Python tool can never again emit a body outline as a teaching stroke; it emits dot centroids + bbox hints to `tools/authoring_hints.json` with a loud deprecation header (D-05; supports Phase success criterion 1 at the source).
</success_criteria>

<output>
Create `.planning/phases/02.1-stroke-reference-correction/02.1-03-SUMMARY.md` when done.
</output>
