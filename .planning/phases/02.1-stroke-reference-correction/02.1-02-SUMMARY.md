---
phase: 02.1-stroke-reference-correction
plan: 02
subsystem: curriculum
tags: [dart, flutter, curriculum, golden-test, load-time-validation, sign-off]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: validateReferenceStrokes (D-04 guard) + ReferencePath.resolve + StrokeSpec.type
  - phase: 02-curriculum-schema-first-letter-seed
    provides: letters.json alif entry, CurriculumRepository, test FontLoader bootstrap
provides:
  - Corrected alif open top→bottom centerline (the proven exemplar, D-06)
  - Load-time D-04 validation guard in CurriculumRepository (an outline cannot load silently)
  - alif reference-overlay golden (sign-off + CI regression artifact, D-07)
  - Human-re-affirmed alif signedOff:true against corrected geometry
affects: [03-trace-one-letter-end-to-end]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Load-time curriculum validation: validate into a local before caching so a throw never poisons the in-memory cache"
    - "Per-letter reference-overlay golden (resolved pen path over faint glyph) as both human sign-off artifact and CI regression gate"

key-files:
  created:
    - test/curriculum/alif_reference_test.dart
    - test/curriculum/reference_overlay_golden_test.dart
    - test/goldens/alif_reference_overlay.png
  modified:
    - assets/curriculum/letters.json
    - lib/data/curriculum_repository.dart
    - test/data/curriculum_repository_test.dart

key-decisions:
  - "alif centerline authored as the STROKE-REFERENCE §7.3 candidate ([[0.5,0.0]..[0.5,1.0]], x=0.5, perfectly vertical) — owner+mother approved as-is at the human gate, no lean/start-x adjustment requested"
  - "Load-time guard throws StateError with the offending letter id; validates into a local so a throw leaves _letters null (retry re-runs the guard)"
  - "letters.json edited by a line-precise splice of only alif's stroke block — the other 27 letters and all formatting are byte-for-byte unchanged"

patterns-established:
  - "Curriculum reference data is gated by the D-04 validator at load AND in an integration test against the shipped file"

requirements-completed: [CUR-01]

# Metrics
duration: ~30min
completed: 2026-06-01
---

# Phase 02.1 Plan 02: alif re-author + load gating + sign-off Summary

**alif is re-authored as a correct open top→bottom centerline, the shipped curriculum is gated at load by the D-04 validator, a per-letter overlay golden is the sign-off + regression artifact, and alif is re-signed-off by an explicit owner+mother visual confirmation.**

## Performance
- **Duration:** ~30 min (inline foreground, incl. the human gate)
- **Completed:** 2026-06-01
- **Tasks:** 3 (2 autonomous + 1 blocking human gate)
- **Files modified:** 6 (3 created, 3 modified)

## Accomplishments
- **Task 1 (D-06 geometry)** — replaced alif's 64-point closed outline in `assets/curriculum/letters.json` with the open centerline `[[0.5,0.0],[0.5,0.25],[0.5,0.5],[0.5,0.75],[0.5,1.0]]`, `type:"line"`, `direction:"topToBottom"`. Line-precise splice: 6 insertions / 256 deletions, only the alif stroke touched; the other 27 letters remain `referenceStrokes:[]`, `signedOff:false`. `alif_reference_test.dart` pins length≈1.0, monotonic-y, straightness (max perp deviation < 0.02), validator-pass, and the three intact commonMistakes.
- **Task 2 (load guard + D-07 golden)** — `CurriculumRepository._ensureLoaded` now runs `validateReferenceStrokes` over every letter and throws with the offending id on any violation (T-02.1-03), validated into a local so a throw never poisons the cache. Added an integration test (the SHIPPED letters.json passes) + a closed-loop fixture (load throws). Built `reference_overlay_golden_test.dart` rendering alif's `ReferencePath.resolve` pen path over the faint Noto Naskh glyph in draw order, baseline `test/goldens/alif_reference_overlay.png`.
- **Task 3 (blocking human gate, D-06)** — the owner + his mother visually confirmed the overlay golden: a single straight top→bottom downstroke over the glyph (not a loop). **Approved as-is** — no start-x / lean adjustment requested. `alif.signedOff:true` is re-affirmed by this explicit human action against the corrected geometry (legitimacy carried by the human confirmation, not an agent flip).

## Task Commits
1. **Task 1: corrected alif centerline + property test** — `4564da8` (feat)
2. **Task 2: load-time D-04 guard + repo tests + overlay golden** — `610e42a` (feat)
3. **Task 3: human sign-off** — no file change (signedOff already true; re-affirmed by owner+mother visual confirmation on 2026-06-01). Recorded here as the durable sign-off record.

## Files Created/Modified
- `assets/curriculum/letters.json` — alif referenceStrokes replaced with the open centerline + `type:"line"` (alif only).
- `lib/data/curriculum_repository.dart` — load-time `validateReferenceStrokes` guard in `_ensureLoaded`.
- `test/curriculum/alif_reference_test.dart` — alif centerline property tests.
- `test/data/curriculum_repository_test.dart` — shipped-file integration test + closed-loop-throws test.
- `test/curriculum/reference_overlay_golden_test.dart` + `test/goldens/alif_reference_overlay.png` — the overlay golden + baseline.

## Decisions Made
See `key-decisions` frontmatter. Owner+mother approved the §7.3 candidate geometry unchanged.

## Deviations from Plan
None — plan executed as written. The human gate approved the autonomous starting geometry without adjustment, so no golden regeneration was needed.

## Issues Encountered
- **Pre-existing, unrelated full-suite failure:** `test/glyph_audit_golden_test.dart` (Phase-1 D-12 Noto Naskh golden) fails by 1.19% pixels. Confirmed failing identically at the phase base commit `363995b` (before any 02.1 work) — an environment/font-rendering mismatch on this machine, NOT introduced by this phase. Likely needs a `--update-goldens` regenerate of that Phase-1 baseline on the current toolchain; tracked as a known issue, out of scope for 02.1.

## Verification
- `flutter test test/curriculum/alif_reference_test.dart` → 6/6 pass.
- `flutter test test/data/curriculum_repository_test.dart test/curriculum/reference_overlay_golden_test.dart` → 11/11 pass.
- `flutter analyze lib/data/curriculum_repository.dart …` → No issues found.
- Full suite: 59 pass / 1 fail (the pre-existing glyph_audit golden only).
- Inspection: only alif changed in letters.json; the load guard throws on a loop fixture (negative test present).

## User Setup Required
None.

## Next Phase Readiness
- alif is the proven, signed-off exemplar — Phase 3 can build the trace→score→animate loop on the corrected centerline via `ReferencePath.resolve`.
- The shipped curriculum is gated at load and in CI; an outline cannot reach the app.

---
*Phase: 02.1-stroke-reference-correction*
*Completed: 2026-06-01*
