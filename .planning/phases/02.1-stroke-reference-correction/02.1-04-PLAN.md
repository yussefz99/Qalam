---
phase: 02.1-stroke-reference-correction
plan: 04
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - lib/dev/authoring_screen.dart
  - lib/dev/authoring_export.dart
  - test/features/authoring/authoring_screen_test.dart
autonomous: true

requirements: [CUR-01]

must_haves:
  truths:
    - "The owner can trace a letter's strokes over a faint Noto Naskh glyph with a finger and the live ink renders (reusing the Phase-1 Listener + CustomPainter capture)"
    - "Each traced stroke is taggable with order, label, type (line/curve/dot), and direction"
    - "The screen exports a normalized (0..1) referenceStrokes JSON fragment ready to paste into letters.json"
    - "Exported strokes pass the D-04 validator (an authored open centerline, never a closed loop)"
    - "Captured stroke points stay in memory only — never persisted, logged, or transmitted (T-01-05)"
  artifacts:
    - path: "lib/dev/authoring_screen.dart"
      provides: "Dev-only Riverpod trace-over-glyph authoring screen (D-02)"
      min_lines: 60
    - path: "lib/dev/authoring_export.dart"
      provides: "Pure-Dart normalize + serialize traced strokes → referenceStrokes JSON fragment"
    - path: "test/features/authoring/authoring_screen_test.dart"
      provides: "Widget test: synthesize pointer events, tag, export a normalized fragment"
  key_links:
    - from: "lib/dev/authoring_screen.dart"
      to: "lib/dev/authoring_export.dart"
      via: "normalizes captured local points to 0..1 and serializes the tagged strokes"
      pattern: "normalize"
    - from: "lib/dev/authoring_export.dart"
      to: "lib/core/scoring/stroke_validation.dart"
      via: "the exported fragment is validatable by the D-04 guard"
      pattern: "validateReferenceStrokes"
---

<objective>
Build the in-app authoring trace screen (D-02): a dev-only Riverpod screen where the owner (with his mother) traces each stroke over a faint Noto Naskh glyph backdrop — in her prescribed order and direction — tags each stroke (order, label, type, direction), and exports a normalized `referenceStrokes` JSON fragment ready to paste into `letters.json`. Reuse the proven Phase-1 `Listener` + smoothed `CustomPainter` capture so the captured data is in exactly the coordinate space and format the scorer/animation consume (animation-path == scoring-path == authored-path by construction).

Purpose: This is Slice 2 — "the owner can author ANY letter's strokes in-app." It produces the data for the remaining letters (authored at Phase 7's gate). It is an internal tool: it does NOT need full child-facing brand polish (Claude's Discretion), but stroke points MUST stay in memory only (T-01-05).

Output: A dev authoring screen, a pure-Dart export/normalize helper, and a widget test proving trace → tag → normalized export.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STROKE-REFERENCE.md
@.planning/phases/02.1-stroke-reference-correction/02.1-CONTEXT.md
@.planning/phases/02.1-stroke-reference-correction/02.1-VALIDATION.md
@lib/screens/practice_screen.dart
@lib/models/letter.dart
@lib/core/scoring/stroke_validation.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-Dart export helper — normalize tagged strokes → referenceStrokes fragment</name>
  <files>lib/dev/authoring_export.dart, test/features/authoring/authoring_screen_test.dart</files>
  <read_first>
    - lib/models/letter.dart (StrokeSpec shape — order/label/type/points/direction — the export must match this so it pastes into letters.json)
    - lib/core/scoring/stroke_validation.dart (the validator the exported fragment must pass)
    - .planning/research/STROKE-REFERENCE.md §5 (normalize ALL strokes together against the letter's overall bbox so relative positions like dot-below-body are preserved) and §7.2 (the export step of the candidate workflow)
  </read_first>
  <behavior>
    - Given a list of captured strokes (each: raw local Offsets + order + label + type + direction), the helper normalizes ALL strokes together to 0..1 against the combined bounding box (so a dot's position relative to the body is preserved).
    - A traced near-vertical downstroke exports as a single open "line" stroke, direction "topToBottom", first y < last y, that PASSES validateReferenceStrokes.
    - A single-tap stroke tagged type:"dot" exports with exactly one point and direction "tap".
    - The exported fragment is valid JSON shaped like letters.json `referenceStrokes` (a list of {order,label,type,points,direction}).
  </behavior>
  <action>
    Create `lib/dev/authoring_export.dart` (pure Dart — no widget imports) exposing a function that takes the captured tagged strokes and returns the normalized `referenceStrokes` JSON fragment (and/or a `List<StrokeSpec>`). Compute the combined bounding box across ALL strokes' points and normalize every point to 0..1 against it (preserve relative stroke positions per §5). Round coordinates to a sane precision (e.g. 4 dp). Emit each stroke as {order, label, type, points, direction}. Do NOT log or persist any point data. Then create `test/features/authoring/authoring_screen_test.dart` (export-helper unit cases first): feed synthetic captured strokes for the three behaviors above and assert the normalized output and that it passes `validateReferenceStrokes`.
  </action>
  <verify>
    <automated>flutter test test/features/authoring/authoring_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/dev/authoring_export.dart` exists, imports no Flutter widget packages, and normalizes all strokes against the combined bbox.
    - The exported downstroke fragment passes `validateReferenceStrokes`.
    - A dot exports as exactly one point with direction "tap".
    - `flutter test test/features/authoring/authoring_screen_test.dart` exits 0 for the export cases.
    - No point-data logging/persistence in the helper.
  </acceptance_criteria>
  <done>A pure-Dart export helper normalizes tagged traced strokes into a valid, validator-passing referenceStrokes fragment.</done>
</task>

<task type="auto">
  <name>Task 2: The dev authoring trace screen — trace over glyph, tag, export (D-02)</name>
  <files>lib/dev/authoring_screen.dart, test/features/authoring/authoring_screen_test.dart</files>
  <read_first>
    - lib/screens/practice_screen.dart (the Listener `_InkSurface` + smoothed `_InkPainter` capture + global→local conversion + in-memory `_strokes` model + T-01-05 security note to mirror)
    - lib/dev/authoring_export.dart (the export helper from Task 1)
    - lib/models/letter.dart (StrokeSpec/Letter — for the faint glyph char + the export shape)
    - .planning/research/STROKE-REFERENCE.md §3(c1) (the in-app authoring screen recommendation; finger-only is fine) and §7.5 (overlay/draw-order)
    - docs/design/kit/project/SKILL.md (brand rules — relevant only lightly; this is an internal tool, not child-facing)
  </read_first>
  <action>
    Create `lib/dev/authoring_screen.dart` — a dev-only StatefulWidget/ConsumerWidget (Riverpod ONLY — no BLoC/GetX) that: (1) renders a chosen letter's Noto Naskh glyph FAINTLY as a backdrop (reuse the ArabicText/font setup; faint opacity); (2) reuses the Phase-1 `Listener` (onPointerDown/Move/Up) + smoothed `CustomPainter` ink to capture finger strokes in local coordinates, holding strokes in an in-memory list (mirror practice_screen's `_strokes` model and its T-01-05 in-memory-only note — no persistence, no `print`/`debugPrint` of points); (3) lets the owner tag each completed stroke's order, label, type (line/curve/dot), and direction via simple controls (a plain dropdown/segmented control + text field is fine — internal tool, Claude's Discretion on layout); (4) on Export, calls `lib/dev/authoring_export.dart` and surfaces the normalized JSON fragment (e.g. shown in a selectable text area / copied to clipboard) ready to paste into `letters.json`. Keep it OUT of the child-facing router/nav — expose it only as a dev entry (e.g. a dev-only route guarded by a debug flag, consistent with the existing `lib/dev/` directory). Do NOT wire it into the normal Home navigation. Extend `test/features/authoring/authoring_screen_test.dart` with a widget test that pumps the screen, synthesizes pointer events to trace a downstroke, tags it, taps Export, and asserts a normalized fragment is produced (e.g. visible in the export field) that passes the validator.
  </action>
  <verify>
    <automated>flutter test test/features/authoring/authoring_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/dev/authoring_screen.dart` exists (≥60 lines), uses Riverpod (no BLoC/GetX), reuses a Listener-based capture, and renders a faint glyph backdrop.
    - Captured points are held in memory only; no `print`/`debugPrint` of point data and no persistence (T-01-05).
    - The screen is NOT wired into the child-facing navigation (dev-only entry).
    - The widget test synthesizes a traced stroke, tags it, exports, and asserts a validator-passing fragment.
    - `flutter test test/features/authoring/authoring_screen_test.dart` exits 0.
  </acceptance_criteria>
  <done>The owner can trace any letter over a faint glyph, tag the strokes, and export a normalized validator-passing referenceStrokes fragment — finger-only, points in-memory only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner finger trace → in-memory capture → exported JSON | Pointer input is captured locally and held in memory; the exported curriculum fragment is reference data (not child data) and is safe to persist when pasted into letters.json. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-06 | Information disclosure | captured stroke points (and any reuse of the P1 capture widget) | mitigate | Points stay in memory only (mirror practice_screen's T-01-05 contract): never persisted, never `print`/`debugPrint`-logged, never transmitted; discarded on dispose. The widget test confirms no persistence path. The owner authoring his own reference data is not child data, but the capture-widget reuse keeps the same in-memory discipline so it stays safe when reused for children in Phase 3. |
| T-02.1-07 | Elevation of privilege | dev authoring screen reachable by a child | mitigate | The screen is a dev-only entry behind a debug flag and is NOT wired into the child-facing navigation. |
| T-02.1-02 | Information disclosure | network/transmission of points | accept | No HTTP/Firebase/ML Kit/network calls exist in this phase; nothing to transmit. |
| T-02.1-SC | Tampering | pub installs | accept | No new pub packages (reuses existing Flutter/Riverpod + flutter_test). No legitimacy gate needed. |
</threat_model>

<verification>
- `flutter test test/features/authoring/authoring_screen_test.dart` exits 0 (export-helper + widget cases).
- `flutter analyze` reports no new errors in `lib/dev/authoring_screen.dart` / `lib/dev/authoring_export.dart`.
- Inspection: no `print`/`debugPrint` of point data; no persistence call; the screen is not in the child-facing router.
</verification>

<success_criteria>
- The owner can author a letter's strokes in-app by tracing over a faint Noto Naskh glyph, in his mother's order and direction, exporting normalized referenceStrokes (order, label, type, direction) that pass the D-04 validator (Phase success criterion 2).
- Captured points stay in memory only (T-01-05).
</success_criteria>

<output>
Create `.planning/phases/02.1-stroke-reference-correction/02.1-04-SUMMARY.md` when done.
</output>
