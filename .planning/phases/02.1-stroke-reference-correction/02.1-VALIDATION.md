---
phase: 02.1
slug: stroke-reference-correction
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-01
---

# Phase 02.1 — Validation Strategy

> Seeded from `.planning/research/STROKE-REFERENCE.md` § 8 (Validation Architecture).
> Planner assigns task IDs / waves during planning.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `flutter_test` (SDK) + golden tests |
| **Config file** | `analysis_options.yaml`; `test/flutter_test_config.dart` (loads bundled Arabic TTFs into the headless engine — P1 pattern; required so Arabic goldens don't render tofu) |
| **Quick run command** | `flutter test test/curriculum/ test/core/scoring/` |
| **Full suite command** | `flutter test` |
| **Authoring-tool check** | Python validator in the (repurposed) tool if SVG-fallback is ever used; the Dart load-time guard is the primary gate |

---

## Sampling Rate

- **After every task commit:** `flutter test test/curriculum/ test/core/scoring/` (fast, pure-Dart + small goldens)
- **After every plan wave:** `flutter test` (full suite incl. overlay goldens)
- **Before `/gsd-verify-work`:** Full suite green **+ owner visual confirmation of the alif overlay golden before re-setting `signedOff: true`**

---

## Per-Task Verification Map

> Task IDs / waves assigned by the planner. Requirement → behavior → test mapping is the source contract.

| Req | Behavior | Test Type | Automated Command | File Exists |
|-----|----------|-----------|-------------------|-------------|
| CUR-01 | A non-dot `referenceStrokes` stroke is an **open** centerline, never a closed loop (catches the original bug) | unit | `flutter test test/curriculum/stroke_spec_validation_test.dart` | ❌ W0 |
| CUR-01 | `direction` string agrees with `points.first→last` order | unit | same | ❌ W0 |
| CUR-01 | `type == "dot"` ⇒ single point; dots ordered after body strokes | unit | same | ❌ W0 |
| CUR-01 | All coords in [0,1]; `order` 1..N contiguous | unit | same | ❌ W0 |
| CUR-01 | `StrokeSpec.fromJson` parses the new `type` field with a safe default ("line") | unit | `flutter test test/models/letter_test.dart` (extend P2 tests) | ⚠️ extend |
| CUR-01 | Loading `letters.json` through `CurriculumRepository` passes the validator (no outline in shipped data) | integration | `flutter test test/data/curriculum_repository_test.dart` (extend) | ⚠️ extend |
| CUR-01 | alif's corrected stroke: length ≈ 1.0, monotonic-y, straight (perp-deviation ≈ 0) | unit | `flutter test test/curriculum/alif_reference_test.dart` | ❌ W0 |
| CUR-01 (S1-04 prereq) | The resolved reference path used by the dotted guide == the animation path == the scorer path (one source of truth) | unit | `flutter test test/core/scoring/reference_path_test.dart` | ❌ W0 |
| CUR-01 | Each authored letter renders a sensible pen path overlaid on its glyph (owner-confirmable) | golden | `flutter test test/curriculum/reference_overlay_golden_test.dart` (loads TTFs) | ❌ W0 |
| CUR-01 | The authoring trace screen exports a normalized `referenceStrokes` fragment (order/label/type/direction) for a traced letter | widget | `flutter test test/features/authoring/authoring_screen_test.dart` (synthesize pointer events) | ❌ W0 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/curriculum/stroke_spec_validation_test.dart` — the open-not-closed + direction + dot + range guard (**the single most valuable test — makes the original bug impossible to reintroduce**)
- [ ] `test/curriculum/alif_reference_test.dart` — alif centerline properties
- [ ] `test/core/scoring/reference_path_test.dart` — path identity (guide == animation == scorer)
- [ ] `test/curriculum/reference_overlay_golden_test.dart` — per-letter visual overlay golden (owner QA artifact)
- [ ] `test/features/authoring/authoring_screen_test.dart` — trace → normalized export
- [ ] Extend `test/models/letter_test.dart` for the `type` field default
- [ ] Extend `test/data/curriculum_repository_test.dart` — shipped `letters.json` passes the validator
- [ ] No new test framework needed (flutter_test present); add `mocktail` dev-dep ONLY if a real mock is required

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Owner+mother confirm alif's pen path matches how she teaches it | CUR-01 / D-06 | Pedagogy sign-off — no automated metric can replace the human gate | Watch the alif overlay golden / in-app animation; confirm start-x, lean, top→bottom motion; then re-set `signedOff: true`. |
| Authoring a stroke by tracing feels right with a finger | D-02 | Pointer/render behavior on real hardware | On the tablet: open the authoring screen, trace alif over the faint glyph, confirm the captured path is sensible before export. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] The closed-loop validator test exists and is green (the regression guard)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
