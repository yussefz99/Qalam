---
phase: 02.1-stroke-reference-correction
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - assets/curriculum/letters.json
  - lib/data/curriculum_repository.dart
  - test/curriculum/alif_reference_test.dart
  - test/data/curriculum_repository_test.dart
  - test/curriculum/reference_overlay_golden_test.dart
  - test/goldens/alif_reference_overlay.png
autonomous: false

requirements: [CUR-01]

must_haves:
  truths:
    - "alif's referenceStrokes is a single open top→bottom centerline (length ≈ 1.0, monotonic-y, straight) — not the old 64-point closed outline"
    - "alif's three commonMistakes (too_short, wrong_direction, too_curved) are valid against the corrected centerline"
    - "Loading the shipped letters.json through CurriculumRepository runs the D-04 validator and would throw/surface a clear error if any non-dot outline were present"
    - "A per-letter visual-overlay golden renders alif's pen path over the faint Noto Naskh glyph in draw order (the owner sign-off + CI regression artifact)"
    - "alif is re-signed-off (signedOff:true) ONLY after the owner+mother visually confirm the overlay — never silently flipped by an agent"
  artifacts:
    - path: "assets/curriculum/letters.json"
      provides: "Corrected alif referenceStrokes (open centerline) + re-set signedOff after human gate"
      contains: "\"id\": \"alif\""
    - path: "lib/data/curriculum_repository.dart"
      provides: "Load-time D-04 validation guard hooked into _ensureLoaded"
      contains: "validateReferenceStrokes"
    - path: "test/curriculum/alif_reference_test.dart"
      provides: "alif centerline property tests (length≈1.0, monotonic-y, straight)"
    - path: "test/curriculum/reference_overlay_golden_test.dart"
      provides: "Per-letter pen-path-over-glyph overlay golden (loads TTFs)"
    - path: "test/goldens/alif_reference_overlay.png"
      provides: "The alif overlay golden baseline (sign-off artifact)"
  key_links:
    - from: "lib/data/curriculum_repository.dart"
      to: "lib/core/scoring/stroke_validation.dart"
      via: "calls the validator in _ensureLoaded after parsing each Letter"
      pattern: "validateReferenceStrokes"
    - from: "test/data/curriculum_repository_test.dart"
      to: "assets/curriculum/letters.json"
      via: "loads the SHIPPED letters.json and asserts the validator passes (no outline ships)"
      pattern: "letters.json"
    - from: "test/curriculum/reference_overlay_golden_test.dart"
      to: "assets/curriculum/letters.json"
      via: "renders alif's resolved reference path over the faint glyph"
      pattern: "alif"
---

<objective>
Re-author alif as the proven exemplar (D-06): replace its 64-point closed-outline loop with the corrected open top→bottom centerline (STROKE-REFERENCE.md §7.3), hook the D-04 validator into `CurriculumRepository` so the shipped data is gated at load (and in an integration test), add the alif centerline property test, build the per-letter visual-overlay golden (D-07), and — only after the owner+mother visually confirm the overlay — re-set alif's `signedOff: true` behind a blocking human gate.

Purpose: alif is `signedOff: true` today with WRONG data. Correcting it re-opens signed-off curriculum, which is the owner's mother's domain — so the geometry + tests are autonomous, but the sign-off itself is a human gate that no agent may flip silently. This is the thinnest end-to-end proof that the correction works for one real letter, and it directly unblocks Phase 3.

Output: Corrected alif data, a load-time validation guard, the alif property test, the overlay golden + baseline, and a re-signed-off alif.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STROKE-REFERENCE.md
@.planning/phases/02.1-stroke-reference-correction/02.1-CONTEXT.md
@.planning/phases/02.1-stroke-reference-correction/02.1-VALIDATION.md
@lib/data/curriculum_repository.dart
@lib/core/scoring/reference_path.dart
@lib/core/scoring/stroke_validation.dart
@test/data/curriculum_repository_test.dart
@test/flutter_test_config.dart
@assets/curriculum/letters.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace alif's outline with the corrected open centerline + alif property test (D-06 geometry)</name>
  <files>assets/curriculum/letters.json, test/curriculum/alif_reference_test.dart</files>
  <read_first>
    - assets/curriculum/letters.json (the alif entry — referenceStrokes[0] is the 64-point closed loop with first [0.2816,0.0], last [0.5049,0.006])
    - .planning/research/STROKE-REFERENCE.md §7.3 (the corrected alif candidate: open top→bottom centerline `[[0.5,0.0],[0.5,0.25],[0.5,0.5],[0.5,0.75],[0.5,1.0]]`, type "line", direction "topToBottom") and §6 (why the three commonMistakes become valid)
    - lib/core/scoring/stroke_validation.dart (the validator the new alif data must pass)
  </read_first>
  <behavior>
    - alif's referenceStrokes has exactly one stroke, type "line", direction "topToBottom".
    - The stroke's first point is at the top (y≈0.0) and last at the bottom (y≈1.0); y is monotonic non-decreasing (no vertical sign reversal).
    - The stroke is near-straight: max perpendicular deviation from the first→last line ≈ 0 (within a documented small tolerance).
    - The stroke's normalized total length ≈ 1.0 (NOT the old ≈3.27 perimeter).
    - The corrected alif passes `validateReferenceStrokes` (not a closed loop).
    - alif's three commonMistakes ids (too_short, wrong_direction, too_curved) and their checks are unchanged and remain present.
  </behavior>
  <action>
    In `assets/curriculum/letters.json`, replace alif's `referenceStrokes[0].points` (the 64-point closed outline) with the corrected open top→bottom centerline from STROKE-REFERENCE.md §7.3 — a handful of points along a near-vertical line from the top to the bottom (the scorer resamples anyway). Add `"type": "line"` to the stroke (D-03) and keep `direction: "topToBottom"`, `label: "vertical_stroke"`, `order: 1`. The exact start-x / slight Naskh lean is confirmed by the owner with his mother in Task 3's human gate — use the §7.3 candidate (x≈0.5) as the autonomous starting geometry. Leave the three `commonMistakes` exactly as-is (they become valid against the centerline per §6). Do NOT touch any other letter — the other 27 stay `referenceStrokes: []`, `signedOff: false` (D-08). Do NOT change `signedOff` in this task — it stays as-is until the human gate (Task 3). Then create `test/curriculum/alif_reference_test.dart` loading alif from the shipped letters.json (via CurriculumRepository or direct decode) and asserting the six behaviors above; document the straightness/length tolerances in comments.
  </action>
  <verify>
    <automated>flutter test test/curriculum/alif_reference_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `assets/curriculum/letters.json` alif `referenceStrokes[0]` has `type` "line", `direction` "topToBottom", first point y≈0.0, last point y≈1.0, and is NOT a closed loop.
    - No other letter's entry is modified (the 27 remain `referenceStrokes: []`, `signedOff: false`).
    - `test/curriculum/alif_reference_test.dart` asserts length≈1.0, monotonic-y, straight, and validator-pass.
    - `flutter test test/curriculum/alif_reference_test.dart` exits 0.
    - alif `signedOff` is unchanged in this task (gated to Task 3).
  </acceptance_criteria>
  <done>alif carries a correct open top→bottom centerline that passes the D-04 validator; its property test is green; the three commonMistakes are intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook the D-04 validator into CurriculumRepository load + overlay golden (load-time guard + D-07 artifact)</name>
  <files>lib/data/curriculum_repository.dart, test/data/curriculum_repository_test.dart, test/curriculum/reference_overlay_golden_test.dart, test/goldens/alif_reference_overlay.png</files>
  <read_first>
    - lib/data/curriculum_repository.dart (the `_ensureLoaded` parse path; `.fromStrings` test seam; rootBundle loader)
    - lib/core/scoring/stroke_validation.dart (validateReferenceStrokes) and lib/core/scoring/reference_path.dart (resolve — used to render the overlay path)
    - test/data/curriculum_repository_test.dart (existing tests + fixtures to extend)
    - test/flutter_test_config.dart (the FontLoader bootstrap — required so the Arabic glyph in the golden does not render tofu, Pitfall 3)
    - .planning/research/STROKE-REFERENCE.md §7.4 (load-time guard) and §7.5 (overlay golden — pen path over faint glyph, animated in draw order)
    - test/glyph_audit_golden_test.dart (the existing golden pattern + test/goldens layout to mirror)
  </read_first>
  <behavior>
    - After `_ensureLoaded` parses letters, every non-dot reference stroke is run through `validateReferenceStrokes`; if any violation is found, loading surfaces a clear error (throw with the offending letter id + violation message) rather than silently shipping bad data.
    - Loading the SHIPPED `assets/curriculum/letters.json` through the repository passes the validator (no outline ships) — an integration test asserts this.
    - The existing CurriculumRepository tests (28-letter, sort, hit/miss, cache identity) still pass.
    - A golden test renders alif's resolved reference path (via ReferencePath.resolve) as a pen path overlaid on the faint Noto Naskh alif glyph, in draw order, and matches `test/goldens/alif_reference_overlay.png`.
  </behavior>
  <action>
    In `lib/data/curriculum_repository.dart`, after the `_letters` list is built in `_ensureLoaded`, call `validateReferenceStrokes` on each letter's `referenceStrokes` and throw a descriptive error (letter id + the violation strings) if non-empty — so an outline can never load silently. Keep the `.fromStrings` test seam working and the kept-alive Riverpod provider unchanged. Extend `test/data/curriculum_repository_test.dart` with an integration test that loads the real shipped `letters.json` (not a fixture) and asserts it passes the validator, plus a test that a fixture containing a closed-loop stroke causes load to throw. Then create `test/curriculum/reference_overlay_golden_test.dart`: build a small CustomPainter (test-local or a minimal reusable widget) that draws alif's resolved reference path over the faint glyph in draw order and `matchesGoldenFile('../goldens/alif_reference_overlay.png')`; generate the baseline with `flutter test --update-goldens`. Rely on `test/flutter_test_config.dart` to load the TTFs (do not re-implement font loading). Keep stroke/reference points in-memory only — no logging of point data.
  </action>
  <verify>
    <automated>flutter test test/data/curriculum_repository_test.dart test/curriculum/reference_overlay_golden_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/data/curriculum_repository.dart` calls `validateReferenceStrokes` in `_ensureLoaded` and throws on violation with the letter id.
    - An integration test loads the shipped `letters.json` and asserts validator-pass; a fixture-with-loop test asserts load throws.
    - `test/goldens/alif_reference_overlay.png` exists and is matched by the overlay golden test (glyph renders via the TTF loader — no tofu).
    - `flutter test test/data/curriculum_repository_test.dart test/curriculum/reference_overlay_golden_test.dart` exits 0.
    - No `print`/`debugPrint` of point data is introduced.
  </acceptance_criteria>
  <done>The shipped curriculum is gated at load by the D-04 validator; the alif overlay golden is the sign-off + CI regression artifact and renders the real Arabic glyph.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Owner + mother visual sign-off, then re-set alif signedOff:true (D-06 human gate)</name>
  <files>assets/curriculum/letters.json, test/goldens/alif_reference_overlay.png</files>
  <read_first>
    - .planning/research/STROKE-REFERENCE.md §7.3 (the start-x / Naskh lean is the owner+mother's call) and §7.2 (the candidate → review → signedOff workflow)
    - .planning/phases/02.1-stroke-reference-correction/02.1-VALIDATION.md (Manual-Only Verifications — the alif sign-off row)
    - assets/curriculum/letters.json (alif entry — signedOff currently true with the now-corrected geometry)
    - test/goldens/alif_reference_overlay.png (the artifact to confirm)
  </read_first>
  <what-built>
    Tasks 1–2 replaced alif's outline with the corrected open top→bottom centerline, gated the shipped curriculum with the D-04 validator at load, and produced the alif overlay golden (pen path over the faint Noto Naskh glyph in draw order). alif's `signedOff` flag has NOT been re-affirmed by a human yet — that is this gate.
  </what-built>
  <action>
    This is a blocking human gate (autonomous:false). Do NOT have an agent silently flip `signedOff`. Walk the owner (with his mother) through the how-to-verify steps below; if she requests a different start-x / Naskh lean, adjust alif's `referenceStrokes[0].points` in `assets/curriculum/letters.json` (still a single open top→bottom line that passes `validateReferenceStrokes`) and regenerate the overlay golden via `flutter test --update-goldens`. Only after her visual confirmation, ensure alif's `signedOff: true` is re-affirmed by this explicit human action (D-06). Re-run `flutter test` to confirm green.
  </action>
  <how-to-verify>
    1. Open `test/goldens/alif_reference_overlay.png` and view the rendered alif (or run the in-app authoring/overlay surface if available) — confirm the pen path is a single straight top→bottom downstroke over the glyph, not a loop around it.
    2. With the owner's mother present, confirm the start-x and the slight Naskh lean match how she teaches alif. If she wants a different start-x/lean, adjust alif's `referenceStrokes[0].points` in `assets/curriculum/letters.json` accordingly (still a single open top→bottom line passing the validator) and regenerate the golden with `flutter test --update-goldens`.
    3. Confirm alif's three commonMistakes (too_short, wrong_direction, too_curved) read correctly against the corrected centerline.
    4. Only after that visual confirmation, ensure `assets/curriculum/letters.json` alif has `signedOff: true` (re-affirmed by this human action — D-06). If at any point it had been set false, set it true now; if it remained true with corrected geometry, this gate is the human re-affirmation that makes it legitimate.
  </how-to-verify>
  <verify>
    <human-check>Owner + mother visually confirmed the alif overlay golden matches her taught stroke (single straight top→bottom), start-x/lean approved, and alif signedOff:true is re-affirmed.</human-check>
  </verify>
  <acceptance_criteria>
    - alif `signedOff: true` in `assets/curriculum/letters.json` is re-affirmed only after the human visual confirmation (not flipped by an agent).
    - If the geometry was adjusted for lean/start-x, the overlay golden was regenerated and `flutter test` is green.
    - The full suite passes: `flutter test`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the owner+mother confirm the alif overlay and signedOff:true, or describe the start-x/lean adjustment needed.</resume-signal>
  <done>alif is re-signed-off by an explicit human action after visual overlay confirmation; the corrected centerline is the proven exemplar for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curriculum data → app (read + load-time validation) | This plan wires the validator into the load path so an outline can never load silently. |
| golden render (headless engine) | TTFs loaded into the test engine; no network, no child input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-03 | Tampering | shipped letters.json (an outline re-entering curriculum) | mitigate | The load-time `validateReferenceStrokes` guard (Task 2) throws on any non-dot closed loop; the integration test asserts the shipped file passes and a loop-fixture throws. |
| T-02.1-04 | Repudiation / integrity | alif signedOff flag | mitigate | signedOff is re-set ONLY behind the blocking human gate (Task 3, `autonomous:false`) — no agent silently flips signed-off curriculum (D-06, CLAUDE.md pedagogy boundary). |
| T-02.1-02 | Information disclosure | child / stroke point data | accept | No capture surface here; only curriculum reference data (not child data) is rendered. No point-data logging is introduced (golden renders to an image, not a log). |
| T-02.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs (flutter_test + existing TTF loader only). No legitimacy gate needed. |
</threat_model>

<verification>
- `flutter test test/curriculum/alif_reference_test.dart test/data/curriculum_repository_test.dart test/curriculum/reference_overlay_golden_test.dart` exits 0.
- `flutter test` (full suite incl. goldens) green before the human gate, and again after any lean/start-x adjustment.
- Inspection: only the alif entry changed in `letters.json`; the other 27 remain `referenceStrokes: []`, `signedOff: false`.
- The load-time guard throws on a loop fixture (negative test present).
</verification>

<success_criteria>
- alif carries a correct open top→bottom centerline (length≈1.0, monotonic, straight), its three commonMistakes are valid against it, and it is re-signed-off after a visual overlay confirmation (Phase success criterion 3).
- The shipped curriculum is gated at load and in CI by the D-04 validator; an outline cannot reach the app (Phase success criterion 1).
- A per-letter overlay golden exists as the sign-off + regression artifact (Phase success criterion 4, D-07).
</success_criteria>

<output>
Create `.planning/phases/02.1-stroke-reference-correction/02.1-02-SUMMARY.md` when done.
</output>
