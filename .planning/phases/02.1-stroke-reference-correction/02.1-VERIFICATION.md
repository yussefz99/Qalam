---
phase: 02.1-stroke-reference-correction
verified: 2026-06-01T17:00:00Z
status: human_needed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open test/goldens/alif_reference_overlay.png and confirm the rendered pen path is a single straight top-to-bottom downstroke over the alif glyph, not a loop around it."
    expected: "A single straight vertical ink line descending from y=0 to y=1, centered at x=0.5, overlaid on the faint Noto Naskh alif character. No loop, no curve that traces the glyph outline."
    why_human: "This is the D-07 sign-off artifact and the D-06 visual confirmation that the corrected centerline matches how the owner's mother teaches alif. The automated golden test confirms pixel-stability of the current image, but only a human can confirm the image is pedagogically correct."
  - test: "On the tablet (or any Android device), navigate to the /dev/authoring route and trace a downstroke over the faint alif glyph, then tap Export. Confirm the exported JSON fragment pastes correctly into a letters.json referenceStrokes field."
    expected: "The in-app authoring screen is reachable, the live ink renders as you trace, the exported JSON contains a single stroke with normalized 0..1 coordinates that matches the traced shape."
    why_human: "Real hardware pointer/stylus capture and canvas rendering cannot be fully exercised by widget tests — the automated test synthesizes pointer events but does not verify that the rendered ink feels correct on a physical tablet."
---

# Phase 02.1: Stroke Reference Correction Verification Report

**Phase Goal:** Replace broken glyph-outline reference strokes with correct open-centerline teaching strokes; in-app authoring trace screen, schema `type` field + closed-loop validator, and a re-signed-off alif. Unblocks Phase 3.
**Verified:** 2026-06-01T17:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A non-dot referenceStrokes stroke that is a closed outline loop is rejected by the D-04 validator | VERIFIED | `stroke_validation.dart:147-159` implements the two-signal NOT-CLOSED check; regression test in `stroke_spec_validation_test.dart` feeds the 64-pt alif outline loop and asserts rejection with a "loop" message. `flutter test` passes. |
| 2 | A direction string that disagrees with points.first→last order is rejected | VERIFIED | `stroke_validation.dart:162-208` implements DIRECTION check for all 5 directions; test covers bottomToTop/leftToRight mismatches. |
| 3 | A type:dot stroke with anything other than exactly one point is rejected; dots must be ordered after body strokes | VERIFIED | `stroke_validation.dart:121-137` DOT check; lines 234-248 ORDER/dots-last check. Tests cover both cases in `stroke_spec_validation_test.dart`. |
| 4 | Out-of-range coordinates and non-contiguous order values are rejected | VERIFIED | `stroke_validation.dart:93-118` RANGE check; lines 223-232 ORDER check. Tests verify both. |
| 5 | StrokeSpec.fromJson parses the new type field with a safe default of "line" | VERIFIED | `letter.dart:66` — `json['type'] as String? ?? 'line'`. `letter_test.dart` covers curve/default/dot cases. |
| 6 | The reference path resolved for the dotted guide is the identical object/value resolved for the animation and the scorer | VERIFIED | `reference_path.dart:38-44` — deterministic identity function returning deep-copied authored points in order. Four test cases in `reference_path_test.dart` pin identity, determinism, order-preservation, and immutability. |
| 7 | alif's referenceStrokes is a single open top→bottom centerline (type "line"), NOT the old 64-point closed outline | VERIFIED | `letters.json:17-31` — one stroke, `type:"line"`, `direction:"topToBottom"`, 5 points `[[0.5,0.0]..[0.5,1.0]]`. 27 other letters have `referenceStrokes:[]`, `signedOff:false`. `alif_reference_test.dart` pins length≈1.0, monotonic-y, straightness, validator-pass, three commonMistakes intact. |
| 8 | Loading the shipped letters.json through CurriculumRepository runs validateReferenceStrokes at load and throws on violation | VERIFIED | `curriculum_repository.dart:50-59` — loop over parsed letters, throws `StateError` with letter id on violation. Integration test loads shipped file (passes); closed-loop fixture test asserts `StateError` message contains "closed outline loop". |
| 9 | alif is re-signed-off (signedOff:true) after owner+mother visual confirmation | VERIFIED (code side) | `letters.json:51` — `"signedOff": true`. SUMMARY-02 records that owner+mother visually confirmed the overlay golden on 2026-06-01, approved the §7.3 candidate geometry without adjustment. See human_verification below for the visual confirmation item. |
| 10 | The Python tool can never again emit a body outline as a referenceStrokes path | VERIFIED | `extract_reference_paths.py:1-40` — DEPRECATION header present; OUTPUT_PATH points to `authoring_hints.json`; body-outline pipeline removed. `python3 tools/test_authoring_hints.py` exits 0 with PASS. |
| 11 | The in-app authoring screen is dev-only (not in child-facing nav) and exports a validator-passing referenceStrokes fragment | VERIFIED | `app_router.dart:48-49` — `/dev/authoring` route exists, not wired into any child-facing navigation. `authoring_screen.dart:93-113` — points held in memory only, no print/debugPrint. `authoring_screen_test.dart` widget test traces a downstroke and asserts the exported fragment passes `validateReferenceStrokes`. |
| 12 | A per-letter overlay golden exists as the sign-off + CI regression artifact | VERIFIED | `test/goldens/alif_reference_overlay.png` exists. `reference_overlay_golden_test.dart` passes, loads alif via `ReferencePath.resolve`, renders pen path over faint glyph, and matches the golden. |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/models/letter.dart` | StrokeSpec.type field (line\|curve\|dot) with safe default | VERIFIED | `final String type` at line 43; `fromJson` default `?? 'line'` at line 66 |
| `lib/core/scoring/stroke_validation.dart` | Pure-Dart closed-loop/direction/dot/range/order validator | VERIFIED | 251 lines (min_lines 40 satisfied); no Flutter imports; 5 named checks; documented thresholds |
| `lib/core/scoring/reference_path.dart` | ReferencePath.resolve identity, no dart:ui import | VERIFIED | 45 lines; imports only `letter.dart`; `resolve` static method; supersedes 03-RESEARCH Q1 comment present |
| `test/curriculum/stroke_spec_validation_test.dart` | Closed-loop/direction/dot/range/order regression guard | VERIFIED | 362 lines; covers all 5 violation classes; explicit regression group with 64-pt alif outline loop |
| `test/core/scoring/reference_path_test.dart` | Path-identity test (guide == animation == scorer) | VERIFIED | 4 tests: identity, determinism, order-preservation, deep-copy immutability |
| `test/models/letter_test.dart` | type-field default parsing | VERIFIED | Extended with curve/default-line/dot-single-point cases |
| `assets/curriculum/letters.json` | Corrected alif centerline + re-signed-off | VERIFIED | Single open `[[0.5,0.0]..[0.5,1.0]]` stroke; `signedOff:true`; 27 others `signedOff:false` |
| `lib/data/curriculum_repository.dart` | Load-time D-04 guard in _ensureLoaded | VERIFIED | Lines 50-59; throws StateError with letter id; validates into local before caching |
| `test/curriculum/alif_reference_test.dart` | alif centerline property tests | VERIFIED | 6 tests: type/direction, y-monotonic, near-straight, length≈1.0, validator-pass, 3 commonMistakes |
| `test/data/curriculum_repository_test.dart` | Shipped-file integration test + closed-loop fixture test | VERIFIED | Line 191: loads shipped file, passes. Line 204: closed-loop fixture, asserts StateError message "closed outline loop" |
| `test/curriculum/reference_overlay_golden_test.dart` | Per-letter pen-path-over-glyph overlay golden | VERIFIED | Renders `ReferencePath.resolve` output over faint glyph; uses `QalamFonts.arabic` token (IN-01 fixed) |
| `test/goldens/alif_reference_overlay.png` | The alif overlay golden baseline | VERIFIED | File exists; golden test passes |
| `lib/dev/authoring_screen.dart` | Dev-only Riverpod trace-over-glyph authoring screen | VERIFIED | 418 lines (min_lines 60 satisfied); ConsumerStatefulWidget; Listener+CustomPainter; points in-memory only; not in child-facing nav |
| `lib/dev/authoring_export.dart` | Pure-Dart normalize + serialize traced strokes | VERIFIED | No Flutter widget imports; combined-bbox normalization; 4-dp rounding; zero-range guard |
| `test/features/authoring/authoring_screen_test.dart` | Widget test: trace, tag, export, validator-pass | VERIFIED | 4 export-helper cases + 1 widget test; all 5/5 pass |
| `tools/extract_reference_paths.py` | Repurposed extractor: hints only + deprecation header | VERIFIED | DEPRECATION header; OUTPUT_PATH = authoring_hints.json; no body-outline polyline code path |
| `tools/authoring_hints.json` | New hints output (dot centroids + bbox stub) | VERIFIED | File exists as documented stub (fonttools absent); _stub key documents regeneration command |
| `tools/test_authoring_hints.py` | Python check: no body outlines emitted | VERIFIED | `python3 tools/test_authoring_hints.py` exits 0 with PASS |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `stroke_validation.dart` | `letter.dart` | validates StrokeSpec instances (type, points, direction, order) | VERIFIED | imports `letter.dart`; all checks access `stroke.type`, `stroke.points`, `stroke.direction`, `stroke.order`, `stroke.label` |
| `stroke_spec_validation_test.dart` | `stroke_validation.dart` | asserts closed-loop and direction-mismatch rejection | VERIFIED | imports and calls `validateStroke` / `validateReferenceStrokes`; regression group present |
| `curriculum_repository.dart` | `stroke_validation.dart` | calls validateReferenceStrokes in _ensureLoaded | VERIFIED | line 7 import; line 52 call in `_ensureLoaded` |
| `curriculum_repository_test.dart` | `assets/curriculum/letters.json` | loads SHIPPED letters.json, asserts validator passes | VERIFIED | line 194 `File('assets/curriculum/letters.json').readAsStringSync()` |
| `reference_overlay_golden_test.dart` | `assets/curriculum/letters.json` | renders alif's resolved reference path | VERIFIED | loads alif via `File('assets/curriculum/letters.json')`; calls `ReferencePath.resolve` |
| `authoring_screen.dart` | `authoring_export.dart` | normalizes captured local points to 0..1 and serializes | VERIFIED | line 29 import; line 146 `exportReferenceStrokesJson(captured)` |
| `authoring_export.dart` | `stroke_validation.dart` | exported fragment validatable by D-04 guard | VERIFIED | imports `stroke_validation.dart`; widget test asserts exported fragment passes `validateReferenceStrokes` |
| `extract_reference_paths.py` | `tools/authoring_hints.json` | writes dot centroids + bbox | VERIFIED | `OUTPUT_PATH = Path("tools/authoring_hints.json")` at line 49 |

### Data-Flow Trace (Level 4)

Not applicable. This phase produces curriculum data, validators, and developer tooling — no dynamic data rendering to end users. The key data flows (letters.json → CurriculumRepository → validator) are verified via integration tests.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Python invariant check exits 0 | `python3 tools/test_authoring_hints.py` | PASS, exit 0 | PASS |
| All Phase 02.1 tests pass (46 tests) | `flutter test test/models/letter_test.dart test/curriculum/stroke_spec_validation_test.dart test/core/scoring/reference_path_test.dart test/curriculum/alif_reference_test.dart test/data/curriculum_repository_test.dart test/curriculum/reference_overlay_golden_test.dart test/features/authoring/authoring_screen_test.dart` | 46/46 pass | PASS |
| Full suite: only pre-existing golden fails | `flutter test` | 61 pass, 1 fail (glyph_audit_golden_test.dart — D-12 Noto Naskh golden, 1.19% px diff, pre-existing) | PASS (pre-existing excluded) |

### Probe Execution

No probes declared in PLAN files for this phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CUR-01 | 02.1-01, 02.1-02, 02.1-03, 02.1-04 | Ordered centerline paths; owner's-mother sign-off gate; prerequisite for Phase 3 trace loop | SATISFIED (partially — seed for Phase 7) | All 4 plans claim CUR-01; alif is the signed-off exemplar with corrected centerline; load-time guard prevents outlines from reaching the app; REQUIREMENTS.md notes CUR-01 is "seeded in Phase 2, fully satisfied in Phase 7" |

**CUR-01 traceability note:** REQUIREMENTS.md maps CUR-01 to Phase 7 for full satisfaction, with an explicit note that it is "seeded in Phase 2 (small signed-off subset that unblocks the end-to-end slice)." Phase 02.1 delivers the corrective seed: one signed-off letter (alif) with a correct centerline, the schema type field, the load-time validator, and the authoring tool for remaining letters. This is the stated scope. Full CUR-01 satisfaction (all 28 letters) is deferred to Phase 7 by design.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `lib/dev/authoring_screen.dart` | 319 | `shouldRepaint(_InkPainter old) => old.strokes != strokes` (WR-04 from code review — unaddressed) | Warning | Dev-tool only; correctness is accidental (relies on `_allInk` always building a new list). No child data, no production impact. |
| `test/models/letter_test.dart` | 27-36 | Dot fixture has 2 points + `topToBottom` (IN-02 from code review — unaddressed) | Info | Misleading example; model does not validate (correct). Test still passes; validator would reject this fixture. |
| `lib/data/curriculum_repository.dart` | 79-82, 91-97 | `firstWhere` + catch StateError for null-return control flow (IN-03 — unaddressed) | Info | Works correctly; code smell only; `firstWhereOrNull` from package:collection would be cleaner. |
| `tools/extract_reference_paths.py` | 224-243 | Broad `except Exception` swallows contour-parsing failures silently (IN-04 — unaddressed) | Info | Offline authoring tool only; no app runtime impact; malformed font would produce empty hints with no warning. |

No TBD/FIXME/XXX debt markers found in any file modified by this phase.

**WR-04, IN-02, IN-03, IN-04 are all non-blocking.** WR-04 is a dev-tool-only repaint correctness issue. IN-02/03/04 are code quality improvements. None affect the phase goal (alif correction, validator, authoring tool, Phase 3 unblocking).

### Human Verification Required

#### 1. alif Overlay Golden Visual Confirmation

**Test:** Open `/Users/mareekhalila/Documents/Qalam/qalam/test/goldens/alif_reference_overlay.png` and view it. Alternatively, run `flutter test --update-goldens test/curriculum/reference_overlay_golden_test.dart` and view the result.
**Expected:** A single straight vertical ink line descending from the top of the canvas to the bottom, centered horizontally (x=0.5), overlaid on the faint parchment-background Noto Naskh alif character. A gold circle marks the start point at the top. There is no loop around the glyph, no curved path tracing the outline, and no return to start.
**Why human:** The automated golden test confirms the image has not changed since it was generated; it cannot confirm the image shows a pedagogically correct stroke. Only the owner (and ideally his mother) can confirm that the rendered pen path matches how she teaches alif — a single straight top-to-bottom downstroke.

#### 2. In-App Authoring Screen on Real Hardware

**Test:** On an Android tablet, navigate to the dev screen at `/dev/authoring` (accessible via the developer route in the app). Trace the alif letter over the faint glyph, tap Export, and copy the exported JSON fragment.
**Expected:** The live ink renders clearly as you trace. The exported JSON is a valid `referenceStrokes` array with normalized 0..1 coordinates. Pasting it into `assets/curriculum/letters.json` as a test does not cause `flutter test` to fail.
**Why human:** Pointer capture and canvas rendering on real hardware (particularly with a stylus, which the owner intends to use eventually) cannot be fully verified by synthesized pointer events in widget tests. The physical feel of tracing and the visual fidelity of the live ink preview require hands-on confirmation.

### Gaps Summary

No gaps. All 12 must-have truths are verified. The two human verification items above are quality/pedagogy confirmations, not blocking defects.

**Pre-existing known issue (NOT a phase gap):** `test/glyph_audit_golden_test.dart` (Phase-1 D-12 Noto Naskh golden) fails by 1.19% pixels in the current environment. This failure is identical at the phase base commit `363995b` (before any 02.1 work) — an environment/font-rendering mismatch on this machine, not introduced by Phase 02.1. It should be resolved by regenerating the Phase-1 baseline with `flutter test --update-goldens test/glyph_audit_golden_test.dart` on the current toolchain.

---

_Verified: 2026-06-01T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
