---
phase: 02.1-stroke-reference-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/letter.dart
  - lib/core/scoring/stroke_validation.dart
  - lib/core/scoring/reference_path.dart
  - test/models/letter_test.dart
  - test/curriculum/stroke_spec_validation_test.dart
  - test/core/scoring/reference_path_test.dart
autonomous: true
requirements: [CUR-01]

must_haves:
  truths:
    - "A non-dot referenceStrokes stroke that is a closed outline loop is rejected by a pure-Dart validator (the original bug cannot reach letters.json)"
    - "A direction string that disagrees with the actual points.first→points.last order is rejected"
    - "A type:dot stroke with anything other than exactly one point is rejected; dots must be ordered after body strokes"
    - "Out-of-range coordinates and non-contiguous order values are rejected"
    - "StrokeSpec.fromJson parses the new type field with a safe default of line"
    - "The reference path resolved for the dotted guide is the identical object/value resolved for the animation and the scorer (one source of truth, S1-04)"
  artifacts:
    - path: "lib/models/letter.dart"
      provides: "StrokeSpec.type field (line|curve|dot) with safe default"
      contains: "final String type"
    - path: "lib/core/scoring/stroke_validation.dart"
      provides: "Pure-Dart closed-loop/direction/dot/range/order validator (D-04 crown-jewel guard)"
      min_lines: 40
    - path: "lib/core/scoring/reference_path.dart"
      provides: "ReferencePath.resolve identity over authored centerline strokes (S1-04 one source of truth)"
      contains: "resolve"
    - path: "test/curriculum/stroke_spec_validation_test.dart"
      provides: "The closed-loop/direction/dot/range/order regression guard tests"
      min_lines: 40
    - path: "test/core/scoring/reference_path_test.dart"
      provides: "Path-identity test (guide == animation == scorer)"
    - path: "test/models/letter_test.dart"
      provides: "type-field default parsing test (extends Phase 2 tests)"
  key_links:
    - from: "lib/core/scoring/stroke_validation.dart"
      to: "lib/models/letter.dart"
      via: "validates StrokeSpec instances (type, points, direction, order)"
      pattern: "StrokeSpec"
    - from: "test/curriculum/stroke_spec_validation_test.dart"
      to: "lib/core/scoring/stroke_validation.dart"
      via: "asserts the validator rejects a closed loop and a direction mismatch"
      pattern: "validate"
---

<objective>
Add the `type` field to `StrokeSpec` (D-03), build the pure-Dart closed-loop/direction/dot/range/order validator (D-04 — the crown-jewel guard that makes the original outline bug impossible to reintroduce), and add the one-source-of-truth `ReferencePath.resolve` (S1-04) as an identity over authored centerline strokes. This is the schema-and-guard half of Slice 1; Plan 02 then re-authors alif against it.

Purpose: The closed-loop validator is the single most valuable artifact in this phase — it is the regression guard that would have caught the Phase-2 outline defect at load and in CI. The `type` field removes the fragile "is this a dot? infer from points.length" magic. `ReferencePath.resolve` becomes trivial (identity) precisely because we author correct centerlines, giving the cleanest realization of "authored path == animated path == scored path."

Output: Modified `StrokeSpec`, two new pure-Dart files under `lib/core/scoring/`, and the three Wave-0 test files (validation guard, path identity, type-default) turned green.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STROKE-REFERENCE.md
@.planning/phases/02.1-stroke-reference-correction/02.1-CONTEXT.md
@.planning/phases/02.1-stroke-reference-correction/02.1-VALIDATION.md
@lib/models/letter.dart
@test/models/letter_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the type field to StrokeSpec (D-03) with a safe default</name>
  <files>lib/models/letter.dart, test/models/letter_test.dart</files>
  <read_first>
    - lib/models/letter.dart (the StrokeSpec class — order/label/points/direction)
    - test/models/letter_test.dart (existing StrokeSpec.fromJson test to extend)
    - .planning/research/STROKE-REFERENCE.md §5 (recommended minimal model change; the safe-default rationale)
  </read_first>
  <behavior>
    - StrokeSpec.fromJson with a "type" key of "curve" yields type == "curve".
    - StrokeSpec.fromJson with NO "type" key yields type == "line" (safe default `json['type'] as String? ?? 'line'` — existing Phase-2 data still parses).
    - A type:"dot" stroke with points [[x,y]] parses with type == "dot" and one point.
    - Existing full-data and placeholder Letter.fromJson tests still pass unchanged.
  </behavior>
  <action>
    Add a `final String type;` field to `StrokeSpec` (line/curve/dot per D-03), add it to the const constructor as a required-with-default-in-factory parameter, and parse it in `StrokeSpec.fromJson` as `json['type'] as String? ?? 'line'` so existing `letters.json` and the Phase-2 tests still parse. Keep `order`, `label`, `points` (normalized 0..1 List<List<double>>), and `direction` exactly as-is — points remain the source of truth, direction stays a human-readable hint. Do NOT add Dart enums or codegen — keep it a plain String to stay low-magic for the owner (Claude's Discretion notes this). Extend `test/models/letter_test.dart` with the four behavior cases above (do not rewrite existing tests).
  </action>
  <verify>
    <automated>flutter test test/models/letter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/letter.dart` contains `final String type`.
    - `StrokeSpec.fromJson` reads `json['type'] as String? ?? 'line'`.
    - `flutter test test/models/letter_test.dart` exits 0, including the new type-default case.
    - No Dart enum or build_runner codegen introduced for the type field.
  </acceptance_criteria>
  <done>StrokeSpec carries a validated-elsewhere `type` field that defaults to "line"; all letter model tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the closed-loop/direction/dot/range/order validator (D-04 crown-jewel guard)</name>
  <files>lib/core/scoring/stroke_validation.dart, test/curriculum/stroke_spec_validation_test.dart</files>
  <read_first>
    - lib/models/letter.dart (StrokeSpec after Task 1 — type/order/label/points/direction)
    - .planning/research/STROKE-REFERENCE.md §7.4 (the five named checks) and §2 (why alif's 64-pt loop is wrong: first→last distance 0.2234, perimeter 3.27, one vertical sign-change)
    - .planning/phases/02.1-stroke-reference-correction/02.1-VALIDATION.md (the per-behavior test map this guard must satisfy)
  </read_first>
  <behavior>
    - A closed-loop stroke (points.first ≈ points.last AND total path length ~2–3× the bbox diagonal — i.e. alif's old 64-pt outline) is REJECTED for type != "dot".
    - A valid open top→bottom centerline ([[0.5,0.0]..[0.5,1.0]], type:"line", direction:"topToBottom") PASSES.
    - direction "topToBottom" with points where points.last.y < points.first.y is REJECTED (direction disagrees with point order); each of topToBottom/bottomToTop/leftToRight/rightToLeft/tap is checked against first→last.
    - type:"dot" with !=1 point is REJECTED; a dot with exactly one point and direction "tap" PASSES.
    - A coordinate outside [0,1] is REJECTED.
    - order values not contiguous 1..N, or a dot ordered before a body stroke, are REJECTED.
  </behavior>
  <action>
    Create `lib/core/scoring/stroke_validation.dart` (pure Dart, no Flutter imports) exposing a validator over a `List<StrokeSpec>` (e.g. `validateReferenceStrokes(List<StrokeSpec>) -> List<String>` returning human-readable violation messages, empty == valid) plus a single-stroke helper. Implement the five D-04 named checks: (1) NOT-CLOSED for non-dot strokes — `distance(points.first, points.last) > epsilon` AND total polyline length is NOT roughly 2–3× the bbox diagonal (catch an outline sneaking back in); pick a documented epsilon and length-ratio threshold (Claude's Discretion — comment the chosen values and why); (2) DIRECTION agrees with `points.first → points.last` for each direction string; (3) DOT sanity: `type == "dot"` ⇒ exactly one point; (4) RANGE: all coords in [0,1]; (5) ORDER: `order` 1..N contiguous, dots after body strokes. Do NOT use fenced code in any other artifact; this is the only place thresholds live. Then create `test/curriculum/stroke_spec_validation_test.dart` covering every behavior case above, INCLUDING a regression test that feeds alif's original 64-point closed outline (reuse the loop signature: first≈last, perimeter≈3.27) and asserts it is rejected — this is the test that makes the original bug impossible to reintroduce.
  </action>
  <verify>
    <automated>flutter test test/curriculum/stroke_spec_validation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/scoring/stroke_validation.dart` exists, imports no Flutter packages (pure Dart), and is at least 40 lines.
    - The validator rejects a closed loop, a direction/point mismatch, a multi-point dot, an out-of-range coord, and a non-contiguous order.
    - `test/curriculum/stroke_spec_validation_test.dart` contains an explicit regression case asserting alif's old 64-point outline loop is rejected.
    - `flutter test test/curriculum/stroke_spec_validation_test.dart` exits 0.
    - Chosen epsilon and length-ratio threshold are documented in a comment.
  </acceptance_criteria>
  <done>The pure-Dart D-04 guard rejects all five violation classes; the regression test pins the original outline bug as permanently caught.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ReferencePath.resolve identity + path-identity test (S1-04 one source of truth)</name>
  <files>lib/core/scoring/reference_path.dart, test/core/scoring/reference_path_test.dart</files>
  <read_first>
    - .planning/research/STROKE-REFERENCE.md §6 (one source of truth; resolve becomes the identity function on correct authored data; this supersedes 03-RESEARCH Q1 derive-from-outline)
    - .planning/phases/03-trace-one-letter-end-to-end/03-RESEARCH.md lines 190-213 and 243-256 (the lib/core/scoring/reference_path.dart structure Phase 3 builds the scorer/animation around; `ReferencePath.resolve(letter.referenceStrokes)`)
    - lib/models/letter.dart (StrokeSpec — what resolve consumes)
  </read_first>
  <behavior>
    - `ReferencePath.resolve(strokes)` over a correct authored centerline returns the same ordered point data the dotted guide, the animation, and the scorer all consume — i.e. for authored centerlines it is the identity over the points (no outline-derivation step).
    - Calling resolve twice with the same input yields equal results (a single deterministic source of truth — guide == animation == scorer).
    - resolve preserves stroke order and per-stroke point order.
  </behavior>
  <action>
    Create `lib/core/scoring/reference_path.dart` (pure Dart, no Flutter UI imports — it returns plain point data, not a `dart:ui` Path; Phase 3 wraps it into a `Path` for PathMetric) exposing `ReferencePath.resolve(List<StrokeSpec>)` that returns the resolved ordered points for scoring/animation. Per STROKE-REFERENCE.md §6, on correct authored centerline data this is the IDENTITY (just returns the authored points in order) — leave a header comment stating this supersedes 03-RESEARCH Q1's derive-from-outline option and that the function exists so the guide/animation/scorer share exactly one resolution. Keep it minimal; Phase 3 owns the scorer and the dart:ui Path wrapping. Then create `test/core/scoring/reference_path_test.dart` asserting the three behaviors above (the path-identity / one-source-of-truth contract).
  </action>
  <verify>
    <automated>flutter test test/core/scoring/reference_path_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/scoring/reference_path.dart` exists, exposes `resolve`, and imports no `dart:ui`/Flutter widget packages.
    - `test/core/scoring/reference_path_test.dart` asserts resolve is deterministic and order-preserving (guide == animation == scorer source).
    - `flutter test test/core/scoring/reference_path_test.dart` exits 0.
    - A header comment notes this supersedes 03-RESEARCH Q1 derive-from-outline.
  </acceptance_criteria>
  <done>`ReferencePath.resolve` is the deterministic identity over authored centerlines; the path-identity test pins S1-04's one-source-of-truth contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curriculum data → app (read) | `letters.json` reference data is read into typed models; this plan adds the validator that gates that data. No child input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | reference stroke data (an outline loop re-entering letters.json) | mitigate | The D-04 validator (Task 2) rejects closed loops/out-of-range/order violations; the regression test pins alif's old outline as permanently caught. This is the core guard of the phase. |
| T-02.1-02 | Information disclosure | child stroke points | accept | This plan touches NO capture surface — it operates only on curriculum reference data (not child data). No `print`/`debugPrint` of any point data is introduced. |
| T-02.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure-Dart additions; flutter_test already present). No legitimacy gate needed. |
</threat_model>

<verification>
- `flutter test test/models/letter_test.dart test/curriculum/stroke_spec_validation_test.dart test/core/scoring/reference_path_test.dart` exits 0.
- `flutter analyze` reports no new errors in the three new/modified lib files.
- The validator file imports no Flutter packages (pure Dart) — confirm by inspection.
</verification>

<success_criteria>
- StrokeSpec has a `type` field defaulting to "line"; existing Phase-2 data and tests still parse (D-03).
- The D-04 validator rejects closed loops, direction/point mismatches, malformed dots, out-of-range coords, and bad order — with a regression test pinning alif's original outline (Phase success criterion 1).
- `ReferencePath.resolve` is a deterministic identity over authored centerlines (Phase success criterion 4, S1-04).
</success_criteria>

<output>
Create `.planning/phases/02.1-stroke-reference-correction/02.1-01-SUMMARY.md` when done.
</output>
