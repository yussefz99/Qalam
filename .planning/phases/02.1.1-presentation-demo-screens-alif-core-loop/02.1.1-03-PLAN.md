---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 03
type: execute
wave: 2
depends_on: [02.1.1-01, 02.1.1-02]
files_modified:
  - lib/demo/screens/demo_home_screen.dart
  - lib/demo/screens/demo_watch_screen.dart
  - lib/router/demo_routes.dart
  - test/demo/demo_home_screen_test.dart
  - test/demo/demo_watch_screen_test.dart
autonomous: true
requirements: [S1-04, PLAT-03]
must_haves:
  truths:
    - "The demo Home shows the real idle mascot, a warm static greeting, and an alif 'Today's Lesson' card that navigates to Watch."
    - "The demo Home shows NO running star counter, NO weekly tally, NO 3-star lesson rating — the gamification chrome from the mockup is omitted."
    - "The Watch screen shows the dotted alif with a numbered stroke-order start-dot and the write-pose mascot, and a 'Start Tracing' affordance that navigates to Trace."
  artifacts:
    - path: "lib/demo/screens/demo_home_screen.dart"
      provides: "De-gamified demo Home (mascot + greeting + alif lesson card → Watch)"
      min_lines: 60
    - path: "lib/demo/screens/demo_watch_screen.dart"
      provides: "Watch screen: dotted alif guide + numbered start-dot + write-pose mascot + Start Tracing CTA → Trace"
      min_lines: 60
  key_links:
    - from: "lib/demo/screens/demo_home_screen.dart"
      to: "/demo/watch"
      via: "the alif lesson card onTap context.go"
      pattern: "context\.go.*demo/watch"
    - from: "lib/demo/screens/demo_watch_screen.dart"
      to: "/demo/trace"
      via: "the Start Tracing CTA context.go"
      pattern: "context\.go.*demo/trace"
    - from: "lib/demo/screens/demo_watch_screen.dart"
      to: "DemoAlif reference points"
      via: "CustomPainter renders the dotted alif from the single reference source"
      pattern: "DemoAlif"
---

<objective>
Build the first two screens of the walkthrough — the de-gamified demo Home and the Watch
(stroke-order) screen — wiring them into the /demo route slots created in Wave 1.

Purpose: Home and Watch open the narrative (DP-04). Home must prove the anti-gamification
stance even in the demo (DP-06) — the mockup's star counter / weekly tally / 3-star rating
are OMITTED. Watch shows the dotted alif with the numbered gold start-dot and the write-pose
mascot demonstrating, with a single clear affordance into Trace (DP-03).
Output: demo_home_screen.dart, demo_watch_screen.dart, and the two route builders wired.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@lib/screens/home_screen.dart
@lib/theme/colors.dart
@lib/theme/dimens.dart
@lib/theme/text_styles.dart
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-01-SUMMARY.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Demo Home screen — de-gamified, mascot + greeting + alif lesson card → Watch</name>
  <files>lib/demo/screens/demo_home_screen.dart, lib/router/demo_routes.dart, test/demo/demo_home_screen_test.dart</files>
  <read_first>
    - lib/screens/home_screen.dart (the Phase-1 home shell to upgrade: null-safe l10n read `AppLocalizations.of(context)?.x ?? fallback`, the AppBar wordmark, the _OpenPracticeButton sticker-shadow CTA via QalamTheme extension, the ArabicText island, ConstrainedBox(maxWidth) layout)
    - lib/widgets/qalam_mascot.dart (QalamMascot(pose: QalamPose.idle) — from plan 01)
    - lib/demo/demo_alif.dart (DemoAlif.glyph/displayName/arabicName — from plan 01)
    - lib/router/demo_routes.dart (the _DemoPlaceholder builder for /demo/home to replace; DemoStep.home/.watch path helpers)
    - lib/theme/colors.dart, lib/theme/dimens.dart, lib/theme/text_styles.dart (tokens: QalamColors.bg/surface/primary, QalamSpace, QalamTargets.targetComfy, QalamTextStyles.display/body/label/button)
    - docs/design/kit/project/screenshots/home.png (warmth target) + 03-UI-SPEC "Mandatory omissions" table (OMIT: header star counter, weekly tally block, three lesson stars)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-06 (anti-gamification BINDING) + specifics (static placeholder greeting name)
  </read_first>
  <behavior>
    - Test 1: DemoHomeScreen renders the idle-pose QalamMascot, the warm greeting text (demoHomeGreeting), and an alif "Today's Lesson" card showing the alif glyph via ArabicText and the lesson title.
    - Test 2: tapping the alif lesson card navigates to /demo/watch (assert the router location changed; use a test GoRouter).
    - Test 3 (anti-gamification, BINDING): the rendered tree contains NO running star count, NO weekly-tally widget, NO three-star lesson rating, NO "+N" hype — assert there is no Text matching a star-count/"stars"/"weekly"/"streak" pattern and the icons/star.svg is absent from Home.
    - Test 4: no MaterialApp debug banner concerns — the screen is a plain Scaffold on QalamColors.bg (parchment, never white) with tokens only.
  </behavior>
  <action>
    Create `DemoHomeScreen` by adapting home_screen.dart's structure: a parchment Scaffold with a left nav-rail-free, screenshot-clean layout (the demo does not need the Journey/Parent rail — keep it minimal and warm). Show QalamMascot(idle) beside a warm static greeting (demoHomeGreeting, placeholder name) and a body line; below it an alif "Today's Lesson" card (eyebrow demoLessonEyebrow + title demoLessonTitle + the alif glyph in an ArabicText display island, glyph from DemoAlif) styled as a raised surface card (QalamColors.surfaceRaised/surface + QalamShadows + QalamRadii.xl), tappable → `context.go(DemoStep.watch.path)`. The card is a >= targetComfy touch target. OMIT every gamification element from the mockup (DP-06): no header "⭐ N" counter, no "THIS WEEK · N stars" block, no three lesson stars, no progress bar reading as score. Replace the /demo/home placeholder builder in demo_routes.dart with `const DemoHomeScreen()`. Tokens only; copy via gen-l10n with null-safe reads; no raw hex.
  </action>
  <acceptance_criteria>
    - `lib/demo/screens/demo_home_screen.dart` defines `DemoHomeScreen` and references `QalamPose.idle`, `DemoAlif`, and `ArabicText`.
    - No raw hex and no `Colors.` in the file (grep returns nothing for `0x[0-9A-Fa-f]{8}` and `Colors\.`).
    - No gamification tokens: `grep -Ei "weekly|streak|badge|stars\b" lib/demo/screens/demo_home_screen.dart` returns nothing; `grep -c "star.svg" lib/demo/screens/demo_home_screen.dart` returns 0.
    - The /demo/home route builder in demo_routes.dart now returns DemoHomeScreen (grep "DemoHomeScreen" demo_routes.dart succeeds).
    - All four behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/demo_home_screen_test.dart && ! grep -Ei "weekly|streak|badge" lib/demo/screens/demo_home_screen.dart && grep -q "DemoHomeScreen" lib/router/demo_routes.dart && flutter analyze lib/demo/screens/demo_home_screen.dart</automated>
  </verify>
  <done>The de-gamified demo Home renders the idle mascot, warm greeting, and alif lesson card → Watch, with all gamification chrome omitted and asserted absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Watch screen — dotted alif + numbered start-dot + write mascot → Trace</name>
  <files>lib/demo/screens/demo_watch_screen.dart, lib/router/demo_routes.dart, test/demo/demo_watch_screen_test.dart</files>
  <read_first>
    - lib/screens/practice_screen.dart (the CustomPainter + parchment-inside-soft-aqua-card pattern to mirror for the dotted-guide canvas; quadratic-smoothed Path drawing)
    - lib/demo/demo_alif.dart (DemoAlif reference points — the single source for the dotted alif geometry)
    - lib/core/scoring/reference_path.dart (ReferencePath.resolve — the one-source-of-truth resolver; the dotted guide must derive from these points, NOT Text('ا') — Pitfall 5)
    - lib/widgets/qalam_mascot.dart (QalamMascot(pose: QalamPose.write))
    - lib/router/demo_routes.dart (the /demo/watch placeholder to replace; DemoStep.trace.path)
    - lib/theme/colors.dart (QalamColors.bg/inkStroke for ink, QalamColors.reward for the numbered start-dot — gold is REWARDS-ONLY and the start-dot is an allowed gold use per UI-SPEC) + dimens.dart (QalamInk.strokeWidth, QalamTargets)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (Watch eyebrow "WATCH · STROKE ORDER", heading "Watch me write alif.", tip card "Start at the gold dot. Follow the line down."; OMIT "Play sound"/"Listen"; gold start-dot allowed)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-01 (static, no engine — the demo guide may be a static painted dotted path; no animation required, animation optional)
  </read_first>
  <behavior>
    - Test 1: DemoWatchScreen renders the write-pose QalamMascot, the eyebrow (demoWatchEyebrow), the heading (demoWatchHeading), and the tip card (demoWatchTip).
    - Test 2: the dotted alif guide is painted by a CustomPainter that consumes DemoAlif reference points (the alif vertical line) — assert a CustomPaint is present and the screen does NOT render the glyph as Text('ا') (one source of truth, Pitfall 5).
    - Test 3: the numbered gold start-dot uses QalamColors.reward (the only allowed gold here) and is non-interactive (not a touch target).
    - Test 4: a "Start Tracing" CTA (demoStartTracing, >= targetComfy) navigates to /demo/trace; there is NO "Play sound"/"Listen" affordance (DP-07 omission).
  </behavior>
  <action>
    Create `DemoWatchScreen`: a parchment Scaffold with the eyebrow/heading at top (QalamTextStyles.label/display), the write-pose mascot beside a raised canvas card (mirror practice_screen's soft-aqua-frame + parchment-inset card), and a `_DottedAlifPainter` CustomPainter that draws the alif guide as a DOTTED path from `ReferencePath.resolve(DemoAlif.referenceStrokes)` / DemoAlif's points (NOT Text) with a numbered gold start-dot (QalamColors.reward) at the stroke's first point. The guide may be fully static (no animation needed for the demo, DP-01); a short auto-play pen draw is allowed at Claude's discretion but must not block the screenshot state. Below: the tip card (demoWatchTip) and a primary "Start Tracing" sticker-shadow CTA (>= targetComfy) → `context.go(DemoStep.trace.path)`. OMIT any audio/"Listen"/"Play sound" affordance (DP-07). Replace the /demo/watch placeholder builder with `const DemoWatchScreen()`. Tokens only; gold used ONLY for the start-dot; copy via gen-l10n; no raw hex.
  </action>
  <acceptance_criteria>
    - `lib/demo/screens/demo_watch_screen.dart` defines `DemoWatchScreen`, references `QalamPose.write`, `DemoAlif`, and uses a `CustomPaint`/`CustomPainter` for the guide.
    - The glyph is NOT rendered as Text('ا') on this screen: `grep -c "Text('ا')\|Text(\"ا\")" lib/demo/screens/demo_watch_screen.dart` returns 0.
    - No audio affordance: `grep -Ei "listen|play sound|playsound" lib/demo/screens/demo_watch_screen.dart` returns nothing.
    - Gold is used only via QalamColors.reward for the start-dot (grep "QalamColors.reward" succeeds; no other reward-color misuse — start-dot only).
    - The /demo/watch builder in demo_routes.dart returns DemoWatchScreen (grep succeeds).
    - All four behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/demo_watch_screen_test.dart && ! grep -Ei "listen|play sound" lib/demo/screens/demo_watch_screen.dart && grep -q "DemoWatchScreen" lib/router/demo_routes.dart && flutter analyze lib/demo/screens/demo_watch_screen.dart</automated>
  </verify>
  <done>The Watch screen renders the dotted alif (from the single reference source, never Text), the numbered gold start-dot, the write-pose mascot, and a Start Tracing CTA → Trace, with no audio chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference data → painted guide | DemoAlif points drive the dotted-guide CustomPainter |
| card tap / CTA → navigation | user taps move the walkthrough forward |

Offline, mocked-data UI demo: no persisted input, no auth, no network, no secrets. ASVS L1
surface is minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.1-07 | Tampering | dotted-guide geometry | accept | Guide derives from validated DemoAlif/ReferencePath points (one source of truth); no user-tamperable input feeds the painter. |
| T-02.1.1-08 | Denial of Service | missing mascot on Watch/Home | mitigate | QalamMascot graceful fallback (plan 01) keeps both screens screenshot-stable if an asset is missing. |
| T-02.1.1-09 | Repudiation | navigation | accept | No security-relevant actions occur; navigation is a presentation walkthrough with no state mutation. |
| T-02.1.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs; all deps already vetted. |
</threat_model>

<verification>
- `flutter test test/demo/demo_home_screen_test.dart test/demo/demo_watch_screen_test.dart` green.
- `flutter analyze` clean for both screens.
- Home → Watch → Trace navigation works; gamification chrome asserted absent on Home; no audio chrome on Watch.
</verification>

<success_criteria>
- Demo Home is de-gamified (DP-06) with the idle mascot, warm greeting, and alif card → Watch (DP-04).
- Watch shows the dotted alif from the single reference source (Pitfall 5), the gold start-dot, the write mascot, and Start Tracing → Trace (DP-03/DP-07).
- Tokens only, copy via gen-l10n, parchment never white.
</success_criteria>

<output>
Create `.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-03-SUMMARY.md` when done.
</output>
