---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 05
subsystem: ui
tags: [flutter, go_router, gen-l10n, anti-gamification, rtl, custom-painter]

# Dependency graph
requires:
  - phase: 02.1.1-presentation-demo-screens-alif-core-loop
    provides: DottedGuidePainter + inkColor override (plan 04); QalamMascot tryAgain/cheer/idle poses + DemoAlif.heroMissFix (plan 01); demoMissFix/demoPassPraise/demoCelebration*/demoBackHome/demoTryAgain l10n + DemoStep chain (plan 02)
  - phase: 01-foundation
    provides: QalamColors.warnSoft/success/reward tokens, ArabicText RTL island, QalamTheme buttonShadow, bundled assets/icons/star.svg
provides:
  - DemoFeedbackScreen (miss = HERO coral failing-stroke + named fix → Try Again to clean-pass; pass = specific praise → Celebration)
  - DemoCelebrationScreen (cheer mascot + exactly ONE gold star + "You Learned Alif." + "أحسنت" + Back Home → demo Home)
  - demoPassContinue l10n key (clean-pass CTA label)
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Route-selected screen variants: one DemoFeedbackScreen takes a DemoFeedbackVariant (miss|pass), wired as two _screenFor cases — no duplicate scaffold"
    - "Coral failing-stroke via DottedGuidePainter(inkColor: QalamColors.warnSoft) — the reusable painter's optional ink color carries the only error treatment (never red)"
    - "Single mastery star = SvgPicture.asset(star.svg, colorFilter reward) with a graceful Icon fallback; exactly one, never a counter"

key-files:
  created:
    - lib/demo/screens/demo_feedback_screen.dart
    - lib/demo/screens/demo_celebration_screen.dart
    - test/demo/demo_feedback_screen_test.dart
    - test/demo/demo_celebration_screen_test.dart
  modified:
    - lib/router/demo_routes.dart
    - lib/demo/widgets/dotted_guide_painter.dart
    - lib/l10n/app_en.arb

key-decisions:
  - "The miss Try Again navigates via DemoStep.feedbackMiss.next.path (→ feedbackPass) — the retry-now-clean narrative is reachable by tapping (no dead end). The enum is used here deliberately because the key_link pattern asserts on feedbackMiss.next/feedbackPass."
  - "Added a demoPassContinue gen-l10n key for the clean-pass CTA label (plan 02 authored no pass-CTA label); the pass CTA reads it rather than hardcoding 'Continue' (DP-02)."
  - "Removed the now-dead _DemoPlaceholder from demo_routes.dart and its theme imports — all six DemoStep cases now resolve to real screens; the no-dead-end route test still passes."
  - "Celebration Back Home navigates by literal '/demo/home' to satisfy the key_link contract; the mastery star degrades to an Icon.star_rounded (reward gold) if the SVG is missing."

patterns-established:
  - "Error treatment is coral-only, asserted by a source-read test (no Colors.red/redAccent/0xFFFF0000)."
  - "Anti-gamification on Celebration is doubly enforced: exactly one keyed mastery star (findsOneWidget) + a predicate finder asserting no counter/tally/journey Text."

requirements-completed: [S1-05, PLAT-03]

# Deviations
deviations:
  - rule: 3
    severity: non-blocking
    what: "Fixed two malformed key_link entries in this plan's frontmatter: an over-escaped `context\\\\.go...` pattern (→ single-backslash) and a `from:` path with a `(miss)` annotation appended (`...demo_feedback_screen.dart (miss)` → bare path)."
    why: "Both made the SDK's verify.key-links matcher fail on correct code — the over-escaped regex is unsatisfiable, and the parenthetical made the source file unresolvable ('Source file not found'). After the fix, key-links are 4/4. Navigation correctness was already proven by the Task-1 widget tests (Try Again → /demo/feedback/pass; Back Home → /demo/home)."
  - rule: 3
    severity: non-blocking
    what: "Added the demoPassContinue key to lib/l10n/app_en.arb (outside this plan's declared files_modified)."
    why: "Plan 02 did not author a clean-pass CTA label; DP-02 forbids hardcoded copy. The key is the minimal faithful fix. Generated app_localizations*.dart are gitignored (arb is the source of truth)."

# Metrics
duration: ~30min
completed: 2026-06-01
---

# Phase 02.1.1 Plan 05: HERO Feedback + Celebration Summary

**The walkthrough's emotional core and its close: the HERO Feedback miss (the alif's failing stroke highlighted in coral with a SPECIFIC named fix in the tutor's voice and a pressure-free Try Again that advances to the clean-pass praise), and a calm Celebration (cheer mascot + exactly ONE gold star + "You Learned Alif. أحسنت." + Back Home) — closing the loop to demo Home with no dead ends.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-06-01
- **Tasks:** 2/2 (both TDD: RED → GREEN)

## What shipped

### Task 1 — HERO Feedback (miss + pass, DP-04/DP-05/DP-06)
- `DemoFeedbackScreen({variant})`. **MISS (the hero):** the alif drawn fully in coral via `DottedGuidePainter(inkColor: QalamColors.warnSoft)` (never red), the tryAgain-pose mascot, and a warm coral-tinted card carrying the verbatim `demoMissFix` line ("Start your alif at the top and come down…"). A gentle keyed "Try Again" CTA → `DemoStep.feedbackMiss.next.path` (the clean-pass state — reachable by tapping, no dead end). No retry tally, no pressure. **PASS:** a quiet success-tinted affirmation with the specific `demoPassPraise`, and a `demoPassContinue` CTA → Celebration.
- Added optional `inkColor` to `DottedGuidePainter` (default deep-ink preserved; painter test still green).
- Wired both `/demo/feedback` (miss) and `/demo/feedback/pass` (pass) in `_screenFor`.

### Task 2 — Celebration (DP-06)
- `DemoCelebrationScreen`: cheer mascot, a quiet teal "MASTERED" badge, the mastered alif glyph (gold-tint glow) via `ArabicText`, **exactly ONE** gold star (`star.svg` tinted `QalamColors.reward`, keyed, with an `Icon.star_rounded` fallback), the heading "You Learned Alif." and Arabic praise "أحسنت", and a keyed "Back Home" CTA → `/demo/home`. The mockup's extra mark row, running point tally, and journey link are all omitted.
- Wired `/demo/celebration` and removed the now-dead `_DemoPlaceholder` (all six steps now resolve to real screens).

## Verification

- `flutter test test/demo/demo_feedback_screen_test.dart test/demo/demo_celebration_screen_test.dart test/demo/dotted_guide_painter_test.dart` → **12/12 green**.
- `test/router/demo_routes_test.dart` still green (no-dead-end chain intact after wiring all six screens).
- `flutter analyze lib/demo lib/router` → **No issues found**.
- `verify key-links` plan 05 → **4/4 verified** (after the two frontmatter fixes above).
- Acceptance greps: no red (`Colors.red`/`redAccent`/`0xFFFF0000`); `warnSoft` + `demoMissFix` + `feedbackMiss.next`/`feedbackPass` present; no attempt/counter; no "Oops"; exactly one `star.svg`; no `stars`/`+N`/`weekly`/`see journey`/`total`/`confetti`; `reward` present; Back Home targets `/demo/home`.

## Flags for the orchestrator/verifier
- **Two Rule-3 plan-defect fixes (documented above):** an over-escaped key-link regex and a `from:` path polluted with a `(miss)` annotation — both made the SDK matcher fail on correct code; corrected, now 4/4.
- **demoPassContinue l10n key added** (outside declared files_modified) to avoid hardcoded CTA copy (DP-02).
- **Full walkthrough now navigable end-to-end:** Home → Watch → Trace → Feedback·miss → (Try Again) → Feedback·pass → Celebration → (Back Home) → Home, no dead ends — ready for the end-of-phase human screenshot walkthrough in the plan's <human-check>.
- Inline (non-worktree) execution mode (subagent Bash unavailable this session); STATE/ROADMAP owned by the orchestrator.
