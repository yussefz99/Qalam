---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 04
type: execute
wave: 3
depends_on: [02.1.1-01, 02.1.1-02, 02.1.1-03]
files_modified:
  - lib/demo/screens/demo_trace_screen.dart
  - lib/demo/widgets/dotted_guide_painter.dart
  - lib/router/demo_routes.dart
  - test/demo/demo_trace_screen_test.dart
  - test/demo/dotted_guide_painter_test.dart
autonomous: true
requirements: [S1-04, S1-05, PLAT-03]
must_haves:
  truths:
    - "The Trace screen shows the half-traced alif: live ink (deep-ink) drawn over the dotted alif guide."
    - "A 'submit'/stylus-up affordance, labeled via the named gen-l10n key demoTraceSubmit, navigates from Trace to the Feedback miss screen."
    - "The dotted guide and the ink both derive from the single DemoAlif reference source — the glyph is never rendered as Text('ا')."
  artifacts:
    - path: "lib/demo/widgets/dotted_guide_painter.dart"
      provides: "DottedGuidePainter — paints a dotted reference path + optional ink overlay from resolved reference points (reused by Trace; shared with Watch's guide treatment)"
      min_lines: 40
    - path: "lib/demo/screens/demo_trace_screen.dart"
      provides: "Trace screen: half-traced alif (ink over dotted guide) + Stroke 1 of 1 + demoTraceSubmit CTA → Feedback miss"
      min_lines: 60
  key_links:
    - from: "lib/demo/screens/demo_trace_screen.dart"
      to: "/demo/feedback (miss)"
      via: "the submit / stylus-up affordance (labeled demoTraceSubmit) context.go"
      pattern: "context\\.go.*demo/feedback"
    - from: "lib/demo/widgets/dotted_guide_painter.dart"
      to: "DemoAlif reference points"
      via: "paints the dotted guide + ink from the resolved reference geometry"
      pattern: "DemoAlif|reference"
---

<objective>
Build the Trace screen — the half-traced alif with live ink rendered over the dotted guide
— and the reusable DottedGuidePainter behind it, wiring it into the /demo/trace slot and
forward to the Feedback miss screen.

Purpose: Trace is the middle of the narrative (DP-04): the child's ink sits over the dotted
guide, mid-stroke, exactly as Phase 3 will look once the real capture/scorer lands behind
it. The "live ink" is faked per DP discretion (a static painted half-stroke is the safe,
screenshot-stable default; an unscored Listener capture is allowed). No scorer, no capture
engine, no order/count logic (DP-01). One source of truth: guide + ink derive from DemoAlif.
Output: dotted_guide_painter.dart, demo_trace_screen.dart, the /demo/trace builder wired.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@lib/screens/practice_screen.dart
@lib/theme/colors.dart
@lib/theme/dimens.dart
@lib/theme/text_styles.dart
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-01-SUMMARY.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DottedGuidePainter — dotted reference path + ink overlay from one source</name>
  <files>lib/demo/widgets/dotted_guide_painter.dart, test/demo/dotted_guide_painter_test.dart</files>
  <read_first>
    - lib/screens/practice_screen.dart (the _InkPainter: quadratic-smoothed Path through sampled points, deep-ink #0E5B5F via QalamColors.inkStroke, width QalamInk.strokeWidth, round caps/joins, shouldRepaint) — mirror this for the ink overlay
    - lib/demo/demo_alif.dart (DemoAlif reference points, normalized 0..1 — must be scaled to the canvas size)
    - lib/core/scoring/reference_path.dart (ReferencePath.resolve returns ordered point lists; the painter consumes these, NOT a glyph outline or Text)
    - lib/theme/colors.dart (QalamColors.inkStroke for ink; the dotted guide uses a muted neutral derived from tokens — QalamColors.fgMuted/border, NEVER gold for the guide line; gold is reserved for the start-dot only) + dimens.dart (QalamInk.strokeWidth)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md "Ink Rendering" + the dotted-guide note (rendered from resolved referenceStrokes in a CustomPainter — one source of truth, Pitfall 5)
  </read_first>
  <behavior>
    - Test 1: DottedGuidePainter, given a list of resolved normalized reference points and a canvas size, produces a dotted path (assert the painter paints without throwing for the alif single-stroke input).
    - Test 2: when given an `inkProgress` fraction (0..1), the painter draws ink over the first `inkProgress` portion of the stroke (the "half-traced" state) using QalamColors.inkStroke — assert progress 0.0 paints no ink and progress 0.5 paints partial ink (e.g. via a paint-call count or a recording canvas).
    - Test 3: the painter scales normalized 0..1 points to the given Size (a point at [0.5, 1.0] maps to (0.5*w, 1.0*h)).
    - Test 4: shouldRepaint returns true only when inputs (points / inkProgress / size-derived) change.
  </behavior>
  <action>
    Create `DottedGuidePainter` (a CustomPainter) that takes resolved normalized reference points (from DemoAlif/ReferencePath), an `inkProgress` double (0..1, the fraction of the stroke already traced), and paints two layers: (1) the full DOTTED guide path (use a dashed/dotted stroke — build via PathMetric extractPath dashing or a dotted Paint treatment — in a muted neutral from tokens, NOT gold, NOT red); (2) an INK overlay (QalamColors.inkStroke, QalamInk.strokeWidth, round caps/joins, quadratic-smoothed like _InkPainter) covering only the first `inkProgress` fraction of the stroke — this is the "half-traced" look. Scale normalized points to the canvas Size. Implement shouldRepaint comparing points + inkProgress. Pure painting; no engine, no scoring, no capture here. Keep it reusable so Watch (inkProgress=0, full guide) and Trace (inkProgress≈0.5) share it.
  </action>
  <acceptance_criteria>
    - `lib/demo/widgets/dotted_guide_painter.dart` defines `class DottedGuidePainter extends CustomPainter` with an `inkProgress` field.
    - The guide is dotted/dashed (grep for `extractPath\|PathMetric\|dash` or an explicit dotted-paint helper succeeds) and the ink uses `QalamColors.inkStroke`.
    - No gold or red in the guide/ink: `grep -E "reward|warnSoft|Colors\.red" lib/demo/widgets/dotted_guide_painter.dart` returns nothing (the start-dot belongs to the screen, not this painter).
    - No raw hex; no scoring/capture imports (`grep -E "MLKit|mlkit|score|Drift" dotted_guide_painter.dart` returns nothing).
    - All four behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/dotted_guide_painter_test.dart && ! grep -E "reward|warnSoft|Colors\.red" lib/demo/widgets/dotted_guide_painter.dart && flutter analyze lib/demo/widgets/dotted_guide_painter.dart</automated>
  </verify>
  <done>DottedGuidePainter paints the dotted alif guide + a partial ink overlay from the single reference source, token-driven, with no engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trace screen — half-traced alif + Stroke 1 of 1 + submit → Feedback miss</name>
  <files>lib/demo/screens/demo_trace_screen.dart, lib/router/demo_routes.dart, test/demo/demo_trace_screen_test.dart</files>
  <read_first>
    - lib/screens/practice_screen.dart (the parchment-inside-soft-aqua card layout + AppBar/actions pattern to mirror the canvas framing)
    - lib/demo/widgets/dotted_guide_painter.dart (DottedGuidePainter — Task 1; render with inkProgress ≈ 0.5 for the half-traced hero state)
    - lib/demo/demo_alif.dart (DemoAlif reference points + displayName)
    - lib/router/demo_routes.dart (the /demo/trace placeholder to replace; DemoStep.feedbackMiss.path — the submit lands on the HERO miss screen by default, DP)
    - lib/l10n/app_localizations.dart (the demoTraceSubmit getter authored in plan 02 — the named, warm Trace submit / stylus-up CTA label; use it, do NOT introduce a hardcoded string per DP-02)
    - lib/theme/text_styles.dart (QalamTextStyles.label for "Stroke 1 of 1" + eyebrow; display for the heading) + dimens.dart (QalamTargets.targetComfy for the CTA)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (Trace eyebrow "YOUR TURN · TRACE", heading "Now you trace alif.", progress "Stroke 1 of 1"; OMIT "Mark correct" and "Play sound"; keep the per-stroke "Stroke X of N" text, the amber bar must NOT read as a score)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-01 (live ink may be a static painted half-stroke OR an unscored Listener capture — Claude's discretion; no order/count engine), DP-02 (no hardcoded copy — all CTA labels via gen-l10n)
  </read_first>
  <behavior>
    - Test 1: DemoTraceScreen renders the eyebrow (demoTraceEyebrow), heading (demoTraceHeading), "Stroke 1 of 1" progress (demoTraceProgress, Western numerals), and a CustomPaint using DottedGuidePainter at a partial inkProgress (the half-traced state).
    - Test 2: a "submit"/stylus-up affordance labeled with the demoTraceSubmit gen-l10n string (>= targetComfy) navigates to /demo/feedback in the miss variant (assert the test GoRouter location becomes the feedback-miss path).
    - Test 3: the screen does NOT render the glyph as Text('ا') and shows NO "Mark Correct" / "Play sound" affordance (DP-07 omissions).
    - Test 4: no scorer/engine is invoked — the screen has no import of ML Kit, Drift, or a scoring module (the half-traced state is mocked).
  </behavior>
  <action>
    Create `DemoTraceScreen`: a parchment Scaffold with the "YOUR TURN · TRACE" eyebrow, "Now you trace alif." heading, a quiet "Stroke 1 of 1" progress label (neutral, NOT a score — DP-06/UI-SPEC), and the canvas card (mirror practice_screen's framing) painting `DottedGuidePainter(points: DemoAlif resolved, inkProgress: ~0.5)` for the half-traced hero state. The live ink is faked: the static half-stroke painted by the painter is the safe default (DP-01); optionally wrap the canvas in a `Listener` that captures an unscored stroke for liveliness (Claude's discretion) — but it MUST NOT score, count, or persist anything. Add a primary "submit"/stylus-up CTA (a sticker-shadow button >= targetComfy) whose label is the named `demoTraceSubmit` gen-l10n getter (authored in plan 02; do NOT hardcode the string — DP-02) → `context.go(DemoStep.feedbackMiss.path)` (the hero miss screen by default). OMIT "Mark Correct" and "Play sound" (DP-07). Replace the /demo/trace placeholder builder with `const DemoTraceScreen()`. Tokens only; copy via gen-l10n; no raw hex; Western numerals.
  </action>
  <acceptance_criteria>
    - `lib/demo/screens/demo_trace_screen.dart` defines `DemoTraceScreen`, uses `DottedGuidePainter`, and references `DemoAlif`.
    - The submit CTA uses the named l10n key: `grep -q "demoTraceSubmit" lib/demo/screens/demo_trace_screen.dart` succeeds (no hardcoded submit label — DP-02).
    - The glyph is not Text('ا'): `grep -c "Text('ا')\|Text(\"ا\")" lib/demo/screens/demo_trace_screen.dart` returns 0.
    - No omitted affordances: `grep -Ei "mark correct|play sound|listen" lib/demo/screens/demo_trace_screen.dart` returns nothing.
    - No engine: `grep -Ei "mlkit|ml_kit|drift|scorer|score\(" lib/demo/screens/demo_trace_screen.dart` returns nothing.
    - The /demo/trace builder in demo_routes.dart returns DemoTraceScreen (grep succeeds).
    - All four behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/demo_trace_screen_test.dart && grep -q "demoTraceSubmit" lib/demo/screens/demo_trace_screen.dart && ! grep -Ei "mark correct|play sound|mlkit|drift" lib/demo/screens/demo_trace_screen.dart && grep -q "DemoTraceScreen" lib/router/demo_routes.dart && flutter analyze lib/demo/screens/demo_trace_screen.dart</automated>
  </verify>
  <done>The Trace screen renders the half-traced alif (ink over dotted guide from one source), a neutral Stroke 1 of 1, and a demoTraceSubmit-labeled submit affordance → Feedback miss, with no engine and no omitted chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference data → painted guide/ink | DemoAlif points drive both the dotted guide and the mocked ink |
| (optional) pointer input → unscored ink | a Listener stroke, if used, is captured but never scored or persisted |

Offline, mocked-data UI demo: no scorer, no persistence, no network. ASVS L1 surface minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.1-10 | Information Disclosure | captured ink points (if Listener used) | mitigate | If an unscored Listener is used, points stay IN-MEMORY ONLY and are discarded on dispose — never persisted, logged, or transmitted (mirrors practice_screen T-01-05); the safe default is a fully static painted half-stroke with no capture. |
| T-02.1.1-11 | Tampering | guide/ink geometry | accept | Derived from validated DemoAlif/ReferencePath; no tamperable external input feeds the paint. |
| T-02.1.1-12 | Spoofing | none | accept | No identity, no auth, no account in this demo. |
| T-02.1.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs; all deps already vetted. |
</threat_model>

<verification>
- `flutter test test/demo/dotted_guide_painter_test.dart test/demo/demo_trace_screen_test.dart` green.
- `flutter analyze` clean.
- Watch → Trace → Feedback navigation works; the submit CTA is labeled via demoTraceSubmit (no hardcoded copy); no engine, no omitted chrome.
</verification>

<success_criteria>
- Trace shows the half-traced alif (ink over dotted guide) from the single reference source (Pitfall 5), neutral Stroke 1 of 1, demoTraceSubmit CTA → Feedback miss (DP-03/DP-04).
- No scorer/capture/persistence engine (DP-01); "Mark Correct"/"Play sound" omitted (DP-07).
- Tokens only; copy via gen-l10n (submit label via the named demoTraceSubmit key — DP-02); Western numerals.
</success_criteria>

<output>
Create `.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-04-SUMMARY.md` when done.
</output>
</output>
