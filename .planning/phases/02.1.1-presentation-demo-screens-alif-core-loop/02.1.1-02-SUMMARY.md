---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 02
subsystem: ui
tags: [flutter, gen-l10n, go_router, riverpod, l10n, routing, rtl]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: gen-l10n AppLocalizations seam, go_router appRouter provider, QalamColors/QalamSpace/QalamTargets/QalamTextStyles tokens
  - phase: 02.1.1-presentation-demo-screens-alif-core-loop
    provides: 03-UI-SPEC Copywriting Contract (alif), letters.json alif commonMistakes feedback, DP-03/DP-04/DP-05/DP-06 decisions
provides:
  - 18 demo-prefixed gen-l10n copy keys for all six demo screens (Home, Watch, Trace, Feedback·miss, Feedback·pass, Celebration) in the tutor's warm, specific voice
  - the named demoTraceSubmit Trace-submit CTA key (referenced by name in plan 04)
  - DemoStep enum (6 ordered steps) with path/next/label helpers encoding the full forward narrative
  - demoRoutes() route table spread into the app router — the fixed /demo route map plans 03/04/05 fill in
affects: [02.1.1-03, 02.1.1-04, 02.1.1-05, 03-trace-one-letter-end-to-end]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "demo-prefixed l10n namespace for the presentation copy contract (DP-05)"
    - "DemoStep enum as the single source of narrative order + path map; route builders are swappable per-screen-plan without touching the router"
    - "placeholder route builders (tokens-only scaffold) so a route group is navigable end-to-end before its real widgets land"

key-files:
  created:
    - lib/router/demo_routes.dart
    - test/l10n/demo_copy_test.dart
    - test/router/demo_routes_test.dart
  modified:
    - lib/l10n/app_en.arb
    - lib/router/app_router.dart

key-decisions:
  - "Headings authored Title Case (Watch Me Write Alif. / Now You Trace Alif. / You Learned Alif.) per this plan's behavior contract — the 03-UI-SPEC sentence-case mascot lines were superseded by the plan's explicit Title-Case getter assertions."
  - "feedbackMiss → /demo/feedback (default feedback step); feedbackPass → /demo/feedback/pass (distinct sub-path) so both states are independently deep-linkable AND reachable by tapping .next."
  - "Placeholder builders use design-system tokens only (DP-02) so the walkthrough is presentation-calm immediately; plans 03/04/05 replace the builders, not the paths."

patterns-established:
  - "Copy contract: every user-facing demo string is a demo-prefixed gen-l10n getter with an @key description; zero gamification language (DP-06) is test-enforced."
  - "Route contract: DemoStep.next encodes the full no-dead-end forward chain; a route test asserts no demo path resolves to the errorBuilder."

requirements-completed: [S1-04, S1-05, PLAT-03]

# Metrics
duration: ~12min
completed: 2026-06-01
---

# Phase 02.1.1 Plan 02: Demo Copy + /demo Route Group Summary

**18 demo-screen gen-l10n copy getters in the tutor's specific voice (incl. the named demoTraceSubmit CTA) plus a navigable six-step /demo route group whose DemoStep.next chain makes the clean-pass state reachable by tapping.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-01
- **Completed:** 2026-06-01
- **Tasks:** 2 (both TDD: RED → GREEN)
- **Files modified:** 5 (2 lib, 1 arb, 2 test; 1 lib created, 2 test created)

## Accomplishments
- Authored all six-screen demo copy as 18 `demo`-prefixed gen-l10n keys, each with an `@key` description, sourced verbatim from the 03-UI-SPEC Copywriting Contract and letters.json alif `commonMistakes`.
- The HERO miss-fix copy is the specific named fix ("Start your alif at the top and come down — not from the bottom up.") straight from `letters.json`; the clean-pass praise is specific ("Beautiful — straight and tall. أحسنت.").
- Stood up the `/demo` route group wired into the existing `appRouter` with the five prior routes untouched; the `DemoStep.next` chain (home → watch → trace → feedbackMiss → feedbackPass → celebration → home) makes the clean-pass state reachable by tapping, not only by deep link.
- Both tasks done test-first (RED commit then GREEN commit); 8 tests pass, `flutter analyze` clean on both lib files.

## Task Commits

Each task was committed atomically (TDD: test → feat):

1. **Task 1 (RED): failing demo copy contract test** - `b9e7c37` (test)
2. **Task 1 (GREEN): author six-screen demo copy** - `165b70f` (feat)
3. **Task 2 (RED): failing /demo route group test** - `d4daa78` (test)
4. **Task 2 (GREEN): /demo route group wired into app router** - `a610037` (feat)

## Files Created/Modified
- `lib/l10n/app_en.arb` - added 18 demo-prefixed copy keys (greeting, eyebrows, headings, tip, progress, CTAs incl. demoTraceSubmit, named miss-fix, pass praise, Arabic praise island).
- `lib/router/demo_routes.dart` (created, 155 lines) - DemoStep enum + path/next/label helpers + demoRoutes() table + tokens-only _DemoPlaceholder scaffold.
- `lib/router/app_router.dart` - import demo_routes.dart; spread `...demoRoutes()` into the routes list after the /dev seam.
- `test/l10n/demo_copy_test.dart` (created) - four behavior groups pinning labels/headings, CTAs+celebration, named-fix voice, zero gamification.
- `test/router/demo_routes_test.dart` (created) - three behavior groups: all paths resolve, full forward chain ordering, full-chain walk never errors.

## Decisions Made
- **Title-Case headings over SPEC sentence-case.** The 03-UI-SPEC shows mascot lines in sentence case ("Watch me write alif."), but this plan's Task 1 behavior block explicitly asserts Title-Case getters ("Watch Me Write Alif.", etc.) and `demoLessonTitle` containing "Alif". The plan's behavior contract is the binding source for these getters, so the values are Title Case. Flagged here for the verifier/screen plans.
- **Two feedback paths.** `feedbackMiss` is the default `/demo/feedback`; `feedbackPass` is `/demo/feedback/pass`. Keeps both states independently routable while the `.next` chain stays a single forward walkthrough.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reworded two @key descriptions to satisfy the file-level grep gate**
- **Found during:** Task 1 (copy authoring)
- **Issue:** The plan's verify command runs `! grep -Eq "streak|badge|weekly|Oops" lib/l10n/app_en.arb` against the WHOLE file. Two of my `@key` *descriptions* used the words "badge label" and "'Oops, try again'" (explaining what NOT to write), which tripped the file-level grep even though no user-facing *value* contained them (behavior Test 4 passed).
- **Fix:** Reworded the celebration-eyebrow description ("badge label" → "mastery label") and the miss-fix description ("Never a generic 'Oops, try again'" → "Never a generic bounce; always names the exact fix"). No value changed.
- **Files modified:** lib/l10n/app_en.arb
- **Verification:** `! grep -Eq "streak|badge|weekly|Oops" lib/l10n/app_en.arb` now passes; the four copy tests still green.
- **Committed in:** `165b70f` (Task 1 GREEN commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Cosmetic description wording only; no copy-value or behavior change. No scope creep.

## Issues Encountered
- The generated `lib/l10n/app_localizations.dart` is gitignored (regenerated from `app_en.arb` on build), so the plan's `read_first` reference to it is a transient file. I inferred the getter shape from `AppLocalizations.of(context)` usage in `home_screen.dart` and ran `flutter gen-l10n` to materialize the getters before testing. Resolved cleanly.

## Known Stubs

The six `/demo/*` route builders point at a single private `_DemoPlaceholder` scaffold (a calm parchment screen naming the step + a "Next" CTA). **This is intentional and documented in the plan** (Task 2 action: "point each builder at a small private `_DemoPlaceholder` widget … the screen plans (03/04/05) replace these builders with their real widgets"). The plan's goal — a fixed, navigable route map — is fully achieved; the real screen widgets are the explicit deliverable of plans 03/04/05.

| Stub | File | Reason / Resolution |
|------|------|---------------------|
| `_DemoPlaceholder` builders for all six steps | lib/router/demo_routes.dart | Intentional per DP / Task 2 action; plans 03 (Watch/Trace), 04 (Feedback states), 05 (Home/Celebration) swap in real widgets at the same fixed paths. |

## User Setup Required
None - no external service configuration required (offline, mocked-data UI; no packages added — go_router ^17.2.3 already in pubspec).

## Next Phase Readiness
- Screen plans 03/04/05 can now target fixed l10n keys (demo-prefixed) and fixed route slots (`DemoStep.path`) — they replace the `_DemoPlaceholder` builders without touching the router or each other.
- `demoTraceSubmit` exists by name for plan 04's Trace submit affordance.
- No blockers. Note for the verifier: demo headings are Title Case per this plan's behavior contract (see Decisions), which differs from the 03-UI-SPEC sentence-case mascot lines.

## Self-Check: PASSED
- All 4 created/modified files present (demo_routes.dart, app_en.arb, demo_copy_test.dart, demo_routes_test.dart).
- All 4 task commits present in git log (b9e7c37, 165b70f, d4daa78, a610037).
- 8/8 tests green; flutter analyze clean on both lib files.

---
*Phase: 02.1.1-presentation-demo-screens-alif-core-loop*
*Completed: 2026-06-01*
