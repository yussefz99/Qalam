---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/l10n/app_en.arb
  - lib/router/demo_routes.dart
  - lib/router/app_router.dart
  - test/router/demo_routes_test.dart
  - test/l10n/demo_copy_test.dart
autonomous: true
requirements: [S1-04, S1-05, PLAT-03]
must_haves:
  truths:
    - "Every demo screen's copy (greeting, eyebrows, headings, buttons, named-fix lines, celebration) exists as a gen-l10n getter — no hardcoded user-facing strings."
    - "A /demo route group exists with six ordered steps wired Home → Watch → Trace → Feedback → Celebration → Home, no dead ends, no broken routes."
    - "The named-fix feedback copy and the celebration line read in the warm, specific tutor voice — never a generic 'try again'."
  artifacts:
    - path: "lib/l10n/app_en.arb"
      provides: "All demo copy keys for the six screens (alif), generated into AppLocalizations"
      contains: "demoWatchHeading"
    - path: "lib/router/demo_routes.dart"
      provides: "DemoStep enum + the /demo/* route table builder + ordered next/back path helpers, consumed by app_router.dart"
      min_lines: 40
  key_links:
    - from: "lib/router/app_router.dart"
      to: "lib/router/demo_routes.dart"
      via: "demoRoutes() spread into the GoRouter routes list"
      pattern: "demoRoutes\\("
    - from: "lib/router/demo_routes.dart"
      to: "the six demo screen builders"
      via: "GoRoute path → builder, in narrative order with no dead ends"
      pattern: "/demo"
---

<objective>
Author all demo copy as gen-l10n getters and stand up the navigable /demo route group
that wires the six screens in narrative order — so later plans drop their screen widgets
into existing route slots without touching each other's routers.

Purpose: PLAN.md is the contract; the copy contract (DP-05) and the navigable-walkthrough
contract (DP-03) are foundational and shared. Centralizing them in Wave 1 lets the three
screen plans run against fixed l10n keys and a fixed route map. The route group starts
with safe placeholder builders that each screen plan replaces with its real widget.
Output: app_en.arb demo keys, AppLocalizations regenerated, and lib/router/demo_routes.dart.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@lib/l10n/app_en.arb
@lib/router/app_router.dart
@assets/curriculum/letters.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author all six-screen demo copy in app_en.arb (gen-l10n)</name>
  <files>lib/l10n/app_en.arb, test/l10n/demo_copy_test.dart</files>
  <read_first>
    - lib/l10n/app_en.arb (existing key shape: "key": "value" + "@key": {"description": ...}; mirror it exactly)
    - lib/l10n/app_localizations.dart (the generated getter shape callers use; new keys become new getters after gen)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md "Copywriting Contract" (screen labels/headings, buttons Title Case, named-fix table, celebration "You learned alif." + "أحسنت") — this is the verbatim source for alif
    - assets/curriculum/letters.json (alif commonMistakes[].feedback — the named-fix strings, must match the UI-SPEC table)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-05 (specific named fixes), DP-06 (no gamification copy: no counter/tally/streak language), specifics (static placeholder greeting name)
  </read_first>
  <behavior>
    - Test 1: AppLocalizations exposes demo getters for the Home greeting (static placeholder name, e.g. "Let's learn, Layla."), the alif "Today's Lesson" eyebrow + title, and the Watch/Trace eyebrows ("WATCH · STROKE ORDER", "YOUR TURN · TRACE") and headings ("Watch Me Write Alif.", "Now You Trace Alif.").
    - Test 2: the button labels exist Title-Case: "Start Tracing"/"I'll Try", "Try Again", "Back Home", and the celebration heading "You Learned Alif." (English) plus the Arabic praise "أحسنت".
    - Test 3: the hero miss named-fix getter returns a SPECIFIC fix in the tutor voice (contains "Start your alif at the top") and the clean-pass praise getter is specific (e.g. "straight and tall") — neither contains "Oops" nor a bare "try again" with no named fix.
    - Test 4: no demo copy key contains gamification language — assert no value contains "streak", "badge", "weekly", "stars left", or a "+N" hype token (DP-06).
  </behavior>
  <action>
    Add gen-l10n keys for every user-facing string across the six screens, each with an "@key" description, sourced VERBATIM from the 03-UI-SPEC Copywriting Contract for alif (DP-05/DP-07). Use a clear `demo`-prefixed namespace (e.g. demoHomeGreeting, demoLessonEyebrow, demoLessonTitle, demoWatchEyebrow, demoWatchHeading, demoWatchTip, demoTraceEyebrow, demoTraceHeading, demoTraceProgress, demoStartTracing, demoTryAgain, demoCelebrationEyebrow, demoCelebrationHeading, demoCelebrationPraiseAr, demoBackHome, demoMissFix, demoPassPraise). The greeting uses a static placeholder name (no profile system — Phase 5). The miss-fix value is the alif named fix; the pass praise is specific warm praise. NO gamification copy (no counter/tally/streak/badge/"+N"). Run `flutter gen-l10n` (or `flutter pub get` which triggers generation) so AppLocalizations gains the getters. Use Western numerals only for any digit (e.g. "Stroke 1 of 1").
  </action>
  <acceptance_criteria>
    - `flutter gen-l10n` regenerates lib/l10n/app_localizations*.dart with the new getters (build exits 0).
    - `grep -c "demoWatchHeading\|demoMissFix\|demoCelebrationHeading" lib/l10n/app_en.arb` returns >= 3.
    - No demo value contains "Oops", "streak", "badge", "weekly", or a "+" hype token (grep returns nothing for those).
    - The alif named-fix value matches the letters.json wording (grep "Start your alif at the top" succeeds).
    - All four behavior tests in test/l10n/demo_copy_test.dart pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter gen-l10n && flutter test test/l10n/demo_copy_test.dart && ! grep -Eq "streak|badge|weekly|Oops" lib/l10n/app_en.arb</automated>
  </verify>
  <done>All six-screen copy lives in app_en.arb as described getters in the tutor's specific voice, generated and tested, with zero gamification language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /demo route group — DemoStep enum + ordered routes wired into app_router</name>
  <files>lib/router/demo_routes.dart, lib/router/app_router.dart, test/router/demo_routes_test.dart</files>
  <read_first>
    - lib/router/app_router.dart (the @Riverpod appRouter GoRouter with its routes list and the existing /dev seam routes — add the demo group the same way; part 'app_router.g.dart' codegen)
    - lib/screens/home_screen.dart (the existing context.go('/practice') navigation pattern the demo CTAs will mirror)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-03 (navigable in order, no dead ends; Claude's discretion: dedicated demo route group is allowed) and DP-04 (all six states ship)
  </read_first>
  <behavior>
    - Test 1: a GoRouter built from the demo routes resolves each of the six demo paths (/demo/home, /demo/watch, /demo/trace, /demo/feedback, /demo/celebration — feedback carries a miss/pass variant via query or sub-path) to a non-null builder; no path resolves to an error/unknown page.
    - Test 2: DemoStep enum defines the ordered steps and `.next`/`.path` helpers produce the forward chain home→watch→trace→feedback→celebration and celebration's "back home" returns to the demo home (no dead end).
    - Test 3: navigating the full chain with a test GoRouter never lands on the router's errorBuilder/unknown route.
  </behavior>
  <action>
    Create `lib/router/demo_routes.dart` exposing a `DemoStep` enum (home, watch, trace, feedbackMiss, feedbackPass, celebration) with `path` and `next` helpers encoding the narrative order, plus a `demoRoutes()` function returning the `List<RouteBase>` for the /demo group. Wire each path to a builder. For THIS plan, point each builder at a small private `_DemoPlaceholder` widget (a calm parchment scaffold naming the step) so the group is navigable end-to-end immediately; the screen plans (03/04/05) replace these builders with their real widgets. Spread `demoRoutes()` into the appRouter routes list in app_router.dart (keep the existing /, /practice, /settings, /dev/* routes untouched). Re-run codegen if app_router.g.dart needs it (`flutter pub run build_runner build` is NOT needed unless the annotation surface changes — adding routes inside the existing provider does not). Feedback miss is the default feedback step. No dead ends: celebration's forward action targets demo home.
  </action>
  <acceptance_criteria>
    - `lib/router/demo_routes.dart` defines `enum DemoStep` with the six values and a top-level `demoRoutes()` returning `List<RouteBase>`.
    - `grep -c "demoRoutes(" lib/router/app_router.dart` returns 1 (group wired in).
    - The five existing routes (/ , /practice, /settings, /dev/glyph-audit, /dev/authoring) are still present in app_router.dart (grep confirms each).
    - All three behavior tests in test/router/demo_routes_test.dart pass.
    - `flutter analyze` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/router/demo_routes_test.dart && grep -q "demoRoutes(" lib/router/app_router.dart && grep -q "/practice" lib/router/app_router.dart && flutter analyze lib/router/demo_routes.dart lib/router/app_router.dart</automated>
  </verify>
  <done>The /demo route group is wired into the app router, navigable across all six steps with no dead ends, leaving the existing routes intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| l10n source → rendered UI | authored copy strings surfaced to the child |
| route input → screen builder | demo path/query selects which mocked screen state renders |

Offline, mocked-data UI demo: no auth, no persisted input, no secrets, no network. ASVS L1
surface is minimal and proportional.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.1-04 | Tampering | demo copy values | accept | Copy is static authored curriculum text; the demo_copy_test asserts named fixes are specific and gamification language is absent (DP-05/DP-06). |
| T-02.1.1-05 | Denial of Service | unknown /demo path | mitigate | demoRoutes covers all six steps and the router keeps its errorBuilder; the route test asserts no demo path resolves to the unknown page (no dead ends). |
| T-02.1.1-06 | Information Disclosure | route group | accept | No sensitive data flows through routes; the demo carries only an alif step identifier. No child data. |
| T-02.1.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs — go_router ^17.2.3 already vetted in pubspec; no new runtime packages. |
</threat_model>

<verification>
- `flutter gen-l10n` succeeds; AppLocalizations exposes the demo getters.
- `flutter test test/l10n/demo_copy_test.dart test/router/demo_routes_test.dart` green.
- Existing routes intact; the six-step demo chain is navigable with no dead ends.
</verification>

<success_criteria>
- Every demo string is a gen-l10n getter in the tutor's specific voice (DP-05), zero gamification copy (DP-06).
- The /demo group is navigable Home→Watch→Trace→Feedback→Celebration→Home with no dead ends (DP-03/DP-04).
- The screen plans can target fixed l10n keys and fixed route slots.
</success_criteria>

<output>
Create `.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-02-SUMMARY.md` when done.
</output>
