---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 05
type: execute
wave: 4
depends_on: [02.1.1-01, 02.1.1-02, 02.1.1-04]
files_modified:
  - lib/demo/screens/demo_feedback_screen.dart
  - lib/demo/screens/demo_celebration_screen.dart
  - lib/router/demo_routes.dart
  - test/demo/demo_feedback_screen_test.dart
  - test/demo/demo_celebration_screen_test.dart
autonomous: true
requirements: [S1-05, PLAT-03]
must_haves:
  truths:
    - "The Feedback miss screen (the HERO) highlights the failing stroke in coral (never red) and shows a SPECIFIC named fix in the tutor's voice, with a gentle Try Again affordance."
    - "The miss screen's Try Again affordance navigates forward to the Feedback clean-pass screen (DemoStep.feedbackMiss.next == feedbackPass), so the clean-pass state is reachable by tapping — the full chain has no dead ends."
    - "The Feedback clean-pass screen shows specific warm praise and advances to Celebration."
    - "The Celebration shows the cheer mascot, exactly ONE quiet star, the warm line 'You learned alif.' + 'أحسنت', and a Back Home affordance to demo Home — no streaks, badges, confetti, timers, or running counter."
  artifacts:
    - path: "lib/demo/screens/demo_feedback_screen.dart"
      provides: "Feedback screen with miss (HERO, coral failing-stroke + named fix → Try Again navigates to feedbackPass) and clean-pass (specific praise → Celebration) variants"
      min_lines: 70
    - path: "lib/demo/screens/demo_celebration_screen.dart"
      provides: "Celebration: cheer mascot + ONE quiet star + 'You learned alif.' + 'أحسنت' + Back Home → demo Home"
      min_lines: 50
  key_links:
    - from: "lib/demo/screens/demo_feedback_screen.dart"
      to: "DemoAlif.heroMissFix (named fix) + QalamColors.warnSoft"
      via: "the failing stroke highlighted coral + the named-fix line rendered"
      pattern: "warnSoft|heroMissFix|demoMissFix"
    - from: "lib/demo/screens/demo_feedback_screen.dart"
      to: "/demo/feedback (pass)"
      via: "the Try Again affordance navigates to DemoStep.feedbackMiss.next (feedbackPass) — no dead end, clean-pass reachable by tapping"
      pattern: "feedbackMiss\.next|feedbackPass"
    - from: "lib/demo/screens/demo_celebration_screen.dart"
      to: "/demo/home"
      via: "the Back Home affordance context.go (no dead end)"
      pattern: "context\.go.*demo/home"
    - from: "lib/demo/screens/demo_celebration_screen.dart"
      to: "exactly one star (assets/icons/star.svg or painted) tinted QalamColors.reward"
      via: "single mastery star, no counter"
      pattern: "reward"
---

<objective>
Build the HERO Feedback screen (coral failing-stroke + specific named fix) with its
clean-pass variant, and the Celebration screen (one quiet star), closing the walkthrough
loop back to demo Home.

Purpose: The miss screen is the hero shot (DP-04, Specific Ideas) — it proves the app
diagnoses a SPECIFIC stroke problem and gives a SPECIFIC fix in the tutor's warm voice,
framed in coral (never red, never a red X). Its Try Again affordance carries the natural
demo narrative forward — the child retries, this time it's clean — by navigating to the
clean-pass variant (DemoStep.feedbackMiss.next == feedbackPass), so the clean-pass state
is reachable by tapping and the chain has no dead ends (DP-03/DP-04). The pass variant
gives specific praise. The Celebration honors the anti-gamification rule absolutely:
exactly ONE quiet star, no counter/streak/confetti (DP-06). Back Home returns to
/demo/home (no dead end, DP-03). Make the miss screen the most polished. Output:
demo_feedback_screen.dart, demo_celebration_screen.dart, the two route builders wired.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@lib/widgets/arabic_text.dart
@lib/theme/colors.dart
@lib/theme/dimens.dart
@lib/theme/text_styles.dart
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-01-SUMMARY.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feedback screen (HERO) — coral failing stroke + named fix (miss) + specific praise (pass)</name>
  <files>lib/demo/screens/demo_feedback_screen.dart, lib/router/demo_routes.dart, test/demo/demo_feedback_screen_test.dart</files>
  <read_first>
    - lib/demo/widgets/dotted_guide_painter.dart (reuse to render the alif; for the miss state highlight the failing stroke — pass a highlight color so the stroke renders in QalamColors.warnSoft instead of inkStroke)
    - lib/demo/demo_alif.dart (DemoAlif.heroMissFix + namedFixes — the specific named fix strings; the reference points for the highlighted stroke)
    - lib/widgets/qalam_mascot.dart (QalamMascot(pose: QalamPose.tryAgain) for miss; the pass variant may use idle/cheer)
    - lib/router/demo_routes.dart (the /demo/feedback placeholder to replace; DemoStep.feedbackMiss/feedbackPass + .celebration paths; the variant is selected by route — miss is default; DemoStep.feedbackMiss.next == feedbackPass and feedbackPass.next == celebration encode the forward chain authored in plan 02 — wire the miss Try Again CTA to DemoStep.feedbackMiss.next and the pass CTA to DemoStep.feedbackPass.next)
    - lib/theme/colors.dart (QalamColors.warnSoft = coral, the ONLY error treatment; warnSoftTint for the framed fix card; success for the pass tick — NEVER Colors.red, never a red X) + dimens.dart (QalamMotion.easeSoftBack for the gentle wiggle, optional; QalamTargets) + text_styles.dart (body for the named-fix line, label for eyebrow)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (Feedback/error states table: named fixes verbatim; "Try Again" button; coral not red; clean-pass quiet confirmation; DP-05)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md (the miss screen is the HERO — most polished; DP-05 specific named fix; DP-06 no fail counter/no pressure; DP-03/DP-04 no dead ends — the clean-pass state must be tappable from the miss screen)
  </read_first>
  <behavior>
    - Test 1 (HERO miss): the miss variant renders the alif with the failing stroke highlighted in QalamColors.warnSoft (coral) and the specific named-fix line (demoMissFix / DemoAlif.heroMissFix containing "Start your alif at the top") — assert the named-fix text is present and is NOT a generic "try again" with no fix.
    - Test 2 (no red): the miss screen uses NO red anywhere — assert no Colors.red and no warning glyph reading as a red X; the error treatment is coral only.
    - Test 3 (miss → pass, BINDING, no dead end): the miss "Try Again" affordance (>= targetComfy, label via demoTryAgain) navigates forward to the clean-pass variant — drive a test GoRouter from the miss route, tap Try Again, and assert the location becomes DemoStep.feedbackPass.path (the natural retry-now-clean narrative). It carries no try-counter/no "attempt N" text (DP-06).
    - Test 4 (clean pass): the pass variant renders specific warm praise (demoPassPraise, e.g. contains "straight and tall" or similar specific praise — not generic) and a primary CTA that navigates to /demo/celebration.
  </behavior>
  <action>
    Create `DemoFeedbackScreen` taking a variant (miss | pass) selected by the route (miss is the default the Trace submit lands on). MISS (the HERO — make it the most polished): render the alif via DottedGuidePainter with the failing stroke drawn in QalamColors.warnSoft (coral), the tryAgain-pose mascot beside it, and a warmly-framed named-fix card (warnSoftTint background, QalamRadii, the demoMissFix line from DemoAlif.heroMissFix in QalamTextStyles.body) — a SPECIFIC fix in the tutor's voice (DP-05). Add a gentle "Try Again" sticker CTA (>= targetComfy, label via the demoTryAgain gen-l10n getter) that navigates forward via `context.go(DemoStep.feedbackMiss.next.path)` — i.e. to the clean-pass variant — so the child's retry lands on the now-clean pass screen and the clean-pass state is reachable by tapping (no dead end, DP-03/DP-04). The CTA has NO counter, NO "attempt N", NO pressure (DP-06); an optional soft wiggle (QalamMotion.easeSoftBack) is allowed but must settle to a clean screenshot state. NO red, no red X — coral is the only error color. PASS variant: a quiet success confirmation (QalamColors.success, subtle), the specific praise line (demoPassPraise), and a primary CTA → `context.go(DemoStep.feedbackPass.next.path)` (celebration). Replace the /demo/feedback builder(s) with `DemoFeedbackScreen` keyed to the route variant. Tokens only; copy via gen-l10n; no raw hex.
  </action>
  <acceptance_criteria>
    - `lib/demo/screens/demo_feedback_screen.dart` defines `DemoFeedbackScreen`, references `QalamColors.warnSoft`, `DemoAlif`, and `QalamPose.tryAgain`.
    - No red: `grep -E "Colors\.red|0xFFFF0000|#FF0000|redAccent" lib/demo/screens/demo_feedback_screen.dart` returns nothing.
    - The named fix is specific: `grep -q "demoMissFix\|heroMissFix" lib/demo/screens/demo_feedback_screen.dart` succeeds and no generic "Oops" string appears.
    - The miss Try Again is wired forward to the clean-pass state (no dead end): `grep -E "feedbackMiss\.next|feedbackPass" lib/demo/screens/demo_feedback_screen.dart` succeeds, and Test 3 asserts tapping Try Again navigates to DemoStep.feedbackPass.path.
    - No try-counter on miss: `grep -Ei "attempt|tries left|try [0-9]|counter" lib/demo/screens/demo_feedback_screen.dart` returns nothing.
    - The pass CTA targets celebration (grep "feedbackPass.next" or "demo/celebration" or DemoStep.celebration succeeds).
    - The /demo/feedback builder in demo_routes.dart returns DemoFeedbackScreen (grep succeeds).
    - All four behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/demo_feedback_screen_test.dart && ! grep -E "Colors\.red|redAccent|0xFFFF0000" lib/demo/screens/demo_feedback_screen.dart && grep -q "warnSoft" lib/demo/screens/demo_feedback_screen.dart && grep -Eq "feedbackMiss\.next|feedbackPass" lib/demo/screens/demo_feedback_screen.dart && flutter analyze lib/demo/screens/demo_feedback_screen.dart</automated>
  </verify>
  <done>The HERO Feedback miss highlights the failing stroke in coral with a specific named fix and a counter-free Try Again that navigates forward to the clean-pass state (no dead end); the pass variant shows specific praise → Celebration. No red anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Celebration — cheer mascot + ONE quiet star + warm line + Back Home → demo Home</name>
  <files>lib/demo/screens/demo_celebration_screen.dart, lib/router/demo_routes.dart, test/demo/demo_celebration_screen_test.dart</files>
  <read_first>
    - lib/widgets/qalam_mascot.dart (QalamMascot(pose: QalamPose.cheer))
    - lib/widgets/arabic_text.dart (the RTL island for the mastered alif glyph + "أحسنت" — display island for the glyph; arBody for the praise)
    - lib/demo/demo_alif.dart (DemoAlif.glyph "ا", arabicName, displayName)
    - lib/router/demo_routes.dart (the /demo/celebration placeholder to replace; DemoStep.home.path for Back Home — no dead end)
    - assets/icons/star.svg (the single mastery star — tint QalamColors.reward; OR a painted star) + lib/theme/colors.dart (QalamColors.reward gold = REWARDS ONLY, used here for the ONE star + the mastered-glyph glow; QalamColors.primary for the MASTERED badge) + dimens.dart (QalamMotion.durCheer 700ms for a gentle single-star settle, optional)
    - docs/design/kit/project/screenshots/05-celebration-final.png (warmth target) + 03-UI-SPEC "Mandatory omissions" (OMIT: the three gold stars → show exactly ONE; OMIT "TOTAL 42 stars +3 today"; OMIT "See journey"; keep mascot + mastered alif + ONE star + warm line + Back Home)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-06 (exactly ONE quiet star; no streaks/badges/confetti/timers/running counter)
  </read_first>
  <behavior>
    - Test 1: DemoCelebrationScreen renders the cheer-pose mascot, the "MASTERED" eyebrow (demoCelebrationEyebrow), the heading "You learned alif." (demoCelebrationHeading), the mastered alif glyph via ArabicText, and the Arabic praise "أحسنت" (demoCelebrationPraiseAr) via ArabicText.
    - Test 2 (anti-gamification, BINDING): exactly ONE star is rendered — assert a single star element (one star.svg OR one painted star) and NO running counter ("42 stars", "+3 today"), NO weekly tally, NO "See journey", NO three-star row, NO confetti widget.
    - Test 3: the star uses QalamColors.reward (gold = rewards-only, allowed here).
    - Test 4 (no dead end): a "Back Home" affordance (>= targetLarge/targetComfy) navigates to /demo/home (assert the test GoRouter location returns to demo home).
  </behavior>
  <action>
    Create `DemoCelebrationScreen`: a calm full-screen parchment moment (generous QalamSpace breathing room) with the cheer-pose mascot, a teal "MASTERED" badge (QalamColors.primary), the heading "You learned alif." (demoCelebrationHeading), the mastered alif glyph in an ArabicText display island with a soft gold glow (QalamColors.reward, the allowed glyph-glow use), exactly ONE quiet star (assets/icons/star.svg tinted QalamColors.reward via flutter_svg colorFilter, OR a single painted star — with the mascot's graceful-fallback discipline if using the SVG), and the Arabic praise "أحسنت" via ArabicText. A single gentle single-star settle (QalamMotion.durCheer) is allowed but must settle to a clean screenshot state — NO confetti, NO sound blast, NO running counter (DP-06). OMIT the mockup's three stars / "TOTAL N stars +N today" / "See journey" entirely. Add a primary "Back Home" sticker CTA → `context.go(DemoStep.home.path)` (closes the loop, no dead end). Replace the /demo/celebration builder with `const DemoCelebrationScreen()`. Tokens only; copy via gen-l10n; no raw hex.
  </action>
  <acceptance_criteria>
    - `lib/demo/screens/demo_celebration_screen.dart` defines `DemoCelebrationScreen`, references `QalamPose.cheer`, `ArabicText`, and `QalamColors.reward`.
    - Exactly ONE star: `grep -c "star.svg" lib/demo/screens/demo_celebration_screen.dart` returns at most 1; no three-star row, and no counter text — `grep -Ei "stars\b|\+[0-9]|weekly|see journey|total" lib/demo/screens/demo_celebration_screen.dart` returns nothing.
    - No confetti: `grep -Ei "confetti" lib/demo/screens/demo_celebration_screen.dart` returns nothing.
    - Back Home targets demo home (grep "demo/home" or DemoStep.home succeeds).
    - The /demo/celebration builder in demo_routes.dart returns DemoCelebrationScreen (grep succeeds).
    - All four behavior tests pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/demo_celebration_screen_test.dart && ! grep -Ei "confetti|see journey|weekly|\+[0-9] today" lib/demo/screens/demo_celebration_screen.dart && grep -q "DemoCelebrationScreen" lib/router/demo_routes.dart && flutter analyze lib/demo/screens/demo_celebration_screen.dart</automated>
  </verify>
  <done>The Celebration shows the cheer mascot, exactly ONE quiet star, the warm bilingual line, and Back Home → demo Home, with every gamification element omitted and asserted absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DemoAlif copy → rendered feedback | the named-fix string surfaced to the child |
| reward asset → celebration | the single star SVG / painted star rendered |

Offline, mocked-data UI demo: no scorer, no persistence, no network, no auth. ASVS L1
surface minimal and proportional.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.1-13 | Tampering | named-fix copy | accept | The fix string is static authored curriculum copy from DemoAlif; the test asserts it is specific and never generic (DP-05). |
| T-02.1.1-14 | Denial of Service | missing star/mascot SVG on Celebration | mitigate | QalamMascot fallback (plan 01) + a painted-star fallback option keep the celebration screenshot-stable if an asset is missing. |
| T-02.1.1-15 | Elevation of Privilege | none | accept | No privileged action, no account, no destructive path in this demo (UI-SPEC: no destructive actions). |
| T-02.1.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs; flutter_svg already vetted; no new runtime packages. |
</threat_model>

<verification>
- `flutter test test/demo/demo_feedback_screen_test.dart test/demo/demo_celebration_screen_test.dart` green.
- `flutter analyze` clean for both screens.
- The full walkthrough is navigable with no dead ends; the miss Try Again advances to the clean-pass state (tappable); miss is the HERO (coral + specific fix); celebration shows exactly ONE star.

<human-check>
End-of-phase screenshot-readiness walkthrough (human_verify_mode=end-of-phase — gather all phase human checks here, do not interrupt execution):
1. Run the app on a tablet/emulator and navigate to `/demo/home`.
2. Tap through the full loop: Home (idle mascot + warm greeting + alif lesson card) → Watch
   (dotted alif + gold start-dot + write mascot) → Trace (half-traced alif, Stroke 1 of 1) →
   submit → Feedback MISS (failing stroke in coral + the specific named fix "Start your alif
   at the top…", tryAgain mascot) → tap Try Again → Feedback PASS (specific praise) →
   Celebration (cheer mascot + exactly ONE gold star + "You learned alif. أحسنت.") → Back Home
   returns to /demo/home. Confirm every step advances by tapping (no dead ends).
3. Confirm on EVERY screen: parchment background (never stark white), no debug banner, NO
   running star counter / weekly tally / streak / badge / confetti / 3-star rating, and the
   miss feedback names a SPECIFIC fix (never a generic "try again").
4. Confirm the mascot illustrations render (not the fallback box) and Arabic glyphs shape
   correctly (alif + أحسنت), RTL only inside those islands.
</human-check>
</verification>

<success_criteria>
- The HERO Feedback miss highlights the failing stroke in coral with a SPECIFIC named fix in the tutor's voice; its Try Again advances to the clean-pass state (tappable, no dead end); the pass variant gives specific praise (DP-05); never red, never generic.
- Celebration shows the cheer mascot + exactly ONE quiet star + "You learned alif. أحسنت." + Back Home → demo Home, every gamification element omitted (DP-06).
- The walkthrough loops cleanly Home→Watch→Trace→Feedback·miss→Feedback·pass→Celebration→Home with no dead ends (DP-03/DP-04); screens are screenshot-ready (DP-09).
</success_criteria>

<output>
Create `.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-05-SUMMARY.md` when done.
</output>
</output>
