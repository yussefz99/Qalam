---
status: passed
phase: 02.1.1-presentation-demo-screens-alif-core-loop
verified: 2026-06-01
verifier: orchestrator-inline
requirements: [S1-04, S1-05, PLAT-03]
must_haves_total: 12
must_haves_verified: 12
human_verification_pending: 1
note: "Verified inline by the execute-phase orchestrator (adopting the verifier role) — subagent Bash was unavailable this session, so the gsd-verifier subagent could not be spawned. All checks below were run directly against the codebase."
---

# Phase 02.1.1: Presentation Demo Screens (alif core loop) — Verification

## Goal (from ROADMAP)

A beautiful, screenshot-ready, navigable walkthrough of the full Qalam core loop for
alif — **Home → Watch → Trace → Tutor Feedback → Celebration** — built with the real
Flutter widgets and design system but fed **mocked/static content** (no scoring engine,
no persistence, no network/model). For the 2026-06-02 course-staff presentation.

**Requirement framing:** S1-04 / S1-05 / PLAT-03 are *presentational* coverage in this
phase (depicted in polished mocked screens, not engine-backed); they are formally
delivered in Phase 3. This verification confirms the depiction quality and navigability,
not engine behavior.

## Must-Haves — 12/12 verified

| # | Must-have | Source | Status | Evidence |
|---|-----------|--------|--------|----------|
| 1 | Brand mascot SVGs bundled + `QalamMascot` (typed poses, graceful fallback) | 01 | ✓ | `qalam_mascot_test.dart` 2/2; 10 SVGs bundled & registered |
| 2 | `DemoAlif` single static mock source (glyph, reference points, named fixes) | 01 | ✓ | `demo_alif_test.dart` green; verbatim letters.json fixes |
| 3 | All six-screen demo copy as gen-l10n getters; zero gamification language | 02 | ✓ | `demo_copy_test.dart` green |
| 4 | `/demo` route group navigable, full forward chain, no dead ends | 02 | ✓ | `demo_routes_test.dart` 3 groups green (no path → errorBuilder) |
| 5 | De-gamified Home: idle mascot + warm greeting + alif card → Watch | 03 | ✓ | `demo_home_screen_test.dart` 4/4; anti-gamification asserted |
| 6 | Watch: dotted alif + numbered gold start-dot + write mascot → Trace | 03 | ✓ | `demo_watch_screen_test.dart` 4/4; guide painted, not Text |
| 7 | Reusable `DottedGuidePainter` (dotted guide + ink overlay, one source) | 04 | ✓ | `dotted_guide_painter_test.dart` 4/4 |
| 8 | Trace: half-traced alif + neutral "Stroke 1 of 1" + submit → Feedback miss | 04 | ✓ | `demo_trace_screen_test.dart` 4/4; no engine |
| 9 | HERO Feedback miss: coral failing stroke + SPECIFIC named fix (never red) | 05 | ✓ | `demo_feedback_screen_test.dart` T1/T2; source has no red |
| 10 | Miss Try Again → clean-pass (tappable, no dead end); pass praise → Celebration | 05 | ✓ | `demo_feedback_screen_test.dart` T3/T4 |
| 11 | Celebration: cheer mascot + exactly ONE quiet star + "You Learned Alif."/"أحسنت" + Back Home → Home | 05 | ✓ | `demo_celebration_screen_test.dart` 4/4 |
| 12 | Anti-gamification absolute (PLAT-03/DP-06): no counters/streaks/badges/confetti | 03/05 | ✓ | predicate-finder assertions on Home + Celebration; acceptance greps |

## Automated checks

- **Full test suite:** `flutter test` → **102 passed, 1 failed**.
  - The single failure is `test/glyph_audit_golden_test.dart` (Noto Naskh contextual-shaping
    golden, D-12) — a **pre-existing, environment-sensitive** golden mismatch (1.19% pixel
    diff). Confirmed failing identically at the pre-phase base commit `f4ac921`; this phase
    did not touch the test or `goldens/glyph_audit.png`. **Not a regression.**
- **Static analysis:** `flutter analyze` → **No issues found** (whole project).
- **Key-links:** plan 03 = 3/3, plan 04 = 2/2, plan 05 = 4/4 — all verified.
- **Schema drift:** none (UI-only phase, no ORM/DB changes).
- **Regression gate:** prior-phase tests all pass (the only failure is the pre-existing golden).

## Cross-cutting decisions honored

- **DP-01 (no engine):** no ML Kit / Drift / scorer / capture in any demo screen (source-read tests).
- **DP-02 (tokens + gen-l10n):** no raw hex, no Material `Colors.`; every CTA label via a gen-l10n getter (added `demoPassContinue` for the clean-pass CTA).
- **DP-05 (specific fix):** the miss screen shows the verbatim wrong-direction named fix, never a generic bounce.
- **DP-06 (anti-gamification):** no running counters, weekly tallies, streaks, badges, or confetti; exactly one mastery star on Celebration.
- **Pitfall 5 (one source of truth):** the dotted guide + ink are painted from `DemoAlif.referencePoints`, never rendered as `Text('ا')`.

## Human verification pending (1) — screenshot-readiness walkthrough

The plan's end-of-phase `<human-check>` cannot be automated (it needs a tablet/emulator and a
human eye on rendering). Run before the 2026-06-02 presentation:

1. Launch on a tablet/emulator, navigate to `/demo/home`.
2. Tap the full loop: Home → Watch → Trace → submit → Feedback **miss** (coral failing stroke
   + the specific fix "Start your alif at the top…", tryAgain mascot) → **Try Again** →
   Feedback **pass** (specific praise) → Celebration (cheer mascot + exactly ONE gold star +
   "You Learned Alif. أحسنت.") → **Back Home** → `/demo/home`. Every step advances by tapping.
3. Confirm on every screen: parchment (never white), no debug banner, no star counter / weekly
   tally / streak / badge / confetti / 3-star rating; the miss names a SPECIFIC fix.
4. Confirm the mascot illustrations render (not the fallback box) and Arabic shapes correctly
   (alif + أحسنت), RTL only inside those islands.

## Notes / minor follow-ups (non-blocking)

- Two guide painters coexist by design: `DottedAlifPainter` (Watch — carries the numbered gold
  start-dot) and the reusable `DottedGuidePainter` (Trace/Feedback). A future cleanup could fold
  Watch onto the reusable painter; out of scope here.
- Three Rule-3 plan-artifact fixes were applied during execution (over-escaped key-link regexes
  and one `from:` path with a `(miss)` annotation) — all were unsatisfiable/malformed checks in
  the plan frontmatter, corrected so key-links verify the real intent. See plan SUMMARYs.

## Verdict

**PASSED** (automated). All 5 plans complete, all 12 must-haves verified, full walkthrough
navigable with no dead ends, anti-gamification held, no regressions. One human screenshot
walkthrough remains as pre-presentation confirmation (persisted as a HUMAN-UAT item).
