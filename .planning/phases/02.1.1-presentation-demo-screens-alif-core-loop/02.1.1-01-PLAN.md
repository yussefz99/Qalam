---
phase: 02.1.1-presentation-demo-screens-alif-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/mascot/qalam-idle.svg
  - assets/mascot/qalam-write.svg
  - assets/mascot/qalam-cheer.svg
  - assets/mascot/qalam-try-again.svg
  - assets/mascot/qalam-think.svg
  - assets/icons/star.svg
  - assets/icons/check-complete.svg
  - assets/icons/ink-drop.svg
  - assets/icons/qalam-nib.svg
  - assets/icons/lock.svg
  - pubspec.yaml
  - lib/widgets/qalam_mascot.dart
  - lib/demo/demo_alif.dart
  - test/widgets/qalam_mascot_test.dart
  - test/demo/demo_alif_test.dart
autonomous: true
requirements: [S1-04, PLAT-03]
must_haves:
  truths:
    - "The five brand mascot SVGs render from assets/mascot/ inside the running app."
    - "A missing mascot asset degrades to a calm placeholder, never a red error box or a crash."
    - "Every demo screen can read the alif glyph and the three authored named-fix strings from one mocked source — no scorer, no Drift, no network."
  artifacts:
    - path: "assets/mascot/qalam-idle.svg"
      provides: "Idle/home mascot pose (brand SVG, byte-identical to the design kit)"
    - path: "assets/mascot/qalam-write.svg"
      provides: "Writing/demo mascot pose for the Watch screen"
    - path: "assets/mascot/qalam-cheer.svg"
      provides: "Cheer pose for the Celebration screen"
    - path: "assets/mascot/qalam-try-again.svg"
      provides: "Try-again pose for the Feedback miss screen"
    - path: "lib/widgets/qalam_mascot.dart"
      provides: "QalamMascot widget — typed pose enum + flutter_svg render + graceful missing-asset fallback"
      min_lines: 40
    - path: "lib/demo/demo_alif.dart"
      provides: "DemoAlif static mock content (glyph, named-fix strings, mistake ids) sourced from letters.json alif — zero engine"
      min_lines: 30
  key_links:
    - from: "lib/widgets/qalam_mascot.dart"
      to: "assets/mascot/*.svg"
      via: "SvgPicture.asset behind an error-builder fallback"
      pattern: "SvgPicture\\.asset"
    - from: "lib/demo/demo_alif.dart"
      to: "assets/curriculum/letters.json alif commonMistakes[].feedback"
      via: "verbatim named-fix strings copied from the authored curriculum"
      pattern: "needs to be taller|come down|straight line"
---

<objective>
Bundle the real Qalam brand assets into the app and create the shared demo foundation
every later screen depends on: a reusable mascot widget (typed poses + graceful
fallback) and a single static `DemoAlif` mock content source.

Purpose: The mascot SVGs live only in the design kit today (`assets/mascot/` is not even
registered in pubspec). The screens that follow render the real illustrations and depict
the tutor's named fixes — both must read from one bundled, mocked source so the rest of
the phase is fast and impossible to break on stage (DP-01, DP-08).
Output: 10 bundled SVGs, an updated pubspec asset manifest, `QalamMascot`, and `DemoAlif`.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@assets/curriculum/letters.json
@pubspec.yaml
@lib/theme/colors.dart
@lib/theme/dimens.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle the 10 brand SVGs and register assets/mascot/ in pubspec</name>
  <read_first>
    - pubspec.yaml (the existing `flutter.assets:` block — `assets/icons/` is already listed, `assets/mascot/` is NOT)
    - docs/design/kit/project/assets/mascot/ (source: qalam-idle.svg, qalam-write.svg, qalam-cheer.svg, qalam-try-again.svg, qalam-think.svg)
    - docs/design/kit/project/assets/icons/ (source: star.svg, check-complete.svg, ink-drop.svg, qalam-nib.svg, lock.svg)
    - lib/screens/home_screen.dart (the existing SvgPicture.asset('assets/logo.svg') usage — flutter_svg ^2.3.0 is already a dependency)
  </read_first>
  <action>
    Copy all five mascot SVGs from docs/design/kit/project/assets/mascot/ into assets/mascot/ and all five icon SVGs from docs/design/kit/project/assets/icons/ into assets/icons/, byte-for-byte (DP-08; verify identical with a checksum). assets/icons/ is already registered in pubspec but currently empty on the app side — these are the real icon glyphs. Add `- assets/mascot/` to the `flutter.assets:` list in pubspec.yaml (assets/icons/ stays). Run `flutter pub get`. Do NOT modify the fonts block. Do NOT re-encode or optimize the SVGs — verbatim copies only, so the rendered illustration matches the design kit.
  </action>
  <acceptance_criteria>
    - All 10 target files exist under assets/mascot/ and assets/icons/ and are byte-identical to their design-kit sources (diff is empty for each pair).
    - `grep -c "assets/mascot/" pubspec.yaml` returns >= 1 (mascot dir registered).
    - `flutter pub get` exits 0.
    - `flutter analyze` exits 0 (no broken asset references introduced).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && for f in qalam-idle qalam-write qalam-cheer qalam-try-again qalam-think; do diff "docs/design/kit/project/assets/mascot/$f.svg" "assets/mascot/$f.svg" || exit 1; done && for f in star check-complete ink-drop qalam-nib lock; do diff "docs/design/kit/project/assets/icons/$f.svg" "assets/icons/$f.svg" || exit 1; done && grep -v '^#' pubspec.yaml | grep -c "assets/mascot/"</automated>
  </verify>
  <done>The 10 brand SVGs are bundled verbatim and assets/mascot/ is registered; pub get succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QalamMascot widget — typed poses + graceful missing-asset fallback</name>
  <files>lib/widgets/qalam_mascot.dart, test/widgets/qalam_mascot_test.dart</files>
  <read_first>
    - lib/screens/home_screen.dart (the _QalamWordmark SvgPicture.asset pattern + semanticsLabel usage to mirror)
    - lib/theme/colors.dart (QalamColors.surface/fgMuted for the fallback box — NEVER QalamColors-red; there is no red)
    - lib/theme/dimens.dart (QalamRadii, QalamSpace for the fallback sizing)
    - lib/widgets/arabic_text.dart (the "one widget, one concern, const constructor" house style)
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-08 (pose→state map: idle=home, write=Watch demo, cheer=celebration, try-again=miss feedback; every SvgPicture.asset behind a graceful fallback)
  </read_first>
  <behavior>
    - Test 1: QalamMascot(pose: QalamPose.idle) renders an SvgPicture pointing at assets/mascot/qalam-idle.svg.
    - Test 2: each QalamPose value maps to the correct asset path (idle→qalam-idle, write→qalam-write, cheer→qalam-cheer, tryAgain→qalam-try-again, think→qalam-think).
    - Test 3: when SvgPicture's errorBuilder fires (asset missing), the widget renders a calm placeholder Container (a sized, rounded, muted box) and never throws — assert the fallback widget is present and no exception is recorded.
    - Test 4: a non-empty semanticsLabel is provided (accessibility), and the widget exposes a `size` parameter respected by the rendered SVG.
  </behavior>
  <action>
    Create a `QalamMascot` StatelessWidget with a `QalamPose` enum (idle, write, cheer, tryAgain, think) and a const constructor taking `pose`, an optional `size` (default a sensible hero size from QalamSpace, e.g. space24 scale), and an optional `semanticsLabel`. Map each pose to its `assets/mascot/qalam-*.svg` path via a private switch. Render with `SvgPicture.asset` and supply `placeholderBuilder` AND an error fallback (use SvgPicture's error handling path) that returns a `_MascotFallback` — a sized rounded `Container` using QalamColors.surface fill + QalamColors.fgMuted iconography, NO red, no debug text. The fallback keeps the layout stable so a missing asset never breaks a screenshot (DP-08/DP-09). Tokens only; no raw hex, no magic numbers.
  </action>
  <acceptance_criteria>
    - `lib/widgets/qalam_mascot.dart` defines `enum QalamPose` with exactly the five values and a `QalamMascot` widget with a const constructor.
    - `grep -c "SvgPicture.asset" lib/widgets/qalam_mascot.dart` returns 1 (single render path) and the file references an error/fallback builder.
    - No raw hex (`grep -E "0x[0-9A-Fa-f]{8}|#[0-9A-Fa-f]{6}" lib/widgets/qalam_mascot.dart` returns nothing) and no `Colors.red`.
    - All four behavior tests in test/widgets/qalam_mascot_test.dart pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/widgets/qalam_mascot_test.dart && flutter analyze lib/widgets/qalam_mascot.dart</automated>
  </verify>
  <done>QalamMascot renders the right pose SVG, falls back gracefully on a missing asset, and is fully token-driven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DemoAlif — the single static mock content source (zero engine)</name>
  <files>lib/demo/demo_alif.dart, test/demo/demo_alif_test.dart</files>
  <read_first>
    - assets/curriculum/letters.json (alif entry: char "ا", name.display "Alif", the three commonMistakes[].feedback strings — copy these VERBATIM)
    - lib/models/letter.dart (CommonMistake shape: id/check/feedback — DemoAlif mirrors this content without loading the engine)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md "Feedback / error states" table (the named-fix copy contract; fallback line "Something looks off — try again, slower this time." — NEVER "Oops, try again!")
    - .planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-CONTEXT.md DP-01 (mocked/static, no scorer/Drift/network) and DP-05 (specific named fixes sourced from alif commonMistakes)
  </read_first>
  <behavior>
    - Test 1: DemoAlif.glyph equals "ا" and DemoAlif.displayName equals "Alif".
    - Test 2: DemoAlif.heroMissFix returns the alif wrong_direction (or too_short) named fix string verbatim from letters.json — assert it contains "Start your alif at the top" (the hero miss line) and is NOT the generic "Oops, try again!".
    - Test 3: DemoAlif.namedFixes exposes all three authored fixes keyed by mistake id (too_short, wrong_direction, too_curved) with feedback strings matching letters.json byte-for-byte.
    - Test 4: DemoAlif exposes the resolved single-stroke reference points for alif (the topToBottom vertical line) so the Trace/Watch painter has one source of truth, with no dart:ui import in this file.
  </behavior>
  <action>
    Create `DemoAlif` — a pure-Dart static class (no widgets, no dart:ui, no I/O) holding the hardcoded happy-path content for the whole demo: `glyph` ("ا"), `displayName` ("Alif"), `arabicName` ("اَلِف"), the single reference stroke points (the alif vertical line, normalized 0..1, topToBottom — reuse the values from letters.json alif referenceStrokes), and a `namedFixes` map of the three authored CommonMistake feedback strings keyed by id, plus a `heroMissFix` getter that returns the hero miss line. Copy the feedback strings VERBATIM from letters.json (DP-05) — they are the contract. Add a const `fallbackFix` = "Something looks off — try again, slower this time." (the UI-SPEC fallback; never "Oops, try again!"). This class is the ONLY mocked state source — no scorer, no Drift, no network (DP-01).
  </action>
  <acceptance_criteria>
    - `lib/demo/demo_alif.dart` imports neither `dart:ui`, `package:drift`, nor any network package (`grep -E "dart:ui|drift|http" lib/demo/demo_alif.dart` returns nothing).
    - The three named-fix strings appear verbatim (grep for "needs to be taller", "come down — not from the bottom up", "straight line — try to keep it" all match).
    - No "Oops, try again" anywhere in the file.
    - All four behavior tests in test/demo/demo_alif_test.dart pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/demo/demo_alif_test.dart && ! grep -q "Oops" lib/demo/demo_alif.dart && grep -q "needs to be taller" lib/demo/demo_alif.dart</automated>
  </verify>
  <done>DemoAlif holds all mocked alif content verbatim from the curriculum, pure-Dart, engine-free, and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-kit → app bundle | SVG assets copied from docs/ into the shipped app bundle |
| curriculum data → demo source | named-fix strings copied verbatim into DemoAlif |

This phase is an offline, mocked-data UI demo: no network, no user input persisted, no
auth, no secrets. STRIDE surface is minimal (proportional, per ASVS L1).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1.1-01 | Tampering | bundled SVG assets | accept | Assets are static brand illustrations, no executable content; byte-diff against the design-kit source in Task 1 verify guards against accidental corruption. |
| T-02.1.1-02 | Denial of Service | missing/corrupt mascot SVG | mitigate | QalamMascot renders a graceful fallback Container via error builder (Task 2) — a missing asset cannot crash a screen or a screenshot. |
| T-02.1.1-03 | Information Disclosure | DemoAlif content | accept | Content is public curriculum (Arabic letter glyph + authored tutor copy) — no PII, no child data, nothing sensitive. |
| T-02.1.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — flutter_svg ^2.3.0 is already a vetted dependency; no new runtime packages introduced. |
</threat_model>

<verification>
- `flutter pub get` succeeds with assets/mascot/ registered.
- `flutter analyze` clean for the new files.
- `flutter test test/widgets/qalam_mascot_test.dart test/demo/demo_alif_test.dart` green.
- The 10 brand SVGs byte-match the design kit.
</verification>

<success_criteria>
- The real brand mascot + icon SVGs are bundled and registered (DP-08).
- QalamMascot renders the correct pose and degrades gracefully on a missing asset (DP-08/DP-09).
- DemoAlif is the single static, engine-free source of alif glyph + named-fix copy (DP-01/DP-05).
- No new runtime packages; tokens only; no red anywhere.
</success_criteria>

<output>
Create `.planning/phases/02.1.1-presentation-demo-screens-alif-core-loop/02.1.1-01-SUMMARY.md` when done.
</output>
