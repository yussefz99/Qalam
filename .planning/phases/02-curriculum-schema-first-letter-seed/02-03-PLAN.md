---
phase: 02-curriculum-schema-first-letter-seed
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - lib/data/curriculum_repository.dart
  - lib/data/curriculum_repository.g.dart
  - test/data/curriculum_repository_test.dart
autonomous: false
requirements: [CUR-01]
must_haves:
  truths:
    - "CurriculumRepository.getLetters() returns a List<Letter> of length 28 loaded from the bundled letters.json"
    - "CurriculumRepository.getLetter('alif') returns the alif Letter with signedOff: true"
    - "CurriculumRepository.getLesson('lesson_01') returns the lesson_01 Lesson with one item (alif)"
    - "CurriculumRepository.getExercises() returns an empty list without throwing (exercises.json does not exist — D-10)"
    - "The curriculumRepositoryProvider is kept-alive (app-scoped) and typed as CurriculumRepository"
    - "All repository tests pass: flutter test test/data/curriculum_repository_test.dart"
    - "flutter analyze lib/data/curriculum_repository.dart returns no errors"
    - "Owner has confirmed alif is signedOff: true in letters.json before this plan is marked done (D-12)"
  artifacts:
    - path: "lib/data/curriculum_repository.dart"
      provides: "CurriculumRepository class with getLetters(), getLetter(id), getLesson(id), getExercises() — loads from rootBundle, caches in memory, exposes Riverpod kept-alive provider"
      contains: "class CurriculumRepository"
    - path: "lib/data/curriculum_repository.g.dart"
      provides: "Riverpod codegen output for curriculumRepositoryProvider"
      contains: "curriculumRepositoryProvider"
    - path: "test/data/curriculum_repository_test.dart"
      provides: "Integration tests for CurriculumRepository against real JSON fixture strings (not rootBundle) covering full load, getLetter, getLesson, and graceful exercises.json absence"
      contains: "CurriculumRepository"
  key_links:
    - from: "lib/data/curriculum_repository.dart"
      to: "assets/curriculum/letters.json and assets/curriculum/lessons.json"
      via: "rootBundle.loadString('assets/curriculum/letters.json') in CurriculumRepository._load()"
      pattern: "rootBundle.loadString"
    - from: "CurriculumRepository.getLetters()"
      to: "Letter.fromJson (lib/models/letter.dart)"
      via: "decoded JSON list mapped through Letter.fromJson"
      pattern: "Letter.fromJson"
    - from: "curriculumRepositoryProvider (kept-alive)"
      to: "todaysLessonProvider (Phase 3)"
      via: "ref.watch(curriculumRepositoryProvider) in Phase 3's todaysLessonProvider"
      pattern: "curriculumRepositoryProvider"
---

<objective>
Wire the CurriculumRepository — the single data-layer object that loads both curriculum
JSON files via rootBundle, decodes them into typed Letter and Lesson models, caches the
result in memory, and exposes them through a kept-alive Riverpod provider.

This plan is the last piece of Phase 2: after it completes, Phase 3 can call
`curriculumRepositoryProvider` to get the alif Letter with its reference stroke paths
and kick off the first end-to-end trace loop.

The repository must handle the absence of `exercises.json` gracefully (return an empty
list, not throw) per D-10, because that file does not exist until Phase 8.

The plan ends with a human sign-off confirmation that alif's `signedOff` field is `true`
in the committed `letters.json` — the Phase 2 completeness gate per D-12.

Purpose: Complete the data pipeline from bundled JSON files to typed Dart models
accessible via Riverpod across the app.

Output: `lib/data/curriculum_repository.dart`, its generated `.g.dart` counterpart,
and `test/data/curriculum_repository_test.dart`.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-curriculum-schema-first-letter-seed/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@lib/data/app_database.dart
@lib/models/letter.dart
@lib/models/lesson.dart
@assets/curriculum/letters.json
@assets/curriculum/lessons.json
@pubspec.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CurriculumRepository — rootBundle loader, in-memory cache, Riverpod provider</name>
  <files>lib/data/curriculum_repository.dart, lib/data/curriculum_repository.g.dart, test/data/curriculum_repository_test.dart</files>
  <read_first>
    - lib/data/app_database.dart (the @Riverpod(keepAlive: true) provider pattern to mirror exactly)
    - lib/models/letter.dart (Letter and sub-types — the return type of getLetters())
    - lib/models/lesson.dart (Lesson — the return type of getLesson())
    - assets/curriculum/letters.json (read the full file — know the exact JSON shape being parsed)
    - assets/curriculum/lessons.json (read the full file)
    - pubspec.yaml (confirm riverpod_annotation and build_runner are in dependencies — needed for @Riverpod annotation)
  </read_first>
  <behavior>
    Test expectations (write first in test/data/curriculum_repository_test.dart — must FAIL, then implement to GREEN):

    The tests must NOT use rootBundle (it requires a Flutter test harness). Instead, test
    CurriculumRepository by injecting JSON strings directly. The repository must accept
    an optional `letterJson` and `lessonJson` parameter in its constructor (or a
    `CurriculumRepository.fromStrings(String lettersJson, String lessonsJson)` factory)
    for use in tests — the default constructor reads from rootBundle. This is the test
    seam pattern. The Riverpod provider uses the default constructor.

    - Test: CurriculumRepository.fromStrings(lettersJson, lessonsJson).getLetters() returns 28 Letters when lettersJson is a minimal fixture with 28 entries.
    - Test: getLetters() returns Letters in introOrder ascending (alif first with introOrder 1).
    - Test: getLetter('alif') returns the alif Letter; getLetter('nonexistent') returns null.
    - Test: getLesson('lesson_01') returns the Lesson with id 'lesson_01' and 1 item; getLesson('nonexistent') returns null.
    - Test: getExercises() returns an empty list when exercisesJson is null (exercises.json does not exist — D-10).
    - Test: getLetters() is called twice; the result is the same List instance (memory cache — called once not twice).

    For the fixture, you can use a 1-letter JSON (just alif) for most tests and a count
    of 28 for the length test (generate a fixture with 28 minimal entries in the test file).
  </behavior>
  <action>
    Write tests first (RED), then implement to GREEN.

    --- test/data/curriculum_repository_test.dart (write first) ---

    Import `package:flutter_test/flutter_test.dart`, `package:qalam/models/letter.dart`,
    `package:qalam/models/lesson.dart`, `package:qalam/data/curriculum_repository.dart`.
    Do NOT import `package:flutter/services.dart` or use rootBundle in tests.

    Build minimal JSON fixture strings inline in the test file:
    - `_alifJson`: a valid alif letter entry as a JSON string (all required fields,
      referenceStrokes: [], commonMistakes: [], mistakesStatus: "placeholder",
      signedOff: false, cleanRepsToAdvance: 3, audio: {letter: null, examples: []}).
    - `_lesson01Json`: the lesson_01 JSON string.
    - `_make28LettersJson(String singleLetterJson)`: a helper that wraps a JSON array
      of 28 copies of the letter entry (with different ids: "letter_00" through
      "letter_27", introOrder 1–28) for the length test.

    Test the behavior cases listed above. Use `group('CurriculumRepository', () {...})`.

    --- lib/data/curriculum_repository.dart (implement to GREEN) ---

    Create this file. Imports: `dart:convert`, `package:flutter/services.dart` (for
    rootBundle), `package:riverpod_annotation/riverpod_annotation.dart`,
    `package:qalam/models/letter.dart`, `package:qalam/models/lesson.dart`.

    Part file declaration: `part 'curriculum_repository.g.dart';`

    class CurriculumRepository {
      // In-memory cache: loaded once, held for app lifetime (provider is kept-alive)
      List<Letter>? _letters;
      List<Lesson>? _lessons;

      // Injected JSON strings (non-null = test mode; null = load from rootBundle)
      final String? _lettersJsonOverride;
      final String? _lessonsJsonOverride;

      CurriculumRepository() : _lettersJsonOverride = null, _lessonsJsonOverride = null;

      CurriculumRepository.fromStrings(String lettersJson, String lessonsJson)
          : _lettersJsonOverride = lettersJson, _lessonsJsonOverride = lessonsJson;

      Future<void> _ensureLoaded() async {
        if (_letters != null) return;  // already cached

        final lettersRaw = _lettersJsonOverride ??
            await rootBundle.loadString('assets/curriculum/letters.json');
        final lessonsRaw = _lessonsJsonOverride ??
            await rootBundle.loadString('assets/curriculum/lessons.json');

        final lettersDecoded = (json.decode(lettersRaw) as Map<String, dynamic>)['letters'] as List<dynamic>;
        _letters = lettersDecoded
            .map((e) => Letter.fromJson(e as Map<String, dynamic>))
            .toList()
          ..sort((a, b) => a.introOrder.compareTo(b.introOrder));

        final lessonsDecoded = (json.decode(lessonsRaw) as Map<String, dynamic>)['lessons'] as List<dynamic>;
        _lessons = lessonsDecoded
            .map((e) => Lesson.fromJson(e as Map<String, dynamic>))
            .toList();
      }

      Future<List<Letter>> getLetters() async {
        await _ensureLoaded();
        return List.unmodifiable(_letters!);
      }

      Future<Letter?> getLetter(String id) async {
        final letters = await getLetters();
        try {
          return letters.firstWhere((l) => l.id == id);
        } on StateError {
          return null;
        }
      }

      Future<List<Lesson>> getLessons() async {
        await _ensureLoaded();
        return List.unmodifiable(_lessons!);
      }

      Future<Lesson?> getLesson(String id) async {
        final lessons = await getLessons();
        try {
          return lessons.firstWhere((l) => l.id == id);
        } on StateError {
          return null;
        }
      }

      /// Returns empty list if exercises.json does not exist (Phase 8 creates it).
      /// D-10: CurriculumRepository handles its absence gracefully — return empty, not throw.
      Future<List<dynamic>> getExercises() async {
        if (_lettersJsonOverride != null) {
          // Test mode: no exercises file; return empty
          return const [];
        }
        try {
          final raw = await rootBundle.loadString('assets/curriculum/exercises.json');
          final decoded = json.decode(raw) as Map<String, dynamic>;
          return (decoded['exercises'] as List<dynamic>?) ?? [];
        } catch (_) {
          // File absent or malformed — return empty, never throw (D-10)
          return const [];
        }
      }
    }

    // Riverpod kept-alive provider — mirrors app_database.dart pattern
    @Riverpod(keepAlive: true)
    CurriculumRepository curriculumRepository(CurriculumRepositoryRef ref) {
      return CurriculumRepository();
    }

    After writing the file, run build_runner to generate the .g.dart file:
    `dart run build_runner build --delete-conflicting-outputs`

    Confirm `lib/data/curriculum_repository.g.dart` is generated and contains
    `curriculumRepositoryProvider`.

    IMPORTANT: `_ensureLoaded()` must use `await` inside an async method and the
    cache check `if (_letters != null) return;` must appear BEFORE any await, so
    concurrent calls do not trigger double-loads. In the current implementation this
    is safe because Dart is single-threaded — but the guard is still correct practice.

    Do NOT add Firebase, HTTP, or network imports. This is a local-only v1 loader.
    Do NOT add any try/catch around the letters.json or lessons.json loads (let a
    missing bundled asset crash at startup — it is a developer error, not a runtime
    error). ONLY getExercises() swallows errors.
  </action>
  <verify>
    <automated>flutter test test/data/curriculum_repository_test.dart --reporter expanded</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/data/curriculum_repository_test.dart` exits 0 (all tests green).
    - `dart run build_runner build --delete-conflicting-outputs` exits 0 and generates curriculum_repository.g.dart.
    - `grep -c "curriculumRepositoryProvider" lib/data/curriculum_repository.g.dart` returns >= 1.
    - `flutter analyze lib/data/curriculum_repository.dart` reports no errors.
    - Tests cover: 28-letter load, introOrder sorting (alif first), getLetter hit/miss, getLesson hit/miss, getExercises empty-list when no file.
    - `grep -c "keepAlive: true" lib/data/curriculum_repository.dart` returns >= 1 (provider is app-scoped).
  </acceptance_criteria>
  <done>
    CurriculumRepository loads both JSON files, caches in memory, returns typed models,
    handles exercises.json absence gracefully, and has a kept-alive Riverpod provider —
    all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-stack smoke test — run all Phase 2 tests and flutter analyze</name>
  <files></files>
  <read_first>
    - test/models/letter_test.dart (from Plan 02-02)
    - test/models/lesson_test.dart (from Plan 02-02)
    - test/data/curriculum_repository_test.dart (from Task 1 above)
  </read_first>
  <action>
    Run the complete Phase 2 test suite and static analysis to confirm everything
    integrates cleanly end-to-end before the sign-off checkpoint.

    1. Run all Phase 1 + Phase 2 tests together:
       `flutter test`
       Confirm ALL tests pass (Phase 1's 12 tests + Phase 2's new model and repository
       tests). If any Phase 1 test regresses, investigate before continuing.

    2. Run static analysis over the full lib/:
       `flutter analyze`
       Confirm zero errors and zero warnings. If warnings appear in lib/models/ or
       lib/data/curriculum_repository.dart, fix them before the checkpoint.

    3. Verify the import rule holds across all new files:
       `grep -rn "package:qalam/data" lib/models/`
       Must return no matches (models do not import from data/).
       `grep -rn "package:flutter/" lib/models/`
       Must return no matches (models do not import Flutter).

    4. Verify the pubspec asset declaration is present:
       `grep -c "assets/curriculum/" pubspec.yaml`
       Must return >= 1.

    5. Confirm the generated file exists:
       Check that `lib/data/curriculum_repository.g.dart` exists and is non-empty.

    If any check fails, fix the issue before proceeding to the sign-off checkpoint.
    This task is a quality gate, not a coding task — do not proceed until all five
    checks pass.
  </action>
  <verify>
    <automated>flutter test && flutter analyze && grep -rn "package:qalam/data" lib/models/ | grep -c . || true</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` exits 0 (all tests — Phase 1 + Phase 2 — pass).
    - `flutter analyze` exits 0 with no errors.
    - `grep -rn "package:qalam/data" lib/models/` returns no output.
    - `grep -rn "package:flutter/" lib/models/` returns no output.
    - `grep -c "assets/curriculum/" pubspec.yaml` returns >= 1.
    - `lib/data/curriculum_repository.g.dart` exists and contains `curriculumRepositoryProvider`.
  </acceptance_criteria>
  <done>
    All Phase 1 and Phase 2 tests pass; flutter analyze clean; import rule enforced;
    asset declaration present; generated file exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner — confirm alif is signedOff: true before Phase 2 is declared done</name>
  <action>
    This is the Phase 2 completeness gate (D-12). Phase 3 will trace alif — it cannot
    proceed until alif has real reference stroke paths and owner sign-off.

    STEP 1 — Verify alif's sign-off status in letters.json:
    Run this command from the project root:
    ```
    python -c "
    import json
    letters = json.load(open('assets/curriculum/letters.json'))['letters']
    alif = next((l for l in letters if l['id'] == 'alif'), None)
    if alif is None:
        print('ERROR: alif not found in letters.json')
    elif not alif['signedOff']:
        print('BLOCKED: alif signedOff is false — complete Plan 02-01 checkpoint first')
        print('referenceStrokes count:', len(alif['referenceStrokes']))
        print('commonMistakes count:', len(alif['commonMistakes']))
    else:
        print('PASSED: alif signedOff: true')
        print('referenceStrokes:', len(alif['referenceStrokes']), 'stroke(s)')
        print('commonMistakes:', len(alif['commonMistakes']), 'mistake(s)')
    "
    ```

    STEP 2 — If the output shows BLOCKED:
    You need to complete the owner checkpoint in Plan 02-01 (Task 3) first:
    - Run `python tools/extract_reference_paths.py` to generate candidate_paths.json.
    - Open tools/candidate_paths.json and inspect the alif contours.
    - Update alif's `referenceStrokes` in letters.json with the correct contour points.
    - Add commonMistakes for alif (at minimum: too_short, wrong_direction).
    - Set `"signedOff": true` and `"mistakesStatus": "authored"` on alif.
    - Re-run the command from Step 1.

    STEP 3 — If the output shows PASSED:
    Review the alif reference stroke data visually:
    - Open tools/candidate_paths.json and look at alif's contour shape.
    - Look at alif's referenceStrokes in letters.json.
    - Confirm the stroke direction ("topToBottom") matches how the letter is taught.
    - Confirm the common mistakes in the commonMistakes array are child-friendly and
      specific (e.g. "Start your alif at the top and come down — not from the bottom up.").

    STEP 4 — Run the full test suite one final time:
    ```
    flutter test
    ```
    Confirm all tests pass.

    When all checks pass, type "approved" to close Phase 2.
  </action>
  <acceptance_criteria>
    - Python sign-off check prints "PASSED: alif signedOff: true".
    - alif has at least 1 referenceStroke with a non-empty points array.
    - alif has at least 1 commonMistake with child-friendly feedback text.
    - `flutter test` passes (all tests green).
    - Owner has personally confirmed the stroke direction and mistake messages are correct.
  </acceptance_criteria>
  <done>
    Owner confirms: alif is signedOff: true in committed letters.json; reference paths
    are present; common mistakes are child-friendly; all tests pass. Phase 2 is done.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rootBundle (APK assets) → CurriculumRepository | JSON files are bundled read-only in the APK; no network, no external data, no user input crosses this boundary |
| CurriculumRepository → Riverpod providers | Repository is app-scoped (kept-alive); consumers call getLetters/getLesson — read-only access, no mutation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | CurriculumRepository._ensureLoaded | accept | Reads from bundled APK assets only — no writable path, no network source. A corrupted APK is an installation integrity problem, not an app-level attack surface. |
| T-02-07 | Denial of Service | rootBundle.loadString crash on malformed JSON | accept | JSON files are developer-authored and bundled; a malformed file would crash at first launch during development — caught in CI by the flutter test suite before shipping. No runtime recovery needed. |
| T-02-08 | Information Disclosure | CurriculumRepository.getExercises() swallowing errors | accept | exercises.json absence is intentional (D-10); the catch-all in getExercises() returns [] and discards the exception — no sensitive data exposed, no error concealment of concern. |
</threat_model>

<verification>
- `flutter test` exits 0 (all Phase 1 + Phase 2 tests pass).
- `flutter analyze` exits 0.
- `grep -c "curriculumRepositoryProvider" lib/data/curriculum_repository.g.dart` returns >= 1.
- `grep -c "keepAlive: true" lib/data/curriculum_repository.dart` returns >= 1.
- Python sign-off gate prints "PASSED: alif signedOff: true".
- `grep -rn "package:qalam/data" lib/models/` returns no output (import rule enforced throughout Phase 2).
</verification>

<success_criteria>
- CurriculumRepository loads letters.json and lessons.json via rootBundle, caches in memory, and returns typed List<Letter> and Lesson? via async methods.
- getExercises() returns [] when exercises.json is absent — no throw, no crash.
- curriculumRepositoryProvider is kept-alive and generated by build_runner.
- All Phase 2 tests (models + repository) pass alongside all Phase 1 tests.
- flutter analyze is clean.
- alif is signedOff: true in the committed letters.json — the owner's-mother contribution is in the repo.
- Phase 3 can now call `ref.watch(curriculumRepositoryProvider).getLesson('lesson_01')` to get alif and boot the first end-to-end trace.
</success_criteria>

<output>
Create `.planning/phases/02-curriculum-schema-first-letter-seed/02-03-SUMMARY.md` when done.
</output>
