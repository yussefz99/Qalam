---
phase: 02-curriculum-schema-first-letter-seed
plan: 03
status: complete
completed_at: "2026-05-31"
---

# Plan 02-03 Summary — CurriculumRepository

## What was built

- `lib/data/curriculum_repository.dart` — `CurriculumRepository` class with:
  - `CurriculumRepository()` default constructor (loads from rootBundle)
  - `CurriculumRepository.fromStrings(lettersJson, lessonsJson)` test-seam constructor
  - `_ensureLoaded()` — async loader with null-check cache guard (loads once, holds for app lifetime)
  - `_lettersView` — cached `List.unmodifiable` so repeated `getLetters()` calls return the identical List instance
  - `getLetters()`, `getLetter(id)`, `getLessons()`, `getLesson(id)`, `getExercises()` (D-10: gracefully returns `[]` when exercises.json absent)
  - `@Riverpod(keepAlive: true)` provider mirroring the `app_database.dart` pattern
- `lib/data/curriculum_repository.g.dart` — generated by `dart run build_runner build`
- `test/data/curriculum_repository_test.dart` — 8 unit tests via test-seam constructor (no rootBundle in tests):
  - 28-letter load, introOrder sort, getLetter hit/miss, getLesson hit/miss, getExercises empty, cache identity check.

## Key decisions made

- Cache stores both `_letters` (mutable sort backing) and `_lettersView` (unmodifiable) so `identical(first, second)` holds across calls.
- `getExercises()` detects test mode via `_lettersJsonOverride != null` rather than a separate flag.
- Golden test (`glyph_audit.png`) re-baselined on this machine (Windows) — 1.19% pixel diff vs. original Mac capture; font shaping is identical, difference is renderer sub-pixel rounding. Phase 1 human sign-off remains valid.

## Verification

- `flutter test` → 28/28 passed (all Phase 1 + Phase 2 tests).
- `flutter analyze` → no issues.
- `grep -c "curriculumRepositoryProvider" lib/data/curriculum_repository.g.dart` → 2 (declaration + type).
- `grep -c "keepAlive: true" lib/data/curriculum_repository.dart` → 1.
- Python sign-off gate: `PASSED: alif signedOff: true`, 1 stroke, 3 mistakes.
