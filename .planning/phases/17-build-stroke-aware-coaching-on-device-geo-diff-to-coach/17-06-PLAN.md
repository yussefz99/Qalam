---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 06
type: execute
wave: 5
depends_on: [17-05]
files_modified:
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - lib/features/letter_unit/widgets/write_surface.dart
  - lib/tutor/tutor_facts.dart
  - lib/tutor/tutor_facts_builder.dart
  - lib/tutor/remote_agent_brain.dart
  - lib/tutor/tutor_decision.dart
  - lib/features/letter_unit/sections/meet_section.dart
  - test/tutor/payload_nonpii_test.dart
  - test/tutor/remote_agent_brain_test.dart
  - test/features/letter_unit/stroke_image_grep_guard_test.dart
  - server/app/main.py
  - server/app/schema.py
  - server/app/image_judge.py
  - server/tests/test_payload_nonpii.py
  - server/tests/test_endpoint.py
autonomous: true
requirements: [GROUND-04, STRK-01]

must_haves:
  truths:
    - "The deterministic on-device scorer OWNS pass/fail on every path — the verdict renders instantly, offline, with no AI deferral and no flash-then-overwrite (D-A; GROUND-01 restored; UAT F2 structurally fixed)"
    - "No rendered handwriting image ever leaves the device — strokeImage is gone from payload construction, the DTO, and the judge path (GROUND-04 surface shrink, a recorded win)"
    - "A cold, slow, or offline server only affects the WORDS, never the verdict or the star"
    - "English helper copy renders left-to-right (UAT F1)"
  artifacts:
    - path: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      provides: "unconditional scorer verdict path; aiJudge deferral deleted"
    - path: "test/features/letter_unit/stroke_image_grep_guard_test.dart"
      provides: "source-scan guard: strokeImage absent from lib/ payload construction"
      contains: "strokeImage"
    - path: "server/app/main.py"
      provides: "/coach without the image short-circuit"
  key_links:
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      to: "lib/features/letter_unit/widgets/write_surface.dart"
      via: "onStrokeImage wiring removed on both ends"
      pattern: "onStrokeDiff"
    - from: "lib/tutor/remote_agent_brain.dart"
      to: "server/app/schema.py"
      via: "_parseCoachOut tolerates the absent verdict field"
      pattern: "toolName"
---

<objective>
Implement CONTEXT increment 6 (locked D-A — this REVERSES the Phase-17.1 AI-owns-pass/fail):
route every pass/fail through the upgraded on-device scorer and retire the
`strokeImage` → `image_judge` short-circuit. Removal direction under `extra="forbid"` is
CLIENT FIRST (the client stops sending; the optional server field is then deleted — the
only live client is the dev/demo build, so same-phase deletion is safe, RESEARCH Pattern 3).
`image_judge.py` is DELETED, not demoted (RESEARCH A5 conservative default: the image was the
worst spike arm — 0.20 hallucination — and keeping it re-opens the GROUND-02 image reversal
this phase closes; recorded in ADR-017 by 17-08). Also fixes UAT F1 (RTL English copy) and
reconciles the pre-existing write_surface test failure that sits in this touch set.

Purpose: the verdict is instant, offline, $0, and cannot be overturned by a model —
grounding holds by construction; the off-device child-data surface shrinks.
Output: cutover complete on both sides, grep-guard installed, F1 fixed.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-05-SUMMARY.md
@docs/testing/UAT-FULL-2026-07-01.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client cutover — delete the aiJudge deferral and the image render; scorer verdict unconditional</name>
  <files>lib/features/letter_unit/widgets/exercise_scaffold.dart, lib/features/letter_unit/widgets/write_surface.dart, lib/tutor/tutor_facts.dart, lib/tutor/tutor_facts_builder.dart, lib/tutor/remote_agent_brain.dart, lib/tutor/tutor_decision.dart, test/tutor/payload_nonpii_test.dart, test/tutor/remote_agent_brain_test.dart, test/features/letter_unit/stroke_image_grep_guard_test.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the SEVEN pinned seams from 17-PATTERNS §10: _pendingStrokeImage field ~161-165; _onStrokeImage ~190-194; aiJudge decl + guard ~219-231; strokeImage: arg ~241; decision.verdict overrule block ~256-276; catchError aiJudge fallback ~292-300; onStrokeImage wiring ~437 — line numbers may have drifted, re-pin by symbol)
    - lib/features/letter_unit/widgets/write_surface.dart (17-PATTERNS §9 seams: onStrokeImage param + doc ~60/93-98; baa-only PNG render in _onLetterComplete ~233-247; _renderStrokesToBase64Png ~251-305; dart:convert/dart:ui imports)
    - lib/tutor/tutor_facts.dart (strokeImage field ~137-144 + emission ~165-167 — delete both)
    - lib/tutor/tutor_facts_builder.dart (strokeImage param — delete)
    - lib/tutor/remote_agent_brain.dart (_parseCoachOut — confirm it tolerates an ABSENT verdict key; the normal path never had one) and lib/tutor/tutor_decision.dart (the verdict field consumer — remove or leave-null-tolerant, whichever keeps tests honest)
    - test/tutor/payload_nonpii_test.dart (whitelist entries ~44-54, ~148, ~222-225 that mention strokeImage — SHRINK; the source-scan pattern ~242-259 to copy for the grep-guard)
    - test/features/ (find the pre-existing failing write_surface test — reconcile it)
  </read_first>
  <action>
    Client stops sending FIRST (removal ordering, Pattern 3):
    1. exercise_scaffold.dart: delete _pendingStrokeImage, _onStrokeImage, the
       `final aiJudge = strokeImage != null;` branch and its `if (!aiJudge)` guard (the
       applyResult / markLatency / onGraphNodePassed / _recordAttempt block at ~225-231 runs
       UNCONDITIONALLY — this restores GROUND-01 semantics and kills the flash-then-overwrite
       the deferral existed to hide), the `strokeImage:` argument, the whole
       `if (aiJudge) {...}` decision.verdict overrule block inside .then, and simplify
       catchError (the scorer verdict is already applied; on brain failure just clear the
       tutor line). _pendingStrokeDiff/_onStrokeDiff STAY — that is the surviving transport.
       Update the _onResult doc-comment: the deterministic scorer owns the verdict on EVERY
       path (D-A); the brain only enriches the words.
    2. write_surface.dart: delete the onStrokeImage callback param + doc, the baa-only PNG
       render call in _onLetterComplete, _renderStrokesToBase64Png entirely, and the
       dart:convert / dart:ui imports if orphaned.
    3. tutor_facts.dart: delete the strokeImage field + its toMap emission + the 17.1
       doc-comment. tutor_facts_builder.dart: delete the strokeImage param + pass-through.
    4. remote_agent_brain.dart / tutor_decision.dart: verify _parseCoachOut and
       TutorDecision tolerate a missing `verdict` key (they must — the normal path never had
       one); remove the client-side verdict plumbing that only the aiJudge path consumed.
    5. Guards in the SAME task: payload_nonpii_test.dart — remove strokeImage from the
       whitelist + fully-populated fixture; remote_agent_brain_test.dart — shrink the exact
       mirror-set. NEW test/features/letter_unit/stroke_image_grep_guard_test.dart — copy the
       source-scan move (readAsStringSync, strip //-comment lines, then assert): the token
       `strokeImage` appears in NO file under lib/ (scan lib/tutor/ and
       lib/features/letter_unit/ at minimum), and `_renderStrokesToBase64Png` appears nowhere
       under lib/. Use comment-stripped scanning so doc mentions don't false-trip (grep-gate
       hygiene).
    6. Reconcile the pre-existing write_surface test failure (17-VALIDATION baseline note:
       IN this touch set, must not be silently absorbed): run it, read the failure, and
       either FIX it against the current widget contract or rewrite its stale expectations —
       record which in the summary. It must be GREEN after this task.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/tutor/ test/features/ && flutter analyze lib/tutor/ lib/features/letter_unit/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "strokeImage" lib/ --include="*.dart" | grep -v "^.*//"` returns 0 matches
    - `grep -rn "_renderStrokesToBase64Png\|aiJudge\|_pendingStrokeImage" lib/` returns 0 matches
    - test/features/letter_unit/stroke_image_grep_guard_test.dart exists and passes
    - `flutter test test/tutor/ test/features/` exits 0 — INCLUDING the previously-failing write_surface test (reconciled, recorded in summary)
    - onStrokeDiff wiring is untouched (`grep -n "onStrokeDiff" lib/features/letter_unit/widgets/write_surface.dart` still matches)
  </acceptance_criteria>
  <done>No client path defers the verdict; no image is rendered or sent; the guard makes regression impossible to miss.</done>
</task>

<task type="auto">
  <name>Task 2: Server retirement — remove the short-circuit, the strokeImage field, CoachOut.verdict, and delete image_judge.py</name>
  <files>server/app/main.py, server/app/schema.py, server/app/image_judge.py, server/tests/test_payload_nonpii.py, server/tests/test_endpoint.py</files>
  <read_first>
    - server/app/main.py (the `if facts_in.strokeImage:` short-circuit ~84-112: lazy image_judge import, asyncio.to_thread wrap, CoachOut(verdict=...) return — delete the whole block; KEEP the timeout→503 degrade ladder ~114-138 and the decision log)
    - server/app/schema.py (strokeImage field ~139-146 — delete; CoachOut.verdict ~167-172 — delete: response extras are the server's to drop, and Task 1 verified the Dart parser tolerates its absence)
    - server/app/image_judge.py (to DELETE — enumerate its test files/imports first: grep image_judge across server/)
    - server/tests/test_payload_nonpii.py (strokeImage mentions to shrink)
    - server/tests/test_endpoint.py (any short-circuit/verdict tests to delete or rewrite as "image key now 422s")
  </read_first>
  <action>
    1. main.py: delete the strokeImage short-circuit block and its lazy import. The normal
       graph path below it is the shape that remains. Degrade ladder + non-PII logging kept.
    2. schema.py: delete `strokeImage` from TutorFactsIn and `verdict` from CoachOut, with a
       comment: "retired by Plan 17-06 under D-A (scorer owns the verdict; ADR-017); a
       strokeImage key from a stale client now 422s BY DESIGN — the only live client is the
       demo build, cut over in the same phase (client-first removal ordering)."
    3. DELETE server/app/image_judge.py and every test dedicated to it (grep `image_judge`
       across server/ — remove imports/tests; rewrite any endpoint test that posted
       strokeImage into an assertion that such a payload now 422s).
    4. test_payload_nonpii.py: remove strokeImage from the accepted-field fixtures; ADD
       `strokeImage` to the FORBIDDEN_KEYS parametrize list (it is now an unknown key — the
       422 proves the surface shrank).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest -m code -q && test ! -f app/image_judge.py && echo IMAGE-JUDGE-DELETED</automated>
  </verify>
  <acceptance_criteria>
    - server/app/image_judge.py does not exist
    - `grep -rn "strokeImage\|image_judge" server/app/` returns 0 matches
    - `grep -n "verdict" server/app/schema.py` returns 0 matches inside CoachOut
    - A test asserts a payload containing `strokeImage` now 422s
    - `cd server && uv run pytest -m code -q` exits 0
  </acceptance_criteria>
  <done>The server can no longer receive an image or emit a verdict — the off-device child-data surface is smaller than before Phase 17.1, by construction.</done>
</task>

<task type="auto">
  <name>Task 3: UAT F1 — English helper copy renders LTR</name>
  <files>lib/features/letter_unit/sections/meet_section.dart, test/features/letter_unit/meet_section_ltr_test.dart</files>
  <read_first>
    - lib/features/letter_unit/sections/meet_section.dart (the English guidance strings — UAT F1: "On its own…", "Nothing to write…" render RTL so the trailing period jumps left)
    - docs/testing/UAT-FULL-2026-07-01.md (F1 row — the exact symptom)
    - test/features/ (existing meet_section test — PRE-EXISTING FAILURE in the baseline; do not chase it, do not worsen it)
  </read_first>
  <action>
    Wrap the English helper/guidance Text widgets in meet_section.dart with explicit
    `textDirection: TextDirection.ltr` (on the Text) or a `Directionality(textDirection:
    TextDirection.ltr, ...)` wrapper — English copy only; Arabic glyph/letter rendering stays
    RTL untouched. If the same English-copy pattern exists in the exercise flow's helper
    lines (check prompt_header.dart / feedback_panel_v2.dart for hardcoded English guidance
    rendered under the app's RTL Directionality), apply the same one-line fix there and list
    the touched widgets in the summary. Add a NEW widget test
    (meet_section_ltr_test.dart): pump the widget under an RTL Directionality ancestor and
    assert the English helper Text's resolved textDirection is LTR. Do NOT modify the
    pre-existing failing meet_section test beyond what compiles — its baseline status must
    not get worse (record its before/after status in the summary).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/features/letter_unit/meet_section_ltr_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - meet_section.dart contains `TextDirection.ltr` applied to English helper copy
    - test/features/letter_unit/meet_section_ltr_test.dart exists and passes
    - The pre-existing meet_section baseline failure is unchanged-or-fixed, never worse (summary records the observed state)
  </acceptance_criteria>
  <done>English guidance reads left-to-right on-device; a widget test pins it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /coach | after cutover: derived facts ONLY (diff + criteria + word facts); the rendered-image flow no longer exists |
| model response → client | CoachOut carries words/actions only — no verdict field exists for a model to spoof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Information Disclosure | strokeImage path (GROUND-04) | mitigate | field deleted both sides; strokeImage joins FORBIDDEN_KEYS (422 on arrival); grep-guard test scans lib/ for the token — a NET PRIVACY WIN recorded in ADR-017: a rendered image of child handwriting stops leaving the device |
| T-17-16 | Elevation of Privilege | verdict spoofing by the model | mitigate | structural: verdict computed on-device before any model call; CoachOut.verdict deleted so no response field can carry a verdict; G3 guard remains server-side defense in depth |
| T-17-17 | Denial of Service | server cold/offline vs the child's flow | mitigate | verdict + star render before the network call; brain failure only clears the tutor line (catchError simplified) — F2 becomes structurally impossible for the verdict |
| T-17-18 | Tampering | stale-client 422 after strokeImage deletion | accept | only live client is the same-phase demo build; the risk window is nil; recorded in the schema.py comment + ADR-017 |
| T-17-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- `flutter test test/tutor/ test/features/` exits 0 (write_surface reconciled)
- `cd server && uv run pytest -m code -q` exits 0
- Full suite: `flutter test 2>&1 | tail -3` — failure count ≤ the pre-phase baseline minus write_surface (record the number for 17-08's reconciliation)
</verification>

<success_criteria>
- The scorer verdict applies instantly on every path; no aiJudge, no strokeImage, no image_judge, no CoachOut.verdict anywhere
- Grep-guard + FORBIDDEN_KEYS make the retirement regression-proof
- F1 fixed with a pinning widget test
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-06-SUMMARY.md` when done
</output>
