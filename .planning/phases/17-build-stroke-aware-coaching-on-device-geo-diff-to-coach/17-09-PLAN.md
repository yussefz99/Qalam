---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 09
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - test/core/scoring/calibration_fixtures/calibration_fixtures.dart
  - test/core/scoring/calibration_harness_test.dart
autonomous: true
requirements: [STRK-01]

must_haves:
  truths:
    - "The calibration harness scores the REAL scoreLetter per letter × form (baa 4 forms + taa) and prints a per-cell confusion table — the mom-facing tuning artifact"
    - "The F5 form-confusion cell is asserted ZERO: an isolated-bowl sample offered for the medial/final slot is rejected, in the harness, forever"
    - "The harness FITS suggested soft-band thresholds (tcc/tcw) from the labelled distance distributions and prints them labeled PROVISIONAL — it never silently changes production values (D-D)"
  artifacts:
    - path: "test/core/scoring/calibration_fixtures/calibration_fixtures.dart"
      provides: "per-form labelled baa + taa fixtures built from the REAL authored contextualForms references, incl. the F5 trap pairs"
      contains: "form"
    - path: "test/core/scoring/calibration_harness_test.dart"
      provides: "letter × form loop, confusion table, F5 cell == 0 assertion, threshold-fit report"
      contains: "PROVISIONAL"
  key_links:
    - from: "test/core/scoring/calibration_harness_test.dart"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "scoreLetter(sample.strokes, letter, form: sample.form) — the REAL scorer, never a re-implementation (A3 recorded decision)"
      pattern: "scoreLetter\\(.*form"
---

<objective>
Implement CONTEXT increment 5 (locked D-D, D-E): extend the Dart calibration harness (the
recorded deliberate Dart-not-Python exception) to the per-form dimension — baa's four forms
plus taa (the D-E generalization proof: same skeleton, dots differ) — with an F5
form-confusion assertion, and add a threshold-FIT report that derives suggested tcc/tcw from
the labelled distance distributions. Synthetic fixtures pin the regression contract only
(Pitfall 4): production thresholds come from the owner's-mother-labelled child set, a
deferred production gate recorded in 17-10's HUMAN-UAT.

Purpose: verdict correctness per letter × form becomes a Dart test concern (as D-A demands),
with the tuning artifact the mother will calibrate against.
Output: per-form harness + fixtures + fit report.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-form labelled fixtures (baa 4 forms + taa), incl. the F5 trap</name>
  <files>test/core/scoring/calibration_fixtures/calibration_fixtures.dart</files>
  <read_first>
    - test/core/scoring/calibration_fixtures/calibration_fixtures.dart (LabeledSample{letterId, label, strokes} at ~46-66 — the format to extend; the _expectedRejection label→MistakeId pin idiom lives in the harness)
    - assets/curriculum/letters.json (the REAL per-form references to perturb: baa isolated bowl 12pts+dot, initial head 9pts+dot, medial tooth 8pts+dot, final bowl_tail 11pts+dot; taa = same skeletons with dot1+dot2, 3 strokes; all bodies rightToLeft, dots type:'dot')
    - test/core/scoring/shape_match_test.dart (the perturbation technique: build variants FROM the authored points — shaky ±0.012 jitter, flatLine collinear — never invented shapes)
  </read_first>
  <action>
    Extend `LabeledSample` with a `final String? form;` field (null = base/isolated
    reference, preserving every existing sample unchanged). Add per-form fixture groups, each
    built by perturbing the REAL authored contextualForms points:
    - baa × {isolated, initial, medial, final}: one `good` (shaky-jitter of the form's own
      reference + correctly-placed dot below) and named-bad samples per form: `flatBody`
      (collinear body → expect tooCurved), `dotAbove` (dot above the body → dotMisplaced),
      `missingDot` (body only → wrongStrokeCount);
    - the F5 TRAP pairs: the ISOLATED bowl strokes (good isolated sample's strokes) labelled
      bad with `form: 'medial'` and again with `form: 'final'` — expected rejection: the
      shape criterion (tooCurved) — the cell the harness asserts to zero;
    - taa (D-E proof): `good` isolated (bowl + 2 dots above per the authored reference) and
      `wrongDotCount` (single dot → expect the dot/count MistakeId the authored taa data
      defines — read letters.json to name it exactly).
    Every sample documents that it is SYNTHETIC and a regression seed only (Pitfall 4);
    real child captures + the mother's labels are the production calibration set (D-D).
    Keep the existing synthetic baa seed samples byte-untouched (they pin the Phase-4
    regression contract).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter analyze test/core/scoring/calibration_fixtures/ && flutter test test/core/scoring/calibration_harness_test.dart 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - calibration_fixtures.dart compiles; LabeledSample has a `form` field defaulting to null
    - Fixture groups exist for all four baa forms + taa (grep finds `'initial'`, `'medial'`, `'final'`, `'taa'`)
    - The F5 trap samples exist: grep finds a sample comment/name matching isolated-for-medial and isolated-for-final
    - Existing samples unchanged (`git diff` shows additions only around them)
  </acceptance_criteria>
  <done>A labelled per-form sample set exists, grounded in the authored references, with the F5 trap expressible.</done>
</task>

<task type="auto">
  <name>Task 2: Letter × form harness loop, F5-cell assertion, threshold-fit report</name>
  <files>test/core/scoring/calibration_harness_test.dart</files>
  <read_first>
    - test/core/scoring/calibration_harness_test.dart (the letter loop ~130-166; _expectedRejection map ~32-37; the FN/FP _Confusion tally + tearDownAll printed table ~115-124/170-181 — the output style to keep; the inline Letter builder ~42-106)
    - lib/core/scoring/shape_match.dart (shapeDistance — the fit report computes raw distances with it)
    - lib/core/scoring/reference_resolution.dart (resolveReferenceStrokes — the fit report resolves the same per-form reference the scorer uses)
  </read_first>
  <action>
    1. Widen the harness loop to letter × form: score each sample with
       `scoreLetter(sample.strokes, letter, form: sample.form)`; extend _expectedRejection
       with the new named-bad labels (flatBody→tooCurved, dotAbove→dotMisplaced,
       missingDot→wrongStrokeCount, the taa dot-count id, F5 traps→tooCurved). Confusion
       tally gains the form dimension; the tearDownAll table prints per letter × form rows
       (FN = good rejected, FP = named-bad passed) — keep the FN-over-FP tuning-priority note
       (the Phase-4 recorded rule: a child who tried should rarely be told they failed).
    2. Hard assertions: every `good` sample passes (FN == 0 on the synthetic seed); every
       named-bad maps to its expected MistakeId; the F5 cells (isolated-for-medial,
       isolated-for-final) are ZERO passes — this moves the F5 gate from the LLM eval into
       the Dart harness, where D-A says it belongs.
    3. Threshold-fit report (NEW — no in-repo precedent, per 17-PATTERNS No-Analog): for each
       letter × form, compute `shapeDistance(sample body stroke, resolved reference body)`
       for all labelled-good vs labelled-bad samples; print suggested bands per form:
       `suggested tcc = max(good distances)`, `suggested tcw = min(bad distances)` (and flag
       any overlap where max(good) >= min(bad) as UNSEPARABLE — needs more samples). Label
       the whole report block `PROVISIONAL — synthetic seed; production values require the
       owner's-mother-labelled child captures (D-D)`. The report PRINTS ONLY — it must not
       mutate Tolerances or letters.json.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/scoring/calibration_harness_test.dart && flutter test test/core/scoring/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/calibration_harness_test.dart` exits 0
    - The harness contains an explicit F5 assertion (grep finds a zero-pass assertion over the isolated-for-medial/final samples)
    - The printed output contains a per letter × form confusion table AND a threshold-fit block containing the literal string `PROVISIONAL`
    - `git diff lib/` is EMPTY for this plan (report-only: no production values changed)
    - `flutter test test/core/scoring/` exits 0 (whole scoring suite green)
  </acceptance_criteria>
  <done>Verdict correctness per letter × form is pinned in the Dart harness with the F5 cell at zero, and the mother has a fit report to calibrate against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures → harness | synthetic perturbations of authored curriculum data only; no child captures in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | Information Disclosure | fixture data | mitigate | all samples derived from letters.json authored points; no real child strokes committed (D-D labelling happens later, off-repo, mother-controlled) |
| T-17-20 | Tampering | provisional thresholds shipping as production | mitigate | fit report prints only, never mutates Tolerances/letters.json (acceptance: git diff lib/ empty); PROVISIONAL label asserted in output |
| T-17-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- `flutter test test/core/scoring/` exits 0
- The confusion table + fit report render in test output (visually confirm once via `flutter test test/core/scoring/calibration_harness_test.dart -r expanded | tail -40`)
</verification>

<success_criteria>
- Harness runs the REAL scoreLetter per letter × form over baa (4 forms) + taa; F5 cell == 0; good→PASS, named-bad→expected MistakeId
- Threshold-fit report exists, PROVISIONAL-labelled, mutation-free
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-09-SUMMARY.md` when done
</output>
