---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 10
type: execute
wave: 8
depends_on: [17-08, 17-09]
files_modified:
  - docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md
  - .planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-HUMAN-UAT.md
autonomous: true
requirements: [GROUND-04, EVAL-03]

must_haves:
  truths:
    - "The softened GROUND-02 reversal (derived diff + criteria cross the wire, never raw strokes) AND the D-A verdict-authority un-reversal are recorded as one ADR (roadmap success criterion 4)"
    - "The live Cloud Run service runs the Phase-17 contract (criteria accepted, image path gone) — or the deploy is recorded as an explicit pending human gate"
    - "Every human gate (device F1–F6 re-walk, mother's gold-set re-sign, per-form sign-off queue, calibration labelling) is written down with resume instructions — nothing is silently dropped"
  artifacts:
    - path: "docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md"
      provides: "the GROUND-04 ADR: verdict authority + derived-diff data flow + image-path retirement + owner-confirmed D-C amendment"
      min_lines: 50
    - path: ".planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-HUMAN-UAT.md"
      provides: "the end-of-phase human-gate ledger (human_verify_mode=end-of-phase; Phase-16 precedent)"
      min_lines: 30
  key_links:
    - from: "docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md"
      to: "server/app/schema.py"
      via: "documents the criteria/word-facts wire fields + the strokeImage deletion"
      pattern: "GROUND-02"
---

<objective>
Close the phase: write ADR-017 (the 4th roadmap success criterion — the softened GROUND-02
reversal recorded, plus the D-A verdict-authority story), re-deploy the tutor server so the
live service matches the new contract, run the full verification sweep against the known
test baseline, and write the 17-HUMAN-UAT ledger for the gates only humans can close
(device re-walk, the mother's sign-offs — per the 15-07 precedent, signedOff/signed flags
flip ONLY behind a human gate).

ADR numbering: ADR-014/015 exist on disk; "ADR-016" is verbally reserved for the Phase-16
coach bake-off (STATE) — use ADR-017. Verify no ADR-016/017 file appeared since planning
(`ls docs/architecture/ | grep ADR`); if ADR-017 is taken, use the next free number and
record the shift in the summary.

Purpose: the phase's decisions are durable, the deployed reality matches the repo, and the
owner has an exact list of what remains human.
Output: ADR-017, 17-HUMAN-UAT.md, deployed rev + verification record.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-08-SUMMARY.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-09-SUMMARY.md
@docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-017 — scorer owns the verdict; only derived facts cross the wire</name>
  <files>docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md</files>
  <read_first>
    - docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md (the header structure to copy: Status/Supersedes/Affects, Context, numbered Decision)
    - .planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md (D-A…D-F + the 2026-07-05 owner-confirmed amendments — the locked decisions the ADR records)
    - docs/architecture/TUTOR-RESEARCH-FINDINGS.md (§Bottom line + F1–F11 — the evidence citations)
    - .planning/spikes/SPIKE-FINDINGS.md (H1–H5 — geo_diff beat image; grounding held; the eval-must-go-semantic precondition)
  </read_first>
  <action>
    Write ADR-017 in the ADR-015 register, one ADR covering the single architectural story
    (RESEARCH Open Q1 resolution). Status: ACCEPTED (owner, 2026-07-01 — the D-A decision
    date; amendments owner-confirmed 2026-07-05). Supersedes: the Phase-17.1
    AI-owns-pass/fail owner directive (image judge); Amends: GROUND-02 (softened — a DERIVED,
    point-free geometry diff + per-criterion result + word facts cross the wire; raw strokes
    and rendered images NEVER do). Numbered decisions to record:
    1. The deterministic on-device scorer OWNS pass/fail (D-A) — online, per-form,
       multi-criteria, soft 3-zone (cite F1/F2/F4/F9/F10: VLMs must not own fine-grained
       handwriting/dot judgment); the LLM only explains + coaches from the scorer's
       structured output (D-B); grounding holds by construction.
    2. The wire contract: strokeDiff + criteria{criterion, zone, score} + weakestCriterion +
       expectedWord/writtenWord — all derived scalars/strings, extra="forbid" both sides,
       byte-for-byte mirrors (GROUND-04). Name the guard tests
       (test/tutor/payload_nonpii_test.dart, server/tests/test_payload_nonpii.py,
       server/tests/test_criteria_contract.py, the mirror-set assertion).
    3. strokeImage + CoachOut.verdict + image_judge.py RETIRED AND DELETED (not demoted —
       record why: worst spike arm at 0.20 hallucination; an advisory image call would
       re-open the consent reversal this ADR closes). Net privacy improvement: a rendered
       image of child handwriting no longer leaves the device — the GROUND-01/02 consent
       debt from 17.1 shrinks to the derived-diff flow only.
    4. Thresholds are DATA, provisional-synthetic for the demo; production gate = the
       owner's-mother-labelled calibration; per-child adaptive bands deferred to the G8
       learner model (D-D).
    5. OWNER-CONFIRMED amendments (2026-07-05, plan-phase checkpoint — recorded in
       17-CONTEXT.md Decisions): the kinematics criterion is DESCOPED this phase (capture
       has no timestamps — never faked with point spacing; recorded follow-up: add
       PointerEvent.timeStamp capture in a later phase so kinematics can join with real
       data); position is folded into the firm dot-placement check (Open Q3); the five
       shipped criteria are shape/direction/strokeOrder/strokeCount/dot; the demo scores
       unsigned baa initial/medial/final forms (guides already did, precedent ba3923c)
       pending the mother's per-form sign-off, which remains the PRODUCTION gate (A2).
    Consent-copy note: the derived-diff flow still needs onboarding/consent copy before
    production (owner/legal) — out of this phase's scope, listed in HUMAN-UAT.
  </action>
  <verify>
    <automated>test -f /Users/mareekhalila/Documents/Qalam/qalam/docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md && grep -c "GROUND-02" /Users/mareekhalila/Documents/Qalam/qalam/docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/architecture/ADR-017-scorer-owns-verdict-derived-facts.md exists and names the verdict owner (the deterministic on-device scorer)
    - The ADR contains: `Supersedes`, `GROUND-02`, `strokeImage`, `image_judge`, `kinematics`, `signedOff` (all six greppable)
    - The ADR records the deletion-not-demotion rationale for image_judge.py and the net privacy win
    - The ADR's §5 marks the D-C amendment as OWNER-CONFIRMED 2026-07-05 (greppable: `OWNER-CONFIRMED`)
    - Header follows the ADR-015 pattern (Status line with owner + date)
  </acceptance_criteria>
  <done>Roadmap success criterion 4 is satisfied: the softened GROUND-02 reversal and the verdict-authority decision are durable, cited, and owner-attributed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-deploy, full verification sweep, and the 17-HUMAN-UAT ledger</name>
  <files>.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-HUMAN-UAT.md</files>
  <read_first>
    - server/Makefile + server/README.md if present (the documented deploy command for Cloud Run qalam-tutor — project qalam-app-bd7d0, us-central1, keyless Vertex; /health not /healthz)
    - .planning/phases/16-*/16-HUMAN-UAT.md (the ledger format precedent — item, why-human, resume signal)
    - .planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-VALIDATION.md (Manual-Only Verifications table — the three human rows to carry over)
    - docs/testing/UAT-FULL-2026-07-01.md (the F1–F6 punch-list the device re-walk covers)
  </read_first>
  <action>
    1. DEPLOY: re-deploy the tutor server with the repo's documented command (gcloud run
       deploy / make target). Both wire sides are already consistent in-repo, so a single
       deploy is safe (the only live client is the demo build — recorded in 17-07/17-08).
       Verify with `curl -s -o /dev/null -w "%{http_code}" https://qalam-tutor-ogtudswkjq-uc.a.run.app/health`
       → 200. If gcloud is unauthenticated in this environment, do NOT fake it: record the
       exact deploy command as the FIRST item in 17-HUMAN-UAT.md with status PENDING and
       continue.
    2. VERIFICATION SWEEP (record every number in the summary): `flutter test` full suite —
       reconcile against the pre-phase 9-failure baseline: write_surface was reconciled in
       17-07, so the expected remaining known failures are the 3 font-drift goldens (never
       re-bake, per memory), alif_reference, all_letters_validation, meet_section (unless
       17-08 Task 2 fixed it), curriculum_repo_v2 — document the exact final list; any NEW
       failure is a phase defect to fix before closing. `cd server && uv run pytest -m code
       -q` → green. `cd server && make eval` when Vertex ADC is available — record the gate
       result INCLUDING the two-arm STRK-01 baseline table (arm A must beat arm B on
       specificity + variety with 0 grounding violations in both); if ADC is absent, record
       the judge legs + baseline as a HUMAN-UAT item with the exact command.
    3. Write 17-HUMAN-UAT.md (Phase-16 ledger format), items:
       - Cloud Run re-deploy confirmation + warm the service before any device demo
         (min-instances=0 — note the owner's min-instances=1 demo option);
       - Device re-walk of UAT F1–F6 on the iPad/tablet with the new build (expected: F1
         LTR fixed; F2 impossible-by-construction — verdict is on-device; F3 English-primary;
         F4/F6 specific coaching incl. word path; F5 medial/final form-trap fails);
       - Mother's gold-set re-sign: review the regrown stroke-level gold_set.jsonl and flip
         `"signed": true` per case ONLY on her word (15-07 precedent; note this supersedes
         the Phase-16 pending gold sign-off scope — coordinate with the 16-05 gate);
       - Mother's per-form sign-off queue: baa initial/medial/final (+ alif forms) form-level
         signedOff flags — the demo scores them unsigned (OWNER-CONFIRMED default A2,
         2026-07-05; ADR-017 §5);
       - Calibration labelling (D-D production gate): capture real child samples, mother
         labels, re-run the harness fit report, adopt fitted tcc/tcw as letters.json
         tolerance overrides;
       - `make eval` judge legs + two-arm baseline run record (if deferred);
       - Consent copy for the derived-diff data flow (owner/legal, pre-production).
       Each item: what, why it is human, the exact command/file, and the resume signal.
    </action>
  <verify>
    <automated>test -f "/Users/mareekhalila/Documents/Qalam/qalam/.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-HUMAN-UAT.md" && grep -c "signed" "/Users/mareekhalila/Documents/Qalam/qalam/.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-HUMAN-UAT.md"</automated>
    <human-check>Owner: work the 17-HUMAN-UAT ledger — device F1–F6 re-walk on the tablet, mother's gold-set re-sign + per-form sign-offs, calibration labelling session, and (if deferred) the make-eval judge/baseline run — then run /gsd-verify-work 17</human-check>
  </verify>
  <acceptance_criteria>
    - 17-HUMAN-UAT.md exists with ≥ 6 items, each carrying an exact command/file and a resume signal
    - The deploy either verified (`curl .../health` → 200 recorded in the summary) or listed as the first PENDING ledger item with the exact command
    - The summary records: full flutter-test failure list vs baseline (no NEW failures), server `-m code` green, make-eval status (run or deferred-with-command)
    - `grep -c '"signed": true' server/tests/test_eval/gold_set.jsonl` == 0 at plan close (no autonomous sign-off — the flip is the mother's, behind the ledger)
  </acceptance_criteria>
  <done>The live service matches the repo (or the gap is an explicit human item), the suites are reconciled to a documented baseline, and every remaining human gate has a written resume path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Cloud Run deploy | the deployed contract must match both in-repo wire sides (no 422 window between deploys) |
| unsigned content → shipped gates | gold-set / signedOff flags are the mother's authority, never autonomous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | Tampering | deploy-order 422 window | mitigate | single deploy AFTER both wire sides landed in-repo (additive server-first / removal client-first ordering already satisfied by the plan graph); /health probe verifies |
| T-17-22 | Repudiation | unsigned pedagogy shipping | mitigate | acceptance grep proves zero `"signed": true` at close; sign-off flips only via the HUMAN-UAT ledger (15-07 precedent) |
| T-17-23 | Information Disclosure | consent debt | mitigate | ADR-017 records the residual derived-diff flow + the consent-copy TODO as an explicit pre-production ledger item |
| T-17-SC | Tampering | package installs | accept | zero new packages this phase (final sweep confirms pubspec.yaml/pyproject.toml dependency sections unchanged) |
</threat_model>

<verification>
- ADR-017 exists and satisfies roadmap success criterion 4
- Full `flutter test` reconciled to the documented baseline; `uv run pytest -m code -q` green; `make eval` run or explicitly deferred with command
- 17-HUMAN-UAT.md covers: deploy/warm, device F1–F6, gold re-sign, per-form sign-offs, calibration labelling, consent copy
</verification>

<success_criteria>
- All four roadmap success criteria have either automated evidence or a written human gate: STRK-01 (two-arm baseline), GROUND-04 (guards + ADR), EVAL-03 (semantic gate + sign-off gate recorded), ADR recorded
- Phase closes with zero silent gaps: everything not done autonomously is on the ledger
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-10-SUMMARY.md` when done
</output>
