---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - lib/core/scoring/scoring_models.dart
  - lib/core/scoring/tolerances.dart
  - lib/core/scoring/geometric_stroke_scorer.dart
  - test/core/scoring/geometric_stroke_scorer_test.dart
  - test/core/scoring/tolerances_test.dart
autonomous: true
requirements: [STRK-01]

must_haves:
  truths:
    - "A shaky-but-correct stroke PASSES (fuzzy zone tolerated) — the F2 false-fail fix at the per-stroke level"
    - "A flat-line stroke offered for a bowl FAILS as certainly-wrong shape (tooCurved)"
    - "Direction is still evaluated and reported as a criterion; an inverted stroke still fails"
    - "Soft-band thresholds are data (Tolerances knobs), not code constants"
  artifacts:
    - path: "lib/core/scoring/geometric_stroke_scorer.dart"
      provides: "scoreStroke driven by shapeDistance + SoftBand zones instead of the chord-curvature proxy"
      contains: "shapeDistance"
    - path: "lib/core/scoring/scoring_models.dart"
      provides: "CriterionResult{criterion, zone, score} + StrokeResult.criteria"
      contains: "class CriterionResult"
    - path: "lib/core/scoring/tolerances.dart"
      provides: "shapeTcc/shapeTcw/directionCc/directionCw as overridable data knobs"
      contains: "shapeTcc"
  key_links:
    - from: "lib/core/scoring/geometric_stroke_scorer.dart"
      to: "lib/core/scoring/shape_match.dart"
      via: "shapeDistance + SoftBand consumption"
      pattern: "SoftBand\\(tcc:"
    - from: "lib/core/scoring/geometric_stroke_scorer.dart"
      to: "lib/core/scoring/tolerances.dart"
      via: "band built from tolerances, not hardcoded"
      pattern: "tolerances\\.shapeTcc"
---

<objective>
Implement CONTEXT increment 2 (locked D-C): wire the shipped DTW `shapeDistance` +
`SoftBand` 3-zone core into `geometric_stroke_scorer.scoreStroke`, replacing the
chord-curvature proxy with a soft verdict (fail only on certainly-wrong), keeping direction
as a criterion and thresholds as data (D-D). Turns Plan 17-01 Task 1's RED contract GREEN
with zero test edits to that file.

Purpose: the per-stroke scorer stops false-failing shaky-but-correct children (UAT F2 root
cause) while still catching real shape errors — the foundation the letter-level scorer
(17-03) aggregates.
Output: upgraded scorer + models + tolerances; 17-01 Task-1 tests green.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-01-SUMMARY.md
@docs/architecture/TUTOR-RESEARCH-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CriterionResult model + soft-band Tolerances knobs</name>
  <files>lib/core/scoring/scoring_models.dart, lib/core/scoring/tolerances.dart, test/core/scoring/tolerances_test.dart</files>
  <read_first>
    - test/core/scoring/soft_verdict_scorer_test.dart (the RED contract from 17-01 — the exact API to satisfy; do not edit)
    - lib/core/scoring/scoring_models.dart (LetterResult/StrokeResult/MistakeId — extend additively, Pitfall 2: enum names and passed/mistakeId are contracts)
    - lib/core/scoring/tolerances.dart (the preset + overrides + defensive fromJson idiom at its fromJson; A5 rule: `normal` is a behavior-preserving anchor)
    - lib/core/scoring/shape_match.dart (ShapeZone enum + SoftBand.shapeDefault values the new defaults must equal)
    - test/core/scoring/tolerances_test.dart (existing knob tests to extend, not rewrite)
  </read_first>
  <action>
    In scoring_models.dart add `class CriterionResult { final String criterion; final ShapeZone
    zone; final double score; const CriterionResult({required this.criterion, required
    this.zone, required this.score}); }` (import shape_match.dart for ShapeZone) and extend
    `StrokeResult` with `final List<CriterionResult> criteria` defaulting to `const []` —
    `passed`/`mistakeId` unchanged. Field is `criterion`, never `name` (non-PII regex guard,
    pattern-map warning). Document beside MistakeId.tooCurved that under DTW it now means
    "shape certainly-wrong vs the reference" while KEEPING the enum name + the
    `strokeCurvatureExceedsThreshold` check-string pairing (Pitfall 2 — authored feedback and
    the calibration harness `_expectedRejection` key off it).

    In tolerances.dart add four double fields with defaults on ALL presets equal to:
    shapeTcc 0.10, shapeTcw 0.15 (== SoftBand.shapeDefault — assert via test), directionCc 0.3,
    directionCw -0.3. Extend fromJson overrides for `shapeTcc`/`shapeTcw` (same
    `as num? ... ?.toDouble() ?? base.x` idiom as maxCurvature). Mark each knob PROVISIONAL
    (synthetic; production values come from the mom-labelled calibration — D-D) in a doc
    comment. Keep `maxCurvature` as a field for data compatibility but add a deprecation note:
    "no longer read by scoreStroke after Plan 17-02 (chord proxy replaced by shapeDistance);
    kept so authored letters.json tolerance overrides still parse."

    Extend tolerances_test.dart: defaults assertions for the four new knobs + a fromJson
    override round-trip for shapeTcc/shapeTcw.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/scoring/tolerances_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/scoring/scoring_models.dart contains `class CriterionResult` with fields `criterion`, `zone`, `score` and NO field named `name` or containing `point`
    - lib/core/scoring/tolerances.dart contains `shapeTcc`, `shapeTcw`, `directionCc`, `directionCw` with the defaults 0.10/0.15/0.3/-0.3 and fromJson override handling for shapeTcc/shapeTcw
    - `flutter test test/core/scoring/tolerances_test.dart` exits 0
    - `grep -n "PROVISIONAL" lib/core/scoring/tolerances.dart` returns ≥ 1 match
  </acceptance_criteria>
  <done>The result model and data knobs exist exactly as the RED contract names them; tolerances tests green.</done>
</task>

<task type="auto">
  <name>Task 2: scoreStroke soft verdict via shapeDistance</name>
  <files>lib/core/scoring/geometric_stroke_scorer.dart, test/core/scoring/geometric_stroke_scorer_test.dart</files>
  <read_first>
    - test/core/scoring/soft_verdict_scorer_test.dart (RED contract — the behaviors to turn GREEN, zero edits allowed)
    - lib/core/scoring/geometric_stroke_scorer.dart (the 3 predicates: strokeLengthBelowThreshold KEEP firm; strokeDirectionInverted at lines ~104-126 — reuse its displacement math for the direction score; strokeCurvatureExceedsThreshold chord proxy ~134-157 — REPLACE)
    - lib/core/scoring/shape_match.dart (shapeDistance resamples + normalizes INTERNALLY — do not double-normalize the input you hand it; infinity for <2 points)
    - lib/core/scoring/stroke_resampler.dart (resample/normalizeToUnitBox — still used by the direction/length checks)
    - test/core/scoring/geometric_stroke_scorer_test.dart (existing expectations — some pin the chord-proxy internals and may legitimately change; behavioral names tooShort/wrongDirection stay)
  </read_first>
  <action>
    Rewrite `scoreStroke`'s shape leg (per D-C, keeping the first-failing readability):
    1. Firm pre-checks unchanged: raw-point floor and strokeLengthBelowThreshold → tooShort.
    2. Direction: compute a continuous alignment score p in [-1, 1] from the child stroke's
       net displacement projected on the reference direction axis (reuse the existing
       strokeDirectionInverted displacement math; for `topToBottom` use the y axis, for
       `rightToLeft` the negative-x axis; `tap`/dot strokes skip direction). Zone:
       p >= tolerances.directionCc → certainlyCorrect; p <= tolerances.directionCw →
       certainlyWrong (fail, MistakeId.wrongDirection); else fuzzy (PASSES). Score:
       clamp((p - directionCw) / (directionCc - directionCw), 0, 1).
    3. Shape: `final d = shapeDistance(childStroke, spec.points);` then
       `final band = SoftBand(tcc: tolerances.shapeTcc, tcw: tolerances.shapeTcw);`
       zone via band.zoneFor(d), score via band.scoreFor(d). certainlyWrong → fail with
       MistakeId.tooCurved; fuzzy and certainlyCorrect PASS. Delete the
       strokeCurvatureExceedsThreshold predicate body's use in scoreStroke (keep the function
       only if other callers exist — check; otherwise remove it and its threshold read).
    4. Always return StrokeResult with `criteria: [shape CriterionResult, direction
       CriterionResult]` populated on pass AND fail (the letter scorer aggregates them in
       17-03). Preserve the file's pure-Dart header register and the "child points live only
       in local variables" posture.

    Update geometric_stroke_scorer_test.dart ONLY where a test pinned the retired chord-proxy
    internals (e.g. exact curvature-threshold trips): re-express those cases as shape-zone
    expectations. Behavioral cases (tooShort, wrongDirection, good-stroke-passes) must survive
    with their assertions intact. Note any changed test in the summary (this is the sanctioned
    reconciliation, not silent absorption).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/scoring/soft_verdict_scorer_test.dart test/core/scoring/geometric_stroke_scorer_test.dart test/core/scoring/shape_match_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/soft_verdict_scorer_test.dart` exits 0 with ZERO edits to that file (`git diff --stat test/core/scoring/soft_verdict_scorer_test.dart` is empty)
    - lib/core/scoring/geometric_stroke_scorer.dart contains `shapeDistance(` and `tolerances.shapeTcc` and no longer calls the chord-curvature proxy from scoreStroke
    - An inverted-direction stroke still returns `MistakeId.wrongDirection` (existing test green)
    - `flutter test test/core/scoring/` — the only failing files, if any, are the 17-01 Task-2 per-form RED contract (letter_scorer_per_form_test.dart, still RED by design until 17-03)
  </acceptance_criteria>
  <done>Per-stroke scoring is soft, DTW-driven, data-thresholded; RED contract from 17-01 Task 1 is GREEN untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child stylus input → scorer | untrusted stroke geometry enters pure-Dart scoring; stays on-device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | scoreStroke | mitigate | preserve the "child points live only in local variables; nothing printed/logged/persisted" posture in the rewritten shape leg (letter_scorer.dart lines 23-25 register) |
| T-17-04 | Denial of Service | shapeDistance on degenerate input | mitigate | <2-point strokes return infinity → certainlyWrong fail, never throw; tooShort firm pre-check stays first |
| T-17-SC | Tampering | package installs | accept | zero new packages; pubspec.yaml untouched (grep gate in verification) |
</threat_model>

<verification>
- `flutter test test/core/scoring/` → only letter_scorer_per_form_test.dart may still be RED (17-03's contract)
- `git diff --stat` shows no pubspec.yaml change
- `flutter analyze lib/core/scoring/` exits 0
</verification>

<success_criteria>
- 17-01 Task-1 RED file green with zero edits; shaky-correct passes, flat-line fails, direction still a criterion, thresholds read from Tolerances
- No regression in shape_match_test.dart / tolerances_test.dart / stroke_resampler_test.dart
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-02-SUMMARY.md` when done
</output>
