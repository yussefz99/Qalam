---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 08
type: execute
wave: 7
depends_on: [17-07]
files_modified:
  - server/app/main.py
  - server/app/schema.py
  - server/app/image_judge.py
  - server/tests/test_payload_nonpii.py
  - server/tests/test_endpoint.py
  - lib/features/letter_unit/sections/meet_section.dart
  - lib/features/letter_unit/widgets/prompt_header.dart
  - lib/features/letter_unit/widgets/feedback_panel_v2.dart
  - test/features/letter_unit/meet_section_ltr_test.dart
autonomous: true
requirements: [GROUND-04]

must_haves:
  truths:
    - "The server can no longer receive an image or emit a verdict — strokeImage is deleted from the DTO, the short-circuit is gone from main.py, image_judge.py is DELETED, and strokeImage joins FORBIDDEN_KEYS (a stale payload now 422s BY DESIGN)"
    - "The off-device child-data surface is smaller than before Phase 17.1, by construction (GROUND-04 surface shrink, server half — the client stopped sending in 17-07)"
    - "English helper copy renders left-to-right (UAT F1), pinned by a widget test"
  artifacts:
    - path: "server/app/main.py"
      provides: "/coach without the image short-circuit"
    - path: "server/tests/test_payload_nonpii.py"
      provides: "strokeImage moved from accepted-fields to FORBIDDEN_KEYS (422 proves the surface shrank)"
      contains: "strokeImage"
    - path: "test/features/letter_unit/meet_section_ltr_test.dart"
      provides: "widget test: English helper Text resolves LTR under an RTL Directionality ancestor"
      contains: "TextDirection.ltr"
  key_links:
    - from: "server/app/schema.py"
      to: "lib/tutor/tutor_facts.dart"
      via: "client stopped sending strokeImage in 17-07 — deletion here closes the removal lockstep (client-first ordering)"
      pattern: "strokeImage"
---

<objective>
Implement the server half of CONTEXT increment 6 (locked D-A) plus the UAT F1 fix: with the
client already cut over (Plan 17-07 — client-first removal ordering under `extra="forbid"`),
delete the `strokeImage` → `image_judge` path from the server entirely. `image_judge.py` is
DELETED, not demoted (owner-adopted RESEARCH A5 default: the image was the worst spike arm —
0.20 hallucination — and keeping it re-opens the GROUND-02 image reversal this phase closes;
recorded in ADR-017 by 17-10). Also fixes UAT F1 (English helper copy rendering RTL).

Purpose: no image can reach the server and no verdict can leave it — the retirement is
structural on both sides; the child-facing English copy reads correctly.
Output: server retirement complete with the 422 proof, F1 fixed with a pinning widget test.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-07-SUMMARY.md
@docs/testing/UAT-FULL-2026-07-01.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server retirement — remove the short-circuit, the strokeImage field, CoachOut.verdict, and delete image_judge.py</name>
  <files>server/app/main.py, server/app/schema.py, server/app/image_judge.py, server/tests/test_payload_nonpii.py, server/tests/test_endpoint.py</files>
  <read_first>
    - server/app/main.py (the `if facts_in.strokeImage:` short-circuit ~84-112: lazy image_judge import, asyncio.to_thread wrap, CoachOut(verdict=...) return — delete the whole block; KEEP the timeout→503 degrade ladder ~114-138 and the decision log)
    - server/app/schema.py (strokeImage field ~139-146 — delete; CoachOut.verdict ~167-172 — delete: response extras are the server's to drop, and 17-07 verified the Dart parser tolerates its absence)
    - server/app/image_judge.py (to DELETE — enumerate its test files/imports first: grep image_judge across server/)
    - server/tests/test_payload_nonpii.py (strokeImage mentions to shrink)
    - server/tests/test_endpoint.py (any short-circuit/verdict tests to delete or rewrite as "image key now 422s")
  </read_first>
  <action>
    1. main.py: delete the strokeImage short-circuit block and its lazy import. The normal
       graph path below it is the shape that remains. Degrade ladder + non-PII logging kept.
    2. schema.py: delete `strokeImage` from TutorFactsIn and `verdict` from CoachOut, with a
       comment: "retired by Plan 17-08 under D-A (scorer owns the verdict; ADR-017); the
       client stopped sending in 17-07 (client-first removal ordering); a strokeImage key
       from a stale client now 422s BY DESIGN — the only live client is the demo build, cut
       over in the same phase."
    3. DELETE server/app/image_judge.py and every test dedicated to it (grep `image_judge`
       across server/ — remove imports/tests; rewrite any endpoint test that posted
       strokeImage into an assertion that such a payload now 422s).
    4. test_payload_nonpii.py: remove strokeImage from the accepted-field fixtures; ADD
       `strokeImage` to the FORBIDDEN_KEYS parametrize list (it is now an unknown key — the
       422 proves the surface shrank).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest -m code -q && test ! -f app/image_judge.py && echo IMAGE-JUDGE-DELETED</automated>
  </verify>
  <acceptance_criteria>
    - server/app/image_judge.py does not exist
    - `grep -rn "strokeImage\|image_judge" server/app/` returns 0 matches
    - `grep -n "verdict" server/app/schema.py` returns 0 matches inside CoachOut
    - A test asserts a payload containing `strokeImage` now 422s
    - `cd server && uv run pytest -m code -q` exits 0
  </acceptance_criteria>
  <done>The server can no longer receive an image or emit a verdict — the off-device child-data surface is smaller than before Phase 17.1, by construction.</done>
</task>

<task type="auto">
  <name>Task 2: UAT F1 — English helper copy renders LTR</name>
  <files>lib/features/letter_unit/sections/meet_section.dart, lib/features/letter_unit/widgets/prompt_header.dart, lib/features/letter_unit/widgets/feedback_panel_v2.dart, test/features/letter_unit/meet_section_ltr_test.dart</files>
  <read_first>
    - lib/features/letter_unit/sections/meet_section.dart (the English guidance strings — UAT F1: "On its own…", "Nothing to write…" render RTL so the trailing period jumps left)
    - lib/features/letter_unit/widgets/prompt_header.dart and lib/features/letter_unit/widgets/feedback_panel_v2.dart (check for the same English-copy-under-RTL pattern — fix ONLY if present; if untouched, record that in the summary and drop them from the commit)
    - docs/testing/UAT-FULL-2026-07-01.md (F1 row — the exact symptom)
    - test/features/ (existing meet_section test — PRE-EXISTING FAILURE in the baseline; do not chase it, do not worsen it)
  </read_first>
  <action>
    Wrap the English helper/guidance Text widgets in meet_section.dart with explicit
    `textDirection: TextDirection.ltr` (on the Text) or a `Directionality(textDirection:
    TextDirection.ltr, ...)` wrapper — English copy only; Arabic glyph/letter rendering stays
    RTL untouched. Apply the same one-line fix to any hardcoded English guidance rendered
    under the app's RTL Directionality in prompt_header.dart / feedback_panel_v2.dart and
    list the touched widgets in the summary. Add a NEW widget test
    (meet_section_ltr_test.dart): pump the widget under an RTL Directionality ancestor and
    assert the English helper Text's resolved textDirection is LTR. Do NOT modify the
    pre-existing failing meet_section test beyond what compiles — its baseline status must
    not get worse (record its before/after status in the summary).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/features/letter_unit/meet_section_ltr_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - meet_section.dart contains `TextDirection.ltr` applied to English helper copy
    - test/features/letter_unit/meet_section_ltr_test.dart exists and passes
    - The pre-existing meet_section baseline failure is unchanged-or-fixed, never worse (summary records the observed state)
  </acceptance_criteria>
  <done>English guidance reads left-to-right on-device; a widget test pins it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /coach | after retirement: no image field is representable; strokeImage 422s as an unknown key |
| model response → client | CoachOut carries words/actions only — no verdict field exists for a model to spoof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Information Disclosure | strokeImage path, server half (GROUND-04) | mitigate | field + short-circuit + image_judge.py deleted; strokeImage joins FORBIDDEN_KEYS (422 on arrival) — a NET PRIVACY WIN recorded in ADR-017 (17-10): a rendered image of child handwriting can no longer reach the server |
| T-17-16 | Elevation of Privilege | verdict spoofing by the model | mitigate | CoachOut.verdict deleted so no response field can carry a verdict; G3 guard remains server-side defense in depth; client plumbing already removed (17-07) |
| T-17-18 | Tampering | stale-client 422 after strokeImage deletion | accept | only live client is the same-phase demo build, cut over first (17-07); the risk window is nil; recorded in the schema.py comment + ADR-017 |
| T-17-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- `cd server && uv run pytest -m code -q` exits 0
- `flutter test test/features/` exits 0 (F1 test green; baseline not worsened)
- Full suite: `flutter test 2>&1 | tail -3` — failure count ≤ the documented baseline (record for 17-10's reconciliation)
</verification>

<success_criteria>
- No strokeImage, no image_judge, no CoachOut.verdict anywhere in server/; FORBIDDEN_KEYS proves the shrink
- F1 fixed with a pinning widget test
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-08-SUMMARY.md` when done
</output>
