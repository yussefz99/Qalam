---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/faithfulness.py
  - server/tests/test_eval/run_eval.py
  - server/tests/test_eval/run_judge.py
  - server/tests/test_eval/JUDGE_RUBRIC.md
  - server/tests/test_eval/gold_set.jsonl
  - server/tests/test_eval/test_eval_harness.py
  - server/tests/test_eval/run_baseline.py
  - server/tests/test_eval/test_variety.py
  - server/Makefile
autonomous: true
requirements: [EVAL-03, STRK-01]

must_haves:
  truths:
    - "The eval gate no longer fails correct paraphrases: the expected-fix SUBSTRING rule is retired as the gate for live coach lines (semantic judge leg replaces it)"
    - "Invented geometry is caught: a no-false-geometry judge leg exists with the adv_broken_but_pass trap case"
    - "The praise-lexicon zero-tolerance floor (D1) still gates every PR — test_faithfulness.py and test_eval_harness.py stay green throughout"
    - "STRK-01 is measurable: a two-arm (with/without stroke facts) baseline runner scores specificity + variety, with grounding required at 0 violations in BOTH arms"
    - "The gold set is regrown for stroke-level coaching, every new case signed:false awaiting the mother's re-sign"
  artifacts:
    - path: "server/tests/test_eval/run_eval.py"
      provides: "DIMENSIONS extended: semantic_faithfulness, no_false_geometry, specificity, variety; names_fix demoted to advisory"
      contains: "no_false_geometry"
    - path: "server/tests/test_eval/run_judge.py"
      provides: "judge legs for the new dimensions; case rendering includes strokeDiff/criteria facts"
      contains: "semantic_faithfulness"
    - path: "server/tests/test_eval/run_baseline.py"
      provides: "two-arm STRK-01 baseline (geo-facts arm vs label-only arm)"
      min_lines: 60
    - path: "server/tests/test_eval/test_variety.py"
      provides: "model-free duplicate/verbatim-exemplar detector, gates under -m code"
      contains: "pytest.mark.code"
    - path: "server/tests/test_eval/gold_set.jsonl"
      provides: "regrown stroke-level cases incl. adv_broken_but_pass trap, all signed:false"
      contains: "adv_broken_but_pass"
  key_links:
    - from: "server/tests/test_eval/run_eval.py"
      to: "server/app/faithfulness.py"
      via: "D1 gate restricted to the praise-lexicon floor for live lines"
      pattern: "faithfulness"
    - from: "server/tests/test_eval/run_judge.py"
      to: "server/tests/test_eval/JUDGE_RUBRIC.md"
      via: "rubric sections per judge dimension"
      pattern: "JUDGE_DIMENSIONS"
    - from: "server/Makefile"
      to: "server/tests/test_eval/run_baseline.py"
      via: "make eval invokes the two-arm baseline"
      pattern: "run_baseline"
---

<objective>
Implement EVAL-03 and the STRK-01 measurement instrument BEFORE the coaching-contract change
lands (Pitfall 5 ordering: shipping varied coaching while the substring gate still gates
produces false regression alarms). Per the spike verdict (SPIKE-FINDINGS: substring gate
false-flagged 0.55–0.73 of correct paraphrases with ZERO real contradictions), the gate goes
semantic; the praise-lexicon stays the zero-tolerance model-free floor; a no-false-geometry
check catches invented geometry (the canonical adv_broken_but_pass trap); and a two-arm
baseline measures STRK-01's "measurably beats label-only" on specificity + variety.

EXECUTION KICKOFF NOTE (branch discipline): this is a wave-1 plan — ensure the phase-17
branch exists (created from the phase-16 branch tip) before committing; see 17-01's note.

Purpose: the regression gate becomes trustworthy for varied, stroke-aware coaching (D-B/D-F
compatible), and STRK-01 gets its yardstick.
Output: upgraded eval harness, regrown gold set (signed:false), two-arm baseline runner.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-RESEARCH.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/spikes/SPIKE-FINDINGS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic faithfulness + no-false-geometry judge legs; retire the substring gate for live lines</name>
  <files>server/app/faithfulness.py, server/tests/test_eval/run_eval.py, server/tests/test_eval/run_judge.py, server/tests/test_eval/JUDGE_RUBRIC.md, server/tests/test_eval/test_eval_harness.py</files>
  <read_first>
    - server/tests/test_eval/run_eval.py (DIMENSIONS registry lines ~41-49: faithfulness/names_fix/register/correct_arabic; _score_judge_dimension skipped-when-offline pattern ~101-124; gate_passes composition ~149-160 — D1 zero-tolerance floor stays)
    - server/tests/test_eval/run_judge.py (lazy keyless ChatVertexAI ~47-61 with thinking_budget=0; JUDGE_DIMENSIONS ~44; _build_prompt/_parse_score/judge_dimension ~80-113 — note _build_prompt currently renders only verdict + coaching)
    - server/app/faithfulness.py (_PRAISE lexicon ~39-46/60-64 KEEP as the floor; the expected-fix substring rule ~65-66 — the thing being retired AS THE GATE for live lines; evaluate_faithfulness stays intact for pinned fixtures)
    - server/tests/test_eval/test_eval_harness.py (the 16-01/16-03 registry-coverage + D1==100% assertions that must stay green)
    - server/tests/test_faithfulness.py (fixture-pinned tests that MUST stay green — EVAL-03 "floor intact")
    - server/tests/test_eval/JUDGE_RUBRIC.md (existing rubric register to extend)
  </read_first>
  <action>
    1. run_eval.py: extend DIMENSIONS with `semantic_faithfulness`, `no_false_geometry`,
       `specificity`, `variety`. Judge side (skipped-when-offline pattern, JUDGE_THRESHOLD
       0.7): semantic_faithfulness, no_false_geometry, specificity. Model-free side: variety
       (Task 2 provides the detector). Demote `names_fix` (D2) to ADVISORY: it is still
       computed and reported, but `gate_passes` no longer fails on it (add an inline comment
       citing the spike measurement: 0.55–0.73 false-flag rate, 0 real contradictions).
       `gate_passes` keeps `faithfulness` (D1 praise-lexicon) zero-tolerance and adds: ran
       judge legs must meet threshold; skipped judge legs never fail the gate; variety (model-
       free) must meet its threshold.
    2. faithfulness.py: add a praise-only scoring entry point (e.g.
       `evaluate_praise_floor(cases)` reusing _PRAISE + the Arabic أحسنت detection, WITHOUT
       the expected-fix substring component) and route run_eval's live-line D1 leg through it.
       `evaluate_faithfulness` itself is UNCHANGED so test_faithfulness.py and the 16-03
       D1==100% fixture assertions stay green (they run over pinned fixture strings where the
       substring rule is valid).
    3. run_judge.py: add the three judge dimensions to JUDGE_DIMENSIONS; extend _build_prompt
       to render, GENERICALLY, any `strokeDiff` and `criteria` keys present on a case's facts
       (so criteria-bearing cases from Plan 17-05 need no further harness change). Rubrics in
       JUDGE_RUBRIC.md:
       - semantic_faithfulness: "does the line's MEANING contradict the frozen verdict, or
         fail to address the failed criterion? Paraphrases of the fix are FAITHFUL." 
       - no_false_geometry: "is EVERY geometric claim in the line supported by the
         strokeDiff/criteria facts shown? Naming a feature not in the facts scores 0."
       - specificity: "does the line name a localized geometric fact about THIS attempt
         (side/place/feature), not a generic instruction?"
       Judge stays gemini-2.5-flash (judge ≠ coach), thinking_budget=0.
    4. test_eval_harness.py: extend the registry-coverage assertion over the new DIMENSIONS;
       add model-free tests that (a) gate_passes ignores a skipped judge leg, (b) gate_passes
       fails when variety is below threshold, (c) names_fix no longer appears in the gating
       path. Write these tests FIRST (tdd=true), watch them fail, then implement.
  </action>
  <behavior>
    - Test 1: DIMENSIONS contains the 4 new names; registry-coverage test enumerates them
    - Test 2: gate_passes({faithfulness rate 1.0, variety below threshold, judge legs skipped}) == False; same input with variety passing == True
    - Test 3: a case set where names_fix scores 0 but all gating legs pass → gate_passes == True (advisory demotion proven)
    - Test 4: evaluate_praise_floor flags praise-on-fail (incl. أحسنت) but does NOT flag a paraphrased fix line
  </behavior>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest -m code -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest -m code -q` exits 0 (incl. test_faithfulness.py and test_eval_harness.py — the floor stays green)
    - run_eval.py DIMENSIONS contains `semantic_faithfulness`, `no_false_geometry`, `specificity`, `variety`
    - `grep -n "names_fix" server/tests/test_eval/run_eval.py` shows it reported but a comment marks it advisory; a test asserts it cannot fail the gate
    - JUDGE_RUBRIC.md contains a section for each of the three judge dimensions
    - faithfulness.py contains `evaluate_praise_floor` and `evaluate_faithfulness` is byte-unchanged in its scoring logic (git diff shows additions only around it)
  </acceptance_criteria>
  <done>The gate is semantic-ready: paraphrases can pass, invented geometry can fail, the model-free floor still bites, all under the existing two-leg discipline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model-free variety detector, two-arm STRK-01 baseline, gold-set regrow</name>
  <files>server/tests/test_eval/test_variety.py, server/tests/test_eval/run_baseline.py, server/tests/test_eval/gold_set.jsonl, server/Makefile</files>
  <read_first>
    - server/tests/test_eval/gold_set.jsonl (the # comment-header + "signed": false / "drafted_by": "claude" convention; loaders skip # lines per run_eval.py ~52-61; keep ALL 10 existing cases — they gate the label-only paths)
    - server/app/prompts.py (the GOLD EXEMPLARS block — the verbatim strings the detector must flag when echoed)
    - .planning/spikes/_lib/fixtures.py and .planning/spikes/_lib/scoring.py (the spike's two-arm design + perturbation fixtures — reusable DESIGN, port what you need, do not import from .planning at runtime)
    - server/app/schema.py (StrokeDiffIn field vocabulary — gold-case strokeDiff facts must use exactly these keys: summary, bowlDepthRatio, bowlDepthVerdict, bowlSymmetry, sizeVerdict, directionChild, directionReference, tailPresent, dotPresent, dotHorizontal, dotVertical, dotPlacementOk)
    - server/Makefile (eval / eval-code / eval-judge targets to extend)
  </read_first>
  <action>
    1. test_variety.py (pytestmark = pytest.mark.code): a pure function
       `variety_report(lines, exemplars)` in run_eval.py (or a small module it imports)
       returning {distinct_ratio, verbatim_exemplar_hits, duplicate_pairs}. Flags: any line
       equal (case/whitespace-folded) to a GOLD EXEMPLAR string; any two identical lines for
       DISTINCT attempts. Tests: 5 distinct lines → ratio 1.0, 0 hits; the same line 5× → ratio
       0.2; an exemplar echoed verbatim → hits ≥ 1. Wire `variety` scoring in run_eval to this
       function.
    2. run_baseline.py — the STRK-01 two-arm instrument (runs under `make eval`, needs ADC;
       never under -m code): for each fixture case, invoke the coach graph twice — arm A with
       the case's strokeDiff/criteria facts, arm B with those keys stripped (label-only). Score
       both arms: specificity (judge leg), variety (model-free), grounding (praise-floor +
       advance-on-fail check — MUST be 0 violations in BOTH arms). Emit a comparison table and
       exit non-zero if arm A does not beat arm B on specificity AND variety, or if grounding
       > 0 anywhere. Fixture set: reuse the regrown gold cases (below) as inputs.
    3. gold_set.jsonl regrow (KEEP the 10 existing cases; APPEND stroke-level cases, every one
       `"signed": false, "drafted_by": "claude"`): shallow-bowl, right-side-flat, dot-left,
       dot-above (identity), tail-present, too-big — each carrying a realistic strokeDiff
       using ONLY StrokeDiffIn keys; 2 paraphrase-FAITHFUL cases (line paraphrases the fix,
       expected semantic_faithfulness PASS); the `adv_broken_but_pass` trap (scorer PASSED a
       flat bowl; any line asserting "a deep, smooth bowl" must fail no_false_geometry); an F3
       full-Arabic trap. Update the # header: provenance "Phase 17 regrow for stroke-level
       coaching — supersedes the Phase-16 pending sign-off scope; owner's mother re-signs the
       WHOLE set (EVAL-03)".
    4. Makefile: `eval` target additionally runs `uv run python tests/test_eval/run_baseline.py`
       (after run_eval/run_judge), keeping eval-code model-free.
  </action>
  <behavior>
    - Test 1: variety_report catches a verbatim exemplar echo (hits ≥ 1)
    - Test 2: variety_report distinct 5/5 → 1.0; 1/5 → 0.2
    - Test 3: gold_set.jsonl loader still parses every line (extend the existing loader test or add one): every non-# line has signed==false or the original signed field, and every strokeDiff key ∈ StrokeDiffIn fields
  </behavior>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest -m code -q && uv run python -c "import json,sys; [json.loads(l) for l in open('tests/test_eval/gold_set.jsonl') if l.strip() and not l.startswith('#')]; print('GOLD-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest -m code -q` exits 0 and includes the new variety tests
    - gold_set.jsonl contains the string `adv_broken_but_pass` and ≥ 8 new cases, ALL new cases `"signed": false`
    - `grep -c '"signed": true' server/tests/test_eval/gold_set.jsonl` == 0 (nothing auto-signed — the mother's sign-off is a 17-10 human gate, 15-07 precedent)
    - run_baseline.py exists, is invoked from the Makefile `eval` target, and hard-requires 0 grounding violations in both arms (grep shows the zero-tolerance check)
    - `grep -v '^#' server/tests/test_eval/gold_set.jsonl | grep -c 'points\|"x"\|"y"' ` == 0 (no coordinates in gold facts)
  </acceptance_criteria>
  <done>STRK-01 has its two-arm yardstick; the gold set speaks stroke-level; nothing unsigned ships as signed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gold-set fixtures → eval harness | synthetic non-PII cases only; strokeDiff facts use the point-free StrokeDiffIn vocabulary |
| eval harness → Vertex judge | judge legs send fixture facts + coach lines off-process (ADC, keyless); no child data exists in fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Repudiation | gold_set.jsonl sign-off | mitigate | every new case signed:false + drafted_by claude; acceptance grep proves zero signed:true; flip only behind the 17-10 human gate (15-07 precedent) |
| T-17-09 | Information Disclosure | gold-case facts | mitigate | strokeDiff facts restricted to StrokeDiffIn scalar keys; acceptance grep proves no points/x/y in the file |
| T-17-10 | Tampering | eval-gate bypass (substring retirement weakening the gate) | mitigate | D1 praise-lexicon stays zero-tolerance; no_false_geometry adds a NEW failure mode the old gate lacked; test asserts names_fix demotion cannot pass a praise-on-fail |
| T-17-SC | Tampering | package installs | accept | zero new packages (uv/pyproject untouched) |
</threat_model>

<verification>
- `cd server && uv run pytest -m code -q` exits 0 (floor intact + new model-free legs)
- `cd server && make eval` structure verified: judge legs + baseline invoked (full run deferred to 17-10 if ADC absent this session — record which)
- test_faithfulness.py and test_eval_harness.py green throughout (EVAL-03 floor-intact requirement)
</verification>

<success_criteria>
- Semantic faithfulness + no-false-geometry + specificity judge legs and the model-free variety leg exist under the two-leg discipline
- Substring names_fix no longer gates live lines; praise-lexicon floor still zero-tolerance
- Gold set regrown for stroke-level coaching, 100% signed:false on new cases
- Two-arm STRK-01 baseline runner wired into make eval
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-04-SUMMARY.md` when done
</output>
