---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - lib/core/scoring/reference_resolution.dart
  - lib/core/scoring/letter_scorer.dart
  - lib/core/scoring/scoring_models.dart
  - lib/core/exercise_engine/exercise_validator.dart
  - lib/features/letter_unit/widgets/write_surface.dart
  - test/core/scoring/letter_scorer_test.dart
autonomous: true
requirements: [STRK-01]

must_haves:
  truths:
    - "scoreLetter judges against the reference for the ASKED positional form — an isolated-bowl shape submitted for the medial/final slot FAILS (UAT F5 fixed at the scorer, per D-A)"
    - "scoreLetter emits a structured per-criterion result (5 criteria + explicit dot) with the weakest criterion identified — the coaching input D-B requires"
    - "COUNT, ORDER, and the dot side/count check stay FIRM; only geometry (shape/direction) is soft"
    - "Canvas completion, strokeDiff, and the scorer all consume ONE shared reference resolution"
  artifacts:
    - path: "lib/core/scoring/reference_resolution.dart"
      provides: "resolveReferenceStrokes(Letter, String? form) — the single per-form resolver (Pitfall 7)"
      exports: ["resolveReferenceStrokes"]
    - path: "lib/core/scoring/letter_scorer.dart"
      provides: "per-form, multi-criteria scoreLetter returning LetterScore"
      contains: "LetterScore"
    - path: "lib/core/exercise_engine/exercise_validator.dart"
      provides: "_validateGlyph passes the asked form into scoreLetter"
      contains: "form:"
  key_links:
    - from: "lib/core/scoring/letter_scorer.dart"
      to: "lib/core/scoring/reference_resolution.dart"
      via: "reference selection"
      pattern: "resolveReferenceStrokes"
    - from: "lib/features/letter_unit/widgets/write_surface.dart"
      to: "lib/core/scoring/reference_resolution.dart"
      via: "_formStrokes getter delegates to the shared resolver"
      pattern: "resolveReferenceStrokes"
    - from: "lib/core/exercise_engine/exercise_validator.dart"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "scoreLetter(strokes, letter, form: ...)"
      pattern: "scoreLetter\\(.*form"
---

<objective>
Implement CONTEXT increment 3 (locked D-C, D-E): `letter_scorer.scoreLetter` consumes the
PER-FORM reference (`contextualForms[form]`), scores the five criteria + explicit dot check,
and emits a structured `LetterScore` (per-criterion zone + score + the weakest one). COUNT/
ORDER stay firm; the advisory ML-Kit gate stays. The validator threads the asked form in —
fixing UAT F5 form-blindness at the scorer, where D-A says the verdict lives. Turns Plan
17-01 Task 2's RED contract GREEN with zero edits to it.

Purpose: the verdict becomes form-aware and the scorer's output becomes the structured
coaching input D-B requires.
Output: LetterScore, per-form scoring, shared reference resolver, validator wired.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LetterScore + per-form multi-criteria scoreLetter + shared resolver</name>
  <files>lib/core/scoring/reference_resolution.dart, lib/core/scoring/letter_scorer.dart, lib/core/scoring/scoring_models.dart</files>
  <read_first>
    - test/core/scoring/letter_scorer_per_form_test.dart (the RED contract to satisfy — zero edits allowed)
    - lib/core/scoring/letter_scorer.dart (the 5-section first-failing skeleton to EXTEND not replace: count ~71-76, order + _classifyChildDots ~87-93/136-144, shape loop ~95-104, _checkDots ~170-209, _identityGate ~249-265; the tolerance resolution at ~60-69)
    - lib/models/letter.dart (Form model at ~103-134: referenceStrokes, commonMistakes, tolerances? — the per-form layers)
    - lib/features/letter_unit/widgets/write_surface.dart (~157-170 _formStrokes — the resolution logic to HOIST verbatim into the resolver)
    - lib/core/scoring/scoring_models.dart (post-17-02 state: CriterionResult exists; add LetterScore here)
  </read_first>
  <action>
    1. New file lib/core/scoring/reference_resolution.dart (pure Dart, scorer header register):
       `List<StrokeSpec> resolveReferenceStrokes(Letter letter, String? form)` — returns
       `letter.contextualForms?[form]?.referenceStrokes` when form != null and that list is
       non-empty, else `letter.referenceStrokes`. Also
       `Tolerances resolveTolerances(Letter letter, String? form, Tolerances? override)` with
       the order: override → contextualForms[form]?.tolerances → letter.tolerances →
       Tolerances.normal (RESEARCH Pattern 2).
    2. scoring_models.dart: `class LetterScore extends LetterResult` adding
       `final List<CriterionResult> criteria;` and `final CriterionResult? weakest;` with
       const constructor forwarding passed/mistakeId (keeps every existing caller — including
       practice_screen.dart's explicit `LetterResult` annotation — source-compatible; Pitfall 2:
       passed/mistakeId semantics unchanged, criteria additive).
    3. letter_scorer.dart: signature becomes `Future<LetterScore> scoreLetter(childStrokes,
       Letter letter, {String? form, HandwritingRecognizer? recognizer, Tolerances?
       tolerances})`. Reference list = resolveReferenceStrokes(letter, form) (sorted by order
       as today); tolerances = resolveTolerances(letter, form, tolerances). Accumulate five
       CriterionResults — the five-criteria set (shape / direction / strokeOrder /
       strokeCount / dot) is the OWNER-CONFIRMED D-C amendment of 2026-07-05 (kinematics
       descoped pending PointerEvent.timeStamp capture; position folded into the firm dot
       check; see 17-CONTEXT.md Decisions):
       - strokeCount (FIRM): mismatch → certainlyWrong/0.0 + MistakeId.wrongStrokeCount; else certainlyCorrect/1.0;
       - strokeOrder (FIRM, spatial _classifyChildDots unchanged): wrong → certainlyWrong/0.0 + wrongStrokeOrder;
       - shape (SOFT): aggregate the per-stroke shape CriterionResults from scoreStroke — worst zone wins, score = min; certainlyWrong → tooCurved;
       - direction (SOFT): same aggregation over the per-stroke direction results; certainlyWrong → wrongDirection;
       - dot (FIRM, identity-bearing — Pitfall 3): _checkDots unchanged (combined-bbox y-centroid side + count); wrong → certainlyWrong/0.0 + dotMisplaced; else certainlyCorrect/1.0.
       passed = false iff any FIRM criterion fails OR any soft criterion is certainlyWrong;
       mistakeId = the first failing criterion's id in the existing section order (verdict
       parity with today on all currently-failing cases). weakest = min-score criterion.
       Advisory _identityGate kept verbatim (reject-only, confidence floor 0.5). IMPORTANT:
       do not early-return before criteria are accumulated for sections that ran — on a firm
       count fail it is acceptable to emit only the strokeCount criterion plus
       certainlyWrong placeholders; the RED contract defines the exact expectation.
       Preserve the security comment: child points live only in locals, nothing logged.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/scoring/letter_scorer_per_form_test.dart test/core/scoring/letter_scorer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/letter_scorer_per_form_test.dart` exits 0 with zero edits to that file
    - lib/core/scoring/letter_scorer.dart signature contains `String? form` and returns `Future<LetterScore>`
    - lib/core/scoring/reference_resolution.dart exports resolveReferenceStrokes and resolveTolerances
    - Existing letter_scorer_test.dart passes (dot-above still dotMisplaced; dot-first still wrongStrokeOrder; good baa passes) — update it ONLY if an assertion pinned the removed chord proxy, and record any change in the summary
    - `flutter test test/core/scoring/` exits 0 (whole scoring suite green — no remaining RED)
  </acceptance_criteria>
  <done>scoreLetter is form-aware and emits the structured per-criterion result; the F5 trap fails at the scorer; firm checks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Validator threads the asked form; surface delegates to the shared resolver</name>
  <files>lib/core/exercise_engine/exercise_validator.dart, lib/features/letter_unit/widgets/write_surface.dart</files>
  <read_first>
    - lib/core/exercise_engine/exercise_validator.dart (_validateGlyph ~97-118 — the single scoreLetter call site to widen at line ~114; the second scoreLetter call at ~162 — inspect its path and widen identically; _checkPositionalForm ~212-218 shows where the exercise's form expectation lives: exercise.expected?.glyph?.form; _mapMistake ~256-275 — do NOT bypass)
    - lib/core/exercise_engine/ (the ExerciseSpec/Surface model files — confirm the exact accessor for the exercise's surface guideForm)
    - lib/features/letter_unit/widgets/write_surface.dart (~157-170 _referenceStrokes/_formStrokes; ~225-232 computeStrokeDiff best-effort seam — both must consume the SAME resolved list, Pitfall 7)
  </read_first>
  <action>
    1. exercise_validator.dart: in _validateGlyph derive the asked form as
       `exercise.expected?.glyph?.form ?? <the exercise surface's guideForm accessor>` (read
       the actual model to name it; write mode has no painted guide but still has an asked
       form — the VALIDATOR owns form resolution for scoring, RESEARCH Pattern 2). Pass it:
       `scoreLetter(strokes, letter, form: form)`. Apply the same widening to the second
       scoreLetter call site (~line 162) if it scores a glyph with a form expectation.
       CheckResult mapping unchanged in this plan — result.passed/mistakeId flow exactly as
       today (_mapMistake rows untouched; criteria transport to the coach is Plans 17-05/17-06).
    2. write_surface.dart: replace the `_formStrokes` getter body with a delegation to
       `resolveReferenceStrokes(widget.letter, widget.surface.guideForm)` so canvas completion
       count, computeStrokeDiff, and the scorer share one resolution function (Pitfall 7 —
       taa medial completing at 3 strokes must agree with the scorer's expected count). Keep
       the `if (!_isTrace) return const <StrokeSpec>[]` trace guard and the try/catch
       best-effort diff seam exactly as they are.
    Do NOT touch the strokeImage/onStrokeImage seams here — that is Plan 17-07's cutover.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/ && flutter analyze lib/core/exercise_engine/ lib/features/letter_unit/widgets/write_surface.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/exercise_engine/exercise_validator.dart contains `scoreLetter(strokes, letter, form:` (glyph call site widened)
    - lib/features/letter_unit/widgets/write_surface.dart imports reference_resolution.dart and `_formStrokes` no longer duplicates the contextualForms lookup
    - `flutter test test/core/` exits 0; `flutter analyze` on the touched files exits 0
    - `git diff lib/features/letter_unit/widgets/write_surface.dart` contains NO changes to onStrokeImage/_renderStrokesToBase64Png (cutover is 17-07's)
    - The pre-existing write_surface test failure is NOT worsened: `flutter test test/features 2>&1 | tail -5` shows the same known baseline failures as before this plan (record the observed count in the summary)
  </acceptance_criteria>
  <done>The asked positional form reaches the scorer on every glyph validation; one resolver serves canvas, diff, and scorer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child stylus input → validator → scorer | untrusted strokes; scored on-device, never serialized |
| curriculum data → scorer | authored letters.json contextualForms drive the reference — load-time validated upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Elevation of Privilege | form-blind verdict (F5) | mitigate | per-form reference reaches the scorer; the F5 trap (isolated shape for a medial/final slot) fails deterministically — enforced by 17-01's RED test + 17-09's harness cell == 0 |
| T-17-06 | Information Disclosure | LetterScore criteria | mitigate | criteria hold only {criterion, zone, score} scalars — no coordinates representable; security comment posture preserved in letter_scorer.dart |
| T-17-07 | Tampering | demo scoring on unsigned baa forms | accept | baa initial/medial/final are form-level signedOff:false; guides already render them (precedent ba3923c); scoring them for the demo is OWNER-CONFIRMED (A2, 2026-07-05 — 17-CONTEXT.md Decisions); the mother's sign-off remains the PRODUCTION gate, queued in 17-10's HUMAN-UAT; signedOff flags are NOT flipped by this plan |
| T-17-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- `flutter test test/core/scoring/` exits 0 (both 17-01 RED files now green, existing suite green)
- `flutter test test/core/` exits 0
- Full-suite check: `flutter test` failure set is the KNOWN 9-item baseline (do not chase; do not worsen)
</verification>

<success_criteria>
- Per-form, multi-criteria, structured-result scoring live end-to-end from validator to scorer
- F5 trap fails at the scorer; firm checks firm; ML-Kit advisory unchanged
- One shared reference resolver consumed by scorer, validator path, canvas count, and diff
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-03-SUMMARY.md` when done
</output>
