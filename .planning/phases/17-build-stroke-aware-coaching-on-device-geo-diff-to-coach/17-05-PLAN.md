---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 05
type: execute
wave: 4
depends_on: [17-03, 17-04]
files_modified:
  - server/app/schema.py
  - server/app/prompts.py
  - server/app/nodes/coach.py
  - server/app/main.py
  - server/tests/test_criteria_contract.py
  - server/tests/test_payload_nonpii.py
  - server/tests/test_grounding.py
  - lib/core/exercise_engine/check_result.dart
  - lib/core/exercise_engine/exercise_validator.dart
  - lib/tutor/tutor_facts.dart
  - lib/tutor/tutor_facts_builder.dart
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - test/tutor/payload_nonpii_test.dart
  - test/tutor/remote_agent_brain_test.dart
  - test/tutor/tutor_facts_builder_test.dart
autonomous: true
requirements: [STRK-01, GROUND-04]

must_haves:
  truths:
    - "The scorer's per-criterion result (which criterion failed, its zone, by how much) reaches the coach as structured FACTS — the LLM coaches from a decided verdict, never invents one (D-B)"
    - "The word path carries derived word facts (expectedWord vs writtenWord) so word/sentence coaching can be specific (UAT F6)"
    - "Both wire sides mirror byte-for-byte; an extra/unknown key still 422s; no raw point/coordinate key is representable (GROUND-04, no 422 window: server field lands optional-with-default FIRST)"
    - "The coach addendum names the failed criterion and is English-primary (UAT F3), letter/form-parameterized — never per-letter prompt branches"
  artifacts:
    - path: "server/app/schema.py"
      provides: "CriterionIn nested DTO + criteria/weakestCriterion/expectedWord/writtenWord on TutorFactsIn, all optional/defaulted, extra=forbid"
      contains: "class CriterionIn"
    - path: "lib/tutor/tutor_facts.dart"
      provides: "criteria/weakestCriterion/expectedWord/writtenWord mirror fields, omit-when-null"
      contains: "weakestCriterion"
    - path: "server/app/prompts.py"
      provides: "criterion-aware addendum + English-primary constraint + word-facts guidance"
      contains: "criteria"
    - path: "server/tests/test_criteria_contract.py"
      provides: "accepts-legit / defaults-when-omitted / nested-forbid / PII-scan tests for the new fields"
      min_lines: 50
  key_links:
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      to: "lib/tutor/tutor_facts_builder.dart"
      via: "buildTutorFacts(result: ...) — builder derives criteria/word facts from CheckResult"
      pattern: "buildTutorFacts"
    - from: "lib/tutor/tutor_facts.dart"
      to: "server/app/schema.py"
      via: "byte-for-byte key mirror (the 422 lockstep, Pitfall 1)"
      pattern: "criteria"
    - from: "server/app/nodes/coach.py"
      to: "server/app/prompts.py"
      via: "addendum trigger extended: strokeDiff OR criteria OR writtenWord"
      pattern: "criteria"
---

<objective>
Implement CONTEXT increment 4 (locked D-B): the structured per-criterion result from
`LetterScore` becomes the coach's input — which criterion failed, its zone, by how much —
riding the existing strokeDiff transport pattern, plus derived word facts for the F6 word
path. This is a two-sided ADDITIVE wire change under `extra="forbid"`: the server DTO ships
FIRST (optional with defaults — no 422 window), the client mirror follows in the same plan
(Pitfall 1 lockstep; all three guard tests extended in the same tasks as the field changes).

Deploy-ordering note for the executor: in-repo both sides land here; the LIVE Cloud Run
service is re-deployed once at phase close (17-08) — safe because the only live client is
the dev/demo build, and the additive server change is backward-compatible with the deployed
rev either way.

Purpose: grounded-by-construction stroke-aware coaching (STRK-01) with the privacy contract
intact (GROUND-04).
Output: criteria + word facts on the wire, criterion-aware English-primary addendum, guard
tests extended on both sides.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-03-SUMMARY.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server side FIRST — criteria + word-facts DTO, criterion-aware addendum, trigger, logging</name>
  <files>server/app/schema.py, server/app/prompts.py, server/app/nodes/coach.py, server/app/main.py, server/tests/test_criteria_contract.py, server/tests/test_payload_nonpii.py, server/tests/test_grounding.py</files>
  <read_first>
    - server/app/schema.py (StrokeDiffIn — the exact nested point-free DTO template at ~47-82; the optional-field-on-TutorFactsIn pattern at ~131-137; do NOT touch strokeImage/CoachOut.verdict here — 17-06 owns removals)
    - server/tests/test_payload_nonpii.py (the four moves to copy: pytestmark code, defaults-when-omitted, per-key 422 parametrize over FORBIDDEN_KEYS, extra-forbid-pinned; the _PII_TOKEN_RE at ~59 — new key NAMES must not contain name/point/x/y/strokes/offset/coord/raw/nick)
    - server/app/prompts.py (COACH_STROKE_ADDENDUM ~57-73 — the register + anti-parroting structure to extend; system/human message discipline in the header)
    - server/app/nodes/coach.py (trigger at ~55-58: COACH_PROMPT + (ADDENDUM if facts.get("strokeDiff")); the G2/G3/G4 guard ladder ~83-99 — DO NOT TOUCH the guards)
    - server/app/main.py (the non-PII decision log ~148-162 exclude_none pattern to extend; do NOT touch the strokeImage short-circuit ~84-112 — 17-06 removes it)
  </read_first>
  <action>
    1. schema.py — add `class CriterionIn(BaseModel)` with `model_config = ConfigDict(extra="forbid")`
       and exactly three fields: `criterion: str` (allowed values documented:
       'strokeCount'|'strokeOrder'|'shape'|'direction'|'dot'), `zone: str`
       ('certainlyCorrect'|'fuzzy'|'certainlyWrong'), `score: float`. On TutorFactsIn add,
       ALL optional/defaulted (additive = backward compatible, no 422 window):
       `criteria: list[CriterionIn] = Field(default_factory=list)`,
       `weakestCriterion: str | None = None`,
       `expectedWord: str | None = None` and `writtenWord: str | None = None` (the F6 derived
       word facts: the curriculum's expected word and ML Kit's recognized transcription —
       derived non-PII text, never geometry). Doc-comments cite D-B + GROUND-04 and the Dart
       mirror path, matching the strokeDiff field's register.
    2. prompts.py — upgrade COACH_STROKE_ADDENDUM (keep its anti-parroting + grounding
       lines verbatim): when `criteria` present, "coach the FAILED criterion (any
       certainlyWrong entry) or, on a pass, the weakest one (`weakestCriterion`) — name what
       it means for THIS letter"; add the F3 constraint: "Coach in ENGLISH. At most a
       sprinkle of Arabic (أحسنت) — never a full Arabic sentence."; add word guidance: when
       `expectedWord`/`writtenWord` present and differ, "name the specific difference between
       what was written and the expected word — which letter or form differs — warmly, one
       fix." Keep it letter/form-parameterized — zero per-letter branches (G2/G5
       anti-pattern).
    3. coach.py — trigger becomes: append the addendum when
       `facts.get("strokeDiff") or facts.get("criteria") or facts.get("writtenWord")`.
       Guards (G2/G3/G4) byte-unchanged.
    4. main.py — extend the coach-decision warning log with `criteria`/`weakestCriterion`
       (exclude_none, derived-only — same non-PII posture as _sd).
    5. Tests FIRST (tdd): new server/tests/test_criteria_contract.py (pytestmark code)
       copying the four payload_nonpii moves for the new fields: minimal payload validates
       with criteria==[] and the three scalars None; a legit populated payload validates; an
       extra key INSIDE a CriterionIn entry 422s (nested forbid); the _PII_TOKEN_RE scan over
       the new field names/values passes. Extend test_payload_nonpii.py's whitelist/model
       assertions for the four new fields. Extend test_grounding.py: the addendum is appended
       when facts carry only `criteria` (no strokeDiff) — the STRK-01 transport assertion.
  </action>
  <behavior>
    - Test 1: TutorFactsIn.model_validate(minimal) → criteria == [] and weakestCriterion is None (backward-compat, no 422 window)
    - Test 2: a CriterionIn entry with a stray key (e.g. "points") raises ValidationError (nested extra=forbid)
    - Test 3: coach system prompt includes the addendum when facts have criteria but no strokeDiff
    - Test 4: no new field name matches the PII token regex
  </behavior>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest -m code -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest -m code -q` exits 0
    - server/app/schema.py contains `class CriterionIn` with extra="forbid" and TutorFactsIn gains exactly the four fields `criteria`, `weakestCriterion`, `expectedWord`, `writtenWord`, all optional/defaulted
    - POST-shaped validation: `TutorFactsIn.model_validate({"letterId":"baa","section":"traceLetter","passed":True})` succeeds (proven by a test)
    - server/app/nodes/coach.py guard ladder (G2/G3/G4) is unchanged (`git diff server/app/nodes/coach.py` touches only the trigger line/nearby)
    - server/app/prompts.py contains an explicit English-primary instruction and a word-facts coaching instruction
  </acceptance_criteria>
  <done>The server accepts and coaches from the structured per-criterion result + word facts; nothing existing 422s; grounding guards untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client mirror — CheckResult carries the derived facts; TutorFacts/builder/scaffold thread them; guards extended</name>
  <files>lib/core/exercise_engine/check_result.dart, lib/core/exercise_engine/exercise_validator.dart, lib/tutor/tutor_facts.dart, lib/tutor/tutor_facts_builder.dart, lib/features/letter_unit/widgets/exercise_scaffold.dart, test/tutor/payload_nonpii_test.dart, test/tutor/remote_agent_brain_test.dart, test/tutor/tutor_facts_builder_test.dart</files>
  <read_first>
    - lib/core/exercise_engine/check_result.dart (current CheckResult shape — extend additively)
    - lib/core/exercise_engine/exercise_validator.dart (_validateGlyph post-17-03 — where LetterScore is in hand; the word paths at ~128-160 where expectedWord/writtenWord are compared)
    - lib/tutor/tutor_facts.dart (the strokeDiff field + omit-when-null toMap pattern to copy exactly; the doc-comment register citing schema.py + Pitfall 1)
    - lib/tutor/tutor_facts_builder.dart (buildTutorFacts already receives `result: CheckResult` — derive the new facts FROM result, do NOT grow the signature; the signature IS the guard)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (_onResult ~215-242 — buildTutorFacts call; do NOT touch the aiJudge/strokeImage seams — 17-06 owns them)
    - test/tutor/payload_nonpii_test.dart (whitelist ∪ _strokeDiffKeys pattern ~27-75; token-guard regex ~88-91; fully-populated fixture ~114-149)
    - test/tutor/remote_agent_brain_test.dart (~185-196 exact mirror-set assertion to extend)
  </read_first>
  <action>
    1. check_result.dart — add to CheckResult (all optional, derived-only):
       `final List<Map<String, Object?>>? criteria;` (each map exactly
       {criterion, zone, score} — zone serialized as the enum NAME string),
       `final String? weakestCriterion;`, `final String? expectedWord;`,
       `final String? writtenWord;`. Doc-comment: derived non-PII only; mirrors
       TutorFactsIn (Pitfall 1).
    2. exercise_validator.dart — _validateGlyph populates criteria/weakestCriterion from the
       LetterScore (serialize CriterionResult → {criterion, zone: zone.name, score});
       the word-sequence path populates expectedWord/writtenWord on BOTH pass and fail
       (F6: the coach can praise specifically too). _mapMistake untouched.
    3. tutor_facts.dart — add the four mirror fields with the strokeDiff idiom:
       `final List<Map<String, Object?>>? criteria;` etc., emitted omit-when-null in toMap()
       as 'criteria', 'weakestCriterion', 'expectedWord', 'writtenWord' (byte-for-byte the
       schema.py names). Doc-comments cite server/app/schema.py + the 422 trap.
    4. tutor_facts_builder.dart — derive all four from `result` (result.criteria, etc.) inside
       buildTutorFacts; NO new parameters (signature stays the guard: no stroke/Offset params).
    5. exercise_scaffold.dart — no call-site change needed if the builder reads result;
       verify and leave the aiJudge block alone.
    6. Guards, in the SAME task (Pitfall 1): payload_nonpii_test.dart — add `_criteriaKeys =
       {'criterion','zone','score'}` to the whitelist union; extend the fully-populated
       fixture with a representative criteria list + weakestCriterion + word facts; confirm
       the token-guard regex still passes (keys avoid name/point). remote_agent_brain_test.dart
       — extend the exact mirror-set assertion with the four keys. tutor_facts_builder_test.dart
       — builder derives criteria/word facts from CheckResult and omits keys when null.
       Write the test extensions FIRST (tdd), watch RED, implement.
  </action>
  <behavior>
    - Test 1: buildTutorFacts(result with criteria) → facts.toMap()['criteria'] present with only {criterion, zone, score} keys per entry
    - Test 2: buildTutorFacts(result without criteria) → serialized payload byte-identical to the pre-plan shape (omit-when-null)
    - Test 3: mirror-set assertion — fully-populated sent body key set == the literal expected list including the four new keys
    - Test 4: recursive PII scan over a criteria-bearing payload finds no forbidden token
  </behavior>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/tutor/ && flutter analyze lib/tutor/ lib/core/exercise_engine/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/tutor/` exits 0
    - lib/tutor/tutor_facts.dart toMap emits 'criteria'/'weakestCriterion'/'expectedWord'/'writtenWord' omit-when-null; key strings byte-match schema.py field names
    - lib/tutor/tutor_facts_builder.dart signature is UNCHANGED except no new params (git diff shows no signature growth)
    - test/tutor/payload_nonpii_test.dart whitelist includes criterion/zone/score and the recursive scan passes on the populated fixture
    - `grep -rn "'name'" lib/tutor/tutor_facts.dart` == 0 matches for the criteria entries (field is `criterion`)
    - `flutter test test/core/` exits 0 (validator change regression-free)
  </acceptance_criteria>
  <done>Both wire sides mirror; criteria + word facts flow scorer → validator → builder → payload; guards extended in lockstep; no 422 window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /coach | the coaching payload crosses the network — child-adjacent derived data; extra="forbid" both sides |
| ML Kit recognized text → coach prompt | writtenWord is recognized child handwriting entering a HumanMessage as DATA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Information Disclosure | criteria/word wire fields (GROUND-04) | mitigate | CriterionIn holds only {criterion, zone, score} scalars; nested extra="forbid" 422s a leaked coordinate key; both non-PII guard tests + the mirror-set test extended in the SAME tasks as the field change |
| T-17-12 | Tampering | prompt injection via writtenWord | mitigate | recognized text stays DATA in the HumanMessage (never concatenated into the system prompt — prompts.py discipline); the closed 4-tool lock + G3/G4 guards remain the backstop (guards byte-unchanged, asserted) |
| T-17-13 | Tampering | 422 window during deploy | mitigate | additive fields optional-with-default → old client valid against new server AND new client valid against new server; single re-deploy at 17-08; ordering comment recorded in schema.py |
| T-17-14 | Information Disclosure | server logs | mitigate | criteria logged via exclude_none derived-only pattern (main.py existing posture); no raw text of the child beyond the curriculum word |
| T-17-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- `cd server && uv run pytest -m code -q` exits 0
- `flutter test test/tutor/ test/core/` exits 0
- Full-suite failure set unchanged vs the known baseline (spot-check with `flutter test 2>&1 | tail -5`)
</verification>

<success_criteria>
- The coach receives {passed, mistakeId, criteria, weakestCriterion, strokeDiff, word facts} as frozen FACTS (D-B) and its addendum names the failed criterion in English-primary register (F3/F4)
- GROUND-04 guard surface fully extended: whitelist, mirror-set, nested-forbid, PII regex — all green on both sides
- F6: the word path's derived facts reach the coach
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-05-SUMMARY.md` when done
</output>
