---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 05
type: execute
wave: 4
depends_on: [17-03, 17-04]
files_modified:
  - server/app/schema.py
  - server/app/prompts.py
  - server/app/nodes/coach.py
  - server/app/main.py
  - server/tests/test_criteria_contract.py
  - server/tests/test_payload_nonpii.py
  - server/tests/test_grounding.py
autonomous: true
requirements: [STRK-01, GROUND-04]

must_haves:
  truths:
    - "The server accepts the structured per-criterion result + word facts as OPTIONAL, DEFAULTED fields — an old client that sends none of them still validates (additive = server ships FIRST, no 422 window)"
    - "An extra/unknown key — including one nested inside a criterion record — still 422s; no raw point/coordinate key is representable (GROUND-04)"
    - "The coach addendum names the failed criterion and is English-primary (UAT F3), letter/form-parameterized — never per-letter prompt branches; the G2/G3/G4 guards are untouched"
  artifacts:
    - path: "server/app/schema.py"
      provides: "CriterionIn nested DTO + criteria/weakestCriterion/expectedWord/writtenWord on TutorFactsIn, all optional/defaulted, extra=forbid"
      contains: "class CriterionIn"
    - path: "server/app/prompts.py"
      provides: "criterion-aware addendum + English-primary constraint + word-facts guidance"
      contains: "criteria"
    - path: "server/tests/test_criteria_contract.py"
      provides: "accepts-legit / defaults-when-omitted / nested-forbid / PII-scan tests for the new fields"
      min_lines: 50
  key_links:
    - from: "server/app/nodes/coach.py"
      to: "server/app/prompts.py"
      via: "addendum trigger extended: strokeDiff OR criteria OR writtenWord"
      pattern: "criteria"
    - from: "server/app/schema.py"
      to: "lib/tutor/tutor_facts.dart"
      via: "field names the client mirror (Plan 17-06) will copy byte-for-byte (Pitfall 1)"
      pattern: "weakestCriterion"
---

<objective>
Implement the SERVER side of CONTEXT increment 4 (locked D-B) — deliberately split from the
client mirror (Plan 17-06) so the additive server-first deploy direction is structurally
enforced by the plan graph, not just by task ordering: the structured per-criterion result
from `LetterScore` plus derived word facts for the F6 word path enter `TutorFactsIn` as
optional, defaulted fields under `extra="forbid"`. The server-side guard tests extend in the
SAME tasks as the field changes (Pitfall 1). No client file is touched by this plan.

Deploy-ordering note for the executor: this plan makes the server contract a strict superset
of what any client sends — the deployed rev and the new client (17-06) are both valid against
it. The live Cloud Run re-deploy happens once at phase close (17-10).

Purpose: grounded-by-construction stroke-aware coaching (STRK-01) with the privacy contract
intact (GROUND-04), server side first.
Output: CriterionIn DTO + four new TutorFactsIn fields, criterion-aware English-primary
addendum, extended server guards.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-03-SUMMARY.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CriterionIn DTO + criteria/word-facts fields on TutorFactsIn, with contract tests</name>
  <files>server/app/schema.py, server/tests/test_criteria_contract.py, server/tests/test_payload_nonpii.py</files>
  <read_first>
    - server/app/schema.py (StrokeDiffIn — the exact nested point-free DTO template at ~47-82; the optional-field-on-TutorFactsIn pattern at ~131-137; do NOT touch strokeImage/CoachOut.verdict here — Plan 17-08 owns removals)
    - server/tests/test_payload_nonpii.py (the four moves to copy: pytestmark code, defaults-when-omitted, per-key 422 parametrize over FORBIDDEN_KEYS, extra-forbid-pinned; the _PII_TOKEN_RE at ~59 — new key NAMES must not contain name/point/x/y/strokes/offset/coord/raw/nick)
    - test/core/scoring/letter_scorer_per_form_test.dart (the Dart CriterionResult shape being mirrored: {criterion, zone, score}; zone values certainlyCorrect|fuzzy|certainlyWrong)
  </read_first>
  <action>
    schema.py — add `class CriterionIn(BaseModel)` with `model_config = ConfigDict(extra="forbid")`
    and exactly three fields: `criterion: str` (allowed values documented:
    'strokeCount'|'strokeOrder'|'shape'|'direction'|'dot' — the OWNER-CONFIRMED D-C
    amendment set, 2026-07-05), `zone: str` ('certainlyCorrect'|'fuzzy'|'certainlyWrong'),
    `score: float`. On TutorFactsIn add, ALL optional/defaulted (additive = backward
    compatible, no 422 window):
    `criteria: list[CriterionIn] = Field(default_factory=list)`,
    `weakestCriterion: str | None = None`,
    `expectedWord: str | None = None` and `writtenWord: str | None = None` (the F6 derived
    word facts: the curriculum's expected word and ML Kit's recognized transcription —
    derived non-PII text, never geometry). Doc-comments cite D-B + GROUND-04 and the Dart
    mirror path (lib/tutor/tutor_facts.dart, Plan 17-06), matching the strokeDiff field's
    register, and record the deploy direction: "additive — server ships FIRST".

    Tests FIRST (tdd): new server/tests/test_criteria_contract.py (pytestmark code) copying
    the four payload_nonpii moves for the new fields: minimal payload validates with
    criteria==[] and the three scalars None; a legit populated payload validates; an extra
    key INSIDE a CriterionIn entry 422s (nested forbid — mirror
    test_tutorfactsin_rejects_a_leaked_key_inside_a_trajectory_entry); the _PII_TOKEN_RE
    scan over the new field names/values passes. Extend test_payload_nonpii.py's
    whitelist/model assertions for the four new fields.
  </action>
  <behavior>
    - Test 1: TutorFactsIn.model_validate(minimal) → criteria == [] and weakestCriterion is None (backward-compat, no 422 window)
    - Test 2: a CriterionIn entry with a stray key (e.g. "points") raises ValidationError (nested extra=forbid)
    - Test 3: a fully-populated payload (criteria + weakestCriterion + word facts) validates
    - Test 4: no new field name matches the PII token regex
  </behavior>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest tests/test_criteria_contract.py tests/test_payload_nonpii.py -m code -q</automated>
  </verify>
  <acceptance_criteria>
    - server/app/schema.py contains `class CriterionIn` with extra="forbid" and TutorFactsIn gains exactly the four fields `criteria`, `weakestCriterion`, `expectedWord`, `writtenWord`, all optional/defaulted
    - `TutorFactsIn.model_validate({"letterId":"baa","section":"traceLetter","passed":True})` succeeds (proven by a test)
    - A test proves a leaked key inside a CriterionIn entry raises ValidationError
    - `cd server && uv run pytest tests/test_criteria_contract.py tests/test_payload_nonpii.py -m code -q` exits 0
  </acceptance_criteria>
  <done>The server contract is a strict superset — old payloads validate, the new derived facts validate, extras (nested included) 422.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criterion-aware English-primary addendum, trigger, non-PII logging</name>
  <files>server/app/prompts.py, server/app/nodes/coach.py, server/app/main.py, server/tests/test_grounding.py</files>
  <read_first>
    - server/app/prompts.py (COACH_STROKE_ADDENDUM ~57-73 — the register + anti-parroting structure to extend; system/human message discipline in the header)
    - server/app/nodes/coach.py (trigger at ~55-58: COACH_PROMPT + (ADDENDUM if facts.get("strokeDiff")); the G2/G3/G4 guard ladder ~83-99 — DO NOT TOUCH the guards)
    - server/app/main.py (the non-PII decision log ~148-162 exclude_none pattern to extend; do NOT touch the strokeImage short-circuit ~84-112 — Plan 17-08 removes it)
    - server/tests/test_grounding.py (existing addendum-trigger tests to extend)
  </read_first>
  <action>
    1. prompts.py — upgrade COACH_STROKE_ADDENDUM (keep its anti-parroting + grounding lines
       verbatim): when `criteria` present, "coach the FAILED criterion (any certainlyWrong
       entry) or, on a pass, the weakest one (`weakestCriterion`) — name what it means for
       THIS letter"; add the F3 constraint: "Coach in ENGLISH. At most a sprinkle of Arabic
       (أحسنت) — never a full Arabic sentence."; add word guidance: when
       `expectedWord`/`writtenWord` present and differ, "name the specific difference between
       what was written and the expected word — which letter or form differs — warmly, one
       fix." Keep it letter/form-parameterized — zero per-letter branches (G2/G5
       anti-pattern).
    2. coach.py — trigger becomes: append the addendum when
       `facts.get("strokeDiff") or facts.get("criteria") or facts.get("writtenWord")`.
       Guards (G2/G3/G4) byte-unchanged.
    3. main.py — extend the coach-decision warning log with `criteria`/`weakestCriterion`
       (exclude_none, derived-only — same non-PII posture as _sd).
    4. Tests FIRST (tdd): extend test_grounding.py — the addendum is appended when facts
       carry only `criteria` (no strokeDiff), and when facts carry only `writtenWord` — the
       STRK-01 transport assertions.
  </action>
  <behavior>
    - Test 1: coach system prompt includes the addendum when facts have criteria but no strokeDiff
    - Test 2: coach system prompt includes the addendum when facts have writtenWord only
    - Test 3: the G3 advance-on-fail rewrite still fires on a criteria-bearing fail (guards regression)
  </behavior>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest -m code -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest -m code -q` exits 0 (full model-free suite)
    - server/app/nodes/coach.py guard ladder (G2/G3/G4) is unchanged (`git diff server/app/nodes/coach.py` touches only the trigger line/nearby)
    - server/app/prompts.py contains an explicit English-primary instruction and a word-facts coaching instruction
    - `grep -n "strokeImage" server/app/main.py` — the short-circuit block is untouched by this plan (removal is 17-08's)
  </acceptance_criteria>
  <done>The coach coaches from the per-criterion result + word facts in English-primary register; grounding guards untouched; server side complete and deployable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /coach | the coaching payload crosses the network — child-adjacent derived data; extra="forbid" server-side |
| ML Kit recognized text → coach prompt | writtenWord is recognized child handwriting entering a HumanMessage as DATA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Information Disclosure | criteria/word wire fields (GROUND-04) | mitigate | CriterionIn holds only {criterion, zone, score} scalars; nested extra="forbid" 422s a leaked coordinate key; server guard tests extended in the SAME task as the field change |
| T-17-12 | Tampering | prompt injection via writtenWord | mitigate | recognized text stays DATA in the HumanMessage (never concatenated into the system prompt — prompts.py discipline); the closed 4-tool lock + G3/G4 guards remain the backstop (guards byte-unchanged, asserted) |
| T-17-13 | Tampering | 422 window during deploy | mitigate | additive fields optional-with-default → every existing client payload remains valid; server ships first by plan-graph construction (17-06 depends on this plan); single re-deploy at 17-10 |
| T-17-14 | Information Disclosure | server logs | mitigate | criteria logged via exclude_none derived-only pattern (main.py existing posture); no raw text of the child beyond the curriculum word |
| T-17-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- `cd server && uv run pytest -m code -q` exits 0
- `git diff --stat` touches ONLY server/ files (no lib/ or test/ Dart files — the client mirror is 17-06)
</verification>

<success_criteria>
- TutorFactsIn accepts criteria/weakestCriterion/expectedWord/writtenWord as a strict-superset contract; nested forbid + PII scan green
- Addendum is criterion-aware, word-aware, English-primary; guards untouched
- The plan graph itself enforces server-before-client (17-06 depends_on this plan)
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-05-SUMMARY.md` when done
</output>
