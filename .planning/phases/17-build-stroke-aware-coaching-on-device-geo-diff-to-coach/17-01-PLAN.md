---
phase: 17-build-stroke-aware-coaching-on-device-geo-diff-to-coach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/core/scoring/soft_verdict_scorer_test.dart
  - test/core/scoring/letter_scorer_per_form_test.dart
autonomous: true
requirements: [STRK-01]

must_haves:
  truths:
    - "A RED, executable test exists for every increment-2 soft-verdict behavior before implementation (shaky-correct passes, flat-line fails, direction stays a criterion)"
    - "A RED, executable test exists for every increment-3 per-form behavior before implementation (per-form reference selection, LetterScore criteria structure, weakest selection, F5 form-trap)"
    - "Existing scorer tests stay green — the RED contract is additive, in new files only"
  artifacts:
    - path: "test/core/scoring/soft_verdict_scorer_test.dart"
      provides: "RED contract for scoreStroke soft verdict via shapeDistance + Tolerances soft-band knobs"
      min_lines: 60
    - path: "test/core/scoring/letter_scorer_per_form_test.dart"
      provides: "RED contract for per-form scoreLetter, LetterScore/CriterionResult, weakest criterion, F5 trap"
      min_lines: 80
  key_links:
    - from: "test/core/scoring/soft_verdict_scorer_test.dart"
      to: "lib/core/scoring/shape_match.dart"
      via: "perturbation fixtures built with the shape_match_test.dart technique against the REAL authored baa reference"
      pattern: "shakyBowl|flatLine"
    - from: "test/core/scoring/letter_scorer_per_form_test.dart"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "scoreLetter(strokes, letter, form: ...) target API"
      pattern: "form:"
---

<objective>
Author the Wave-0 RED test contract for the scorer upgrade (CONTEXT increments 2 and 3, per
locked decisions D-A and D-C): soft 3-zone per-stroke verdicts via the already-shipped DTW
`shapeDistance` core, and per-form multi-criteria letter scoring with a structured
per-criterion result. Every behavior 17-02/17-03 must implement gets an executable failing
assertion FIRST (Nyquist rule; mirrors the 15-01/16-01 precedent).

EXECUTION KICKOFF NOTE (branch discipline): Phase 16 is paused at human gates on
`gsd/phase-16-presence-voice-eval-demo`. Phase 17 work needs its OWN branch created from the
current phase-16 branch tip (it depends on commits 4e71d6b shape_match + 09d2cde strokeDiff
that live there), e.g. `gsd/phase-17-tutor-redesign-scorer`. Create it before the first
commit of this phase; never commit to the phase-16 branch or main.

Purpose: pin the scorer's new public API and behavior before any implementation, so 17-02
and 17-03 turn RED to GREEN with zero test edits.
Output: two new RED test files under test/core/scoring/.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-CONTEXT.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-RESEARCH.md
@.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-PATTERNS.md
@docs/architecture/TUTOR-RESEARCH-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED contract for the soft per-stroke verdict (increment 2)</name>
  <files>test/core/scoring/soft_verdict_scorer_test.dart</files>
  <read_first>
    - lib/core/scoring/shape_match.dart (the shipped API: shapeDistance(child, ref, {n:32}), SoftBand{tcc,tcw}, ShapeZone, SoftBand.shapeDefault = (0.10, 0.15))
    - lib/core/scoring/geometric_stroke_scorer.dart (current scoreStroke signature + the 3 hard predicates being replaced/kept)
    - lib/core/scoring/scoring_models.dart (StrokeResult{passed, mistakeId}, MistakeId enum — names are a contract)
    - lib/core/scoring/tolerances.dart (preset + overrides fromJson idiom the new knobs must follow)
    - test/core/scoring/shape_match_test.dart (the perturbation-fixture technique: shakyBowl/flatLine built from the REAL authored baa bowl points)
    - test/core/scoring/geometric_stroke_scorer_test.dart (existing conventions + fixtures; do NOT edit it in this task)
    - assets/curriculum/letters.json (baa isolated bowl reference points — source for fixtures)
  </read_first>
  <action>
    Write RED tests (new file only — never modify implementation files in this plan) pinning
    the increment-2 target API, per D-C (soft 3-zone thresholds, DTW shape, direction stays a
    criterion) and D-D (thresholds are data):

    1. `Tolerances` gains four double knobs with these PROVISIONAL defaults on
       `Tolerances.normal`: `shapeTcc == 0.10`, `shapeTcw == 0.15` (must equal
       `SoftBand.shapeDefault`), `directionCc == 0.3`, `directionCw == -0.3`. `Tolerances.fromJson`
       honors `overrides: {"shapeTcc": ..., "shapeTcw": ...}` (same defensive idiom as
       `maxCurvature`).
    2. `CriterionResult` exists in scoring_models with fields `criterion` (String), `zone`
       (ShapeZone), `score` (double). IMPORTANT: the field is `criterion`, NOT `name` — `name`
       trips the non-PII token regex in the payload guards (pattern-map warning). No field may
       contain the substring `point`.
    3. `StrokeResult` gains `criteria` (List of CriterionResult, default const []), keeping
       `passed`/`mistakeId` unchanged.
    4. Behavior over the REAL authored baa isolated bowl StrokeSpec (build fixtures with the
       shape_match_test.dart perturbation technique):
       - shakyBowl (small ±0.012 jitter of the reference) → `scoreStroke(...).passed == true`
         and its shape CriterionResult zone is certainlyCorrect or fuzzy (the F2 fix: fuzzy PASSES);
       - flatLine (12 collinear points across the bowl's x-range) → `passed == false`,
         `mistakeId == MistakeId.tooCurved` (Pitfall 2: the existing enum id + check-string
         contract is KEPT for shape failures — no new MistakeId), shape zone certainlyWrong;
       - a rightToLeft reference drawn left-to-right (reverse the point order) → `passed == false`,
         `mistakeId == MistakeId.wrongDirection`, and the result still carries a `direction`
         CriterionResult (direction remains a criterion per D-C);
       - a 1-point stroke → still fails firm (tooShort unchanged).
    5. Assert `criteria` contains entries with `criterion == 'shape'` and
       `criterion == 'direction'` on every scored stroke (pass or fail).

    Mark the file header: "Wave-0 RED contract for Plan 17-02 (increment 2) — RED by missing
    symbol until 17-02 lands. Compile errors count as RED." Pure Dart, no dart:ui (copy the
    shape_match.dart header register).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/scoring/soft_verdict_scorer_test.dart; test $? -ne 0 && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - test/core/scoring/soft_verdict_scorer_test.dart exists and `flutter test` on it exits NON-zero (RED by missing symbol/compile error)
    - The file contains the literal strings `shapeTcc`, `shapeTcw`, `directionCc`, `directionCw`, `CriterionResult`, `criteria`
    - `grep -c "criterion:" test/core/scoring/soft_verdict_scorer_test.dart` ≥ 1 and `grep -c "name:" test/core/scoring/soft_verdict_scorer_test.dart` on CriterionResult constructions == 0
    - `flutter test test/core/scoring/shape_match_test.dart test/core/scoring/geometric_stroke_scorer_test.dart` still exits 0 (existing tests untouched, no implementation files modified)
  </acceptance_criteria>
  <done>Increment-2's entire behavioral contract is executable and RED; existing scoring suite untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: RED contract for per-form multi-criteria letter scoring (increment 3)</name>
  <files>test/core/scoring/letter_scorer_per_form_test.dart</files>
  <read_first>
    - lib/core/scoring/letter_scorer.dart (current scoreLetter signature: (childStrokes, letter, {recognizer, tolerances}) → Future&lt;LetterResult&gt;; the 5-section first-failing structure; _classifyChildDots; _identityGate)
    - lib/models/letter.dart (Form{referenceStrokes, commonMistakes, tolerances?}; letter.contextualForms map keys 'isolated'|'initial'|'medial'|'final')
    - test/core/scoring/letter_scorer_test.dart (the inline baaLetter()/goodBaa() builder idiom to copy — mirror letters.json shapes; do NOT edit this file)
    - assets/curriculum/letters.json (baa contextualForms: isolated bowl 12pts+dot, initial head 9pts+dot, medial tooth 8pts+dot, final bowl_tail 11pts+dot — build the test Letter from these REAL points)
    - lib/features/letter_unit/widgets/write_surface.dart (lines ~157-170: the _formStrokes resolution idiom that becomes the shared resolver)
  </read_first>
  <action>
    Write RED tests pinning the increment-3 target API (D-C: 5 criteria + explicit dot check;
    D-E: prove on baa forms):

    1. `scoreLetter` gains an optional named `String? form` parameter and returns
       `Future&lt;LetterScore&gt;` where `LetterScore extends LetterResult` (so the existing
       `passed`/`mistakeId` reads and practice_screen's explicit `LetterResult` annotation stay
       source-compatible) adding `criteria` (List&lt;CriterionResult&gt;) and `weakest`
       (CriterionResult?, the lowest-score criterion — the coaching target, D-B).
    2. A shared resolver exists: `resolveReferenceStrokes(Letter letter, String? form)` in
       `lib/core/scoring/reference_resolution.dart` returning
       `contextualForms[form].referenceStrokes` when form non-null AND that list is non-empty,
       else `letter.referenceStrokes` (Pitfall 7: one resolution function for canvas, diff,
       and scorer).
    3. Behavior (build a test Letter whose contextualForms carry the REAL authored baa
       isolated + medial points):
       - good medial-tooth strokes with `form: 'medial'` → passed == true;
       - the F5 trap: isolated-bowl-shaped strokes submitted with `form: 'medial'` →
         passed == false with a shape-criterion certainlyWrong (the isolated bowl must NOT
         pass the medial slot — UAT F5, scored at the SCORER now, not the LLM);
       - `form: null` (or omitted) scores against `letter.referenceStrokes` — the existing
         letter_scorer_test good-baa fixture still passes through the new path;
       - `LetterScore.criteria` carries exactly the five entries `strokeCount`, `strokeOrder`,
         `shape`, `direction`, `dot` (the five-criteria set is the OWNER-CONFIRMED D-C
         amendment of 2026-07-05, recorded in 17-CONTEXT.md Decisions: kinematics is
         DESCOPED — capture has no timestamps, never fake speed from point spacing;
         position folds into the firm dot-placement check; strokeCount is the fifth
         criterion. ADR-017 (Plan 17-10) records the amendment plus the
         PointerEvent.timeStamp capture follow-up);
       - firm checks stay firm (D-C / Pitfall 3): a dot-above-body baa fixture still fails with
         `MistakeId.dotMisplaced`; a wrong-stroke-count fixture still fails with
         `MistakeId.wrongStrokeCount`; their criteria entries report zone certainlyWrong with
         score 0.0 (categorical — never fuzzy);
       - `weakest` is non-null on both pass and fail and equals the criteria entry with the
         minimum score.
    Mark header "Wave-0 RED contract for Plan 17-03 (increment 3)". No edits to any lib/ or
    existing test file.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/core/scoring/letter_scorer_per_form_test.dart; test $? -ne 0 && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - test/core/scoring/letter_scorer_per_form_test.dart exists and `flutter test` on it exits NON-zero (RED)
    - File contains literal strings `LetterScore`, `weakest`, `resolveReferenceStrokes`, `form: 'medial'`, and all five criterion names `strokeCount`, `strokeOrder`, `shape`, `direction`, `dot`
    - An F5 trap test exists: grep finds a test name matching `isolated.*medial` asserting `passed, isFalse`
    - `flutter test test/core/scoring/letter_scorer_test.dart` still exits 0 (untouched)
  </acceptance_criteria>
  <done>Increment-3's per-form + structured-result contract is executable and RED, including the F5 form-trap; nothing existing broke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → scorer | Synthetic stroke fixtures only; no child data, no PII enters test code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | new test files | mitigate | fixtures are perturbations of the authored curriculum reference (letters.json), never captured child strokes; no coordinates leave the repo |
| T-17-02 | Tampering | CriterionResult wire-bound naming | mitigate | field named `criterion` (never `name`, never `*point*`) so the future wire payload passes the non-PII token regex guards by construction |
| T-17-SC | Tampering | npm/pip/cargo/pub installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: not applicable); pubspec.yaml unchanged in this plan |
</threat_model>

<verification>
- `flutter test test/core/scoring/soft_verdict_scorer_test.dart` and `flutter test test/core/scoring/letter_scorer_per_form_test.dart` each exit non-zero (RED)
- `flutter test test/core/scoring/shape_match_test.dart test/core/scoring/geometric_stroke_scorer_test.dart test/core/scoring/letter_scorer_test.dart test/core/scoring/tolerances_test.dart` exits 0 (existing contract untouched)
- `git diff --stat` shows only the two new test files (no lib/ changes)
</verification>

<success_criteria>
- Both RED contract files exist, compile-fail or assert-fail for the RIGHT reason (missing target symbols), and name every increment-2/3 behavior from 17-VALIDATION.md Wave-0 gaps rows 1–2
- Existing test/core/scoring/ suite unchanged and green
</success_criteria>

<output>
Create `.planning/phases/17-build-stroke-aware-coaching-on-device-geo-diff-to-coach/17-01-SUMMARY.md` when done
</output>
