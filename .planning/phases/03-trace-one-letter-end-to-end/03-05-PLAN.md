---
phase: 03-trace-one-letter-end-to-end
plan: 05
type: execute
wave: 4
depends_on: ["03-00", "03-04"]
files_modified:
  - lib/l10n/app_en.arb
  - lib/screens/home_screen.dart
  - test/screens/home_screen_test.dart
autonomous: true
requirements: [S1-04]
must_haves:
  truths:
    - "The demo home renders the warmth of home.png — mascot, a static 'Welcome back' greeting (placeholder name, no profile system), and a 'Today's lesson' card showing the alif glyph + lesson title (D-17)"
    - "Tapping the 'Today's lesson' card navigates into the trace flow at the existing /practice route (lesson_01) that 03-04 owns — no second practice route is invented (D-17/D-02)"
    - "NO header running-star counter ('⭐ 39'), NO 'THIS WEEK · N stars' weekly tally, NO weekly progress bar, NO per-lesson 3-star rating on the card — gamification chrome omitted; no fabricated stars/lessons dataset (D-17/D-03/D-08/PLAT-03)"
    - "Journey and Parent side-nav entries (and any 'See journey' affordance) render visibly disabled/locked and are NOT wired to any route — no dead routes (D-17)"
    - "All copy comes from gen-l10n AppLocalizations; copy reads stay null-safe so the D-05 bare-MaterialApp direction test still renders; tokens only (no raw hex/magic numbers/inline fonts); interactive targets >= QalamTargets floors"
  artifacts:
    - path: "lib/screens/home_screen.dart"
      provides: "Upgraded de-gamified demo home: mascot + static greeting + alif 'Today's lesson' card → /practice, plus locked Journey/Parent nav"
      contains: "Welcome"
    - path: "lib/l10n/app_en.arb"
      provides: "Demo-home l10n getters (greeting, lesson card, nav labels, Coming soon / locked copy)"
      contains: "comingSoon"
    - path: "test/screens/home_screen_test.dart"
      provides: "Widget test: greeting + alif card render, card tap → /practice, anti-gamification absence, Journey/Parent locked"
      contains: "comingSoon"
  key_links:
    - from: "lib/screens/home_screen.dart"
      to: "lib/router/app_router.dart (/practice — owned by 03-04)"
      via: "context.go('/practice') from the Today's lesson card onTap"
      pattern: "context.go\\('/practice'\\)"
    - from: "lib/screens/home_screen.dart"
      to: "lib/l10n/app_localizations.dart"
      via: "null-safe AppLocalizations.of(context)?.<getter> ?? fallback"
      pattern: "AppLocalizations.of\\(context\\)"
---

<objective>
Upgrade the existing Phase-1 Walking-Skeleton `lib/screens/home_screen.dart` into a
presentable, de-gamified **demo home** faithful to the warmth of
`docs/design/kit/project/screenshots/home.png`, for the course-staff demo on 2026-06-02
(D-17). The home shows the qalam mascot, a warm static greeting ("Welcome back, <name>"
with a STATIC placeholder name — no profile system, that is Phase 5), and a "Today's
lesson" card with the **alif** glyph + lesson title that, on tap, launches the
Watch→Trace→Celebrate loop by navigating to the **existing `/practice` route owned by
03-04** (D-17/D-02 — do NOT invent a second practice route). The future **Journey** and
**Parent** side-nav entries (and any "See journey" affordance) render visibly
**disabled/locked "Coming soon"** — present but not wired to any route (Journey is Phase 6,
Parent is Phase 9 — no dead routes).

All gamification chrome from `home.png` is OMITTED (binding, D-17/D-03/D-08/PLAT-03): NO
header running-star counter ("⭐ 39"), NO "THIS WEEK · N stars" tally, NO weekly progress
bar, NO per-lesson 3-star rating on the card. No fabricated stars/lessons dataset is
created to populate omitted widgets.

Purpose: the demo must *feel* real on a tablet in RTL-aware chrome so course staff see
Home → Watch → Trace → Celebrate → Home as a coherent partial app — while staying true to
the Decided anti-gamification rules.

Output: new demo-home l10n getters in `lib/l10n/app_en.arb` (+ regenerated
`app_localizations*.dart`), the upgraded `lib/screens/home_screen.dart`, and the
`test/screens/home_screen_test.dart` widget test (positive nav + anti-gamification absence
+ locked-nav). The existing D-05 `test/direction_test.dart` stays green.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trace-one-letter-end-to-end/03-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@.planning/phases/03-trace-one-letter-end-to-end/03-PATTERNS.md
@.planning/phases/03-trace-one-letter-end-to-end/03-04-PLAN.md
@lib/screens/home_screen.dart
@lib/router/app_router.dart
@lib/theme/colors.dart
@lib/theme/dimens.dart
@lib/theme/text_styles.dart
@test/direction_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo-home l10n strings + upgrade home_screen into the warm de-gamified demo home</name>
  <files>lib/l10n/app_en.arb, lib/screens/home_screen.dart</files>
  <read_first>
    - lib/screens/home_screen.dart (the file being upgraded — Scaffold + AppBar + _QalamWordmark + _PlaceholderCard + _OpenPracticeButton + _PersistenceProof; the null-safe l10n read pattern at lines 39-43; the SvgPicture.asset usage at lines 96-100; the canonical token CTA at lines 163-200; ArabicText display island at lines 102-113 + 144)
    - lib/l10n/app_en.arb (existing getter + @-description shape, e.g. openPractice / homePlaceholderHeading)
    - lib/theme/colors.dart (QalamColors: bg parchment, surface soft-aqua, surfaceRaised, primary ink-teal, fgMuted, reward — reward is REWARDS-ONLY so it must NOT appear on this screen, border)
    - lib/theme/dimens.dart (QalamSpace scale, QalamTargets floors targetMin 64 / targetComfy 72 / targetLarge 96, QalamRadii, QalamShadows.shadowMd + buttonShadow)
    - lib/theme/text_styles.dart (QalamTextStyles display/heading/button/body/label; QalamFonts.arabicDisplay for the alif glyph; ArabicText for the RTL island)
    - lib/widgets/arabic_text.dart (the RTL island used to render the alif glyph on the lesson card)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (Copywriting Contract voice + Title-Case rule; Mandatory omissions table; spacing/touch-target floors; gold = rewards-only)
    - docs/design/kit/project/screenshots/home.png + home-compact.png (the warmth target: mascot top-right, profile chip + greeting, lesson card with glyph tile + title + subtitle; build its warmth, OMIT its ⭐39 / THIS WEEK / 3-star chrome)
    - assets/mascot/ + assets/icons/ (bundled by Plan 03-00, Wave 1: qalam-idle.svg is the home mascot, lock.svg the locked-nav glyph, star.svg the brand star; keep every SvgPicture.asset behind a graceful missing-asset fallback as defense-in-depth — do NOT crash)
  </read_first>
  <action>
    First add new getters to `lib/l10n/app_en.arb` (each with an `@`-description, matching
    the existing file shape) for every demo-home string, voice per UI-SPEC Copywriting
    Contract (warm, Title Case on labels/headings): a greeting `homeGreeting` ("Welcome back,
    Layla." — the name is a STATIC placeholder, no profile system, D-17), a greeting subtitle
    `homeGreetingSubtitle` ("Qalam has a new lesson ready for you."), a lesson eyebrow
    `homeLessonEyebrow` ("LETTERS & WRITING · LESSON 1"), the lesson title `homeLessonTitle`
    ("The Letter Alif"), the lesson subtitle `homeLessonSubtitle` ("Stroke order, tracing,
    and sounds."), the three nav labels `navHome` / `navJourney` / `navParent`, and a locked
    affordance label `comingSoon` ("Coming soon"). Use Western numerals only (SKILL.md). Run
    `flutter gen-l10n` to regenerate `app_localizations*.dart`.

    Then upgrade `lib/screens/home_screen.dart` into the demo home. Keep the existing
    `_QalamWordmark` AppBar title and KEEP the `_PersistenceProof` ProviderScope-guard pattern
    (it makes the bare-MaterialApp D-05 path safe). Replace the `_PlaceholderCard` body with a
    two-part layout faithful to home.png warmth:
    (a) a **greeting block** — the static greeting + subtitle from l10n via the existing
        null-safe read pattern (`AppLocalizations.of(context)?.homeGreeting ?? 'Welcome back,
        Layla.'` etc.) styled with `QalamTextStyles.display` (greeting) + `body` (subtitle),
        beside the qalam **mascot** rendered via
        `SvgPicture.asset('assets/mascot/qalam-idle.svg', ...)` (the idle reed-pen pose,
        bundled by Plan 03-00) wrapped so a missing/empty asset degrades to a `SizedBox`
        rather than crashing (graceful-asset fallback, defense-in-depth);
    (b) a **'Today's lesson' card** (new private `_TodaysLessonCard` widget, analog:
        `_PlaceholderCard` token card) on `QalamColors.surfaceRaised`/`surface` with
        `QalamRadii.xl` + `QalamShadows.shadowMd`, containing a glyph tile rendering the
        **alif** via `ArabicText('ا', display: true)` (Arabic display island), the lesson
        eyebrow/title/subtitle from l10n, wrapped in an `InkWell`/`Material` whose `onTap`
        calls `context.go('/practice')` (the route 03-04 owns — D-17/D-02), with a
        `BoxConstraints(minHeight: QalamTargets.targetLarge)` so the whole card is a hero
        touch target.
    Replace the standalone `_OpenPracticeButton` CTA with the tappable lesson card as the
    single primary entry (the card IS the Start affordance). Tokens only — no raw hex, no
    magic numbers, no inline font families; **do NOT use `QalamColors.reward`** anywhere on
    this screen (gold is rewards-only; no mastery star lives on the demo home). Do NOT add any
    header star counter, weekly tally, weekly progress bar, or per-lesson 3-star rating, and
    do NOT fabricate a stars/lessons dataset (D-17). `flutter analyze lib/screens/home_screen.dart`.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze lib/screens/home_screen.dart lib/l10n</automated>
  </verify>
  <acceptance_criteria>
    - `flutter gen-l10n` succeeds and `lib/l10n/app_localizations.dart` declares getters `homeGreeting`, `homeLessonTitle`, `comingSoon`, `navJourney`, `navParent` (`grep -c "String get homeGreeting\|String get comingSoon\|String get navJourney" lib/l10n/app_localizations.dart` returns >= 3).
    - `grep -c "context.go('/practice')" lib/screens/home_screen.dart` returns >= 1 (the lesson card links into the route 03-04 owns; no new practice route is added).
    - `grep -c "QalamColors.reward" lib/screens/home_screen.dart` returns 0 (gold reward token is not used on the demo home — rewards-only rule).
    - `grep -ci "this week\|stars this week\|⭐\|streak" lib/screens/home_screen.dart lib/l10n/app_en.arb` returns 0 (no weekly tally / running-total chrome).
    - Copy reads remain null-safe: `grep -c "AppLocalizations.of(context)?" lib/screens/home_screen.dart` returns >= 1 and there is no non-null-asserted `.of(context)!` greeting read (so the bare-MaterialApp D-05 test still renders).
    - `flutter analyze lib/screens/home_screen.dart lib/l10n` reports no errors.
  </acceptance_criteria>
  <done>The home screen renders the warm demo home (mascot + static greeting + tappable alif lesson card → /practice) with all copy in gen-l10n, tokens only, no gamification chrome, and graceful handling of the not-yet-bundled mascot asset.</done>
</task>

<task type="auto">
  <name>Task 2: Locked Journey/Parent side-nav (visibly disabled, no routes) on the demo home</name>
  <files>lib/screens/home_screen.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - lib/screens/home_screen.dart (the file from Task 1 — the new Scaffold/body layout; the token CTA + InkWell pattern to mirror for the nav items; the null-safe l10n reads)
    - lib/router/app_router.dart (proves there is NO /journey and NO /parent route — the /parent block is a commented SEAM ONLY for Phase 9 lines 51-60; the nav items MUST NOT call context.go to any route)
    - .planning/phases/03-trace-one-letter-end-to-end/03-CONTEXT.md (D-17 "No dead routes": Journey + Parent + any "See journey" render visibly Coming soon / locked, disabled, not wired)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (icon library line 60: lock.svg lives under assets/icons/ — but that dir is empty; a lock GLYPH is optional, a visibly-disabled state is mandatory)
    - lib/theme/colors.dart (QalamColors.fgMuted / borderSoft / surface for the disabled/locked visual treatment; no red)
    - lib/theme/dimens.dart (QalamSpace gaps; QalamTargets — a disabled nav item is non-interactive but still sized for the layout)
    - docs/design/kit/project/screenshots/home.png (the left nav rail: Home active, Journey + Parent below — render Journey/Parent muted + "Coming soon")
  </read_first>
  <action>
    Add a left nav rail (or a compact top/side nav matching home-compact.png) to the demo
    home as a new private `_DemoNavRail` widget. It shows three entries from l10n: **Home**
    (active — the current screen, ink-teal/`QalamColors.primary` active treatment), and
    **Journey** and **Parent** rendered **visibly disabled/locked**: muted color
    (`QalamColors.fgMuted`), a small "Coming soon" label from the `comingSoon` getter, and NO
    `onTap`/`context.go` — they are inert (Journey is Phase 6, Parent is Phase 9, D-17). If a
    lock affordance is shown, attempt `SvgPicture.asset('assets/icons/lock.svg')` ONLY behind
    a graceful missing-asset fallback (the file is not bundled yet — degrade to no glyph, do
    NOT crash); otherwise render the muted "Coming soon" label alone. Do NOT add `/journey` or
    `/parent` routes to `lib/router/app_router.dart`, and do NOT surface a "See journey"
    affordance that navigates anywhere. If a "See journey"-style hint is shown at all it must
    be the same inert locked treatment. Tokens only; no red (use `fgMuted`/`borderSoft` for
    the disabled look). `flutter analyze lib/screens/home_screen.dart`.
  </action>
  <verify>
    <automated>flutter analyze lib/screens/home_screen.dart && grep -c "context.go('/journey')\|context.go('/parent')" lib/screens/home_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "context.go('/journey')\|context.go('/parent')" lib/screens/home_screen.dart` returns 0 (Journey/Parent are never wired to a route).
    - `grep -c "navJourney\|navParent" lib/screens/home_screen.dart` returns >= 2 (both entries are rendered from l10n).
    - `grep -c "comingSoon" lib/screens/home_screen.dart` returns >= 1 (the locked entries carry the "Coming soon" affordance).
    - `lib/router/app_router.dart` is unchanged regarding new routes: `grep -c "/journey'\|path: '/parent'" lib/router/app_router.dart` returns 0 (the /parent SEAM-ONLY comment block does not count as a route — no active `path: '/parent'`).
    - Any `SvgPicture.asset('assets/icons/...` use is guarded so a missing asset does not throw (graceful fallback present).
    - `flutter analyze lib/screens/home_screen.dart` reports no errors.
  </acceptance_criteria>
  <done>Journey and Parent appear in the demo home nav as visibly disabled "Coming soon" entries that go nowhere, Home is active, and no new routes are introduced — no dead routes.</done>
</task>

<task type="auto">
  <name>Task 3: Demo-home widget test (nav into /practice + anti-gamification absence + locked nav) and keep D-05 green</name>
  <files>test/screens/home_screen_test.dart</files>
  <read_first>
    - lib/screens/home_screen.dart (the upgraded screen — Tasks 1-2: greeting, _TodaysLessonCard, _DemoNavRail; the context.go('/practice') onTap)
    - test/direction_test.dart (the D-05 bare-MaterialApp(home: HomeScreen()) test that must STAY green — proves chrome is LTR; relies on null-safe copy reads; do not break it)
    - test/features/authoring/authoring_screen_test.dart (the in-repo widget-test harness style: pumpWidget, find.text/find.byKey, tap + pumpAndSettle, assertions — reuse this idiom, no mocktail)
    - test/flutter_test_config.dart (auto-loads bundled Arabic TTFs so the alif glyph renders real, not tofu — already applied to this test tree)
    - lib/router/app_router.dart (to drive a real GoRouter, OR assert navigation via a captured route — choose the simplest deterministic approach; a MaterialApp with a /practice stub route is acceptable for asserting the tap navigates)
    - .planning/phases/03-trace-one-letter-end-to-end/03-04-PLAN.md (the negative-assertion / absence-of-widget test style used for PLAT-03 omissions — mirror it: assert text/keys ABSENT)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (Mandatory omissions table — the exact chrome to assert ABSENT)
  </read_first>
  <action>
    Create `test/screens/home_screen_test.dart` wrapping `HomeScreen` in a `ProviderScope` +
    `MaterialApp` (with `AppLocalizations` delegates so the demo copy renders) and a router or
    a stub `/practice` route to observe navigation. Write these `testWidgets` cases:
    (1) **renders the demo home** — `find.text` the greeting ("Welcome back, Layla.") and the
        lesson title ("The Letter Alif"), and assert the alif `ArabicText` glyph is present
        (find.byType(ArabicText) or a key on the glyph tile).
    (2) **lesson card taps into the practice flow** — `tester.tap` the lesson card (key it,
        e.g. `Key('todaysLessonCard')`), `pumpAndSettle`, and assert navigation reached
        `/practice` (via the router's current location or a stub PracticeScreen finder) — the
        single positive entry-point assertion (S1-04).
    (3) **anti-gamification absence (PLAT-03/D-17)** — assert ABSENT: any running star total /
        "⭐ 39" / "THIS WEEK" / "stars this week" text, any weekly progress bar widget, and any
        per-lesson 3-star rating on the card (`expect(find.textContaining('THIS WEEK'),
        findsNothing)` etc., mirroring 03-04's negative-assertion style). Do NOT assert a
        fabricated dataset exists.
    (4) **Journey/Parent are locked** — assert Journey and Parent labels are present
        (`find.text` from l10n) AND carry the "Coming soon" affordance, and that tapping them
        does NOT navigate (router location unchanged after `tester.tap` + `pumpAndSettle`).
    Run the new test, then run the full suite to prove the existing D-05 direction test (and
    all prior tests) stay green.
  </action>
  <verify>
    <automated>flutter test test/screens/home_screen_test.dart test/direction_test.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/screens/home_screen_test.dart` exits 0 with at least 4 test cases (render, card→/practice nav, anti-gamification absence, locked Journey/Parent).
    - The card-tap case proves navigation reaches `/practice` (router location or a PracticeScreen/stub finder) — the S1-04 positive entry assertion.
    - The absence case asserts NO "THIS WEEK" / "stars this week" / running-star-total text and NO weekly progress bar / 3-star rating widget render (PLAT-03/D-17).
    - The locked case asserts tapping Journey/Parent does NOT change the router location (no dead-route navigation).
    - `flutter test test/direction_test.dart` exits 0 (the D-05 bare-MaterialApp direction test still renders with the upgraded home).
    - `flutter test` (full suite) exits 0.
  </acceptance_criteria>
  <done>The demo home is test-proven: it renders the greeting + alif lesson card, the card taps into /practice, the omitted gamification chrome is asserted absent, Journey/Parent are locked and inert, and the full suite (including the D-05 direction test) stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → router | The only crossing on this screen: a card tap requests in-app navigation to /practice (no external input, no network). |
| static demo data → UI | A hardcoded placeholder name + the bundled alif glyph render to screen; no profile/PII is collected, stored, or transmitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | static greeting name on the demo home | accept | The name is a hardcoded non-PII placeholder ("Layla") for the demo only — no profile system, no child data collected/stored/transmitted (CLAUDE.md minimum-child-data rule honored). Phase 5 introduces real profiles under that rule. |
| T-03-07 | Tampering | anti-gamification chrome leaks back from home.png into the demo home (PLAT-03) | mitigate | The demo home OMITS the ⭐ counter / weekly tally / weekly bar / 3-star rating; the widget test asserts their ABSENCE; a grep gate proves no "this week"/"⭐"/"streak" strings and no `QalamColors.reward` use on this screen. |
| T-03-09 | Denial of Service (crash) | SvgPicture.asset on the not-yet-bundled mascot/lock glyphs (assets/icons empty) | mitigate | Every asset reference degrades gracefully on a missing asset (SizedBox fallback, like 03-04's celebration mascot) — a missing illustration cannot crash the demo. |
| T-03-10 | Elevation of Privilege | a dead Journey/Parent route exposes an unbuilt area | mitigate | Journey and Parent are inert disabled "Coming soon" entries with NO onTap and NO route added to app_router.dart; the locked-nav test asserts tapping them does not navigate (the /parent PIN gate remains a Phase-9 SEAM ONLY comment). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime packages introduced (RESEARCH Package Legitimacy Audit; UI-SPEC Registry Safety "no new packages"). No install task. |
</threat_model>

<verification>
- `flutter test` (full suite) green — the demo-home widget test (render, /practice nav, anti-gamification absence, locked nav), the D-05 direction test, and all prior-plan tests.
- `flutter analyze` clean across `lib/screens/home_screen.dart` and `lib/l10n`.
- Grep gates: `context.go('/practice')` present; `QalamColors.reward`, "this week"/"⭐"/"streak", and `context.go('/journey')`/`context.go('/parent')` all absent from `home_screen.dart`; no new `path: '/journey'`/`path: '/parent'` in `app_router.dart`.
- Manual-only demo check (course-staff dry run, owner's finger-only tablet): launch app → demo home shows mascot + "Welcome back" + the alif "Today's lesson" card → tap card → Watch→Trace→Celebrate (03-04) → Back Home; Journey/Parent visibly "Coming soon" and unresponsive.
</verification>

<success_criteria>
- S1-04 (entry point): from a real, presentable demo home the child reaches the trace flow via the alif "Today's lesson" card → /practice (the route 03-04 owns) — proven by the card-tap navigation test.
- D-17 warmth: mascot + static greeting + alif lesson card faithful to home.png warmth, all copy in gen-l10n, tokens only, targets >= QalamTargets floors.
- D-17 anti-gamification: no running-star counter, no weekly tally, no weekly bar, no per-lesson 3-star rating, no fabricated dataset — proven absent by test + grep gates.
- D-17 no dead routes: Journey and Parent render visibly locked "Coming soon" and never navigate — proven by the locked-nav test.
- The existing D-05 direction test stays green (null-safe copy reads preserved).
</success_criteria>

<output>
Create `.planning/phases/03-trace-one-letter-end-to-end/03-05-SUMMARY.md` when done.
</output>
