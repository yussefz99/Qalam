---
phase: 03-trace-one-letter-end-to-end
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/config/debug_flags.dart
  - lib/core/recognition/handwriting_recognizer.dart
  - lib/features/practice/widgets/stroke_canvas.dart
  - lib/features/practice/widgets/stroke_order_animation.dart
  - test/features/practice/stroke_canvas_test.dart
  - test/features/practice/stroke_order_animation_test.dart
autonomous: true
requirements: [S1-04, S1-05]
must_haves:
  truths:
    - "The trace canvas accepts stylus input; in production it rejects finger/touch (palm rejection free); with the debug-finger flag set it accepts touch so the owner's finger-only hardware can drive the loop"
    - "Live ink renders smoothly under the captured pointer, over a dotted guide drawn from the resolved reference path (not Text('ا'))"
    - "The 'Watch me write' animation auto-plays once then offers Replay, with an animated pen-tip tracing the SAME resolved reference path from the gold start-dot in the correct direction"
    - "The HandwritingRecognizer interface seam exists but is unimplemented and makes no calls (D-16)"
  artifacts:
    - path: "lib/config/debug_flags.dart"
      provides: "DebugFlags.allowFingerInput (on-by-default in debug, D-13/D-14)"
      contains: "allowFingerInput"
    - path: "lib/features/practice/widgets/stroke_canvas.dart"
      provides: "Listener-based capture with PointerDeviceKind filter + dotted guide + ink"
      contains: "PointerDeviceKind"
    - path: "lib/features/practice/widgets/stroke_order_animation.dart"
      provides: "PathMetric pen-tip animation over the resolved reference path"
      contains: "computeMetrics"
    - path: "lib/core/recognition/handwriting_recognizer.dart"
      provides: "empty abstract HandwritingRecognizer seam (no ML Kit, no network)"
      contains: "abstract"
  key_links:
    - from: "lib/features/practice/widgets/stroke_order_animation.dart"
      to: "lib/core/scoring/reference_path.dart (ReferencePath.resolve)"
      via: "build dart:ui Path from resolved points (one source of truth, S1-04)"
      pattern: "ReferencePath.resolve"
    - from: "lib/features/practice/widgets/stroke_canvas.dart"
      to: "lib/config/debug_flags.dart (DebugFlags.allowFingerInput)"
      via: "kind filter opens touch only under kDebugMode + flag"
      pattern: "allowFingerInput"
---

<objective>
Build the trace-surface and demo widgets — the "input" half of the loop — extending the
proven Phase-1 `Listener`+`_InkPainter` capture (Pitfall 2 already respected). Three
additions on top of the analog: a `PointerDeviceKind` filter (stylus-only in prod, a
debug-finger flag on-by-default in debug per D-13/D-14 so the owner's finger-only hardware
works), a dotted guide layer drawn from `ReferencePath.resolve` (NOT `Text('ا')` —
Pitfall 5), and a stylus-up hook that surfaces the completed stroke. Plus the "Watch me
write" stroke-order animation (S1-04/D-11): a `PathMetric`-driven pen-tip that traces the
SAME resolved reference path from the gold start-dot, auto-playing once then offering
Replay (D-10). And the empty `HandwritingRecognizer` interface seam (D-16 — no ML Kit, no
network in Phase 3).

Purpose: Delivers SC1 (replayable animation from the scoring path) and the capture half of
SC2/S1-05. Depends on Plan 01 only for the scoring layer's `StrokeResult`/MistakeId types
referenced by the stylus-up callback signature (no behavioral coupling yet — wiring is
Plan 04).

Output: `debug_flags.dart`, `handwriting_recognizer.dart`, `stroke_canvas.dart`,
`stroke_order_animation.dart`, and their widget tests.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trace-one-letter-end-to-end/03-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-RESEARCH.md
@.planning/phases/03-trace-one-letter-end-to-end/03-PATTERNS.md
@.planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md
@.planning/phases/03-trace-one-letter-end-to-end/03-VALIDATION.md
@lib/screens/practice_screen.dart
@lib/core/scoring/reference_path.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: debug flag, recognizer seam, stylus-filtered capture canvas (+ widget test)</name>
  <files>lib/config/debug_flags.dart, lib/core/recognition/handwriting_recognizer.dart, lib/features/practice/widgets/stroke_canvas.dart, test/features/practice/stroke_canvas_test.dart</files>
  <read_first>
    - lib/screens/practice_screen.dart (_InkSurface Listener capture lines 207-228; _local globalToLocal lines 202-205; _InkPainter quadratic-smoothing _paintStroke lines 298-321 — REUSE verbatim; note line 180 "stylus-only rejection is P3")
    - lib/dev/authoring_screen.dart (_CaptureSurface + the IgnorePointer faint-backdrop pattern for the guide layer)
    - lib/theme/dimens.dart (QalamInk.strokeWidth=6 line 90; QalamTargets line 29-31)
    - lib/theme/colors.dart (inkStroke line 63; reward line 51; warnSoft line 59; bg/surface lines 35-36)
    - lib/core/scoring/reference_path.dart (ReferencePath.resolve — build the guide path from this)
    - lib/core/scoring/scoring_models.dart (StrokeResult/MistakeId — the stylus-up callback's result type, from Plan 01)
    - test/features/authoring/authoring_screen_test.dart (pointer-synthesis harness: getRect, startGesture/moveTo/up, pumpAndSettle — lines 119-143)
    - .planning/phases/03-trace-one-letter-end-to-end/03-RESEARCH.md (Pattern 1 capture + _accept; Pitfall 5 finger-only)
    - .planning/phases/03-trace-one-letter-end-to-end/03-PATTERNS.md (stroke_canvas.dart "THREE additions"; debug_flags.dart; handwriting_recognizer.dart seam)
  </read_first>
  <action>
    Create `lib/config/debug_flags.dart` as `abstract final class DebugFlags` (mirrors
    QalamSpace) with `static const bool allowFingerInput = true` and a doc comment citing
    D-13/D-14 (read only behind `kDebugMode` at the call site). Create
    `lib/core/recognition/handwriting_recognizer.dart` as an `abstract interface class
    HandwritingRecognizer` declaring the method the Phase-4 ML Kit impl will satisfy (e.g.
    `Future<RecognitionResult> identify(List<List<double>> stroke)` with a small result
    type) — NO implementation, NO ML Kit import, NO network; add a `// Phase 4:` marker
    exactly like the /parent seam comment in app_router.dart. Create
    `lib/features/practice/widgets/stroke_canvas.dart`: a widget that REUSES the P1
    `Listener` capture and `_paintStroke` smoothing, adds a `_accept(PointerDeviceKind k)`
    gate — `k == PointerDeviceKind.stylus || (kDebugMode && DebugFlags.allowFingerInput &&
    k == PointerDeviceKind.touch)` — applied in onPointerDown/Move/Up, paints the resolved
    reference path (from `ReferencePath.resolve`, scaled to canvas) as a dotted guide UNDER
    the ink in a CustomPainter (NOT Text), and calls an `onStrokeSubmitted(List<Offset>)`
    callback on pointer-up. Tokens only: QalamColors.inkStroke, QalamColors.bg/surface,
    QalamColors.reward for the start-dot, QalamInk.strokeWidth. Keep captured points in
    widget State (in-memory only, T-03-01/T-01-05); no print/debugPrint of points. Create
    `test/features/practice/stroke_canvas_test.dart` using the authoring-test pointer
    synthesis: assert a synthesized stylus stroke produces an onStrokeSubmitted call; assert
    a synthesized touch stroke is REJECTED when allowFingerInput is overridden false (prod
    behavior) and ACCEPTED when the debug-finger flag is on (Pitfall 5). Make the flag
    injectable into the widget (e.g. a constructor `acceptTouch` defaulting to the
    DebugFlags value) so the test can drive both branches deterministically without relying
    on build mode.
  </action>
  <verify>
    <automated>flutter test test/features/practice/stroke_canvas_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/practice/stroke_canvas_test.dart` exits 0.
    - The test exercises BOTH branches: touch rejected in prod-mode, touch accepted with the debug-finger flag (Pitfall 5).
    - `grep -c "PointerDeviceKind.stylus" lib/features/practice/widgets/stroke_canvas.dart` returns >= 1.
    - `grep -c "Text('ا')\|Text(\"ا\")" lib/features/practice/widgets/stroke_canvas.dart` returns 0 (guide is path-drawn, Pitfall 5).
    - `grep -c "ReferencePath.resolve" lib/features/practice/widgets/stroke_canvas.dart` returns >= 1.
    - `grep -ci "mlkit\|ml_kit\|http\|firebase" lib/core/recognition/handwriting_recognizer.dart` returns 0 (empty seam, D-16).
    - `flutter analyze lib/config/ lib/core/recognition/ lib/features/practice/widgets/stroke_canvas.dart` reports no errors.
  </acceptance_criteria>
  <done>Stylus-filtered capture works (prod rejects touch, debug flag opens it), the dotted guide is drawn from the resolved path, the recognizer seam is empty, and the widget test proves both input branches.</done>
</task>

<task type="auto">
  <name>Task 2: stroke-order animation (PathMetric pen-tip, auto-play once + Replay) (+ widget test)</name>
  <files>lib/features/practice/widgets/stroke_order_animation.dart, test/features/practice/stroke_order_animation_test.dart</files>
  <read_first>
    - lib/core/scoring/reference_path.dart (ReferencePath.resolve — the SAME source the scorer uses; wrap into a dart:ui Path here)
    - lib/screens/practice_screen.dart (_InkPainter CustomPainter shape lines 271-326)
    - lib/theme/dimens.dart (QalamMotion.durSlow=420 line 83, easeOutQuart line 77; QalamColors via colors.dart)
    - lib/theme/colors.dart (reward line 51 for start-dot + pen-tip; inkStroke line 63 for the drawn path)
    - .planning/phases/03-trace-one-letter-end-to-end/03-RESEARCH.md (Pattern 2 PathMetric extractPath/getTangentForOffset; Alternatives — reject Rive)
    - .planning/phases/03-trace-one-letter-end-to-end/03-UI-SPEC.md (Interaction & Motion: auto-play once then Replay D-10; gold start-dot; pacing tokens)
    - test/features/practice/stroke_canvas_test.dart (pump/pumpAndSettle widget-test style from Task 1)
  </read_first>
  <action>
    Create `lib/features/practice/widgets/stroke_order_animation.dart`: a widget taking the
    alif `List<StrokeSpec>` (or the resolved points), building a `dart:ui` `Path` from
    `ReferencePath.resolve(...)` scaled to the canvas, and driving an `AnimationController`
    (duration QalamMotion.durSlow, curve easeOutQuart). In a CustomPainter, draw
    `metric.extractPath(0, metric.length * t)` for the progressive stroke, a moving pen-tip
    circle at `metric.getTangentForOffset(metric.length * t).position`
    (QalamColors.reward), and the numbered gold start-dot at
    `metric.getTangentForOffset(0).position`. Auto-play once on first build (D-10); expose
    a `replay()` affordance / public method so the parent screen's "Watch Again" / "Replay"
    button (Plan 04) can re-run it. Do NOT add Rive (RESEARCH Alternatives — violates S1-04
    one-source-of-truth). Create `test/features/practice/stroke_order_animation_test.dart`:
    pump the widget, assert the controller auto-runs once to completion (status completed
    after pumpAndSettle), then invoke replay and assert it runs again (S1-04 replayable;
    D-10 auto-play-once). Assert the animation consumes ReferencePath.resolve output (e.g.
    via a test that the drawn path length matches the resolved-path length, proving one
    source of truth).
  </action>
  <verify>
    <automated>flutter test test/features/practice/stroke_order_animation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/practice/stroke_order_animation_test.dart` exits 0.
    - The test asserts auto-play-once (controller reaches completed) AND that replay re-runs (D-10, S1-04 replayable).
    - `grep -c "computeMetrics\|getTangentForOffset" lib/features/practice/widgets/stroke_order_animation.dart` returns >= 1.
    - `grep -c "ReferencePath.resolve" lib/features/practice/widgets/stroke_order_animation.dart` returns >= 1 (same path as scorer — one source of truth).
    - `grep -ci "rive\|lottie" lib/features/practice/widgets/stroke_order_animation.dart` returns 0.
    - `flutter analyze lib/features/practice/widgets/stroke_order_animation.dart` reports no errors.
  </acceptance_criteria>
  <done>The "Watch me write" pen-tip animation traces the resolved reference path from the gold start-dot, auto-plays once, and replays on demand — driven by the same geometry the scorer uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer hardware → capture widget | Raw stylus/touch events enter the app. Filtered by PointerDeviceKind; geometric only, not parsed/executed. |
| capture widget → (future) scorer | Completed stroke handed up via callback; points stay in memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | stroke_canvas captured points | mitigate | Points held in widget State only; discarded on dispose/retry; no print/debugPrint of points; no persistence/network from this layer. |
| T-03-04 | Spoofing / Tampering | finger/palm input masquerading as a valid stroke in production | mitigate | Prod `_accept` admits only PointerDeviceKind.stylus; touch (resting palm) is dropped — palm rejection is free. Debug-finger path is gated by kDebugMode + DebugFlags.allowFingerInput and is test-injectable, never shipped-on for release input. |
| T-03-05 | Information Disclosure | empty HandwritingRecognizer seam accidentally making a network call | mitigate | Interface only, zero implementation, zero ML Kit/http/firebase imports in Phase 3 (D-16); grep gate asserts no network symbols. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime packages — flutter_svg/vector_math already locked; framework primitives (Listener, PathMetric) only. No install task. |
</threat_model>

<verification>
- `flutter test test/features/practice/stroke_canvas_test.dart test/features/practice/stroke_order_animation_test.dart` green.
- `flutter analyze` clean for the new widget/config/recognition files.
- Nyquist (03-VALIDATION.md): turns green the S1-05/D-13 canvas filter row and the S1-04 animation auto-play/replay row.
</verification>

<success_criteria>
- Stylus-only capture in prod with a working debug-finger path for the owner's hardware.
- Dotted guide and animation both render from `ReferencePath.resolve` (one source of truth, S1-04).
- The recognizer seam is present, empty, and call-free (D-16).
</success_criteria>

<output>
Create `.planning/phases/03-trace-one-letter-end-to-end/03-03-SUMMARY.md` when done.
</output>
