---
phase: 03-trace-one-letter-end-to-end
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/data/progress_repository_test.dart
  - lib/data/app_database.dart
  - lib/data/progress_repository.dart
  - lib/data/drift_progress_repository.dart
autonomous: true
requirements: [S1-10]
must_haves:
  truths:
    - "alif mastery (letterId + clean-rep count + timestamp) persists to Drift and survives a simulated restart"
    - "Adding the LetterMastery table does not crash an existing v1 install (1→2 migration runs)"
    - "Only the mastery result is persisted — never the captured stroke points (T-03-01/T-01-05)"
  artifacts:
    - path: "lib/data/app_database.dart"
      provides: "LetterMastery table + schemaVersion 2 + onUpgrade migration"
      contains: "schemaVersion => 2"
    - path: "lib/data/progress_repository.dart"
      provides: "ProgressRepository interface (recordMastery / isMastered)"
      contains: "abstract"
    - path: "lib/data/drift_progress_repository.dart"
      provides: "Drift-backed ProgressRepository + keepAlive Riverpod provider"
      contains: "@Riverpod(keepAlive: true)"
  key_links:
    - from: "lib/data/drift_progress_repository.dart"
      to: "lib/data/app_database.dart (appDatabaseProvider)"
      via: "constructor takes AppDatabase from ref.watch(appDatabaseProvider)"
      pattern: "appDatabaseProvider"
    - from: "lib/data/app_database.dart"
      to: "LetterMastery (Drift table)"
      via: "insertOnConflictUpdate + select getSingleOrNull"
      pattern: "letterMastery"
---

<objective>
Add the persistence half of mastery (S1-10/D-09): a small Drift `LetterMastery` table, a
`schemaVersion` 1→2 migration so existing v1 installs upgrade cleanly (Pitfall 4), and a
`ProgressRepository` (interface + Drift impl + keepAlive Riverpod provider) exposing
`recordMastery(letterId, cleanReps)` and `isMastered(letterId)`. The captured stroke points
are NEVER persisted — only the derived mastery result (letterId, cleanReps, timestamp),
upholding child-data sensitivity (T-03-01 / T-01-05).

Purpose: Proves the loop is durable — "it remembers" alif mastery across an app restart,
which Phase 6's journey map will read. Independent of the scorer slice (separate files),
so it runs in the same wave.

Output: modified `app_database.dart`, new `progress_repository.dart` +
`drift_progress_repository.dart`, a round-trip + migration integration test, and
regenerated `app_database.g.dart`.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trace-one-letter-end-to-end/03-CONTEXT.md
@.planning/phases/03-trace-one-letter-end-to-end/03-RESEARCH.md
@.planning/phases/03-trace-one-letter-end-to-end/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): mastery round-trip + 1→2 migration test</name>
  <files>test/data/progress_repository_test.dart</files>
  <read_first>
    - test/data/app_database_test.dart (the analog: shared NativeDatabase.memory(), close db1, reopen db2 over the same executor, assert survival — lines 21-36)
    - lib/data/app_database.dart (current schemaVersion => 1, AppSettings table, injected-executor close() contract lines 39-54)
    - .planning/phases/03-trace-one-letter-end-to-end/03-RESEARCH.md (Pattern 4 Drift schema; Pitfall 4 migration; Code Examples mastery round-trip)
  </read_first>
  <behavior>
    - recordMastery('alif', cleanReps: 3) then a simulated restart (close, reopen over shared executor) → isMastered('alif') == true and the read-back cleanReps == 3.
    - A fresh database created at schemaVersion 2 has the letter_mastery table (recordMastery succeeds without throwing "no such table").
    - Migration: opening over an executor seeded at the pre-migration shape upgrades to v2 without error and existing app_settings rows survive (assert a pre-written setting still reads back after the mastery table is added).
  </behavior>
  <action>
    Create `test/data/progress_repository_test.dart` following app_database_test.dart's
    shared-executor restart pattern. Use `DatabaseConnection(NativeDatabase.memory())` and
    `AppDatabase(shared.executor)`. Pin the API: `DriftProgressRepository(AppDatabase db)`
    with `Future<void> recordMastery({required String letterId, required int cleanReps})`
    and `Future<bool> isMastered(String letterId)` (plus a read of cleanReps if exposed).
    The test must be RED because progress_repository.dart / drift_progress_repository.dart
    and the LetterMastery table do not exist yet. Do NOT stub them in this task.
  </action>
  <verify>
    <automated>flutter test test/data/progress_repository_test.dart 2>&1 | grep -E "Error|not found|no such" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/data/progress_repository_test.dart` fails to compile (missing lib targets) — RED.
    - The test asserts both mastery survival across a simulated restart AND that an existing app_settings value survives the migration.
    - `grep -c "NativeDatabase.memory" test/data/progress_repository_test.dart` returns >= 1 (hermetic, no path_provider).
  </acceptance_criteria>
  <done>A red integration test pins recordMastery/isMastered and the migration-preserves-existing-rows behavior.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): LetterMastery table, 1→2 migration, ProgressRepository, build_runner</name>
  <files>lib/data/app_database.dart, lib/data/progress_repository.dart, lib/data/drift_progress_repository.dart</files>
  <read_first>
    - lib/data/app_database.dart (AppSettings table lines 22-28; setSetting/getSetting access pattern lines 57-68; injected-executor close() lines 39-54 — PRESERVE it)
    - lib/data/curriculum_repository.dart (repository + @Riverpod(keepAlive: true) provider lines 117-120 — the provider template)
    - test/data/progress_repository_test.dart (the spec from Task 1)
    - .planning/phases/03-trace-one-letter-end-to-end/03-PATTERNS.md (app_database.dart MODIFY section — table/schema/migration/access patterns with line refs)
  </read_first>
  <action>
    Modify `app_database.dart`: add a `LetterMastery extends Table` with
    `TextColumn letterId` (primary key), `IntColumn cleanReps`, `DateTimeColumn masteredAt`;
    change `@DriftDatabase(tables: [AppSettings, LetterMastery])`; bump
    `int get schemaVersion => 2`; add `MigrationStrategy get migration` with
    `onCreate: (m) => m.createAll()` and `onUpgrade: (m, from, to) async { if (from < 2)
    await m.createTable(letterMastery); }` (Pitfall 4 — required). PRESERVE the
    `_ownsExecutor` close() contract (the restart test depends on it). Add Drift accessors
    `recordMastery` (into(letterMastery).insertOnConflictUpdate with masteredAt =
    DateTime.now()) and `isMastered`/cleanReps read (select…where…getSingleOrNull),
    mirroring setSetting/getSetting. Create `progress_repository.dart` with an
    `abstract interface class ProgressRepository` declaring
    `Future<void> recordMastery({required String letterId, required int cleanReps})` and
    `Future<bool> isMastered(String letterId)`. Create `drift_progress_repository.dart`
    implementing it over an injected `AppDatabase`, plus a
    `@Riverpod(keepAlive: true) ProgressRepository progressRepository(Ref ref) =>
    DriftProgressRepository(ref.watch(appDatabaseProvider));` provider (matches
    curriculum_repository.dart). SECURITY (T-03-01/T-01-05): persist ONLY
    letterId/cleanReps/masteredAt — never stroke points; no logging of progress data. After
    editing run `dart run build_runner build --delete-conflicting-outputs` to regenerate
    `app_database.g.dart` and the new provider `.g.dart`.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/data/progress_repository_test.dart && flutter test test/data/app_database_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/data/progress_repository_test.dart` exits 0 (mastery round-trip + migration green).
    - `flutter test test/data/app_database_test.dart` still exits 0 (the existing D-09 restart test is not regressed).
    - `grep -c "schemaVersion => 2" lib/data/app_database.dart` returns 1.
    - `grep -c "onUpgrade" lib/data/app_database.dart` returns >= 1.
    - `grep -c "@Riverpod(keepAlive: true)" lib/data/drift_progress_repository.dart` returns 1.
    - `flutter analyze lib/data/ test/data/` reports no errors.
  </acceptance_criteria>
  <done>The LetterMastery table + 1→2 migration ship; ProgressRepository persists and reads mastery; existing installs upgrade without losing app_settings rows; codegen is regenerated and the full data test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mastery result → on-device Drift store | The only data written to disk in this phase. App-private storage, no network. |
| existing v1 install → v2 schema | A schema migration runs on upgrade; must not destroy existing app_settings data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | LetterMastery persistence | mitigate | Persist ONLY letterId/cleanReps/masteredAt (non-sensitive). Never persist or log captured stroke points or any child name (profiles are Phase 5). |
| T-03-03 | Denial of Service | 1→2 migration crashes existing installs | mitigate | Explicit `onUpgrade` createTable guarded by `from < 2`; integration test asserts existing app_settings rows survive the upgrade. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime packages — drift is already locked at the 2.31 line (do not bump). No install task. |
</threat_model>

<verification>
- `flutter test test/data/` green (mastery round-trip, migration, and the unchanged D-09 restart proof).
- `flutter analyze` clean for `lib/data/`.
- Nyquist (03-VALIDATION.md): turns green the S1-10/D-09 "mastery persists + survives restart" row and exercises the v1→v2 migration.
</verification>

<success_criteria>
- alif mastery persists to Drift and survives a simulated restart.
- The schema migration runs cleanly on an existing install, preserving prior rows.
- Only the mastery result is persisted; stroke points never touch disk.
</success_criteria>

<output>
Create `.planning/phases/03-trace-one-letter-end-to-end/03-02-SUMMARY.md` when done.
</output>
