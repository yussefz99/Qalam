---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/tutor/tutor_brain.dart
  - lib/tutor/tutor_facts.dart
  - lib/tutor/tutor_decision.dart
  - lib/tutor/tutor_facts_builder.dart
  - lib/tutor/authored_fallback_brain.dart
  - lib/tutor/tutor_dispatcher.dart
  - lib/tutor/tutor_providers.dart
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - test/tutor/tutor_facts_builder_test.dart
  - test/tutor/authored_fallback_brain_test.dart
  - test/tutor/tutor_dispatcher_test.dart
autonomous: true
requirements: [TUTOR-01, TUTOR-02, TUTOR-05, GROUND-01]
must_haves:
  truths:
    - "In airplane mode with no model loaded, every baa coaching moment shows a grounded, correctly-Arabic AuthoredFallback line (the owner's-mother signed-off feedback), and the trace loop never blocks (TUTOR-02)."
    - "The agent acts only through the 4 ACTION tools; the geometry verdict + learner state arrive as injected FACTS — the dispatcher has no 'set verdict' branch (TUTOR-05, GROUND-01)."
    - "The pass/fail + star stays decided by the deterministic scorer at the ExerciseController.applyResult(CheckResult) seam; no TutorBrain/dispatcher path can flip a fail to a pass (GROUND-01)."
    - "Swapping the TutorBrain backend changes no canvas, scorer, or curriculum code, and does NOT touch ExerciseController — the tutor line plugs in via a tutor-owned Riverpod provider read by ExerciseScaffold's _TutorColumn (TUTOR-01)."
  artifacts:
    - path: "lib/tutor/tutor_brain.dart"
      provides: "abstract TutorBrain { Future<TutorDecision> next(TutorFacts facts); } — the one swappable seam"
      contains: "abstract class TutorBrain"
    - path: "lib/tutor/tutor_facts.dart"
      provides: "TutorFacts FACTS-in DTO (letterId, mistakeId enum, struggleTags, passed, recentMistakes) — non-PII, no raw strokes"
      contains: "class TutorFacts"
    - path: "lib/tutor/tutor_decision.dart"
      provides: "TutorDecision ACTIONS-out — exactly one of present_activity/say/give_hint/advance; verdict+star are NOT decisions"
      contains: "TutorDecision"
    - path: "lib/tutor/tutor_facts_builder.dart"
      provides: "the ONE chokepoint that builds TutorFacts from a CheckResult + session state; raw Offsets/nickname/PII never reach it"
      contains: "buildTutorFacts"
    - path: "lib/tutor/authored_fallback_brain.dart"
      provides: "AuthoredFallback TutorBrain — pure Dart, zero model, coaches from the exercise's signed-off feedback map"
      contains: "class AuthoredFallbackBrain implements TutorBrain"
    - path: "lib/tutor/tutor_dispatcher.dart"
      provides: "closed switch over the 4 ACTION tool names → imperative TutorController calls; unrecognized = logged no-op"
      contains: "switch"
    - path: "lib/tutor/tutor_providers.dart"
      provides: "the active TutorBrain provider AND the tutor-owned tutorLineProvider the _TutorColumn reads — the brain's line path into the UI, with ExerciseController untouched"
      contains: "tutorLineProvider"
  key_links:
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      to: "lib/tutor/tutor_brain.dart"
      via: "_onResult builds TutorFacts (chokepoint) then awaits TutorBrain.next + dispatches the line into tutorLineProvider; applyResult(CheckResult) still owns the verdict"
      pattern: "tutor"
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart _TutorColumn"
      to: "lib/tutor/tutor_providers.dart tutorLineProvider"
      via: "_TutorColumn watches tutorLineProvider for the brain line; falls back to applyResult's state.line as the verdict-side fallback only"
      pattern: "tutorLineProvider"
    - from: "lib/tutor/authored_fallback_brain.dart"
      to: "exercise feedback map"
      via: "resolves feedback[mistakeId] / feedback['pass'] from the signed-off curriculum content"
      pattern: "feedback"
---

<objective>
Build the v2 AI-tutor spine and its grounded OFFLINE floor: the swappable `TutorBrain`
interface, the `TutorFacts` (FACTS-in) / `TutorDecision` (ACTIONS-out) types, the ONE
non-PII chokepoint that builds facts, the pure-Dart `AuthoredFallback` brain, and the
native function-call dispatcher — then wire it into the existing `ExerciseController`
seam at `ExerciseScaffold` so the baa trace loop runs end-to-end with a grounded,
correctly-Arabic authored coaching line and zero model loaded.

Purpose: Stand up the architecture (ADR-014 Path A, client-only, the GenUI drop) and
prove the grounding invariant by construction before any cloud model is added: the
scorer keeps owning pass/fail + the star; the tutor only supplies WORDS.

Output: `lib/tutor/` spine (interface + types + builder + AuthoredFallback + dispatcher
+ Riverpod providers including the tutor-owned `tutorLineProvider`), the `ExerciseScaffold`
wiring, and three unit/widget tests.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
@CLAUDE.md
</context>

## Phase Goal

**As a** child practising baa with no network and no model loaded, **I want to** still
hear a specific, warm, correctly-Arabic coaching line on every attempt, **so that** the
patient teacher never goes silent — the offline floor is real and grounded.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TutorBrain seam + TutorFacts/TutorDecision types + the non-PII chokepoint builder</name>
  <files>lib/tutor/tutor_brain.dart, lib/tutor/tutor_facts.dart, lib/tutor/tutor_decision.dart, lib/tutor/tutor_facts_builder.dart, test/tutor/tutor_facts_builder_test.dart</files>
  <read_first>
    - lib/core/scoring/scoring_models.dart (enum MistakeId — the FACTS source of truth)
    - lib/core/exercise_engine/check_result.dart (CheckResult{passed, String? mistakeId} — the verdict bridge)
    - lib/features/letter_unit/exercise_controller.dart (ExerciseState/ExercisePhase — session state the facts derive from)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md §Decision parts 2 & 4 (FACTS-in/ACTIONS-out, TutorFacts whitelist, TutorDecision = one of 4 tools)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md <decisions> (the seam contract)
  </read_first>
  <behavior>
    - buildTutorFacts(letterId, section, CheckResult, recentMistakeIds) returns a TutorFacts whose serialized form (a toJson/toMap) contains ONLY: letterId(String), mistakeId(String enum-name or null), passed(bool), section(String), struggleTags(List<String>), recentMistakes(List<String>). No Offset, no num list, no nickname/profile field.
    - A TutorFacts built from a passing CheckResult has passed==true and mistakeId==null.
    - TutorDecision is a sealed/closed set with exactly four shapes: PresentActivity{coachingLine, letterId}, Say{text}, GiveHint{}, Advance{}. There is no "verdict"/"setPass"/"star" decision shape.
    - The builder signature accepts no raw stroke parameter — a caller physically cannot pass Offsets through it (compile-time whitelist).
  </behavior>
  <action>
    Create `lib/tutor/tutor_brain.dart` with `abstract class TutorBrain { Future<TutorDecision> next(TutorFacts facts); }` (pure Dart — NO firebase_ai / genui / flutter_gemma imports in this file or any sibling created here).
    Create `lib/tutor/tutor_facts.dart` with an immutable `TutorFacts` holding `letterId`, `mistakeId` (nullable String — the MistakeId enum NAME, mirroring CheckResult.mistakeId / the curriculum check keys), `passed`, `section`, `struggleTags`, `recentMistakes`, plus a `toMap()`/`toJson()` that emits ONLY those whitelisted scalar/string-list fields. Do NOT import dart:ui; do NOT add an Offset or stroke field.
    Create `lib/tutor/tutor_decision.dart` with a sealed `TutorDecision` and the four subtypes `PresentActivity(coachingLine, letterId)`, `Say(text)`, `GiveHint()`, `Advance()` — and the 4 canonical tool-name constants (`present_activity`, `say`, `give_hint`, `advance`) as a single source of truth. No verdict/star shape exists.
    Create `lib/tutor/tutor_facts_builder.dart` with `TutorFacts buildTutorFacts({required String letterId, required String section, required CheckResult result, List<String> recentMistakes = const []})` — the ONE chokepoint. It maps `result.passed`/`result.mistakeId` into the DTO and derives `struggleTags` from `recentMistakes` (Claude's discretion on the tag derivation; keep it pure + deterministic). The signature MUST NOT accept strokes or a profile object.
  </action>
  <verify>
    <automated>flutter test test/tutor/tutor_facts_builder_test.dart -r expanded</automated>
  </verify>
  <acceptance_criteria>
    - `test/tutor/tutor_facts_builder_test.dart` exists and asserts: (a) a built TutorFacts.toMap() keys are a subset of {letterId, mistakeId, passed, section, struggleTags, recentMistakes}; (b) the serialized map contains no key matching /stroke|offset|nick|name|x|y|point/i; (c) a passing CheckResult → passed:true, mistakeId:null.
    - `! grep -rEq "dart:ui|package:firebase_ai|package:genui|flutter_gemma" lib/tutor/*.dart` PASSES (exits 0) — i.e. NO matches. NOTE the inverted exit code: a bare grep exits 0 on a MATCH; use the negated quiet form so a forbidden import FAILS the check and a clean tree passes (durable-imports clean in the tutor spine too — these are pure-Dart contract files).
    - `flutter analyze lib/tutor/` exits 0.
    - SC#3 mapped: tools are actions, facts arrive as data, no verdict tool shape exists (TUTOR-05).
  </acceptance_criteria>
  <done>TutorBrain/TutorFacts/TutorDecision compile; the chokepoint builder emits only whitelisted non-PII fields; the builder-payload test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuthoredFallback brain (offline floor) + the native dispatcher</name>
  <files>lib/tutor/authored_fallback_brain.dart, lib/tutor/tutor_dispatcher.dart, test/tutor/authored_fallback_brain_test.dart, test/tutor/tutor_dispatcher_test.dart</files>
  <read_first>
    - lib/tutor/tutor_brain.dart, lib/tutor/tutor_facts.dart, lib/tutor/tutor_decision.dart (Task 1 outputs)
    - lib/features/letter_unit/exercise_controller.dart (applyResult resolves feedback[mistakeId]/feedback['pass'] — AuthoredFallback mirrors this resolution)
    - lib/models/exercise.dart (Exercise.feedback map — the signed-off coaching lines source)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md §Decision parts 3 & 5 (the ~100-line dispatcher; the grounding-invariant enforcement summary)
    - .planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md (the canvas-rebuild kill-shot lesson: dispatch imperatively, never rebuild the canvas from agent state)
  </read_first>
  <behavior>
    - AuthoredFallbackBrain.next(facts): on facts.passed==true returns Say/PresentActivity carrying feedback['pass']; on a miss returns the line at feedback[facts.mistakeId], falling back to the first non-'pass' authored line — never an empty/generic string when any authored line exists.
    - AuthoredFallbackBrain is pure Dart: constructing it and calling next() touches no network, no Firebase, no model — it runs in a plain `flutter test` with no fakes.
    - The dispatcher maps each of the 4 tool names to one imperative TutorController method; an unrecognized name is a logged no-op that returns normally (no throw, no crash).
    - The dispatcher has no branch that sets pass/fail or awards a star.
  </behavior>
  <action>
    Create `lib/tutor/authored_fallback_brain.dart`: `class AuthoredFallbackBrain implements TutorBrain`. Inject the active exercise's `feedback` map (Map<String,String>) so it resolves the SAME authored line `ExerciseController.applyResult` would (`feedback['pass']` on pass, `feedback[mistakeId]` on a miss, first-non-pass authored line as the floor). Return a `Say` (or `PresentActivity` carrying the coaching line for the current letterId) `TutorDecision`. Zero model, airplane-mode safe (TUTOR-02). Pure Dart only.
    Create `lib/tutor/tutor_dispatcher.dart`: a small `TutorController` abstraction (interface with `say(String)`, `presentActivity(String letterId, String coachingLine)`, `giveHint()`, `advance()`) and a `dispatchTutorDecision(TutorDecision, TutorController)` that is a CLOSED `switch` over the sealed TutorDecision (equivalently a switch over the 4 tool-name constants). Unrecognized/default = `debugPrint` no-op, never a throw (ADR-014 §grounding 5). The dispatcher NEVER touches pass/fail or the star — there is no such branch.
  </action>
  <verify>
    <automated>flutter test test/tutor/authored_fallback_brain_test.dart test/tutor/tutor_dispatcher_test.dart -r expanded</automated>
  </verify>
  <acceptance_criteria>
    - `test/tutor/authored_fallback_brain_test.dart` asserts: a pass → decision line == feedback['pass']; a miss with a known mistakeId → line == feedback[mistakeId]; a miss with an unknown id → a non-empty authored floor line; runs with NO Firebase/network fake (pure).
    - `test/tutor/tutor_dispatcher_test.dart` asserts: each of the 4 decision shapes routes to its matching TutorController method (use a spy/recording fake controller); an unrecognized/extended case is a no-op that does not throw; the controller fake exposes no setVerdict/awardStar method that the dispatcher calls.
    - `! grep -rEq "package:firebase_ai|package:genui|flutter_gemma" lib/tutor/authored_fallback_brain.dart lib/tutor/tutor_dispatcher.dart` PASSES (no forbidden imports — negated quiet grep so a match fails, a clean file passes).
    - SC#3/SC#4 mapped: dispatcher is a closed switch over the 4 ACTION tools with no verdict path (TUTOR-05, GROUND-01).
  </acceptance_criteria>
  <done>AuthoredFallback coaches grounded authored lines offline with zero model; the dispatcher routes all 4 tools imperatively and no-ops the rest; both tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the spine into ExerciseScaffold via a tutor-owned provider — baa loop runs with the offline tutor, verdict still scorer-owned, ExerciseController untouched</name>
  <files>lib/tutor/tutor_providers.dart, lib/features/letter_unit/widgets/exercise_scaffold.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the _onResult seam at lines ~133-134; _centerSurface WriteSurface wiring; _TutorColumn at ~291 reads state.line via _bubbleText() at ~395-401 — THIS is what now reads the tutor provider)
    - lib/features/letter_unit/exercise_controller.dart (CONFIRMED: only load/think/applyResult/reset mutate state.line; there is NO public line-setter. applyResult already resolves feedback['pass']/feedback[mistakeId] into state.line. Do NOT add a setter; do NOT move pass/fail out of applyResult; do NOT edit this file at all.)
    - lib/tutor/tutor_facts_builder.dart, lib/tutor/authored_fallback_brain.dart, lib/tutor/tutor_decision.dart, lib/tutor/tutor_dispatcher.dart (the spine to wire)
    - lib/services/auth_service.dart (Firebase init/anon auth — read-only; GeminiBrain in 14-02 reuses it, not this plan)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md <decisions> Topology + Claude's Discretion (Riverpod provider wiring is discretion)
  </read_first>
  <action>
    Create `lib/tutor/tutor_providers.dart` with TWO providers (Riverpod only — CLAUDE.md Decided):
    (1) a provider exposing the active `TutorBrain` — for THIS plan it returns `AuthoredFallbackBrain` (the router that also picks Gemini/Gemma lands in 14-02/14-04; leave a documented single switch point);
    (2) a tutor-owned `tutorLineProvider` (a `NotifierProvider`/`StateProvider` under `lib/tutor/`, name it `tutorLineProvider`) that holds the brain's current `Say`/`PresentActivity` coaching line. This is the brain's defined PATH INTO THE UI and the reason ExerciseController needs no public line-setter: the dispatcher's `TutorController` adapter writes the brain line into `tutorLineProvider`, and `_TutorColumn` READS it.
    Modify `lib/features/letter_unit/widgets/exercise_scaffold.dart` (the ONLY `lib/features/letter_unit/` file this plan edits):
    • Keep `_onResult(CheckResult)` calling `ref.read(exerciseControllerProvider.notifier).applyResult(result)` FIRST and UNCHANGED — the scorer keeps owning pass/fail + the star, and applyResult still resolves its verdict-side `state.line` (GROUND-01).
    • THEN, additively, build `TutorFacts` via the chokepoint `buildTutorFacts(...)`, `await` the active `TutorBrain.next(facts)`, and dispatch the returned decision through `dispatchTutorDecision` into a `TutorController` adapter whose `say`/`presentActivity` WRITE the line into `tutorLineProvider` (NOT into ExerciseController — that controller exposes no line-setter and stays byte-for-byte unmodified).
    • Make `_TutorColumn` read `tutorLineProvider` (convert it to a ConsumerWidget or pass the watched line in) and render THAT as the bubble line. PRECEDENCE RULE (state explicitly in code + a comment): the brain line is authoritative; on the offline/AuthoredFallback path the brain line == the authored line, so `_TutorColumn` shows the brain-provider line and treats `applyResult`'s `state.line` as the verdict-side FALLBACK ONLY (used when `tutorLineProvider` is empty, e.g. before the brain has spoken). The tone/pose still come from `state` (the scorer verdict) — only the bubble TEXT comes from the tutor provider.
    • The StrokeCanvas stays a plain StatefulWidget dispatched-to imperatively — do NOT wrap it in any agent/reactive rebuild scope (Phase 11 kill-shot). On any brain error, leave `tutorLineProvider` empty so `_TutorColumn` falls back to applyResult's already-resolved authored `state.line` (never a dead end).
    Do NOT modify lib/features/letter_unit/exercise_controller.dart, lib/core/scoring/, lib/core/exercise_engine/, lib/features/practice/widgets/stroke_canvas.dart, or assets/curriculum/ (durable / contract-frozen, read-only).
  </action>
  <verify>
    <automated>flutter test test/letter_unit/ test/tutor/ -r expanded && flutter analyze lib/tutor/ lib/features/letter_unit/widgets/exercise_scaffold.dart && git diff --quiet HEAD -- lib/features/letter_unit/exercise_controller.dart</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD -- lib/features/letter_unit/exercise_controller.dart` is EMPTY — `exercise_controller.dart` is UNMODIFIED (the brain's line never required a public line-setter; it routes through `tutorLineProvider`). This is the durable-diff criterion AND avoids tripping 14-03's recursive import guard over `lib/features/letter_unit/`.
    - `_TutorColumn` READS `tutorLineProvider` (grep `tutorLineProvider` in exercise_scaffold.dart returns a match inside/feeding `_TutorColumn`); the bubble text comes from the tutor provider with applyResult's `state.line` as the verdict-side fallback only.
    - The baa Letter-Unit widget test still passes: a passing attempt shows a star + pass line; a miss shows the specific authored fix line (the tutor line is sourced via the spine + `tutorLineProvider`, not removed).
    - `! grep -rEq "package:firebase_ai|package:genui|flutter_gemma" lib/core/scoring/ lib/core/exercise_engine/ lib/features/practice/widgets/stroke_canvas.dart` PASSES (durable layers carry zero forbidden imports — TUTOR-01; negated quiet grep so a match fails, a clean tree passes; the binding guard test lands in 14-03).
    - `git diff --name-only HEAD -- lib/core/scoring/ lib/core/exercise_engine/ lib/features/practice/widgets/stroke_canvas.dart assets/curriculum/` is EMPTY (durable logic untouched).
    - The StrokeCanvas in WriteSurface is not placed under any new ref.watch/agent-state rebuild (manual code check + the canvas key/construction in write_surface.dart is unchanged).
    - SC#1/SC#2 (offline floor) + SC#4 (scorer-owns-verdict) mapped: swapping the brain provider changes no scorer/canvas/curriculum/controller code; applyResult still owns the verdict.
  </acceptance_criteria>
  <done>Entering baa runs the trace loop with the AuthoredFallback tutor supplying the line offline via tutorLineProvider; the scorer still owns pass/fail + star at applyResult; ExerciseController and durable layers untouched; tests + analyze green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child strokes → TutorFacts builder | The ONE chokepoint where in-memory strokes/verdict become a derived non-PII DTO. Raw Offsets and nickname/PII must be physically unable to cross (compile-time whitelist). |
| TutorBrain.next → UI dispatcher | Model/authored output crosses back as one of 4 ACTION tool calls only; it writes a LINE into tutorLineProvider, never a verdict or star. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information Disclosure | TutorFacts DTO / buildTutorFacts | mitigate | Whitelisted DTO with no stroke/PII field; toMap() emits only enum/id/string-list scalars; builder signature cannot accept Offsets (compile-time). Build-failing payload test (full grep test in 14-03). |
| T-14-01-02 | Elevation of Privilege | TutorDecision / dispatcher | mitigate | No verdict/star decision shape exists; dispatcher is a closed switch over 4 ACTION tools that only writes a LINE into tutorLineProvider — no set-pass branch; the agent cannot flip a fail to a pass (GROUND-01). |
| T-14-01-03 | Tampering | durable v1 layers (canvas/scorer/curriculum) + ExerciseController | mitigate | Tutor spine lives only in lib/tutor/ + plugs in at ExerciseScaffold via tutorLineProvider; ExerciseController is unmodified (git-diff empty); durable dirs carry zero firebase_ai/genui/flutter_gemma imports (grep clean; binding guard in 14-03); git-diff over durable paths stays empty. |
| T-14-01-04 | Denial of Service | brain call in the trace loop | accept | This plan's AuthoredFallback is synchronous/pure — no hang risk; on any brain error tutorLineProvider stays empty and _TutorColumn falls back to applyResult's authored line; the network timeout/degrade path is GeminiBrain's concern (14-02). |
</threat_model>

<verification>
- `flutter test test/tutor/ test/letter_unit/` green.
- `flutter analyze` exits 0 over lib/tutor/ and the modified scaffold.
- `git diff --name-only HEAD -- lib/features/letter_unit/exercise_controller.dart` empty (ExerciseController untouched).
- `! grep -rEq "package:firebase_ai|package:genui|flutter_gemma" lib/core/scoring/ lib/core/exercise_engine/ lib/features/practice/widgets/stroke_canvas.dart` passes (negated quiet grep — a match fails, a clean tree passes).
- `git diff --name-only HEAD -- lib/core/scoring/ lib/core/exercise_engine/ lib/features/practice/widgets/stroke_canvas.dart assets/curriculum/` empty.
</verification>

<success_criteria>
- TutorBrain interface + TutorFacts/TutorDecision + chokepoint builder + AuthoredFallback + dispatcher + tutorLineProvider exist under lib/tutor/.
- The baa trace loop runs end-to-end OFFLINE with the AuthoredFallback supplying a grounded, correctly-Arabic coaching line via tutorLineProvider (TUTOR-02).
- The agent acts only via the 4 ACTION tools; verdict + star arrive as FACTS and stay scorer-owned at applyResult (TUTOR-05, GROUND-01).
- Swapping the brain changes no canvas/scorer/curriculum/controller code; ExerciseController is byte-for-byte unmodified; durable layers carry zero forbidden imports (TUTOR-01).
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md` when done
</output>
</content>
</invoke>
