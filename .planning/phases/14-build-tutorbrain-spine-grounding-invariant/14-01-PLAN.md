---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/pyproject.toml
  - server/Dockerfile
  - server/.dockerignore
  - server/.env.example
  - server/app/__init__.py
  - server/app/main.py
  - server/app/schema.py
  - server/app/auth.py
  - server/app/graph.py
  - server/app/state.py
  - server/app/prompts.py
  - server/app/tools.py
  - server/tests/__init__.py
  - server/tests/conftest.py
  - server/tests/test_endpoint.py
  - server/tests/test_auth.py
  - server/README.md
autonomous: false
requirements: [TUTOR-03, TUTOR-05]
user_setup:
  - service: google-cloud-run
    why: "Host the Python LangGraph tutor server; the client calls it over REST/SSE (ADR-015 §1)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → API keys; stored in Secret Manager, injected as a Cloud Run env var — never in the image or the client (AI-SPEC §3 Cloud Run notes)"
      - name: GOOGLE_API_KEY
        source: "Google AI Studio → API keys (for Gemini per-node routing); Secret Manager → Cloud Run env var"
    dashboard_config:
      - task: "Create two Secret Manager secrets (ANTHROPIC_API_KEY, GOOGLE_API_KEY) in project qalam-app-bd7d0 and grant the Cloud Run runtime service account secretAccessor"
        location: "GCP Console → Security → Secret Manager (project qalam-app-bd7d0)"
      - task: "Enable Firebase App Check for the Android app (qalam-app-bd7d0) and register the Play Integrity provider so the client can mint limited-use tokens"
        location: "Firebase Console → App Check"

must_haves:
  truths:
    - "A POST to the /coach endpoint with a valid Firebase ID token + App Check token returns a grounded coaching line end-to-end"
    - "An unauthenticated request (missing/invalid ID token OR App Check token) is rejected before the graph runs"
    - "The deployed TutorFactsIn is the FULL enlarged FACTS contract from day one (the 6 base fields PLUS trajectory + strengthTags) and ACCEPTS the exact enlarged client payload while still rejecting any extra/PII key (extra=forbid)"
    - "Provider API keys are read from environment (Secret Manager on Cloud Run), never hard-coded or shipped in the image"
    - "The service deploys to Cloud Run and answers GET /healthz for the session-start warm-up ping"
  artifacts:
    - path: "server/app/main.py"
      provides: "FastAPI app: POST /coach + GET /healthz, App-Check + Firebase-ID-token dependency, asyncio.wait_for timeout"
      min_lines: 40
    - path: "server/app/auth.py"
      provides: "firebase-admin verification of the Firebase ID token + App Check limited-use token (FastAPI dependency)"
    - path: "server/app/schema.py"
      provides: "The SINGLE source of truth for the request DTO: the FULL enlarged TutorFactsIn (6 base fields + AttemptFactIn + trajectory + strengthTags, extra=forbid) + the CoachOut response DTO — the non-PII wire contract Plans 02/03/04 reference"
      contains: "class TutorFactsIn"
    - path: "server/app/graph.py"
      provides: "build_graph(): minimal one-node StateGraph (coach) returning one forced ACTION tool call"
    - path: "server/Dockerfile"
      provides: "python:3.12-slim + uvicorn entrypoint for Cloud Run"
    - path: "server/pyproject.toml"
      provides: "pinned deps (langgraph, langchain, langchain-anthropic, langchain-google-genai, fastapi, firebase-admin)"
  key_links:
    - from: "server/app/main.py"
      to: "server/app/auth.py"
      via: "FastAPI Depends(verify_caller) on POST /coach"
      pattern: "Depends\\(verify_caller\\)"
    - from: "server/app/main.py"
      to: "server/app/graph.py"
      via: "await graph.ainvoke under asyncio.wait_for"
      pattern: "ainvoke"
    - from: "server/app/graph.py"
      to: "server/app/tools.py"
      via: "coach node binds ACTION_TOOLS with tool_choice='any'"
      pattern: "tool_choice"
---

<objective>
Stand up the Python LangGraph tutor server sub-project and its secure deploy seam: a FastAPI app on Cloud Run with a POST /coach endpoint that verifies the caller (Firebase ID token + App Check), runs a MINIMAL one-node LangGraph graph (a single `coach` node forcing one of the 4 ACTION tools via `tool_choice="any"`), and returns a grounded coaching line end-to-end. Provider keys live in Secret Manager and reach the app only as env vars. This is MVP slice 1: the thinnest end-to-end server stack a real client can call. The full analyze→plan→coach graph (Plan 02) and the client wiring (Plan 03) build on this contract.

This plan also OWNS the request wire contract: `server/app/schema.py` defines the FULL, final enlarged `TutorFactsIn` (the canonical FACTS shape from CAPABLE-AGENT-SPEC: the 6 base fields PLUS `trajectory: list[AttemptFactIn]` + `strengthTags: list[str]`), with `extra="forbid"`, deployed from day one. The minimal one-node graph ignores the extra fields, but the DEPLOYED DTO is the final contract — Plans 02/03/04 build against it, never widen it.

Purpose: Establish the secure client→server boundary AND the single, final request/response DTO contract before any reasoning depth or client work — so Plans 02 and 03 build against a real, deployed, already-enlarged endpoint, not a sketch, and the live /coach never 422s a legit enlarged client payload.
Output: A deployable `server/` FastAPI + LangGraph sub-project, the final enlarged non-PII wire DTOs, the auth dependency, and a `gcloud run deploy` path.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@.planning/research/v2-tutor-architecture/CAPABLE-AGENT-SPEC.md
@docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md

# The wire-contract source of truth on the client side — the server DTO MUST mirror these field names (the enlarged shape lands in Plan 03)
@lib/tutor/tutor_facts.dart
@lib/tutor/tutor_decision.dart
@lib/firebase_options.dart
</context>

## Phase Goal

**As a** child practicing baa on the tablet, **I want to** receive a warm, grounded coaching line from the cloud tutor after I trace, **so that** I get a real teacher's specific feedback instead of a canned message — and the app never blocks waiting for it.

(This plan delivers the server end of that loop: a deployed endpoint that returns one grounded line. Plan 03 wires the client to it; Plan 02 deepens the reasoning.)

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the server/ sub-project + pinned deps + Dockerfile + auth dependency</name>
  <read_first>
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md (§3 Installation, Recommended Project Structure, Cloud Run deploy notes)
    - docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md (§1 topology, §4 grounding, §5 stateless)
    - lib/firebase_options.dart (the GCP project id qalam-app-bd7d0, the Android appId — the App Check audience)
  </read_first>
  <files>server/pyproject.toml, server/Dockerfile, server/.dockerignore, server/.env.example, server/app/__init__.py, server/app/auth.py, server/tests/__init__.py, server/tests/conftest.py, server/tests/test_auth.py, server/README.md</files>
  <action>
    Create the `server/` Python sub-project per AI-SPEC §3 "Recommended Project Structure". Use `uv` and a `pyproject.toml` with the AI-SPEC §3 pinned ranges: langgraph>=1.2,<2; langchain>=1.0,<2; langchain-anthropic>=1.0,<2; langchain-google-genai>=3.0,<4; fastapi>=0.115; uvicorn[standard]>=0.32; pydantic>=2.9,<3; sse-starlette>=2.1; firebase-admin (current GA) for token verification; and dev deps pytest + pytest-asyncio + httpx. Do a quick Context7/changelog check of the exact resolved langgraph/langchain versions at build (AI-SPEC §3 "pin at build") and record them in server/README.md. Target Python 3.12 (AI-SPEC pitfall 3: avoids the <3.11 async context-var footgun).

    Write the Dockerfile on `python:3.12-slim`, install deps via uv, and set the entrypoint to plain `uvicorn app.main:app --host 0.0.0.0 --port $PORT` (AI-SPEC §3 Cloud Run notes — do NOT use `langgraph dev`/LangGraph Platform; we own the transport). Add a .dockerignore excluding tests, .env, __pycache__.

    Write `app/auth.py` as a FastAPI dependency `verify_caller(request)` that: (a) reads the `Authorization: Bearer <idToken>` header and verifies it with `firebase_admin.auth.verify_id_token` (TUTOR-03 — client→server gated by Firebase ID token); (b) reads the App Check token header (`X-Firebase-AppCheck`) and verifies it with `firebase_admin.app_check.verify_token` as a limited-use token for replay protection (AI-SPEC §3 / ADR-015 §1); (c) raises HTTP 401 on either failure so the request never reaches the graph. Initialize `firebase_admin` once at import using Application Default Credentials (Cloud Run runtime SA) for project qalam-app-bd7d0. Read NO provider keys here — keys are env-only and used in models (Plan 02). Put the GCP project id in `.env.example` with ANTHROPIC_API_KEY / GOOGLE_API_KEY placeholders and a comment that real values live in Secret Manager, never committed.

    Write `tests/conftest.py` with a pytest-asyncio httpx ASGI client fixture and a fixture that monkeypatches `verify_id_token` / `verify_token` so endpoint tests do not hit Firebase. Write `tests/test_auth.py` asserting: a request with no/invalid Authorization header → 401; a request with a valid ID token but missing/invalid App Check token → 401 (mark these `code`). Document the `gcloud run deploy` one-liner (with `--set-secrets ANTHROPIC_API_KEY=...:latest,GOOGLE_API_KEY=...:latest`, `--timeout`, `--min-instances=0`) in server/README.md per AI-SPEC §3.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_auth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/pyproject.toml` pins langgraph/langchain/langchain-anthropic/langchain-google-genai/fastapi/firebase-admin within the AI-SPEC §3 ranges; resolved versions recorded in server/README.md.
    - `server/Dockerfile` uses python:3.12-slim and a plain uvicorn entrypoint (no langgraph-cli / LangGraph Platform).
    - `verify_caller` rejects a missing/invalid Firebase ID token with 401 AND a missing/invalid App Check token with 401, before the graph runs.
    - No provider API key string appears anywhere in `server/` (keys are env-only); `grep -rEi "sk-ant|AIza[0-9A-Za-z_-]{20,}" server/` returns nothing.
  </acceptance_criteria>
  <done>The server sub-project exists with pinned deps, a Cloud Run Dockerfile, and an auth dependency that gates the endpoint on both tokens; auth tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: The FINAL enlarged non-PII wire DTOs (the single contract) + minimal one-node graph + the /coach and /healthz endpoints</name>
  <read_first>
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md (§3 Entry Point Pattern, Async-First Design 4b, §4 Tool Use, §6 G1/G5)
    - .planning/research/v2-tutor-architecture/CAPABLE-AGENT-SPEC.md (the canonical enlarged FACTS shape: the attempt trajectory + the session learner model — strengthTags as well as struggleTags)
    - lib/tutor/tutor_facts.dart (the 6 base whitelisted fields the request DTO mirrors: letterId, section, passed, mistakeId, struggleTags, recentMistakes — Plan 03 enlarges the CLIENT type to add trajectory + strengthTags to MATCH this DTO)
    - lib/tutor/tutor_decision.dart (the 4 ACTION tool names + their payloads — the response must serialize to these)
  </read_first>
  <files>server/app/schema.py, server/app/state.py, server/app/tools.py, server/app/prompts.py, server/app/graph.py, server/app/main.py, server/tests/test_endpoint.py</files>
  <action>
    Write `app/schema.py` as the SINGLE source of truth for the request wire contract — the FULL, final enlarged `TutorFactsIn`, deployed from day one (per CAPABLE-AGENT-SPEC: TutorFacts carries the attempt trajectory + the session learner model, not just the last mistakeId). Define a new nested model `AttemptFactIn` (the per-attempt non-PII record: `passed: bool`, `mistakeId: str | None`, `section: str`) with its OWN `model_config = ConfigDict(extra="forbid")`. Then `TutorFactsIn` with ALL of: the 6 base fields — `letterId: str`, `section: str`, `passed: bool`, `mistakeId: str | None`, `struggleTags: list[str]`, `recentMistakes: list[str]` — PLUS the enlarged fields `trajectory: list[AttemptFactIn]` and `strengthTags: list[str]`, and `model_config = ConfigDict(extra="forbid")` so any unexpected field (a leaked stroke/PII key) is a 422, not silently accepted (supports the GROUND-02 stance from the server side). This is the final contract: do NOT plan to widen it later — Plans 02/03/04 reference THIS definition, they do not redefine or extend it.

    Write the response DTO `CoachOut` carrying the chosen tool name (Literal over the 4 names) + its args (`coachingLine`/`text`/`letterId` optional), the source (`"agent"`), and a `grounded: bool`. (Plan 02 adds the internal Insight/Plan models + sets `CoachOut.grounded`; it does NOT touch `TutorFactsIn`.)

    Write `app/state.py` (`TutorState` TypedDict with `facts: dict`, `decision: dict`, `log: Annotated[list, add]` — AI-SPEC pitfall 1: the reducer on the accumulator) and `app/tools.py` (the 4 `@tool` ACTION functions `present_activity`, `say`, `give_hint`, `advance` exactly as AI-SPEC §4; the server does NOT execute them — they exist to be bound and forced). Write `app/prompts.py` with a minimal COACH_PROMPT carrying the mother's-voice register rule + the grounding rule "never contradict the verdict; on a fail, never `advance` and never claim success" (cache-stable system prefix per 4b). Keep ANALYZE/PLAN prompts as stubs/placeholders Plan 02 fills.

    Write `app/graph.py` `build_graph()`: a SINGLE `coach` node (START→coach→END) that binds `coach_model = init_chat_model(...)` (read model id + provider from env, default Claude Haiku per the §4 routing table) with `.bind_tools(ACTION_TOOLS, tool_choice="any")` (AI-SPEC §4 — the action-space lock, GROUND-01/G2), injects the FACTS as TEXT in a HumanMessage (the minimal node may ignore the enlarged trajectory/strengthTags fields — Plan 02's analyze node consumes them; never a responseSchema combined with tools — ADR-014 §2/AI-SPEC), and writes `state["decision"] = resp.tool_calls[0]`. Apply a server-side grounding guard (G3): if the forced tool is `advance` but `facts.passed` is false, replace it with a grounded `say` and set `grounded=False`-equivalent handling. Compile with `InMemorySaver()` (AI-SPEC pitfall 4 — stateless server).

    Write `app/main.py`: the FastAPI app; `GET /healthz` → 200 (the warm-up ping, AI-SPEC §3 Cloud Run); `POST /coach` gated by `Depends(verify_caller)` (Plan-01 Task-1 dependency), parsing `TutorFactsIn`, calling `await asyncio.wait_for(graph.ainvoke(...), timeout=<budget>)` (AI-SPEC §3 timeout note / G5 — a slow model returns fast so the client degrades), mapping `state["decision"]` to `CoachOut`. On timeout/exception return a structured error status the client maps to AuthoredFallback (do NOT 200-with-empty). Use `await`, never `asyncio.run` inside the route (4b Async-First).

    Write `tests/test_endpoint.py`: with the auth dependency overridden and the coach model monkeypatched to return a forced `say` tool call — (1) POST a sample fail-verdict TutorFactsIn carrying the FULL enlarged payload (a populated `trajectory` list of AttemptFactIn + `strengthTags`) → 200 with `CoachOut.toolName == "say"` (proves the deployed DTO ACCEPTS the exact enlarged client payload); (2) POST with an extra `strokes` field (and separately `x`/`y`/`childName`) → 422 (extra=forbid, on both TutorFactsIn and the nested AttemptFactIn); (3) assert an `advance`-on-fail forced call is rewritten to a grounded `say` (G3). Mark model-free assertions `code`.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_endpoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `TutorFactsIn` is the FULL enlarged contract: the 6 base fields PLUS `trajectory: list[AttemptFactIn]` + `strengthTags: list[str]`, with `extra="forbid"` on both `TutorFactsIn` and `AttemptFactIn`.
    - The deployed DTO ACCEPTS the exact enlarged client payload (populated trajectory + strengthTags) with 200, AND rejects each of {strokes, x, y, childName, nickname} with 422 (extra=forbid). This is the day-one contract — no "widen later" gap.
    - `POST /coach` requires `Depends(verify_caller)` and returns a `CoachOut` whose `toolName` is one of {present_activity, say, give_hint, advance}.
    - The coach node binds `tool_choice="any"` and the response is exactly one tool call; an `advance`-on-fail is rewritten to a grounded `say` (G3).
    - `GET /healthz` returns 200 with no auth required (warm-up ping); the graph runs under `asyncio.wait_for` and a timeout returns a non-200 error the client can degrade on.
  </acceptance_criteria>
  <done>A valid authenticated /coach request carrying the full enlarged payload returns one grounded ACTION via the minimal graph; the DTO is the locked, final, enlarged non-PII contract; /healthz answers; the build is Cloud-Run-deployable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The server is deployed to Cloud Run with secrets wired. Claude has: created the Secret Manager secrets (or printed the exact `gcloud secrets create` + `gcloud run deploy --set-secrets` commands if it lacks gcloud auth), run the deploy, and curled the live endpoint. The endpoint verifies a Firebase ID token + App Check token and returns a grounded coaching line.</what-built>
  <how-to-verify>
    1. Confirm the Cloud Run service URL printed by the deploy (project qalam-app-bd7d0).
    2. Hit `GET <url>/healthz` in a browser or `curl` — expect 200.
    3. Confirm `curl -XPOST <url>/coach` WITHOUT an Authorization header returns 401 (the endpoint is not open).
    4. Confirm in the GCP Console that ANTHROPIC_API_KEY and GOOGLE_API_KEY are Secret Manager references on the service (NOT plaintext env values), and that no key is in the container image.
    5. Confirm Firebase App Check shows the Android app registered with Play Integrity.
  </how-to-verify>
  <resume-signal>Type "approved" once the live endpoint answers /healthz, rejects unauthenticated /coach, and keys are Secret Manager references — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → Cloud Run /coach | Untrusted child device crosses the network; must present a valid Firebase ID token + App Check token |
| Cloud Run app → Secret Manager | Provider keys injected as env vars by the runtime SA; never in image/client |
| Request body → graph | The non-PII FACTS payload; only the enlarged whitelist (6 base fields + trajectory[AttemptFactIn] + strengthTags) may enter (extra=forbid on both models) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | POST /coach caller | mitigate | `verify_caller` verifies the Firebase ID token via firebase-admin; no token → 401 before the graph (Task 1) |
| T-14-02 | Tampering/Replay | client→server token | mitigate | App Check limited-use token verified per request (`X-Firebase-AppCheck`); replay-protected (Task 1) |
| T-14-03 | Information Disclosure | request body | mitigate | `TutorFactsIn` + nested `AttemptFactIn` extra=forbid → any leaked stroke/PII field is a 422 (Task 2); GROUND-02 build-failing test lands in Plan 04 |
| T-14-04 | Information Disclosure | provider API keys | mitigate | Keys in Secret Manager → env vars; grep gate asserts no key string in `server/`; image excludes .env (Task 1) |
| T-14-05 | Denial of Service | slow/hung model call | mitigate | `asyncio.wait_for` + Cloud Run `--timeout`; timeout returns fast → client degrades to AuthoredFallback (Task 2 / G5) |
| T-14-06 | Elevation | agent emits a verdict/5th action | mitigate | `tool_choice="any"` pins the closed 4-tool set; `advance`-on-fail rewritten to grounded `say` (Task 2 / G2,G3) |
| T-14-SC | Tampering | pip installs (langgraph/langchain/firebase-admin) | mitigate | RESEARCH Package Legitimacy Audit + pinned versions; all are GA, widely-used PyPI packages (see verification note below) |
</threat_model>

<verification>
- `cd server && uv run pytest -q` passes (auth + endpoint tests, model-free `code` checks).
- Live: `GET /healthz` → 200; unauthenticated `POST /coach` → 401; keys are Secret Manager references (checkpoint).
- `grep -rEi "sk-ant|AIza[0-9A-Za-z_-]{20,}" server/` returns nothing (no committed keys).
- The deployed `TutorFactsIn` accepts the exact enlarged client payload (trajectory + strengthTags) AND rejects extra/PII keys — verified by the Task-2 endpoint tests (this is the single, final contract Plan 03's client and Plan 04's guard build on).

Note (package legitimacy): the 14-RESEARCH/AI-SPEC dependency set is langgraph, langchain, langchain-anthropic, langchain-google-genai, fastapi, uvicorn, pydantic, sse-starlette, firebase-admin — all GA, first-party or de-facto-standard PyPI packages named directly in AI-SPEC §3 (sourced from official LangChain docs). If a formal `## Package Legitimacy Audit` table is absent from RESEARCH, treat each as `[ASSUMED]` and the executor verifies `pypi.org/project/<name>` before `uv pip install`; none are `[SUS]`/`[SLOP]`.
</verification>

<success_criteria>
- A deployed Cloud Run service answers `/healthz` and serves authenticated `/coach` requests with a grounded ACTION (TUTOR-03 partial, TUTOR-05 structural lock).
- Provider keys live only in Secret Manager; the client and image carry none (TUTOR-03).
- The request DTO is the locked, FINAL, enlarged non-PII contract Plan 03's client mirrors and Plan 04's guard builds on — accepts the full enlarged payload, rejects extra/PII keys, never widened later.
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md` when done — record the resolved langgraph/langchain versions, the Cloud Run service URL, the FULL enlarged TutorFactsIn field set (6 base + AttemptFactIn + trajectory + strengthTags), and the deploy command.
</output>
</output>
