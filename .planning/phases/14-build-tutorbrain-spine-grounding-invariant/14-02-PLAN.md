---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - server/app/graph.py
  - server/app/state.py
  - server/app/models.py
  - server/app/prompts.py
  - server/app/schema.py
  - server/app/nodes/__init__.py
  - server/app/nodes/analyze.py
  - server/app/nodes/plan.py
  - server/app/nodes/coach.py
  - server/app/curriculum.py
  - server/app/main.py
  - server/tests/test_grounding.py
  - server/tests/test_graph.py
autonomous: true

requirements: [TUTOR-05, GROUND-01]

must_haves:
  truths:
    - "On a struggle the graph runs analyze → plan → coach; on a clean pass it runs analyze → coach (one cheap hop), via a conditional edge"
    - "Each node binds its own model via init_chat_model (per-node routing), eval-tunable from env"
    - "The coach node emits exactly one of the 4 ACTION tools (tool_choice='any'); no verdict/star tool exists"
    - "advance (action or Plan.intent) is impossible when facts.passed is false — the agent cannot flip a fail to a pass"
    - "present_activity.letter_id / Plan.next_exercise_id outside the authored baa config set is rejected to AuthoredFallback (curriculum guard)"
    - "A malformed/None structured reply retries twice then fails closed (the client degrades to AuthoredFallback)"
  artifacts:
    - path: "server/app/nodes/analyze.py"
      provides: "analyze node + Insight Pydantic schema (struggle_tags/strength_tags/pattern_note)"
      contains: "class Insight"
    - path: "server/app/nodes/plan.py"
      provides: "plan node + Plan Pydantic schema (next_exercise_id/intent/rationale), curriculum-membership guard"
      contains: "class Plan"
    - path: "server/app/nodes/coach.py"
      provides: "coach node binding the 4 ACTION tools with tool_choice='any' + grounding guard"
    - path: "server/app/models.py"
      provides: "per-node init_chat_model routing table, env-driven"
    - path: "server/app/graph.py"
      provides: "StateGraph: analyze → {plan|coach} → coach → END with conditional edge"
      contains: "add_conditional_edges"
    - path: "server/app/curriculum.py"
      provides: "the authored, signed-off baa exercise/letter id set the curriculum guard validates against"
  key_links:
    - from: "server/app/graph.py"
      to: "server/app/nodes/analyze.py"
      via: "add_node(analyze) + add_edge(START, analyze)"
      pattern: "add_node"
    - from: "server/app/graph.py"
      to: "needs_plan router"
      via: "add_conditional_edges('analyze', needs_plan)"
      pattern: "add_conditional_edges"
    - from: "server/app/nodes/coach.py"
      to: "server/app/tools.py"
      via: "bind_tools(ACTION_TOOLS, tool_choice='any')"
      pattern: "tool_choice=.any."
    - from: "server/app/nodes/plan.py"
      to: "server/app/curriculum.py"
      via: "validate next_exercise_id ∈ AUTHORED_BAA_IDS after parse"
      pattern: "AUTHORED_BAA"
---

<objective>
Deepen the minimal graph from Plan 01 into the full grounded agent: an `analyze → plan → coach` StateGraph with a conditional edge (clean pass = one cheap hop, struggle = three), per-node model routing via `init_chat_model` (model-agnostic, env-tunable), structured `Insight`/`Plan` outputs with bounded retry, the 4 ACTION tools forced via `tool_choice="any"`, FACTS-as-text, and the server-side grounding invariant (no verdict tool; `advance` impossible on a fail; curriculum-membership guard). This is MVP slice 2: it replaces the Plan-01 stub coach with the real reasoning the child's line is grounded in.

Purpose: Make the coaching line genuinely respond to the child's scored attempt and recent struggles while structurally guaranteeing the agent can never override the deterministic verdict (GROUND-01) and can only act through the closed 4-tool space (TUTOR-05).
Output: The complete analyze/plan/coach node set, the routing table, the curriculum guard, and the grounding tests.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
@docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md

# This plan builds on the Plan 01 server skeleton — read its SUMMARY for the resolved versions + DTO contract
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md
</context>

## Phase Goal

**As a** child who keeps over-curving baa's boat-body, **I want to** the tutor to notice the repeated mistake, plan a focused next step, and name the exact fix, **so that** the feedback teaches the right correction the way a teacher beside me would — and never tells me "great job" on a stroke that failed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: analyze + plan nodes with structured output, bounded retry, and the curriculum guard</name>
  <read_first>
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md (§3 Entry Point Pattern, §4b Structured Outputs with Pydantic + retry logic, §4 Model Configuration table, §1b domain rubric D2/D3/D4/D8)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md (resolved langgraph version; the TutorState + schema shapes)
    - server/app/state.py (the TutorState from Plan 01 — add insight/plan fields)
  </read_first>
  <files>server/app/models.py, server/app/state.py, server/app/curriculum.py, server/app/prompts.py, server/app/nodes/__init__.py, server/app/nodes/analyze.py, server/app/nodes/plan.py, server/tests/test_graph.py</files>
  <behavior>
    - Given a fail trajectory with a repeated `shallowBowl`, analyze returns an Insight whose `struggle_tags` includes the boat-curve struggle (not the dot).
    - Given a clean pass with no struggle, analyze returns empty `struggle_tags` (so the router skips plan).
    - Given an Insight with a struggle, plan returns a `Plan` whose `next_exercise_id` is in the authored baa set and whose `intent` is a drill/retest (not `advance`) — on a fail, `intent == "advance"` is rejected.
    - A `next_exercise_id` NOT in the authored baa set → the curriculum guard raises → the run fails closed (caller maps to AuthoredFallback).
    - A model reply that fails Pydantic validation retries twice, then fails closed (no infinite loop; AI-SPEC §4b).
  </behavior>
  <action>
    Write `app/models.py`: a routing table that builds each node's model with `init_chat_model(model, model_provider=..., temperature=..., max_tokens=...)`, reading the model id/provider per node from env with the AI-SPEC §4 defaults (analyze=gemini-2.5-flash temp 0 max 512; plan=claude-sonnet-4-6 temp 0.2 max 512; coach=claude-haiku-4-5 temp ~0.5 max 256). Confirm the exact model strings at build (AI-SPEC §4 — Gemini/Claude naming shifts). Set `max_tokens` on EVERY node (AI-SPEC §4b — never unbounded). Keep it env-driven so the eval bake-off can retune without code change.

    Extend `app/state.py` `TutorState` with `insight: dict` and `plan: dict` (keep the `Annotated[list, add]` reducer on `log` — AI-SPEC pitfall 1).

    Write `app/curriculum.py`: `AUTHORED_BAA_IDS: frozenset[str]` — the signed-off baa exercise/section ids (source them from the client's baa curriculum content; if a single canonical list is not yet exported, define the known baa section ids used in the app and note the source in the SUMMARY). Provide `is_authored(exercise_id) -> bool` (G4 curriculum membership — Pydantic validates shape, this validates curriculum).

    Write `app/nodes/analyze.py`: the `Insight` Pydantic model (struggle_tags, strength_tags, pattern_note per AI-SPEC §4b) and an `analyze(state)` node calling `analyze_model.with_structured_output(Insight)` with the ANALYZE_PROMPT (system) + FACTS as TEXT (HumanMessage). Wrap the call in a bounded retry helper (2 retries on `pydantic.ValidationError`/None, then raise a typed `StructuredOutputError`) — log node/model/raw-text/error each retry (AI-SPEC §4b).

    Write `app/nodes/plan.py`: the `Plan` Pydantic model (next_exercise_id, intent Literal['drill_isolated','retest_whole','hint','advance'], rationale) and a `plan(state)` node, same retry wrapper. AFTER parse, enforce two grounding guards: (1) `is_authored(plan.next_exercise_id)` else raise (G4); (2) `plan.intent == "advance"` requires `state["facts"]["passed"] is True` else downgrade intent to `retest_whole` (G3 — cannot advance on a fail). Fill ANALYZE_PROMPT + PLAN_PROMPT in `app/prompts.py` (cache-stable system prefixes carrying the baa curriculum facts + the grounding rule).

    Write `tests/test_graph.py` exercising the behaviors above with the node models monkeypatched to return canned structured objects (model-free, mark `code`): assert struggle detection, the clean-pass empty-struggle case, the curriculum-guard rejection, the advance-on-fail downgrade, and the 2-retry-then-raise path.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Each node model is built via `init_chat_model` with an explicit `max_tokens`, read from env with the §4 defaults.
    - `analyze` returns a validated `Insight`; `plan` returns a validated `Plan`; both retry twice then raise a typed error (no infinite loop).
    - `Plan.next_exercise_id` not in `AUTHORED_BAA_IDS` raises (curriculum guard); `Plan.intent == "advance"` on a `passed=false` fact is downgraded.
    - `TutorState` keeps the `Annotated[list, add]` reducer on `log`.
  </acceptance_criteria>
  <done>analyze + plan produce validated, grounded structured outputs with bounded retry and a curriculum-membership guard; the agent cannot plan an advance on a fail or an unauthored exercise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coach node + conditional-edge graph wiring + server-side grounding guards + grounding tests</name>
  <read_first>
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md (§3 Entry Point Pattern + Key Abstractions, §4 Tool Use, §5 D1/D7/D9, §6 G2/G3/G4/G5)
    - server/app/tools.py (the 4 ACTION tool defs from Plan 01)
    - server/app/main.py (the /coach handler from Plan 01 — update the retry→degrade mapping)
  </read_first>
  <files>server/app/nodes/coach.py, server/app/graph.py, server/app/main.py, server/app/schema.py, server/tests/test_grounding.py</files>
  <action>
    Write `app/nodes/coach.py`: a `coach(state)` node binding the 4 ACTION tools `coach_model.bind_tools(ACTION_TOOLS, tool_choice="any")` (AI-SPEC §4 — the action-space lock, GROUND-01/TUTOR-05/G2), injecting `{facts, insight, plan}` as TEXT (HumanMessage) with the COACH_PROMPT (mother's-voice + grounding rule + the small fixed gold few-shot exemplars per AI-SPEC §4b — "deeper curve at the bottom" vs "Oops, try again!"). Apply the online grounding guards (G3): if the forced tool is `advance` and `facts.passed` is false, rewrite to a grounded `say` and flag; if `present_activity.letter_id` is not in `AUTHORED_BAA_IDS`, reject to a `say`/degrade (G4). Set `state["decision"]` to the single chosen tool call.

    Rewire `app/graph.py` `build_graph()` to the full DAG (AI-SPEC §3 Entry Point Pattern): add_node analyze/plan/coach; `add_edge(START, "analyze")`; `add_conditional_edges("analyze", needs_plan)` where `needs_plan` returns `"coach"` when `facts.passed and not insight.struggle_tags` else `"plan"`; `add_edge("plan","coach")`; `add_edge("coach", END)`; compile with `InMemorySaver()`. Remove the Plan-01 single-node stub.

    Update `app/main.py`: on a `StructuredOutputError`/timeout/exception from the graph, return the structured non-200 error (AI-SPEC §6 G5) the client maps to AuthoredFallback — never block, never a raw stack trace. Extend `CoachOut` with a `grounded: bool` reflecting whether a guard rewrote the action.

    Write `tests/test_grounding.py` (the D1/D7/D9 hard checks, all `code`, model monkeypatched): (D7) the decision is exactly one tool call whose name ∈ the 4-set; (D1/G3) a forced `advance` with `facts.passed=false` is rewritten to `say` and `advance` is never emitted on a fail; (G4) a `present_activity` with an unauthored letter_id is rejected; (D9/G5) an injected timeout / StructuredOutputError yields the degrade error the client uses for AuthoredFallback within the timeout budget; (routing) a clean pass with no struggle takes analyze→coach (plan node not invoked) while a struggle takes analyze→plan→coach.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_grounding.py tests/test_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `coach` binds `tool_choice="any"` over exactly the 4 ACTION tools; the decision is always exactly one in-set tool call (D7).
    - `advance` is never emitted (action or plan intent) when `facts.passed` is false — verified by a grounding test (D1/G3); an unauthored `present_activity` letter_id is rejected (G4).
    - The graph routes analyze→coach on a clean no-struggle pass and analyze→plan→coach on a struggle (conditional edge), verified by the routing test.
    - A timeout/StructuredOutputError returns a structured non-200 the client maps to AuthoredFallback (D9/G5) — never a 200-with-empty, never a raw stack.
  </acceptance_criteria>
  <done>The full analyze→plan→coach graph emits exactly one grounded ACTION per request, structurally cannot contradict the verdict, and fails closed to the client's offline floor on any error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen verdict (facts) → agent nodes | The scorer's pass/fail enters as an immutable FACT; no node may re-judge or flip it |
| model reply → state | Untrusted model output; structured nodes validate via Pydantic, coach via forced tool-calling |
| Plan.next_exercise_id / present_activity.letter_id → curriculum | The agent must reference only authored, signed-off baa ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Elevation | coach/plan emitting `advance` on a fail | mitigate | G3: `advance` rewritten/downgraded when `facts.passed` is false, in both plan (intent) and coach (action) — Tasks 1 & 2 |
| T-14-08 | Spoofing | model fabricates an exercise id | mitigate | G4 curriculum guard: `next_exercise_id`/`letter_id` ∉ AUTHORED_BAA_IDS → rejected to AuthoredFallback (Tasks 1 & 2) |
| T-14-09 | Tampering | model emits a 5th action / free text | mitigate | G2: `tool_choice="any"` forces exactly one of the 4 tools; the model cannot emit a 5th (Task 2) |
| T-14-10 | DoS | malformed reply loops / hangs | mitigate | Bounded 2-retry then fail-closed; `asyncio.wait_for` budget; degrade to AuthoredFallback (Tasks 1 & 2 / G5) |
| T-14-11 | Repudiation | which guard fired is invisible | mitigate | `log` accumulator + `CoachOut.grounded` flag record when a guard rewrote the action (Task 2) |
| T-14-SC | Tampering | no new package installs in this plan | accept | Plan reuses Plan-01's pinned deps; no new installs → no new supply-chain surface |
</threat_model>

<verification>
- `cd server && uv run pytest -q` passes (graph + grounding + the Plan-01 endpoint/auth tests).
- The grounding hard checks (D1/D7/D9) are green and run model-free in CI.
- Manual: a sample struggle FACTS through a real model (with keys set locally) yields a coaching line that names the flagged fix and never claims success on a fail (D1/D2 — full eval harness is Phase 16, this is a smoke check).
</verification>

<success_criteria>
- The analyze→plan→coach graph runs with per-node routing and a conditional edge (TUTOR-05).
- The agent acts only through the 4 ACTION tools and cannot flip a fail to a pass or reference an unauthored exercise (GROUND-01, TUTOR-05).
- Any model/validation/timeout failure fails closed so the client degrades to AuthoredFallback (G5/D9).
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-02-SUMMARY.md` when done — record the per-node model assignments used, the AUTHORED_BAA_IDS source, and the grounding test coverage.
</output>
