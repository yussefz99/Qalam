---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - test/tutor/grounding_non_pii_payload_test.dart
  - test/tutor/durable_layers_no_forbidden_imports_test.dart
autonomous: true
requirements: [GROUND-02, TUTOR-01, GROUND-01]
must_haves:
  truths:
    - "A build-failing test asserts the serialized TutorFacts network payload contains ONLY derived non-PII fields — if raw stroke coordinates or any PII field (nickname/name/profile) can reach the payload, the build goes RED (GROUND-02)."
    - "A guard test fails the build if any durable v1 layer (canvas / scorer / curriculum / exercise-engine) carries a firebase_ai / genui / flutter_gemma import (TUTOR-01)."
    - "Together the two guards encode the grounding invariant as an executable, non-skippable gate (GROUND-01/02, TUTOR-01)."
  artifacts:
    - path: "test/tutor/grounding_non_pii_payload_test.dart"
      provides: "the GROUND-02 build-failing payload guard over the TutorFacts chokepoint"
      contains: "buildTutorFacts"
    - path: "test/tutor/durable_layers_no_forbidden_imports_test.dart"
      provides: "the TUTOR-01 forbidden-import grep guard over the durable dirs"
      contains: "firebase_ai"
  key_links:
    - from: "test/tutor/grounding_non_pii_payload_test.dart"
      to: "lib/tutor/tutor_facts_builder.dart"
      via: "builds TutorFacts from a CheckResult + a synthetic stroke/PII attempt and asserts the serialized payload has no stroke/PII key"
      pattern: "toMap|toJson"
    - from: "test/tutor/durable_layers_no_forbidden_imports_test.dart"
      to: "lib/core/scoring/ lib/core/exercise_engine/ lib/features/practice/widgets/stroke_canvas.dart lib/features/letter_unit/"
      via: "reads each durable .dart file and asserts no forbidden import line"
      pattern: "firebase_ai|genui|flutter_gemma"
---

<objective>
Lock the grounding invariant down with two executable, build-failing guards: (1) the
GROUND-02 non-PII payload test over the `TutorFacts` chokepoint — proving raw strokes
and PII physically cannot reach the network payload; and (2) the TUTOR-01 forbidden-import
guard — proving the durable v1 layers (canvas, scorer, curriculum, exercise-engine) carry
zero `firebase_ai` / `genui` / `flutter_gemma` imports.

Purpose: Make the two hardest invariants of ADR-014 non-negotiable and self-enforcing —
they fail the build the moment someone leaks a stroke/PII field or pulls a forbidden
import into a durable layer.

Output: two guard tests under `test/tutor/`. This plan adds NO production code and shares
no files with 14-02, so it runs parallel in Wave 2.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md
</context>

## Phase Goal

**As a** parent trusting Qalam with my child's data, **I want to** know that raw strokes
and my child's nickname can never leave the device and the tutor can never tamper with
the scorer, **so that** the grounding + child-safety promise is enforced by the build
itself, not by reviewer vigilance.

<tasks>

<task type="auto">
  <name>Task 1: GROUND-02 — the build-failing non-PII network-payload guard</name>
  <files>test/tutor/grounding_non_pii_payload_test.dart</files>
  <read_first>
    - lib/tutor/tutor_facts_builder.dart (the ONE chokepoint — what this test pins)
    - lib/tutor/tutor_facts.dart (the DTO + toMap/toJson whitelist)
    - lib/core/exercise_engine/check_result.dart (CheckResult — the verdict the facts derive from; note its own T-07-03-01 security comment: only bool + authored key)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md §grounding part 3 (a unit test asserts no raw strokes / no PII in the serialized payload — GROUND-02)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md <decisions> Grounding invariant item 4 (build-failing)
  </read_first>
  <action>
    Create `test/tutor/grounding_non_pii_payload_test.dart`: build a `TutorFacts` through the chokepoint `buildTutorFacts(...)` for both a pass and a miss, serialize it (`toMap()`/`toJson()`), and assert: (a) every key is in the allowed set {letterId, mistakeId, passed, section, struggleTags, recentMistakes}; (b) no key OR stringified value matches the forbidden pattern /stroke|offset|coordinate|\bx\b|\by\b|point|nick|name|profile|avatar|pii/i; (c) every leaf value is a String, bool, or List<String> — no num list / Offset / nested object that could smuggle coordinates. Add a "defense" case proving the builder signature cannot even accept strokes (a comment + the absence of any stroke param — assert via the public API shape). The test is build-failing: a regression that adds a stroke/PII field to TutorFacts turns it RED.
    Add NO production code. If 14-01's TutorFacts already exposes a leaky field, the test going RED is the correct signal (do not weaken the test to pass).
  </action>
  <verify>
    <automated>flutter test test/tutor/grounding_non_pii_payload_test.dart -r expanded</automated>
  </verify>
  <acceptance_criteria>
    - The test exists, builds TutorFacts via `buildTutorFacts`, and asserts the serialized payload key-set ⊆ {letterId, mistakeId, passed, section, struggleTags, recentMistakes}.
    - The test asserts no key/value matches /stroke|offset|coordinate|point|nick|name|profile|avatar|pii/i and every leaf is String/bool/List<String>.
    - Running it on the current spine is GREEN (the 14-01 DTO is already clean); a deliberately-added stroke field would make it RED (documented in the test as the regression it catches).
    - SC#5 mapped: a guard/test fails the build if raw strokes or any PII field can reach the network payload (GROUND-02).
  </acceptance_criteria>
  <done>The non-PII payload guard passes on the clean spine and would fail the build on any stroke/PII leak into TutorFacts.</done>
</task>

<task type="auto">
  <name>Task 2: TUTOR-01 — durable-layers-carry-no-forbidden-imports guard</name>
  <files>test/tutor/durable_layers_no_forbidden_imports_test.dart</files>
  <read_first>
    - test/spike_genui/durable_layers_unchanged_test.dart (the Phase 11 git-diff guard — the PATTERN to adapt; this plan replaces the throwaway git-diff semantics with a durable forbidden-IMPORT grep because Phase 14 legitimately edits exercise_scaffold.dart)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md §Decision part 4 last line (durable v1 layers carry ZERO firebase_ai/genui/flutter_gemma imports) + §grounding
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md <decisions> TutorBrain item (enforce with a guard test — grep durable dirs for forbidden imports)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the ONE letter_unit file 14-01 legitimately edits to wire the tutor — confirm its tutor wiring imports lib/tutor/ types, NOT firebase_ai directly)
  </read_first>
  <action>
    Create `test/tutor/durable_layers_no_forbidden_imports_test.dart`: enumerate every `.dart` file under the durable dirs — `lib/core/scoring/`, `lib/core/exercise_engine/`, `lib/features/practice/widgets/stroke_canvas.dart`, and `lib/features/letter_unit/` (recursively) — read each file, strip comment lines, and assert NONE contains an import of `package:firebase_ai`, `package:genui`, or `package:flutter_gemma` (the forbidden set). Use a comment-stripping read (filter lines starting with `//` and `///`) so a doc-comment mentioning a package name does not self-invalidate the gate. On failure, print the offending file + line for a readable RED.
    Rationale comment in the test: the tutor plugs into letter_unit ONLY via lib/tutor/ types (TutorBrain/TutorFacts/dispatcher); a durable file importing firebase_ai directly is the regression this catches (TUTOR-01). NOTE: the curriculum durable layer is `assets/curriculum/` (data, no imports) — the import guard covers the code durable dirs; assets are covered by the 14-01 git-diff verification.
    Add NO production code.
  </action>
  <verify>
    <automated>flutter test test/tutor/durable_layers_no_forbidden_imports_test.dart -r expanded</automated>
  </verify>
  <acceptance_criteria>
    - The test enumerates `.dart` files under lib/core/scoring/, lib/core/exercise_engine/, lib/features/practice/widgets/stroke_canvas.dart, and lib/features/letter_unit/ (recursive) and asserts no comment-stripped line imports firebase_ai/genui/flutter_gemma.
    - Comment lines are stripped before the import check (a `// ... firebase_ai ...` doc comment does NOT fail the gate — no self-invalidating grep).
    - The test is GREEN on the current tree (14-01 wired the tutor via lib/tutor/ types only); planting a `import 'package:firebase_ai/...';` into any durable file would make it RED.
    - SC#1 mapped: durable layers carry zero GenUI/A2UI/firebase_ai imports; swapping the backend changes no durable code (TUTOR-01).
  </acceptance_criteria>
  <done>The forbidden-import guard passes on the clean tree and would fail the build if any durable code file imports firebase_ai/genui/flutter_gemma.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TutorFacts chokepoint → network | The guarded boundary: this plan's tests are the gate that proves only derived non-PII facts cross it (GROUND-02). |
| tutor layer → durable v1 layers | The guarded boundary: durable code must never import the AI packages (TUTOR-01) — the tutor plugs in only via lib/tutor/ types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Information Disclosure | TutorFacts serialized payload | mitigate | Build-failing payload guard: key-set whitelist + forbidden-pattern + leaf-type assertions; a stroke/PII leak turns the build RED (GROUND-02). |
| T-14-03-02 | Tampering | durable v1 code layers | mitigate | Forbidden-import guard greps the durable dirs (comment-stripped); a firebase_ai/genui/flutter_gemma import in any durable file fails the build (TUTOR-01). |
| T-14-03-03 | Repudiation | the guards themselves | accept | Tests live in test/tutor/ and run in the normal suite; if deleted, the suite no longer enforces — acceptable for a Technion build (CI gate is Phase 16 EVAL-02). |
</threat_model>

<verification>
- `flutter test test/tutor/grounding_non_pii_payload_test.dart test/tutor/durable_layers_no_forbidden_imports_test.dart` green on the clean tree.
- Manual sanity: temporarily adding a stroke field to TutorFacts → payload guard RED; planting a firebase_ai import in lib/core/scoring/ → import guard RED (revert both).
</verification>

<success_criteria>
- The GROUND-02 non-PII payload guard is build-failing and green on the clean spine.
- The TUTOR-01 forbidden-import guard is build-failing and green on the clean durable layers.
- Both guards are comment-safe and print the offending location on failure.
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-03-SUMMARY.md` when done
</output>
