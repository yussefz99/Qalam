---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - pubspec.yaml
  - lib/tutor/tutor_facts.dart
  - lib/tutor/tutor_facts_builder.dart
  - lib/tutor/tutor_decision.dart
  - lib/tutor/remote_agent_brain.dart
  - lib/tutor/tutor_providers.dart
  - lib/tutor/authored_fallback_brain.dart
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - test/tutor/remote_agent_brain_test.dart
  - test/tutor/tutor_facts_builder_test.dart
  - test/tutor/tutor_providers_test.dart
autonomous: false
requirements: [TUTOR-01, TUTOR-02, TUTOR-03]

must_haves:
  truths:
    - "RemoteAgentBrain calls the Cloud Run server with a Firebase ID token + App Check token and returns the server's grounded ACTION as a TutorDecision"
    - "On timeout/offline/server-error, RemoteAgentBrain auto-degrades to AuthoredFallback so the trace loop never blocks"
    - "TutorFacts now carries the scored-attempt trajectory + the session learner model (struggle/strength tags), not just the last mistakeId"
    - "TutorFacts.toJson() mirrors 14-01's TutorFactsIn field set/casing EXACTLY (trajectory + strengthTags included) so the server's extra=forbid never 422s a legit payload"
    - "Swapping the brain backend changes no canvas, scorer, or curriculum code — only the tutorBrainFactoryProvider switch point"
    - "The coaching line reaches the UI via tutorLineProvider, read by exercise_scaffold; ExerciseController stays byte-for-byte unmodified (GROUND-01)"
  artifacts:
    - path: "lib/tutor/remote_agent_brain.dart"
      provides: "RemoteAgentBrain: POSTs TutorFacts to the server, parses CoachOut → TutorDecision, auto-degrades to AuthoredFallback"
      min_lines: 40
    - path: "lib/tutor/tutor_facts.dart"
      provides: "TutorFacts reshaped with trajectory + learner-model fields, still non-PII serialized, mirroring 14-01's TutorFactsIn"
      contains: "trajectory"
    - path: "lib/tutor/tutor_decision.dart"
      provides: "TutorDecision reshaped to carry an optional plan/sequence alongside the ACTION"
    - path: "lib/tutor/tutor_providers.dart"
      provides: "tutorBrainFactoryProvider router: online→RemoteAgentBrain wrapping AuthoredFallback, the single switch point"
  key_links:
    - from: "lib/tutor/remote_agent_brain.dart"
      to: "the Cloud Run /coach endpoint"
      via: "http POST with Authorization + X-Firebase-AppCheck headers"
      pattern: "X-Firebase-AppCheck"
    - from: "lib/tutor/remote_agent_brain.dart"
      to: "lib/tutor/authored_fallback_brain.dart"
      via: "catch timeout/error → fallback.next(facts)"
      pattern: "AuthoredFallback"
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      to: "lib/tutor/tutor_providers.dart"
      via: "reads tutorBrainFactoryProvider + tutorLineProvider (ExerciseController untouched)"
      pattern: "tutorBrainFactoryProvider"
---

<objective>
Reshape the client tutor seam for the capable server agent and wire it end-to-end: add a `RemoteAgentBrain` that calls the Cloud Run `/coach` endpoint (Firebase ID token + App Check token), parses the server's grounded ACTION into a `TutorDecision`, and auto-degrades to `AuthoredFallback` on timeout/offline/error; enrich `TutorFacts` to carry the scored-attempt trajectory + session learner model and `TutorDecision` to carry the plan; and route the brain through the single `tutorBrainFactoryProvider` switch point so the coaching line reaches the UI via `tutorLineProvider` with `ExerciseController` untouched. This is MVP slice 3: the child now gets the cloud tutor's line online and the authored floor offline, through one seam.

The client's enlarged `TutorFacts.toJson()` MUST mirror Plan 01's already-deployed `TutorFactsIn` (the single source of truth in `server/app/schema.py`: the 6 base fields + `trajectory: list[AttemptFactIn]` + `strengthTags`) field-for-field — the server DTO is the contract, not a to-be-written SUMMARY. Because 14-01 deployed the FULL enlarged DTO from day one, a correct client payload validates with 200; this plan's job is to make the client emit exactly that shape.

Purpose: Connect the client to the server (TUTOR-03) while preserving the swappable seam (TUTOR-01) and the offline floor (TUTOR-02), keeping the grounding invariant intact (the scorer still owns the verdict at the untouched ExerciseController).
Output: RemoteAgentBrain, the reshaped FACTS/decision types, the router, and the scaffold wiring.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md

# The server wire contract this client MUST match — the SINGLE source of truth is 14-01's deployed DTO, read the file (not just the SUMMARY)
@server/app/schema.py
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md
@.planning/research/v2-tutor-architecture/CAPABLE-AGENT-SPEC.md

# The existing Wave-1 seam to reshape (do NOT discard — extend)
@lib/tutor/tutor_brain.dart
@lib/tutor/tutor_facts.dart
@lib/tutor/tutor_facts_builder.dart
@lib/tutor/tutor_decision.dart
@lib/tutor/authored_fallback_brain.dart
@lib/tutor/tutor_dispatcher.dart
@lib/tutor/tutor_providers.dart

# The seam this wires into — read-only / untouched
@lib/features/letter_unit/exercise_controller.dart
@lib/features/letter_unit/widgets/exercise_scaffold.dart
@lib/core/exercise_engine/check_result.dart
@lib/services/auth_service.dart
</context>

## Phase Goal

**As a** child practicing baa, **I want to** hear the cloud tutor's specific, warm line right after I trace — and a grounded authored line when I'm offline, **so that** every attempt gets real feedback and the loop never stalls.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape TutorFacts + TutorDecision for the capable agent (trajectory + plan), mirroring 14-01's TutorFactsIn, keeping the non-PII guarantee</name>
  <read_first>
    - lib/tutor/tutor_facts.dart (the current 6 whitelisted fields + the toMap/toJson serialization)
    - lib/tutor/tutor_facts_builder.dart (the one chokepoint — its signature is the guard)
    - lib/tutor/tutor_decision.dart (the 4 ACTION shapes + TutorTool names)
    - server/app/schema.py (the FINAL deployed TutorFactsIn — the SINGLE source of truth: 6 base fields + AttemptFactIn{passed,mistakeId,section} + trajectory + strengthTags; mirror these names/casing EXACTLY)
    - .planning/research/v2-tutor-architecture/CAPABLE-AGENT-SPEC.md (why TutorFacts carries the trajectory + session learner model)
    - test/tutor/tutor_facts_builder_test.dart (the existing GROUND-02-style whitelist test to extend, NOT break — and the SUBSTRING regex at ~line 31 that must be tightened)
  </read_first>
  <files>lib/tutor/tutor_facts.dart, lib/tutor/tutor_facts_builder.dart, lib/tutor/tutor_decision.dart, test/tutor/tutor_facts_builder_test.dart</files>
  <behavior>
    - buildTutorFacts produces a TutorFacts whose serialized map keys are a SUBSET of the (now larger) non-PII whitelist and match NONE of the tightened coordinate/PII token guard (see action: word-boundary single letters + full PII/geometry tokens, NOT a substring scan).
    - The new legit keys `trajectory`, `strengthTags`, `nextExerciseId` PASS the tightened guard (they no longer collide with a bare "y"/"name" substring); a real `x`/`y`/`strokes`/`offset`/`childName` key FAILS the guard.
    - The trajectory field holds only derived non-PII items (per-attempt passed/mistakeId/section summaries), never raw Offsets — the builder signature still cannot accept strokes/PII.
    - A TutorDecision can carry an optional plan (next-exercise intent) alongside its ACTION; an ACTION with no plan still constructs (the offline floor path).
    - The serialized TutorFacts matches the server's TutorFactsIn field names/casing exactly (so the server's extra=forbid does not 422 a legit payload).
  </behavior>
  <action>
    Extend `lib/tutor/tutor_facts.dart` (TUTOR-03 capable agent / ADR-015 §Seam impact / CAPABLE-AGENT-SPEC): add a `trajectory` field — a `List<AttemptFact>` where `AttemptFact` is a tiny non-PII value type `{passed: bool, mistakeId: String?, section: String}` (derived per scored attempt this session, mirroring the server's `AttemptFactIn`) — and the session learner-model tags `struggleTags` + `strengthTags`. Keep `toMap()/toJson()` emitting ONLY the enlarged whitelist of scalar/string-list/derived-record fields; there is still deliberately no Offset/stroke/PII field and the type cannot hold one. Mirror the deployed server `TutorFactsIn` field names/casing from `server/app/schema.py` EXACTLY (the single source of truth — the server is already deployed with this shape; the client must match it byte-for-byte: base fields letterId/section/passed/mistakeId/struggleTags/recentMistakes, plus trajectory[{passed,mistakeId,section}] + strengthTags). Add `strengthTags` if absent.

    Extend `lib/tutor/tutor_facts_builder.dart`: the chokepoint now also accepts the derived `trajectory` (a list of the per-attempt non-PII records) and derives `strengthTags` deterministically (the inverse of struggles — sections passed cleanly). The signature STILL accepts no stroke/Offset/profile parameter (the guard by construction). Keep `_deriveStruggleTags` behavior.

    Extend `lib/tutor/tutor_decision.dart`: add an optional `TutorPlan` value type (`{nextExerciseId: String?, intent: String?, rationale: String?}`) and let each `TutorDecision` carry an optional `plan` (default null) WITHOUT changing the sealed 4-ACTION shape set or the `TutorTool` names — the ACTION space stays closed (GROUND-01).

    TIGHTEN THE NON-PII GUARD REGEX (Blocker fix): the existing scan in `test/tutor/tutor_facts_builder_test.dart` (~line 31) is `RegExp(r'stroke|offset|nick|name|x|y|point', caseSensitive:false)` — a SUBSTRING match, so the new legit key `trajectory` (contains "y") and `strengthTags`/`nextExerciseId` would falsely FAIL. Replace it with a coordinate/PII-specific token guard: word-boundary-anchored single letters plus full geometry/PII tokens, e.g. `RegExp(r'\b(x|y)\b|stroke|offset|coord|point|raw|nick|\bname\b', caseSensitive:false)`. Update the test's `_whitelist` set to add the new field names (`trajectory`, `strengthTags`, and the AttemptFact record keys `passed`/`mistakeId`/`section` already covered). Add assertions BOTH directions: (1) a real `x`/`y`/`strokes`/`offset`/`childName` key FAILS the tightened guard; (2) `trajectory`/`strengthTags`/`nextExerciseId` PASS it. Recursively walk nested trajectory record keys when scanning. The existing pass/fail-verdict whitelist tests must still pass.
  </action>
  <verify>
    <automated>flutter test test/tutor/tutor_facts_builder_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `TutorFacts` carries a non-PII `trajectory` (List of derived per-attempt records {passed,mistakeId,section}) + `struggleTags`/`strengthTags`; `toMap()` keys are a subset of the enlarged whitelist.
    - The tightened guard regex matches a real `x`/`y`/`strokes`/`offset`/`childName` key but NOT `trajectory`/`strengthTags`/`nextExerciseId` — both directions asserted in the test.
    - The builder signature still accepts no stroke/Offset/profile parameter (raw geometry physically cannot enter).
    - `TutorDecision` carries an optional `plan` while the sealed ACTION-shape set and `TutorTool` names are unchanged (closed action space).
    - The serialized field names/casing match the deployed server `TutorFactsIn` in `server/app/schema.py` (the single source of truth) — not a SUMMARY.
  </acceptance_criteria>
  <done>The FACTS-in/ACTIONS-out types model the capable agent (trajectory + learner model + plan) mirroring the deployed server DTO, the non-PII guarantee holds by construction, and the tightened guard no longer collides with legitimate new field names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RemoteAgentBrain (server call + auth headers + auto-degrade) + add firebase_app_check/http deps</name>
  <read_first>
    - server/app/schema.py (the deployed /coach request/response DTOs — the field set the client POSTs and parses)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-01-SUMMARY.md (the Cloud Run /coach URL + the auth headers)
    - lib/tutor/tutor_brain.dart (the seam: Future<TutorDecision> next(TutorFacts))
    - lib/tutor/authored_fallback_brain.dart (the offline floor RemoteAgentBrain wraps)
    - lib/services/auth_service.dart (currentUser → getIdToken for the Authorization header)
    - pubspec.yaml (firebase_app_check is in the lock transitively; promote to a direct dep)
  </read_first>
  <files>pubspec.yaml, lib/tutor/remote_agent_brain.dart, test/tutor/remote_agent_brain_test.dart</files>
  <behavior>
    - On a 200 CoachOut response, RemoteAgentBrain returns the matching TutorDecision (say/present_activity/give_hint/advance) carrying the server's coaching line + optional plan.
    - On a timeout (configurable budget), RemoteAgentBrain returns the AuthoredFallback's decision instead — the call never throws to the caller.
    - On a non-200 / network error / offline, RemoteAgentBrain degrades to AuthoredFallback.
    - The request carries an Authorization: Bearer <Firebase ID token> header AND an X-Firebase-AppCheck token header.
    - The request body is exactly TutorFacts.toJson() (the non-PII payload mirroring TutorFactsIn) — no extra fields, so the server's extra=forbid returns 200.
  </behavior>
  <action>
    Add the client deps to `pubspec.yaml` via `flutter pub add firebase_app_check http` (promote firebase_app_check from the transitive lock to a direct dep; `http` for the REST call). Re-pin to the FlutterFire lockstep generation already in the project (do NOT bump mismatched majors — see the existing FlutterFire note in pubspec). Record resolved versions in the SUMMARY.

    Write `lib/tutor/remote_agent_brain.dart`: `class RemoteAgentBrain implements TutorBrain`, constructed with the server base URL, an `http.Client` (injectable for tests), an `AuthService` (for `currentUser?.getIdToken()`), an App Check token getter (`FirebaseAppCheck.instance.getLimitedUseToken()` — the limited-use/replay-protection token the server verifies), the wrapped `AuthoredFallback` floor, and a timeout budget. `next(facts)`: build the headers (Authorization bearer ID token + `X-Firebase-AppCheck`), POST `facts.toJson()` to `/coach` under the timeout, parse `CoachOut` → the matching `TutorDecision` (map the tool name via `TutorTool`), attaching the plan if present. On ANY failure (timeout/offline/non-200/parse error/missing token) return `await fallback.next(facts)` — RemoteAgentBrain NEVER throws to its caller (TUTOR-02/03 — the loop never blocks, G5). Fire the `/healthz` warm-up ping is OUT of scope here (a later UI hook); document it in the SUMMARY as a follow-up.

    Write `test/tutor/remote_agent_brain_test.dart` with a `MockClient` (package:http/testing.dart): (a) a stubbed 200 CoachOut → assert the returned TutorDecision matches the tool name + line; (b) a stubbed 503 → assert the returned decision equals the AuthoredFallback's decision; (c) a delayed response beyond the budget → assert the AuthoredFallback decision (no throw); (d) assert the captured request has the Authorization + X-Firebase-AppCheck headers and a body equal to `facts.toJson()` with no extra keys. Inject fake ID-token/App-Check getters so the test runs without Firebase.
  </action>
  <verify>
    <automated>flutter test test/tutor/remote_agent_brain_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - A 200 CoachOut maps to the correct `TutorDecision` (tool name + coaching line + optional plan).
    - A timeout/non-200/offline/parse-error/missing-token path returns the AuthoredFallback decision and RemoteAgentBrain never throws to its caller.
    - The outbound request carries `Authorization: Bearer <idToken>` AND `X-Firebase-AppCheck`, with the body == `facts.toJson()` (no extra keys).
    - `firebase_app_check` + `http` are direct deps pinned to the project's FlutterFire generation.
  </acceptance_criteria>
  <done>RemoteAgentBrain talks to the grounded server with both auth tokens and silently degrades to the offline floor — the keys never touch the client and the loop never blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Route the brain through the single switch point + wire the line into the scaffold (ExerciseController untouched)</name>
  <read_first>
    - lib/tutor/tutor_providers.dart (tutorBrainFactoryProvider — the single switch point; tutorLineProvider)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the _onResult flow + _TutorLineAdapter + _TutorColumn already wired in Wave 1)
    - lib/features/letter_unit/exercise_controller.dart (must stay byte-for-byte unmodified — read to confirm no edit)
  </read_first>
  <files>lib/tutor/tutor_providers.dart, lib/tutor/authored_fallback_brain.dart, lib/features/letter_unit/widgets/exercise_scaffold.dart, test/tutor/tutor_providers_test.dart</files>
  <action>
    Update `lib/tutor/tutor_providers.dart` `tutorBrainFactoryProvider` to be the SINGLE switch point (ADR-015 §Seam impact / TUTOR-01): build a `RemoteAgentBrain` wrapping an `AuthoredFallbackBrain(feedback)` as its degrade floor — the online→Remote, failure→Authored router lives HERE and nowhere else. Read the server base URL from a const/config (a `--dart-define` or a config provider; record the chosen mechanism in the SUMMARY). Keep `AuthoredFallbackBrain` as-is except for any tiny constructor change needed to satisfy the new `TutorDecision.plan` optionality (it returns ACTIONs with no plan — the offline floor).

    Confirm `exercise_scaffold.dart`'s existing `_onResult` already: (1) calls `ExerciseController.applyResult` FIRST and unchanged (GROUND-01); (2) builds FACTS via the chokepoint; (3) asks `tutorBrainFactoryProvider`'s brain; (4) dispatches the line into `tutorLineProvider`; (5) on a brain error clears `tutorLineProvider` so the verdict-side line is the floor. The Wave-1 scaffold already does this — extend ONLY to pass the new `trajectory` into `buildTutorFacts` (accumulate per-attempt `AttemptFact`s in the existing `_recentMistakes` neighborhood, still non-PII). Make NO edit to `exercise_controller.dart`.

    Write `test/tutor/tutor_providers_test.dart`: a ProviderContainer test asserting `tutorBrainFactoryProvider` returns a brain that, with no server reachable (inject a failing client via an override or a test seam), still yields a grounded AuthoredFallback decision — proving the swap changes no scaffold/controller code and the offline floor holds. Assert (by reading the file in the test or a guard) that `exercise_controller.dart` exposes no line-setter (the line path is `tutorLineProvider`).
  </action>
  <verify>
    <automated>flutter test test/tutor/tutor_providers_test.dart && flutter analyze lib/tutor lib/features/letter_unit</automated>
  </verify>
  <acceptance_criteria>
    - `tutorBrainFactoryProvider` is the single switch point returning RemoteAgentBrain(wrapping AuthoredFallback); no canvas/scorer/curriculum/controller code changed to swap the backend.
    - `exercise_scaffold.dart` passes the new non-PII `trajectory` into `buildTutorFacts`; `applyResult` still runs first and unchanged.
    - `exercise_controller.dart` is byte-for-byte unmodified (no line-setter added) — the line reaches the UI only via `tutorLineProvider` (GROUND-01).
    - The provider test proves an unreachable server still yields a grounded AuthoredFallback line (the offline floor through the real seam).
  </acceptance_criteria>
  <done>The capable agent is wired through the one switch point into the live exercise loop; online it coaches, offline it falls to the authored floor, and the scorer's seam is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The baa trace loop now talks to the live Cloud Run tutor. Claude has run the app (or a baa Letter-Unit widget driver) against the deployed server URL and confirmed: a traced attempt online produces the server's coaching line in the tutor bubble; airplane mode (or an unreachable URL) produces the grounded AuthoredFallback line and the loop still advances.</what-built>
  <how-to-verify>
    1. Run the app on the device/emulator pointed at the Cloud Run URL (the `--dart-define` from Task 3). Trace a baa attempt — confirm the tutor bubble shows a warm, specific, correctly-Arabic line (the server's), not a canned message.
    2. Trace a deliberately bad attempt — confirm the line names a fix and never says "great job" / never auto-advances on a fail (GROUND-01).
    3. Turn on airplane mode and trace again — confirm a grounded AuthoredFallback line appears and the Clear/Try-again/Next loop still works (never blocks).
    4. Confirm no API key is in the client build (it is not — keys are server-side; this is a visual sanity check that the line came from the server when online and the floor when offline).
  </how-to-verify>
  <resume-signal>Type "approved" once the online line, the grounded-fail behavior, and the airplane-mode floor all check out — or describe the issue (recall the Phase 07 baa device bugs: verify on a real running app, not blind).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Cloud Run /coach | The client must present a Firebase ID token + App Check token; only the non-PII payload crosses |
| TutorFacts builder → wire | The chokepoint serializes only whitelisted derived fields; raw strokes/PII cannot enter by construction |
| server response → UI | The coaching line reaches the UI only via tutorLineProvider; it can never flip the scorer's verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Information Disclosure | TutorFacts.toJson payload | mitigate | The enlarged whitelist still excludes strokes/PII; the builder signature accepts no geometry; the whitelist test's guard is tightened to coordinate/PII-specific tokens so it catches real x/y/strokes/offset/childName keys while passing legit trajectory/strengthTags (Task 1); the build-failing GROUND-02 guard lands in Plan 04 |
| T-14-13 | Spoofing | client→server request | mitigate | Authorization: Bearer Firebase ID token + X-Firebase-AppCheck limited-use token on every request (Task 2) |
| T-14-14 | DoS | server slow/unreachable | mitigate | Timeout budget → auto-degrade to AuthoredFallback; RemoteAgentBrain never throws (Task 2 / G5) |
| T-14-15 | Tampering | server response flips a verdict | mitigate | The line reaches the UI via tutorLineProvider only; ExerciseController.applyResult owns pass/fail and is untouched (Task 3 / GROUND-01) |
| T-14-16 | Information Disclosure | API key in client | mitigate | No model key in the client by construction (keys are Secret Manager server-side); checkpoint visual confirm |
| T-14-SC | Tampering | pub add firebase_app_check + http | mitigate | firebase_app_check is first-party FlutterFire (already transitively present); `http` is the canonical verified-publisher (dart.dev) package — verify pub.dev publisher before add |
</threat_model>

<verification>
- `flutter test test/tutor/` passes (facts/decision reshape, RemoteAgentBrain degrade, provider offline-floor).
- `flutter analyze lib/tutor lib/features/letter_unit` clean.
- The tightened non-PII guard catches a real x/y/strokes/offset/childName key AND passes trajectory/strengthTags/nextExerciseId (both directions asserted).
- `git diff --exit-code lib/features/letter_unit/exercise_controller.dart` shows NO change (the controller is untouched — GROUND-01).
- Checkpoint: online line + grounded-fail + airplane-mode floor confirmed on a running app.
</verification>

<success_criteria>
- One seam hosts swappable backends; swapping changes no canvas/scorer/curriculum code (TUTOR-01).
- Online the server coaches; on timeout/offline the client auto-degrades to AuthoredFallback; keys never in the client (TUTOR-02, TUTOR-03).
- The client payload mirrors the deployed server TutorFactsIn exactly, so a legit enlarged payload validates 200 (no 422), while the tightened guard still blocks any geometry/PII key.
- The scorer still owns the verdict at the untouched ExerciseController; the agent supplies only the line (GROUND-01).
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-03-SUMMARY.md` when done — record the server-URL config mechanism, the resolved firebase_app_check/http versions, the trajectory shape passed into buildTutorFacts, and the final tightened guard regex.
</output>
</output>
