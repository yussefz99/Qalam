---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - lib/tutor/gemma_brain.dart
  - lib/tutor/tutor_brain_router.dart
  - test/tutor/gemma_brain_test.dart
autonomous: true
requirements: [TUTOR-04]
must_haves:
  truths:
    - "A GemmaBrain backend exists behind the same TutorBrain interface as an evaluated candidate — it never sits on the demo's critical path (TUTOR-04)."
    - "GemmaBrain is experimental/stub here: its adoption is gated on the Phase 13 bake-off, not assumed; the router only ever reaches it behind an explicit experimental flag and otherwise prefers Gemini/AuthoredFallback (TUTOR-04)."
    - "Swapping in GemmaBrain changes no canvas, scorer, or curriculum code — it is just a third implementation of the one interface (TUTOR-01/TUTOR-04)."
  artifacts:
    - path: "lib/tutor/gemma_brain.dart"
      provides: "GemmaBrain implements TutorBrain — on-device (flutter_gemma) candidate, experimental stub, degrades to AuthoredFallback when no model is loaded"
      contains: "class GemmaBrain implements TutorBrain"
  key_links:
    - from: "lib/tutor/tutor_brain_router.dart"
      to: "lib/tutor/gemma_brain.dart"
      via: "the router reaches GemmaBrain ONLY behind an explicit experimental flag; default paths never select it (never on the critical path)"
      pattern: "experimental|Gemma"
---

<objective>
Add the third backend behind the one seam: a `GemmaBrain` stub (the on-device
`flutter_gemma` candidate) that satisfies TUTOR-04 as an evaluated, experimental option —
never on the demo's critical path. Wire the router so it can reach GemmaBrain only behind
an explicit experimental flag, defaulting to Gemini/AuthoredFallback otherwise.

Purpose: Close the v2 architecture's three-backend shape (Authored / Gemini / Gemma) while
honoring the scope fence — GemmaBrain stays a stub; the real on-device model + adoption
decision are the Phase 13 bake-off / Phase 16 (deferred here).

Output: `GemmaBrain` stub + the router's experimental branch + one test. This plan is the
smallest slice; it leaves the baa loop working exactly as before (Gemini online / Authored
offline) unless the experimental flag is set.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-02-SUMMARY.md
</context>

## Phase Goal

**As a** maintainer planning the on-device future, **I want to** have a GemmaBrain slot
that proves the three-backend interface holds, **so that** the Phase 13 bake-off can drop
a real on-device model in behind the same seam without it ever risking the live demo.

<tasks>

<task type="auto">
  <name>Task 1: GemmaBrain stub behind the TutorBrain interface</name>
  <files>lib/tutor/gemma_brain.dart, test/tutor/gemma_brain_test.dart</files>
  <read_first>
    - lib/tutor/tutor_brain.dart, lib/tutor/tutor_decision.dart, lib/tutor/tutor_facts.dart (the seam from 14-01)
    - lib/tutor/authored_fallback_brain.dart (the degrade target when no Gemma model is loaded)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md §Decision part 4 (GemmaBrain = flutter_gemma 1.0.2, on-device, EXPERIMENTAL, gated on the Phase 13 bake-off; native <tool_call> tokens) + §Consequences (GemmaBrain Arabic+4-tool quality UNPROVEN; ~2.5GB / 8GB-RAM class — stays experimental)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md <scope_fence> (No real Gemma model shipping decision — stub/experimental only) + <deferred>
  </read_first>
  <action>
    Create `lib/tutor/gemma_brain.dart`: `class GemmaBrain implements TutorBrain`. Implement it as an EXPERIMENTAL STUB — no real flutter_gemma model load on the critical path. `next(facts)`: when no on-device model is loaded (the default in this phase), return the injected `AuthoredFallbackBrain.next(facts)` decision (so it is always grounded + safe). Leave a clearly-commented seam (the place a future Phase-13/16 plan would load the flutter_gemma model + run its native `<tool_call>` loop over the same 4 tool names) but DO NOT ship the model load or the ~2.5GB download UX here (scope fence). Do NOT add flutter_gemma to durable layers; if the stub references the package type at all, keep it ONLY in lib/tutor/gemma_brain.dart (and confirm 14-03's import guard still passes — durable dirs stay clean). Inject AuthoredFallback as the degrade target via constructor, mirroring GeminiBrain.
  </action>
  <verify>
    <automated>flutter test test/tutor/gemma_brain_test.dart -r expanded && flutter analyze lib/tutor/gemma_brain.dart && flutter test test/tutor/durable_layers_no_forbidden_imports_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/tutor/gemma_brain.dart` declares `class GemmaBrain implements TutorBrain` and, with no model loaded, returns the AuthoredFallback decision (grounded, never blank).
    - `test/tutor/gemma_brain_test.dart` asserts GemmaBrain.next(facts) returns the AuthoredFallback line in the no-model (stub) state and never throws.
    - The 14-03 forbidden-import guard still passes (flutter_gemma, if referenced, lives ONLY in lib/tutor/, never in a durable dir).
    - No ~2.5GB model download or flutter_gemma model-load runs on any default path (scope fence — experimental stub only).
    - SC#1 mapped: a third backend behind the one interface; swapping it changes no durable code (TUTOR-04, TUTOR-01).
  </acceptance_criteria>
  <done>GemmaBrain exists as a grounded experimental stub behind the TutorBrain seam; tests + analyze + the import guard are green.</done>
</task>

<task type="auto">
  <name>Task 2: Router experimental branch — GemmaBrain reachable only behind an explicit flag, never default</name>
  <files>lib/tutor/tutor_brain_router.dart, test/tutor/tutor_brain_router_test.dart</files>
  <read_first>
    - lib/tutor/tutor_brain_router.dart (14-02 — online→Gemini / offline→Authored; this task adds the experimental Gemma branch)
    - lib/tutor/gemma_brain.dart (Task 1)
    - lib/tutor/gemini_brain.dart, lib/tutor/authored_fallback_brain.dart (the default backends — must stay the defaults)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md §Decision part 4 (router: online+capable → Gemini; offline/experimental-on-device → Gemma; no model/failure → AuthoredFallback) + Revisit-trigger 3 (Gate GemmaBrain on the Phase 13 bake-off — do not ship on external evidence)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md <specifics> (never on the critical path)
  </read_first>
  <action>
    Modify `lib/tutor/tutor_brain_router.dart`: add an explicit `experimentalGemma` boolean input (default FALSE — Claude's discretion on where it is sourced: a const/feature flag, NOT a runtime user toggle in the demo path). Heuristic: `experimentalGemma == true` (and a Gemma model present) → GemmaBrain; else online+capable → GeminiBrain; else → AuthoredFallbackBrain. With the flag FALSE (the demo/default), the router NEVER returns GemmaBrain — preserving "never on the critical path" (TUTOR-04). Keep the heuristic small + explainable. Do not change the provider's default behavior from 14-02 (Gemini online / Authored offline) when the flag is unset.
  </action>
  <verify>
    <automated>flutter test test/tutor/tutor_brain_router_test.dart test/tutor/gemma_brain_test.dart -r expanded && flutter analyze lib/tutor/</automated>
  </verify>
  <acceptance_criteria>
    - With `experimentalGemma == false` (default), the router selects GeminiBrain online and AuthoredFallbackBrain offline — GemmaBrain is never returned (a router test asserts this).
    - With `experimentalGemma == true` and a Gemma model present, the router selects GemmaBrain (a router test asserts this).
    - `flutter analyze lib/tutor/` exits 0; the 14-02 default routing behavior is unchanged when the flag is unset.
    - SC#1 mapped: GemmaBrain is an evaluated candidate behind the same interface, gated, never on the demo's critical path (TUTOR-04).
  </acceptance_criteria>
  <done>The router reaches GemmaBrain only behind the explicit experimental flag; the default demo path stays Gemini-online / Authored-offline; tests + analyze green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → GemmaBrain (on-device) | An experimental, flag-gated path. When unset (the demo default) it is never reached; when set, GemmaBrain still degrades to the grounded AuthoredFallback. No new network egress (Gemma is on-device). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Elevation of Privilege | GemmaBrain (unproven model) | mitigate | The scorer still owns pass/fail + star (GROUND-01); GemmaBrain only supplies a LINE via the same 4-tool seam — on-device weakness degrades coaching quality, never the verdict. |
| T-14-04-02 | Denial of Service | accidental Gemma on the critical path | mitigate | experimentalGemma defaults FALSE; router never returns GemmaBrain on the demo path; no ~2.5GB download runs by default (scope fence). |
| T-14-04-03 | Tampering | flutter_gemma import bleed | mitigate | flutter_gemma (if referenced) confined to lib/tutor/gemma_brain.dart; the 14-03 forbidden-import guard keeps durable dirs clean. |
</threat_model>

<verification>
- `flutter test test/tutor/` green (incl. the 14-03 import guard).
- `flutter analyze lib/tutor/` exits 0.
- Default-flag router test proves GemmaBrain is never selected on the demo path.
</verification>

<success_criteria>
- GemmaBrain exists as a grounded experimental stub behind the one TutorBrain interface (TUTOR-04).
- The router reaches it only behind an explicit flag; the default demo path is unchanged (TUTOR-04 "never on the critical path").
- Durable layers stay free of flutter_gemma (the 14-03 guard still green) (TUTOR-01).
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-04-SUMMARY.md` when done
</output>
