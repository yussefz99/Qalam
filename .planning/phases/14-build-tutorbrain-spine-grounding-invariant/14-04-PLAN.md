---
phase: 14-build-tutorbrain-spine-grounding-invariant
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - server/tests/test_grounding.py
  - server/tests/test_payload_nonpii.py
  - test/tutor/payload_nonpii_test.dart
  - test/tutor/durable_layers_no_agent_imports_test.dart
  - test/tutor/authored_fallback_offline_test.dart
autonomous: true
requirements: [TUTOR-01, TUTOR-02, TUTOR-04, GROUND-02]

must_haves:
  truths:
    - "A build-failing test asserts the serialized server REQUEST body carries only the non-PII whitelist — any stroke/Offset/PII field fails the build (GROUND-02)"
    - "A guard test fails the build if any durable v1 layer (canvas/scorer/curriculum) imports an agent/framework/network package (TUTOR-04)"
    - "An offline-floor test proves every coaching moment (pass + each baa mistakeId) yields a grounded, correctly-Arabic AuthoredFallback line with the loop never blocking (TUTOR-02)"
    - "The non-PII assertion runs on BOTH sides: the Dart payload (client) and the server request DTO (server)"
    - "The non-PII guard uses coordinate/PII-specific token matching (word-boundary single letters + full tokens) so it catches real x/y/strokes/offset/childName keys while passing legit trajectory/strengthTags/nextExerciseId"
  artifacts:
    - path: "test/tutor/payload_nonpii_test.dart"
      provides: "build-failing assertion the serialized TutorFacts payload exposes only whitelisted non-PII fields (GROUND-02 client side), using the tightened token guard"
      contains: "strokes"
    - path: "server/tests/test_payload_nonpii.py"
      provides: "build-failing assertion the server TutorFactsIn rejects any non-whitelisted/PII field (GROUND-02 server side)"
    - path: "test/tutor/durable_layers_no_agent_imports_test.dart"
      provides: "static-scan guard: durable v1 layers carry zero firebase_ai/genui/flutter_gemma/http/langgraph imports (TUTOR-04)"
    - path: "test/tutor/authored_fallback_offline_test.dart"
      provides: "offline-floor coverage for baa: pass + every authored mistakeId yields a grounded line, loop never blocks (TUTOR-02)"
  key_links:
    - from: "test/tutor/payload_nonpii_test.dart"
      to: "lib/tutor/tutor_facts.dart"
      via: "serialize a fully-populated TutorFacts and scan keys"
      pattern: "toJson|toMap"
    - from: "test/tutor/durable_layers_no_agent_imports_test.dart"
      to: "lib/core, lib/features/practice, lib/models (curriculum)"
      via: "read source files + assert no forbidden import strings"
      pattern: "firebase_ai|genui|flutter_gemma|langgraph"
---

<objective>
Lock the phase's catastrophic-if-violated invariants with build-failing guards: (1) the GROUND-02 non-PII guard on BOTH the client payload AND the server request body — any raw stroke/Offset/PII field fails the build; (2) the TUTOR-04 durable-layers guard — the v1 canvas/scorer/curriculum carry zero agent/framework/network imports; (3) the TUTOR-02 offline-floor guard — every baa coaching moment (pass + each authored mistakeId) yields a grounded, correctly-Arabic AuthoredFallback line and the loop never blocks. This is MVP slice 4: it makes the grounding + non-PII + offline guarantees permanent regression tests, not promises.

Purpose: Convert the three structural invariants the whole phase rests on into automated, build-failing checks so no future change can silently leak child data, contaminate the durable layers, or break the offline floor.
Output: Two non-PII payload tests (Dart + Python), one import-guard test, one offline-floor coverage test.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-CONTEXT.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
@docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md

# The reshaped types + server DTO these guards lock
@server/app/schema.py
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-02-SUMMARY.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-03-SUMMARY.md
@lib/tutor/tutor_facts.dart
@test/tutor/tutor_facts_builder_test.dart
@lib/tutor/authored_fallback_brain.dart
@lib/core/exercise_engine/check_result.dart
</context>

## Phase Goal

**As a** parent trusting the app with my child, **I want to** be sure no stroke or name ever leaves the device and the tutor always works offline, **so that** my child's data stays private and practice never stalls — guaranteed by the build, not by hope.

<tasks>

<task type="auto">
  <name>Task 1: GROUND-02 build-failing non-PII guard on both the client payload and the server request body</name>
  <read_first>
    - lib/tutor/tutor_facts.dart (the enlarged whitelist + toJson — from Plan 03)
    - server/app/schema.py (TutorFactsIn extra=forbid + nested AttemptFactIn extra=forbid — from Plan 01)
    - test/tutor/tutor_facts_builder_test.dart (the existing whitelist scan + the TIGHTENED guard regex Plan 03 installed — build on it, do NOT regress to a substring scan)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-03-SUMMARY.md (the final serialized field names + the final tightened guard regex)
  </read_first>
  <files>test/tutor/payload_nonpii_test.dart, server/tests/test_payload_nonpii.py</files>
  <action>
    Write `test/tutor/payload_nonpii_test.dart` (the GROUND-02 client guard, ADR-015 §4 / G1): construct a FULLY-populated `TutorFacts` (every field set, trajectory with multiple AttemptFacts, struggle + strength tags), serialize via `toJson()`, recursively walk ALL keys (including nested trajectory records), and assert every key is in the explicit non-PII whitelist AND no key matches the TIGHTENED coordinate/PII token guard (NOT a substring scan — reuse the exact regex Plan 03 settled on so `trajectory`/`strengthTags`/`nextExerciseId` pass): `RegExp(r'\b(x|y)\b|stroke|offset|coord|point|raw|nick|\bname\b', caseSensitive:false)`. This test is build-failing: if a future field leaks geometry/PII it fails. Add BOTH-direction assertions: (a) a synthetic key set including `x`, `y`, `strokes`, `offsets`, `childName`, `nickname` each FAILS the guard; (b) the real serialized keys including `trajectory`, `strengthTags`, `nextExerciseId` each PASS the guard (proving the tightened regex does not collide with legit fields). Add an assertion that `TutorFacts` exposes no constructor/field accepting a `List<Offset>`/stroke (a reflection-free check: confirm the builder signature + the toJson key set, mirroring the existing builder test's intent). Filter any comment lines if you grep source (`grep -v '^//' | grep -c` — do not gate on unfiltered files).

    Write `server/tests/test_payload_nonpii.py` (the GROUND-02 server guard — the chokepoint is the SERVER REQUEST body per ADR-015 §4): assert `TutorFactsIn` (and the nested `AttemptFactIn`) rejects (Pydantic ValidationError / 422) any payload carrying a non-whitelisted key (`strokes`, `x`, `y`, `offsets`, `nickname`, `childName`), AND assert a legit payload (the exact enlarged field set from `server/app/schema.py` / the 14-03 SUMMARY — base fields + a populated `trajectory` + `strengthTags`) validates. Mark `code`. This pins extra=forbid on both models as a permanent regression guard, not an incidental config.

    Ensure both tests are wired into the default test run (Dart: under `test/`; Python: collected by `pytest`) so a leak fails the build on either side.
  </action>
  <verify>
    <automated>flutter test test/tutor/payload_nonpii_test.dart && cd server && uv run pytest tests/test_payload_nonpii.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The Dart test serializes a fully-populated TutorFacts and fails if ANY key (incl. nested trajectory) is outside the whitelist or matches the tightened token guard; it asserts BOTH directions (x/y/strokes/offsets/childName/nickname FAIL the guard; trajectory/strengthTags/nextExerciseId PASS).
    - The Python test asserts TutorFactsIn AND AttemptFactIn reject each of {strokes, x, y, offsets, nickname, childName} and accept the legit enlarged payload (with a populated trajectory + strengthTags).
    - Both tests run in the default test suites (build-failing on either side).
  </acceptance_criteria>
  <done>The non-PII chokepoint is a build-failing guard on both the client payload and the server request DTO — raw geometry/PII cannot cross undetected, and the tightened guard never falsely trips on the legit enlarged fields (GROUND-02).</done>
</task>

<task type="auto">
  <name>Task 2: Durable-layers-no-agent-imports guard + AuthoredFallback offline-floor coverage</name>
  <read_first>
    - lib/tutor/authored_fallback_brain.dart (the offline floor + its line-resolution mirror of applyResult)
    - lib/features/letter_unit/exercise_controller.dart (the applyResult resolution the floor must match)
    - assets/curriculum/exercises.json + the baa Exercise.feedback maps (the authored pass + per-mistakeId lines — the canonical signed seed under assets/curriculum, loaded via lib/data/curriculum_repository.dart)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md (§4 "Durable v1 layers carry ZERO firebase_ai/genui/flutter_gemma imports")
  </read_first>
  <files>test/tutor/durable_layers_no_agent_imports_test.dart, test/tutor/authored_fallback_offline_test.dart</files>
  <action>
    Write `test/tutor/durable_layers_no_agent_imports_test.dart` (TUTOR-04 / ADR-014 §4): enumerate the durable v1 layer source files — `lib/core/**` (scorer/exercise_engine), `lib/features/practice/**` (StrokeCanvas), and the curriculum model/data files (`lib/models/**`, `lib/data/**`) — read each, strip comment lines, and assert NONE import a forbidden package: `firebase_ai`, `genui`, `flutter_gemma`, `langgraph`, `http`, `package:qalam/tutor/remote_agent_brain.dart`, or the json_schema_builder spike. Use `grep -v '^\s*//' | grep -c` semantics (filter comments first — header prose naming these packages must not self-trip the gate). The test enumerates files and asserts a count of 0 forbidden imports per durable file; it fails the build if any durable layer takes an agent/framework/network dependency (the seam keeps them clean — TUTOR-01/04).

    Write `test/tutor/authored_fallback_offline_test.dart` (TUTOR-02 — the offline floor, airplane mode): load the baa exercise(s)' authored `feedback` map(s) and construct `AuthoredFallbackBrain(feedback: ...)`. For EACH coaching moment — a pass (`feedback['pass']`) and EVERY authored `mistakeId` key — call `next(facts)` and assert: (a) it returns a grounded `TutorDecision` carrying a NON-EMPTY authored line; (b) the line equals what `ExerciseController.applyResult` would resolve for the same verdict (byte-identical — the floor mirrors the verdict-side line); (c) it never throws and never blocks (pure Dart, no Firebase/network/model — confirm by the absence of those imports in the test + the brain). Add a case: an UNKNOWN mistakeId still yields the first authored non-'pass' line (never a generic "try again"). If the baa lines contain Arabic, assert they are non-empty strings as authored (correct-Arabic is the owner's-mother sign-off, not a code check — note this in the SUMMARY).
  </action>
  <verify>
    <automated>flutter test test/tutor/durable_layers_no_agent_imports_test.dart test/tutor/authored_fallback_offline_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The import-guard test enumerates `lib/core`, `lib/features/practice`, `lib/models`, `lib/data` and fails if any (comment-stripped) source imports firebase_ai/genui/flutter_gemma/langgraph/http/remote_agent_brain/json_schema_builder.
    - The offline-floor test covers the pass moment + every authored baa mistakeId + an unknown-id case; each yields a non-empty grounded line matching applyResult's resolution; nothing throws or blocks.
    - Both tests run pure-Dart with no Firebase/network/model dependency.
  </acceptance_criteria>
  <done>The durable layers are guaranteed import-clean and the offline floor is proven grounded across every baa coaching moment — both as build-failing regression tests (TUTOR-02, TUTOR-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized FACTS → network | The only data crossing; the guard fails the build if anything outside the non-PII whitelist appears |
| durable v1 layers → dependencies | The scorer/canvas/curriculum must stay free of agent/framework/network imports |
| offline floor → child | With zero model, every coaching moment must still produce a grounded authored line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17 | Information Disclosure | client payload + server request body | mitigate | Build-failing non-PII token-guard scan on BOTH the Dart toJson and the server TutorFactsIn/AttemptFactIn; tightened to coordinate/PII-specific tokens so it catches real geometry/PII keys without tripping legit trajectory/strengthTags (Task 1 / GROUND-02 / G1) |
| T-14-18 | Tampering | durable v1 layers gaining an agent/network import | mitigate | Static-scan guard fails the build on any forbidden import in core/practice/models/data (Task 2 / TUTOR-04) |
| T-14-19 | DoS | offline coaching moment with no grounded line | mitigate | Offline-floor test proves every pass + mistakeId (+ unknown id) yields a non-empty grounded line, no throw/block (Task 2 / TUTOR-02) |
| T-14-SC | Tampering | no new package installs in this plan | accept | Plan adds only tests; no new dependencies → no supply-chain surface |
</threat_model>

<verification>
- `flutter test test/tutor/` passes — including the two new non-PII/import/offline guards.
- `cd server && uv run pytest -q` passes — including the server-side non-PII guard.
- The tightened non-PII guard asserts both directions: geometry/PII keys FAIL, legit trajectory/strengthTags/nextExerciseId PASS.
- Sanity: deliberately add a `strokes` field to a TutorFacts toJson locally → the Dart guard fails (then revert); add `import 'package:http/http.dart'` to a `lib/core` file → the import guard fails (then revert). (Document this proof in the SUMMARY; do not commit the deliberate breakage.)
</verification>

<success_criteria>
- GROUND-02 is a build-failing guard on both the client payload and the server request body, using the tightened token guard that does not collide with legit enlarged fields.
- TUTOR-04: the durable layers carry zero agent/framework/network imports, enforced by the build.
- TUTOR-02: the offline floor yields a grounded line for every baa coaching moment, enforced by the build.
</success_criteria>

<output>
Create `.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-04-SUMMARY.md` when done — record the durable-layer file globs scanned, the baa feedback source used, the final tightened guard regex, and the deliberate-breakage proof results.
</output>
</output>
