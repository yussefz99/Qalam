---
phase: 04-scoring-quality-calibration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/core/scoring/letter_scorer.dart
  - lib/core/scoring/geometric_stroke_scorer.dart
  - test/core/scoring/letter_scorer_test.dart
  - test/core/scoring/geometric_stroke_scorer_test.dart
autonomous: true
requirements: [S1-05, PLAT-03]
must_haves:
  truths:
    - "A wrong-stroke-count attempt is rejected with MistakeId.wrongStrokeCount (SC#1)"
    - "A wrong-stroke-order attempt is rejected with MistakeId.wrongStrokeOrder (SC#1)"
    - "A taa-dots-when-shown-baa attempt is rejected with MistakeId.dotMisplaced (SC#1, the ب/ت/ث distinction)"
    - "A size-and-position-varied good-faith attempt passes via whole-letter (combined-bbox) normalization (SC#3)"
    - "The scorer reads per-letter tolerances from the Letter, never file-level constants (SC#4)"
  artifacts:
    - path: "lib/core/scoring/letter_scorer.dart"
      provides: "scoreLetter orchestrator — count→order→per-stroke→dot, pure Dart, parameterized by letter.tolerances"
      contains: "scoreLetter"
      min_lines: 60
  key_links:
    - from: "lib/core/scoring/letter_scorer.dart"
      to: "lib/core/scoring/geometric_stroke_scorer.dart"
      via: "delegates each body stroke to scoreStroke, threaded with letter.tolerances"
      pattern: "scoreStroke"
    - from: "lib/core/scoring/letter_scorer.dart"
      to: "lib/core/scoring/stroke_validation.dart"
      via: "reuses dot/order semantics (type=='dot', dots-last, order 1..N)"
      pattern: "type == 'dot'|type != 'dot'"
---

<objective>
Build THE SPINE: the pure-Dart `LetterScorer.scoreLetter` orchestrator that wraps the
existing per-stroke `scoreStroke` to evaluate a whole multi-stroke letter — stroke
**count**, stroke **order**, per-stroke shape/direction (delegated), and a **dot**
predicate (count + relative position) — turning the RED contract tests from Plan 01
GREEN. Parameterize the previously-hardcoded thresholds from `letter.tolerances` so the
scorer reads data, not constants.

Purpose: This makes SC#1 real on a multi-stroke letter (baa) and SC#3 hold via
whole-letter (combined-bbox) normalization, while keeping order/count firm even when
shape tolerance is generous (Pitfall 4). It is the heart of the deepest-risk phase.
Output: `letter_scorer.dart`, the threshold-parameterized `geometric_stroke_scorer.dart`,
green `letter_scorer_test.dart`.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scoring-quality-calibration/04-CONTEXT.md
@.planning/phases/04-scoring-quality-calibration/04-RESEARCH.md
@.planning/phases/04-scoring-quality-calibration/04-PATTERNS.md
@.planning/phases/04-scoring-quality-calibration/04-01-SUMMARY.md
</context>

## Phase Goal

**As a** child learning to write Arabic, **I want to** have my wrong-order, wrong-count,
and sloppy-shape attempts caught with a specific named fix while my good-faith
size-and-position-varied attempts pass, **so that** the feedback is honest and
encouraging instead of either too strict or too lenient.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize scoreStroke thresholds from tolerances and build the scoreLetter orchestrator</name>
  <files>lib/core/scoring/letter_scorer.dart, lib/core/scoring/geometric_stroke_scorer.dart</files>
  <read_first>
    - lib/core/scoring/geometric_stroke_scorer.dart (lines 11-26 threshold consts → now read from Tolerances; 35-59 scoreStroke leaf to wrap and parameterize; 68-82 feedbackForMistake)
    - lib/core/scoring/letter_scorer.dart (does not exist — create; PATTERNS analog is geometric_stroke_scorer.dart one level up)
    - lib/core/scoring/stroke_validation.dart (lines 121 dot==1 point+tap; 224-248 order 1..N + dots-last contract to REUSE, not re-derive)
    - lib/dev/authoring_export.dart (lines 63-75 _combinedBounds, 90-109 normalizeToStrokeSpecs — the whole-letter combined-bbox normalization to replicate for the dot-position check, Pitfall 2)
    - lib/core/scoring/stroke_resampler.dart (resample, normalizeToUnitBox — existing primitives)
    - lib/core/scoring/scoring_models.dart (extended MistakeId + LetterResult from Plan 01)
    - lib/models/letter.dart (Letter.tolerances, referenceStrokes, StrokeSpec)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: letter_scorer.dart — THE SPINE; Pattern 2)
  </read_first>
  <behavior>
    - scoreStroke now accepts/reads tolerances (minRawPoints, resampleN, maxCurvature) from the letter's Tolerances instead of file-level consts; alif with the `normal` preset reproduces the existing geometric_stroke_scorer_test fixtures exactly (cleanAlif passes, tooShort/inverted/curved fail with the same MistakeIds — RESEARCH A5 behavior-preserving).
    - scoreLetter on a baa whose childStrokes count != referenceStrokes count → LetterResult.fail(MistakeId.wrongStrokeCount). COUNT stays firm regardless of shape tolerance (Pitfall 4).
    - scoreLetter on baa drawn in the wrong order (dot before body) → MistakeId.wrongStrokeOrder.
    - scoreLetter on a baa body + two-dots-above (taa pattern shown for baa) → MistakeId.dotMisplaced (dot count/position vs reference; whole-letter combined-bbox normalization so relative dot position survives — Pitfall 2).
    - scoreLetter on a small/offset but correct baa → LetterResult.pass() (SC#3 — combined-bbox normalization prevents penalizing size/offset).
    - scoreLetter completes well under the sub-300 ms whole-letter budget (latency assert).
  </behavior>
  <action>
    Refactor `geometric_stroke_scorer.dart` so `scoreStroke` reads its thresholds from a
    `Tolerances` argument (or a tolerances-bearing parameter) instead of the file-level
    `_kMinRawPoints`/`_kResampleN`/`_kMaxCurvature`. Keep the doc-comment rationale beside
    the now-default `normal` values. Do NOT change predicate function names
    (strokeLengthBelowThreshold/strokeDirectionInverted/strokeCurvatureExceedsThreshold) —
    the check-string contract is binding.
    Create `lib/core/scoring/letter_scorer.dart` — pure Dart (no dart:ui/Flutter; copy the
    header from scoring_models.dart:1). Implement
    `LetterResult scoreLetter(List<List<List<double>>> childStrokes, Letter letter,
    {HandwritingRecognizer? recognizer})`: partition referenceStrokes into body (type !=
    'dot') and dots (type == 'dot') exactly as stroke_validation does. (1) COUNT: if
    childStrokes.length != referenceStrokes.length → fail(wrongStrokeCount). (2) ORDER +
    per-stroke: validate draw order against reference order and delegate each body stroke to
    `scoreStroke(..., letter.tolerances)`; on a mismatch return wrongStrokeOrder, on a
    per-stroke shape failure return the per-stroke MistakeId. (3) DOT: normalize the WHOLE
    letter together (combined bbox per authoring_export _combinedBounds — NOT each stroke in
    isolation) and check dot count + relative position vs reference → dotMisplaced on
    mismatch. (4) leave a hook to consult `recognizer` only AFTER a geometric pass (the
    actual ML Kit gate policy is wired in Plan 04 / built in Plan 03; here accept an injected
    recognizer and apply the D-04 advisory rule if one is supplied, ignoring low confidence).
    Return LetterResult.pass() when all checks pass.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/letter_scorer_test.dart test/core/scoring/geometric_stroke_scorer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/letter_scorer_test.dart` is GREEN (the RED contract tests from Plan 01 now pass).
    - `flutter test test/core/scoring/geometric_stroke_scorer_test.dart` still GREEN with alif under the `normal` preset (behavior-preserving, A5).
    - `grep -v '^//' lib/core/scoring/letter_scorer.dart | grep -c "import 'dart:ui'\|package:flutter"` returns 0.
    - wrong-count baa → MistakeId.wrongStrokeCount; wrong-order → wrongStrokeOrder; taa-dots-for-baa → dotMisplaced; small/offset correct baa → passed (all asserted).
    - `grep -c "referenceStrokes.first" lib/core/scoring/letter_scorer.dart` returns 0 (no single-stroke shortcut in the orchestrator).
  </acceptance_criteria>
  <done>scoreLetter orchestrates count/order/per-stroke/dot over a whole letter, reads per-letter tolerances, uses combined-bbox normalization, and the Plan 01 contract tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SC#3 multi-stroke normalization regression coverage</name>
  <files>test/core/scoring/geometric_stroke_scorer_test.dart, test/core/scoring/letter_scorer_test.dart</files>
  <read_first>
    - test/core/scoring/geometric_stroke_scorer_test.dart (the smallCorrect fixture that already proves single-stroke size-invariance; extend to multi-stroke)
    - test/core/scoring/scoring_fixtures.dart (lines 9-47: named point-list fixture shape to replicate)
    - lib/core/scoring/letter_scorer.dart (the scoreLetter just built)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: calibration_fixtures / SC#3)
  </read_first>
  <behavior>
    - A baa drawn at ~40% scale and offset toward a corner, correct order/count/dot, passes (combined-bbox normalization handles size+offset — SC#3).
    - The SAME small baa but with the dot placed ABOVE the body fails with dotMisplaced (normalization does NOT erase the dot's relative position — Pitfall 2 guard).
  </behavior>
  <action>
    Add inline multi-stroke baa fixtures (correct-small-offset, and small-but-dot-above) to
    `letter_scorer_test.dart` (or a shared fixture file mirroring scoring_fixtures.dart) and
    assert the two behaviors above. This is the explicit SC#3 + Pitfall-2 regression pair
    proving normalization helps good-faith attempts without hiding the ب/ت/ث dot distinction.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/` all green.
    - A small+offset correct baa passes; the same small baa with the dot misplaced fails with MistakeId.dotMisplaced (both asserted in the same test group).
  </acceptance_criteria>
  <done>SC#3 normalization and the Pitfall-2 dot-position guard are encoded as passing regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child strokes (in-memory) → scoreLetter | Untrusted point arrays; must be bounded/validated, never logged or persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | `letter_scorer.dart` | mitigate | Pure-Dart scorer holds child points only in local variables; no `print`/`debugPrint`/persistence of raw strokes (in-memory-only discipline T-01-05). Only the derived LetterResult leaves the function. |
| T-04-04 | DoS | `scoreLetter` over malformed/huge stroke arrays | mitigate | COUNT check runs first; resample caps point counts to `resampleN`; latency assert guards the sub-300 ms budget so a pathological input cannot stall the UI. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan; slopcheck N/A. |
</threat_model>

<verification>
- `flutter test test/core/scoring/` fully green (orchestrator + alif behavior-preserving + SC#3 pair).
- Pure-Dart discipline confirmed by grep (no dart:ui/Flutter in letter_scorer.dart).
- No `referenceStrokes.first` shortcut in the orchestrator.
</verification>

<success_criteria>
- SC#1: wrong count/order/dot rejected with the correct named MistakeId.
- SC#3: size/offset-varied good-faith baa passes; misplaced-dot small baa still fails.
- SC#4 (partial): scorer reads per-letter tolerances; alif unchanged under `normal`.
</success_criteria>

<output>
Create `.planning/phases/04-scoring-quality-calibration/04-02-SUMMARY.md` when done.
</output>
