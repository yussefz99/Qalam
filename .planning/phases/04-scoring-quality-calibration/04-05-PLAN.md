---
phase: 04-scoring-quality-calibration
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - lib/dev/authoring_screen.dart
  - lib/dev/authoring_export.dart
  - test/core/scoring/calibration_harness_test.dart
  - test/core/scoring/calibration_fixtures/calibration_fixtures.dart
  - test/core/scoring/calibration_fixtures/README.md
autonomous: true
requirements: [S1-05, PLAT-03]
must_haves:
  truths:
    - "Labeled child-sample capture exists as a dev-only mode behind /dev (never child-facing), exporting labeled coordinate fixtures (D-02)"
    - "The calibration harness runs the REAL scoreLetter over labeled fixtures and reports per-letter false-positive/false-negative counts (SC#4)"
    - "Named common mistakes are encoded as regression fixtures — known-bad rejected, known-good accepted (SC#4)"
  artifacts:
    - path: "test/core/scoring/calibration_harness_test.dart"
      provides: "Pure-Dart confusion-table harness over labeled fixtures (no Python re-impl — RESEARCH A3)"
      contains: "scoreLetter"
    - path: "test/core/scoring/calibration_fixtures/calibration_fixtures.dart"
      provides: "Labeled multi-stroke fixture sets per letter per category (synthetic seed; real-tablet sets land in Plan 06)"
  key_links:
    - from: "test/core/scoring/calibration_harness_test.dart"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "runs the real scorer over every labeled fixture"
      pattern: "scoreLetter"
    - from: "lib/dev/authoring_screen.dart"
      to: "test/core/scoring/calibration_fixtures/"
      via: "labeled-sample export writes fixture coordinate files"
      pattern: "label|fixture"
---

<objective>
Stand up the calibration infrastructure: extend the Phase 02.1 authoring screen into a
**labeled-sample capture mode** (D-02) that exports labeled coordinate fixtures, and a
pure-Dart **calibration harness** (`flutter test`, NOT a Python re-implementation) that
runs the real `scoreLetter` over labeled fixtures and reports per-letter false-positive /
false-negative counts. Seed it with synthetic placeholder fixtures encoding each named
common mistake so the harness is green and the regression contract (known-bad rejected,
known-good accepted) is in place; the real-tablet child samples replace the synthetic
seed in Plan 06.

Purpose: SC#4 infrastructure — the harness + fixtures are the mechanism by which the
owner's mother tunes tolerances and by which named common mistakes become regression
tests. Building it autonomously here (with synthetic seed) lets the whole phase ship its
machinery without blocking on tablet/child access; Plan 06 swaps in real samples.
Output: labeled-capture authoring mode, the confusion-table harness, seed fixtures + a
README documenting the fixture format and the FN-over-FP tuning priority.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scoring-quality-calibration/04-CONTEXT.md
@.planning/phases/04-scoring-quality-calibration/04-RESEARCH.md
@.planning/phases/04-scoring-quality-calibration/04-PATTERNS.md
@.planning/phases/04-scoring-quality-calibration/04-02-SUMMARY.md
</context>

## Phase Goal

**As the** owner's mother (pedagogical authority), **I want to** capture real labeled
child handwriting samples and see how the scorer classifies them per letter, **so that**
I can tune each letter's pass tolerances against real children without touching code.

<tasks>

<task type="auto">
  <name>Task 1: Extend the authoring screen into a labeled-sample capture mode (D-02)</name>
  <files>lib/dev/authoring_screen.dart, lib/dev/authoring_export.dart</files>
  <read_first>
    - lib/dev/authoring_screen.dart (lines 11-14 SECURITY in-memory-only; 31-38 _types/_directions dropdown idiom; 250-284 _CaptureSurface Listener; 324-418 _TagAndExportPanel export panel to extend with a label selector)
    - lib/dev/authoring_export.dart (lines 6 pure-Dart header; 63-109 _combinedBounds + normalizeToStrokeSpecs — the whole-letter normalization to reuse)
    - lib/router/app_router.dart (lines 56-62 the /dev/authoring route + kDebugMode reachability rule T-02.1-07)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: authoring_screen.dart + authoring_export.dart)
    - .planning/phases/04-scoring-quality-calibration/04-RESEARCH.md (§Calibration Methodology — the label taxonomy)
  </read_first>
  <action>
    Add a LABEL SELECTOR to the authoring export panel for the failure taxonomy: `good`,
    `wrong_order`, `wrong_direction`, `wrong_count`, `scribble`, `wrong_letter`, and the
    D-01-specific `taa_when_shown_baa`. Reuse the existing `_CaptureSurface` Listener +
    `normalizeToStrokeSpecs`/`_combinedBounds` (whole-letter combined-bbox — same normalization
    the orchestrator's dot check needs). Add an export path that writes a labeled multi-stroke
    coordinate fixture (letter id + label + `List<List<List<double>>>`) in the fixture format
    defined in Task 2 — NOT the `referenceStrokes` fragment. Keep the mode behind the existing
    `/dev/authoring` route + `kDebugMode`, NEVER child-facing (T-02.1-07). In-memory-only
    discipline (T-01-05): only labeled fixture coordinates (test data) are written; nothing
    transmitted, nothing logged.
  </action>
  <verify>
    <automated>flutter analyze lib/dev/authoring_screen.dart lib/dev/authoring_export.dart; grep -q "taa_when_shown_baa" lib/dev/authoring_screen.dart && grep -q "normalizeToStrokeSpecs" lib/dev/authoring_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/dev/` clean.
    - A label selector with the 7 taxonomy values exists in the authoring panel (grep for `taa_when_shown_baa` and `scribble`).
    - The labeled export reuses combined-bbox normalization (grep for `normalizeToStrokeSpecs` or `_combinedBounds` usage in the export path).
    - The capture mode stays behind `/dev/authoring` + `kDebugMode` (router unchanged for child nav; grep confirms no new child-facing route).
  </acceptance_criteria>
  <done>The authoring screen captures labeled multi-stroke child samples and exports them as fixtures, dev-only, reusing the proven whole-letter normalization.</done>
</task>

<task type="auto">
  <name>Task 2: Build the pure-Dart calibration harness + seed regression fixtures</name>
  <files>test/core/scoring/calibration_harness_test.dart, test/core/scoring/calibration_fixtures/calibration_fixtures.dart, test/core/scoring/calibration_fixtures/README.md</files>
  <read_first>
    - test/core/scoring/scoring_fixtures.dart (lines 9-47 named point-list fixture shape to replicate)
    - test/core/scoring/geometric_stroke_scorer_test.dart (the fixture-driven group-runner structure)
    - lib/core/scoring/letter_scorer.dart (scoreLetter — the REAL scorer the harness runs, Plan 02)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: calibration_harness_test.dart + calibration_fixtures/)
    - .planning/phases/04-scoring-quality-calibration/04-RESEARCH.md (§Calibration Methodology steps 5-6 — per-letter FP/FN, FN-over-FP priority; A3 Dart-not-Python)
  </read_first>
  <action>
    Define the labeled-fixture format in `calibration_fixtures.dart`: per-letter, per-label,
    multi-stroke `List<List<List<double>>>` lists, each carrying its label (e.g. a record or a
    small class `{letterId, label, strokes}`). Replicate the named-fixture doc-comment style of
    scoring_fixtures.dart. SEED it with SYNTHETIC fixtures (clearly marked synthetic) that
    encode each named common mistake for baa: a `good` baa, a `wrong_count`, a `wrong_order`,
    and a `taa_when_shown_baa`. (Real-tablet samples replace these in Plan 06 — emulator/mouse
    is rejected for tolerance-SETTING, but synthetic seed is fine to make the harness green and
    pin the regression contract per Pitfall 3's "unit tests only" carve-out.)
    Create `calibration_harness_test.dart`: load every labeled fixture, run the REAL
    `scoreLetter` over each (NOT a re-implementation — A3), and build a per-letter confusion
    table — count FALSE NEGATIVES (`good` samples rejected) and FALSE POSITIVES (`wrong_*` /
    `scribble` / `taa_when_shown_baa` samples passed). Assert the regression contract: every
    `good` seed passes and every named-bad seed is rejected with the expected MistakeId. Print
    the confusion table (test output) so the mother + owner can read per-letter FP/FN during
    tuning. Document in README.md: the fixture format, how to add real-tablet captures (from
    Task 1's export), and the FN-over-FP tuning priority (lean encouraging for good-faith;
    keep order/count/identity firm — RESEARCH step 5).
  </action>
  <verify>
    <automated>flutter test test/core/scoring/calibration_harness_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/calibration_harness_test.dart` green: every `good` seed passes, every named-bad seed is rejected with the expected MistakeId.
    - The harness calls the real `scoreLetter` (grep for `scoreLetter` in the harness; no re-implemented scoring math).
    - The harness emits a per-letter FP/FN confusion table to test output.
    - README documents the fixture format + the FN-over-FP tuning priority and that real-tablet samples (Plan 06) replace the synthetic seed.
  </acceptance_criteria>
  <done>A pure-Dart confusion-table harness runs the real scorer over labeled fixtures, the named-common-mistake regression contract is green on synthetic seeds, and the format is documented for real-sample replacement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| labeled-sample capture (dev) → fixture files | Captured child coordinates written as test data only; never transmitted; dev-only reachability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | labeled-sample capture mode | mitigate | Capture stays behind `/dev/authoring` + `kDebugMode`, never in child nav (T-02.1-07); only labeled fixture coordinates (intended test data) are written, nothing transmitted, nothing logged (T-01-05). No PII — coordinates only, no name/age. |
| T-04-12 | Tampering/DoS | calibration_harness over malformed fixtures | mitigate | Harness runs the same validated scoreLetter; malformed fixtures surface as test failures, not crashes. Pure-Dart, headless. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan; slopcheck N/A. |
</threat_model>

<verification>
- `flutter test test/core/scoring/calibration_harness_test.dart` green.
- `flutter analyze lib/dev/` clean.
- Capture mode dev-only (no new child-facing route).
- Harness runs the real scoreLetter (no re-implementation — A3).
</verification>

<success_criteria>
- SC#4 infra: labeled-sample capture + confusion-table harness + documented fixture format.
- Named common mistakes encoded as regression fixtures (synthetic seed green; real samples in Plan 06).
</success_criteria>

<output>
Create `.planning/phases/04-scoring-quality-calibration/04-05-SUMMARY.md` when done.
</output>
