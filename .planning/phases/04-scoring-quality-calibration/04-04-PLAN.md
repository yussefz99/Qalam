---
phase: 04-scoring-quality-calibration
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - lib/features/practice/widgets/stroke_canvas.dart
  - lib/features/practice/practice_screen.dart
  - lib/providers/practice_providers.dart
  - lib/l10n/app_en.arb
  - test/features/practice/multi_stroke_capture_test.dart
  - test/features/practice/getting_ready_test.dart
  - test/core/scoring/mistake_mapping_test.dart
autonomous: true
requirements: [S1-05, PLAT-03]
must_haves:
  truths:
    - "The capture surface accumulates a whole multi-stroke letter instead of discarding prior strokes (closes the structural gap that blocks SC#1)"
    - "practice_screen scores the whole accumulated letter via scoreLetter, not a single stroke against referenceStrokes.first"
    - "Every new failure category (count/order/dot/identity) shows authored, specific feedback — never a generic fallback (PLAT-03 / Pitfall 7)"
    - "If the ML Kit model is not yet cached, the practice flow shows a calm 'getting ready' state, not an error (D-05)"
  artifacts:
    - path: "lib/features/practice/widgets/stroke_canvas.dart"
      provides: "Whole-letter accumulation (no per-pointer-down clear) + letter-complete signal"
    - path: "lib/l10n/app_en.arb"
      provides: "Authored feedback strings for wrongStrokeCount/wrongStrokeOrder/dotMisplaced/wrongLetterIdentity + getting-ready copy"
  key_links:
    - from: "lib/features/practice/practice_screen.dart"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "scoreLetter(accumulatedStrokes, letter) after letter-complete signal"
      pattern: "scoreLetter"
    - from: "lib/features/practice/practice_screen.dart"
      to: "lib/services/model_download_service.dart"
      via: "consults isReady to show getting-ready vs practice"
      pattern: "isReady|ModelDownloadService"
---

<objective>
Wire the spine into the UI: fix the multi-stroke accumulation gap in `StrokeCanvas` (it
clears prior strokes on every pointer-down today), make `practice_screen` call
`scoreLetter` over the whole accumulated letter (instead of `scoreStroke` against
`referenceStrokes.first`), author the feedback strings + l10n for the four new failure
categories so none fall through to the generic fallback, surface the ML Kit "getting
ready" state from the model-download service, and apply the D-04 advisory ML Kit gate in
the practice flow.

Purpose: This is the task that makes SC#1 actually *reachable by a child* — the
orchestrator from Plan 02 is dead code until capture accumulates a whole letter and the
screen calls it. It also lands SC#2 (gate consulted on a geometric pass) and closes the
PLAT-03 / Pitfall-7 loop (authored copy for every new failure).
Output: accumulating canvas, scoreLetter-driven practice screen, authored l10n,
getting-ready state, the session state-machine change for whole-letter verdicts.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scoring-quality-calibration/04-CONTEXT.md
@.planning/phases/04-scoring-quality-calibration/04-RESEARCH.md
@.planning/phases/04-scoring-quality-calibration/04-PATTERNS.md
@.planning/phases/04-scoring-quality-calibration/04-02-SUMMARY.md
@.planning/phases/04-scoring-quality-calibration/04-03-SUMMARY.md
@docs/design/kit/project/SKILL.md
</context>

## Phase Goal

**As a** child learning to write Arabic, **I want to** draw a whole multi-stroke letter
and get a specific, warm fix when my order/count/dot/identity is wrong (and a quiet pass
when it is right), **so that** the practice loop is honest and encouraging on real
multi-stroke letters like baa.

<tasks>

<task type="auto">
  <name>Task 1: Fix the multi-stroke accumulation gap in StrokeCanvas and add a letter-complete signal</name>
  <files>lib/features/practice/widgets/stroke_canvas.dart, test/features/practice/multi_stroke_capture_test.dart</files>
  <read_first>
    - lib/features/practice/widgets/stroke_canvas.dart (lines 88-96 _onDown with the `_completedStrokes.clear()` bug; 115-126 _commitStroke single-stroke forward; 14-18 SECURITY in-memory-only header T-01-05; 226-245 _buildScaledPath already lifts pen between strokes)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: stroke_canvas.dart — STRUCTURAL TRAP)
    - .planning/phases/04-scoring-quality-calibration/04-RESEARCH.md (Pitfall 1 multi-stroke capture gap; Open Q1 completion trigger)
  </read_first>
  <action>
    Remove the `_completedStrokes.clear()` in `_onDown` so completed strokes accumulate into a
    whole-letter `List<List<Offset>>`. Keep `_commitStroke` appending each finished stroke to
    `_completedStrokes` (stop the `..clear()..add` single-stroke pattern). Add a deliberate
    **letter-complete signal** — Claude's-discretion per Open Q1; recommend count-reached for a
    2-part letter (fire when accumulated stroke count == `referenceStrokes.length`), exposed via
    a new `onLetterComplete(List<List<Offset>> strokes)` callback (keep `onStrokeSubmitted` for
    per-stroke immediate feedback if retained). Preserve the SECURITY in-memory-only discipline
    (T-01-05): accumulated points are never logged/persisted; only the callback carries them.
    Add `test/features/practice/multi_stroke_capture_test.dart` (widget test): simulate two
    stylus strokes (pointer down/move/up twice) and assert the canvas accumulates BOTH strokes
    and fires `onLetterComplete` with a 2-element list (not 1, not cleared).
  </action>
  <verify>
    <automated>flutter test test/features/practice/multi_stroke_capture_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_completedStrokes.clear()" lib/features/practice/widgets/stroke_canvas.dart` returns 0.
    - `flutter test test/features/practice/multi_stroke_capture_test.dart` green: two strokes accumulate and onLetterComplete fires once with a 2-element List<List<Offset>>.
    - The SECURITY header / in-memory-only comment is preserved (grep for it).
  </acceptance_criteria>
  <done>The canvas accumulates a whole multi-stroke letter and signals completion; the single-stroke clear bug is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Score the whole letter in practice_screen, author feedback l10n, and surface the getting-ready + ML Kit gate</name>
  <files>lib/features/practice/practice_screen.dart, lib/providers/practice_providers.dart, lib/l10n/app_en.arb, test/features/practice/getting_ready_test.dart, test/core/scoring/mistake_mapping_test.dart</files>
  <read_first>
    - lib/features/practice/practice_screen.dart (lines 77-93 score call + SECURITY guard; 222-225 the `referenceStrokes.first` hardcode to replace; 417-424 _kThinkBeat UX beat; 1117-1132 _feedbackString switch to extend)
    - lib/providers/practice_providers.dart (lines 141-174 onStrokeResult state machine — resets streak on any miss; the per-stroke-vs-whole-letter change, Open Q2)
    - lib/core/scoring/letter_scorer.dart (scoreLetter from Plan 02)
    - lib/services/model_download_service.dart (isReady from Plan 03)
    - lib/core/recognition/handwriting_recognizer.dart (widened identify seam from Plan 03 — the D-04 advisory rule applied here)
    - assets/curriculum/letters.json (alif commonMistakes feedback — the authored-voice precedent for the new strings)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: practice_screen.dart; Shared Pattern: authored feedback only)
    - docs/design/kit/project/SKILL.md (coral not red, no emoji, Western numerals, no letterSpacing on Arabic)
  </read_first>
  <action>
    Replace the single-stroke score path: have practice_screen call
    `scoreLetter(accumulatedStrokes, letter, recognizer: <MlKitRecognizer when isReady>)` once on
    the `onLetterComplete` signal, instead of `scoreStroke(..., referenceStrokes.first)` at
    lines 222-225. Keep the Offset→`List<List<double>>` conversion and the SECURITY guard
    (raw points stay local; only the LetterResult enters the controller). Apply the D-04
    advisory rule when wiring the recognizer (reject only on a confidently-different letter
    AFTER a geometric pass; ignore low confidence — Pitfall 1).
    Update `PracticeSessionController.onStrokeResult` (or add an `onLetterResult`) so
    count/order/identity are whole-letter verdicts surfaced after the last stroke, while
    per-stroke shape feedback can stay immediate (Open Q2 — keep the warm coaching beat).
    Add the four new `_feedbackString` cases (lines 1117-1132) — wrongStrokeCount,
    wrongStrokeOrder, dotMisplaced, wrongLetterIdentity — each returning an l10n string,
    never the generic fallback (Pitfall 7). Add those keys to `app_en.arb` in the authored
    tutor voice (placeholder copy the mother refines in Plan 06; warm + specific, e.g. "Baa
    is two parts — the boat, then one dot underneath."). Run `flutter gen-l10n`.
    Show the model-download "getting ready" state: when `ModelDownloadService.isReady` is
    false and the child reaches practice, render a calm getting-ready state (no error, no hard
    block, D-05); geometric scoring still works, only the ML Kit gate is skipped until ready.
    Extend `mistake_mapping_test.dart` to assert each new MistakeId maps to a non-empty
    authored string (no fallback). Add `getting_ready_test.dart` (widget): model-not-ready →
    getting-ready UI shown, not an error.
  </action>
  <verify>
    <automated>flutter gen-l10n; flutter test test/features/practice/getting_ready_test.dart test/core/scoring/mistake_mapping_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "referenceStrokes.first" lib/features/practice/practice_screen.dart` returns 0 (single-stroke score path removed).
    - `grep -c "scoreLetter" lib/features/practice/practice_screen.dart` ≥ 1.
    - mistake_mapping_test asserts wrongStrokeCount/wrongStrokeOrder/dotMisplaced/wrongLetterIdentity each map to a non-empty authored l10n string (no MistakeId.fallback for these) — green.
    - getting_ready_test: model-not-ready renders the getting-ready state, not an error — green.
    - The D-04 advisory rule is applied where the recognizer is consulted (a good geometric pass + a wrong ML Kit candidate still passes — covered by the Plan 01/02 scorer test; here verify the screen passes the recognizer only when isReady).
    - No emoji / no `letterSpacing` on Arabic in the new copy (design-kit compliance).
  </acceptance_criteria>
  <done>The practice loop scores the whole accumulated letter, every new failure shows authored specific feedback, the ML Kit gate is consulted advisorily when ready, and the getting-ready state replaces any hard block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child strokes (in-memory) → practice_screen → scoreLetter | Untrusted points; raw strokes must stay local and never be logged/persisted |
| ML Kit model availability → practice UI | Not-ready state must degrade gracefully, never error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | accumulating `stroke_canvas.dart` + practice_screen | mitigate | Preserve the in-memory-only SECURITY discipline (T-01-05): accumulated multi-stroke points are never logged/persisted; only the derived LetterResult crosses into the controller. |
| T-04-09 | Availability | getting-ready path when model not cached | mitigate | D-05 calm getting-ready state; geometric scoring still works without the gate; no hard block (best-effort). |
| T-04-10 | Spoofing/Integrity | ML Kit gate over-trusted | mitigate | D-04 advisory-only: gate is consulted only after a geometric pass and only rejects a confidently-different letter; never overrides a good-faith pass (Pitfall 1). |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan; slopcheck N/A. |
</threat_model>

<verification>
- `flutter test test/features/practice/` green (multi-stroke capture + getting-ready).
- `flutter test test/core/scoring/mistake_mapping_test.dart` green (authored copy for every new MistakeId).
- No `referenceStrokes.first` and no `_completedStrokes.clear()` remain (grep).
- `flutter gen-l10n` succeeds; new keys present in app_en.arb.
</verification>

<success_criteria>
- SC#1 reachable by a child: whole-letter capture → scoreLetter → named fix.
- SC#2 in the flow: ML Kit gate consulted advisorily when ready.
- PLAT-03 / Pitfall 7: every new failure shows authored specific feedback.
- D-05: getting-ready state, never a hard block.
</success_criteria>

<output>
Create `.planning/phases/04-scoring-quality-calibration/04-04-SUMMARY.md` when done.
</output>
