---
phase: 04-scoring-quality-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/scoring/scoring_models.dart
  - lib/core/scoring/tolerances.dart
  - lib/models/letter.dart
  - lib/core/scoring/stroke_validation.dart
  - test/core/scoring/letter_scorer_test.dart
  - test/core/scoring/tolerances_test.dart
autonomous: true
requirements: [S1-05, PLAT-03]
must_haves:
  truths:
    - "Per-letter tolerances are read from curriculum data, not code constants (SC#4)"
    - "The `normal` preset equals today's hardcoded scorer constants (behavior-preserving — RESEARCH A5)"
    - "Every new failure category (count/order/dot/identity) has a MistakeId whose name mirrors a commonMistakes[].check string (PLAT-03 / Pitfall 7)"
  artifacts:
    - path: "lib/core/scoring/tolerances.dart"
      provides: "Tolerances class — preset→numeric expansion (loose/normal/strict + overrides), pure Dart"
      contains: "class Tolerances"
    - path: "lib/core/scoring/scoring_models.dart"
      provides: "Extended MistakeId enum (count/order/dot/identity) + LetterResult value class"
      contains: "wrongStrokeCount"
    - path: "lib/models/letter.dart"
      provides: "Letter.tolerances field (nullable, falls back to normal)"
      contains: "Tolerances? tolerances"
    - path: "test/core/scoring/letter_scorer_test.dart"
      provides: "Failing RED tests for SC#1/SC#2/D-04 — the contract scoreLetter must satisfy in Plan 02"
  key_links:
    - from: "lib/core/scoring/scoring_models.dart"
      to: "assets/curriculum/letters.json commonMistakes[].check"
      via: "enum-name == check-string contract"
      pattern: "wrongStrokeCount|strokeCountMismatch"
    - from: "lib/models/letter.dart"
      to: "lib/core/scoring/tolerances.dart"
      via: "Letter.fromJson parses tolerances block"
      pattern: "Tolerances.fromJson"
---

<objective>
Lay the pure-Dart contract foundation for the whole-letter scorer: extend `MistakeId`
with the four new failure categories, add the `LetterResult` value class, relocate the
hardcoded scorer thresholds into a data-driven `Tolerances` class (with `normal` ==
today's constants so the move is behavior-preserving), add a `tolerances` field to the
`Letter` model, extend the load-time validator to cover it, and write the **RED** unit
tests that define exactly what `LetterScorer.scoreLetter` (Plan 02) must do.

Purpose: This is the interface-first / Wave-0 task. Plan 02 implements against the
contracts and failing tests defined here; everything downstream (UI wiring, ML Kit gate,
calibration) depends on these types existing. It directly satisfies SC#4's "tolerances
are data not code" requirement and sets up SC#1/SC#2 to become real.
Output: Extended scoring models, `tolerances.dart`, `Letter.tolerances`, extended
validator, and failing `letter_scorer_test.dart` + `tolerances_test.dart`.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scoring-quality-calibration/04-CONTEXT.md
@.planning/phases/04-scoring-quality-calibration/04-RESEARCH.md
@.planning/phases/04-scoring-quality-calibration/04-PATTERNS.md
</context>

## Phase Goal

**As a** child learning to write Arabic, **I want to** have my wrong-order, wrong-count,
and sloppy-shape attempts caught with a specific named fix while my good-faith
size-and-position-varied attempts pass, **so that** the feedback is honest and
encouraging instead of either too strict or too lenient.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MistakeId + LetterResult and create the data-driven Tolerances class</name>
  <files>lib/core/scoring/scoring_models.dart, lib/core/scoring/tolerances.dart, test/core/scoring/tolerances_test.dart</files>
  <read_first>
    - lib/core/scoring/scoring_models.dart (the enum + StrokeResult to extend — PATTERNS analog is itself)
    - lib/core/scoring/geometric_stroke_scorer.dart (lines 11-26: the `_kMinRawPoints=10`, `_kResampleN=32`, `_kMaxCurvature=0.25` consts that move into Tolerances; lines 35-59: scoreStroke shape; lines 68-82: feedbackForMistake map)
    - lib/models/letter.dart (lines 55-70: StrokeSpec.fromJson — the fromJson parsing idiom to replicate for Tolerances.fromJson)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (sections: scoring_models.dart, tolerances.dart)
  </read_first>
  <behavior>
    - tolerances_test: Tolerances.fromJson({"preset":"normal"}) yields minRawPoints==10, resampleN==32, maxCurvature==0.25 (LOCKED behavior-preserving anchor, RESEARCH A5).
    - tolerances_test: Tolerances.fromJson({"preset":"loose"}) yields a lower maxCurvature strictness (more permissive) than normal; "strict" yields stricter than normal.
    - tolerances_test: Tolerances.fromJson({"preset":"normal","overrides":{"maxCurvature":0.30}}) yields maxCurvature==0.30 with the other knobs still at normal.
    - tolerances_test: an unknown/absent preset falls back to normal (no throw — pure value parsing).
  </behavior>
  <action>
    In `scoring_models.dart`, add four values to `MistakeId` keeping the enum-name ==
    `commonMistakes[].check` contract documented at lines 7-9: `wrongStrokeCount` (check
    "strokeCountMismatch"), `wrongStrokeOrder` (check "strokeOrderWrong"), `dotMisplaced`
    (check "dotPositionWrong"/"dotCountWrong"), `wrongLetterIdentity` (check
    "letterIdentityMismatch", the ML Kit gate per D-04). Add a `LetterResult` value class
    mirroring `StrokeResult` (lines 17-25): `final bool passed`, `final MistakeId?
    mistakeId`, `const` constructor, plus `LetterResult.fail(MistakeId id)` and
    `LetterResult.pass()` factories.
    Create `lib/core/scoring/tolerances.dart` — pure Dart (no dart:ui/Flutter, copy the
    header discipline from scoring_models.dart:1). Define a `Tolerances` class holding at
    least `minRawPoints`, `resampleN`, `maxCurvature` (candidate extra knobs from D-03 —
    minStrokeLength, directionStrictness, shapeDistanceThreshold, dotPositionTolerance —
    add only what Plan 02 will read). Define a `_presets` map with `loose`/`normal`/`strict`
    where `normal` == the three consts pulled from geometric_stroke_scorer.dart:16,21,26.
    Add `Tolerances.fromJson` that reads `preset` then applies numeric `overrides`. Preserve
    each const's doc-comment rationale alongside its value. Per D-03, this satisfies SC#4
    (data not code) and is teacher-legible via named presets.
    Write `tolerances_test.dart` covering the behavior block above.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/tolerances_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/tolerances_test.dart` passes.
    - `grep -v '^//' lib/core/scoring/tolerances.dart | grep -c "import 'dart:ui'\|package:flutter"` returns 0 (pure-Dart discipline).
    - `Tolerances.fromJson({'preset':'normal'})` produces minRawPoints==10, resampleN==32, maxCurvature==0.25 (asserted in test).
    - MistakeId contains wrongStrokeCount, wrongStrokeOrder, dotMisplaced, wrongLetterIdentity; LetterResult exposes `.fail(id)` and `.pass()`.
  </acceptance_criteria>
  <done>Tolerances class is data-driven with a behavior-preserving normal preset; MistakeId + LetterResult extended; tolerances_test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Letter.tolerances field, extend the load-time validator, and write the RED LetterScorer contract tests</name>
  <files>lib/models/letter.dart, lib/core/scoring/stroke_validation.dart, test/core/scoring/letter_scorer_test.dart</files>
  <read_first>
    - lib/models/letter.dart (lines 117, 133-154: the AudioRef nullable-nested-model precedent to copy for tolerances; line 64-66: StrokeSpec.type `?? 'line'` backward-compat default)
    - lib/core/scoring/tolerances.dart (created in Task 1 — the type Letter.tolerances holds)
    - lib/core/scoring/stroke_validation.dart (lines 83 validateStroke, 213 validateReferenceStrokes, 121 dot semantics, 224-248 order + dots-last contract — returns violation strings, never throws)
    - test/core/scoring/mistake_mapping_test.dart (lines 19-68: inline Letter/StrokeSpec builder idiom for tests)
    - test/core/scoring/geometric_stroke_scorer_test.dart (lines 30-62: group-per-category test structure; 64-76: latency assert pattern)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (sections: letter.dart, letters.json, letter_scorer_test.dart)
  </read_first>
  <behavior>
    - Letter.fromJson with no "tolerances" key parses successfully with tolerances == null (backward-compat for Phase-2 entries).
    - Letter.fromJson with a "tolerances" block parses it into a Tolerances via Tolerances.fromJson.
    - validateReferenceStrokes still passes for valid baa-shaped data; rejects a tolerances block with an out-of-range numeric override (returns a violation string, does not throw).
    - letter_scorer_test (RED — these MUST fail now because scoreLetter does not exist yet; mark the import/symbol so the suite compiles as a failing test, e.g. a skipped-but-present test or a TODO-guarded expectation per the analog): defines the contract for Plan 02 — wrong stroke COUNT on a 2-stroke baa returns LetterResult(passed:false, MistakeId.wrongStrokeCount); wrong stroke ORDER returns MistakeId.wrongStrokeOrder; taa-dots-when-shown-baa returns MistakeId.dotMisplaced; a mocktail fake HandwritingRecognizer returning a confidently-different letter on a geometric-pass returns MistakeId.wrongLetterIdentity; and the D-04 no-override rule — a good geometric pass with a low-confidence/wrong ML Kit candidate STILL passes.
  </behavior>
  <action>
    In `letter.dart`, add `final Tolerances? tolerances;` to `Letter`, parsing it in
    `Letter.fromJson` exactly like the `audio` precedent (line 117/136/152): read
    `json['tolerances'] as Map<String,dynamic>?`, build via `Tolerances.fromJson` when
    present, null otherwise. Import tolerances.dart.
    In `stroke_validation.dart`, extend `validateReferenceStrokes` (or add a sibling
    `validateTolerances`) to range-check the `tolerances` block (reject out-of-range numeric
    overrides) — return violation strings, never throw, mirroring the existing V5 pattern.
    Create `test/core/scoring/letter_scorer_test.dart` as the RED contract test: inline
    `Letter`/`StrokeSpec` builders for a 2-stroke baa (body line + dot tap) mirroring
    mistake_mapping_test.dart:19-68. Add a `mocktail` fake `HandwritingRecognizer`. Cover the
    behaviors above. Add a whole-letter latency assert stub (< sub-300 ms budget) mirroring
    geometric_stroke_scorer_test.dart:64-76. These tests fail until Plan 02 ships
    `scoreLetter` — that is intentional (Wave-0 RED). Add `mocktail: ^1.0.5` to
    dev_dependencies if absent (STACK.md).
  </action>
  <verify>
    <automated>flutter test test/core/scoring/ 2>&1 | grep -E "letter_scorer_test|tolerances_test|All tests|Some tests"; flutter analyze lib/models/letter.dart lib/core/scoring/stroke_validation.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` reports no errors on letter.dart, tolerances.dart, stroke_validation.dart, scoring_models.dart.
    - A Letter JSON map omitting "tolerances" parses with tolerances == null (asserted in a model test or letter_scorer_test setup).
    - `letter_scorer_test.dart` compiles and is present, asserting MistakeId.wrongStrokeCount/wrongStrokeOrder/dotMisplaced/wrongLetterIdentity expectations and the D-04 no-override rule; it is RED (scoreLetter unimplemented) — Plan 02 turns it green.
    - validateReferenceStrokes returns a non-empty violation list for an out-of-range tolerances override (asserted).
  </acceptance_criteria>
  <done>Letter carries an optional tolerances block; validator guards it; the RED LetterScorer contract test exists and defines SC#1/SC#2/D-04 for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curriculum JSON → Letter model | Bundled asset data parsed at load; malformed tolerances/strokes must not crash the scorer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/DoS | `validateReferenceStrokes` / new tolerances validation | mitigate | Range-check the tolerances block at load; return violation strings (never throw) so malformed curriculum data degrades gracefully rather than crashing (V5 input validation). |
| T-04-02 | Information Disclosure | `tolerances.dart` / `scoring_models.dart` | accept | Pure-Dart value types; no child stroke data, no PII, no I/O. No mitigation needed. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan (mocktail is a pre-vetted pub.dev dev-dep per STACK.md, not npm/pip/cargo); slopcheck N/A. |
</threat_model>

<verification>
- `flutter test test/core/scoring/tolerances_test.dart` green.
- `flutter analyze` clean on all modified lib files.
- `letter_scorer_test.dart` present and RED (defines the Plan 02 contract).
- No `dart:ui`/Flutter import in tolerances.dart (grep).
</verification>

<success_criteria>
- Tolerances are data-driven with a behavior-preserving `normal` preset (SC#4 foundation).
- MistakeId + LetterResult extended; the enum-name == check-string contract holds for all four new categories (PLAT-03 / Pitfall 7).
- Letter.tolerances parses backward-compatibly; validator guards it (V5).
- The RED contract test fully specifies scoreLetter for Plan 02 (SC#1/SC#2/D-04).
</success_criteria>

<output>
Create `.planning/phases/04-scoring-quality-calibration/04-01-SUMMARY.md` when done.
</output>
