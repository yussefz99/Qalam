---
phase: 04-scoring-quality-calibration
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - assets/curriculum/letters.json
  - lib/l10n/app_en.arb
  - test/core/scoring/calibration_fixtures/calibration_fixtures.dart
  - test/core/scoring/calibration_harness_test.dart
  - test/core/scoring/letters_signoff_test.dart
autonomous: false
requirements: [S1-05, PLAT-03]
must_haves:
  truths:
    - "baa, taa, thaa carry authored reference strokes + common mistakes + per-letter tolerances, signed off by the owner's mother (D-01)"
    - "Per-letter pass tolerances are tuned against REAL-tablet child samples, false-negatives and false-positives separately, leaning encouraging for good-faith attempts (SC#4, Pitfall 3 > 4)"
    - "The 'wrote taa when shown baa' attempt is rejected and a good-faith size/position-varied attempt passes, proven on real samples (SC#1/SC#3)"
  artifacts:
    - path: "assets/curriculum/letters.json"
      provides: "baa/taa/thaa authored + signedOff:true + per-letter tolerances; alif tolerances block"
      contains: "\"signedOff\": true"
    - path: "test/core/scoring/calibration_fixtures/calibration_fixtures.dart"
      provides: "Real-tablet labeled child sample sets replacing the synthetic seed"
  key_links:
    - from: "assets/curriculum/letters.json tolerances"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "tuned per-letter tolerances drive the scorer (data not code)"
      pattern: "tolerances|preset"
---

<objective>
Close SC#4 with the owner's mother in the loop: author + sign off the baa-family (baa ب,
taa ت, thaa ث) reference strokes and common mistakes via the labeled-capture authoring
screen (D-01), capture REAL-tablet labeled child samples to replace the synthetic seed,
and tune each letter's pass tolerances — false-negatives and false-positives separately,
leaning encouraging for good-faith attempts while keeping order/count/identity firm. The
calibration harness reports per-letter FP/FN until the tuned tolerances satisfy the
regression contract.

Purpose: This is the pedagogical heart of the phase — tolerances and stroke shapes come
from the mother, not from code (Pitfall 6), and are tuned against real children, not the
emulator (Pitfall 3). It is deliberately isolated as the one human-gated, tablet-dependent
plan so the rest of the phase ships autonomously.
Output: signed-off baa/taa/thaa in letters.json with tuned per-letter tolerances,
real-tablet fixtures, finalized authored feedback copy, a green calibration harness on
real samples, and a sign-off gate.

NOTE (environment blocker per RESEARCH): this plan REQUIRES a real Android tablet + real
child handwriting samples + the owner's mother. It cannot be completed on emulator/mouse
(rejected for tolerance-setting). If hardware/children are unavailable, the rest of Phase 4
(Plans 01-05) still ships; this plan blocks at its checkpoints until access exists.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scoring-quality-calibration/04-CONTEXT.md
@.planning/phases/04-scoring-quality-calibration/04-RESEARCH.md
@.planning/phases/04-scoring-quality-calibration/04-PATTERNS.md
@.planning/phases/04-scoring-quality-calibration/04-04-SUMMARY.md
@.planning/phases/04-scoring-quality-calibration/04-05-SUMMARY.md
@.planning/phases/02.1-stroke-reference-correction/02.1-02-SUMMARY.md
</context>

## Phase Goal

**As the** owner's mother (the curriculum authority), **I want to** author and sign off
the baa-family strokes and tune each letter's pass tolerances against real children,
**so that** the scorer is as strict as it should be — firm on order/count/identity, kind
on good-faith shape — using my pedagogy, not guessed code constants.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author + sign off baa/taa/thaa and capture real-tablet labeled child samples</name>
  <read_first>
    - lib/dev/authoring_screen.dart (the labeled-capture mode shipped in Plan 05)
    - .planning/phases/02.1-stroke-reference-correction/02.1-02-SUMMARY.md (the alif authoring + overlay sign-off pattern to mirror)
    - .planning/phases/04-scoring-quality-calibration/04-RESEARCH.md (§Calibration Methodology — protocol, label taxonomy, ~15-20/letter/category, A4 ar-model spike)
  </read_first>
  <action>
    HUMAN-ACTION CHECKPOINT — requires a real Android tablet + the owner's mother + real children.
    Plans 01-05 shipped the labeled-sample capture mode (in the /dev/authoring screen), the
    scoreLetter orchestrator, the ML Kit gate, and the calibration harness with synthetic seed
    fixtures. This step uses them on a real tablet (NOT the emulator — emulator/mouse is rejected
    for tolerance-setting, Pitfall 3 / D-02). Steps:
    1. Open /dev/authoring. With the owner's mother, trace baa, taa, thaa in her prescribed stroke
       order and direction over the faint Noto Naskh glyph; export each letter's referenceStrokes
       (body line + dot tap). She authors each letter's 3-4 commonMistakes copy in her voice —
       including the new count/order/dot checks (strokeCountMismatch, strokeOrderWrong,
       dotPositionWrong) and keeping the existing length/direction/curvature body checks.
    2. Switch to labeled-sample mode. Have real children draw each letter; the mother labels each
       capture live: good / wrong_order / wrong_direction / wrong_count / scribble / wrong_letter /
       taa_when_shown_baa. Target ~15-20 samples per letter per category (RESEARCH A1). Export the
       labeled fixtures.
    3. Confirm on the tablet that the ML Kit ar model recognizes isolated ب/ت/ث well enough for the
       D-04 coarse gate (Open Q3 / A4) — a confidently-different letter is caught, a correct letter
       is not falsely rejected.
    Provide the exported referenceStrokes, authored commonMistakes copy, and labeled fixture files
    to the executor for the next (automated) task. Confirm visual overlay correctness (mirror the
    Phase 02.1 alif overlay sign-off).
  </action>
  <verify>
    <automated>MISSING — human-action checkpoint; the exported artifacts are verified automatically in Task 2 (`flutter test test/core/scoring/calibration_harness_test.dart`)</automated>
  </verify>
  <acceptance_criteria>
    - Exported referenceStrokes for baa/taa/thaa (body line + dot tap) captured on a real tablet in the mother's stroke order.
    - Authored commonMistakes copy (mother's voice) for each letter, covering the count/order/dot checks + the existing body checks.
    - Labeled real-tablet sample fixtures across the 7 taxonomy categories (incl. taa_when_shown_baa).
    - Tablet confirmation that the ML Kit ar gate catches a confidently-different letter without false-rejecting correct ب/ت/ث.
  </acceptance_criteria>
  <resume-signal>Type "authored" with the exported strokes/copy/fixtures attached, or describe what is blocking (no tablet / no children / model issue).</resume-signal>
  <done>baa/taa/thaa strokes + mother's commonMistakes copy + labeled real-tablet fixtures are captured and handed to the executor; the ML Kit gate is confirmed on the tablet.</done>
</task>

<task type="auto">
  <name>Task 2: Land authored baa/taa/thaa + real fixtures and run the harness</name>
  <files>assets/curriculum/letters.json, lib/l10n/app_en.arb, test/core/scoring/calibration_fixtures/calibration_fixtures.dart</files>
  <read_first>
    - assets/curriculum/letters.json (the alif entry as the authored exemplar; baa/taa/thaa empty placeholders to fill; the tolerances block shape)
    - lib/core/scoring/stroke_validation.dart (validateReferenceStrokes — guards the new strokes; the check-string contract)
    - test/core/scoring/calibration_fixtures/calibration_fixtures.dart (the synthetic seed from Plan 05 to replace with real captures)
    - lib/l10n/app_en.arb (the feedback keys from Plan 04 — replace placeholder copy with the mother's authored copy)
    - .planning/phases/04-scoring-quality-calibration/04-PATTERNS.md (section: letters.json)
  </read_first>
  <action>
    Write the owner's-mother-authored referenceStrokes, commonMistakes (every check string
    matching a MistakeId enum value's documented check-name), and an initial tolerances block
    ({"preset":"normal"} + any overrides) for baa/taa/thaa in letters.json. Set
    mistakesStatus: "authored". Add a tolerances block to alif too. Do NOT flip
    signedOff: false → true yet (that is Task 4 after tuning).
    Replace the synthetic seed fixtures in calibration_fixtures.dart with the REAL labeled
    captures from Task 1 (mark them real-tablet sourced). Replace the Plan-04 placeholder l10n
    feedback strings with the mother's authored copy.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/calibration_harness_test.dart; flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - letters.json baa/taa/thaa carry non-empty referenceStrokes + commonMistakes + a tolerances block; mistakesStatus == "authored" (still signedOff: false pre-tuning).
    - Every commonMistakes check string matches a MistakeId check-name (validated by stroke_validation / the mapping test).
    - calibration_fixtures.dart contains real-tablet labeled sets (synthetic seed removed/marked superseded).
    - `flutter test test/core/scoring/calibration_harness_test.dart` runs over the real fixtures and emits a per-letter FP/FN table (may still show FP/FN before tuning — that is expected input to Task 3).
  </acceptance_criteria>
  <done>Authored baa/taa/thaa + alif tolerances + real labeled fixtures + mother's feedback copy are in the repo; the harness runs over real samples.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Tune per-letter tolerances with the mother (FN/FP separately) until the regression contract holds</name>
  <read_first>
    - test/core/scoring/calibration_harness_test.dart (the per-letter FP/FN confusion table to read)
    - assets/curriculum/letters.json (the per-letter tolerances blocks to adjust — data, not code)
    - .planning/phases/04-scoring-quality-calibration/04-RESEARCH.md (§Calibration Methodology step 5 — FN-over-FP priority)
  </read_first>
  <action>
    DECISION CHECKPOINT — for each of alif/baa/taa/thaa, choose the tolerance preset and any numeric
    overrides that minimize false-negatives on good-faith attempts while keeping wrong
    order/count/dot/identity firmly rejected. Run the calibration harness; read its per-letter FP/FN
    confusion table over the real-tablet samples. With the owner's mother, adjust each letter's
    tolerances (preset or numeric override) in letters.json — NO code change (SC#4) — and re-run the
    harness. Priority (RESEARCH step 5 / CONTEXT specifics / Pitfall 3 > Pitfall 4): lean encouraging
    for good-faith attempts (minimize FN — a wrongly-rejected child is the worst outcome) while
    keeping order/count/identity firm. The taa_when_shown_baa category MUST stay rejected (the ب/ت/ث
    dot distinction). Two tuning shapes to choose between, per letter:
    - Option A — per-letter preset (loose/normal/strict) only: simplest, most teacher-legible; one
      knob per letter; may not separate FN/FP finely enough for a specific letter.
    - Option B — preset + targeted numeric overrides per letter: fine control where a preset alone
      leaves residual FN or FP; slightly less legible, so document the rationale alongside the
      override.
    Record the chosen tolerances per letter for Task 4 to freeze.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/calibration_harness_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The harness FP/FN table is acceptable per letter: good-faith real samples pass (FN minimized) and every named-bad category — incl. taa_when_shown_baa — stays rejected (FP == 0 on the named-bad set).
    - The chosen tolerances are expressed purely in letters.json data (preset + optional overrides) — no scorer code change (SC#4).
    - Chosen per-letter tolerances recorded for Task 4.
  </acceptance_criteria>
  <resume-signal>Type the chosen tolerances per letter (e.g. "baa: loose + maxCurvature 0.32; taa: normal") once the harness shows acceptable per-letter FN/FP, or describe the residual issue.</resume-signal>
  <done>Per-letter tolerances are tuned in data against real samples with the mother; FN minimized, named-bad firmly rejected; choices recorded for freezing.</done>
</task>

<task type="auto">
  <name>Task 4: Freeze tuned tolerances as regression assertions and gate sign-off</name>
  <files>test/core/scoring/calibration_harness_test.dart, test/core/scoring/letters_signoff_test.dart, assets/curriculum/letters.json</files>
  <read_first>
    - test/core/scoring/calibration_harness_test.dart (the confusion-table harness from Plan 05 — add hard assertions)
    - assets/curriculum/letters.json (apply the Task-3 tuned tolerances; flip signedOff)
    - .planning/phases/02.1-stroke-reference-correction/02.1-02-SUMMARY.md (the alif sign-off gate pattern to mirror)
  </read_first>
  <action>
    Apply the Task-3 tuned tolerances to baa/taa/thaa (and alif) in letters.json. Add hard
    regression assertions to calibration_harness_test.dart: for the tuned tolerances, every good
    real sample PASSES (FN == 0 on the committed set) and every named-bad category — including
    taa_when_shown_baa — is REJECTED with the expected MistakeId (FP == 0 on the committed set).
    Create letters_signoff_test.dart asserting baa/taa/thaa have non-empty referenceStrokes +
    commonMistakes + a tolerances block and signedOff: true (mirror the Phase 02.1 alif gate). Flip
    signedOff: false → true for baa/taa/thaa ONLY after the mother's sign-off (recorded at the
    Task-1/Task-3 checkpoints). Run the load-time validator over the final data.
  </action>
  <verify>
    <automated>flutter test test/core/scoring/ test/features/practice/; flutter test test/core/scoring/letters_signoff_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/scoring/calibration_harness_test.dart` green with FN == 0 and FP == 0 on the committed tuned set (every good passes, every named-bad — incl. taa_when_shown_baa — rejected).
    - letters_signoff_test.dart green: baa/taa/thaa carry authored strokes + tolerances + signedOff: true.
    - `grep -c '"signedOff": true' assets/curriculum/letters.json` ≥ 4 (alif + baa + taa + thaa).
    - Full `flutter test` suite green (the tuned tolerances did not regress alif or the practice flow).
  </acceptance_criteria>
  <done>Tuned per-letter tolerances are frozen as regression assertions, baa/taa/thaa are signed off, and the full suite is green — SC#4 closed on the calibration letters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real child handwriting → labeled fixtures | Captured coordinates become committed test data; must be coordinate-only, no PII, dev-captured |
| curriculum JSON (tuned tolerances + authored strokes) → scorer | Author-controlled data; load-time validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | committed real-child fixtures | mitigate | Fixtures are coordinate-only test data (no name/age/photo); captured via the dev-only labeled mode (T-02.1-07); nothing transmitted (T-01-05). Review the committed fixture files contain only numeric coordinates + a category label. |
| T-04-14 | Tampering | authored baa/taa/thaa strokes + tolerances | mitigate | Load-time validateReferenceStrokes + tolerances validation guard the data; signedOff flips true only after the mother's sign-off (Pitfall 6 — no pedagogy invented in code). |
| T-04-15 | Spoofing | ML Kit ar confusing ب/ت/ث | accept | D-04 advisory-only gate (never overrides a good geometric pass) bounds the blast radius; Task-1 on-tablet spike (A4/Open Q3) confirms the coarse gate before relying on it; taa_when_shown_baa is caught geometrically (dot check), not by ML Kit. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan; slopcheck N/A. |
</threat_model>

<verification>
- `flutter test` full suite green with tuned tolerances frozen.
- Harness asserts FN==0 / FP==0 on the committed real-tablet set (incl. taa_when_shown_baa rejected).
- baa/taa/thaa signedOff: true only post mother sign-off; load-time validator clean.
- Committed fixtures are coordinate-only (no PII).
</verification>

<success_criteria>
- SC#1: wrong order/count/dot rejected with named fix, proven on real samples.
- SC#3: good-faith size/position-varied attempt passes on real samples.
- SC#4: per-letter tolerances are data the mother tuned without a code change; named common mistakes are frozen regression tests; baa/taa/thaa signed off (D-01).
</success_criteria>

<output>
Create `.planning/phases/04-scoring-quality-calibration/04-06-SUMMARY.md` when done.
</output>
