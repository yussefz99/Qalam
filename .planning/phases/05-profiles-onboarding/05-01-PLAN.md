---
phase: 05-profiles-onboarding
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/data/child_profile_repository_test.dart
  - test/features/onboarding/onboarding_data_test.dart
  - test/features/onboarding/onboarding_screen_test.dart
  - test/router/onboarding_gate_test.dart
  - test/data/app_database_test.dart
  - test/screens/home_screen_test.dart
autonomous: true
requirements: [S1-02, S1-03]
must_haves:
  truths:
    - "Every Phase 5 requirement has at least one failing automated test before implementation begins"
    - "S1-03's no-free-text invariant is encoded as an executable assertion (find.byType(TextField) findsNothing)"
    - "The redirect gate's no-loop invariant is encoded as an executable assertion"
  artifacts:
    - path: "test/data/child_profile_repository_test.dart"
      provides: "S1-02 persist + grade-resolve unit test (RED)"
    - path: "test/features/onboarding/onboarding_data_test.dart"
      provides: "S1-02 grade map + fixed-set unit test (RED)"
    - path: "test/features/onboarding/onboarding_screen_test.dart"
      provides: "S1-03 no-free-text + PopScope widget test (RED)"
    - path: "test/router/onboarding_gate_test.dart"
      provides: "redirect gate + no-loop router test (RED)"
  key_links:
    - from: "test/data/app_database_test.dart"
      to: "ChildProfiles v2->v3 migration"
      via: "shared NativeDatabase.memory() restart simulation"
      pattern: "ChildProfiles|childProfiles"
    - from: "test/screens/home_screen_test.dart"
      to: "childProfileProvider"
      via: "ProviderScope override of the profile provider"
      pattern: "childProfileProvider"
---

<objective>
Wave 0: author the failing-test contract for the entire phase BEFORE any implementation
exists. Create the four new test files and extend the two existing test files named in
05-VALIDATION.md, so every S1-02 / S1-03 / gate behavior has an executable assertion that
is RED until the corresponding implementation lands in Waves 1-3.

Purpose: Nyquist validation — establish the feedback-sampling contract up front so each
later task has a concrete `<automated>` target (no "MISSING — Wave 0 must create…" gaps).
Output: 4 new test files (RED) + 2 extended test files (RED for the new cases), referencing
symbols that do not yet exist (ChildProfiles, ChildProfileRepository, onboarding_data,
OnboardingGate, OnboardingScreen, childProfileProvider).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-profiles-onboarding/05-CONTEXT.md
@.planning/phases/05-profiles-onboarding/05-RESEARCH.md
@.planning/phases/05-profiles-onboarding/05-PATTERNS.md
@.planning/phases/05-profiles-onboarding/05-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the data-layer RED tests (S1-02)</name>
  <files>test/data/child_profile_repository_test.dart, test/features/onboarding/onboarding_data_test.dart, test/data/app_database_test.dart</files>
  <read_first>
    - test/data/progress_repository_test.dart (the EXACT analog: shared `NativeDatabase.memory()` via `DatabaseConnection`, close-then-reopen restart simulation, and the "existing rows survive the migration" pattern at Test 3)
    - test/data/app_database_test.dart (the file being EXTENDED — match its existing structure and imports)
    - lib/data/app_database.dart (current schema v2 + LetterMastery accessors; the migration idiom these tests will exercise)
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (sections for child_profile_repository_test.dart, app_database_test.dart, onboarding_data_test.dart)
  </read_first>
  <action>
    Create `test/data/child_profile_repository_test.dart` mirroring `progress_repository_test.dart`:
    `TestWidgetsFlutterBinding.ensureInitialized()`; build a shared executor via
    `DatabaseConnection(NativeDatabase.memory())`, construct `AppDatabase` over it, wrap in a
    `ChildProfileRepository`. Test 1 — `create(nicknameId:'nick_star', avatarId:'avatar_1',
    grade:'kg', startingLessonId:'alif')` then `getProfile()` returns a row whose fields match,
    and the row survives a simulated restart (close db1, reopen db2 over the same shared executor,
    `hasProfile()` is true). Test 2 — a fresh empty DB reports `hasProfile()` false. Import
    `package:qalam/data/app_database.dart` and `package:qalam/data/child_profile_repository.dart`.
    These imports/symbols do NOT yet exist — the test is RED by design (S1-02).

    Create `test/features/onboarding/onboarding_data_test.dart` as a plain (non-widget) `test(...)`
    suite (analog: the non-widget tests in demo_routes_test.dart). Import
    `package:qalam/features/onboarding/onboarding_data.dart`. Assert: `gradeToStartingLessonId`
    contains keys `kg, grade1, grade2, grade3, grade4plus`; every value equals `'alif'` (the
    Phase-5 default per D-5/S1-02); `resolveStartingLessonId('grade2') == 'alif'` and
    `resolveStartingLessonId('unknown') == 'alif'`; `kAvatarIds.length == 6`; the nickname set has
    between 8 and 10 entries inclusive. RED by design (S1-02).

    Extend `test/data/app_database_test.dart` with a new test group for the v2->v3 migration:
    over a shared `NativeDatabase.memory()`, write an `AppSettings` row AND a `LetterMastery` row,
    then assert a `ChildProfiles` insert (via `createProfile(...)`) succeeds and ALL THREE rows
    survive a simulated restart (the "existing rows preserved across migration" invariant, same
    approach the progress_repository_test uses for v1->v2). Do not weaken or remove existing tests.
    RED by design (S1-02 migration).

    No fenced code blocks in production files; tests reference not-yet-existing symbols on purpose.
  </action>
  <acceptance_criteria>
    - `test/data/child_profile_repository_test.dart` exists and imports `package:qalam/data/child_profile_repository.dart`
    - `test/features/onboarding/onboarding_data_test.dart` exists and asserts every grade key resolves to `'alif'` and `kAvatarIds.length == 6`
    - `test/data/app_database_test.dart` contains a new migration test referencing `ChildProfiles` and preserving AppSettings + LetterMastery rows
    - `flutter test test/data/child_profile_repository_test.dart test/features/onboarding/onboarding_data_test.dart` FAILS to compile/run (symbols absent) — RED is the expected Wave 0 state
    - Each test asserts behavior, not subjective state
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data/child_profile_repository_test.dart test/features/onboarding/onboarding_data_test.dart 2>&1 | Select-String -Pattern "child_profile_repository|onboarding_data" -SimpleMatch</automated>
  </verify>
  <done>The three data-layer test files exist and reference the not-yet-built ChildProfiles table, ChildProfileRepository, and onboarding_data symbols; running them is RED (symbols absent), confirming the contract is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Author the screen + router RED tests (S1-03, gate)</name>
  <files>test/features/onboarding/onboarding_screen_test.dart, test/router/onboarding_gate_test.dart, test/screens/home_screen_test.dart</files>
  <read_first>
    - test/screens/home_screen_test.dart (the file being EXTENDED + the analog for the onboarding screen test: `ProviderScope` + `MaterialApp.router` harness with `AppLocalizations.localizationsDelegates`, tap + finder assertions, the anti-gamification Test 3 to keep unchanged)
    - test/router/demo_routes_test.dart (the analog for onboarding_gate_test: `MaterialAppHarness` + real `GoRouter` + `_SentinelError` errorBuilder used to prove "no redirect loop")
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (sections for onboarding_screen_test.dart, onboarding_gate_test.dart, home_screen_test.dart)
    - .planning/phases/05-profiles-onboarding/05-CONTEXT.md (S1-03 no-free-text rule; gate redirect rules; Home greeting integration)
  </read_first>
  <action>
    Create `test/features/onboarding/onboarding_screen_test.dart` mirroring the
    `ProviderScope` + `MaterialApp.router` harness in home_screen_test.dart. Pump the (not-yet-
    existing) `OnboardingScreen` and assert the S1-03 invariant: `find.byType(TextField)`,
    `find.byType(TextFormField)`, and `find.byType(EditableText)` ALL `findsNothing` (no free-text,
    no keyboard). Assert a `PopScope` with `canPop: false` is present (back is blocked). Add a flow
    test: tap an avatar cell, a nickname cell, a grade chip, then the "Let's go" CTA, and assert the
    profile is persisted (a stubbed/overridden repository records the create) and navigation lands on
    Home. Import `package:qalam/features/onboarding/onboarding_screen.dart`. RED by design (S1-03).

    Create `test/router/onboarding_gate_test.dart` mirroring demo_routes_test.dart. Build a real
    `GoRouter` configured with the gate `redirect` + `refreshListenable: OnboardingGate(...)` and a
    `_SentinelError` errorBuilder. Assert: with `OnboardingGate(false)`, resolving any non-onboarding
    location lands on `/onboarding`; with `OnboardingGate(true)`, `/` resolves to Home and visiting
    `/onboarding` redirects to `/`; after `gate.markProfileCreated()` the router moves off
    `/onboarding` WITHOUT ever hitting the `_SentinelError` screen (no loop, never exceeds
    redirectLimit). Import the gate + router symbols (not yet existing). RED by design (gate).

    Extend `test/screens/home_screen_test.dart`: update the existing greeting test (currently asserts
    the hardcoded `'Welcome back, Layla.'`) to pump within a `ProviderScope` that overrides
    `childProfileProvider` to return a profile with `nicknameId:'nick_star'`, then assert the greeting
    shows the resolved nickname label (the placeholder Arabic label for `nick_star`) and an avatar is
    rendered. Keep the anti-gamification Test 3 unchanged. RED by design (S1-03 "shown on home").

    No fenced code blocks in production files; assertions are executable, not subjective.
  </action>
  <acceptance_criteria>
    - `test/features/onboarding/onboarding_screen_test.dart` exists and asserts `find.byType(TextField) findsNothing`, `find.byType(TextFormField) findsNothing`, `find.byType(EditableText) findsNothing`, and a `PopScope(canPop: false)` is present
    - `test/router/onboarding_gate_test.dart` exists, builds a GoRouter with the gate redirect + a `_SentinelError` errorBuilder, and asserts no-profile->/onboarding, profile->/, and no loop after markProfileCreated()
    - `test/screens/home_screen_test.dart` greeting test now overrides `childProfileProvider` and asserts the chosen nickname label renders (no longer the hardcoded 'Layla')
    - `flutter test test/features/onboarding/onboarding_screen_test.dart test/router/onboarding_gate_test.dart` is RED (symbols absent) — expected Wave 0 state
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/onboarding/onboarding_screen_test.dart test/router/onboarding_gate_test.dart 2>&1 | Select-String -Pattern "onboarding_screen|onboarding_gate" -SimpleMatch</automated>
  </verify>
  <done>The screen test encodes the S1-03 no-free-text + PopScope invariants, the router test encodes the gate + no-loop invariants, and the home test extension encodes the "nickname shown on home" behavior — all RED against not-yet-built symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child/parent taps → app state | Fixed-set tap selections (grade/avatar/nickname) cross into app state; no free-text, no keyboard surface |
| app state → local Drift DB | Selected IDs persisted to app-private SQLite (no network this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | child profile (real-name / PII leak) | mitigate | Tests assert NO `TextField`/`TextFormField`/`EditableText` anywhere on onboarding (S1-03); only fixed-set IDs are ever asserted/persisted |
| T-05-02 | Tampering | out-of-set avatar/nickname/grade IDs | mitigate | `onboarding_data_test.dart` pins the exact fixed sets; Wave 1 validates selections ∈ sets before insert |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase adds NO new packages (RESEARCH Package Legitimacy Audit: none added); slopcheck not required |
</threat_model>

<verification>
- `flutter test test/data/ test/features/onboarding/ test/router/ test/screens/` runs and the NEW assertions are RED (implementation absent) — this is the expected Wave 0 state, not a failure of the plan.
- No production source files are modified in this plan (tests only).
- `flutter analyze` may report undefined-symbol errors inside the new test files (expected — they reference Wave 1-3 symbols).
</verification>

<success_criteria>
- All four new test files exist at their VALIDATION.md paths.
- Both existing test files extended with the new RED cases.
- Every S1-02, S1-03, and gate behavior in 05-VALIDATION.md has a corresponding executable assertion.
- No false greens: every new test is RED until its implementation lands.
</success_criteria>

<output>
Create `.planning/phases/05-profiles-onboarding/05-01-SUMMARY.md` when done.
</output>
