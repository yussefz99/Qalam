---
phase: 05-profiles-onboarding
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - lib/data/app_database.dart
  - lib/data/app_database.g.dart
  - lib/data/child_profile_repository.dart
  - lib/data/child_profile_repository.g.dart
  - lib/providers/profile_providers.dart
  - lib/providers/profile_providers.g.dart
  - lib/features/onboarding/onboarding_data.dart
autonomous: true
requirements: [S1-02, S1-03]
must_haves:
  truths:
    - "A child profile (nicknameId, avatarId, grade, startingLessonId, createdAt) persists in app-private Drift storage across a restart (S1-02)"
    - "Every grade option resolves to a startingLessonId via a single-source map, defaulting to alif (S1-02)"
    - "The avatar set is exactly 6 fixed IDs and the nickname set is 8-10 fixed ID+label pairs — no free-text (S1-03)"
  artifacts:
    - path: "lib/data/app_database.dart"
      provides: "ChildProfiles table + v2->v3 migration + hasProfile/getProfile/createProfile accessors"
      contains: "class ChildProfiles"
    - path: "lib/data/child_profile_repository.dart"
      provides: "ChildProfileRepository + childProfileRepositoryProvider (keepAlive)"
      contains: "ChildProfileRepository"
    - path: "lib/providers/profile_providers.dart"
      provides: "childProfileProvider (invalidatable async read) + OnboardingGate + onboardingGateProvider"
      contains: "OnboardingGate"
    - path: "lib/features/onboarding/onboarding_data.dart"
      provides: "kAvatarIds, kNicknames, gradeToStartingLessonId, resolveStartingLessonId"
      contains: "gradeToStartingLessonId"
  key_links:
    - from: "lib/data/child_profile_repository.dart"
      to: "appDatabaseProvider"
      via: "ref.watch(appDatabaseProvider)"
      pattern: "ref\\.watch\\(appDatabaseProvider\\)"
    - from: "lib/providers/profile_providers.dart"
      to: "childProfileRepositoryProvider"
      via: "ref.watch(childProfileRepositoryProvider).getProfile()"
      pattern: "childProfileRepositoryProvider"
---

<objective>
Wave 1 vertical-slice DATA foundation (S1-02 / S1-03 storage half): add the `ChildProfiles`
Drift table with a v2->v3 migration mirroring the existing `LetterMastery` v1->v2 idiom, expose
it through a `ChildProfileRepository` codegen provider, add the invalidatable `childProfile` async
read provider plus the `OnboardingGate` ChangeNotifier, and author `onboarding_data.dart` (6 avatar
IDs, 8-10 placeholder nickname ID+labels, and the `gradeToStartingLessonId` map defaulting all
grades to `alif`). This turns Wave 0's data-layer RED tests GREEN.

Purpose: persist the minimum child data (fixed-set IDs only, no real name — S1-03) in app-private
storage, and resolve grade -> curriculum entry point (S1-02), reusing the project's verified Drift +
Riverpod-codegen idioms exactly (the risk is deviating, not the library).
Output: extended app_database (+ regenerated .g.dart), new repository + providers + data files
(+ their regenerated .g.dart), all committed.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-profiles-onboarding/05-CONTEXT.md
@.planning/phases/05-profiles-onboarding/05-RESEARCH.md
@.planning/phases/05-profiles-onboarding/05-PATTERNS.md
@CLAUDE.md
@lib/data/app_database.dart
@lib/data/drift_progress_repository.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChildProfiles table + v2->v3 migration + accessors</name>
  <files>lib/data/app_database.dart, lib/data/app_database.g.dart</files>
  <behavior>
    - A `ChildProfiles` insert round-trips: `getProfile()` returns the inserted nicknameId/avatarId/grade/startingLessonId.
    - `hasProfile()` is false on an empty DB, true after one insert.
    - Migrating a v2 DB (with existing AppSettings + LetterMastery rows) to v3 creates ChildProfiles and preserves the existing rows.
  </behavior>
  <read_first>
    - lib/data/app_database.dart (the file being modified — copy the LetterMastery table shape lines 35-42, the migration block lines 63-69, and the recordMastery accessor style lines 98-124; do NOT touch the `_ownsExecutor`/`close()` logic — Pitfall 7)
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (app_database.dart section — exact table + migration + accessor template)
    - test/data/app_database_test.dart and test/data/child_profile_repository_test.dart (the RED tests this task must turn GREEN)
  </read_first>
  <action>
    In `lib/data/app_database.dart`: add a `ChildProfiles extends Table` beside `LetterMastery` with
    columns `id` (`integer().autoIncrement()`), `nicknameId` (`text()`), `avatarId` (`text()`),
    `grade` (`text()`), `startingLessonId` (`text()`), `createdAt` (`integer()`) — NO real-name
    column, NO free-text field (S1-03 / Pitfall 6). Add `ChildProfiles` to the `@DriftDatabase(tables:
    [...])` list. Bump `schemaVersion` from 2 to 3. In `onUpgrade`, ADD a version-guarded line
    `if (from < 3) await m.createTable(childProfiles);` AFTER the existing `if (from < 2)` line (keep
    it idempotent — Pitfall 4). Add three accessors mirroring `recordMastery`/`getSetting` style:
    `hasProfile()` (`select(childProfiles)..limit(1)).getSingleOrNull() != null`), `getProfile()`
    (returns `ChildProfile?`), and `createProfile({required nicknameId, avatarId, grade,
    startingLessonId})` performing a plain `into(childProfiles).insert(ChildProfilesCompanion.insert(
    ..., createdAt: DateTime.now().millisecondsSinceEpoch))`. Mirror the SECURITY no-log comment
    convention already in the file (never log profile values — T-05-01 / Pitfall, V8). Then run
    `dart run build_runner build --delete-conflicting-outputs` to regenerate `app_database.g.dart`
    (the generated `childProfiles` getter + `ChildProfilesCompanion` — note Drift pluralization,
    Pitfall 4) and COMMIT the regenerated `.g.dart` (it is git-tracked — Pitfall 5).
  </action>
  <acceptance_criteria>
    - `app_database.dart` contains `class ChildProfiles` and `int get schemaVersion => 3`
    - `app_database.dart` `onUpgrade` contains `if (from < 3)` creating `childProfiles`, and the existing `if (from < 2)` line is unchanged
    - `app_database.dart` contains `createProfile`, `getProfile`, and `hasProfile` accessors and NO `name`/free-text column
    - `app_database.g.dart` regenerated (contains `ChildProfilesCompanion`)
    - `flutter test test/data/app_database_test.dart` exits 0 (migration + preservation tests green)
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs; flutter test test/data/app_database_test.dart</automated>
  </verify>
  <done>The ChildProfiles table exists, schema is v3 with an idempotent v2->v3 migration, accessors persist/read a profile with no real-name field, and the extended app_database migration test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChildProfileRepository + profile_providers + onboarding_data</name>
  <files>lib/data/child_profile_repository.dart, lib/data/child_profile_repository.g.dart, lib/providers/profile_providers.dart, lib/providers/profile_providers.g.dart, lib/features/onboarding/onboarding_data.dart</files>
  <behavior>
    - `ChildProfileRepository.create(...)` then `getProfile()` round-trips and survives a simulated restart (S1-02).
    - `gradeToStartingLessonId` maps every grade option {kg, grade1, grade2, grade3, grade4plus} -> 'alif'; `resolveStartingLessonId('unknown') == 'alif'`.
    - `kAvatarIds.length == 6`; the nickname set has 8-10 fixed ID+label entries.
  </behavior>
  <read_first>
    - lib/data/drift_progress_repository.dart (the EXACT analog to copy + rename — the whole file: `part` directive, plain class wrapping `AppDatabase`, `@Riverpod(keepAlive: true)` provider with untyped `Ref ref`)
    - lib/data/app_database.dart (the skeletonProof `@riverpod Future<...>` async-read shape lines 146-155 — the template for childProfile)
    - lib/features/journey/journey_screen.dart (the `_kLetters` typedef + `const List<...>` style, lines 37-66 — the template for the avatar/nickname const sets)
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (child_profile_repository.dart, profile_providers.dart, onboarding_data.dart sections)
    - test/data/child_profile_repository_test.dart and test/features/onboarding/onboarding_data_test.dart (the RED tests this task turns GREEN)
  </read_first>
  <action>
    Create `lib/data/child_profile_repository.dart` by copying `drift_progress_repository.dart` and
    renaming: `part 'child_profile_repository.g.dart';`, a `ChildProfileRepository` class holding
    `final AppDatabase _db` and delegating `hasProfile()`, `getProfile()`, and `create({required
    nicknameId, avatarId, grade, startingLessonId})` to the matching `AppDatabase` accessors; and
    `@Riverpod(keepAlive: true) ChildProfileRepository childProfileRepository(Ref ref) =>
    ChildProfileRepository(ref.watch(appDatabaseProvider));` (untyped `Ref` — repo convention).

    Create `lib/providers/profile_providers.dart`: `part 'profile_providers.g.dart';`; a bare
    `@riverpod Future<ChildProfile?> childProfile(Ref ref) => ref.watch(childProfileRepositoryProvider)
    .getProfile();` (NOT keepAlive, so Wave 2 can `ref.invalidate` it after the onboarding write so
    Home re-reads); and an `OnboardingGate extends ChangeNotifier` with a `bool get hasProfile`, a
    constructor taking the seed bool, and `markProfileCreated()` that sets the flag true and calls
    `notifyListeners()`; plus `@Riverpod(keepAlive: true) OnboardingGate onboardingGate(Ref ref) =>
    OnboardingGate(false);` (overridden at boot in Wave 2 — Pattern 3).

    Create `lib/features/onboarding/onboarding_data.dart` (MECHANISM only — D-5; real values are the
    owner's mother's domain): `const List<String> kAvatarIds` with exactly 6 entries
    `avatar_1..avatar_6`; a `typedef NicknameOption = ({String id, String label});` and a
    `const List<NicknameOption> kNicknames` with 8-10 placeholder Arabic-flavored entries
    (e.g. `(id:'nick_star', label:'نجمة')`, `nick_moon`/`قمر`, `nick_lion`/`أسد`, …) carrying a LOUD
    `// TODO(owner's-mother sign-off): finalize nickname wording — placeholders only` comment; the
    `const Map<String,String> gradeToStartingLessonId` with all five grade keys -> `'alif'` carrying a
    LOUD `// TODO(owner's-mother sign-off): replace 'alif' with real per-grade entry-point ids` comment;
    and `String resolveStartingLessonId(String grade) => gradeToStartingLessonId[grade] ?? 'alif';`.
    Add a code comment flagging the letterId-vs-lessonId namespace question for Phase 6 (RESEARCH
    Open-Q1 / Assumption A2): the value is a LETTER id (`alif`) for now. Keep ID->label / ID->asset
    mapping in code, NEVER in the DB. Do NOT add any package. Run `dart run build_runner build
    --delete-conflicting-outputs` and commit the regenerated `child_profile_repository.g.dart` and
    `profile_providers.g.dart`.
  </action>
  <acceptance_criteria>
    - `child_profile_repository.dart` contains `class ChildProfileRepository` and `@Riverpod(keepAlive: true)` exposing `childProfileRepository`
    - `profile_providers.dart` contains `class OnboardingGate extends ChangeNotifier` with `markProfileCreated()` calling `notifyListeners()`, and a bare `@riverpod` `childProfile` async read
    - `onboarding_data.dart` contains `kAvatarIds` (6 entries), `kNicknames` (8-10 entries), `gradeToStartingLessonId` (5 keys all 'alif'), `resolveStartingLessonId`, and at least two `TODO(owner's-mother sign-off)` comments
    - `onboarding_data.dart` contains NO TextField/TextFormField and NO `name` free-text storage
    - `flutter test test/data/child_profile_repository_test.dart test/features/onboarding/onboarding_data_test.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs; flutter test test/data/child_profile_repository_test.dart test/features/onboarding/onboarding_data_test.dart</automated>
  </verify>
  <done>The repository + providers + onboarding_data exist following the verified Drift/Riverpod idioms; the S1-02 persist test, grade-map test, and fixed-set assertions are all GREEN; placeholder nickname wording and per-grade values are flagged for the owner's mother's sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected fixed-set IDs → repository.create() | Only known IDs (avatar/nickname/grade) + a resolved startingLessonId cross into persistence |
| repository → app-private Drift file | App-private SQLite (`getApplicationDocumentsDirectory`); no network this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ChildProfiles table (real-name / PII) | mitigate | Table has ONLY fixed-set IDs + grade + startingLessonId + createdAt; NO `name`/free-text column (S1-03 / Pitfall 6); profile values never logged (mirror app_database no-log convention) |
| T-05-03 | Information Disclosure | local DB at rest | accept | App-private storage via existing `getApplicationDocumentsDirectory`; acceptable for v1 local-only (matches Phase-1 T-01-02 posture) |
| T-05-04 | Tampering | grade key not in map | mitigate | `resolveStartingLessonId` falls back to `'alif'` for any unknown grade (never null/crash); covered by onboarding_data_test |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase adds NO new packages (RESEARCH Package Legitimacy Audit: none added); slopcheck not required |
</threat_model>

<verification>
- `dart run build_runner build --delete-conflicting-outputs` succeeds; `app_database.g.dart`, `child_profile_repository.g.dart`, `profile_providers.g.dart` regenerated and committed.
- `flutter test test/data/ test/features/onboarding/onboarding_data_test.dart` exits 0 (all Wave-1-owned tests green).
- `flutter analyze` exits 0 for the new/modified source files.
- `drift`/`drift_dev` versions UNCHANGED (still on the 2.31 line — do NOT bump).
</verification>

<success_criteria>
- ChildProfiles table at schema v3 with an idempotent v2->v3 migration preserving existing rows.
- ChildProfileRepository + childProfileRepositoryProvider + childProfile + OnboardingGate exist following repo idioms.
- onboarding_data.dart provides the 6 avatars, 8-10 placeholder nicknames, and the all-->alif grade map, each flagged for owner sign-off.
- A profile persists across a simulated restart (S1-02), every grade resolves (S1-02), and storage holds only fixed-set IDs (S1-03).
</success_criteria>

<output>
Create `.planning/phases/05-profiles-onboarding/05-02-SUMMARY.md` when done.
</output>
