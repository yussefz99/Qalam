---
phase: 05-profiles-onboarding
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - lib/screens/home_screen.dart
  - lib/l10n/app_en.arb
autonomous: true
requirements: [S1-03]
must_haves:
  truths:
    - "The Home greeting shows the child's chosen nickname label (resolved from nicknameId), not the hardcoded 'Layla' (S1-03 'shown on home')"
    - "The Home greeting header shows the child's chosen avatar (resolved from avatarId)"
    - "The bare (no-ProviderScope) test harness still renders Home without crashing (scope-aware reader degrades gracefully)"
  artifacts:
    - path: "lib/screens/home_screen.dart"
      provides: "Greeting header reads childProfileProvider for nickname label + avatar circle"
      contains: "childProfileProvider"
    - path: "lib/l10n/app_en.arb"
      provides: "{nickname}-placeholdered greeting key"
      contains: "nickname"
  key_links:
    - from: "lib/screens/home_screen.dart"
      to: "childProfileProvider"
      via: "ConsumerWidget ref.watch(childProfileProvider).when(...)"
      pattern: "childProfileProvider"
    - from: "lib/screens/home_screen.dart"
      to: "kNicknames / kAvatarIds (onboarding_data)"
      via: "nicknameId -> label and avatarId -> placeholder visual mapping (presentation only)"
      pattern: "nicknameId|avatarId"
---

<objective>
Wave 3 vertical-slice HOME INTEGRATION (S1-03 "the choice ... shows on the home surface"): replace
the hardcoded `'Welcome back, Layla.'` greeting in `home_screen.dart` with the child's chosen
nickname label and avatar, read from `childProfileProvider`. Resolve `nicknameId -> label` and
`avatarId -> placeholder visual` via the `onboarding_data.dart` presentation maps (never the DB).
Add the `{nickname}`-placeholdered ARB greeting key. Keep the scope-aware reader split so the bare
test harness (no `ProviderScope`) still renders, and keep all PLAT-03 anti-gamification invariants.

Purpose: close the S1-03 loop — the avatar/nickname the child picked at onboarding now visibly
greets them on Home, completing the "fixed-set identity shown on the home screen" requirement. Turns
Wave 0's home_screen_test greeting extension GREEN.
Output: edited home_screen greeting subtree + a templated ARB greeting key.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-profiles-onboarding/05-CONTEXT.md
@.planning/phases/05-profiles-onboarding/05-PATTERNS.md
@CLAUDE.md
@lib/screens/home_screen.dart
@lib/features/onboarding/onboarding_data.dart
@lib/providers/profile_providers.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home greeting reads the profile (nickname label + avatar)</name>
  <files>lib/screens/home_screen.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - lib/screens/home_screen.dart (the file being modified — `_GreetingHeader` lines 230-272 currently hardcodes `l10n?.homeGreeting ?? 'Welcome back, Layla.'` at line 257 and renders the mascot SVG; the scope-aware `_PersistenceProof`/`_PersistenceProofReader` split lines 374-404 is the EXACT pattern to mirror for graceful degradation; the file-header PLAT-03 invariants lines 11-13)
    - lib/features/onboarding/onboarding_data.dart (kNicknames for nicknameId->label, kAvatarIds for avatarId->placeholder visual — presentation maps; add a small ID->label / ID->placeholder lookup if not already present, in code NOT the DB)
    - lib/providers/profile_providers.dart (the `childProfile` async provider built in Wave 1 — `.when(data:, loading:, error:)`)
    - lib/widgets/arabic_text.dart (render the Arabic nickname label via ArabicText, NOT raw Text / NOT global Directionality)
    - lib/l10n/app_en.arb (the file being modified — `homeGreeting` lines 392-400 is the convention to convert to a `{nickname}` placeholder template)
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (home_screen.dart + app_en.arb sections; "Scope-aware provider read" shared pattern)
    - test/screens/home_screen_test.dart (the extended RED test this task turns GREEN)
  </read_first>
  <action>
    In `lib/screens/home_screen.dart`: convert the greeting subtree of `_GreetingHeader` so the
    greeting line + avatar are driven by `childProfileProvider`. Mirror the scope-aware split used by
    `_PersistenceProof`/`_PersistenceProofReader` (lines 374-404): a `StatelessWidget` that checks for
    an ancestor `UncontrolledProviderScope` and degrades to the current static greeting (or
    `SizedBox.shrink()` for the avatar) when absent — so the bare D-05 direction test still renders —
    and a `ConsumerWidget` that does `ref.watch(childProfileProvider).when(data: profile => …,
    loading: => current static fallback, error: => current static fallback)`. When a profile is
    present: resolve its `nicknameId` to a display label via the `onboarding_data.dart` nickname map
    and render it through `ArabicText`; compose the greeting from the new `{nickname}` ARB template
    (`l10n?.homeGreeting(label) ?? 'Welcome back, $label.'`); and render the chosen avatar from
    `avatarId` as the same placeholder colored circle used on onboarding (reuse the rounded
    `Container`/`BorderRadius.circular` idiom lines 306-315; keep the SVG mascot fallback discipline
    lines 241-250). Keep ALL PLAT-03 invariants (lines 11-13): NO gold/`QalamColors.reward`, NO
    counters/streaks. Keep app chrome LTR (no global `Directionality.rtl`).

    In `lib/l10n/app_en.arb`: convert `homeGreeting` to a placeholdered template
    `"Welcome back, {nickname}."` with a sibling `@homeGreeting` block adding
    `"placeholders": { "nickname": { "type": "String" } }` and a description. l10n is regenerated by
    `flutter gen-l10n` / `flutter pub get` / build (NOT build_runner — Pattern 7).
  </action>
  <acceptance_criteria>
    - `home_screen.dart` contains `childProfileProvider` and a `.when(` reading it for the greeting
    - `home_screen.dart` no longer hardcodes the greeting as the ONLY source — the chosen nickname label renders when a profile is present (the literal 'Welcome back, Layla.' remains only as a no-scope/loading/error fallback)
    - `home_screen.dart` renders an avatar circle resolved from `avatarId` and contains NO `QalamColors.reward`
    - `app_en.arb` `homeGreeting` is a `{nickname}` template with a `placeholders` block for `nickname` of type `String`
    - `flutter test test/screens/home_screen_test.dart` exits 0 (greeting shows the overridden profile's nickname; anti-gamification test still green; bare-harness render still works)
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/screens/home_screen_test.dart</automated>
  </verify>
  <done>Home greets the child by their chosen nickname label with their chosen avatar, sourced from the profile provider; the bare test harness still renders via the scope-aware fallback; PLAT-03 invariants hold; the extended home_screen test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile (Drift) → Home UI | nicknameId/avatarId read from the local profile and resolved to a display label/visual in the presentation layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Home greeting (real-name display) | mitigate | Greeting shows only the fixed-set nickname LABEL resolved from `nicknameId`; no real name exists to display (S1-03); profile values are not logged |
| T-05-07 | Denial of Service | Home crashes when no profile / no scope | mitigate | Scope-aware reader degrades to a static fallback on no-scope/loading/error (mirrors `_PersistenceProofReader`); bare harness still renders |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase adds NO new packages (RESEARCH Package Legitimacy Audit: none added); slopcheck not required |
</threat_model>

<verification>
- `flutter test test/screens/home_screen_test.dart` exits 0.
- Full phase gate: `flutter test` (full suite green) + `flutter analyze` (exit 0) before `/gsd-verify-work`.
- No `QalamColors.reward`, no counters/streaks added to Home; no global `Directionality.rtl`.
- Owner's-mother sign-off still flagged (nickname wording + per-grade entry points carried from Wave 1).
</verification>

<success_criteria>
- The Home greeting shows the chosen nickname label (via ArabicText) + the chosen avatar, read from `childProfileProvider` (S1-03 "shown on home").
- The `{nickname}` ARB template drives the greeting with a literal fallback for the bare harness.
- The full `flutter test` suite is green and `flutter analyze` exits 0 — Phase 5 closes both S1-02 and S1-03 end-to-end.
</success_criteria>

<output>
Create `.planning/phases/05-profiles-onboarding/05-04-SUMMARY.md` when done.
</output>
