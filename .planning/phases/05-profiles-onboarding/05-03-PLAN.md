---
phase: 05-profiles-onboarding
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - lib/features/onboarding/onboarding_screen.dart
  - lib/features/onboarding/widgets/grade_chips.dart
  - lib/features/onboarding/widgets/avatar_grid.dart
  - lib/features/onboarding/widgets/nickname_grid.dart
  - lib/router/app_router.dart
  - lib/router/app_router.g.dart
  - lib/main.dart
  - lib/l10n/app_en.arb
autonomous: true
requirements: [S1-02, S1-03]
must_haves:
  truths:
    - "On first launch with no profile, the child is redirected to /onboarding and cannot reach Home or back out (S1-03 + gate)"
    - "Onboarding is one scrollable card: grade chips -> avatar grid -> nickname grid -> 'Let's go' CTA, with NO free-text input anywhere (S1-03)"
    - "Tapping 'Let's go' resolves grade->startingLessonId, writes the profile, flips the gate, and navigates to Home with no redirect loop (S1-02 + gate)"
    - "On subsequent launches with a profile present, onboarding is skipped and the app opens at Home"
  artifacts:
    - path: "lib/features/onboarding/onboarding_screen.dart"
      provides: "Single combined onboarding card + PopScope(canPop:false) + submit sequence"
      contains: "PopScope"
      min_lines: 60
    - path: "lib/router/app_router.dart"
      provides: "/onboarding route + sync redirect (both rules) + refreshListenable: gate"
      contains: "/onboarding"
    - path: "lib/main.dart"
      provides: "Boot-time hasProfile read + appDatabaseProvider/onboardingGateProvider overrides"
      contains: "OnboardingGate"
  key_links:
    - from: "lib/main.dart"
      to: "onboardingGateProvider"
      via: "ProviderScope override seeded with boot-time hasProfile()"
      pattern: "onboardingGateProvider\\.overrideWith"
    - from: "lib/router/app_router.dart"
      to: "onboardingGateProvider"
      via: "ref.watch(onboardingGateProvider) used as refreshListenable + redirect source"
      pattern: "onboardingGate"
    - from: "lib/features/onboarding/onboarding_screen.dart"
      to: "childProfileRepositoryProvider"
      via: "create() then markProfileCreated() then invalidate(childProfileProvider) then context.go('/')"
      pattern: "markProfileCreated"
---

<objective>
Wave 2 vertical-slice ONBOARDING + GATE (the end-to-end first-launch flow): build the single
combined onboarding card (grade chips -> avatar grid -> nickname grid -> "Let's go") with NO
free-text and `PopScope(canPop:false)`; wire the GoRouter `/onboarding` route with a synchronous
redirect gate (both rules, no loop) driven by `OnboardingGate` as `refreshListenable`; do the
one-time boot `hasProfile()` read in `main.dart` and override `appDatabaseProvider` +
`onboardingGateProvider` so the gate value is correct before the first frame (no async-in-redirect,
no flicker — Pattern 3). On submit: resolve grade->startingLessonId, write the row, flip the gate,
invalidate the profile read, and `context.go('/')`.

Purpose: deliver S1-03's "child picks avatar + nickname from a fixed set on first open" and the
locked onboarding gate end-to-end — after this wave a fresh install lands a real user on a working
onboarding screen and, after one tap-through, on Home. Turns Wave 0's onboarding_screen + gate RED
tests GREEN.
Output: onboarding screen + 3 picker widgets, extended router (+ regenerated .g.dart), extended
main.dart, new l10n keys.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-profiles-onboarding/05-CONTEXT.md
@.planning/phases/05-profiles-onboarding/05-RESEARCH.md
@.planning/phases/05-profiles-onboarding/05-PATTERNS.md
@CLAUDE.md
@lib/router/app_router.dart
@lib/main.dart
@lib/screens/home_screen.dart
@lib/features/onboarding/onboarding_data.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router gate + main.dart boot read</name>
  <files>lib/router/app_router.dart, lib/router/app_router.g.dart, lib/main.dart</files>
  <read_first>
    - lib/router/app_router.dart (the file being modified — the documented `/parent` redirect seam lines 69-77 shows exactly where the guard goes; `appRouter` is already `@Riverpod(keepAlive: true)`; keep the `kDemoMode` flag + existing 5 routes + `...demoRoutes()`)
    - lib/main.dart (the file being modified — keep `ensureInitialized()` + `await lockOrientation()` ordering; current `main` wraps `ProviderScope(child: QalamApp())` with no overrides)
    - lib/data/app_database.dart (the `appDatabaseProvider` + `close()`/`_ownsExecutor` logic — Pitfall 7: override the provider with the boot instance, let it own disposal)
    - lib/providers/profile_providers.dart (the `OnboardingGate` + `onboardingGateProvider` built in Wave 1)
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (app_router.dart + main.dart sections; RESEARCH Pattern 3 + Pitfalls 1, 2, 7)
    - test/router/onboarding_gate_test.dart (the RED test this task turns GREEN)
  </read_first>
  <action>
    In `lib/router/app_router.dart`: inside `appRouter`, read `final gate =
    ref.watch(onboardingGateProvider);`. Add `refreshListenable: gate` to the `GoRouter`. Add a
    synchronous `redirect: (context, state)` that returns `null` when `kDemoMode` (demo bypasses the
    gate); computes `final onOnboarding = state.matchedLocation == '/onboarding';`; returns
    `'/onboarding'` when `!gate.hasProfile && !onOnboarding`; returns `'/'` when `gate.hasProfile &&
    onOnboarding` (BOTH rules — the reverse rule prevents the redirect loop, Pitfall 1); otherwise
    `null`. NEVER `await` inside redirect (it is synchronous — Pitfall 2). Add a
    `GoRoute(path: '/onboarding', builder: (c, s) => const OnboardingScreen())` to the routes list
    (import the onboarding screen). Keep `initialLocation: kDemoMode ? '/demo/home' : '/'` and all
    existing routes + `...demoRoutes()`.

    In `lib/main.dart`: after `await lockOrientation()`, construct `final db = AppDatabase();` and
    `final hasProfile = await db.hasProfile();` (one-time boot read). Change `runApp` to pass
    `ProviderScope(overrides: [...])`: override `appDatabaseProvider` to return that same `db` with
    `ref.onDispose(db.close)` (one DB instance — Pitfall 7), and override `onboardingGateProvider` to
    return `OnboardingGate(hasProfile)`. Keep `child: const QalamApp()`. Import the providers + app_database.
    Run `dart run build_runner build --delete-conflicting-outputs` (regenerates `app_router.g.dart`)
    and commit the regenerated file.
  </action>
  <acceptance_criteria>
    - `app_router.dart` contains a `GoRoute(path: '/onboarding'` and a synchronous `redirect:` with BOTH `!gate.hasProfile && !onOnboarding -> '/onboarding'` and `gate.hasProfile && onOnboarding -> '/'` rules
    - `app_router.dart` contains `refreshListenable:` set to the gate and contains NO `await` inside the redirect callback
    - `main.dart` contains a boot-time `await db.hasProfile()` and `ProviderScope(overrides:` with `onboardingGateProvider.overrideWith` and `appDatabaseProvider.overrideWith`
    - `flutter test test/router/onboarding_gate_test.dart` exits 0 (no-profile->/onboarding, profile->/, no loop)
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs; flutter test test/router/onboarding_gate_test.dart</automated>
  </verify>
  <done>The router redirects to /onboarding when no profile exists and to Home when it does, with both rules preventing a loop; main.dart seeds the gate from a one-time boot read with a single shared DB instance; the gate router test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding screen + picker widgets + l10n</name>
  <files>lib/features/onboarding/onboarding_screen.dart, lib/features/onboarding/widgets/grade_chips.dart, lib/features/onboarding/widgets/avatar_grid.dart, lib/features/onboarding/widgets/nickname_grid.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - lib/screens/home_screen.dart (chrome skeleton lines 40-76: Scaffold->SafeArea->SingleChildScrollView->ConstrainedBox->Column; the teal "Let's go" CTA pill idiom lines 340-355; null-safe l10n lines 38/257; ArabicText usage lines 31/314; the `_NavItem`/`_TodaysLessonCard` GestureDetector tap-cell + stable-Key idiom lines 134-362)
    - lib/features/journey/journey_screen.dart (ConsumerWidget + ref.watch + `Scaffold(backgroundColor: QalamColors.bg)`; JourneyNodeWidget tap-state grid cell)
    - lib/theme/colors.dart, lib/theme/dimens.dart, lib/theme/text_styles.dart, lib/theme/brand_theme_ext.dart (QalamColors/Space/Radii/Targets/TextStyles + QalamTheme.buttonShadow — semantic tokens only, NEVER QalamColors.reward)
    - lib/features/onboarding/onboarding_data.dart (kAvatarIds, kNicknames, gradeToStartingLessonId, resolveStartingLessonId)
    - lib/widgets/arabic_text.dart (the ArabicText widget — render Arabic nickname labels here, NOT a global Directionality)
    - lib/l10n/app_en.arb (the file being modified — every key has a sibling `@key` metadata block; the `homeGreeting` convention lines 392-400)
    - .planning/phases/05-profiles-onboarding/05-PATTERNS.md (onboarding_screen.dart + widgets sections; RESEARCH Pattern 4 PopScope, Pitfall 3 no-global-RTL, Pitfall 6 no-free-text)
    - test/features/onboarding/onboarding_screen_test.dart (the RED test this task turns GREEN)
  </read_first>
  <action>
    Create `lib/features/onboarding/onboarding_screen.dart` as a `ConsumerStatefulWidget` (it holds
    the three in-progress selections) wrapped in `PopScope(canPop: false)` (blocks Android back +
    predictive-back gesture — Pattern 4; `WillPopScope` is deprecated, do NOT use it). Chrome mirrors
    home_screen: `Scaffold(backgroundColor: QalamColors.bg)` -> `SafeArea` -> `SingleChildScrollView`
    -> `ConstrainedBox(maxWidth ~720)` -> a single surface `Column` card containing, in order: (1) the
    grade chips, (2) the avatar grid, (3) the nickname grid, and (4) the teal "Let's go" CTA pill.
    App chrome stays LTR (NO global `Directionality.rtl` — Pitfall 3); Arabic nickname labels render
    via `ArabicText`. Tokens: `QalamColors.bg`/`surface`/`border`/`primary` (ink-teal CTA),
    `QalamSpace.*`, `QalamRadii.pill/xl/lg`, `QalamTargets.targetMin` (>=44dp taps),
    `QalamTextStyles.heading/body/label`, `qalam.buttonShadow` via
    `Theme.of(context).extension<QalamTheme>() ?? QalamTheme.light`. NEVER use `QalamColors.reward`
    (PLAT-03). NO `TextField`/`TextFormField`/`EditableText`/keyboard anywhere (S1-03 / Pitfall 6).
    l10n via `final l10n = AppLocalizations.of(context);` + `l10n?.key ?? 'literal fallback'`.

    Create the three picker widgets under `lib/features/onboarding/widgets/` using the
    `GestureDetector(onTap:)` + `DecoratedBox`/`Container` tap-cell idiom (`QalamColors.surface`
    default, `QalamColors.primary`/`primaryTint` selected), each tappable carrying a stable `Key`
    for the widget test:
    - `grade_chips.dart` — five chips for `kg, grade1, grade2, grade3, grade4plus` (labels via l10n);
      single-select; reports the chosen grade key.
    - `avatar_grid.dart` — a `Wrap`/`GridView` of the 6 `kAvatarIds` rendered as placeholder colored
      circles (D-3: real art is a later asset swap — map ID->placeholder visual in code, no DB);
      single-select; reports the chosen avatarId.
    - `nickname_grid.dart` — a grid of the `kNicknames` rendered with `ArabicText` for each label;
      single-select; reports the chosen nicknameId.

    Submit handler on "Let's go" (enabled only when all three are selected): validate each selection
    is a member of its fixed set (`kAvatarIds`, `kNicknames` ids, the grade-map keys — T-05-02 / V5);
    `final lessonId = resolveStartingLessonId(grade);`; `await
    ref.read(childProfileRepositoryProvider).create(nicknameId:…, avatarId:…, grade:…,
    startingLessonId: lessonId);`; `ref.read(onboardingGateProvider).markProfileCreated();`;
    `ref.invalidate(childProfileProvider);`; then `if (context.mounted) context.go('/');`.

    Extend `lib/l10n/app_en.arb` with onboarding keys (each with a sibling `@key` block + description):
    the five grade labels, the "Let's go" CTA, and any onboarding heading/prompt copy. (Nickname
    display labels stay in `onboarding_data.dart` as placeholder data — the label can change with no
    data migration.) l10n is regenerated by `flutter gen-l10n` / build, NOT build_runner (Pattern 7).
  </action>
  <acceptance_criteria>
    - `onboarding_screen.dart` contains `PopScope` with `canPop: false` and contains NO `TextField`, `TextFormField`, or `EditableText`
    - `onboarding_screen.dart` contains the submit sequence `markProfileCreated`, `invalidate`, and `context.go('/')` in that order, gated on all three selections
    - `onboarding_screen.dart` contains NO `QalamColors.reward` reference (PLAT-03) and NO global `Directionality`
    - the three widget files exist; each tappable cell carries a stable `Key`; nickname labels use `ArabicText`
    - `app_en.arb` contains the five grade label keys + a "Let's go" CTA key, each with a sibling `@`-metadata block
    - `flutter test test/features/onboarding/onboarding_screen_test.dart` exits 0 (no-free-text + PopScope + tap-through-persists-and-navigates)
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/onboarding/onboarding_screen_test.dart</automated>
  </verify>
  <done>A single scrollable onboarding card lets the child tap grade + avatar + nickname (no free-text, no keyboard, no gold), and "Let's go" persists the profile, flips the gate, and navigates to Home; back is blocked; the onboarding screen widget test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end onboarding flow human verification</name>
  <files>lib/features/onboarding/onboarding_screen.dart</files>
  <read_first>
    - .planning/phases/05-profiles-onboarding/05-VALIDATION.md (Manual-Only Verifications — the visual-fidelity check)
    - docs/design/kit/project/ui_kits/qalam_app/ (the design kit; an onboarding_preview.html mockup if present, else the kit tokens/screenshots)
    - .planning/phases/05-profiles-onboarding/05-CONTEXT.md (screen layout + field order + "Let's go" + no-skip)
  </read_first>
  <action>
    No code change. This is the end-of-phase human visual check (config `human_verify_mode:
    end-of-phase`, so this is a `<human-check>`, not an interactive checkpoint). After Tasks 1-2 are
    green, run the app on a tablet/emulator on a FRESH install (no existing profile) and walk the
    first-launch flow once.
  </action>
  <acceptance_criteria>
    - On a fresh install the app opens on /onboarding (not Home) and the Android back button does nothing.
    - The card shows, in order, the grade chips, the 6 placeholder avatar circles, and the placeholder Arabic nickname grid — no text field, no keyboard ever appears.
    - The screen uses the parchment background + ink-teal "Let's go" pill, NO gold/reward chrome, and Arabic labels render as correct connected glyphs (no tofu).
    - Tapping grade + avatar + nickname then "Let's go" lands on Home; force-quitting and relaunching opens directly on Home (onboarding skipped).
  </acceptance_criteria>
  <verify>
    <human-check>
      1. Build/run on a tablet or emulator with NO existing profile (fresh install or cleared app data): `flutter run`.
      2. Confirm the app opens on the onboarding card (NOT Home) and pressing the Android back button / back gesture does nothing.
      3. Confirm there is no text field and the soft keyboard never appears; visuals match the Qalam design kit (parchment bg, ink-teal "Let's go" pill, no gold) and Arabic nickname labels render as correct connected glyphs.
      4. Tap one grade chip, one avatar, one nickname, then "Let's go" — confirm it navigates to Home.
      5. Force-quit and relaunch — confirm it opens directly on Home (onboarding skipped). Reply "approved" or describe issues.
    </human-check>
  </verify>
  <done>A human confirms the first-launch onboarding gate, the no-keyboard fixed-set pickers, the design-kit visuals (no gold), and that a created profile persists so onboarding is skipped on relaunch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child/parent taps → onboarding state | All inputs are fixed-set taps (grade/avatar/nickname); zero free-text/keyboard surface (S1-03) |
| onboarding submit → repository.create() | Selections validated ∈ fixed sets before persistence |
| router redirect → reachable screens | Gate controls whether the child can leave /onboarding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | onboarding inputs (real-name / PII) | mitigate | Screen has NO `TextField`/`TextFormField`/`EditableText`/keyboard; widget test asserts `findsNothing` (S1-03 / Pitfall 6) |
| T-05-02 | Tampering | out-of-set avatar/nickname/grade selection | mitigate | Submit validates each selection ∈ `kAvatarIds`/`kNicknames`/grade-map keys before `create()` (V5 input validation) |
| T-05-05 | Elevation of Privilege | child bypasses onboarding to reach Home | mitigate | `PopScope(canPop:false)` blocks back; router redirect forces `/onboarding` until a profile exists (no-skip, CONTEXT) |
| T-05-06 | Denial of Service | redirect loop / "too many redirects" error screen | mitigate | Both redirect rules implemented + gate flips via `markProfileCreated()`->`notifyListeners()`; gate router test asserts no `_SentinelError` (Pitfall 1) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase adds NO new packages (RESEARCH Package Legitimacy Audit: none added); slopcheck not required |
</threat_model>

<verification>
- `dart run build_runner build --delete-conflicting-outputs` succeeds; `app_router.g.dart` regenerated + committed.
- `flutter test test/router/onboarding_gate_test.dart test/features/onboarding/` exits 0.
- `flutter analyze` exits 0.
- No `await` inside the router redirect; no global `Directionality.rtl`; no `QalamColors.reward`; no `TextField` on onboarding.
- Human visual check (Task 3) approved.
</verification>

<success_criteria>
- Fresh install -> /onboarding (gated, no back-out); profile present -> Home; no redirect loop.
- One scrollable card with grade chips + avatar grid + nickname grid + "Let's go", all taps, no keyboard (S1-03).
- "Let's go" resolves grade->startingLessonId (S1-02), writes the profile, flips the gate, invalidates the read, and navigates to Home.
- Design-kit chrome (parchment/ink-teal), no gamification, Arabic labels via ArabicText.
</success_criteria>

<output>
Create `.planning/phases/05-profiles-onboarding/05-03-SUMMARY.md` when done.
</output>
