---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 04
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - lib/data/app_database.dart
  - lib/data/graph_position_repository.dart
  - lib/tutor/tutor_facts.dart
  - lib/tutor/tutor_facts_builder.dart
autonomous: true
requirements: [DYN-02]
tags: [drift, tutor, resume, persistence]

must_haves:
  truths:
    - "The child's graph position (cleared competencies/tiers, current node) persists to Drift and survives an app restart"
    - "Per-essential-exercise clean-reps are persisted so isMasteryMet can be evaluated after a relaunch"
    - "The non-PII FACTS payload carries clearedTiers/clearedCompetencies for resume trajectory replay"
    - "The schema migration is version-guarded and idempotent (v4→v5)"
  artifacts:
    - path: "lib/data/app_database.dart"
      provides: "LetterGraphPosition table + per-exercise reps + schemaVersion 5 + getPosition/setPosition accessors"
      contains: "LetterGraphPosition"
    - path: "lib/data/graph_position_repository.dart"
      provides: "GraphPositionRepository interface + Drift impl + keepAlive provider"
      contains: "GraphPositionRepository"
    - path: "lib/tutor/tutor_facts.dart"
      provides: "clearedTiers/clearedCompetencies whitelisted non-PII fields (Dart side of the wire contract)"
      contains: "clearedTiers"
  key_links:
    - from: "lib/data/graph_position_repository.dart"
      to: "lib/data/app_database.dart"
      via: "thin delegation to getPosition/setPosition"
      pattern: "getPosition|setPosition"
    - from: "lib/tutor/tutor_facts_builder.dart"
      to: "lib/tutor/tutor_facts.dart"
      via: "chokepoint builds clearedTiers/clearedCompetencies into the non-PII facts"
      pattern: "cleared(Tiers|Competencies)"
---

<objective>
The resume-persistence slice for DYN-02 (D-08): a new Drift `LetterGraphPosition` table persists the child's graph position (cleared competencies/tiers + current node) so re-entering the baa unit restores it across an app restart, per-essential-exercise clean-reps are persisted so `isMasteryMet` can be evaluated after a relaunch (Open Q3), and the non-PII FACTS payload gains `clearedTiers`/`clearedCompetencies` (the Dart half of the wire contract whose server half landed in 15-02) so the agent's reasoning resumes where it left off.

Purpose: DYN-02 "resume still works." The in-memory `_resumeByLetter` map is lost on restart; D-08 requires durable on-device state (server stays stateless — COPPA posture). The two new FACTS fields replay the trajectory to the agent. 15-05 consumes this repository + these fields to drive the dynamic flow and gate the star.
Output: Drift v4→v5 migration + LetterGraphPosition + per-exercise reps + GraphPositionRepository + the two non-PII FACTS fields, turning the 15-01 RED graph_position_repository_test green and the resume read available to 15-05.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-PATTERNS.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LetterGraphPosition table + per-exercise clean-reps + v4→v5 migration + accessors + repository</name>
  <files>lib/data/app_database.dart, lib/data/graph_position_repository.dart</files>
  <behavior>
    - LetterGraphPosition persists letterId (PK), currentExerciseId (nullable), clearedCompetencies + clearedTiers (JSON-encoded List<String>), updatedAt
    - Per-essential-exercise clean-reps persist (extend LetterReps to a (letterId, exerciseId) composite key, or a sibling table) so isMasteryMet reads them after a relaunch
    - getPosition(letterId) returns the persisted position (Future, not a stream — Pitfall 6); setPosition writes it
    - schemaVersion 4→5; onUpgrade `if (from < 5)` creates the new table(s); a child with no row defaults to graph root (clean start)
    - A second AppDatabase re-opened over the same store reads the persisted position (simulated restart, D-08)
  </behavior>
  <read_first>
    - lib/data/app_database.dart (the LetterReps table lines 59-74; @DriftDatabase(tables: [...]) line 76; schemaVersion line 92; the version-guarded onUpgrade switch lines 95-113 with the `if (from < N)` idempotency guard (Pitfall 4); recordMastery/setCleanReps/cleanRepsFor accessors lines ~143-229)
    - lib/data/drift_progress_repository.dart (the thin-delegation interface + @Riverpod(keepAlive:true) provider idiom — the whole 53-line file to mirror)
    - test/data/app_database_test.dart (the temp-file real-onUpgrade restart shape ~118-187 and the shared-in-memory restart ~25-39 — what graph_position_repository_test exercises)
    - 15-PATTERNS.md "lib/data/app_database.dart" (table + onUpgrade + Open Q3 clean-rep decision) + "lib/data/graph_position_repository.dart" sections ; 15-RESEARCH.md § Pattern 5 + Open Q3 + Pitfall 6
  </read_first>
  <action>
    In app_database.dart add `class LetterGraphPosition extends Table` (letterId PK text; currentExerciseId text().nullable(); clearedCompetencies text()() and clearedTiers text()() each storing a JSON-encoded List<String>; updatedAt dateTime()()) and register it in @DriftDatabase(tables:[..., LetterGraphPosition]). Resolve Open Q3 — persist clean-reps PER EXERCISE for the mastery condition: either change LetterReps to a composite (letterId, exerciseId) primary key, or add a sibling LetterExerciseReps table; choose the lower-migration-risk option and document the choice in the SUMMARY. Bump `schemaVersion` 4→5 and add `if (from < 5) { await m.createTable(letterGraphPosition); <per-exercise reps migration>; }` to the SAME onUpgrade switch (version-guarded, idempotent — Pitfall 4). Add accessors mirroring recordMastery/setCleanReps: `getPosition(letterId)` (Future, not a stream — Pitfall 6), `setPosition(...)`, and per-exercise clean-reps read/write. Run `dart run build_runner build` to regenerate app_database.g.dart (gitignored, regenerated, NEVER hand-edited). Create lib/data/graph_position_repository.dart: a `GraphPositionRepository` interface + `DriftGraphPositionRepository` delegating to the new accessors + a `@Riverpod(keepAlive: true)` provider reading appDatabaseProvider (mirror DriftProgressRepository exactly — all SQL stays in AppDatabase, the Phase-1 convention). SECURITY: persist only ids/counts/timestamps — never stroke points / PII (mirror the LetterReps SECURITY comment).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -3 && flutter test test/data/graph_position_repository_test.dart test/data/app_database_test.dart</automated>
  </verify>
  <done>graph_position_repository_test GREEN: a position written to db1 survives re-opening db2 over the same store (the D-08 simulated restart). app_database_test stays green (migration idempotent, prior tables intact). Per-exercise clean-reps persist and read back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add clearedTiers/clearedCompetencies to the non-PII FACTS (Dart side of the 422 lockstep)</name>
  <files>lib/tutor/tutor_facts.dart, lib/tutor/tutor_facts_builder.dart</files>
  <behavior>
    - TutorFacts carries clearedTiers + clearedCompetencies as List<String> (default const []), and toMap()/toJson() emit them
    - buildTutorFacts accepts clearedTiers/clearedCompetencies params (read from the Drift position on resume) and threads them into the facts
    - The serialized payload still carries no stroke/PII keys
  </behavior>
  <read_first>
    - lib/tutor/tutor_facts.dart (the whitelisted toMap lines 113-122; the constructor + final fields ~74-106; the doc comment naming the extra="forbid" 422 trap)
    - lib/tutor/tutor_facts_builder.dart (the buildTutorFacts chokepoint lines 24-41 — the signature IS the guard, accepts no stroke/Offset/profile param; the deterministic _derive* helpers ~47-80)
    - server/app/schema.py (the TutorFactsIn fields ADDED in 15-02 — these two Dart fields MUST mirror them byte-for-byte; Pitfall 1)
    - 15-PATTERNS.md "lib/tutor/tutor_facts.dart + tutor_facts_builder.dart" section + "extra=forbid wire-contract lockstep" shared pattern ; 15-RESEARCH.md Pitfall 1
  </read_first>
  <action>
    Add `final List<String> clearedTiers` and `final List<String> clearedCompetencies` (default `const []`) to TutorFacts: constructor params + the whitelisted toMap (pure string-lists — no geometry, no PII). Add matching `List<String> clearedTiers = const []` + `List<String> clearedCompetencies = const []` params to buildTutorFacts and thread them into the constructed TutorFacts (the values come from the Drift graph position on resume — D-08 trajectory replay). Pitfall 1 (CRITICAL): these MUST exactly mirror the TutorFactsIn fields that 15-02 added to server/app/schema.py — same names, both as optional string-lists — or the live /coach 422s. The server re-deploy must follow BOTH 15-02 and this plan landing (note the cross-plan deploy ordering in the SUMMARY). The non-PII regression assertion over the two new fields is extended in 15-05 (it owns both the Dart and Python non-PII test files together).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/tutor/tutor_facts_builder_test.dart && flutter analyze lib/tutor/tutor_facts.dart lib/tutor/tutor_facts_builder.dart 2>&1 | tail -3</automated>
  </verify>
  <done>TutorFacts carries the two new string-list fields in its constructor + toMap; buildTutorFacts accepts + threads them; the facts-builder test stays green; analyze is clean. Field names match server/app/schema.py exactly (verified by reading both).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-device Drift store | App-private resume state; no server child data (COPPA posture) |
| client → /coach | Enlarged FACTS cross here; the two new fields must be non-PII and mirrored server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-ID | Information Disclosure | clearedTiers/clearedCompetencies leak PII | mitigate | Both are derived string-lists of tier/competency ids; the buildTutorFacts signature accepts no stroke/profile param (the guard); the non-PII regression test (15-05) asserts no PII key |
| T-15-04-DoS | Denial of Service | Failed/corrupt v4→v5 migration | mitigate | Version-guarded idempotent onUpgrade (`if (from < 5)`); a child with no position row defaults to graph root (no crash, clean start); app_database_test covers the real-onUpgrade path |
| T-15-04-T | Tampering | 422 from un-mirrored FACTS field | mitigate | Field names mirror server/app/schema.py (15-02) exactly; server re-deploy gated to follow both sides landing (Pitfall 1) |
| T-15-SC | Tampering | pub installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy: N/A) |
</threat_model>

<verification>
- `dart run build_runner build` regenerates app_database.g.dart cleanly (Task 1).
- `flutter test test/data/graph_position_repository_test.dart test/data/app_database_test.dart` GREEN (Task 1).
- `flutter test test/tutor/tutor_facts_builder_test.dart` + `flutter analyze` clean (Task 2).
- Per-wave merge: full client suite `flutter test` green.
</verification>

<success_criteria>
- Graph position persists to Drift and survives a simulated restart (D-08).
- Per-essential-exercise clean-reps persist for the mastery condition (Open Q3 resolved).
- TutorFacts carries clearedTiers/clearedCompetencies as non-PII string-lists mirroring the server DTO (Pitfall 1).
- The v4→v5 migration is version-guarded and idempotent.
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-04-SUMMARY.md` when done. State the Open-Q3 clean-rep schema choice and the server-re-deploy lockstep with 15-02.
</output>
