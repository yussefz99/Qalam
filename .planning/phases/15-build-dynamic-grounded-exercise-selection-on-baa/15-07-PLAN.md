---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 07
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-03", "15-05"]
files_modified:
  - assets/curriculum/curriculum_graph.json
  - server/app/curriculum_data/curriculum_graph.json
autonomous: false
requirements: [DYN-01, DYN-02]
tags: [curriculum, signoff, grounding]

must_haves:
  truths:
    - "The drafted baa curriculum graph (competency mapping, difficulty tiers, 70/30 split, clean-reps) is reviewed and signed by the owner-mother at the tier level"
    - "signedOff flips false→true ONLY after her sign-off; nothing curriculum-shaping ships unsigned"
    - "The derived server copy is regenerated and re-deployed after the flip"
  artifacts:
    - path: "assets/curriculum/curriculum_graph.json"
      provides: "The signed baa curriculum graph (signedOff:true after the human gate)"
      contains: "\"signedOff\": true"
  key_links:
    - from: "assets/curriculum/curriculum_graph.json"
      to: "server/app/curriculum_data/curriculum_graph.json"
      via: "generate.py re-derives the signed server copy"
      pattern: "curriculum_graph"
---

<objective>
The owner-mother sign-off gate (D-05) — the ONLY place `signedOff` flips false→true. The drafted 19-config curriculum graph is NEW pedagogy (competency mapping, difficulty tiers, the 70/30 essential/enrichment split, per-skill clean-reps) and is her domain; she reviews the clean sign-off sheet at the tier level and signs, mirroring the `AUTHORED_BAA_IDS` sign-off gate. Code and tests have proceeded against the PROVISIONAL graph throughout the phase; only the SHIP path waits on her. This plan is non-autonomous by construction (Pitfall 4: never ship model-DRAFTED pedagogy as if signed).

Purpose: the project rule "never ship model-authored curriculum unsigned" + D-05. After this gate, the dynamic baa selection is the signed demo path.
Output: the signed graph (`signedOff:true`) and the re-derived + re-deployed server copy — OR captured adjustments if she changes the mapping/tiers/reps.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@docs/curriculum/baa-curriculum-graph-signoff-sheet.md
@docs/curriculum/national-curriculum-grade1.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner-mother reviews + signs the baa curriculum graph at the tier level</name>
  <files>assets/curriculum/curriculum_graph.json, server/app/curriculum_data/curriculum_graph.json</files>
  <what-built>
    A PROVISIONAL baa curriculum graph (assets/curriculum/curriculum_graph.json, signedOff:false) drafted from docs/curriculum/national-curriculum-grade1.md, plus a clean sign-off sheet (docs/curriculum/baa-curriculum-graph-signoff-sheet.md) listing every one of the 19 baa configs with its drafted competency, essential/enrichment flag, difficulty tier (منقول→منظور→غير منظور), and draft clean-reps. The dynamic selection flow, offline walker, mastery condition, and server graph rail all run against this provisional graph (all automated tests green from Waves 1-2).
  </what-built>
  <action>
    This is a blocking human-verify gate — the ONLY place `signedOff` flips. Do NOT auto-approve (autonomous:false; never auto-advance, D-05/Pitfall 4). Present docs/curriculum/baa-curriculum-graph-signoff-sheet.md to the owner-mother for tier-level review (competency mapping, the 70/30 essential/enrichment split, the منقول/منظور/غير منظور difficulty tiers, and the per-skill minCleanReps). If she adjusts the mapping/tiers/reps, apply her changes to assets/curriculum/curriculum_graph.json and re-run the Wave-1/2 suites. ONLY after she signs, flip `"signedOff": false` → `"signedOff": true` in assets/curriculum/curriculum_graph.json, confirm no ت/ث content crept in (baa's own dot only, D-11), then regenerate the derived server copy (`cd server && uv run python -m app.curriculum_data.generate`) and re-deploy Cloud Run so the signed graph is live. Record a human-UAT entry alongside the signedOff:true commit.
  </action>
  <how-to-verify>
    1. Open docs/curriculum/baa-curriculum-graph-signoff-sheet.md and review the table WITH the owner's mother.
    2. Confirm at the TIER level (D-05): (a) the competency mapping per config (recognize / positionalForms / copyWrite / fluentReading / wordBuilding / grammarTransform), (b) the 70/30 essential-vs-enrichment split (which competencies gate the mastery star), (c) the difficulty-tier (منقول/منظور/غير منظور) assignment for the writing exercises, (d) the per-skill clean-reps (minCleanReps) numbers — these are HERS to set (D-07).
    3. If she ADJUSTS anything, record the changes and apply them to assets/curriculum/curriculum_graph.json (competency, essential, tier, minCleanReps fields), then re-run the Wave-1/2 suites to confirm nothing breaks.
    4. ONLY after she signs: flip `"signedOff": false` → `"signedOff": true` in assets/curriculum/curriculum_graph.json. Reinforce baa's own dot only — confirm NO ت/ث content crept in (D-11).
    5. Regenerate the derived server copy: `cd server && uv run python -m app.curriculum_data.generate` (this re-derives server/app/curriculum_data/curriculum_graph.json from the signed asset).
    6. Re-deploy the Cloud Run server so the signed graph is live (mirrors the Phase-14 deploy gate), then re-run both full suites.
  </how-to-verify>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python3 -c "import json; a=json.load(open('assets/curriculum/curriculum_graph.json')); s=json.load(open('server/app/curriculum_data/curriculum_graph.json')); assert a['signedOff'] is True and s['signedOff'] is True; assert all(n['exerciseId'].startswith('baa.') for n in a['nodes']); print('signed + derived + baa-only OK')"</automated>
  </verify>
  <acceptance_criteria>
    - assets/curriculum/curriculum_graph.json carries `"signedOff": true` (the flip happened ONLY after sign-off): `python3 -c "import json; assert json.load(open('assets/curriculum/curriculum_graph.json'))['signedOff'] is True"`
    - The derived server copy is regenerated from the signed asset (its signedOff matches): `cd server && python3 -c "import json; assert json.load(open('app/curriculum_data/curriculum_graph.json'))['signedOff'] is True"`
    - Full suites green after any sign-off adjustments: `flutter test` and `cd server && uv run pytest -q`
    - No ت/ث content in the graph (D-11): the node id set is baa.* only
    - A corresponding human-UAT entry records the sign-off (no signedOff:true commit without it — Pitfall 4)
  </acceptance_criteria>
  <done>The owner-mother has signed the graph at the tier level; assets + derived server copy both carry signedOff:true; the server is re-deployed; both full suites are green; a human-UAT entry records the sign-off. If she could not sign, signedOff stays false and the provisional graph remains dev-only.</done>
  <resume-signal>Type "signed" (sign-off complete, signedOff flipped + server re-derived/re-deployed), "adjusted: <changes>" (she changed the mapping/tiers/reps — applied + re-tested, then signed), or "blocked: <reason>" (sign-off not yet possible — leave signedOff:false; the provisional graph remains the dev path only).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted pedagogy → child-facing ship path | Model-drafted curriculum must not reach a child as if signed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-T | Tampering/Integrity | Unsigned (model-DRAFTED) pedagogy reaching a child | mitigate | signedOff:false until the blocking-human checkpoint; flip ONLY after her tier-level sign-off; a human-UAT entry must accompany the signedOff:true commit (Pitfall 4 / D-05). Never auto-approvable (autonomous:false). |
| T-15-07-D11 | Integrity (scope) | ت/ث content slips into the signed graph | mitigate | Sign-off step explicitly confirms baa.* nodes only, no cross-letter contrast (D-11) |
| T-15-07-T2 | Tampering | Signed asset and derived server copy drift | mitigate | generate.py re-derives the server copy from the signed asset; re-deploy after the flip; the derived copy is never hand-edited |
| T-15-SC | Tampering | installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy: N/A) |
</threat_model>

<verification>
- `assets/curriculum/curriculum_graph.json` has `signedOff: true` ONLY after sign-off (with a human-UAT entry).
- The derived server copy matches and is re-deployed.
- Both full suites green after any adjustments.
- No ت/ث nodes (D-11).
</verification>

<success_criteria>
- The owner-mother has reviewed and signed the baa curriculum graph at the tier level (competency mapping, 70/30 split, difficulty tiers, clean-reps).
- signedOff is true ONLY post-sign-off; the derived server copy is regenerated + re-deployed.
- Nothing curriculum-shaping ships unsigned (D-05); baa's own dot only (D-11).
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-07-SUMMARY.md` when done, plus a human-UAT entry recording the sign-off.
</output>
