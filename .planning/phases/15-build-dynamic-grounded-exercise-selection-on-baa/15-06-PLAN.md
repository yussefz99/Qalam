---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 06
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - server/app/faithfulness.py
  - server/tests/test_faithfulness.py
  - server/tests/fixtures/faithfulness_set.jsonl
autonomous: true
requirements: [GROUND-03]
tags: [grounding, eval, server]

must_haves:
  truths:
    - "A model-free check flags coaching that praises a failed stroke"
    - "A model-free check flags coaching that names the wrong fix on a fail"
    - "The check reports a faithfulness RATE over the labeled set"
    - "The check is model-agnostic — it scores coaching against fixed verdicts and calls no model"
  artifacts:
    - path: "server/app/faithfulness.py"
      provides: "Deterministic verdict-vs-coaching contradiction check + rate reporter"
      contains: "def"
    - path: "server/tests/fixtures/faithfulness_set.jsonl"
      provides: "Labeled (verdict, coaching) gold + adversarial set"
    - path: "server/tests/test_faithfulness.py"
      provides: "GREEN tests: flags praise-on-fail, flags wrong-fix, reports a rate"
  key_links:
    - from: "server/tests/test_faithfulness.py"
      to: "server/app/faithfulness.py"
      via: "imports the check function under test"
      pattern: "from app.faithfulness import|import faithfulness"
    - from: "server/app/faithfulness.py"
      to: "server/tests/fixtures/faithfulness_set.jsonl"
      via: "scores the labeled set"
      pattern: "faithfulness_set"
---

<objective>
The narrow GROUND-03 slice (D-10): a model-free Python check over fixed scorer verdicts + a small labeled (verdict, coaching) set that flags any coaching which praises a failed stroke or names the wrong fix, and reports a faithfulness RATE. This is the SEED Phase 13/16 grow into the full Claude-vs-Gemini bake-off + regression gate — it is deliberately a floor, not a ceiling, and is model-agnostic (it does NOT pre-empt the Phase-13 model choice).

Purpose: GROUND-03 — "grounding faithfulness is measurable; the harness detects any model claim that contradicts the geometry; a faithfulness rate is reported." Verdict-driven (buildable without mom); the Arabic-register dimension stays Phase 13 (D-10).
Output: server/app/faithfulness.py + the labeled fixture + the test, turning the 15-01 RED faithfulness tests green.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-PATTERNS.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic faithfulness check + labeled set + the GROUND-03 tests green</name>
  <files>server/app/faithfulness.py, server/tests/test_faithfulness.py, server/tests/fixtures/faithfulness_set.jsonl</files>
  <behavior>
    - A pure function flags a contradiction when: (a) the verdict is a FAIL but the coaching contains a praise token (beautiful/perfect/great job/well done/أحسنت/mastered), OR (b) the verdict is a FAIL and the coaching does NOT mention the expected-fix token
    - A faithfulness rate = faithful/total is computed and printed over the labeled JSONL set
    - The gold set is constructed-faithful so the rate is 1.0; adversarial entries are flagged correctly by the flag tests
    - The check calls no model and needs no auth/Firebase (pure offline file read)
  </behavior>
  <read_first>
    - server/tests/test_grounding.py (the model-free `pytestmark = pytest.mark.code` pattern, module-top, no model/network) ; server/tests/conftest.py (the fake_firebase autouse fixture is for ENDPOINT tests — the faithfulness check needs none)
    - server/tests/test_payload_nonpii.py (the LEGIT_FACTS constant + @pytest.mark.parametrize discipline for a fixture-driven model-free test)
    - server/pyproject.toml ([tool.pytest.ini_options] markers — the `code` marker declaration the test relies on)
    - server/tests/test_faithfulness.py + server/tests/fixtures/faithfulness_set.jsonl (the RED stubs + fixture authored in 15-01 — the contract to satisfy)
    - 15-RESEARCH.md § "The model-free faithfulness check (GROUND-03, D-10)" code example + Assumption A6 (the lexicon is a floor) ; 15-PATTERNS.md "server/tests/test_faithfulness.py" section
  </read_first>
  <action>
    Create server/app/faithfulness.py with a deterministic `_contradicts(passed, coaching, expected_fix)` (praise-on-fail via a `_PRAISE` token set including the Arabic أحسنت; wrong-fix when a fail's coaching omits the expected_fix token) and a `faithfulness_rate(cases) -> float` reporter that returns faithful/total. Keep it model-AGNOSTIC (scores coaching against fixed verdicts; calls no model — D-10) and document it as a FLOOR not a ceiling (A6 — Phase 13/16 add the calibrated judge). Flesh out server/tests/fixtures/faithfulness_set.jsonl (10-16 labeled cases) so the gold set is constructed-faithful (rate 1.0) and includes the adversarial cases the flag tests assert on (a fail with "Great job!"; a fail naming the dot when the curve failed). Turn the 15-01 RED tests green: `test_flags_praise_on_fail`, `test_flags_wrong_fix`, and `test_faithfulness_rate_reported` (prints + asserts the rate; run with -s to see the printed rate). `pytestmark = pytest.mark.code`; no auth/Firebase fixtures. Note `.dockerignore` excludes tests/ so the fixture never ships to the image (correct — offline CI only).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest tests/test_faithfulness.py -s -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_flags_praise_on_fail` is GREEN: a FAIL verdict whose coaching contains a praise token (incl. the Arabic أحسنت) is flagged as a contradiction.
    - `test_flags_wrong_fix` is GREEN: a FAIL whose coaching omits the expected-fix token is flagged.
    - `test_faithfulness_rate_reported` is GREEN: `faithfulness_rate` returns faithful/total and is printed (`-s`) + asserted over the constructed-faithful gold set (rate 1.0).
    - `server/app/faithfulness.py` is model-AGNOSTIC (calls no model) and needs no auth/Firebase (`pytestmark = pytest.mark.code`, pure offline file read); it is documented as a FLOOR not a ceiling (A6 — Phase 13/16 add the calibrated judge).
  </acceptance_criteria>
  <done>All three faithfulness tests GREEN: praise-on-fail is flagged, wrong-fix is flagged, and a faithfulness rate is printed + asserted over the constructed-faithful gold set. The check calls no model and needs no auth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coaching output → grounding gate | The model's coaching is untrusted; the check measures it against the fixed verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-T | Tampering | Coaching that contradicts the geometry verdict | mitigate | The deterministic check flags praise-on-fail + wrong-fix and reports a rate; a regression drops the rate (GROUND-03). Model-agnostic floor; Phase 13/16 add the calibrated judge. |
| T-15-06-R | Repudiation/eval gap | Lexicon too coarse (misses paraphrased praise) | accept | A6: a lexicon check is coarse BY DESIGN for the minimal slice (D-10 explicitly minimal); documented as a floor; the calibrated-judge ceiling is Phase 13/16 scope |
| T-15-SC | Tampering | pip installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy: N/A) |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_faithfulness.py -s -q` GREEN with the rate printed.
- Per-wave merge: full server suite `uv run pytest -q` green.
</verification>

<success_criteria>
- The check flags praise-on-fail and wrong-fix; it reports a faithfulness rate.
- It is model-agnostic (no model call) and needs no auth (offline pytest).
- It is documented as a seed/floor that Phase 13/16 grow into the full bake-off + regression gate.
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-06-SUMMARY.md` when done.
</output>
</output>
