---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 02
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - server/app/curriculum_data/generate.py
  - server/app/curriculum_data/curriculum_graph.json
  - server/app/curriculum.py
  - server/app/nodes/plan.py
  - server/app/prompts.py
  - server/app/schema.py
  - server/tests/test_grounding.py
autonomous: true
requirements: [DYN-01]
tags: [curriculum, langgraph, tutor, grounding]

must_haves:
  truths:
    - "The server derives its curriculum-graph copy from the single-source asset via generate.py (no hand-edit, cannot drift)"
    - "Given a repeated struggle, the plan node selects within the reachable tier rather than a forward jump"
    - "G5 rejects an exercise in an unreached difficulty tier; G6 rejects an exercise whose prerequisites are uncleared"
    - "Backward remediation (a lower tier of an already-reached competency) is graph-LEGAL and passes the guards"
    - "The /coach DTO accepts clearedTiers/clearedCompetencies (server side of the wire contract)"
  artifacts:
    - path: "server/app/curriculum_data/curriculum_graph.json"
      provides: "Derived server copy of the baa graph (generated, committed, never hand-edited)"
    - path: "server/app/curriculum.py"
      provides: "Graph loader + reachable_tiers/prerequisites_met/tier_of helpers"
      contains: "CURRICULUM_GRAPH"
    - path: "server/app/nodes/plan.py"
      provides: "G5 (tier-reachable) + G6 (prereq) guards on top of G3/G4"
      contains: "StructuredOutputError"
    - path: "server/app/schema.py"
      provides: "clearedTiers/clearedCompetencies on TutorFactsIn (extra=forbid)"
      contains: "clearedTiers"
  key_links:
    - from: "server/app/curriculum_data/generate.py"
      to: "assets/curriculum/curriculum_graph.json"
      via: "read single-source asset, derive server copy"
      pattern: "curriculum_graph\\.json"
    - from: "server/app/nodes/plan.py"
      to: "server/app/curriculum.py"
      via: "import reachable_tiers/prerequisites_met/tier_of for G5/G6"
      pattern: "from app.curriculum import"
---

<objective>
The server graph-rail slice for DYN-01: the deployed LangGraph plan node now reasons over the curriculum graph and is railed by two new post-parse guards (G5 tier-reachability, G6 prerequisites) on top of the existing G3 verdict-lock / G4 membership guards. The graph is derived from the single-source asset by `generate.py` (cannot drift), loaded once at import in `curriculum.py`, and the wire DTO accepts the two new cleared-state FACTS fields. End-to-end demoable behavior of this slice: the agent picks within the reachable tier on a struggle, can step BACKWARD to remediate, and can never present an unreached tier or skip a prerequisite — turning the RED `test_plan_graph.py` green.

Purpose: DYN-01 (D-01: selection gated by the curriculum graph — forward-only along the prereq chain + within-tier choice + backward remediation; D-02: the membership rail stays the inner guard; D-04: the server copy is derived from the single-source asset) — "the agent selects the next exercise from baa's authored configs, reasoning about recent mistakes; the curriculum rails the choices." This is the server half of the dynamism; the offline-walker half is 15-03.
Output: derived server graph copy + curriculum helpers + G5/G6 guards + thickened plan prompt + the server-side wire fields. `test_plan_graph.py` (15-01 RED) goes GREEN.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-PATTERNS.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the server graph copy + load it + add the curriculum helpers</name>
  <files>server/app/curriculum_data/generate.py, server/app/curriculum_data/curriculum_graph.json, server/app/curriculum.py</files>
  <behavior>
    - generate.py reads assets/curriculum/curriculum_graph.json and writes a byte-faithful baa.* server copy to server/app/curriculum_data/curriculum_graph.json (ensure_ascii=False, indent=2)
    - curriculum.py loads CURRICULUM_GRAPH once at import (mirrors AUTHORED_BAA_IDS); a missing graph fails closed (caught → empty/rail rejects everything, never crashes the import path used by /coach)
    - tier_of(exerciseId) returns the node's tier or None; reachable_tiers(clearedTiers) returns the set of tiers the child may enter (manqul always reachable; manzur after manqul cleared; ghayrManzur after manzur); prerequisites_met(exerciseId, clearedCompetencies) is True iff every prereq competency of the node's competency is cleared
  </behavior>
  <read_first>
    - server/app/curriculum_data/generate.py (the existing `regenerate()` derive-from-assets: _HERE/_OUT/_REPO_ROOT/_UNITS/_EXERCISES, the FileNotFoundError guard, the write_text(ensure_ascii=False, indent=2), the __main__ summary block — EXTEND this same function)
    - server/app/curriculum.py (`_load_authored_ids`, `AUTHORED_BAA_IDS`, `is_authored` — the load-once-at-import idiom to mirror)
    - assets/curriculum/curriculum_graph.json (the single-source asset authored in 15-01 — its competencies/tiers/nodes shape)
    - 15-PATTERNS.md "assets/curriculum/curriculum_graph.json + …generate.py" section (the exact lines to copy) + § "Loading the graph on the server" code example
    - 15-RESEARCH.md § Pattern 1 + § "Loading the graph on the server (mirror _load_authored_ids)"
  </read_first>
  <action>
    Extend generate.py's `regenerate()`: add `_GRAPH = _REPO_ROOT / "assets" / "curriculum" / "curriculum_graph.json"`, read it, filter `nodes` to `baa.*` ids, and `write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n")` to a NEW `_HERE / "curriculum_graph.json"`. Extend the FileNotFoundError guard + the __main__ one-line summary to mention the graph. Run `cd server && uv run python -m app.curriculum_data.generate` to produce server/app/curriculum_data/curriculum_graph.json (commit it, like baa_authored_ids.json — NEVER hand-edit the derived file). In curriculum.py add `_GRAPH_PATH = … / "curriculum_data" / "curriculum_graph.json"`, a `_load_graph()` returning the parsed dict, `CURRICULUM_GRAPH = _load_graph()` loaded ONCE at import, and the three helpers `tier_of`, `reachable_tiers`, `prerequisites_met` (plus a small `remediation_target` helper if convenient for symmetry with the Dart walker). A missing/unreadable graph file must fail closed (the rail rejects, falling to AuthoredFallback — Security: T-15-02-T) — never raise at import in a way that 500s /coach. Do NOT touch is_authored / AUTHORED_BAA_IDS (G4 stays the inner guard, D-02). Confirm the Dockerfile copies the whole `app/` package and `.dockerignore` does NOT exclude the new `curriculum_data/curriculum_graph.json` (RESEARCH A4) so the derived graph ships to the image.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run python -m app.curriculum_data.generate && uv run python -c "from app.curriculum import CURRICULUM_GRAPH, tier_of, reachable_tiers, prerequisites_met; assert CURRICULUM_GRAPH['letterId']=='baa'; assert 'manqul' in reachable_tiers([]); assert 'ghayrManzur' not in reachable_tiers([]); print('curriculum helpers OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run python -m app.curriculum_data.generate` regenerates `server/app/curriculum_data/curriculum_graph.json` from the asset cleanly; the derived copy is byte-faithful (baa.* nodes only) and is NOT hand-edited.
    - `from app.curriculum import CURRICULUM_GRAPH` loads ONCE at import; `CURRICULUM_GRAPH['letterId'] == 'baa'`.
    - `reachable_tiers([])` includes `manqul` and excludes `ghayrManzur`; `tier_of` returns a node's tier or None; `prerequisites_met` is True only when every prereq competency is cleared.
    - A missing/unreadable derived graph fails closed (the rail rejects → AuthoredFallback) and never raises at import in a way that 500s /coach; `is_authored`/`AUTHORED_BAA_IDS` are untouched (G4 stays the inner guard, D-02).
    - The Dockerfile copies `app/` and `.dockerignore` does not exclude `curriculum_data/curriculum_graph.json` (RESEARCH A4 — the derived graph ships to the image).
  </acceptance_criteria>
  <done>The derived server graph copy exists and matches the asset; CURRICULUM_GRAPH loads at import; reachable_tiers/prerequisites_met/tier_of behave (manqul reachable from empty, ghayrManzur not). is_authored untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add G5/G6 graph guards + thicken the plan prompt + extend the FACTS DTO</name>
  <files>server/app/nodes/plan.py, server/app/prompts.py, server/app/schema.py, server/tests/test_grounding.py</files>
  <behavior>
    - G5: if the chosen next_exercise_id has a tier and that tier is not in reachable_tiers(facts["clearedTiers"]), raise StructuredOutputError (→ degrade-to-floor)
    - G6: if prerequisites_met(next_exercise_id, facts["clearedCompetencies"]) is False, raise StructuredOutputError
    - Backward remediation (a lower tier of an already-reached competency) passes G5+G6 (Pitfall 3 — the prereqs are met, the tier is reachable)
    - TutorFactsIn accepts clearedTiers/clearedCompetencies as optional string-lists (extra=forbid still pins the rest)
  </behavior>
  <read_first>
    - server/app/nodes/plan.py (the G3/G4 guard block lines 77-98 — the EXACT idiom: `raise StructuredOutputError(...)` + `logger.warning`; the `from app.curriculum import is_authored` import; `_structured_plan` lines ~57-63 for the FACTS-in-HumanMessage / prompt-in-SystemMessage split; `.with_structured_output(Plan, method="json_mode")` line ~57 — KEEP json_mode)
    - server/app/prompts.py (PLAN_PROMPT lines ~74-93 — the existing GROUNDING RULE; thicken it, don't replace it)
    - server/app/schema.py (TutorFactsIn lines ~47-78 — the `model_config = ConfigDict(extra="forbid")` + the trajectory/strengthTags `Field(default_factory=list, ...)` to mirror)
    - server/tests/test_grounding.py (the G4 cases + `test_endpoint_degrades_on_structured_error` ~234-260 — the degrade contract the new guards ride; add a graph-membership case here)
    - 15-PATTERNS.md "server/app/nodes/plan.py" + "server/app/schema.py" sections (the exact guard idiom + the 422-trap warning)
    - 15-RESEARCH.md § Pattern 3 (G5/G6 code + the prompt-thickening spec) + Pitfall 1 + Pitfall 3
  </read_first>
  <action>
    In plan.py, AFTER the existing G4 block, import `reachable_tiers, prerequisites_met, tier_of` from app.curriculum and add G5 (tier-reachability) and G6 (prerequisite chain) — each structurally identical to G4: a one-line `logger.warning` + `raise StructuredOutputError(...)`. CRITICAL (Pitfall 3): G5/G6 check only reachability + prereqs-met, so a LOWER tier of an already-reached competency passes — backward remediation must NOT be rejected. Do NOT touch G3/G4 or the degrade machinery. In prompts.py, thicken PLAN_PROMPT (keep it in the SystemMessage, cache-stable): add (1) the reachable-tier list + cleared competencies as context, (2) the rule "on a repeated struggle in tier X, remediate to the next-easier tier of the SAME competency (ghayrManzur→manzur→manqul); never jump a tier forward," and (3) "within the reachable tier, choose the exercise targeting the child's recent mistakeIds/struggleTags." In schema.py add `clearedTiers: list[str] = Field(default_factory=list, ...)` and `clearedCompetencies: list[str] = Field(default_factory=list, ...)` to TutorFactsIn — Pitfall 1: these MUST be mirrored in lib/tutor/tutor_facts.dart, which 15-04 owns; note the lockstep dependency in the SUMMARY (the live /coach 422s if the Dart side ships the field before the server is re-deployed, or vice-versa). Extend test_grounding.py with one graph-membership case (an unauthored id still rejected by G4 — unchanged).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest tests/test_plan_graph.py tests/test_grounding.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_plan_graph.py` is GREEN: a repeated struggle selects within-tier; G5 rejects an unreached tier (StructuredOutputError); G6 rejects an unmet prereq; backward remediation (a lower tier of an already-reached competency) is NOT rejected (Pitfall 3).
    - `test_grounding.py` stays GREEN — G3 verdict-lock, G4 membership, and the degrade-to-floor machinery are untouched; the added graph-membership case (an unauthored id) is still rejected by G4.
    - `TutorFactsIn` accepts `clearedTiers`/`clearedCompetencies` as optional string-lists with `extra="forbid"` still pinning every other field.
    - PLAN_PROMPT is thickened (not replaced) in the SystemMessage with the reachable-tier/cleared-competency context + the ghayrManzur→manzur→manqul remediation rule + the within-tier mistake-targeting rule.
  </acceptance_criteria>
  <done>test_plan_graph.py is GREEN: struggle selects within-tier, G5 rejects an unreached tier, G6 rejects an unmet prereq, backward remediation is allowed. test_grounding.py stays green (G3/G4/degrade untouched). TutorFactsIn accepts the two new fields under extra=forbid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /coach | Untrusted enlarged FACTS payload crosses here; Pydantic extra=forbid is the chokepoint |
| agent (LLM) → selection | The model's proposed next_exercise_id is untrusted; guards rail it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-T | Tampering | Missing/tampered curriculum_graph.json at runtime | mitigate | Graph loaded once at import; a missing/unreadable file fails closed (rail rejects → AuthoredFallback floor); G4 (is_authored) still bounds every id independently |
| T-15-02-E | Elevation/Tampering | Agent flips a fail→pass or skips a prerequisite | mitigate | G3 verdict-lock unchanged (agent cannot advance on a fail); G5/G6 restrict SELECTION only, never the verdict; the star is on-device (ADR-014). Backward remediation explicitly allowed (Pitfall 3). |
| T-15-02-ID | Information Disclosure | Enlarged FACTS leak PII/strokes | mitigate | clearedTiers/clearedCompetencies are derived string-lists (tier/competency ids, no PII); extra=forbid rejects any un-whitelisted field; the non-PII regression test is extended in 15-05 |
| T-15-SC | Tampering | pip installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy: N/A) |
</threat_model>

<verification>
- `uv run python -m app.curriculum_data.generate` regenerates the derived graph cleanly (Task 1).
- Curriculum helpers behave on empty/partial cleared-state (Task 1 verify).
- `uv run pytest tests/test_plan_graph.py tests/test_grounding.py -q` is GREEN (Task 2).
- Per-wave merge: `cd server && uv run pytest -q` full server suite green.
</verification>

<success_criteria>
- The server graph copy is derived (not hand-written) from the single-source asset and committed.
- G5/G6 rail the plan node; backward remediation passes; forward jumps are rejected to the floor.
- The plan prompt reasons over the reachable tier + cleared competencies + recent mistakes.
- TutorFactsIn accepts clearedTiers/clearedCompetencies (server side of the wire contract; Dart side in 15-04 — re-deploy lockstep).
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-02-SUMMARY.md` when done. Note the 422-trap lockstep with 15-04 (server re-deploy must follow both sides landing).
</output>
</output>
