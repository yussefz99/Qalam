---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 05
type: execute
wave: 2
depends_on: ["15-02", "15-03", "15-04"]
files_modified:
  - lib/tutor/selection_providers.dart
  - lib/features/letter_unit/letter_unit_controller.dart
  - lib/features/letter_unit/letter_unit_screen.dart
  - test/features/letter_unit/letter_unit_screen_test.dart
  - test/tutor/payload_nonpii_test.dart
  - server/tests/test_payload_nonpii.py
autonomous: true
requirements: [DYN-01, DYN-02]
tags: [tutor, letter_unit, riverpod, grounding]

must_haves:
  truths:
    - "Entering the baa unit runs the dynamic selection flow, not LetterUnitController's fixed 6-section walk"
    - "A fail re-surfaces a remediation exercise (backward remediation), not the next linear section"
    - "Selection routes online (RemoteAgent's plan.nextExerciseId when graph-legal) and offline (the walker), degrading independently of coaching"
    - "The quiet star fires ONLY when isMasteryMet is true; never on merely navigating to the mastery section"
    - "recordMastery is NOT called for a clicked-through unit with unmet reps"
    - "The enlarged FACTS carry no PII/strokes (regression, both client and server)"
  artifacts:
    - path: "lib/tutor/selection_providers.dart"
      provides: "ExerciseSelector router provider (online↔offline) — single switch point"
      contains: "ExerciseSelector"
    - path: "lib/features/letter_unit/letter_unit_screen.dart"
      provides: "Unit-level config-presenter fed by the selector (replaces the _section switch)"
    - path: "lib/features/letter_unit/letter_unit_controller.dart"
      provides: "Drift-persisted position + mastery gated on isMasteryMet"
      contains: "isMasteryMet"
  key_links:
    - from: "lib/tutor/selection_providers.dart"
      to: "lib/curriculum/curriculum_graph_walker.dart"
      via: "offline path uses CurriculumGraphWalker; online uses RemoteAgent plan.nextExerciseId"
      pattern: "CurriculumGraphWalker|nextExerciseId"
    - from: "lib/features/letter_unit/letter_unit_controller.dart"
      to: "lib/curriculum/mastery_condition.dart"
      via: "gate recordMastery behind isMasteryMet"
      pattern: "isMasteryMet"
    - from: "lib/features/letter_unit/letter_unit_controller.dart"
      to: "lib/data/graph_position_repository.dart"
      via: "persist + restore graph position (replaces _resumeByLetter)"
      pattern: "graphPositionRepository|GraphPositionRepository"
---

<objective>
The integration slice that makes DYN-02 real end-to-end: a Riverpod `ExerciseSelector` router picks online (the RemoteAgent's `plan.nextExerciseId` when present AND graph-legal) vs offline (the `CurriculumGraphWalker`), the baa unit is driven at the UNIT level by the selected config (Open Q1 — replacing `LetterUnitController`'s fixed `_section` switch and its in-memory `_resumeByLetter` map), the quiet star is gated strictly behind `isMasteryMet` (Pitfall 2 — never on navigation), graph position persists to/from Drift, and the non-PII regression is extended over the two new FACTS fields on both client and server.

Purpose: DYN-02 "the dynamic, resume-aware flow replaces the fixed section walk for the baa unit end-to-end; resume still works; one quiet star at mastery" + DYN-01's visible dynamism (backward remediation re-surfacing an easier exercise). This is the wiring slice — it consumes the rail (15-02), the walker+mastery (15-03), and the persistence (15-04).
Output: the selection router + the rewired letter unit + the mastery-gated star + the non-PII regression, turning the 15-01 RED dynamic_selection_test green and flipping the old letter_unit_screen_test mastery-on-navigation assertion.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-PATTERNS.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The ExerciseSelector router provider (online↔offline single switch point)</name>
  <files>lib/tutor/selection_providers.dart</files>
  <behavior>
    - A Riverpod provider exposes an ExerciseSelector that, given facts + the resumed GraphPosition, returns the next baa exercise id
    - Online: use the RemoteAgent decision's plan.nextExerciseId WHEN present AND graph-legal (tier reachable + prereqs met + authored); otherwise fall to the walker
    - Offline/timeout/error/illegal: use CurriculumGraphWalker (selection degrades independently of coaching, which still falls to AuthoredFallbackBrain)
  </behavior>
  <read_first>
    - lib/tutor/tutor_providers.dart (the tutorBrainFactoryProvider single-switch-point lines ~71-88 — the ONE place online↔offline routing lives — and TutorLineNotifier lines ~98-111, the Notifier<String?> model since Riverpod 3 dropped StateProvider; keepAlive mirrors appDatabaseProvider)
    - lib/tutor/remote_agent_brain.dart + lib/tutor/tutor_decision.dart (the decision's TutorPlan.nextExerciseId the online path reads; the degrade-to-floor behavior)
    - lib/curriculum/curriculum_graph_walker.dart (the ExerciseSelector seam + CurriculumGraphWalker from 15-03) ; lib/curriculum/curriculum_graph.dart (the legality helpers tierOf/nextForward)
    - 15-PATTERNS.md "lib/tutor/ selection seam" section ; 15-RESEARCH.md § Pattern 4 (router placement) + Pitfall 5
  </read_first>
  <action>
    Create lib/tutor/selection_providers.dart mirroring tutorBrainFactoryProvider's single-switch shape: a `@Riverpod(keepAlive: true)` (or hand-written keepAlive) provider returning an `ExerciseSelector`. Online path: read the RemoteAgent decision's `plan.nextExerciseId`; accept it ONLY if it is graph-legal (authored via the existing membership, tier reachable, prereqs met — reuse the curriculum_graph legality helpers so client and server agree). Offline/timeout/error/illegal: delegate to `CurriculumGraphWalker` (constructed over the parsed CurriculumGraph). Riverpod-only (CLAUDE.md Decided — reject any BLoC/GetX). Pitfall 5: selection and coaching degrade on SEPARATE axes — coaching still routes to AuthoredFallbackBrain; selection routes to the walker; never revert selection to the old fixed 6-section linear sequence. If a single-value notifier is needed, model it on TutorLineNotifier (Notifier<String?>), not StateProvider.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -3 && flutter test test/tutor/</automated>
  </verify>
  <done>The selection router provider exists, picks the online plan.nextExerciseId when graph-legal and the walker otherwise, and degrades selection independently of coaching. Existing tutor tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the baa unit by selection + gate the star on isMasteryMet (retire the fixed walk)</name>
  <files>lib/features/letter_unit/letter_unit_controller.dart, lib/features/letter_unit/letter_unit_screen.dart, test/features/letter_unit/letter_unit_screen_test.dart</files>
  <behavior>
    - Entering the baa unit presents the SELECTED exercise config (via ExerciseScaffold) — not the fixed _section(index) switch
    - A fail re-surfaces a remediation exercise (backward remediation), not the next linear section
    - Graph position is read from / written to Drift (replaces _resumeByLetter); resume restores it after a relaunch
    - recordMastery fires ONLY when isMasteryMet is true (essential core at mom's reps); NOT on reaching the mastery section
    - The old letter_unit_screen_test Test 3 assertion (mastery on goTo(5)) FLIPS: a clicked-through unit with unmet reps does NOT call recordMastery
  </behavior>
  <read_first>
    - lib/features/letter_unit/letter_unit_controller.dart (the _onEnterSection mastery-on-navigation bug + cleanReps:0 write lines ~109-129; the _resumeByLetter in-memory map being retired)
    - lib/features/letter_unit/letter_unit_screen.dart (the _section(data,index) switch lines ~244-250 keyed by section id — what the unit-level presenter replaces, Open Q1)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart + exercise_controller.dart (the already-config-driven scaffold + the _onResult per-attempt seam where the verdict is scored and brain.next(facts) fires for coaching — selection invoked at the unit level reuses this)
    - lib/curriculum/mastery_condition.dart (isMasteryMet from 15-03) ; lib/data/graph_position_repository.dart (getPosition/setPosition from 15-04) ; lib/tutor/selection_providers.dart (Task 1)
    - test/features/letter_unit/letter_unit_screen_test.dart (the _pump+ProviderScope harness ~94-113; Test 3 ~148-161 — the assertion to FLIP) ; test/features/letter_unit/dynamic_selection_test.dart (the 15-01 RED end-to-end test to turn green)
    - 15-PATTERNS.md "letter_unit_controller.dart + letter_unit_screen.dart (REPLACE the fixed walk)" section ; 15-RESEARCH.md § Pattern 6 + Open Q1 + Pitfall 2
  </read_first>
  <action>
    Rewire the baa letter unit to drive at the UNIT level (Open Q1): replace letter_unit_screen.dart's `_section(data,index)` switch with a single config-presenter that renders the SELECTED exercise config through the existing config-driven ExerciseScaffold, fed by the ExerciseSelector router (Task 1). In letter_unit_controller.dart: (a) replace the in-memory `_resumeByLetter` map with reads/writes through graphPositionRepository (getPosition at entry — a Future, not a stream, Pitfall 6; setPosition on each cleared step) — durable resume (D-08); (b) DELETE the `state.atMastery → _recordMastery()` auto-write and instead gate `recordMastery()` strictly behind `isMasteryMet(graph, perExerciseCleanReps)` (D-06, Pitfall 2 — never the star for merely navigating; never write cleanReps:0). On a fail, present the selector's remediation choice (backward remediation), not the next section. Update test/features/letter_unit/letter_unit_screen_test.dart: FLIP the old Test-3 mastery-on-goTo(5) assertion to assert recordMastery is NOT called on a clicked-through unit with unmet reps (reuse the _pump + ProviderScope-override harness). The 15-01 dynamic_selection_test (a fail re-surfaces a remediation) goes green. Do NOT introduce ت/ث coaching (D-11). Anti-gamification: still exactly ONE quiet star (CLAUDE.md Decided).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/features/letter_unit/dynamic_selection_test.dart test/features/letter_unit/letter_unit_screen_test.dart</automated>
  </verify>
  <done>dynamic_selection_test GREEN (the unit runs the selection flow; a fail re-surfaces a remediation exercise). letter_unit_screen_test GREEN with the flipped assertion (no recordMastery on a clicked-through unit with unmet reps; star only when isMasteryMet). Resume reads/writes Drift position. Exactly one quiet star at mastery.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the non-PII regression over the two new FACTS fields (client + server)</name>
  <files>test/tutor/payload_nonpii_test.dart, server/tests/test_payload_nonpii.py</files>
  <read_first>
    - test/tutor/payload_nonpii_test.dart (the `_whitelist` set lines ~27-39 + `_fullyPopulatedFacts` lines ~74-87 — add the two new string-list fields, assert no PII key)
    - server/tests/test_payload_nonpii.py (the LEGIT_FACTS dict lines ~28-40 + test_extra_forbid_is_pinned_on_both_models lines ~104-107 — add the two fields, assert extra=forbid still pins, assert no PII)
    - lib/tutor/tutor_facts.dart + server/app/schema.py (the two fields landed in 15-04 / 15-02 — the contract under test)
    - 15-PATTERNS.md "extra=forbid wire-contract lockstep" shared pattern ; 15-RESEARCH.md Pitfall 1
  </read_first>
  <action>
    Extend test/tutor/payload_nonpii_test.dart: add clearedTiers/clearedCompetencies to the `_whitelist` and to `_fullyPopulatedFacts`, and assert the serialized payload carries them AND still trips no PII/stroke key (the existing `\b[xy]\b|stroke|offset|coord|point|raw|nick|name` regex). Extend server/tests/test_payload_nonpii.py: add the two fields to LEGIT_FACTS, assert TutorFactsIn accepts them, that extra=forbid still rejects an un-whitelisted field, and that the two fields carry no PII. Both must be green — proving the enlarged wire contract is non-PII on both ends (GROUND-02 regression).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/tutor/payload_nonpii_test.dart && cd server && uv run pytest tests/test_payload_nonpii.py -q</automated>
  </verify>
  <done>Both non-PII tests GREEN with the two new fields included: they serialize, carry no PII/stroke key, and extra=forbid still pins the server DTO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → on-device star | The agent's advance is a suggestion; the star is on-device only |
| client → /coach | The enlarged non-PII FACTS cross here |
| selection (online) → presented config | The agent's choice is untrusted; the client re-checks graph-legality before presenting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-E | Elevation/Tampering | Agent grants the star / flips fail→pass | mitigate | recordMastery gated strictly on isMasteryMet (on-device, ADR-014/D-06); the controller never writes the star off a CoachOut; cleanReps:0 navigation-write deleted (Pitfall 2) |
| T-15-05-T | Tampering | Online selection presents an illegal/unauthored config | mitigate | The router re-checks graph-legality (authored + tier-reachable + prereqs-met) client-side before presenting; illegal → walker (Pitfall 5) |
| T-15-05-ID | Information Disclosure | Enlarged FACTS leak PII/strokes | mitigate | Non-PII regression extended on BOTH client + server over the two new fields (Task 3); buildTutorFacts signature accepts no stroke/profile param |
| T-15-05-D11 | Integrity (scope) | Cross-letter ت/ث coaching | mitigate | No ت/ث content introduced; baa's own dot only (D-11) |
| T-15-SC | Tampering | pub installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy: N/A) |
</threat_model>

<verification>
- `flutter test test/features/letter_unit/dynamic_selection_test.dart test/features/letter_unit/letter_unit_screen_test.dart` GREEN.
- `flutter test test/tutor/payload_nonpii_test.dart` + `cd server && uv run pytest tests/test_payload_nonpii.py -q` GREEN.
- Per-wave merge: full client suite `flutter test` + full server suite `uv run pytest -q` green.
- Manual UAT (human gate): on device with `--dart-define=TUTOR_BASE_URL=<cloud-run>` (after server re-deploy), trace baa, fail a stroke, confirm a remediation exercise re-surfaces online.
</verification>

<success_criteria>
- Entering the baa unit runs the dynamic selection flow, not the fixed section walk (DYN-02).
- A fail re-surfaces a remediation exercise (backward remediation, DYN-01/D-03).
- Selection degrades online↔offline independently of coaching (Pitfall 5).
- The quiet star fires only on isMasteryMet; never on navigation (D-06/Pitfall 2); exactly one star.
- Resume restores graph position from Drift after a relaunch (D-08).
- The enlarged FACTS are non-PII on both client and server (GROUND-02 regression).
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-05-SUMMARY.md` when done. Flag the device-UAT online-path item (needs the re-deployed server, mirrors the Phase-14 UAT gate).
</output>
