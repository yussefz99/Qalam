---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - assets/curriculum/curriculum_graph.json
  - docs/curriculum/baa-curriculum-graph-signoff-sheet.md
  - server/tests/test_plan_graph.py
  - server/tests/test_faithfulness.py
  - server/tests/fixtures/faithfulness_set.jsonl
  - test/curriculum/curriculum_graph_test.dart
  - test/curriculum/curriculum_graph_walker_test.dart
  - test/curriculum/mastery_condition_test.dart
  - test/data/graph_position_repository_test.dart
  - test/features/letter_unit/dynamic_selection_test.dart
autonomous: true
requirements: [DYN-01, DYN-02, GROUND-03]
tags: [curriculum, tutor, grounding, drift, langgraph]

must_haves:
  truths:
    - "Every Phase-15 requirement has at least one RED (failing) automated test before implementation begins"
    - "The provisional baa curriculum graph asset exists (signedOff:false) so all downstream tests can read it"
    - "A clean owner-mother sign-off sheet exists drafting the 19-config competency/tier/clean-reps mapping"
  artifacts:
    - path: "assets/curriculum/curriculum_graph.json"
      provides: "Provisional baa curriculum graph (competency/tier/prerequisites/minCleanReps; signedOff:false)"
      contains: "\"signedOff\": false"
    - path: "docs/curriculum/baa-curriculum-graph-signoff-sheet.md"
      provides: "Clean tier-level sign-off sheet for owner-mother (D-05)"
    - path: "server/tests/test_plan_graph.py"
      provides: "RED tests for G5/G6 graph guards + backward remediation (DYN-01)"
    - path: "server/tests/test_faithfulness.py"
      provides: "RED tests for the GROUND-03 faithfulness check"
    - path: "test/curriculum/curriculum_graph_walker_test.dart"
      provides: "RED tests for the offline deterministic walker (DYN-01/DYN-02)"
    - path: "test/curriculum/mastery_condition_test.dart"
      provides: "RED tests for the on-device mastery condition (DYN-02)"
    - path: "test/data/graph_position_repository_test.dart"
      provides: "RED test for Drift resume position across a simulated restart (DYN-02)"
  key_links:
    - from: "test/curriculum/curriculum_graph_test.dart"
      to: "assets/curriculum/curriculum_graph.json"
      via: "rootBundle asset parse"
      pattern: "curriculum_graph\\.json"
    - from: "server/tests/test_plan_graph.py"
      to: "server/app/nodes/plan.py"
      via: "imports the plan node under test (RED until G5/G6 added)"
      pattern: "from app"
---

<objective>
Establish the Nyquist RED contract for Phase 15: every requirement (DYN-01, DYN-02, GROUND-03) gets a failing automated test BEFORE any implementation, and the one shared data artifact every downstream slice reads — the provisional baa curriculum graph asset — is authored here in the Flutter curriculum assets (D-04), PROVISIONAL (`signedOff:false`, mirroring the `AUTHORED_BAA_IDS` gate). Also produce the clean owner-mother sign-off sheet (D-05) that Plan 15-07 gates on.

Purpose: Phase 15 is a thickening of the deployed Phase-14 spine. Locking RED tests first means each Wave-1/2 slice has an executable definition of done, and creating the graph asset once (here) removes a file-ownership conflict from the parallel Wave-1 plans.
Output: 1 provisional graph asset + 1 sign-off sheet + 5 NEW test files (3 Dart, 2 Python) + 1 JSONL fixture. All tests RED (failing by missing symbol / missing graph behavior) by design.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-PATTERNS.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the provisional baa curriculum graph asset + the owner-mother sign-off sheet</name>
  <files>assets/curriculum/curriculum_graph.json, docs/curriculum/baa-curriculum-graph-signoff-sheet.md</files>
  <read_first>
    - docs/curriculum/national-curriculum-grade1.md (the competency list + منقول→منظور→غير منظور difficulty lattice + the DRAFT baa-config→competency mapping; the §"Open for owner-mother sign-off" framing) — the pedagogical source
    - assets/curriculum/exercises.json + assets/curriculum/units.json (the canonical baa.* exercise ids the graph nodes key on; each entry's signedOff/skill/type fields)
    - server/app/curriculum_data/baa_authored_ids.json (the _meta + sign-off shape to mirror — PATTERNS.md "_meta + sign-off shape")
    - 15-RESEARCH.md § Pattern 2 (the exact recommended graph schema: competencies[], tiers[], nodes[] with competency/tier/minCleanReps; essential flag = 70/30 split)
    - 15-PATTERNS.md "assets/curriculum/curriculum_graph.json" section (the _meta block + signedOff gate)
  </read_first>
  <action>
    Author assets/curriculum/curriculum_graph.json as a SEPARATE file from exercises.json (RESEARCH A5 — independent sign-off gate). Top-level keys: `_meta` (title, source pointing to docs/curriculum/national-curriculum-grade1.md, `regenerate` = "cd server && uv run python -m app.curriculum_data.generate", and a sign_off note), `letterId: "baa"`, `signedOff: false`, `competencies` (the forward prerequisite chain recognize→positionalForms→copyWrite→fluentReading each `essential:true`, plus wordBuilding/grammarTransform `essential:false` per the 70/30 split — each with an `id`, `essential`, `prerequisites` list), `tiers: ["manqul","manzur","ghayrManzur"]` (the إملاء copy→look-write→dictation ramp), and `nodes` — one node per signed-off baa.* exercise id in exercises.json, each carrying `exerciseId`, `competency`, `tier` (non-null ONLY for writing/إملاء exercises, else null), and `minCleanReps` (DRAFT values — A2; e.g. trace ~3, write ~2, teach/sentence ~1). signedOff MUST be false (PROVISIONAL, Pitfall 4 / D-05) — do NOT flip it here; 15-07 owns the flip. Then write docs/curriculum/baa-curriculum-graph-signoff-sheet.md as a clean human-readable table (one row per node: exerciseId | competency | essential? | tier | draft minCleanReps) plus the three sign-off questions she confirms at the tier level (competency mapping, the 70/30 essential/enrichment split, the per-skill clean-reps). Reinforce baa's own dot only — introduce NO ت/ث content (D-11).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python3 -c "import json,sys; g=json.load(open('assets/curriculum/curriculum_graph.json')); assert g['signedOff'] is False; assert g['letterId']=='baa'; assert {'manqul','manzur','ghayrManzur'}.issubset(set(g['tiers'])); assert any(c.get('essential') for c in g['competencies']) and any(not c.get('essential') for c in g['competencies']); assert all('exerciseId' in n and 'competency' in n and 'minCleanReps' in n for n in g['nodes']); print('graph OK', len(g['nodes']), 'nodes')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/curriculum/curriculum_graph.json` parses and carries `"signedOff": false` (PROVISIONAL — the flag is NOT flipped here; 15-07 owns the flip).
    - `letterId == "baa"`; `tiers` contains the three-tier إملاء ramp {manqul, manzur, ghayrManzur}; every node id is `baa.*` only (no ت/ث content — D-11).
    - `competencies` carries BOTH at least one `essential:true` entry and one `essential:false` entry (the 70/30 split); every `nodes[]` entry has `exerciseId` + `competency` + `minCleanReps`.
    - `docs/curriculum/baa-curriculum-graph-signoff-sheet.md` lists one reviewable row per node plus the three tier-level sign-off questions (competency mapping, 70/30 split, per-skill clean-reps).
  </acceptance_criteria>
  <done>curriculum_graph.json parses, carries signedOff:false, has both essential and enrichment competencies, the three-tier إملاء ramp, and every node has exerciseId/competency/minCleanReps. The sign-off sheet lists every node as a reviewable row with the three tier-level sign-off questions.</done>
</task>

<task type="auto">
  <name>Task 2: RED server tests — graph rail (DYN-01) + faithfulness check (GROUND-03)</name>
  <files>server/tests/test_plan_graph.py, server/tests/test_faithfulness.py, server/tests/fixtures/faithfulness_set.jsonl</files>
  <read_first>
    - server/tests/test_grounding.py (the model-free `pytest.mark.code` pattern; `_patch_all` / `_FakeModel` monkeypatch of the plan model at lines ~50-55,162-183; `test_endpoint_degrades_on_structured_error` at ~234-260 — the degrade contract)
    - server/tests/test_payload_nonpii.py (LEGIT_FACTS constant + @pytest.mark.parametrize discipline; the `extra="forbid"` guard test)
    - server/app/nodes/plan.py (the existing G3/G4 guard block lines 77-98; `next_exercise_id`/`intent` fields) — the tests describe G5/G6 that do NOT exist yet (RED)
    - 15-RESEARCH.md § "The model-free faithfulness check" code example + § Phase Requirements → Test Map (the exact test names)
    - 15-PATTERNS.md "server/tests/test_faithfulness.py" + "plan.py" test-analog sections
  </read_first>
  <action>
    Create server/tests/test_plan_graph.py with `pytestmark = pytest.mark.code`, monkeypatching the plan model exactly like test_grounding.py's `_FakeModel`/`_patch_all`. Author these RED cases (named verbatim per 15-VALIDATION.md): `test_struggle_selects_within_tier` (a repeated `shallowBowl` struggle in facts → the plan picks a trace-drill within the reachable tier, not a forward jump), `test_unreached_tier_rejected` (a plan choosing an exercise whose tier is NOT in reachable_tiers(clearedTiers) raises StructuredOutputError — G5), `test_prereq_unmet_rejected` (a plan choosing an exercise whose competency prereqs are uncleared raises StructuredOutputError — G6), `test_backward_remediation_allowed` (a plan choosing a LOWER tier ghayrManzur→manzur of an already-reached competency is NOT rejected — Pitfall 3). These reference `app.curriculum` helpers `reachable_tiers`/`prerequisites_met`/`tier_of` that do not exist yet → RED by import/attribute error. Create server/tests/fixtures/faithfulness_set.jsonl with 10-16 labeled cases (`passed`, `mistakeId`, `expectedFix`, `coaching`) — constructed-faithful gold set PLUS adversarial entries used by the flag tests (a fail with "Great job!"; a fail naming the dot when the curve failed). Create server/tests/test_faithfulness.py with `pytestmark = pytest.mark.code` and RED tests `test_flags_praise_on_fail`, `test_flags_wrong_fix`, `test_faithfulness_rate_reported` referencing a `_contradicts`/check function in the not-yet-written module → RED. Do NOT implement the check logic here (15-06 does); the tests assert the contract.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/server && uv run pytest tests/test_plan_graph.py tests/test_faithfulness.py -q 2>&1 | tail -15; echo "EXPECT: collection/import errors or failures (RED by design)"</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/test_plan_graph.py` carries `pytest.mark.code` and contains the four verbatim cases {test_struggle_selects_within_tier, test_unreached_tier_rejected, test_prereq_unmet_rejected, test_backward_remediation_allowed}; it imports `reachable_tiers`/`prerequisites_met`/`tier_of` from `app.curriculum` and FAILS/ERRORS RED (those helpers do not exist yet).
    - `server/tests/test_faithfulness.py` carries `pytest.mark.code`, contains {test_flags_praise_on_fail, test_flags_wrong_fix, test_faithfulness_rate_reported}, references the not-yet-written faithfulness check, and FAILS/ERRORS RED.
    - `server/tests/fixtures/faithfulness_set.jsonl` has 10-16 labeled cases including BOTH constructed-faithful gold entries AND the adversarial entries the flag tests assert on (a fail with "Great job!"; a fail naming the dot when the curve failed).
    - No test in either file is GREEN (a green Wave-0 test means the contract is too weak).
  </acceptance_criteria>
  <done>Both server test files exist, carry `pytest.mark.code`, name the exact tests from 15-VALIDATION.md, and FAIL/ERROR (RED) because the graph helpers + faithfulness check do not yet exist. The fixture JSONL has both faithful and adversarial cases.</done>
</task>

<task type="auto">
  <name>Task 3: RED Dart tests — graph/walker/mastery + Drift resume + dynamic flow (DYN-01/DYN-02)</name>
  <files>test/curriculum/curriculum_graph_test.dart, test/curriculum/curriculum_graph_walker_test.dart, test/curriculum/mastery_condition_test.dart, test/data/graph_position_repository_test.dart, test/features/letter_unit/dynamic_selection_test.dart</files>
  <read_first>
    - test/data/app_database_test.dart (the TWO restart shapes lines ~25-39 shared-in-memory and ~118-187 temp-file real-onUpgrade — the D-08 simulated-restart pattern to copy for the position test)
    - test/features/letter_unit/letter_unit_screen_test.dart (the `_pump` + ProviderScope-override harness lines ~94-113; Test 3 lines ~148-161 that currently asserts recordMastery fires on goTo(5) — this assertion must FLIP)
    - test/tutor/payload_nonpii_test.dart (the `_whitelist` set + `_fullyPopulatedFacts` — the non-PII discipline the new fields extend)
    - 15-RESEARCH.md § Recommended Project Structure (the lib/curriculum/ + lib/data/graph_position_repository.dart files these tests target) + § Phase Requirements → Test Map (exact behaviors)
    - 15-PATTERNS.md "lib/curriculum/…" + "lib/data/app_database.dart" test-analog sections
  </read_first>
  <action>
    Create the RED Dart tests, each importing symbols that do not exist yet (RED by missing symbol): test/curriculum/curriculum_graph_test.dart (asserts CurriculumGraph parses the asset: essentialNodes filter, tier_of/nextForward/remediateOneTier helpers, signedOff getter). test/curriculum/curriculum_graph_walker_test.dart (CurriculumGraphWalker implements ExerciseSelector: `facts.passed` → graph.nextForward(position); a fail → one tier down within the SAME competency ghayrManzur→manzur→manqul; at the manqul floor re-presents in place — Pitfall 5: NEVER the old linear sequence). test/curriculum/mastery_condition_test.dart (isMasteryMet true ONLY when every essential node's clean-reps ≥ its minCleanReps; enrichment nodes never gate; false on a clicked-through unit — Pitfall 2). test/data/graph_position_repository_test.dart (copy the temp-file real-onUpgrade restart shape: setPosition on db1 → close → re-open db2 over the same store → getPosition returns the persisted clearedCompetencies/clearedTiers/currentExerciseId; mirrors the Phase-09 persisted-cooldown shape). test/features/letter_unit/dynamic_selection_test.dart (the baa unit driven by the ExerciseSelector, NOT the fixed _section switch: a fail re-surfaces a remediation exercise, not the next linear section). All RED.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/curriculum/ test/data/graph_position_repository_test.dart test/features/letter_unit/dynamic_selection_test.dart 2>&1 | tail -20; echo "EXPECT: compile/missing-symbol failures (RED by design)"</automated>
  </verify>
  <acceptance_criteria>
    - All five Dart test files exist: test/curriculum/{curriculum_graph_test, curriculum_graph_walker_test, mastery_condition_test}.dart, test/data/graph_position_repository_test.dart, test/features/letter_unit/dynamic_selection_test.dart.
    - Each imports a not-yet-existing symbol (CurriculumGraph / CurriculumGraphWalker / isMasteryMet / the LetterGraphPosition repo / the dynamic-selection wiring) and FAILS to compile or fails RED.
    - Each test names the exact observable behavior from 15-VALIDATION.md: walker advances on pass / steps one tier down on fail (never the old linear order); mastery true only at mom's reps on essential nodes; position survives a simulated restart; a fail re-surfaces a remediation, not the next linear section.
    - No Dart test in this set is GREEN.
  </acceptance_criteria>
  <done>All five Dart test files exist and FAIL to compile / fail (RED) because lib/curriculum/* , the LetterGraphPosition repo, and the dynamic-selection wiring do not exist yet. Each test names the observable behavior from 15-VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo author → committed data asset | The drafted curriculum graph is NEW pedagogy authored by a model; must not reach a child as if signed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-S | Tampering/Integrity | assets/curriculum/curriculum_graph.json | mitigate | Asset ships `signedOff: false` (PROVISIONAL, D-05/Pitfall 4); the human-verify checkpoint (15-07) is the ONLY place the flag flips. Code/tests may exercise the provisional graph; the demo/ship path gates on the flag. |
| T-15-01-D11 | Integrity (pedagogy scope) | curriculum_graph.json node set | accept | Graph reinforces baa's own dot only; no ت/ث nodes (D-11). Verified by the node id set being baa.* only. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy Audit: N/A). If any plan unexpectedly proposes a dependency, gate behind a blocking-human checkpoint. |
</threat_model>

<verification>
- `python3 -c` graph-shape assertion passes (Task 1).
- `cd server && uv run pytest tests/test_plan_graph.py tests/test_faithfulness.py -q` collects and FAILS RED (Task 2).
- `flutter test test/curriculum/ test/data/graph_position_repository_test.dart test/features/letter_unit/dynamic_selection_test.dart` FAILS RED (Task 3).
- No test is green — green tests in Wave 0 mean the contract is too weak.
</verification>

<success_criteria>
- The provisional graph asset exists with `signedOff:false`, baa-only nodes, the three-tier ramp, and an essential/enrichment split.
- The sign-off sheet drafts the full per-node mapping with three tier-level sign-off questions.
- Every Phase-15 requirement (DYN-01, DYN-02, GROUND-03) has at least one RED automated test naming the exact behavior from 15-VALIDATION.md.
- All new tests are RED by missing symbol / missing graph behavior — none accidentally green.
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-01-SUMMARY.md` when done.
</output>
</output>
