---
phase: 15-build-dynamic-grounded-exercise-selection-on-baa
plan: 03
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - lib/curriculum/curriculum_graph.dart
  - lib/curriculum/curriculum_graph_walker.dart
  - lib/curriculum/mastery_condition.dart
  - test/tutor/durable_layers_no_agent_imports_test.dart
autonomous: true
requirements: [DYN-01, DYN-02]
tags: [curriculum, tutor, offline]

must_haves:
  truths:
    - "A pure-Dart CurriculumGraph parses the asset and answers nextForward / remediateOneTier / essentialNodes"
    - "The offline walker advances the chain on a pass and remediates one tier down on a fail (same adaptivity as online)"
    - "On a fail the walker NEVER reverts to the old fixed linear section sequence"
    - "isMasteryMet is true only when every essential node meets mom's clean-reps; enrichment never gates the star"
    - "lib/curriculum/ carries zero cloud-AI / Firebase / Flutter-render imports"
  artifacts:
    - path: "lib/curriculum/curriculum_graph.dart"
      provides: "Pure-Dart graph parser + nextForward/remediateOneTier/essentialNodes/tierOf"
      min_lines: 40
    - path: "lib/curriculum/curriculum_graph_walker.dart"
      provides: "ExerciseSelector seam + CurriculumGraphWalker deterministic offline walk"
      contains: "implements ExerciseSelector"
    - path: "lib/curriculum/mastery_condition.dart"
      provides: "Deterministic on-device mastery evaluator (70/30, mom's clean-reps)"
      contains: "isMasteryMet"
  key_links:
    - from: "lib/curriculum/curriculum_graph_walker.dart"
      to: "lib/curriculum/curriculum_graph.dart"
      via: "walker reads the graph for forward/remediation moves"
      pattern: "CurriculumGraph"
    - from: "lib/curriculum/curriculum_graph.dart"
      to: "assets/curriculum/curriculum_graph.json"
      via: "asset parse via the curriculum-loader path"
      pattern: "curriculum_graph"
---

<objective>
The offline-parity slice (D-09) and the on-device mastery condition (D-06) — both pure Dart, no cloud imports. A `CurriculumGraph` parses the single-source asset; an `ExerciseSelector` seam with a `CurriculumGraphWalker` drives the SAME graph deterministically (advance on pass, remediate one tier down on fail); and `isMasteryMet` evaluates the essential-core 70/30 condition the scorer-owned star gates on. This is the clearest demoable MVP slice: a baa fail re-surfaces an easier exercise via the walker — backward remediation working end-to-end OFFLINE, with zero model.

Purpose: DYN-01 (D-01: the SAME curriculum graph rails selection offline — forward on pass, one tier down on fail; D-04: the Dart walker reads the single-source asset directly) — "responds to mistakes rather than a fixed order" satisfied offline by backward remediation (D-03/D-09); DYN-02's "one quiet star at mastery" gated by the deterministic on-device condition (D-06, ADR-014 invariant). The Drift wiring (15-04) and the screen integration (15-05) consume these pure layers.
Output: lib/curriculum/{curriculum_graph, curriculum_graph_walker, mastery_condition}.dart turning the 15-01 RED Dart unit tests green; the no-agent-imports guard extended to cover lib/curriculum/.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-CONTEXT.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-RESEARCH.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-PATTERNS.md
@.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-Dart CurriculumGraph parser + the ExerciseSelector/CurriculumGraphWalker seam</name>
  <files>lib/curriculum/curriculum_graph.dart, lib/curriculum/curriculum_graph_walker.dart</files>
  <behavior>
    - CurriculumGraph.fromJson(map) parses competencies/tiers/nodes; exposes essentialNodes (essential==true), tierOf(exerciseId), signedOff, nextForward(position) (next reachable node along the prereq chain), remediateOneTier(competency, position) (one tier down within the same competency: ghayrManzur→manzur→manqul; null at the manqul floor)
    - CurriculumGraphWalker.selectNext(facts, position): facts.passed → graph.nextForward(position); else → graph.remediateOneTier(facts.section, position) ?? position.currentExerciseId (drill in place at the floor) — NEVER the old linear sequence (Pitfall 5)
  </behavior>
  <read_first>
    - lib/tutor/tutor_brain.dart (the seam declaration lines ~18-20 — one method, FACTS-in to decision-out, "pure Dart, no cloud-AI imports" — the shape to mirror for ExerciseSelector)
    - lib/tutor/authored_fallback_brain.dart (the pure-Dart deterministic impl behind a seam, lines ~19-53 — the shape CurriculumGraphWalker copies)
    - lib/tutor/tutor_decision.dart (TutorPlan{nextExerciseId,intent,rationale} lines 36-47; TutorDecision.plan — online selection reads decision.plan.nextExerciseId; do NOT add a 5th tool)
    - lib/tutor/tutor_facts.dart (TutorFacts.passed/section/struggleTags — the facts the walker reads)
    - assets/curriculum/curriculum_graph.json (the asset shape authored in 15-01)
    - 15-PATTERNS.md "lib/curriculum/…" section (the seam + impl shapes) ; 15-RESEARCH.md § Pattern 4 + § "The deterministic graph walk"
  </read_first>
  <action>
    Create lib/curriculum/curriculum_graph.dart as a pure-Dart parser (defensive fromJson idiom like tutor_facts.dart — whitelisted fields, no Flutter render import, no cloud import). Model a `GraphNode` (exerciseId, competency, tier?, minCleanReps, essential), the competency prereq chain, and the tier order [manqul, manzur, ghayrManzur]. Expose `essentialNodes`, `tierOf(id)`, `signedOff`, `nextForward(GraphPosition)` (the next reachable node respecting prereqs/tier), and `remediateOneTier(competency, position)` (drop one tier within the same competency; null at the manqul floor). Define a small `GraphPosition` value type (currentExerciseId, clearedCompetencies, clearedTiers) here or in a shared spot 15-04 reuses. Create lib/curriculum/curriculum_graph_walker.dart declaring `abstract class ExerciseSelector { String? selectNext(TutorFacts facts, GraphPosition position); }` and `class CurriculumGraphWalker implements ExerciseSelector` whose selectNext advances on a pass and remediates one tier down on a fail (Pitfall 5: never the fixed linear sequence; at the manqul floor re-present in place). Pure deterministic — no LLM, no network. Load the asset via the existing rootBundle curriculum-loader path (where CurriculumRepository reads assets/curriculum/*.json) — keep the parser itself free of rootBundle so it stays unit-testable from a Map.
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/curriculum/curriculum_graph_test.dart test/curriculum/curriculum_graph_walker_test.dart</automated>
  </verify>
  <done>curriculum_graph_test + curriculum_graph_walker_test GREEN: parse exposes essentialNodes/tierOf/nextForward/remediateOneTier; the walker advances on pass and steps one tier down on fail (ghayrManzur→manzur), drilling in place at manqul, never the old linear order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: On-device mastery condition (D-06) + extend the no-agent-imports guard</name>
  <files>lib/curriculum/mastery_condition.dart, test/tutor/durable_layers_no_agent_imports_test.dart</files>
  <behavior>
    - isMasteryMet(graph, cleanRepsByExercise) returns true iff every essential node's cleanReps >= its minCleanReps; enrichment nodes never gate the star; returns false when any essential node is below its reps (a clicked-through unit) — Pitfall 2
    - lib/curriculum/ files contain no cloud-AI / Firebase / network / Flutter-render imports
  </behavior>
  <read_first>
    - lib/tutor/tutor_facts_builder.dart (the deterministic pure-fn idiom `_deriveStrengthTags`/`_deriveStruggleTags` lines ~47-80 — the model for a pure no-PII evaluator)
    - lib/features/letter_unit/letter_unit_controller.dart (the `_onEnterSection` mastery-on-navigation bug lines ~109-129 writing cleanReps:0 — what isMasteryMet REPLACES; do not edit it here, 15-05 does)
    - test/tutor/durable_layers_no_agent_imports_test.dart (the existing import-purity guard to extend to lib/curriculum/)
    - 15-PATTERNS.md "mastery_condition.dart" pattern + "Pure-Dart, no-cloud-import seam impls" shared pattern ; 15-RESEARCH.md § Pattern 6
  </read_first>
  <action>
    Create lib/curriculum/mastery_condition.dart with `bool isMasteryMet(CurriculumGraph g, Map<String,int> cleanRepsByExercise)` iterating g.essentialNodes only (70/30 split — enrichment never gates), returning false the moment any essential node is below its minCleanReps. Pure Dart, no PII, no server trust (ADR-014 / D-06: the agent's intent:"advance" is a SUGGESTION; the star is computed on-device from Drift clean-reps — 15-04 supplies the per-exercise counts, 15-05 wires the recordMastery gate). Extend test/tutor/durable_layers_no_agent_imports_test.dart so it also asserts every file under lib/curriculum/ carries no cloud-AI/Firebase/network/Flutter-render import (the same regex/scan it already runs over lib/tutor/).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/curriculum/mastery_condition_test.dart test/tutor/durable_layers_no_agent_imports_test.dart</automated>
  </verify>
  <done>mastery_condition_test GREEN: star condition true only when every essential node meets mom's reps, false on an under-rep clicked-through unit, enrichment irrelevant. The no-agent-imports guard passes over lib/curriculum/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → on-device star | The agent's advance suggestion is untrusted; the star is computed on-device only |
| durable layer → cloud/agent code | lib/curriculum/ must stay free of agent/cloud imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-E | Elevation/Tampering | Mastery granted off a server CoachOut | mitigate | isMasteryMet is pure on-device over Drift clean-reps on essential nodes only; never reads a server response (ADR-014, D-06). Star write (15-05) gates strictly on this. |
| T-15-03-T | Tampering | Offline fallback silently linear (D-09 regression) | mitigate | The walker drives the same graph adaptively (advance/remediate); the walker test asserts a fail produces a remediation, not the next linear section (Pitfall 5) |
| T-15-03-ID | Information Disclosure | Curriculum layer leaks PII | mitigate | lib/curriculum/ carries only tier/competency/exercise ids; the no-agent-imports guard is extended to cover it |
| T-15-SC | Tampering | pub installs | mitigate | No packages installed this phase (RESEARCH § Package Legitimacy: N/A) |
</threat_model>

<verification>
- `flutter test test/curriculum/` GREEN (graph + walker + mastery).
- `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` GREEN over lib/curriculum/.
- Per-wave merge: `flutter test` full client suite green.
</verification>

<success_criteria>
- A pure-Dart graph + deterministic walker drive the same graph offline (advance/remediate); a fail yields a remediation, never the old linear order (D-09, Pitfall 5).
- isMasteryMet gates the star on the essential 70/30 core at mom's clean-reps; enrichment never gates (D-06).
- lib/curriculum/ is import-pure (no cloud/agent/render imports).
</success_criteria>

<output>
Create `.planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-03-SUMMARY.md` when done.
</output>
