---
phase: 07-learning-engine-letter-unit-built-to-the-claude-design-proto
plan: 06
type: execute
wave: 4
depends_on: [07-04, 07-05]
files_modified:
  - lib/features/letter_unit/sections/words_section.dart
  - lib/features/letter_unit/sections/listen_write_section.dart
  - lib/features/letter_unit/sections/mastery_section.dart
  - lib/features/letter_unit/letter_unit_screen.dart
  - lib/features/letter_unit/letter_unit_controller.dart
  - lib/router/app_router.dart
  - lib/l10n/app_en.arb
  - test/features/letter_unit/letter_unit_screen_test.dart
  - test/features/letter_unit/words_section_test.dart
  - test/features/letter_unit/listen_write_section_test.dart
  - test/router/letter_unit_route_test.dart
autonomous: true
requirements: [CUR-01, S1-06]
user_setup: []

must_haves:
  truths:
    - "The Words section shows three baa vocab cards (door/duck/milk), each plays its word audio offline and opens a trace surface."
    - "The Listen & Write section is the recall gate: no dotted guide — listen, then write the word/first-letter from memory, scored on-device with authored feedback."
    - "The Mastery section shows ONE quiet star + the mascot + a warm line (no totals/streaks/badges); 'See journey' and 'Next letter' wire to existing nav."
    - "The LetterUnit shell sequences the 6 sections with the R→L ProgressRibbon app bar; home and the journey map deep-link into the resume-aware unit."
  artifacts:
    - path: "lib/features/letter_unit/letter_unit_screen.dart"
      provides: "The 6-section unit shell (app bar + ribbon + section host), resume-aware"
      contains: "LetterUnitScreen"
    - path: "lib/features/letter_unit/sections/listen_write_section.dart"
      provides: "Section 5 — recall gate (write from memory, no guide)"
      contains: "ListenWriteSection"
    - path: "lib/features/letter_unit/sections/mastery_section.dart"
      provides: "Section 6 — one quiet star celebration, anti-gamification"
      contains: "MasterySection"
  key_links:
    - from: "lib/router/app_router.dart"
      to: "lib/features/letter_unit/letter_unit_screen.dart"
      via: "a /unit?letter= route the home + journey deep-link into"
      pattern: "LetterUnitScreen"
    - from: "lib/features/letter_unit/letter_unit_screen.dart"
      to: "lib/data/curriculum_repository.dart"
      via: "getUnit(letterId) drives the section order + ProgressRibbon"
      pattern: "getUnit"
    - from: "lib/features/letter_unit/sections/words_section.dart"
      to: "lib/providers/audio_providers.dart"
      via: "each word card plays its word audio offline"
      pattern: "audioPlayerProvider"
---

<objective>
Complete the baa Letter Unit: the back-half sections — **Words** (Section 4 — three baa vocab cards,
each plays its word clip and opens a trace surface), **Listen & Write** (Section 5 — the recall gate:
no dotted guide, write the word/first-letter from memory, scored), and **Mastery** (Section 6 — ONE
quiet star celebration, no totals) — plus the **LetterUnit shell** that sequences all 6 sections
behind the R→L ProgressRibbon app bar, and the **deep-link/resume wiring** so home and the journey map
open the resume-aware unit (reusing existing nav). Pixel-faithful to the prototype.

Purpose: close the end-to-end baa experience — Meet→Watch→Forms→Words→Listen&Write→Mastery — reachable
from the journey map and home, resuming where the child left off. Output: 3 section widgets, the unit
shell + controller, the `/unit?letter=` route + home/journey deep-links, ARB copy, and tests (CUR-01
engine complete; S1-06 word audio).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design/prototypes/letter-unit-baa/SCHEMA-BINDINGS.md
@.planning/phases/07-learning-engine-letter-unit-built-to-the-claude-design-proto/07-04-SUMMARY.md
@.planning/phases/07-learning-engine-letter-unit-built-to-the-claude-design-proto/07-05-SUMMARY.md
@lib/router/app_router.dart
@lib/features/practice/widgets/mastery_celebration.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Words (Section 4) + Listen & Write (Section 5) + Mastery (Section 6)</name>
  <files>lib/features/letter_unit/sections/words_section.dart, lib/features/letter_unit/sections/listen_write_section.dart, lib/features/letter_unit/sections/mastery_section.dart, lib/l10n/app_en.arb, test/features/letter_unit/words_section_test.dart, test/features/letter_unit/listen_write_section_test.dart</files>
  <read_first>
    - docs/design/prototypes/letter-unit-baa/prototype/letter-unit/unit.js (the `words()` grid + `traceWord()`, the `listenWrite()` recall gate with the word/first-letter toggle + the big Play, the `mastery()` one-star celebration with the gold glow/sparks — reproduce all three 1:1)
    - docs/design/prototypes/letter-unit-baa/prototype/letter-unit/unit.css (.words/.wordgrid/.wordcard/.lw/.lw-prompt/.lw-card/.bigplay/.noguide/.mastery/.glow/.star-big/.spark — exact styling)
    - docs/design/prototypes/letter-unit-baa/EXERCISE-CONFIGS.json (baa.writeWord.* / baa.connectWord.* / baa.writeLetter.fromSound — the Words + Listen&Write configs; feedback lines)
    - assets/curriculum/words.json (the baa vocab from 07-01 — door/duck/milk, audio ids)
    - lib/features/practice/widgets/mastery_celebration.dart (the EXISTING one-quiet-star celebration to REUSE for Section 6 — masteredLetterId/glyph/letterName/onBackHome/onNextLesson; do not rebuild)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart + write_surface.dart (the write-mode WriteSurface for Listen&Write recall gate)
    - lib/providers/audio_providers.dart (offline word audio)
    - CLAUDE.md Decided (anti-gamification: ONE star, no totals/streaks/badges) + MEMORY design-predates-antigamification (omit any prototype counter)
  </read_first>
  <behavior>
    - Test: WordsSection renders three vocab cards (door/duck/milk) from words.json; tapping a card's Play plays its word audio id offline; tapping a card opens a trace surface for that word.
    - Test: ListenWriteSection is mode:write (NO dotted guide / no guide glyph — the "noguide / from memory" badge is shown); the word/first-letter toggle switches the config; a scored pass shows the authored praise, a fail the authored fix; finishing requires the word task.
    - Test: MasterySection shows exactly ONE star (reusing MasteryCelebration) + the mascot + the warm line; NO running total, NO weekly tally, NO streak, NO badge; "See journey" and "Next letter" CTAs are present and wired to callbacks.
  </behavior>
  <action>
    Build `lib/features/letter_unit/sections/words_section.dart` (`WordsSection`) reproducing the prototype `words()` grid: three `.wordcard`s from `words.json` (image stub, Arabic word with the baa highlighted teal, rom/en, Play), each card opening a trace WriteSurface (`traceWord`) fed the matching writeWord/connectWord config; Play plays the word's audio id offline. Build `lib/features/letter_unit/sections/listen_write_section.dart` (`ListenWriteSection`) reproducing `listenWrite()`: the recall gate — a write-mode WriteSurface (the `.noguide` "from memory" badge, NO guide glyph), the word↔first-letter toggle swapping between `baa.writeWord.dictation` and `baa.writeLetter.fromSound` configs, the big Play, scored pass/fail from the configs' authored feedback. Build `lib/features/letter_unit/sections/mastery_section.dart` (`MasterySection`) REUSING the existing `MasteryCelebration` widget for the one-quiet-star + mascot + warm line; "See journey" → onSeeJourney, "Next letter" → onNext. OMIT any counter/tally the prototype `mastery()` might imply — ONE star only (CLAUDE.md Decided; MEMORY note). Add ARB copy; gen-l10n. Write the Words + Listen&Write widget tests (Mastery coverage folds into the unit-screen test in Task 2).
  </action>
  <verify>
    <automated>flutter gen-l10n &amp;&amp; flutter test test/features/letter_unit/words_section_test.dart test/features/letter_unit/listen_write_section_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/letter_unit/words_section_test.dart test/features/letter_unit/listen_write_section_test.dart` passes.
    - WordsSection test asserts 3 vocab cards and that a card Play calls the audio player with the word's audio id.
    - ListenWriteSection test asserts NO dotted-guide glyph is shown (write-mode / recall gate) and that the word/first-letter toggle swaps the active config.
    - `grep -q "MasteryCelebration" lib/features/letter_unit/sections/mastery_section.dart` succeeds (reuses the existing celebration).
    - `grep -rqiE "weekly|tally|streak|\\+[0-9]+ today|badge|running total" lib/features/letter_unit/sections/mastery_section.dart` returns nothing (anti-gamification — one star only).
  </acceptance_criteria>
  <done>Words, Listen&Write, and Mastery sections render config-driven with offline audio and a single quiet star; the recall gate writes from memory.</done>
</task>

<task type="auto">
  <name>Task 2: LetterUnit shell (app bar + R→L ribbon + section sequencing, resume-aware) + /unit route + home/journey deep-links</name>
  <files>lib/features/letter_unit/letter_unit_screen.dart, lib/features/letter_unit/letter_unit_controller.dart, lib/router/app_router.dart, lib/l10n/app_en.arb, test/features/letter_unit/letter_unit_screen_test.dart, test/router/letter_unit_route_test.dart</files>
  <read_first>
    - docs/design/prototypes/letter-unit-baa/prototype/letter-unit/index.html (the unit shell: appbar with back/close + the R→L ribbon + the kid chip; the body = tutor column + activity column — reproduce the shell 1:1)
    - docs/design/prototypes/letter-unit-baa/prototype/letter-unit/unit.js (the `go(n)` section nav + `buildRibbon()` R→L ribbon + the `SECTIONS` order + the back/close handlers + `visited` resume tracking)
    - docs/design/prototypes/letter-unit-baa/prototype/letter-unit/unit.css (.appbar/.ribbon/.dot/.unitwrap/.body/.tutor/.activity — the shell layout)
    - lib/router/app_router.dart (the existing GoRouter — the `/practice?lesson=` deep-link pattern + ValueKey-per-id + query-param validation T-06-03 to mirror for `/unit?letter=`; keep the onboarding/parent gates intact)
    - lib/screens/home_screen.dart (the today-card `/practice?lesson=` nav around line 627/682/706 — add/redirect the baa lesson to open the unit; do NOT break the alif lesson path)
    - lib/features/journey/journey_screen.dart (the node-tap `/practice?lesson=` nav around line 414 — route baa's node into the unit)
    - lib/data/curriculum_repository.dart (getUnit(letterId) → the 6 ordered sections from 07-01)
    - .planning/phases/07-05-...SUMMARY + 07-06 Task 1 (the 6 section widget constructor APIs to sequence)
  </read_first>
  <action>
    Build `lib/features/letter_unit/letter_unit_screen.dart` (`LetterUnitScreen extends ConsumerWidget`, takes `letterId` + optional `resumeSection`): reproduce the prototype unit shell 1:1 — the app bar (back/close + the R→L `.ribbon` of 6 dots reusing ProgressRibbon + the kid chip) and the two-column body (tutor column + activity column) — and host the current section widget. Load the section order from `getUnit(letterId)`; render the matching section (MeetSection/WatchTraceSection/FormsSection/WordsSection/ListenWriteSection/MasterySection) and advance via the controller. Build `lib/features/letter_unit/letter_unit_controller.dart` (a Riverpod Notifier — NO BLoC/GetX) holding the current section index + `visited` set for resume; on Mastery, mark the letter mastered via the existing ProgressRepository seam and persist resume position so re-entry resumes (resume-aware). In `lib/router/app_router.dart` add a `GoRoute(path: '/unit')` reading `?letter=` (validate against the curriculum catalog, ValueKey per id, degrade unknown→today — mirror the `/practice` T-06-03 pattern); keep the onboarding + parent gates untouched. Wire the deep-links: in `home_screen.dart` route the baa today-card to `/unit?letter=baa` (leave alif's `/practice` path working — baa opens the unit, alif keeps the thin loop unless redirected; document the choice); in `journey_screen.dart` route baa's node tap to `/unit?letter=baa`. Add ARB copy; gen-l10n. Write `letter_unit_screen_test.dart` (the shell renders 6 ribbon dots R→L, sequences sections, Mastery shows one star) and `letter_unit_route_test.dart` (`/unit?letter=baa` builds LetterUnitScreen; unknown letter degrades; the journey/home deep-link targets resolve).
  </action>
  <verify>
    <automated>flutter gen-l10n &amp;&amp; flutter test test/features/letter_unit/letter_unit_screen_test.dart test/router/letter_unit_route_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/letter_unit/letter_unit_screen_test.dart test/router/letter_unit_route_test.dart` passes.
    - The shell test asserts 6 R→L ribbon dots and that advancing moves through all 6 sections to a single-star Mastery.
    - `grep -q "path: '/unit'" lib/router/app_router.dart` succeeds and the route validates `?letter=` (unknown → graceful degrade, no error to the child).
    - `grep -q "/unit?letter=baa" lib/screens/home_screen.dart && grep -q "/unit?letter=baa" lib/features/journey/journey_screen.dart` succeeds (home + journey deep-link into the unit).
    - `grep -rqiE "BlocProvider|GetX|Get\\.put" lib/features/letter_unit/` returns nothing (Riverpod-only).
    - The onboarding + parent route gates are unchanged (`flutter test test/router/` exits 0 — no regression).
  </acceptance_criteria>
  <done>The 6-section baa unit is sequenced behind the R→L ribbon shell, resume-aware, and reachable from home + the journey map; Mastery shows one quiet star.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route query (`?letter=`) → unit | an externally-influenceable deep-link param (route restoration) |
| child progress write (mastery) | the only write — to local Drift, not Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | `/unit?letter=` deep-link | mitigate | The letter param is validated against the curriculum catalog before use; an unknown/missing id degrades silently to today's letter, never an error or arbitrary load (mirror the `/practice` T-06-03 pattern). |
| T-07-06-02 | Information disclosure | mastery progress | mitigate | Mastery writes go to LOCAL Drift only (existing ProgressRepository) — no child data to Firestore (D-11). Resume position is local. |
| T-07-06-03 | Spoofing | anti-gamification | accept | Mastery shows ONE quiet star via the existing celebration; no totals/streaks/badges (grep-guarded). |
| T-07-06-04 | Elevation of privilege | route gates | mitigate | The onboarding + parent gates are left intact; the new `/unit` route does not bypass them (regression-guarded by test/router/). |
| T-07-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. |
</threat_model>

<verification>
- `flutter test test/features/letter_unit/ test/router/` passes.
- `flutter analyze lib/features/letter_unit/ lib/router/app_router.dart` exits 0.
- `<human-check>` On a tablet (landscape RTL): from the journey map, tapping baa opens the unit; the 6-section flow runs Meet→Watch→Forms→Words→Listen&Write→Mastery; the ribbon advances R→L; exiting and re-entering resumes; Mastery shows exactly one quiet star, no counters.
</verification>

<success_criteria>
- The full 6-section baa Letter Unit is reachable, navigable, resume-aware, and pixel-faithful (CUR-01 engine end-to-end; SC#1/#2/#5).
- Word audio plays offline (S1-06); Mastery shows one quiet star (anti-gamification).
- Home and the journey map deep-link into the unit reusing existing nav (SC#5).
</success_criteria>

<output>
Create `.planning/phases/07-learning-engine-letter-unit-built-to-the-claude-design-proto/07-06-SUMMARY.md` when done. Note the `/unit` route + resume contract and confirm SC#1/#2/#5 are met structurally (content sign-off + real per-form strokes land in Plan 07-07).
</output>
