---
phase: 07-learning-engine-letter-unit-built-to-the-claude-design-proto
plan: 07
type: execute
wave: 5
depends_on: [07-06]
files_modified:
  - assets/curriculum/letters.json
  - assets/curriculum/exercises.json
  - assets/curriculum/words.json
  - tools/firebase/seed_firestore.py
  - tools/firebase/export_firestore.py
  - test/curriculum/baa_signoff_test.dart
autonomous: false
requirements: [CUR-01, S1-06]
user_setup: []

must_haves:
  truths:
    - "baa carries authored open-centerline reference strokes for all FOUR contextual forms (isolated, initial, medial, final), each validated by the closed-loop validator."
    - "baa's vocab words (door/duck/milk) and per-letter/per-word pronunciation audio ids resolve to bundled clips that play offline."
    - "Every baa exercise/form is marked signedOff:true ONLY after the owner's mother reviews and signs off — represented as an explicit human gate, never fabricated by an executor."
    - "The signed-off baa Schema v2 content is seeded into Firestore (exercises/words/units + baa forms) and round-trips losslessly with the bundled seed."
  artifacts:
    - path: "assets/curriculum/letters.json"
      provides: "baa with contextualForms.{isolated,initial,medial,final} authored Form objects + signedOff flags"
      contains: "contextualForms"
    - path: "tools/firebase/seed_firestore.py"
      provides: "extended Python seed writing words/exercises/units + baa forms into Firestore"
      contains: "exercises"
    - path: "test/curriculum/baa_signoff_test.dart"
      provides: "load-time guard: all four baa forms valid; signed-off gate honored"
      contains: "baa"
  key_links:
    - from: "assets/curriculum/letters.json"
      to: "lib/core/scoring/stroke_validation.dart"
      via: "the load-time closed-loop validator runs over every authored baa form stroke"
      pattern: "validateReferenceStrokes"
    - from: "tools/firebase/seed_firestore.py"
      to: "assets/curriculum/exercises.json"
      via: "the Python seed reads the signed-off seed JSONs and writes Firestore"
      pattern: "exercises.json"
---

<objective>
Make baa real end-to-end (SC#4): author the open-centerline reference strokes for all FOUR contextual
forms (isolated already exists; add initial/medial/final), finalize the vocab + pronunciation-audio
bindings so they play offline, and flip `signedOff:true` ONLY after the **owner's mother reviews and
signs off** — an explicit human gate, never fabricated. Then seed the signed-off Schema v2 content
(baa forms + words + exercises + unit) into Firestore via the extended Python tooling, round-tripping
losslessly with the bundled seed.

This plan is `autonomous: false`. Per the HARD CONSTRAINT and CLAUDE.md curriculum rule: **never ship a
model-authored letter unsigned.** The model DRAFTS the per-form stroke data, mistakes, and feedback;
the **owner's mother REVIEWS and SIGNS OFF** (decided 2026-06-11). The sign-off is a blocking human
checkpoint — the executor stops and waits; it does not set `signedOff:true` on her behalf.

Purpose: turn the structurally-complete baa unit (Plans 01-06) into authored, signed-off, offline,
audio-backed content. Output: authored baa forms + vocab + audio, the human sign-off gate, the
extended Firestore seed/export, and a load-time validity + sign-off test.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/learning-experience/SCHEMA-V2.md
@docs/design/prototypes/letter-unit-baa/EXERCISE-CONFIGS.json
@docs/design/prototypes/letter-unit-baa/CHANGES.md
@assets/curriculum/letters.json
@tools/firebase/seed_firestore.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: DRAFT baa's four-form reference strokes + vocab + audio bindings (signedOff stays false)</name>
  <files>assets/curriculum/letters.json, assets/curriculum/exercises.json, assets/curriculum/words.json, test/curriculum/baa_signoff_test.dart</files>
  <read_first>
    - assets/curriculum/letters.json (the existing baa entry: 2 reference strokes, isolated only, signedOff:false — extend to contextualForms with all four)
    - lib/models/letter.dart (the Form model + contextualForms field from 07-01 — the shape to author into)
    - lib/core/scoring/stroke_validation.dart (validateReferenceStrokes — the closed-loop/direction/range/order guard every authored stroke must pass; kClosedLoopEpsilon 0.06 from Phase 06-09)
    - .planning/research/STROKE-REFERENCE.md (the open-centerline authoring method — pen-tip travel, not glyph outline; the Phase-02.1 correction that unblocks the scorer + animation)
    - lib/dev/authoring_screen.dart (the in-app trace-over-glyph authoring tool from Phase 02.1 — the intended capture path for real per-form strokes on a tablet)
    - docs/design/prototypes/letter-unit-baa/EXERCISE-CONFIGS.json (the feedback lines per exercise — prototype-authored DRAFTS to refine; keep signedOff:false)
    - .planning/research/learning-experience/SCHEMA-V2.md (§2 Form: referenceStrokes + commonMistakes + tolerances per form)
    - MEMORY curriculum-drafting-strategy (model DRAFTS, owner's mother REVIEWS + signs off; 04-06 tablet calibration stays human+device)
  </read_first>
  <action>
    In `assets/curriculum/letters.json`, extend the baa entry to `contextualForms` with four `Form` objects: `isolated` (port the existing 2-stroke boat+dot), and DRAFT `initial`/`medial`/`final` open-centerline reference strokes (normalized 0..1, ordered, typed line/curve/dot, with `direction` matching point order — NOT glyph outlines). Draft each form's `commonMistakes` (3-4 named checks → child-friendly fix lines, tutor's voice) and per-form `tolerances` (start from `Tolerances.normal`). Refine the prototype-authored `feedback` lines in `assets/curriculum/exercises.json` for accuracy. Finalize `assets/curriculum/words.json` baa vocab (باب/بطة/حليب — text, audio id, image id, gloss.en, letters[]). KEEP every `signedOff:false` — this task only DRAFTS; the human sign-off is Task 2. Where real per-form strokes require a tablet capture (the authoring tool is device-gated, like 04-06), draft a best-effort centerline AND note in the SUMMARY that it needs tablet re-capture at sign-off. Write `test/curriculum/baa_signoff_test.dart`: assert all four baa forms' reference strokes PASS `validateReferenceStrokes` (no closed loops, valid directions/ranges/order); assert each form has 3-4 commonMistakes; assert the test is currently RED on the sign-off assertion (baa not yet signedOff) so Task 2's human gate is provably required.
  </action>
  <verify>
    <automated>flutter test test/curriculum/baa_signoff_test.dart --plain-name "reference strokes valid" &amp;&amp; flutter test test/data/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "contextualForms" assets/curriculum/letters.json` succeeds and the baa entry has all four form keys (isolated/initial/medial/final).
    - `python3 -c "import json,sys; d=json.load(open('assets/curriculum/letters.json')); baa=[l for l in d['letters'] if l['id']=='baa'][0]; cf=baa.get('contextualForms',{}); sys.exit(0 if all(cf.get(f) for f in ['isolated','initial','medial','final']) else 1)"` exits 0.
    - The validity test asserts every baa form passes `validateReferenceStrokes` (no closed loops — load-time guard green).
    - The sign-off assertion is RED (baa.signedOff still false) — proving Task 2's human gate is required, not bypassed.
    - `flutter test test/data/` passes (the extended letters.json still loads through the repository validator).
    - `grep -rqiE "v1|simplified|static for now|placeholder for now" assets/curriculum/letters.json` returns nothing (drafts are real strokes, not stubs).
  </acceptance_criteria>
  <done>baa has DRAFT open-centerline strokes for all four forms (validator-clean), vocab + audio bindings finalized, signedOff still false pending human review.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Owner's-mother sign-off gate — review + approve baa forms/vocab/audio</name>
  <what-built>The model drafted baa's four contextual-form reference strokes, common mistakes, feedback lines, vocab, and pronunciation-audio bindings. All are `signedOff:false`. The pronunciation clips are an owner deliverable. The strokes may need a real-tablet re-capture via the in-app authoring tool (`/dev/authoring`).</what-built>
  <how-to-verify>
    1. With the owner's mother, review each baa form (isolated/initial/medial/final): is the stroke order, direction, and shape correct for how SHE teaches it? Use the in-app authoring tool on a tablet to re-capture any form whose drafted centerline is wrong (capture in her prescribed order/direction over the faint Noto Naskh glyph; export normalized strokes).
    2. Review the 3-4 common mistakes per form and their child-friendly fix lines — do they name the real mistakes she sees, in the tutor's warm specific voice?
    3. Review the vocab (door/duck/milk) and confirm the pronunciation recordings (letter + words) — supply the real clips into `assets/audio/` (replacing the placeholders from Plan 07-02) so they play offline.
    4. Only when she approves a form/exercise, set its `signedOff:true` (and baa's letter-level `signedOff:true` once all forms + exercises are approved).
  </how-to-verify>
  <action>This is a blocking human checkpoint (gate="blocking-human"). The executor presents what-built, performs the how-to-verify steps WITH the human, and HALTS until the resume-signal is given. The executor does not proceed autonomously past this gate.</action>
  <resume-signal>Type "signed off: baa" once the owner's mother has reviewed and approved all four forms + vocab + audio and the signedOff flags are set true (per-exercise and letter-level). Or list which forms/exercises remain pending.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Seed signed-off baa Schema v2 into Firestore (extend Python seed/export) + sign-off regression test</name>
  <files>tools/firebase/seed_firestore.py, tools/firebase/export_firestore.py, test/curriculum/baa_signoff_test.dart</files>
  <read_first>
    - tools/firebase/seed_firestore.py (the existing letters/lessons/meta seed + the point_codec usage — extend to write words/exercises/units + baa contextualForms, idempotent .set(), Admin SDK bypasses rules)
    - tools/firebase/export_firestore.py (the inverse export — extend symmetrically so the round-trip stays lossless; refreshes the bundled snapshot)
    - tools/firebase/point_codec.py (the {x,y}⇄[x,y] codec for nested point arrays — reuse for the new forms' strokes)
    - assets/curriculum/exercises.json + words.json + units.json (the signed-off seed sources from Tasks 1-2)
    - .planning/phases/06.1-firebase-curriculum-backend/06.1-03-SUMMARY.md (the seed/export round-trip + SA-key/region checkpoint pattern to mirror)
    - .planning/research/learning-experience/SCHEMA-V2.md (§4 Firestore mapping: exercises/{id}, words/{id}, units/{letterId}, letters adds contextualForms)
  </read_first>
  <action>
    Extend `tools/firebase/seed_firestore.py` to ALSO write the `exercises`, `words`, and `units` collections (doc-per-entry, idempotent `.set()`) and to include baa's `contextualForms` under the `letters/baa` doc (re-shaping each form's stroke points through `point_codec.encode` — Firestore forbids arrays-of-arrays). Extend `tools/firebase/export_firestore.py` with the exact inverse so the round-trip is lossless and the bundled snapshot refreshes. Add a Python self-check (or extend the existing one) asserting exercises/words/units/baa-forms round-trip identically. In `test/curriculum/baa_signoff_test.dart`, make the sign-off assertion the REGRESSION gate: after the human sign-off (Task 2), `baa.signedOff == true` and every baa exercise `signedOff == true` — this test now passes and freezes baa as signed-off curriculum. Do NOT run the actual Firestore seed if Firebase auth/SA-key is unavailable — note it as a human-gated deploy step (mirror 06.1-03's SA-key checkpoint); the code + self-check are the autonomous deliverable.
  </action>
  <verify>
    <automated>python3 tools/firebase/seed_firestore.py --self-check &amp;&amp; flutter test test/curriculum/baa_signoff_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "exercises" tools/firebase/seed_firestore.py && grep -q "units" tools/firebase/seed_firestore.py && grep -q "words" tools/firebase/seed_firestore.py` succeeds (seed extended to the new collections).
    - The Python self-check asserts exercises/words/units + baa contextualForms round-trip losslessly (seed→export identity).
    - After sign-off, `flutter test test/curriculum/baa_signoff_test.dart` passes including the `baa.signedOff == true` regression assertion.
    - `export_firestore.py` produces the same JSON shape the bundled seed loads (round-trip parity — no field lost).
    - The SUMMARY notes whether the live Firestore seed ran or is human-gated on the SA-key/region (like 06.1-03).
  </acceptance_criteria>
  <done>Signed-off baa Schema v2 (forms + words + exercises + unit) seeds into Firestore and round-trips losslessly; the sign-off regression test freezes baa as approved curriculum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → load-time validator | drafted reference strokes must pass the closed-loop guard before they can score |
| admin SDK → Firestore (seed) | the Python seed writes curriculum (bypasses client rules via the service account) |
| sign-off flag → child | `signedOff:true` is the gate that lets content reach the child |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | authored strokes | mitigate | Every drafted form stroke passes `validateReferenceStrokes` at load time (closed-loop/direction/range/order); an outline can never reach the scorer (Phase 02.1/06-09 guard preserved). |
| T-07-07-02 | Elevation of privilege | sign-off gate | mitigate | `signedOff:true` is set ONLY by the human (owner's mother) checkpoint — the executor never flips it. A blocking-human gate; `workflow.auto_advance` is ignored. The sign-off regression test freezes it. |
| T-07-07-03 | Tampering | admin SA-key | mitigate | The service-account key is gitignored (06.1 pattern); the seed bypasses rules only via the trusted SA. Live seed is human-gated on the key — never committed. |
| T-07-07-04 | Information disclosure | curriculum content | mitigate | Seed writes curriculum only (forms/vocab/exercises) — no child PII/progress to Firestore (D-11). |
| T-07-07-SC | Tampering | pip installs | mitigate | No new package installs (extends the existing firebase-admin tooling). |
</threat_model>

<verification>
- `flutter test test/curriculum/baa_signoff_test.dart` passes (validity always; sign-off regression after the human gate).
- `python3 tools/firebase/seed_firestore.py --self-check` passes (round-trip parity).
- `flutter test test/data/` passes (extended letters.json loads through the repository validator).
- `<human-check>` (end-of-phase, the phase-7 owner's-mother review): all four baa forms trace + score correctly on a tablet, the words + audio play offline, and the unit ends in one quiet star — baa is end-to-end real and signed off.
</verification>

<success_criteria>
- baa is end-to-end real: all four contextual forms authored + owner's-mother signed off, vocab + audio play offline, on-device scoring + authored feedback, one quiet star (SC#4 / CUR-01 / S1-06).
- The sign-off is a human gate, never fabricated; signed-off content seeds into Firestore losslessly.
</success_criteria>

<output>
Create `.planning/phases/07-learning-engine-letter-unit-built-to-the-claude-design-proto/07-07-SUMMARY.md` when done. Record which baa forms were tablet-re-captured vs drafted, the sign-off status (and any pending forms/exercises), whether the live Firestore seed ran or is SA-key-gated, and confirm SC#4 is met.
</output>
