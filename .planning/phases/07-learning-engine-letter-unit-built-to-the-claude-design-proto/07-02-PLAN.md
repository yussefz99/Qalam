---
phase: 07-learning-engine-letter-unit-built-to-the-claude-design-proto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/providers/audio_providers.dart
  - lib/services/asset_audio_player.dart
  - assets/audio/README.md
  - firestore.rules
  - test/services/asset_audio_player_test.dart
  - test/firestore/rules_v2.test.md
autonomous: false
requirements: [S1-06]
user_setup: []

must_haves:
  truths:
    - "A tappable Play button can play a bundled pre-recorded clip per letter and per word, fully offline (no TTS, no network)."
    - "The audio player resolves a placeholder audioId to a bundled asset path and plays it; an unknown/absent id degrades silently (never crashes, never blocks the child)."
    - "Firestore rules allow signed-in reads of words/exercises/units and deny all client writes (curriculum is read-only)."
  artifacts:
    - path: "lib/services/asset_audio_player.dart"
      provides: "AssetLetterAudioPlayer implementing the existing LetterAudioPlayer seam over a real audio package"
      contains: "implements LetterAudioPlayer"
    - path: "assets/audio/README.md"
      provides: "Bundled-clip manifest: audioId → asset path, real-vs-placeholder, owner deliverable note"
      contains: "snd.baa"
    - path: "firestore.rules"
      provides: "words/exercises/units read-requires-auth + write-denied match blocks"
      contains: "match /exercises/"
  key_links:
    - from: "lib/providers/audio_providers.dart"
      to: "lib/services/asset_audio_player.dart"
      via: "audioPlayerProvider returns AssetLetterAudioPlayer (replacing NoopLetterAudioPlayer)"
      pattern: "AssetLetterAudioPlayer"
    - from: "lib/services/asset_audio_player.dart"
      to: "assets/audio/"
      via: "audioId → bundled asset path resolution"
      pattern: "assets/audio"
---

<objective>
Make pronunciation audio real and offline (S1-06). Add a vetted audio package, implement
`AssetLetterAudioPlayer` over the EXISTING `LetterAudioPlayer` seam (Phase 4 left a Noop), bundle
the baa pronunciation clips (letter + the three vocab words), declare `assets/audio/`, and resolve
the placeholder `audioId`s from EXERCISE-CONFIGS.json (`snd.baa`, `word.baab`, `word.batta`,
`word.baab`/`haliib`, sentence ids) to bundled asset paths. Extend `firestore.rules` so the new
Schema v2 collections (words/exercises/units) are read-requires-auth + client-write-denied, matching
the letters/lessons policy.

Purpose: every Play button across the 6 unit sections (Meet, the Listen card, Words, Listen & write)
must play a bundled clip with no network round-trip; and the engine's new Firestore collections must
inherit the locked child-safety read-only posture.
Output: the audio package + legitimacy gate, the AssetLetterAudioPlayer + provider swap, the bundled
clip manifest, and the extended Firestore rules.

This plan is `autonomous: false`: it installs a package (blocking package-legitimacy human checkpoint)
and the real pronunciation recordings are an owner deliverable (placeholder silence ships until the
owner supplies clips — surfaced as a human-action note, never fabricated).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@lib/providers/audio_providers.dart
@firestore.rules
@docs/design/prototypes/letter-unit-baa/EXERCISE-CONFIGS.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate — verify the audio package before install</name>
  <what-built>Selected an audio playback package for offline bundled-clip playback (no TTS). Candidate: `audioplayers` (or `just_audio`) — to be verified for legitimacy before install per the Package Legitimacy Gate.</what-built>
  <how-to-verify>
    1. Confirm the chosen package on pub.dev: verified publisher, recent releases, high likes/pub-points (audioplayers is a long-standing flutter.dev-adjacent community package; just_audio is widely used).
    2. Confirm it supports playing a bundled Flutter asset offline (AssetSource / setAsset) on Android with no network.
    3. Approve the exact version to pin, or name an alternative.
  </how-to-verify>
  <action>This is a blocking human checkpoint (gate="blocking-human"). The executor presents what-built, performs the how-to-verify steps WITH the human, and HALTS until the resume-signal is given. The executor does not proceed autonomously past this gate.</action>
  <resume-signal>Type "approved: &lt;package&gt;@&lt;version&gt;" or name a different package/version.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: AssetLetterAudioPlayer over the LetterAudioPlayer seam + provider swap + bundled clip manifest</name>
  <files>pubspec.yaml, lib/services/asset_audio_player.dart, lib/providers/audio_providers.dart, assets/audio/README.md, test/services/asset_audio_player_test.dart</files>
  <read_first>
    - lib/providers/audio_providers.dart (the EXISTING LetterAudioPlayer interface + NoopLetterAudioPlayer + audioPlayerProvider to replace — keep the interface stable for practice_screen.dart)
    - docs/design/prototypes/letter-unit-baa/EXERCISE-CONFIGS.json (every audioId the player must resolve: snd.baa, word.batta, word.baab, sentence.albaab-kabiir, + the words.json ids)
    - lib/features/practice/practice_screen.dart (the existing audioPlayerProvider consumer + the "Hear the letter" button — confirm the seam shape it calls: playLetter(assetPath))
    - assets/curriculum/words.json (created in 07-01 — the word audio ids to map; if 07-01 not merged, read EXERCISE-CONFIGS.json audio ids)
    - .planning/research/learning-experience/SCHEMA-V2.md (§2 Letter.audio.letterSound + Word.audio = audioId)
  </read_first>
  <action>
    Add the approved audio package to `pubspec.yaml` at the version from the checkpoint. Implement `lib/services/asset_audio_player.dart`: `AssetLetterAudioPlayer implements LetterAudioPlayer` wrapping a single reusable player instance; `playLetter(String audioIdOrPath)` resolves an `audioId` (e.g. `snd.baa`) to a bundled asset path via a small `_audioIdToAsset` map declared from the manifest, plays it from the asset (offline, AssetSource-equivalent), and SWALLOWS errors (unknown id / missing file → no-op, never throws — the child never sees an audio error; mirror the Noop's never-block posture). Replace `audioPlayerProvider` in `lib/providers/audio_providers.dart` to return `AssetLetterAudioPlayer` (keep `LetterAudioPlayer` interface + `NoopLetterAudioPlayer` for tests). Create `assets/audio/README.md` documenting each `audioId` → asset path → real-vs-placeholder status (all PLACEHOLDER until the owner records; `snd.baa`, `word.baab`, `word.batta`, `word.haliib`, `sentence.albaab-kabiir`). Ship silent/empty placeholder clip files OR map unknown ids to a no-op so the build is green without real recordings (document which). Add `assets/audio/` to the pubspec `flutter.assets` list. Write `test/services/asset_audio_player_test.dart` asserting: a known audioId resolves to the expected asset path (test the pure `_audioIdToAsset` resolution, not real playback); an unknown id resolves to null/no-op and `playLetter` never throws.
  </action>
  <verify>
    <automated>flutter test test/services/asset_audio_player_test.dart && flutter analyze lib/services/asset_audio_player.dart lib/providers/audio_providers.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/services/asset_audio_player_test.dart` passes.
    - The test asserts `playLetter('does.not.exist')` completes without throwing (silent degrade).
    - `grep -q "implements LetterAudioPlayer" lib/services/asset_audio_player.dart` succeeds.
    - `grep -q "AssetLetterAudioPlayer" lib/providers/audio_providers.dart` succeeds (provider swapped).
    - `grep -q "assets/audio/" pubspec.yaml` succeeds.
    - `assets/audio/README.md` lists at least `snd.baa`, `word.baab`, `word.batta` with a real-vs-placeholder column.
  </acceptance_criteria>
  <done>A tappable Play resolves an audioId to a bundled clip and plays it offline; unknown ids degrade silently; the seam practice_screen already consumes is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Firestore rules for words/exercises/units (read-requires-auth, writes denied)</name>
  <files>firestore.rules, test/firestore/rules_v2.test.md</files>
  <read_first>
    - firestore.rules (the existing letters/lessons/meta match blocks + the deny-by-default catch-all + the v2 custom-claim seam comment pattern to mirror exactly)
    - .planning/phases/06.1-firebase-curriculum-backend/06.1-05-SUMMARY.md (the D-10/D-11 rules posture + the Rules-Playground verification format to mirror)
    - .planning/research/learning-experience/SCHEMA-V2.md (§4 the new collections: words/{id}, exercises/{id}, units/{letterId})
  </read_first>
  <action>
    In `firestore.rules`, add three `match` blocks — `match /words/{wordId}`, `match /exercises/{exerciseId}`, `match /units/{letterId}` — each with `allow read: if request.auth != null;` and `allow write: if false;`, plus the same commented v2 custom-claim seam (`// allow write: if request.auth.token.role == 'admin';`) used for letters/lessons (D-10). Keep the deny-by-default catch-all LAST and unchanged (D-11 — no child-data path). Do NOT touch the letters/lessons/meta blocks. Update `firestore.rules` header comment to note words/exercises/units inherit the same read-requires-auth + write-denied curriculum policy. Create `test/firestore/rules_v2.test.md` documenting a Rules-Playground verification checklist (signed-in anon read of an exercises doc = ALLOW; unauth read = DENY; any client write to exercises/words/units = DENY; a fictitious child-data path = DENY) as a PENDING server-side human check, mirroring 06.1-05's `rules.test.md`. The deploy itself (`firebase deploy --only firestore:rules`) is the autonomous deliverable; note in the SUMMARY whether the executor could deploy or it is human-gated on Firebase auth.
  </action>
  <verify>
    <automated>grep -c "allow write: if false;" firestore.rules</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "match /exercises/" firestore.rules && grep -q "match /words/" firestore.rules && grep -q "match /units/" firestore.rules` succeeds.
    - `grep -c "allow write: if false;" firestore.rules` returns ≥ 6 (letters, lessons, meta, words, exercises, units).
    - The deny-by-default catch-all `match /{document=**}` remains the LAST match block (`tail` of the rules file still shows `allow read, write: if false;`).
    - `test/firestore/rules_v2.test.md` documents the 4-check Rules-Playground verification (ALLOW signed-in read, DENY unauth read, DENY client write, DENY child-data path).
  </acceptance_criteria>
  <done>words/exercises/units are read-requires-auth + client-write-denied, matching the locked child-safety curriculum posture; child data stays out of Firestore (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | the audio package is third-party code linked into the app |
| Firestore → app (new collections) | words/exercises/units are untrusted-until-validated curriculum content |
| bundled audio asset → speaker | bundled clips ship in the APK (trusted); no untrusted network fetch at trace time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | audio package supply chain | mitigate | Package Legitimacy Gate: blocking human checkpoint verifies the package on pub.dev (verified publisher, version pinned) BEFORE install. |
| T-07-02-02 | Tampering | curriculum collections | mitigate | Firestore rules: words/exercises/units read-requires-auth + `allow write: if false` — content written only via admin SDK (D-10). |
| T-07-02-03 | Information disclosure | Firestore child data | mitigate | No child-data collection added; deny-by-default catch-all preserved (D-11). Audio clips are bundled assets, not user data. |
| T-07-02-04 | DoS / offline | audio playback | mitigate | Clips are BUNDLED (no network fetch at trace time, PLAT-01); unknown/missing id is a silent no-op, never blocks the child. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | mitigate | Audio package is the only install; gated by the blocking-human legitimacy checkpoint above. T-07-02-01 covers it. |
</threat_model>

<verification>
- `flutter test test/services/asset_audio_player_test.dart` passes.
- `grep -c "allow write: if false;" firestore.rules` ≥ 6 and the catch-all remains last.
- `flutter build apk --debug` exits 0 (audio package links on Android).
</verification>

<success_criteria>
- A tappable Play plays a bundled pronunciation clip per letter and per word, offline, no TTS (S1-06).
- Unknown audioIds degrade silently; the child never sees an audio error.
- words/exercises/units inherit the read-only, child-safe Firestore posture.
</success_criteria>

<output>
Create `.planning/phases/07-learning-engine-letter-unit-built-to-the-claude-design-proto/07-02-SUMMARY.md` when done. Record the chosen audio package + pinned version, the audioId→asset map, and which clips are real vs placeholder (owner deliverable). Note whether the rules deploy ran or is human-gated.
</output>
