---
phase: 11-spike-genui-catalog-native-stylus-canvas-kill-shot
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - .planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "The same baa trace is captured on a REAL Pixel Tablet + stylus on the embedded arm AND the standalone arm (D-05/D-06) — emulator/finger does NOT satisfy this"
    - "A human judges by feel whether GenUI hosting degrades the canvas (indistinguishable = pass, any stutter/drop/reset = fail)"
    - "Whether _StrokeCanvasState survives at least one GenUI surface update (coaching-line re-render) is observed and recorded (Pitfall 1)"
    - "A SPIKE-FINDINGS doc states an explicit pass/fail verdict with what was observed (SC-2)"
    - "A `GATE: keep|drop` line is recorded and handed to Phase 14 (SC-3)"
    - "The hard 3-day time-box is respected — if clean hosting is not working by then, difficulty itself is the FAIL/drop verdict (D-08); the spike never ends inconclusive"
    - "The durable layers are confirmed unchanged at spike end (SC-4) and the App-Check-unenforced throwaway posture is flagged to NOT carry into Phase 14"
  artifacts:
    - path: ".planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md"
      provides: "Written verdict + observations + the GATE line + the recorded-version/posture hand-off to Phase 14"
      contains: "GATE:"
  key_links:
    - from: ".planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md"
      to: "Phase 14 (TutorBrain spine)"
      via: "GATE: keep|drop consumed as the architecture decision"
      pattern: "GATE: (keep|drop)"
---

<objective>
Produce the only deliverable that matters: a confident, evidence-backed GATE. On a REAL Pixel
Tablet with a stylus, trace baa on the GenUI-embedded canvas [A] and on the identical standalone
canvas [B] (D-05), capture both on video, and judge by FEEL whether GenUI hosting degrades the
pen — ink must follow the stylus tip with no visible stutter, drop, or mid-trace reset, and the
embedded canvas must keep its stroke state across at least one GenUI surface update (Pitfall 1).
Then write SPIKE-FINDINGS with an explicit pass/fail verdict and a `GATE: keep|drop` line, and
package it via /gsd:spike-wrap-up so Phase 14 reads which architecture to build. The hard 3-day
time-box forces a GATE either way: a clean indistinguishable A/B = keep; anything short of it,
including the time-box being exhausted, = drop (D-08). The spike never ends inconclusive.

Purpose: this is the kill-shot's recorded result — the architecture decision the whole v2.0 build
branches on.
Output: SPIKE-FINDINGS.md with the verdict, the GATE, and the version/posture hand-off; packaged
for Phase 14.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-RESEARCH.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-VALIDATION.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-CONTEXT.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-01-SUMMARY.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device A/B on a real Pixel Tablet — embedded vs standalone baa trace (feel-based, D-06)</name>
  <action>
    Human-verify on real hardware (no CLI substitute, D-06): deploy the Plan-02 spike target to a real
    Pixel Tablet + stylus, screen-record, trace baa on the GenUI-embedded arm [A] and the standalone arm
    [B] on the same baa fixture, trigger at least one GenUI surface update mid/post trace, and judge by
    FEEL whether the two arms are indistinguishable and whether _StrokeCanvasState survives the surface
    update. Emulator/finger does NOT satisfy this. Report PASS or FAIL — difficulty/time-box-exhaustion
    is itself a valid FAIL/drop result (D-08). See &lt;how-to-verify&gt; for the exact steps.
  </action>
  <what-built>The spike app (Plan 02) runs as its own target with an A/B toggle between the GenUI-embedded canvas [A] and the identical standalone canvas [B] on the same baa fixture.</what-built>
  <how-to-verify>
    On a REAL Pixel Tablet with a stylus (NOT an emulator, NOT a finger — D-06/Pitfall 6):
    1. Deploy the spike target: `flutter run -t lib/spike_genui/main_spike_genui.dart` (or build+install the spike APK) onto the Pixel Tablet.
    2. Start screen recording (e.g. `adb shell screenrecord`, or the tablet's built-in capture).
    3. Arm [A] EMBEDDED: trace baa on the GenUI-hosted canvas (coaching line above, native canvas below). Watch the ink: does it follow the stylus tip smoothly, or stutter / drop points / reset mid-trace?
    4. Trigger at least one GenUI surface update (e.g. the coaching line re-renders) mid- or post-trace; confirm in-progress/complete ink is NOT reset (Pitfall 1 — _StrokeCanvasState survival).
    5. Arm [B] STANDALONE: trace baa on the identical canvas with no GenUI parent.
    6. Compare [A] vs [B] by FEEL: can you tell which is which by ink responsiveness?
    7. Check the spike-local initState/dispose prints (if added): did _StrokeCanvasState tear down during a trace?
    8. Keep the captured video — it IS the evidence attached to the GATE.
    PASS = indistinguishable, smooth in both, state survives the surface update, achieved within the
    3-day time-box without touching a durable file. FAIL = embedded arm stutters/drops/resets where
    standalone is smooth, OR the stylus drag is stolen by a GenUI scroll ancestor (Pitfall 2), OR
    hosting needed a durable-file edit (SC-4 guard would have gone red), OR the time-box is exhausted
    (D-08 — difficulty itself is the drop verdict).
  </how-to-verify>
  <resume-signal>Report one of: "PASS — indistinguishable A/B, state survived, video captured" OR "FAIL — &lt;what degraded: stutter/drop/reset/stolen-drag/durable-edit/time-box&gt;, video captured". Either outcome is a valid, recordable result — a kill-shot must be allowed to kill.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write SPIKE-FINDINGS with the verdict + the GATE, then package via /gsd:spike-wrap-up</name>
  <read_first>
    - .planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-VALIDATION.md (§What constitutes defensible PASS vs FAIL evidence; the GATE-either-way protocol)
    - .planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-01-SUMMARY.md + 11-02-SUMMARY.md (resolved genui/firebase_ai/firebase_core versions; the installed-API accessor form; gesture/scroll observations; App Check fallback usage)
    - .planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-RESEARCH.md (§Security Domain — the App-Check-unenforced posture that must be flagged for Phase 14)
  </read_first>
  <files>.planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md</files>
  <action>
    Write SPIKE-FINDINGS.md recording, from the device A/B above: (1) the explicit pass/fail VERDICT
    on whether the GenUI core catalog can cleanly host the real-time native canvas (SC-2), with exactly
    what was observed — ink behavior in each arm, whether _StrokeCanvasState survived the surface update,
    whether the mixed tree rendered in one surface, any gesture/scroll-ancestor conflict (Pitfall 2),
    and how much of the time-box was spent (D-08). (2) A single decisive line `GATE: keep` OR
    `GATE: drop` (SC-3) — keep = build on the GenUI SDK in Phase 14; drop = Phase 14 builds the raw
    firebase_ai function-calling + tiny native dispatcher fallback (D-02). (3) The HAND-OFF block for
    Phase 14: the resolved genui / firebase_ai / firebase_core versions (the known-good lockfile delta,
    Pitfall 4), the installed genui 0.9.x data-binding accessor form, and an EXPLICIT flag that App
    Check was left UNENFORCED in throwaway scope (D-13) and MUST be enforced in Phase 14 (TUTOR-03) —
    this throwaway posture must NOT silently carry into prod. (4) Confirm SC-4: the durable layers were
    unchanged the entire spike (cite the green git-diff guard). Difficulty itself counts as FAIL/drop
    evidence — do NOT iterate past the time-box to manufacture a "keep".
    Then run `/gsd:spike-wrap-up` to package the findings so Phase 14 can consume the GATE (D-13). If the
    command is unavailable, ensure SPIKE-FINDINGS.md is at the canonical spike path with the GATE line
    grep-able, and note the manual packaging in the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md && grep -E "GATE: (keep|drop)" .planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - SPIKE-FINDINGS.md exists at `.planning/spikes/11-genui-native-canvas/SPIKE-FINDINGS.md` (SC-2).
    - It contains exactly one decisive `GATE: keep` or `GATE: drop` line (SC-3): `grep -cE "GATE: (keep|drop)" <path>` returns 1.
    - It states an explicit pass/fail verdict with the observed A/B behavior and the surface-update state-survival result.
    - It records the resolved genui/firebase_ai/firebase_core versions and FLAGS the App-Check-unenforced posture as not-for-Phase-14 (`grep -E "App Check|firebase_ai|genui" <path>` matches).
    - SC-4 confirmed: `git diff --quiet HEAD -- lib/features/practice/widgets/stroke_canvas.dart lib/features/letter_unit/ lib/core/scoring/ lib/core/exercise_engine/ assets/curriculum/` exits 0.
    - Findings packaged via /gsd:spike-wrap-up (or the manual fallback path documented in the SUMMARY).
  </acceptance_criteria>
  <done>A written verdict + a single recorded GATE: keep|drop line + the version/posture hand-off exist at the canonical spike path and are packaged for Phase 14; durable layers confirmed unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike findings -> Phase 14 | The GATE decision crosses from a throwaway spike into production architecture; the throwaway security posture must NOT cross with it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Spoofing/DoS/cost | App-Check-unenforced posture leaking into Phase 14 | mitigate | SPIKE-FINDINGS MUST explicitly flag App Check was unenforced in throwaway scope (D-13) and that Phase 14 enforces it (TUTOR-03). Enforced by the Task-2 acceptance grep for the App-Check flag. |
| T-11-04 | Tampering | durable canvas/scorer/curriculum | mitigate | SC-4 git-diff guard cited green in the findings; spike is deletable (revert pubspec + delete lib/spike_genui/ returns to baseline — SC-4 by construction, D-10). |
| T-11-05 | Repudiation | the GATE decision | mitigate | The decision is recorded as a single grep-able `GATE: keep|drop` line with the device video as evidence, so Phase 14 inherits an unambiguous, auditable architecture choice (SC-3). |
</threat_model>

<verification>
- On-device A/B captured on a real Pixel Tablet + stylus (D-06); embedded vs standalone judged by feel.
- SPIKE-FINDINGS.md exists with an explicit verdict (SC-2) and exactly one `GATE: keep|drop` line (SC-3).
- Version + App-Check-posture hand-off recorded for Phase 14.
- SC-4 durable-diff guard green; spike is fully deletable.
- Findings packaged via /gsd:spike-wrap-up.
</verification>

<success_criteria>
- A confident GATE (keep or drop) recorded and handed to Phase 14 — the spike's whole reason to exist.
- The decision is evidence-backed (device A/B video + observed behavior), respects the 3-day time-box, and never ends inconclusive (D-08).
- The throwaway security posture (App Check unenforced) is explicitly fenced off from Phase 14.
- Durable layers confirmed unchanged (SC-4); the spike is deletable to return the repo to baseline.
</success_criteria>

<output>
Create `.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-03-SUMMARY.md` when done.
Reference the SPIKE-FINDINGS path and restate the recorded GATE so the phase close + Phase 14 inherit it.
</output>
