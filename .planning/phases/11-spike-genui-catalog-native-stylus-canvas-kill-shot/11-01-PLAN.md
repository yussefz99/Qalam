---
phase: 11-spike-genui-catalog-native-stylus-canvas-kill-shot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/spike_genui/fixtures/baa_reference.dart
  - test/spike_genui/durable_layers_unchanged_test.dart
  - test/spike_genui/correct_package_test.dart
  - .planning/spikes/11-genui-native-canvas/NOTES.md
autonomous: false
requirements: []
user_setup:
  - service: firebase-ai-logic
    why: "The Gemini Flash call in the GenUI arm routes through Firebase AI Logic on the existing app qalam-app-bd7d0; the AI Logic / Gemini API toggle is console state, not in git, and is blocking for any model call."
    dashboard_config:
      - task: "Enable Firebase AI Logic (Gemini API) on project qalam-app-bd7d0"
        location: "Firebase Console -> Build -> AI Logic (Gemini API) -> Get started / enable the Gemini Developer API"

must_haves:
  truths:
    - "pubspec.yaml declares genui and firebase_ai and does NOT declare flutter_genui"
    - "firebase_core floor is >=4.11.0 so firebase_ai 3.13.0 resolves"
    - "A durable-layers git-diff guard fails the build if any sacred path is touched (SC-4)"
    - "baa's signed-off referenceStrokes are available to the spike read-only without importing the curriculum loader"
    - "Firebase AI Logic is confirmed enabled on qalam-app-bd7d0 (or the blocker is recorded)"
  artifacts:
    - path: "lib/spike_genui/fixtures/baa_reference.dart"
      provides: "const List<StrokeSpec> baaReferenceStrokes copied read-only from letters.json"
      contains: "baaReferenceStrokes"
    - path: "test/spike_genui/correct_package_test.dart"
      provides: "Guard: genui present, flutter_genui absent"
    - path: "test/spike_genui/durable_layers_unchanged_test.dart"
      provides: "Guard: git diff on durable paths is empty (SC-4)"
  key_links:
    - from: "test/spike_genui/durable_layers_unchanged_test.dart"
      to: "lib/features/practice/widgets/stroke_canvas.dart + lib/features/letter_unit/ + lib/core/scoring/ + lib/core/exercise_engine/ + assets/curriculum/"
      via: "git diff --quiet HEAD -- <durable paths>"
      pattern: "git diff --quiet"
    - from: "lib/spike_genui/fixtures/baa_reference.dart"
      to: "lib/models/letter.dart"
      via: "StrokeSpec import (read-only)"
      pattern: "StrokeSpec"
---

<objective>
Lay the throwaway-spike foundation: install the CORRECT first-party packages (genui, not
the discontinued flutter_genui), bump firebase_core to the floor firebase_ai needs, enable
Firebase AI Logic in the console, copy baa's signed-off reference strokes into a
self-contained read-only fixture, and stand up the two automatable structural guards that
keep the spike honest — the SC-4 durable-layers-unchanged git-diff guard and the
genui-not-flutter_genui package guard.

Purpose: every later spike task depends on the right SDK being installed, the durable layers
being provably untouched (TUTOR-01 / SC-4), and the model backend being reachable. This plan
de-risks all three before any GenUI catalog code is written.
Output: a resolved pubspec with genui + firebase_ai, the two guard tests, the baa fixture,
and a confirmed AI Logic console state.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-RESEARCH.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-PATTERNS.md
@.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-CONTEXT.md

# Read-only durable sources (DO NOT MODIFY — the SC-4 guard proves you did not):
@lib/models/letter.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install genui + firebase_ai, bump firebase_core, add the two structural guards</name>
  <read_first>
    - pubspec.yaml (the firebase_core ^4.10.0 / firebase_auth ^6.5.2 pins to bump)
    - .planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-RESEARCH.md (§Standard Stack, §Package Legitimacy Audit, Pitfalls 3 & 4)
    - .planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-VALIDATION.md (§Wave 0 Requirements)
  </read_first>
  <files>pubspec.yaml, test/spike_genui/correct_package_test.dart, test/spike_genui/durable_layers_unchanged_test.dart</files>
  <action>
    Install the CORRECT packages per RESEARCH §Standard Stack: run `flutter pub add genui firebase_ai`
    and `flutter pub add firebase_core:^4.11.0` in the same resolution pass; optionally add
    genui_catalog for CoreCatalogItems. Do NOT install flutter_genui — it is discontinued
    (replacedBy: genui, Pitfall 3). genui is first-party (publisher labs.flutter.dev), firebase_ai
    is first-party (publisher firebase.google.com) — both VERIFIED in the Package Legitimacy Audit, so
    NO per-package legitimacy checkpoint is required. Let pub resolve the FlutterFire lockstep set
    (firebase_auth may move within 6.x); do NOT hand-pin mismatched majors. Record the resolved
    versions of genui, firebase_ai, firebase_core in the SUMMARY (Pitfall 4 — Phase 14 inherits this
    known-good set).
    Then author two guard tests under test/spike_genui/:
    (a) correct_package_test.dart — reads pubspec.yaml as a String and asserts it CONTAINS a
    `genui:` dependency line and does NOT contain a `flutter_genui:` dependency line (filter out
    comment lines beginning with '#' before matching so a header comment naming flutter_genui cannot
    self-invalidate the gate).
    (b) durable_layers_unchanged_test.dart — runs `git diff --quiet HEAD --` over the SACRED paths
    lib/features/practice/widgets/stroke_canvas.dart lib/features/letter_unit/ lib/core/scoring/
    lib/core/exercise_engine/ assets/curriculum/ via Process.run, and asserts exit code 0 (empty diff,
    SC-4). This is the TUTOR-01 invariant enforced as a test.
    Both are plain flutter_test files (no widget pump needed).
  </action>
  <verify>
    <automated>flutter pub get && flutter test test/spike_genui/correct_package_test.dart test/spike_genui/durable_layers_unchanged_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^[[:space:]]*genui:" pubspec.yaml` succeeds and `grep -vE '^[[:space:]]*#' pubspec.yaml | grep -q "flutter_genui:"` fails (genui present, flutter_genui absent).
    - `grep -E "firebase_core:" pubspec.yaml` shows a floor >= ^4.11.0.
    - `flutter pub get` resolves with exit 0 (genui + firebase_ai + bumped firebase_core co-resolve).
    - Both guard tests pass: the package guard is green and the durable-layers git-diff guard reports an empty diff over all five sacred paths.
  </acceptance_criteria>
  <done>genui + firebase_ai installed, flutter_genui absent, firebase_core bumped to ^4.11.0, both structural guard tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Copy baa's signed-off reference strokes into a self-contained read-only fixture</name>
  <read_first>
    - assets/curriculum/letters.json (the `baa` entry's referenceStrokes — the signed-off source of truth)
    - lib/models/letter.dart (the StrokeSpec constructor: order, label, points List&lt;List&lt;double&gt;&gt;, direction, type)
    - .planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-PATTERNS.md (§fixtures/baa_reference.dart — the exact data + Q1 resolution)
  </read_first>
  <files>lib/spike_genui/fixtures/baa_reference.dart</files>
  <action>
    Resolve Open Question Q1 by COPYING baa's referenceStrokes — do NOT import the durable curriculum
    loader (it drags Firestore/Drift providers into the throwaway). Create a
    `const List&lt;StrokeSpec&gt; baaReferenceStrokes` constructed against the StrokeSpec from
    package:qalam/models/letter.dart, with the two strokes exactly as PATTERNS.md §baa_reference.dart
    lists them: order 1 'body' type 'curve' direction 'rightToLeft' (the 12-point boat) and order 2
    'dot' type 'dot' direction 'tap' (the single point). Verify each point against the live
    assets/curriculum/letters.json baa entry before committing — if the authored data has drifted since
    PATTERNS.md was written, copy the LIVE values (letters.json is the source of truth, not the doc).
    Add a header comment stating this is throwaway spike code, that it copies signed-off baa data
    read-only, and that the durable curriculum is intentionally NOT imported (D-09/D-10/Q1). The canvas
    widget under test stays the REAL one; only its data is a local fixture.
  </action>
  <verify>
    <automated>flutter analyze lib/spike_genui/fixtures/baa_reference.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StrokeSpec(" lib/spike_genui/fixtures/baa_reference.dart` returns 2 (boat + dot).
    - The fixture imports only `package:qalam/models/letter.dart` (StrokeSpec) — no curriculum loader, no Firestore, no Drift import (`grep -E "firestore|drift|curriculum_repository" lib/spike_genui/fixtures/baa_reference.dart` returns nothing).
    - `flutter analyze lib/spike_genui/fixtures/baa_reference.dart` exits 0.
    - The two strokes' coordinates match the live `baa` entry in assets/curriculum/letters.json (verified by reading both).
  </acceptance_criteria>
  <done>baaReferenceStrokes is a self-contained const fixture matching the signed-off letters.json baa data, importing only StrokeSpec, analyzing clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable Firebase AI Logic on qalam-app-bd7d0 (human-action checkpoint)</name>
  <what-built>Packages installed and guards green; the spike now needs the Gemini backend reachable.</what-built>
  <decision>Enable Firebase AI Logic (Gemini API) on the existing Firebase project so the GenUI arm's model call can reach Gemini Flash.</decision>
  <action>
    Human-only console step (no CLI/API path): in the Firebase Console, enable Firebase AI Logic
    (Gemini Developer API) on project qalam-app-bd7d0 so the Plan-02 present_activity model call can
    reach Gemini Flash. App Check stays UNENFORCED in this throwaway scope (D-13); if the backend
    rejects the call for missing App Check, register the App Check DEBUG provider (RESEARCH A3). Record
    the outcome (and the App-Check-unenforced posture) so it does NOT carry into Phase 14.
  </action>
  <context>
    There is no Flutter/CLI path to toggle Firebase AI Logic — it is a one-time Firebase Console action
    on project qalam-app-bd7d0 (the same app firebase_core already initializes). Without it, the
    present_activity model call in Plan 02 fails and the embedded arm cannot render. The API key stays
    in the Firebase AI Logic backend (never in the client). App Check is left UNENFORCED in this
    throwaway scope (D-13) — if the backend rejects calls for missing App Check, register the App Check
    DEBUG provider as a fallback (RESEARCH Assumption A3); record the App-Check-unenforced posture so it
    does NOT silently carry into Phase 14.
  </context>
  <how-to-verify>
    1. Open the Firebase Console -> project qalam-app-bd7d0 -> Build -> AI Logic (Gemini API).
    2. Click "Get started" / enable the Gemini Developer API for this project.
    3. Confirm the console shows AI Logic enabled (no billing required for the Gemini Developer API dev tier).
  </how-to-verify>
  <resume-signal>Type "enabled" once AI Logic is on for qalam-app-bd7d0 (or "blocked: &lt;reason&gt;" — a blocked backend within the time-box itself pushes toward a conservative GATE, D-08).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike client -> Firebase AI Logic | Outbound model traffic from a child's app; only present_activity text + letterId may cross (no strokes, no PII) |
| pub.dev -> build | Third-party package installs (genui, firebase_ai) enter the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | pubspec installs (genui, firebase_ai, genui_catalog) | accept | All three are first-party (publishers labs.flutter.dev / firebase.google.com), VERIFIED in RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] packages → no install checkpoint needed. flutter_genui is REMOVED (discontinued). |
| T-11-02 | Spoofing/DoS | Gemini API-key abuse, App Check unenforced | accept (throwaway only) | Acceptable ONLY because the spike is local, time-boxed, deletable, and the key stays in the Firebase AI Logic backend (never in client). MUST be flagged in SPIKE-FINDINGS to NOT carry into Phase 14 (Phase 14 enforces App Check — TUTOR-03). |
| T-11-03 | Information disclosure | model call payload | mitigate | The transport (Plan 02) sends only present_activity text + letterId:"baa" — NEVER raw List&lt;Offset&gt; strokes, NEVER nickname/name/PII. Prefigures GROUND-02; this plan plants the constraint via the fixture (hardcoded letterId, no child fields). |
</threat_model>

<verification>
- `flutter pub get` resolves clean with genui + firebase_ai + firebase_core ^4.11.0.
- Both Wave-0 guard tests pass (package guard + SC-4 durable-diff guard).
- baa fixture analyzes clean and matches letters.json.
- Firebase AI Logic confirmed enabled on qalam-app-bd7d0 (or blocker recorded).
- `git diff --quiet HEAD -- lib/features/practice/widgets/stroke_canvas.dart lib/features/letter_unit/ lib/core/scoring/ lib/core/exercise_engine/ assets/curriculum/` exits 0 (no durable file touched).
</verification>

<success_criteria>
- genui (not flutter_genui) + firebase_ai installed; firebase_core bumped to the floor; resolved versions recorded.
- The two automatable guards exist and are green (feed SC-4 + build-sanity for the whole spike).
- baa reference strokes available read-only as a self-contained fixture (Q1 resolved without rewiring curriculum).
- AI Logic console state confirmed (or the blocker that informs the GATE is recorded).
</success_criteria>

<output>
Create `.planning/phases/11-spike-genui-catalog-native-stylus-canvas-kill-shot/11-01-SUMMARY.md` when done.
Record the resolved genui / firebase_ai / firebase_core versions and the AI Logic console outcome.
</output>
