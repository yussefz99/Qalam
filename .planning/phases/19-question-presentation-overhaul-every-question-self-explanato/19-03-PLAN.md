---
phase: 19-question-presentation-overhaul-every-question-self-explanato
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - lib/features/letter_unit/widgets/prompt_header.dart
  - lib/features/letter_unit/widgets/copy_stimulus.dart
  - lib/l10n/app_en.arb
autonomous: true
requirements: [QP-03, QP-04, QP-05, QP-06]
must_haves:
  truths:
    - "completeWord/fillBlank render the word at full stimulus size with a big highlighted RTL slot box at the gap; the literal __blank__/_letter_ marker never reaches the screen"
    - "Listen-and-write questions show a large tappable audio card that auto-plays once and replays on tap, silent-degrading to the TTS say-line when a clip is absent"
    - "writeWord.copy reveals the word large, hides it on 'I'm Ready' or first stroke, and a Peek/Hide toggle re-reveals it — nothing on a timer"
    - "Recall writeLetter/writeWord questions show no letter model (the remediation arc is the only hint path)"
  artifacts:
    - path: "lib/features/letter_unit/widgets/prompt_header.dart"
      provides: "Enlarged slot box (_GapWord/_GapLetter) + hero audio card (_AudioPart) with auto-play-once"
      contains: "gapSlot"
    - path: "lib/features/letter_unit/widgets/copy_stimulus.dart"
      provides: "Child-controlled reveal→hide→peek StatefulWidget for writeWord.copy (D-05)"
      min_lines: 40
  key_links:
    - from: "PromptHeader _TextPart reveal path"
      to: "CopyStimulus"
      via: "reveal=='thenHide' now renders the child-controlled widget, not a static opacity dim"
      pattern: "CopyStimulus"
    - from: "audio card mount"
      to: "auto-play-once / TTS fallback"
      via: "initState fire mirroring the scaffold auto-speak; silent-degrade on missing clip"
      pattern: "audioCard"
---

<objective>
Land D-05/D-06/D-07/D-08: the stimulus zone in `PromptHeader` becomes self-explanatory per
type — a big highlighted RTL slot box at the gap for `completeWord`/`fillBlank` (the literal
`__blank__` marker retired), a large replayable audio card for listen-and-write questions,
and a child-controlled hide+peek for `writeWord.copy` (replacing today's static opacity dim).
Recall write types carry no letter model — the Phase-18 remediation arc is the only hint path
(one hint mechanism, not two).

Purpose: completes the "every question self-explanatory on screen" goal on the stimulus side
(the instruction bar in 19-02 covers the "what to do" strip). All changes extend the single
`PromptHeader` render path — no bespoke widget tree per type.
Output: enlarged slot/audio renderers in `prompt_header.dart`, the new `copy_stimulus.dart`
widget, and QP-03/04/05/06 tests green.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-CONTEXT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enlarge the gap slot box (D-06) + hero audio card (D-07)</name>
  <files>lib/features/letter_unit/widgets/prompt_header.dart, lib/l10n/app_en.arb, test/features/letter_unit/prompt_header_slot_audio_test.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/prompt_header.dart (`_AudioPart` teal-button skeleton 140-188; `_GapWord` 426-444 / `_GapLetter` 447-461 teal-ring rendering; `_TextPart._tokens()` marker split 390-412, regex `(__blank__|_letter_)` at 393; `_arabic()` glyph style 414-422; `_ImagePart` errorBuilder silent-degrade posture 310-321; the `_visuals` say-filter 63-64 to PRESERVE)
    - test/features/letter_unit/prompt_header_slot_audio_test.dart (the RED contract from 19-01 — `Key('gapSlot')`, `Key('audioCard')`, no `__blank__`/`_letter_` text, auto-play-once — satisfy with zero test edits)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (initState auto-speak at 254-273 — the auto-fire precedent for the audio card's mount-time play)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md (§2 audio card: --ink-teal fill, white 40px speaker + "Listen" label, radius 28, min-height 96, padding 24, sticker shadow 0 4px 0 --deep-ink; §3 slot box: radius 14, 2px --ink-teal outline, min-height ~64, min-width 72 word / 56 letter, gentle pulse --dur-slow 420ms --ease-in-out; RTL-correct)
    - lib/theme/qalam_tokens.dart (token names)
  </read_first>
  <behavior>
    - Test: completeWord/fillBlank renders `Key('gapSlot')` at the gap; the word shows at 40px Arabic RTL; `find.text('__blank__')` and `find.text('_letter_')` are findsNothing
    - Test: a listen exercise renders `Key('audioCard')` at min-height ≥96 with a "Listen" label; auto-play fires once on mount; tap replays
    - Test: with no clip asset, the audio card still renders and tap falls back to the TTS say-line seam (no error surface)
  </behavior>
  <action>
    Two renderers in `prompt_header.dart`, both extend-in-place (no new widget tree per type).

    (1) Slot box (D-06/QP-04): enlarge `_GapWord`/`_GapLetter` to the UI-SPEC §3 contract —
    keep the teal-ring `Container(BoxDecoration(border: inkTeal width 2))` (Flutter draws a
    solid ring; the CSS-dashed look is not used), raise height to ~64 to match the 40px glyph
    line, min-width 72 (word) / 56 (letter), radius 14, add the gentle opacity/scale pulse
    (--dur-slow 420ms --ease-in-out, no gold). Add `Key('gapSlot')`. KEEP the
    `_TextPart._tokens()` marker split at 393 so `__blank__`/`_letter_` still never leak to
    screen (Pitfall 6). Lay the word + slot out RTL via the existing Wrap/Directionality so
    the slot sits at the correct reading position.

    (2) Audio card (D-07/QP-05): add a hero variant of `_AudioPart` — the same
    `Semantics(button) → Material → InkWell → Container` skeleton (BoxShadow deepInk
    Offset(0,4) sticker shadow) upsized to --ink-teal fill, white 40px `Icons.volume_up_rounded`
    + "Listen" label (20px Fredoka 500 white), radius 28, min-height 96, padding 24. Add
    `Key('audioCard')`. Auto-play the clip ONCE on mount (mirror the scaffold initState
    auto-speak pattern) via the existing audio seam (`onAudioTap`/TTS fallback); tap replays.
    If the 18.1 partner clip asset is absent, the card STILL renders and tap silent-degrades to
    the TTS say-line seam — no error surface, no broken-audio icon (mirror the `_ImagePart`
    errorBuilder posture). Add the "Listen" label as an ARB key + English default — append it
    at the BOTTOM of the ARB key map (immediately before the closing brace; 19-02 owns the TOP
    block after `@@locale`, so the top/bottom split keeps the parallel Wave-2 edits
    merge-clean); run `flutter gen-l10n`. Pull all values from `QalamTokens`; no gold.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/letter_unit/prompt_header_slot_audio_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The 19-01 `prompt_header_slot_audio_test.dart` passes with zero test edits
    - `find.text('__blank__')` and `find.text('_letter_')` are findsNothing (marker never leaks)
    - The slot box carries `Key('gapSlot')` (radius 14, 2px ink-teal outline) and the audio card `Key('audioCard')` (min-height 96, "Listen")
    - The audio card renders AND falls back to TTS when no clip asset exists (behavior assertion in the test)
    - No gold token; all values cite QalamTokens; `flutter analyze lib/features/letter_unit/widgets/prompt_header.dart` exits 0
  </acceptance_criteria>
  <done>Slot box and audio card render per UI-SPEC on the live header, the marker never leaks, auto-play-once + silent-degrade work, and the 19-01 slot/audio test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Copy hide+peek widget (D-05) + recall no-model rule (D-08)</name>
  <files>lib/features/letter_unit/widgets/copy_stimulus.dart, lib/features/letter_unit/widgets/prompt_header.dart, lib/l10n/app_en.arb, test/features/letter_unit/copy_stimulus_test.dart, test/features/letter_unit/recall_no_model_test.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/prompt_header.dart (`_TextPart` card shell 351-423: surfaceRaised radius 16 aquaEdge border, the Color(0x1A0E5B5F) shadow, `_arabic()` 40px arBody w600 deepInk; the static dim to REPLACE — `dim = part.reveal == 'thenHide'` at 359, `Opacity(opacity: dim ? 0.18 : 1.0)` at 372)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (`_ExerciseScaffoldState._instructionHold` toggle 226/286/296 — the only setState-driven analog in this feature; the WriteSurface stroke-start hook to detect first stroke)
    - test/features/letter_unit/copy_stimulus_test.dart + test/features/letter_unit/recall_no_model_test.dart (the RED contracts from 19-01 — satisfy with zero test edits)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md (§4 copy hide+peek: revealed=word 40px + "I'm Ready" button min-height 64; hide on "I'm Ready" OR first stroke; "Peek" re-reveals, "Hide" toggles back; nothing on a timer; three states revealed/hidden/peeking. §5 recall absence rule. Copywriting: "I'm Ready"/"Peek"/"Hide" Title Case; single-word Semantics labels)
  </read_first>
  <behavior>
    - Test: CopyStimulus in `revealed` shows the word (40px Arabic) + "I'm Ready"; tapping "I'm Ready" → `hidden` (word gone); "Peek" → `peeking` (word back); "Hide" → `hidden`
    - Test: no timer hides the word (pump a long duration without tap → state unchanged)
    - Test: recall writeLetter/writeWord configs carry no model/ghost part (≥1 recall config exists)
  </behavior>
  <action>
    (1) CopyStimulus (D-05/QP-03): create `lib/features/letter_unit/widgets/copy_stimulus.dart`
    — a small `StatefulWidget` with three states (revealed / hidden / peeking). Reuse the
    `_TextPart` card shell (surfaceRaised, radius 16, aquaEdge border, the shadow) + `_arabic()`
    40px glyph for the word. In `revealed`, show the word + an "I'm Ready" button (Title Case,
    min-height 64). Tapping "I'm Ready" OR the first WriteSurface stroke hides the word (card
    blanks to a calm placeholder) → `hidden`. In `hidden`, show a "Peek" button (min-height 64,
    ≥16px from siblings) that re-reveals → `peeking`; while peeking show a "Hide" toggle back to
    `hidden`. NOTHING vanishes on a timer (D-05 — every reveal/hide is child-controlled).
    Semantics labels carry the full verb+noun intent ("Peek at the word", etc.). Then REPLACE
    the static dim in `_TextPart` (359/372) for `reveal == 'thenHide'` exercises so it renders
    `CopyStimulus` instead of the opacity-0.18 Opacity. Wire the first-stroke hide via a
    callback the scaffold/WriteSurface already exposes on stroke start (do not invent a new
    capture path — mounts through `presentGraphExercise`). Add the "I'm Ready"/"Peek"/"Hide"
    ARB keys + English defaults, appended at the BOTTOM of the ARB key map with this plan's
    other keys (19-02 owns the TOP block — keeps parallel edits merge-clean); run
    `flutter gen-l10n`. Tokens only; no gold.

    (2) Recall no-model (D-08/QP-06): ensure no `writeLetter`/`writeWord` RECALL config carries
    a model/ghost surface part — the remediation arc is the only hint path. If any recall config
    authors a model part, this is a data issue; assert it away in the 19-01
    `recall_no_model_test.dart` (green it with zero test edits). Add a lightweight render guard
    only if the test proves a config still leaks a model part; otherwise the assertion over the
    configs is the deliverable (no new UI).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/letter_unit/copy_stimulus_test.dart test/features/letter_unit/recall_no_model_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `copy_stimulus.dart` exists (≥40 lines) with three named states; the 19-01 copy_stimulus test passes with zero test edits
    - `_TextPart` for `reveal == 'thenHide'` now renders `CopyStimulus` (grep: `CopyStimulus`), and the static `Opacity(opacity: dim ? 0.18` path is gone
    - No timer-driven hide exists (source assertion: no `Future.delayed`/`Timer` hiding the word)
    - `recall_no_model_test.dart` passes: ≥1 recall config exists and none carry a model/ghost part
    - Tokens only, no gold; `flutter analyze` clean on both widget files
  </acceptance_criteria>
  <done>writeWord.copy is child-controlled reveal→hide→peek (no timer), recall types show no model, and both 19-01 tests are green with no test edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child → WriteSurface / copy widget | Stylus + tap input crosses here; the copy widget's first-stroke hook reads stroke-start only (no persistence, no network) |
| audio card → clip asset / TTS | Local asset read or local TTS; no network round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Denial of service | audio card missing-clip path | mitigate | Silent-degrade to the TTS say-line (mirror `_ImagePart` errorBuilder); the card never crashes the loop on a missing asset |
| T-19-08 | Information disclosure | copy/slot render | accept | Renders only authored curriculum content (no PII, no child data); no user input is stored or transmitted |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |
</threat_model>

<verification>
Run `flutter test test/features/letter_unit/` — slot/audio, copy_stimulus, and recall_no_model
tests are green; no other letter_unit test regresses. `flutter analyze` clean. Cold device UAT
of each stimulus type (sound off) is the end-of-phase human gate.
</verification>

<success_criteria>
completeWord/fillBlank show a big RTL slot box (no `__blank__` leak); listen-and-write shows a
large auto-playing/replayable audio card that silent-degrades; writeWord.copy is child-controlled
reveal→hide→peek with no timer; recall types show no model. All four QP-03/04/05/06 tests green
with zero test edits.
</success_criteria>

<output>
Create `.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-03-SUMMARY.md` when done.
</output>
