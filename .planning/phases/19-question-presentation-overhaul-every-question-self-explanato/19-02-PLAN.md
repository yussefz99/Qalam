---
phase: 19-question-presentation-overhaul-every-question-self-explanato
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - lib/l10n/app_en.arb
  - test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart
autonomous: true
requirements: [QP-01, QP-02]
must_haves:
  truths:
    - "Every graded non-teachCard question renders a persistent instruction strip (icon + per-type text) at a fixed position, readable with sound off"
    - "The instruction bar is tappable to re-hear the spoken line; there is exactly ONE replay affordance on screen (the old _HearAgainCta pill is gone)"
    - "The bar is hidden on teachCard and empty-say-line surfaces (via _hasInstruction)"
  artifacts:
    - path: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      provides: "The instruction bar in _mainColumn + per-type template map; _HearAgainCta deleted and folded into the bar"
      contains: "instructionBar"
    - path: "lib/l10n/app_en.arb"
      provides: "~10 per-type instruction template ARB keys (owner signs the strings — D-04)"
  key_links:
    - from: "instruction bar tap"
      to: "_speakInstructionThenRelease"
      via: "InkWell onTap re-invokes the existing say-line seam"
      pattern: "_speakInstructionThenRelease"
    - from: "instruction bar text"
      to: "exercise.type template map"
      via: "per-type resolution (NOT the say line — Pitfall 6)"
      pattern: "exercise\\.type"
---

<objective>
Land D-01/D-02/D-03: a persistent instruction bar in `ExerciseScaffold._mainColumn` — a
fixed strip (leading per-type icon + short child-readable text + trailing speaker glyph)
that renders the same place on every graded question type, tappable to replay the spoken
line. It ABSORBS the 18-12 `_HearAgainCta` pill so there is exactly one replay affordance
(the Phase-07 double-Hear-button device bug is the cautionary precedent). This makes every
non-trace question tell the child what to do from the screen alone; the say line stays the
spoken/bubble reinforcement layer.

Purpose: fixes the 2026-07-12 owner UAT finding "questions don't show what's being asked."
Output: the bar widget + the per-type template map + ~10 ARB strings; the instruction-bar
live-path test (19-01) turns GREEN.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-CONTEXT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-type instruction template map + ARB strings</name>
  <files>lib/features/letter_unit/widgets/exercise_scaffold.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (`ExerciseScaffoldStrings` 52-83 — English defaults + call-site l10n; `hearAgain` default at 66)
    - lib/l10n/app_en.arb (existing `@key` metadata entries 2-28 — the key+metadata shape to mirror)
    - lib/models/exercise.dart (`Exercise.type` 22-24 — the parsed type field the map keys on)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md (Copywriting Contract — the exact per-type table: traceLetter→"Trace the letter", writeLetter→"Write the letter", writeWord→"Write the word", writeWord copy-variant→"Copy the word", listen-variant→"Listen and write", connectWord→"Join the letters", completeWord→"Write the missing letter", fillBlank→"Write the missing part", transformWord→"Change the word", buildSentence→"Build the sentence", microDrill→"Practice this part", unknown→"Look and write"; icon column)
  </read_first>
  <behavior>
    - Test: `instructionTemplateFor(type)` returns "Write the missing letter" for `completeWord`, "Copy the word" for the writeWord copy variant, "Listen and write" for a listen variant (exercise carrying an AudioPart), "Practice this part" for `microDrill`
    - Test: unknown/null type falls back to "Look and write"
    - Test: microDrill criterion override — `dot`→"Practice the dot", `shape`→"Practice the curve", `strokeOrder`→"Practice the start"
  </behavior>
  <action>
    Add a pure per-type resolver — a map `exercise.type → (IconData icon, String template)` —
    exposed as a method/getter that the bar reads. Keys are exactly the ~10 types observed in
    `exercises.json` (`traceLetter, writeLetter, writeWord, connectWord, completeWord,
    fillBlank, transformWord, buildSentence, teachCard, microDrill`) plus the two writeWord
    sub-variants (copy vs listen, disambiguated by presence of a reveal/copy flag vs an
    `AudioPart`). Copy the exact template strings and icon choices from UI-SPEC Copywriting
    Contract (brand glyph `assets/icons/qalam-nib.svg` for traceLetter; Material `*_rounded`
    icons for the rest — `Icons.create_rounded`, `Icons.content_copy_rounded`,
    `Icons.hearing_rounded`, `Icons.link_rounded`, `Icons.border_color_rounded`,
    `Icons.autorenew_rounded`, `Icons.notes_rounded`, `Icons.center_focus_strong_rounded`,
    fallback `Icons.visibility_rounded`). Include the microDrill per-criterion override
    (dot/shape/strokeOrder). Add the ~10 template strings as ARB keys in `app_en.arb`
    (mirror the existing `@key` metadata shape) AND mirror the English defaults into
    `ExerciseScaffoldStrings` (so widget tests stay l10n-independent — the `hearAgain`
    precedent). Run `flutter gen-l10n` after editing the ARB (generated files are gitignored).
    Sentence case for the bar text; the icon set stays within the design kit — no emoji, no
    gold (anti-gamification). Do NOT render the `say` line here (Pitfall 6).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze lib/features/letter_unit/widgets/exercise_scaffold.dart lib/l10n</automated>
  </verify>
  <acceptance_criteria>
    - `flutter gen-l10n` regenerates cleanly and `flutter analyze` exits 0 on the edited files
    - The resolver returns the exact UI-SPEC template for each of the ~10 types (source assertion: strings match the UI-SPEC table verbatim)
    - `app_en.arb` gains ~10 new instruction keys with metadata; `ExerciseScaffoldStrings` carries English defaults for each
    - No hard-coded hex/px/font; no gold token referenced (grep the new code: no `gold`)
  </acceptance_criteria>
  <done>The per-type template map + ARB strings exist; gen-l10n is clean; analyze passes; the microDrill criterion override is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the instruction bar in _mainColumn + fold _HearAgainCta</name>
  <files>lib/features/letter_unit/widgets/exercise_scaffold.dart, test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (`_mainColumn` ribbon Row 696-715 → PromptHeader 717-721; the `_HearAgainCta` block to DELETE 730-739; the `_HearAgainCta` widget skeleton 1089-1133; `_hasInstruction` 234-238; `_speakInstructionThenRelease` 276-299; `_isTeachCard` 228; `_instructionHold` 226/286/296)
    - test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart (the RED contract from 19-01 — the `Key('instructionBar')` + single-replay-affordance + tap-replays-say-line + hidden-on-teachCard assertions to satisfy with ZERO test edits)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md (§1 Instruction bar contract: --teal-tint fill, --aqua-edge 1.5px border, radius 16, min-height 64, padding 24, icon↔text gap 12, leading icon 24px ink-teal, trailing speaker glyph 24px ink-teal, Semantics(button:true,label:"Hear it again"), the bar is never dimmed — only the canvas is held while speaking)
    - lib/theme/qalam_tokens.dart (the token names to pull — never hard-code)
  </read_first>
  <behavior>
    - Test (live path, from 19-01): a graded completeWord exercise renders `Key('instructionBar')` with text "Write the missing letter"
    - Test: exactly one replay affordance — the old "Hear again" pill is findsNothing; tapping the bar calls the TTS speak seam
    - Test: teachCard renders no instruction bar (findsNothing)
  </behavior>
  <action>
    Insert a persistent instruction strip in `_mainColumn` BETWEEN the ribbon Row (696-715)
    and `PromptHeader` (717-721), guarded by the existing `_hasInstruction` getter (already
    excludes teachCard + empty say-lines — matches UI-SPEC "hidden on teachCard"). Give it
    `Key('instructionBar')`. Compose it from the widget skeleton of `_HearAgainCta`
    (`Semantics(button:true,label:"Hear it again") → Material → InkWell → Container(BoxDecoration
    + Row[leading icon, per-type text, trailing speaker glyph])`) upsized to the UI-SPEC §1
    contract (min-height 64, --teal-tint fill, --aqua-edge 1.5px border, radius 16, padding 24,
    gap 12; icon + text from Task 1's resolver keyed on `widget.exercise.type`; trailing
    `Icons.volume_up_rounded` in ink-teal). The whole bar is ONE tap target whose `onTap` is
    `_speakInstructionThenRelease` verbatim (re-hear). DELETE the old `_HearAgainCta` block at
    730-739 and remove the now-unused `_HearAgainCta` class (1089-1133) — its behavior lives on
    the bar now (D-03: one replay affordance, never two). Keep the say-line auto-speak-once and
    the 8s `_instructionHold` (canvas dimmed 0.45 + IgnorePointer while speaking) unchanged; the
    bar itself is never dimmed. Pull every value from `QalamTokens`/`QalamTextStyles`; no gold.
    The bar must mount through `presentGraphExercise` (it lives inside the scaffold `build`, so
    it runs in initState re-mount — do not break the epoch key).
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The 19-01 instruction-bar live-path test passes with ZERO edits to the test file
    - grep of `exercise_scaffold.dart` shows the `_HearAgainCta` class and the 730-739 pill block are GONE (source assertion: no `_HearAgainCta`)
    - The bar carries `Key('instructionBar')` and `Semantics(..., label: 'Hear it again')`
    - No gold token appears in the bar code; all values cite QalamTokens (no raw hex/px)
    - `flutter analyze lib/features/letter_unit/widgets/exercise_scaffold.dart` exits 0
  </acceptance_criteria>
  <done>The bar renders per-type on the live path, replaces the pill (one replay affordance), is hidden on teachCard, and the 19-01 test is green with no test edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child → WriteSurface | Stylus input crosses here (unchanged this plan — the bar adds no new input path) |
| bar tap → TTS seam | Re-invokes the existing local `_speakInstructionThenRelease`; no network, no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information disclosure | instruction bar text | accept | Bar renders only per-type static templates (no PII, no child data); text comes from a fixed map, not user input |
| T-19-06 | Tampering | double replay control (Phase-07 device bug class) | mitigate | Fold `_HearAgainCta` into the bar — exactly one replay affordance, enforced by the 19-01 test |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |
</threat_model>

<verification>
Run the quick suite `flutter test test/features/letter_unit/` — the instruction-bar test is
green; no other letter_unit test regresses. `flutter analyze` clean on the edited files. Cold
device UAT (reading the bar with sound off) is the end-of-phase human gate.
</verification>

<success_criteria>
Every graded non-teachCard question shows a persistent, tappable instruction bar with the
correct per-type text; there is one replay affordance (pill gone); teachCard shows no bar;
the 19-01 live-path test is green with zero test edits.
</success_criteria>

<output>
Create `.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-02-SUMMARY.md` when done.
</output>
