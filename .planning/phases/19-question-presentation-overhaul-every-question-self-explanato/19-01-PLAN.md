---
phase: 19-question-presentation-overhaul-every-question-self-explanato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart
  - test/features/letter_unit/copy_stimulus_test.dart
  - test/features/letter_unit/prompt_header_slot_audio_test.dart
  - test/features/letter_unit/recall_no_model_test.dart
  - test/curriculum/learned_letters_lint_test.dart
  - test/data/app_database_test.dart
autonomous: true
requirements: [QP-01, QP-02, QP-03, QP-04, QP-05, QP-06, QP-07, QP-09]
must_haves:
  truths:
    - "Every Track-A/B/C behavior has a FAILING automated test naming its exact expected behavior before any implementation lands (Nyquist RED contract)"
    - "The instruction-bar test mounts the real scaffold through presentGraphExercise (live path), never a bare scaffold (Phase-15 dead-wire lesson)"
    - "The v6→v7 two-profile migration test runs against a temp-FILE NativeDatabase, not shared in-memory"
  artifacts:
    - path: "test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart"
      provides: "QP-01/02 live-path RED test for the persistent instruction bar + tap-to-replay"
      contains: "presentGraphExercise"
    - path: "test/features/letter_unit/copy_stimulus_test.dart"
      provides: "QP-03 RED test for writeWord.copy reveal→hide→peek state"
    - path: "test/features/letter_unit/prompt_header_slot_audio_test.dart"
      provides: "QP-04/05 RED test for the RTL slot box + large audio card"
    - path: "test/features/letter_unit/recall_no_model_test.dart"
      provides: "QP-06 assertion that recall writeLetter/writeWord configs carry no model/ghost part"
    - path: "test/curriculum/learned_letters_lint_test.dart"
      provides: "QP-07 RED lint over exercises.json letters ⊆ cumulative introOrder set"
    - path: "test/data/app_database_test.dart"
      provides: "QP-09 v6→v7 migration + two-profile isolation RED case (temp-file DB)"
      contains: "from < 7"
  key_links:
    - from: "test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart"
      to: "exercise_presenter.presentGraphExercise"
      via: "ProviderScope overrides + controller.start() live mount"
      pattern: "presentGraphExercise"
    - from: "test/data/app_database_test.dart"
      to: "temp-file NativeDatabase v6→v7"
      via: "Directory.systemTemp + PRAGMA user_version rewind"
      pattern: "NativeDatabase\\(.*File"
---

<objective>
Author the Wave-0 RED contract for Phase 19: a failing automated test for every locked
behavior across all three tracks (presentation, content/lint, keying migration), so the
implementation plans (19-02..19-06) turn them GREEN with zero test edits (the established
18-01/15-01/09-01 Wave-0 pattern). No production code changes here — the tests are RED by
missing symbol / by new assertion.

Purpose: Nyquist compliance — every downstream `<verify>` must reference a real automated
check that exists before the code. The instruction-bar test is authored on the LIVE mount
path (`presentGraphExercise`) because a bare-scaffold test would repeat the Phase-15
dead-wire failure. The migration test is authored on a temp-FILE database because a shared
in-memory executor cannot exercise the real `onUpgrade` path (v3→v4 precedent).
Output: 5 new test files, all RED, + one extended migration case authored complete but
skip-marked (`skip: 'v6→v7 lands in 19-06 (QP-09)'`) so intermediate plans run
`app_database_test.dart` green; 19-06 un-skips it.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-CONTEXT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-PATTERNS.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-VALIDATION.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Presentation RED tests (Track A — QP-01..QP-06)</name>
  <files>test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart, test/features/letter_unit/copy_stimulus_test.dart, test/features/letter_unit/prompt_header_slot_audio_test.dart, test/features/letter_unit/recall_no_model_test.dart</files>
  <read_first>
    - test/features/letter_unit/agent_pick_live_path_test.dart (THE live-path mount analog — `_pumpStarted` lines 150-192: ProviderScope overrides for appDatabaseProvider/graphPositionRepositoryProvider/curriculumGraphProvider/childModelProvider/ttsCoachSpeakerProvider=NoopTtsCoachSpeaker, mount ExerciseScaffold, drive controller.start(), the epoch-tolerant `_graphNode` finder lines 264-268)
    - test/features/letter_unit/prompt_header_test.dart (widget-test analog for PromptHeader parts)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the render path; `_hasInstruction` 234-238, `_speakInstructionThenRelease` 276-299, `_HearAgainCta` 730-739/1089-1133, `ExerciseScaffoldStrings` 52-83)
    - lib/features/letter_unit/widgets/prompt_header.dart (`_AudioPart` 140-188, `_GapWord`/`_GapLetter` 426-461, `_TextPart` reveal path 351-423, `_visuals` say-filter 63-64)
    - lib/models/exercise.dart (Exercise/PromptPart/TextPart/Surface/Gap shapes; `exercise.type`)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-UI-SPEC.md (§1-§5 contracts + Copywriting per-type template table)
  </read_first>
  <action>
    Author four RED widget tests, all RED-by-missing-symbol against not-yet-built code.

    (1) `exercise_scaffold_instruction_bar_test.dart` (QP-01/02) — MOUNT VIA THE LIVE PATH:
    copy the `agent_pick_live_path_test.dart` `_pumpStarted` harness verbatim (same
    ProviderScope overrides incl. `ttsCoachSpeakerProvider` overridden to a spy over
    `NoopTtsCoachSpeaker` that records `speak`/`stop` calls). Assert: for a graded
    non-teachCard exercise the scaffold renders a persistent instruction strip whose text
    equals the per-type template (e.g. `completeWord` → "Write the missing letter",
    `writeWord.copy` → "Copy the word") sourced from `exercise.type`, NOT from the `say`
    line; assert exactly ONE replay affordance exists (no `_HearAgainCta`/"Hear again" pill
    ALONGSIDE the bar — find zero of the old pill, one tappable bar); tapping the bar invokes
    the recorded TTS `speak` (re-hear). Assert the bar is HIDDEN on a `teachCard`
    (`_hasInstruction` false). Use a `find.byKey(Key('instructionBar'))` contract the widget
    must satisfy.

    (2) `copy_stimulus_test.dart` (QP-03) — pump the new `CopyStimulus` widget (from
    `lib/features/letter_unit/widgets/copy_stimulus.dart`, not yet created). Assert three
    states per UI-SPEC §4: word visible (40px Arabic) + "I'm Ready" button in `revealed`;
    tapping "I'm Ready" hides the word (word finder gone / card blank) → `hidden`; a "Peek"
    button re-reveals → `peeking`; a "Hide" toggle returns to hidden. Assert NOTHING hides on
    a timer (pump a long duration with no tap → word stays in its current state).

    (3) `prompt_header_slot_audio_test.dart` (QP-04/05) — extend PromptHeader coverage.
    For `completeWord`/`fillBlank`: assert the word renders at full stimulus size with a
    highlighted slot box (`find.byKey(Key('gapSlot'))`) at the gap and assert the literal
    strings `__blank__` and `_letter_` NEVER appear via `find.text` (they stay filtered in
    `_TextPart._tokens`). For a listen-and-write exercise (has an `AudioPart`): assert the
    audio stimulus renders as a LARGE card (min-height ≥96, `find.byKey(Key('audioCard'))`)
    with a "Listen" label, distinct from the instruction bar's replay; assert auto-play fired
    once on mount (spy the audio seam / TTS fallback).

    (4) `recall_no_model_test.dart` (QP-06) — a data/widget assertion: load `exercises.json`,
    select `writeLetter`/`writeWord` RECALL configs (no `AudioPart`, no reveal), and assert
    they carry NO model/ghost surface part (no faint-glyph model part) — the remediation arc
    is the only hint path (D-08). Assert at least one recall config exists so the test is not
    vacuously green.

    Every finder above (`instructionBar`, `gapSlot`, `audioCard`, `CopyStimulus`) is the
    contract the implementer must satisfy — do not invent symbols the impl can dodge. Pull
    the per-type template strings verbatim from UI-SPEC Copywriting Contract so 19-02 greens
    them with zero edits. Do NOT re-bake any golden (font drift is pre-existing).
  </action>
  <verify>
    <automated>flutter test test/features/letter_unit/exercise_scaffold_instruction_bar_test.dart test/features/letter_unit/copy_stimulus_test.dart test/features/letter_unit/prompt_header_slot_audio_test.dart test/features/letter_unit/recall_no_model_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist and FAIL (RED) — compile-error or assertion failure by missing symbol/behavior, never skipped or vacuously green
    - `exercise_scaffold_instruction_bar_test.dart` contains a `presentGraphExercise`-based mount (grep: `presentGraphExercise`) and asserts exactly ONE replay affordance
    - The instruction-bar test asserts the bar text equals a per-type template (e.g. "Write the missing letter") and NOT the `say` line
    - `prompt_header_slot_audio_test.dart` asserts `find.text('__blank__')` and `find.text('_letter_')` are both `findsNothing`
    - `recall_no_model_test.dart` asserts ≥1 recall config exists AND none carry a model/ghost part
  </acceptance_criteria>
  <done>Four RED presentation tests exist; running them fails by missing symbol/behavior; the instruction-bar test is on the live `presentGraphExercise` path.</done>
</task>

<task type="auto">
  <name>Task 2: Content-lint + migration RED tests (Tracks B/C — QP-07, QP-09)</name>
  <files>test/curriculum/learned_letters_lint_test.dart, test/data/app_database_test.dart</files>
  <read_first>
    - test/curriculum/baa_signoff_test.dart (raw-JSON load + assert + carve-out pattern `_loadExercisesRaw()` 139-159 — the lint analog)
    - test/data/app_database_test.dart (the v3→v4 temp-file migration test 118-185: temp dir 122-124, seed current schema, DROP + `PRAGMA user_version = N` rewind 137-139, fresh NativeDatabase executor per restart 145-172, idempotence restart 174-185)
    - lib/data/app_database.dart (six progress tables + PKs: LetterMastery 39-46, LetterReps 73-80, LetterGraphPosition 98-107, LetterExerciseReps 122-130, ArcStateRows 161-174, LetterCriterionEvidence 143-150; ChildProfiles 54; schemaVersion 236; onUpgrade 239-277)
    - assets/curriculum/exercises.json (the `letters` field per exercise; the 7 offending baa cards enumerated in RESEARCH Card Rewrite Content Model)
    - assets/curriculum/letters.json (`introOrder`: alif=1, baa=2, taa=3, thaa=4)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md (Card Rewrite Content Model table; Pitfall 1/2 for the migration test)
  </read_first>
  <action>
    Author two RED tests.

    (1) `test/curriculum/learned_letters_lint_test.dart` (QP-07/D-12) — copy the raw-JSON
    load shape from `baa_signoff_test.dart`. Read `exercises.json` + `letters.json` raw (no
    model parse). Rank letters by `letters.json` `introOrder` (A2: alif=1, baa=2 is the
    canonical rank, NOT the classical ابجد list). For the baa unit (learned-set =
    {alif, baa}, introOrder ≤ 2), assert every baa exercise's `letters` array ⊆ the learned
    set, with a documented carve-out list for cards that will be GATED out (D-19) rather than
    rewritten. Author the assertion so it is RED TODAY (the 7 offending cards —
    `baa.connectWord.kitaab`, `baa.transformWord.{dual,plural,opposite}`,
    `baa.fillBlank.adjective`, `baa.buildSentence.{hear,picture}` — currently violate it and
    are not yet gated/rewritten). Encode the disposition contract so 19-05 greens it by
    gating/rewriting exactly those cards.

    (2) Extend `test/data/app_database_test.dart` (QP-09/D-14/D-16) — add a v6→v7 migration +
    two-profile isolation case using a temp-FILE `NativeDatabase` (Pitfall 2 — never shared
    in-memory). Seed a v6 database file with the CURRENT schema, rewind to `user_version = 6`,
    write progress rows for one profile (a `LetterGraphPosition`, a `LetterExerciseReps`, a
    `LetterMastery`, an `ArcStateRows`), then open a fresh `AppDatabase` over the same file to
    trigger `onUpgrade` to 7. Assert (a) the adopted rows survive keyed to the current
    `ChildProfiles.id` (D-16, no progress loss); (b) a SECOND profile id reads NULL/clean
    position (success criterion 4 — fresh profile starts at the opening); (c) the legacy
    `LetterReps` table is gone after v7 (D-15). Author the case with an explicit skip marker:
    `skip: 'v6→v7 lands in 19-06 (QP-09)'` — the case body is complete and would be RED, but
    the skip keeps `app_database_test.dart` green for the intermediate plans that run the whole
    file (19-04's verify). 19-06 removes the skip as its un-skip step (the ONLY test edit it is
    permitted). Follow the exact temp-file / DROP / `PRAGMA user_version` rewind idiom from the
    v3→v4 test.
  </action>
  <verify>
    <automated>flutter test test/curriculum/learned_letters_lint_test.dart test/data/app_database_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `learned_letters_lint_test.dart` exists and FAILS today, flagging the 7 enumerated offending baa cards
    - The lint ranks by `letters.json` `introOrder` (grep: `introOrder`), NOT a hardcoded classical order
    - `app_database_test.dart` contains a new v6→v7 case that uses a temp-file NativeDatabase (grep in the new case: `systemTemp` or a `File(` executor) and `PRAGMA user_version`
    - The migration case asserts both profile-A rows survive AND a second profile reads clean, AND `LetterReps` is absent post-v7 — with a complete body and the exact marker `skip: 'v6→v7 lands in 19-06 (QP-09)'` (source assertion), so `flutter test test/data/app_database_test.dart` exits 0 whole-file
  </acceptance_criteria>
  <done>The lint test exists and fails on the 7 offending cards; the migration case exists complete-but-skip-marked on a temp-file database, keeping app_database_test.dart green until 19-06 un-skips it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → repo | Wave-0 tests are read-only over source/assets; no runtime input crosses a network or persistence boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-W0 | Information disclosure | migration two-profile RED test | mitigate | The test encodes the cross-profile-leak invariant (profile B reads clean) so 19-06 cannot green it without real isolation |
| T-19-02-W0 | Denial of service (progress loss) | migration backfill RED test | mitigate | The test asserts adopted rows survive; 19-06 cannot green it while losing data |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: "No new external packages") — supply-chain surface unchanged |
</threat_model>

<verification>
Run `flutter test test/features/letter_unit/ test/curriculum/learned_letters_lint_test.dart test/data/app_database_test.dart` — the new/extended cases are RED (expected). The rest of the suite is unaffected except the documented pre-existing goldens (alif_reference, mastery/glyph) — do NOT re-bake them.
</verification>

<success_criteria>
Five new/extended test files exist, each FAILING for the right reason (missing symbol / new assertion), the instruction-bar test is on the live `presentGraphExercise` path, and the migration test is a temp-file test. `nyquist_compliant` can flip true once these back every downstream `<verify>`.
</success_criteria>

<output>
Create `.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-01-SUMMARY.md` when done.
</output>
