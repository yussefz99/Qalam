---
phase: 19-question-presentation-overhaul-every-question-self-explanato
plan: 05
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - assets/curriculum/curriculum_graph.json
  - assets/curriculum/exercises.json
  - server/app/curriculum_data/curriculum_graph.json
  - server/app/curriculum_data/exercises.json
  - test/curriculum/learned_letters_lint_test.dart
  - test/curriculum/baa_signoff_test.dart
  - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md
autonomous: true
requirements: [QP-07, QP-08]
must_haves:
  truths:
    - "The three authored baa micro-drill nodes (dot/bowl/start) are back in the live curriculum graph and microdrill_selection_test stays green"
    - "The baa unit's cards use only learned letters (alif+baa by introOrder); the 4-5 cards that fundamentally need unlearned letters are removed from the baa graph's reachable set and filed for a later letter's unit"
    - "A learned-letters lint fails the build if any baa exercise's letters exceed the cumulative introOrder set (minus documented gated carve-outs)"
    - "The mother's review packet exists covering the 7 offending cards with current content, rendering, a drafted rewrite, and a rewrite-vs-gate recommendation"
  artifacts:
    - path: "assets/curriculum/curriculum_graph.json"
      provides: "microDrill competency + 3 drill nodes re-added (D-18); the 4-5 offending nodes removed (D-19)"
      contains: "microDrill"
    - path: "test/curriculum/learned_letters_lint_test.dart"
      provides: "QP-07 lint enforcing letters ⊆ cumulative introOrder set"
    - path: ".planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md"
      provides: "D-10 mother's one-sitting review packet for the 7 cards (non-blocking human gate)"
  key_links:
    - from: "assets/curriculum/curriculum_graph.json"
      to: "server/app/curriculum_data/curriculum_graph.json"
      via: "generate.py re-derive (never hand-edit the server copy)"
      pattern: "regenerate"
    - from: "learned_letters_lint_test"
      to: "exercises.json letters + letters.json introOrder"
      via: "raw-JSON load + introOrder rank assertion (A2)"
      pattern: "introOrder"
---

<objective>
Land the content track: D-18 (re-add the 3 authored baa micro-drill nodes to the live graph),
D-12/D-19 (the learned-letters rule enforced by a lint + the 4-5 cards that need unlearned
letters removed from the baa graph and filed for later units), D-09 (draft rewrites for the
rewritable card(s), shipped `signedOff:false`), and D-10/D-21 (the mother's one-sitting review
packet). After every content edit, re-derive the server copies via `generate.py`.

Purpose: the first unit becomes the authoring template Phases 20-21 inherit — a child is never
shown a card demanding a letter they have not learned, micro-drills return behind the reworked
presentation, and the mother's sign-off is a decoupled non-blocking gate (rewrites ship
provisional now, she signs whenever available — the deadline never waits on her sitting, D-11).
Output: edited graph + exercises assets, re-derived server copies, the lint green, the
baa_signoff invariant carved out, and the review packet artifact.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-CONTEXT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-add the 3 micro-drill nodes to the live graph (D-18)</name>
  <files>assets/curriculum/curriculum_graph.json</files>
  <read_first>
    - assets/curriculum/curriculum_graph.json (competency shape 37-58 `{id, essential, prerequisites[]}`; node shape 60-64 `{exerciseId, competency, tier, minCleanReps}`; the `_meta.microDrills` note and the `owner_amendment_2026_07_12` note at the head recording that the nodes were PARKED/removed)
    - test/tutor/microdrill_selection_test.dart (`loadGraph()` fixture 50-83 that ONLY adds the drill nodes IF absent — re-adding them to the live asset keeps it green; the node fixture shape at 60-81 to mirror byte-for-byte)
    - assets/curriculum/exercises.json (the microDrill exercise configs already exist at 2080-2199: `baa.microDrill.dot` with `criteria:["dot"]` + `spotlightZone:"dot"`, `baa.microDrill.bowl`, `baa.microDrill.start`, all `signedOff:false` — D-18 re-adds only the GRAPH NODES, not the configs)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md (Code Examples — the 3 node blocks: microDrill competency `{"id":"microDrill","essential":false,"prerequisites":[]}` + nodes dot→dot, bowl→shape, start→strokeOrder, tier:null, minCleanReps:1, essential:false)
  </read_first>
  <action>
    Add the `microDrill` competency `{"id":"microDrill","essential":false,"prerequisites":[]}`
    to `competencies[]` and the three drill nodes to `nodes[]`, mirroring the
    `microdrill_selection_test.dart` fixture (60-81) byte-for-byte: `baa.microDrill.dot`
    (competency microDrill, tier null, minCleanReps 1, criterion dot, essential false),
    `baa.microDrill.bowl` (criterion shape), `baa.microDrill.start` (criterion strokeOrder).
    These are ADDITIVE enrichment nodes that never gate the star (D-06, essential:false). Update
    the `_meta.microDrills` note and the `owner_amendment_2026_07_12` note to record that the
    nodes are RESTORED (behind the reworked presentation + 18-10 SpotlightOverlay). Do NOT flip
    the file-level `signedOff` (the 15-07 tier-structure sign-off stays; the drill CONTENT stays
    `signedOff:false` at the exercise level in exercises.json — its sign-off is the mother's
    packet gate). Do NOT touch the exercise configs. Do NOT re-run generate.py here — Task 2
    makes the final content edits and re-derives once.
  </action>
  <verify>
    <automated>flutter test test/tutor/microdrill_selection_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `curriculum_graph.json` contains the `microDrill` competency + the 3 drill nodes (grep: three `baa.microDrill.` node entries)
    - `microdrill_selection_test.dart` passes with ZERO test edits (the live graph now supplies the nodes the fixture used to add)
    - The file-level `signedOff` is unchanged; the drill exercise configs in exercises.json are untouched
    - The `_meta` notes record the restore
  </acceptance_criteria>
  <done>The 3 authored micro-drill nodes are back in the live graph, the selection test is green, and the file-level sign-off is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Learned-letters lint green + gate/rewrite offending cards + re-derive server (D-12/D-19/D-09)</name>
  <files>assets/curriculum/curriculum_graph.json, assets/curriculum/exercises.json, server/app/curriculum_data/curriculum_graph.json, server/app/curriculum_data/exercises.json, test/curriculum/learned_letters_lint_test.dart, test/curriculum/baa_signoff_test.dart</files>
  <read_first>
    - test/curriculum/learned_letters_lint_test.dart (the RED lint from 19-01 — the assertion + the documented gated carve-out list to satisfy)
    - test/curriculum/baa_signoff_test.dart (the sign-off invariant + carve-out block 137-172 — microDrills + baa.traceLetter.final are already carved out; extend the carve-out for gated/rewritten cards, same shape, Pitfall 8)
    - assets/curriculum/exercises.json (the 7 offending cards with their `letters` fields: `baa.connectWord.kitaab` [kaaf,taa,alif,baa], `baa.transformWord.dual` [baa,alif,noon], `baa.transformWord.plural` [alif,baa,waaw], `baa.transformWord.opposite` [saad,ghayn,yaa,raa], `baa.fillBlank.adjective` [kaaf,baa,yaa,raa], `baa.buildSentence.hear` + `baa.buildSentence.picture` [alif,laam,baa,kaaf,yaa,raa]; the microDrill block signedOff:false at 2114 for the rewrite-provisional shape)
    - assets/curriculum/letters.json (`introOrder` alif=1 baa=2 — the canonical rank, A2)
    - assets/curriculum/curriculum_graph.json (the reachable node set — removing an offending node = drop it from nodes[] so it is unreachable in the baa unit's cleared state; A6/D-19: no cross-unit-prerequisite schema field)
    - server/app/curriculum_data/generate.py (`regenerate()` at 38 — reads the two client assets, writes the server copies filtered to baa.*; run `cd server && uv run python -m app.curriculum_data.generate`)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md (Card Rewrite Content Model table — per-card disposition: kitaab rewrite-or-gate; dual/plural/opposite/adjective/hear/picture gate to later units)
  </read_first>
  <action>
    Apply the D-19 dispositions and green the lint. For each of the 7 offending cards, follow the
    RESEARCH Card Rewrite Content Model recommendation (final disposition is the mother's in Task
    3's packet, but the deadline-critical CODE state is set now): GATE the cards that fundamentally
    need unlearned letters — `baa.transformWord.{dual,plural,opposite}`, `baa.fillBlank.adjective`,
    `baa.buildSentence.{hear,picture}` — by REMOVING their nodes from `curriculum_graph.json`
    `nodes[]` so they are unreachable in the baa unit's cleared set (D-19; file them for the target
    letter's unit in a code comment / the review packet — no cross-unit schema field, A6). For
    `baa.connectWord.kitaab`, draft a rewrite to an alif+baa word (e.g. باب) — edit
    `text`/`expected`/`letters`/`feedback` in `exercises.json` and ship `signedOff:false` (the
    mother confirms rewrite-vs-gate in Task 3). Update the 19-01 `learned_letters_lint_test.dart`
    carve-out list to match exactly the gated cards so the lint is GREEN (every remaining baa
    exercise's `letters` ⊆ {alif,baa} by introOrder; gated cards excluded, rewritten cards now
    conform). Extend `baa_signoff_test.dart`'s carve-out (137-172) to include the rewritten/gated
    cards (Pitfall 8 — same shape as the microDrills + baa.traceLetter.final carve-out). Then
    re-derive BOTH server copies via `generate.py` (never hand-edit them). Do NOT set
    `signedOff:true` on any rewritten card (only the mother flips content sign-off).
  </action>
  <verify>
    <automated>cd server && uv run python -m app.curriculum_data.generate && cd .. && flutter test test/curriculum/ test/tutor/microdrill_selection_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `learned_letters_lint_test.dart` is GREEN: every non-gated baa exercise's `letters` ⊆ {alif,baa} by introOrder; the gated cards are in the documented carve-out
    - The 5-6 gated nodes are absent from `curriculum_graph.json` `nodes[]` (grep: no `transformWord.dual`/`plural`/`opposite`, `fillBlank.adjective`, `buildSentence.hear`/`picture` nodes)
    - Any rewritten card (e.g. kitaab→باب) carries `signedOff:false` (source assertion: no rewritten card is `signedOff:true`)
    - `baa_signoff_test.dart` is GREEN with the extended carve-out; `microdrill_selection_test.dart` stays green
    - The server `curriculum_data/*.json` are re-derived (identical to what `generate.py` produces — no hand edits; the diff matches a fresh regenerate run)
  </acceptance_criteria>
  <done>The lint is green, offending cards are gated/rewritten (rewrites signedOff:false), baa_signoff is carved out, and the server copies are re-derived via generate.py.</done>
</task>

<task type="auto">
  <name>Task 3: Mother's review packet artifact (D-10/D-21)</name>
  <files>.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md</files>
  <read_first>
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md (Card Rewrite Content Model table — the 7 cards, their `letters`, unlearned letters, and per-card rewrite-vs-gate recommendation; the intro-order source note; A5 — confirm the owner's № 10, 15-20 list agrees with the lint's 7-card flag set)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-CONTEXT.md (D-09/D-10/D-11/D-21 — the packet's purpose, the non-blocking framing, the № identity resolution)
    - assets/curriculum/exercises.json (the current content of the 7 cards to quote in the packet)
    - the 15-07 / 17-10 sign-off packet precedent (how a mother-sign-off record is structured, e.g. .planning/phases/15-.../15-HUMAN-UAT.md)
  </read_first>
  <action>
    Write `19-REVIEW-PACKET.md` — the mother's one-sitting review artifact (D-10). For EACH of the
    7 offending cards present: (1) its exercise id + current content (text/expected/letters/feedback
    quoted from exercises.json); (2) a plain description of its on-screen rendering under the new
    presentation; (3) the drafted rewrite (if any — e.g. kitaab→باب) OR the gate disposition
    applied in Task 2; (4) a clear rewrite-vs-gate RECOMMENDATION with the honest-alif+baa-word
    reasoning. Add a header note that this is NON-BLOCKING (the code already ships the drafts
    `signedOff:false`; her edits/sign-off land whenever she is available — D-11) and that her
    changes land as `exercises.json` edits + `signedOff` flips (15-07/17-10 pattern). Add the A5
    reconciliation section: map the owner's card-order shorthand "№ 10, 15-20" against the lint's
    7-card flag set and note any card in the owner's list that the `letters` field marks clean (to
    reconcile in the sitting). This is a documentation artifact — no code, no `signedOff` flips
    here (the mother owns those).
  </action>
  <verify>
    <automated>test -f .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md && grep -c "baa\." .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md</automated>
  </verify>
  <acceptance_criteria>
    - `19-REVIEW-PACKET.md` exists and covers all 7 offending cards (each with current content + rendering + drafted rewrite/gate + recommendation)
    - The packet carries the NON-BLOCKING header note and the 15-07/17-10 sign-off-flip instruction
    - The A5 № 10, 15-20 ↔ lint-flag-set reconciliation section is present
    - No `signedOff:true` flips occur in this task (the mother owns content sign-off)
  </acceptance_criteria>
  <done>The mother's review packet exists, covers the 7 cards with dispositions + recommendations, is framed non-blocking, and reconciles the owner's № numbering with the lint flag set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curriculum asset → load-time validator + lint | Content is validated at load (validateLetter/validateReferenceStrokes) and now the learned-letters lint; a bad edit fails a test, never reaches a child |
| client asset → server copy | `generate.py` re-derives the stateless server rail; the server copy is never hand-edited (drift = a rail mismatch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | (pedagogical correctness — V5 input validation) | baa card content | mitigate | The D-12 learned-letters lint fails the build if any baa exercise demands an unlearned letter; the 7 offending cards are gated/rewritten |
| T-19-09 | Tampering | rewritten card content shipped as final | mitigate | Rewrites ship `signedOff:false`; only the mother flips content sign-off (packet gate); baa_signoff carve-out keeps the invariant honest |
| T-19-10 | Integrity | server rail mismatch | mitigate | Re-derive server `curriculum_data/*.json` via generate.py after every content edit; never hand-edit |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |
</threat_model>

<verification>
Run `flutter test test/curriculum/ test/tutor/microdrill_selection_test.dart` — the lint and
baa_signoff are green, micro-drill selection stays green. Confirm the server copies match a fresh
`generate.py` run (no hand edits). The review packet is the non-blocking human gate (mother's
sitting); it does not block the deadline.
</verification>

<success_criteria>
Micro-drills are back in the live graph (selection green); the baa unit's cards use only learned
letters (lint green, offending cards gated/rewritten signedOff:false); the server rail is
re-derived; and the mother's review packet exists for her later, non-blocking sitting.
</success_criteria>

<output>
Create `.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-05-SUMMARY.md` when done.
</output>
