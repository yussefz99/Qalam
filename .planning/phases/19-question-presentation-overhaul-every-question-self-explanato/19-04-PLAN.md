---
phase: 19-question-presentation-overhaul-every-question-self-explanato
plan: 04
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - lib/data/app_database.dart
  - lib/data/progress_repository.dart
  - lib/data/drift_progress_repository.dart
  - lib/providers/parent_providers.dart
  - lib/providers/practice_providers.dart
  - lib/providers/progression_providers.dart
  - test/providers/progression_providers_test.dart
autonomous: true
requirements: [QP-09]
must_haves:
  truths:
    - "All three live LetterReps reader sites (parent dashboard in-progress, practice provider clean-reps, journey progression ribbon) read from LetterExerciseReps aggregates — LetterReps is no longer on any live read/write path"
    - "The journey progression ribbon still shows the same clean-reps value it showed before the fold (behavior-preserving)"
    - "The LetterReps table still exists (unused) so 19-06 can drop it in the v6→v7 migration after this fold lands"
  artifacts:
    - path: "lib/data/app_database.dart"
      provides: "LetterExerciseReps aggregate accessors (per-letter clean-reps + in-progress list) that replace the LetterReps readers"
      contains: "ExerciseReps"
    - path: "lib/data/progress_repository.dart"
      provides: "Repository interface methods for the folded per-letter aggregate reads/writes"
  key_links:
    - from: "progression_providers watchLetterCleanReps"
      to: "LetterExerciseReps aggregate"
      via: "_bindDriftStream bridge (never StreamProvider.future — Pitfall 5)"
      pattern: "_bindDriftStream"
    - from: "parent_providers in-progress"
      to: "LetterExerciseReps aggregate"
      via: "allInProgressByExerciseReps()"
      pattern: "ByExerciseReps"
---

<objective>
Prepare D-15 (retire the legacy `LetterReps` counter) by folding its three live reader/writer
sites onto `LetterExerciseReps` aggregates BEFORE 19-06 drops the table — the fixed execution
order the phase mandates (fold readers first, drop table second, in the same v6→v7 story).
This plan does NOT change the schema version or PKs and does NOT drop the table: it re-points
the parent dashboard in-progress list, the practice-provider clean-reps read/write-through, and
the journey progression ribbon `watchCleanReps` onto new `LetterExerciseReps`-derived accessors,
behavior-preserving, so the app stays green with `LetterReps` present-but-unused.

Purpose: one way to count reps (`LetterExerciseReps`), not two — and a safe, testable increment
so 19-06's DROP cannot break the dashboard or the ribbon. RESEARCH flags this fold as the
highest deadline-risk sub-item; isolating it as its own plan de-risks the migration.
Output: the aggregate accessors + the three re-pointed readers + updated provider tests.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-CONTEXT.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LetterExerciseReps aggregate accessors</name>
  <files>lib/data/app_database.dart, lib/data/progress_repository.dart, lib/data/drift_progress_repository.dart</files>
  <read_first>
    - lib/data/app_database.dart (LetterReps accessors to REPLACE: `setCleanReps`/`getCleanReps` 392-408, `watchCleanReps` 419-423, `allInProgress` 644-646; the LetterExerciseReps accessors to aggregate over: `getExerciseCleanReps`/`setExerciseCleanReps`/`incrementExerciseCleanReps`/`exerciseCleanRepsFor` 485-522; LetterExerciseReps table 122-130)
    - lib/data/progress_repository.dart (the interface: `setCleanReps` 31, `getCleanReps` 40, `watchCleanReps` 53 — the methods to fold)
    - lib/data/drift_progress_repository.dart (the delegations 33-46)
    - lib/curriculum (how essential exercises per letter are enumerated — the aggregate must define per-letter clean-reps as a stable function over the letter's LetterExerciseReps rows, e.g. essential-floor or max; mirror how isMasteryMet reads essential nodes so the ribbon value stays consistent)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md (Pitfall 3 — the three reader sites; Pitfall 5 — Riverpod stream pause)
  </read_first>
  <behavior>
    - Test: `watchLetterCleanReps(letterId)` emits the same clean-reps value the old `watchCleanReps` did for an equivalent seeded LetterExerciseReps state; emits 0 for an unpracticed letter
    - Test: `allInProgressByExerciseReps()` returns the set of letters with ≥1 exercise clean-rep (matching the old `allInProgress` semantics)
  </behavior>
  <action>
    Add per-letter aggregate accessors over `LetterExerciseReps` that reproduce the three
    `LetterReps` reads without touching the schema. Define a single documented aggregation rule
    for "the letter's clean-reps" over its `LetterExerciseReps` rows (choose the rule that
    matches what the journey ribbon showed before — an essential-node floor or max across the
    letter's exercises; state the choice in a comment and keep it consistent with `isMasteryMet`
    essential-node reads). Add: `watchLetterCleanReps(String letterId) → Stream<int>` (aggregate
    stream), `letterCleanReps(String letterId) → Future<int>` (one-shot), and
    `allInProgressByExerciseReps()` (letters with ≥1 exercise clean-rep, matching `allInProgress`
    semantics). Surface these on the `ProgressRepository` interface + `DriftProgressRepository`
    delegation. Do NOT remove the old `LetterReps` accessors yet (19-06 drops them with the
    table). Never read a Drift stream via `StreamProvider.future` — the ribbon consumer uses the
    existing `_bindDriftStream` bridge in the next task (Pitfall 5). Regenerate
    `app_database.g.dart` (`dart run build_runner build`) — no schema change, so the version
    stays 6.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/data/app_database_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `watchLetterCleanReps`, `letterCleanReps`, `allInProgressByExerciseReps` exist on the DB + repository interface + drift impl (grep: `ByExerciseReps` and `watchLetterCleanReps`)
    - build_runner regenerates cleanly; schemaVersion is still 6 (source assertion: `schemaVersion => 6`)
    - The aggregate rule is documented in a comment and matches the essential-node reads used by isMasteryMet
    - `flutter test test/data/app_database_test.dart` passes whole-file — the v6→v7 case is skip-marked by 19-01 (`skip: 'v6→v7 lands in 19-06 (QP-09)'`) until 19-06 un-skips it; do NOT weaken/remove that case to get green
  </acceptance_criteria>
  <done>Aggregate accessors over LetterExerciseReps exist on the DB + repository, build_runner is clean, schema unchanged, and the old LetterReps accessors remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-point the three LetterReps readers + fold the write-through</name>
  <files>lib/providers/parent_providers.dart, lib/providers/practice_providers.dart, lib/providers/progression_providers.dart, test/providers/progression_providers_test.dart</files>
  <read_first>
    - lib/providers/parent_providers.dart (`allInProgress()` read at 119 → the dashboard "in progress" list; the hand-written `parentProgressProvider` assembly 113-135 — keep hand-written, it reads Drift row types, Pitfall 6)
    - lib/providers/practice_providers.dart (`getCleanReps` 164, `setCleanReps` 200, the D-10/D-20 write-through `_persistCleanReps` ~194-204, the "write to LetterReps on every change" note ~239)
    - lib/providers/progression_providers.dart (`watchCleanReps` reader 95; the `_bindDriftStream` bridge 43-64; the family notifier shape 86-98)
    - test/providers/progression_providers_test.dart (the assertions that reference `setCleanReps`/`watchCleanReps` — update to the folded accessor in lockstep)
    - lib/features/parent/parent_progress.dart (the dashboard view model that renders in-progress)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-RESEARCH.md (Pitfall 3 reader list; Pitfall 5 stream bridge; Pitfall 6 hand-written provider rule)
  </read_first>
  <behavior>
    - Test (progression): the journey ribbon provider emits the same clean-reps value via the new `watchLetterCleanReps` aggregate that it emitted via `watchCleanReps` for an equivalent seeded state
    - Test: the parent dashboard in-progress list is populated from `allInProgressByExerciseReps()` and matches the prior letters set
    - Test: the practice provider no longer writes to `LetterReps` (write-through targets LetterExerciseReps or is retired)
  </behavior>
  <action>
    Re-point all three LetterReps consumers onto the Task-1 aggregates, behavior-preserving.
    (a) `progression_providers.dart`: replace `watchCleanReps(letterId)` with
    `watchLetterCleanReps(letterId)` via the SAME `_bindDriftStream` bridge (never a bare
    `StreamProvider.future` — Pitfall 5); update `test/providers/progression_providers_test.dart`
    in lockstep (change the assertion to the folded accessor; RED-then-green within this task).
    (b) `parent_providers.dart`: replace `db.allInProgress()` at 119 with
    `db.allInProgressByExerciseReps()`; keep `parentProgressProvider` hand-written (Pitfall 6);
    the dashboard view model shape is unchanged. (c) `practice_providers.dart`: replace the
    `getCleanReps`/`setCleanReps` read + the D-10/D-20 write-through — if the practice provider
    is still on a live path, re-point its read to `letterCleanReps` and its write-through to the
    per-exercise `incrementExerciseCleanReps`/`setExerciseCleanReps`; if the read confirms it is
    legacy/dead (superseded by the graph-driven controller writing LetterExerciseReps), retire
    the write-through and document that in the SUMMARY. After this task NO live code path reads or
    writes `LetterReps` (the table stays present-but-unused for 19-06 to drop). If a
    parent-dashboard test references `allInProgress`, update it in lockstep too.
  </action>
  <verify>
    <automated>flutter test test/providers/ test/features/parent/ test/data/app_database_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep across `lib/providers/` shows NO live call to `watchCleanReps`/`getCleanReps`/`setCleanReps`/`allInProgress` (the LetterReps accessors) — every reader now uses the `*ExerciseReps`/`watchLetterCleanReps` aggregates
    - The progression ribbon read goes through `_bindDriftStream` (grep: `_bindDriftStream`), not `StreamProvider.future`
    - `test/providers/progression_providers_test.dart` and any parent-dashboard test are updated and GREEN with the folded accessors
    - The `LetterReps` table class still exists in `app_database.dart` (source assertion: `class LetterReps` present) — not dropped this plan
    - `flutter test test/providers/ test/features/parent/` passes
  </acceptance_criteria>
  <done>All three LetterReps readers + the write-through are folded onto LetterExerciseReps aggregates, the ribbon value is behavior-preserved, provider/dashboard tests are green, and LetterReps is present-but-unused for 19-06 to drop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider → on-device Drift | All reads/writes are local SQLite; no network, no wire fields (ADR-017 boundary preserved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Denial of service (progress loss) | reader fold | mitigate | Behavior-preserving aggregate rule + provider tests assert the ribbon/dashboard values match pre-fold; no data is deleted (LetterReps stays until 19-06) |
| T-19-03 | Information disclosure | folded accessors | accept | Aggregates read only local ids/counts; no `childProfileId` or PII enters any wire field (no `TutorFacts`/coach payload touched) |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan |
</threat_model>

<verification>
Run the quick suite `flutter test test/providers/ test/features/parent/ test/data/app_database_test.dart`
— folded reads are green, no regression in the dashboard/ribbon. `flutter analyze` clean. The
LetterReps table remains present-but-unused (verified by grep) so 19-06 can drop it safely.
</verification>

<success_criteria>
The three live LetterReps reader/writer sites read from LetterExerciseReps aggregates,
behavior-preserving; the journey ribbon and parent dashboard show the same values as before; no
live path touches LetterReps; the table is still present for 19-06 to drop.
</success_criteria>

<output>
Create `.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-04-SUMMARY.md` when done.
</output>
