---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - test/curriculum/learned_letters_lint_test.dart
autonomous: true
requirements: [QP-07, D-12]
must_haves:
  truths:
    - "The lint enforces the seen-letters bar on EVERY live letter regardless of signedOff — the draft exemption is gone (D-04, criterion 1)"
    - "The exception allowlist holds only mother-review-pending baa exceptions (the 4 D-09 ids, each with id + reason); taa/thaa carry no exceptions after triage"
    - "signedOff no longer controls enforcement anywhere; it means only 'the mother confirmed THIS content' (D-05)"
  artifacts:
    - path: "test/curriculum/learned_letters_lint_test.dart"
      provides: "the enforce-every-letter lint with the signedOff dispatch collapsed"
      min_lines: 150
      contains: "QP-07"
  key_links:
    - from: "test/curriculum/learned_letters_lint_test.dart::unlearnedFor"
      to: "tools/content/validate.py::unlearned_letters_for_exercise"
      via: "behavioral parity (both read stored letters[] vs introOrder)"
      pattern: "introOrder"
---

<objective>
L1 — collapse the lint's two-way `signedOff` dispatch so EVERY live letter unit is held
to the seen-letters bar (D-04/D-05). Today signed letters are ENFORCED and unsigned
letters are merely ACKNOWLEDGED (never fail); after this plan the enforcement is
identical for baa, taa, thaa, alif — `signedOff` is decoupled from enforcement.

Purpose: criterion 1's "the lint covers every letter with no draft exemption; the
allowlist holds only mother-approved exceptions". This runs AFTER Plan 02 triaged the
content to zero, so flipping the lint to enforce-all is GREEN, not red.
Output: the rewritten lint with a single enforcement path + a parity assertion vs the
Python predicate.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the signedOff dispatch — enforce every live letter</name>
  <files>test/curriculum/learned_letters_lint_test.dart</files>
  <read_first>
    - test/curriculum/learned_letters_lint_test.dart (the file being modified — the ENFORCED block L157–189, the ACKNOWLEDGED else L190–213, the header contract L11–22, `_discoverUnitGraphs` L55–93, `unlearnedFor` L127–135, `introOrder` build L107–110, the coverage assertion L216–225)
    - 25-CONTEXT.md D-04/D-05 (draft exemption removed; signedOff decoupled from enforcement)
    - 25-PATTERNS.md "learned_letters_lint_test.dart — L1" (L39–81): the exact branch to change, what to preserve verbatim, and the header-contract rewrite
  </read_first>
  <action>
    Delete the `if (unit.signedOff) { …ENFORCED… } else { …ACKNOWLEDGED… }` split
    (L157–213). Apply the ENFORCED block's logic (build the `learned` set, compute
    `violations` via `unlearnedFor`, `expect(violations, isEmpty, …)`) to ALL discovered
    units unconditionally — taa, thaa, alif now run the same violation assertion baa
    does. Preserve VERBATIM: `_discoverUnitGraphs()` (L55–93), the coverage assertion
    (L216–225), `unlearnedFor` (L127–135), the `introOrder` map (L107–110), and the
    baa-specific learned-set sanity pins (L164–169). Rewrite the header contract
    (L11–22) to the D-04/D-05 rule: `signedOff` no longer controls enforcement; every
    live letter is held to the bar; `signedOff` now means only "the mother confirmed
    THIS content." Keep the coverage assertion that both live letters are visited.
  </action>
  <acceptance_criteria>
    - `learned_letters_lint_test.dart` has NO `if (unit.signedOff)` enforcement branch and NO `printOnFailure` acknowledged path — grep shows neither `unit.signedOff` as a dispatch nor an `else` acknowledged block
    - The single `expect(violations, isEmpty, …)` assertion runs for every unit in `_discoverUnitGraphs()`
    - `flutter test test/curriculum/learned_letters_lint_test.dart` passes GREEN (Plan 02 triaged content to zero)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && ! grep -q 'if (unit.signedOff)' test/curriculum/learned_letters_lint_test.dart && flutter test test/curriculum/learned_letters_lint_test.dart</automated>
  </verify>
  <done>Every live letter is enforced by one assertion; the draft exemption is gone; the test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Scope the exception allowlist + assert predicate parity + confirm no other signedOff enforcement reader</name>
  <files>test/curriculum/learned_letters_lint_test.dart</files>
  <read_first>
    - test/curriculum/learned_letters_lint_test.dart L112–125 (`baaOwnerApprovedExceptions` — the 4 D-09 ids), L170–189 (the exception-application + the "no rot" liveness check)
    - tools/content/validate.py `unlearned_letters_for_exercise` (Plan 01) — the Python predicate this Dart lint must match behaviorally
    - 25-CONTEXT.md D-09 (the 4 baa exceptions are NOT mother-approved yet — the lint keeps them as the exception surface; the packet flips or re-points each) and D-10 (taa/thaa violators re-pointed/removed to zero — no exceptions)
    - 25-PATTERNS.md "signedOff is HUMAN sign-off, never an enforcement switch" (L340–346): verify NO other code reads signedOff as an enforcement gate
  </read_first>
  <action>
    Keep `baaOwnerApprovedExceptions` as exactly the 4 D-09 ids, each annotated with its
    reason (device-UAT origin + "pending mother approval in the Phase-25 packet"). Assert
    taa/thaa/alif carry NO exception set (empty) — after Plan 02 all their violators are
    re-pointed/removed, so an exception would be dead. Add a parity test case that, for a
    crafted card `{letters:['taa']}` at the baa unit, the Dart `unlearnedFor` returns a
    non-empty list AND documents (in a comment) that this matches
    `validate.py::unlearned_letters_for_exercise` — the wall's four-layer parity. Confirm
    via grep that no lib/ logic reads `signedOff` as an enforcement gate (parse-only reads
    in models/letter.dart, models/exercise.dart, curriculum_graph.dart are fine); record
    the grep result in the SUMMARY as the D-04/D-05 decoupling proof.
  </action>
  <acceptance_criteria>
    - The allowlist contains exactly the 4 D-09 baa ids, each with an inline reason comment; taa/thaa/alif exception sets are empty
    - A parity assertion covers the `{letters:['taa']}`-at-baa case
    - `grep -rn "signedOff" lib/ | grep -v '\.g\.dart'` shows only parse/construct reads (no enforcement branch) — recorded in SUMMARY
    - `flutter test test/curriculum/learned_letters_lint_test.dart` green
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/curriculum/learned_letters_lint_test.dart && test $(grep -c "baa.fillBlank.adjective\|baa.transformWord.dual\|baa.transformWord.plural\|baa.transformWord.opposite" test/curriculum/learned_letters_lint_test.dart) -ge 4</automated>
  </verify>
  <done>The allowlist holds only the 4 mother-pending baa exceptions with reasons; parity documented; signedOff proven decoupled from enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle content → the build gate | The lint is the CI-time guard that no bundled card demands an unseen letter. It is one of four parity layers; on its own it does not defend the Firestore-first path (that is L2/L3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-T | Tampering | a future letter slipping through unlinted | mitigate | `_discoverUnitGraphs` + the coverage assertion (preserved verbatim) fail if any live graph is not visited |
| T-25-03-R | Repudiation | an exception silently rotting (an allowlisted id no longer live) | mitigate | the existing "no rot" liveness check (L184–189) is preserved — every allowlisted id must be a live node |
| T-25-03-E | Elevation (of a draft to enforced-exempt) | signedOff being read as an enforcement bypass | mitigate | Task 2 greps lib/ to confirm signedOff is parse-only; the lint no longer dispatches on it (D-04/D-05) |
</threat_model>

<verification>
- `flutter test test/curriculum/learned_letters_lint_test.dart` green with every letter enforced.
- No `if (unit.signedOff)` enforcement branch remains.
- Allowlist = the 4 D-09 baa ids only; taa/thaa/alif have none.
</verification>

<success_criteria>
Criterion 1 (lint leg): the lint covers every letter with no draft exemption; the allowlist holds only mother-review-pending exceptions (id + reason each); signedOff is decoupled from enforcement.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-03-SUMMARY.md` when done — include the lib/ signedOff grep result as the decoupling proof.
</output>
