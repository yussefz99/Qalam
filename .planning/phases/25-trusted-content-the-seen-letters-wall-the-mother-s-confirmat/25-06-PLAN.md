---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 06
type: execute
wave: 4
depends_on: [25-01, 25-02, 25-03, 25-04, 25-05]
files_modified:
  - .planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-REVIEW-PACKET.md
  - assets/curriculum/graphs/baa.json
  - assets/curriculum/curriculum_graph.json
autonomous: true
requirements: [CUR-01]
must_haves:
  truths:
    - "The packet is a single walkthrough-ready checklist covering EVERY owner-directed change since the mother's last sign-off (D-13, criterion 4 scope)"
    - "Each row carries an inline confirm/reject/rework verdict slot the owner reads aloud and marks on the spot (D-11)"
    - "baa.json shows signedOff:false during assembly — honest state, since live baa diverged from her signed spec (D-14); byte-parity with curriculum_graph.json held"
  artifacts:
    - path: ".planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-REVIEW-PACKET.md"
      provides: "the per-item review checklist + summary table + inline verdict column"
      min_lines: 60
      contains: "confirm / reject / rework"
    - path: "assets/curriculum/graphs/baa.json"
      provides: "baa flipped to signedOff:false (honest divergence state)"
      contains: "signedOff"
  key_links:
    - from: "25-REVIEW-PACKET.md rows"
      to: "assets/curriculum/exercises.json _review notes"
      via: "each packet row mirrors the card's _review text (greppable diff)"
      pattern: "_review"
    - from: "assets/curriculum/graphs/baa.json"
      to: "assets/curriculum/curriculum_graph.json"
      via: "byte-parity after the signedOff flip"
      pattern: "signedOff"
---

<objective>
Leg 2, part 1 — assemble the mother's re-confirmation packet as ONE walkthrough-ready
checklist covering every owner-directed change since her last sign-off (D-13), and flip
baa to the honest `signedOff:false` state during assembly (D-14). Per D-12 this leg is
NON-BLOCKING: assembly completes without waiting for her; the walkthrough + ingestion is
Plan 07.

Purpose: criterion 4's coverage half — the packet must cover EVERY change, each with a
verdict slot. baa's flag is set to its honest state because live baa diverged from her
signed spec (minCleanReps forced 1 vs signed 3, both buildSentence removed, 4 exceptions)
— SAFE now that enforcement is decoupled from the flag (D-04/D-05).
Output: 25-REVIEW-PACKET.md + baa/curriculum_graph.json at signedOff:false (byte-parity).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the 25-REVIEW-PACKET.md walkthrough checklist (D-11/D-13)</name>
  <files>.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-REVIEW-PACKET.md</files>
  <read_first>
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-REVIEW-PACKET.md (the PRIMARY analog — per-item block: current content → what we did → recommendation; the summary table `| # | Card | Unlearned letters | Applied now | Recommendation |`; the "What you sign" close)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-HUMAN-UAT.md (the SECONDARY analog — the inline verdict/`expected:`/`result:` capture format + the Summary tally)
    - assets/curriculum/exercises.json (grep `_review` for the full re-point diff Plan 02 produced — D-08); the 4 D-09 baa exception ids (L556/L600/L644/L689); the baa buildSentence configs (`baa.buildSentence.hear` L736, `.picture` L780); the new `alif.writeLetter.fromPicture` (L2091+)
    - .planning/phases/15-build-dynamic-grounded-exercise-selection-on-baa/15-HUMAN-UAT.md (baa's mother-signed spec: writing/tracing = 3 clean reps — the minCleanReps=1 divergence)
    - 25-02-SUMMARY.md (Plan 02's re-point/remove/forced-exception list — the exact diff to enumerate)
    - 25-CONTEXT.md D-11/D-13 (delivery + scope) + 25-PATTERNS.md "NEW: mother's packet" (L413–431)
  </read_first>
  <action>
    Create 25-REVIEW-PACKET.md merging the two precedents: 19-REVIEW-PACKET's per-item
    detail block PLUS a 19-HUMAN-UAT-style inline verdict column (`confirm / reject /
    rework: ___`) on every row. Cover EVERY D-13 item as its own row/block: (1)
    minCleanReps=1 across ALL graphs including baa's signed spec (name the 15-HUMAN-UAT
    divergence: signed 3 for writing/tracing → forced 1); (2) both baa buildSentence
    removals (baa.buildSentence.hear/.picture) + the taa/thaa buildSentence removals; (3)
    the alif letter-level shrink + the new alif.writeLetter.fromPicture draft (show the
    real card — أسد + lion art); (4) Lane-B image re-points / feedback rewordings (grep
    `_review`); (5) the full re-point diff from Plan 02 (D-08 — every changed word old→new);
    (6) the 4 D-09 baa exceptions (each must become mother-approved OR be re-pointed/removed).
    Each row mirrors the card's `_review` text so the doc and the JSON agree. Add a summary
    table + a "What you sign" close. Do NOT flip any signedOff in this task (Task 2 handles
    baa; Plan 07 handles ingestion).
  </action>
  <acceptance_criteria>
    - Every id that carries a `_review` note in exercises.json appears as a packet row (`grep '_review' exercises.json` ids ⊆ packet ids)
    - All six D-13 item groups are present, each with a `confirm / reject / rework: ___` slot
    - The 4 D-09 baa exception ids each appear with the explicit "needs mother approval or re-point/remove" framing
    - The doc renders as valid markdown with a summary table
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && P=.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-REVIEW-PACKET.md && grep -q "confirm / reject / rework" "$P" && for id in baa.fillBlank.adjective baa.transformWord.dual baa.transformWord.plural baa.transformWord.opposite alif.writeLetter.fromPicture; do grep -q "$id" "$P" || { echo "MISSING $id"; exit 1; }; done && echo "packet covers required ids"</automated>
  </verify>
  <done>The packet is one walkthrough checklist covering every change with a verdict slot per row.</done>
</task>

<task type="auto">
  <name>Task 2: Flip baa to the honest signedOff:false state (D-14), holding byte-parity</name>
  <files>assets/curriculum/graphs/baa.json, assets/curriculum/curriculum_graph.json</files>
  <read_first>
    - assets/curriculum/graphs/baa.json (`signedOff:true` today — the flag to flip)
    - assets/curriculum/curriculum_graph.json (baa canonical — MUST stay byte-parity)
    - test/curriculum/graph_asset_parity_test.dart (the parity guard)
    - 25-CONTEXT.md D-14 (flip false during assembly = honest state; reconfirm live → true per item in Plan 07; SAFE because enforcement is decoupled — D-04/D-05) + 25-PATTERNS.md L240–243
  </read_first>
  <action>
    Set `signedOff` to `false` in BOTH assets/curriculum/graphs/baa.json AND
    assets/curriculum/curriculum_graph.json (byte-parity) — the honest state, since live
    baa diverged from the spec the mother signed (15-HUMAN-UAT). Add a one-line note in the
    SUMMARY that this is intentional and temporary (Plan 07 flips confirmed items back to
    true); it is SAFE because the lint (Plan 03) no longer reads signedOff as an
    enforcement gate — baa stays fully enforced at signedOff:false. Change ONLY the
    signedOff field; do not touch nodes or minCleanReps here (those are the mother's call
    in Plan 07).
  </action>
  <acceptance_criteria>
    - `signedOff:false` in both graphs/baa.json and curriculum_graph.json
    - `flutter test test/curriculum/graph_asset_parity_test.dart` passes (byte-parity held)
    - `flutter test test/curriculum/learned_letters_lint_test.dart` STILL passes (baa enforced despite signedOff:false — proves D-04/D-05 decoupling)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python -c "import json; assert json.load(open('assets/curriculum/graphs/baa.json'))['signedOff'] is False; assert json.load(open('assets/curriculum/curriculum_graph.json'))['signedOff'] is False; print('baa signedOff:false')" && flutter test test/curriculum/graph_asset_parity_test.dart test/curriculum/learned_letters_lint_test.dart</automated>
  </verify>
  <done>baa is at its honest signedOff:false state, byte-parity held, still fully enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembly → the packet doc | The packet is planning documentation. Its only risk is completeness (a missed change) or leaking child data into a review doc. |
| the signedOff flip → enforcement | The flip is SAFE only because Plan 03 decoupled enforcement from signedOff; a false window that re-enabled violating content would be a regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-R | Repudiation | a change omitted from the packet (the mother never sees it) | mitigate | Task 1's greppable `_review`⊆packet check + enumerating all six D-13 groups |
| T-25-06-E | Elevation | the signedOff:false window silently un-enforcing baa | mitigate | Task 2's verify runs the lint AT signedOff:false and requires it green (enforcement decoupled — D-04/D-05) |
| T-25-06-I | Info disclosure | child PII/strokes in the packet | accept | the packet contains only curriculum content + change descriptions — no child identifiers, no strokes (assert in SUMMARY) |
</threat_model>

<verification>
- The packet covers every `_review` id + all six D-13 groups, each with a verdict slot.
- baa at signedOff:false, byte-parity + lint both green.
</verification>

<success_criteria>
Criterion 4 (assembly half): the packet covers every owner-directed change since her last sign-off, with an inline verdict slot per row; baa is in its honest divergence state ready for the walkthrough.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-06-SUMMARY.md` when done — confirm no child data in the packet and note the temporary baa false-window.
</output>
