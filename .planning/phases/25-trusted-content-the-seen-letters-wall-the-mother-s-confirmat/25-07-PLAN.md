---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 07
type: execute
wave: 5
depends_on: [25-06]
files_modified:
  - .planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-HUMAN-UAT.md
  - assets/curriculum/graphs/alif.json
  - assets/curriculum/graphs/baa.json
  - assets/curriculum/curriculum_graph.json
  - assets/curriculum/exercises.json
autonomous: false
requirements: [CUR-01]
must_haves:
  truths:
    - "Every packet item has a recorded verdict from the mother (confirm/reject/rework) in 25-HUMAN-UAT.md (criterion 4)"
    - "signedOff flags match her answers EXACTLY — confirmed items flip true, taa/thaa letters stay false (Phase 27), rejects are restored or re-worked to her instruction (criterion 4)"
    - "The ingestion path works same-session AND from the captured doc (D-12 async robustness)"
    - "If baa's graph changed (e.g. minCleanReps restored), a Cloud Run redeploy is owner-authorized explicitly or explicitly deferred — never silently assumed free"
  artifacts:
    - path: ".planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-HUMAN-UAT.md"
      provides: "the captured per-item verdicts + a Summary tally"
      contains: "verdict"
    - path: "assets/curriculum/graphs/baa.json"
      provides: "baa signedOff reflecting the mother's reconfirmation verdicts"
      contains: "signedOff"
  key_links:
    - from: "25-HUMAN-UAT.md verdicts"
      to: "assets/curriculum/graphs/*.json + exercises.json signedOff"
      via: "ingestion flips flags to match recorded verdicts exactly"
      pattern: "signedOff"
    - from: "assets/curriculum/curriculum_graph.json (a baa graph change)"
      to: "the Cloud Run tutor server"
      via: "owner-authorized redeploy gate (or explicit deferral)"
      pattern: "minCleanReps"
---

<objective>
Leg 2, part 2 — the live walkthrough (autonomous:false: every mother-verdict step is
gated). The owner reads each packet row to the mother, captures her confirm/reject/rework
verdict, then the verdicts are ingested: confirmed items flip `signedOff:true`, rejected
items are restored or re-worked to her instruction. Per D-12 the happy path is
same-session (she's next to the owner), but the ingestion reads the captured verdict doc
so it is robust if she is ever unavailable. If her verdicts change baa's graph, a Cloud
Run redeploy is owner-gated, never assumed.

Purpose: criterion 4 — every verdict recorded; signedOff matches her answers exactly;
rejects restored/re-worked.
Output: 25-HUMAN-UAT.md verdicts + ingested signedOff flags + a resolved (authorized or
deferred) server-redeploy decision.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-REVIEW-PACKET.md
@.planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: The mother's live walkthrough — capture her verdict for every packet row (CUR-01)</name>
  <files>.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-REVIEW-PACKET.md (the checklist the owner reads aloud, row by row)
    - .planning/phases/19-question-presentation-overhaul-every-question-self-explanato/19-HUMAN-UAT.md (the verdict-capture format to mirror: expected/result rows + a Summary tally)
    - 25-CONTEXT.md D-11 (live walkthrough, owner captures verdicts) + D-13 (the full scope of items)
  </read_first>
  <action>
    The owner reads each 25-REVIEW-PACKET.md row aloud to the mother (physically next to
    him — D-11) and records her verdict into 25-HUMAN-UAT.md (mirror 19-HUMAN-UAT.md
    format + a Summary tally). Scope = every D-13 item: minCleanReps=1 across all graphs
    incl. baa's signed spec, the buildSentence removals, the alif shrink + the new
    alif.writeLetter.fromPicture, Lane-B re-points/rewordings, the full re-point diff, and
    the 4 baa exceptions. She is the curriculum authority (CUR-01) — these are her
    pedagogical decisions, not Claude's. Same-session capture is the real workflow (D-12);
    if she is ever unavailable, the SAME doc is filled asynchronously later.
  </action>
  <human-check>
    For EVERY packet row, the owner records the mother's verdict as ONE of:
    - confirm — accept the change as-is (its signedOff will flip true in Task 2 (ingest) where applicable)
    - reject — restore the prior content
    - rework — change per her instruction (capture the instruction VERBATIM in the row)
    Reply with the per-item verdicts (or confirm 25-HUMAN-UAT.md is fully filled with a Summary tally) — then ingestion proceeds.
  </human-check>
  <acceptance_criteria>
    - 25-HUMAN-UAT.md has one verdict line per packet row (confirm/reject/rework), each rework carrying her verbatim instruction
    - A Summary tally (total / confirmed / rejected / reworked) is present
    - No row is left without a verdict
  </acceptance_criteria>
  <verify>
    <human-check>Owner confirms 25-HUMAN-UAT.md carries a verdict for every packet row plus a Summary tally.</human-check>
  </verify>
  <done>Every packet row has the mother's recorded verdict; rework instructions captured verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Ingest the verdicts — flip signedOff to match her answers; restore/rework rejects (criterion 4)</name>
  <files>assets/curriculum/graphs/alif.json, assets/curriculum/graphs/baa.json, assets/curriculum/curriculum_graph.json, assets/curriculum/exercises.json</files>
  <read_first>
    - .planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-HUMAN-UAT.md (the captured verdicts — the source of truth for the ingestion; mirror 19-HUMAN-UAT.md format)
    - assets/curriculum/graphs/{alif,baa}.json + assets/curriculum/curriculum_graph.json (the flags to flip per confirmed items — baa reconfirm→true D-14, alif fromPicture promote→true D-15 if approved) + assets/curriculum/exercises.json (the alif card + any reworked cards)
    - tools/content/promote_letter.py L189–213 (`enrich_exercise` — the path that would re-derive/re-emit any reworked card; forces signedOff:false, so a promote-to-true is a deliberate post-approval flag set, mirror the 15-07 human-verify precedent)
    - 25-CONTEXT.md D-12 (same-session + async ingestion), D-14 (baa reconfirm per item → true; rework rejects), D-15 (alif promote on approval), D-10 (taa/thaa LETTERS stay signedOff:false — letter-signing is Phase 27, NOT here)
    - test/curriculum/graph_asset_parity_test.dart + test/curriculum/learned_letters_lint_test.dart (must stay green after ingestion)
  </read_first>
  <action>
    Drive the flag changes from 25-HUMAN-UAT.md (the SAME code path whether captured live
    or async — D-12 robustness). For each CONFIRMED item: set the relevant `signedOff` to
    true — baa back to true in BOTH graphs/baa.json and curriculum_graph.json (byte-parity,
    D-14) if she confirms baa's content; alif.writeLetter.fromPicture → signedOff:true in
    exercises.json (D-15) if she approves it. taa/thaa LETTER graphs STAY signedOff:false
    (D-10 — signing them is Phase 27). For each REJECTED item: restore the prior content
    (from git history / the prior word) or RE-WORK it to her captured instruction (re-point
    the word, re-emit via promote_letter if a graph node changed), and leave that item's
    signedOff false until it matches her spec. Assert the final signedOff state matches the
    recorded verdicts EXACTLY (a confirmed item is true, a rejected/pending item is false).
    Keep the enforcement gates green throughout (they are independent of signedOff — D-04).
  </action>
  <acceptance_criteria>
    - Every packet row has a corresponding verdict line in 25-HUMAN-UAT.md; a Summary tally (total/confirmed/rejected/reworked) is present
    - `signedOff` in each touched asset matches its recorded verdict (confirmed→true, rejected/pending→false); taa/thaa letter graphs remain false
    - Rejected items are restored or re-worked per her instruction (SUMMARY lists each)
    - `flutter test test/curriculum/learned_letters_lint_test.dart test/curriculum/graph_asset_parity_test.dart` green; `python -m content.validate --gate` (from tools/) exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python -c "import json; json.load(open('assets/curriculum/graphs/baa.json')); json.load(open('assets/curriculum/curriculum_graph.json')); json.load(open('assets/curriculum/exercises.json')); print('assets valid')" && flutter test test/curriculum/learned_letters_lint_test.dart test/curriculum/graph_asset_parity_test.dart && cd tools && python -m content.validate --gate</automated>
  </verify>
  <done>Verdicts recorded; signedOff flags match her answers exactly; rejects restored/re-worked; all gates green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Resolve the conditional Cloud Run redeploy — owner-gated (server stays baa-only)</name>
  <files>(decision-only — no files modified; disposition recorded in 25-07-SUMMARY.md)</files>
  <read_first>
    - assets/curriculum/curriculum_graph.json (the server's source — check whether baa's graph structurally changed this phase)
    - 25-CONTEXT.md code_context L212–216 + Deferred Ideas L245–247 (the redeploy is owner-authorized, never assumed free; server baa-only)
    - .planning memory: tutor-server-deployed (every prod deploy is owner-gated; firebase/gcloud creds may be expired; fresh explicit owner wording each time)
  </read_first>
  <action>
    Ingestion is complete. IF the mother's verdicts changed baa's graph (e.g. she restored
    minCleanReps to 3 for writing/tracing, rejecting the demo's =1), then
    curriculum_graph.json — which the Cloud Run tutor server reads — has changed, and the
    signed graph must go live via a redeploy. Determine the disposition with the owner and
    record it in 25-07-SUMMARY.md; do NOT run any prod deploy without fresh explicit owner
    authorization. Server stays baa-only this phase.
  </action>
  <human-check>
    1. Run `git diff --stat assets/curriculum/curriculum_graph.json` against the phase-start commit — did baa's graph structurally change (a minCleanReps or node change, not just the signedOff flag round-tripping false→true)?
    2. If NO structural change — the server is unaffected; owner replies "no redeploy needed" (informational).
    3. If YES — a Cloud Run `qalam-tutor` redeploy is required. This needs FRESH EXPLICIT owner authorization + owner-supplied wording (every prod deploy is owner-gated; creds may be expired). Owner replies "authorize redeploy" (with wording) to do it now, OR "defer redeploy to <phase>" to record it as an explicit debt item — never silently assumed free.
  </human-check>
  <acceptance_criteria>
    - The git-diff check on curriculum_graph.json is run and its result recorded
    - A disposition is recorded in 25-07-SUMMARY.md: "no redeploy needed", "authorized (redeployed)", or "deferred to <phase>"
    - No prod deploy ran without explicit owner authorization
  </acceptance_criteria>
  <verify>
    <human-check>Owner replies "no redeploy needed", "authorize redeploy" (with wording), or "defer redeploy to <phase>"; the disposition is recorded in 25-07-SUMMARY.md.</human-check>
  </verify>
  <done>The server-redeploy question is explicitly resolved (not-needed / authorized / deferred) and recorded — never silently assumed free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mother's verdict → signedOff flags | Only the curriculum authority (CUR-01) may confirm content; ingestion must reflect her answers exactly, never Claude's guess. |
| baa graph change → prod Cloud Run server | A server redeploy pushes content to live AI coaching for children; it is owner-gated (fresh authorization each time). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-T | Tampering | ingestion setting signedOff:true on content she did NOT confirm | mitigate | Task 1 drives flags from the recorded 25-HUMAN-UAT.md verdicts and asserts exact match; the checkpoint captures her answers verbatim |
| T-25-07-E | Elevation | an unauthorized prod redeploy pushing unreviewed content live | mitigate | the human-action checkpoint requires fresh explicit owner authorization; server stays baa-only |
| T-25-07-I | Info disclosure | child PII/strokes in the UAT verdict doc | accept | 25-HUMAN-UAT.md records only per-item verdicts + rework instructions — no child identifiers or strokes |
| T-25-07-R | Repudiation | a signedOff:true window on content that later fails enforcement | mitigate | enforcement is decoupled from signedOff (D-04); the lint gate runs after ingestion and must be green |
</threat_model>

<verification>
- Every packet item has a recorded verdict; signedOff matches exactly; rejects restored/re-worked (criterion 4).
- Lint + parity green; audit gate exits 0 after ingestion.
- The server-redeploy decision is recorded (authorized / not-needed / deferred) — never silently assumed.
</verification>

<success_criteria>
Criterion 4: the mother's packet covers every owner-directed change since her last sign-off; every verdict is recorded; signedOff flags match her answers exactly; rejected changes are restored or re-worked.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-07-SUMMARY.md` when done — record the verdict tally, the exact signedOff end-state, each restored/reworked reject, and the server-redeploy disposition.
</output>
