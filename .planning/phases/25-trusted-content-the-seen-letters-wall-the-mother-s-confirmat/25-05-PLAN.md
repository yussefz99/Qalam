---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 05
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - lib/tutor/exercise_selector_provider.dart
  - lib/features/letter_unit/letter_unit_controller.dart
  - test/features/letter_unit/l3_illegal_card_guard_test.dart
  - test/tutor/l3_learned_letters_parity_test.dart
autonomous: false
requirements: [QP-07, D-12]
must_haves:
  truths:
    - "The walker/selector never presents an illegal card even if bad data ships — it SKIPs to the next legal node (D-01, criterion 3)"
    - "Skipping never strands the mastery star — an essential node with no legal path still reaches mastery (D-02, hard invariant)"
    - "Every guard firing is logged loudly (id + demanded letter + skipped); no silent catch swallows a failure (D-03)"
    - "The proof is a live-path test that seeds an illegal card via the real data path, not a mock (memory: dead-wire mandate)"
    - "The filter honors the SAME approved-exception set as L1/L2 (the 4 D-09 baa ids) — it never drops them, so a mother-approved exception is still presentable at runtime (parity, not a regression)"
  artifacts:
    - path: "lib/tutor/exercise_selector_provider.dart"
      provides: "the seen-letters filter over selection candidates (new I/O lives in lib/tutor/, not lib/curriculum/)"
      contains: "L3 guard"
    - path: "lib/features/letter_unit/letter_unit_controller.dart"
      provides: "de-silenced selection/clear catches (loud debugPrint) — the mastery gate behavior preserved"
      contains: "debugPrint"
    - path: "test/features/letter_unit/l3_illegal_card_guard_test.dart"
      provides: "the one-case-per-file live-path guard proof"
      min_lines: 120
      contains: "recordMasteryIfMet"
  key_links:
    - from: "lib/tutor/exercise_selector_provider.dart (the filter)"
      to: "assets/curriculum/exercises.json letters[] + letters.json introOrder"
      via: "learned-set predicate read in lib/tutor/ (network/agent-free lib/curriculum/ untouched)"
      pattern: "introOrder"
    - from: "the L3 skip"
      to: "CurriculumGraphWalker._nextReachableForward / isLegalSelection"
      via: "reuse the existing forward scan — no new rail"
      pattern: "selectFrom"
---

<objective>
L3 — the last line. Even if bad data ships to Firestore, the runtime selector must never
present a card that demands an unseen letter: it SKIPs it and advances to the next legal
node (D-01), the mastery star always stays reachable (D-02, a hard non-waivable
invariant), and every firing is logged loudly (D-03). L2 and L3 defend different surfaces
of the same Firestore-first bypass — L2 refuses at the write, L3 refuses at read.

Purpose: criterion 3 — a live-path test seeds an illegal card via the data path and
proves the walker never presents it, the star stays reachable, and the guard logs loudly.
The owner then confirms the demonstrated degradation behavior (the roadmap's reserved
L3-decision gate — this plan is autonomous:false).
Output: the filter in lib/tutor/, de-silenced controller catches, a live-path test, and
an owner sign-off on the SKIP disposition.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
@lib/tutor/exercise_selector_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the seen-letters filter to the selection path (D-01 SKIP + D-03 loud log) — honoring the shared exception set</name>
  <files>lib/tutor/exercise_selector_provider.dart, test/tutor/l3_learned_letters_parity_test.dart</files>
  <read_first>
    - lib/tutor/exercise_selector_provider.dart (the file being modified — `RouterExerciseSelector.selectNext` L131–160 narrows `candidates` then accepts-or-walks; `curriculumGraphProvider` L72–83 loads the per-letter graph; the layering note L22–26 / L266–277 in PATTERNS: new I/O belongs in lib/tutor/, NOT lib/curriculum/)
    - lib/curriculum/curriculum_graph_walker.dart L121–136 (`selectFrom`) + L149–164 (`_nextReachableForward` — the forward scan the SKIP advances through) — READ-ONLY, reuse do not edit
    - lib/curriculum/curriculum_graph.dart L311–325 (`isLegalSelection`) — reuse, do not invent a new rail
    - assets/curriculum/letters.json (`introOrder`) + assets/curriculum/exercises.json (each card's `letters[]`) — the learned-set source the filter needs
    - test/curriculum/learned_letters_lint_test.dart L127–135 (`unlearnedFor`, the reach-ahead predicate) AND L112–125 (`baaOwnerApprovedExceptions`, the 4 D-09 ids the filter must ALSO honor)
    - tools/content/validate.py `OWNER_APPROVED_EXCEPTIONS` (Plan 01 — the shared Python source of the same 4 ids that L2 imports); L3 mirrors this set as a Dart const
    - 25-PATTERNS.md "exercise_selector_provider.dart — L3 illegal-card filter" (L246–277) + "The learned-letters legality rule" (L317–333)
    - 25-CONTEXT.md D-09 (the 4 baa exceptions stay live; the packet flips each to mother-approved or re-points/removes it — L3 must not silently suppress an approved exception)
    - test/tutor/durable_layers_no_agent_imports_test.dart (the guard that keeps lib/curriculum/ agent/network/IO-free — the filter's IO must NOT land there)
  </read_first>
  <action>
    In lib/tutor/, add a seen-letters predicate that, for the current letter, drops any
    candidate whose exercises.json `letters[]` contains a letter with `introOrder >` the
    unit's introOrder (same rule as `unlearnedFor`). Apply it as ONE MORE narrowing over
    `out.candidates` inside `RouterExerciseSelector.selectNext` (L137–159) BEFORE the
    accept-agent / walker-pick step, so an illegal candidate is dropped and the walker's
    `selectFrom` / `_nextReachableForward` advances to the next legal node (D-01 SKIP —
    reuse the existing forward scan, never a new rail). EXEMPT the approved reach-ahead
    exceptions: a candidate whose id is in the shared approved-exception set is NEVER
    dropped even if it reaches ahead — parity with L1 (`baaOwnerApprovedExceptions`) and
    L2 (`OWNER_APPROVED_EXCEPTIONS`), so once the mother approves the 4 D-09 ids in the
    packet (25-07) they stay presentable at runtime. Define a Dart const
    `kApprovedReachAheadExceptions` (the 4 D-09 ids: `baa.fillBlank.adjective`,
    `baa.transformWord.dual/.plural/.opposite`) in lib/tutor/ with a comment naming the
    lint's `baaOwnerApprovedExceptions` + validate.py's `OWNER_APPROVED_EXCEPTIONS` as the
    mirrored sources. Thread the learned-set into `RouterExerciseSelector` (a
    `Set<String> learnedLetters` or a `bool Function(String exerciseId) isSeenLegal`
    constructor param supplied by the provider, reading letters.json/exercises.json — the
    I/O stays in lib/tutor/, keeping lib/curriculum/ pure); the predicate drops a candidate
    ONLY when it reaches ahead AND its id is not in `kApprovedReachAheadExceptions`. For
    every drop, `debugPrint('L3 guard: <id> illegal (demands <letter>), skipped')` — the
    loud voice from recordMasteryIfMet (controller L615–619). Do NOT edit lib/curriculum/
    (the durable_layers guard must stay green). ALSO create
    test/tutor/l3_learned_letters_parity_test.dart — a small PURE unit test (no widgets)
    pinning the Dart reach-ahead predicate + `kApprovedReachAheadExceptions` to the same
    rule L1 enforces (advisory-4 drift guard): assert a reach-ahead non-exception id is
    dropped, a within-learned-set id is kept, each of the 4 D-09 ids is kept despite
    reaching ahead, and `kApprovedReachAheadExceptions` equals the 4 D-09 ids literally.
  </action>
  <acceptance_criteria>
    - `RouterExerciseSelector.selectNext` never returns a reach-ahead candidate UNLESS its id is in `kApprovedReachAheadExceptions`
    - The 4 D-09 exception ids are NOT dropped by the filter even though they reach ahead (parity with L1/L2) — asserted by the parity test
    - `test/tutor/l3_learned_letters_parity_test.dart` passes: predicate parity + `kApprovedReachAheadExceptions` == the 4 D-09 ids
    - The filter's letters.json/exercises.json reads live in lib/tutor/ — `flutter test test/tutor/durable_layers_no_agent_imports_test.dart` stays green
    - A drop emits `L3 guard: … illegal (demands …), skipped` via debugPrint; `flutter analyze lib/tutor/exercise_selector_provider.dart` is clean
  </acceptance_criteria>
  <verify>
    <automated>grep -q "L3 guard" lib/tutor/exercise_selector_provider.dart && grep -q "kApprovedReachAheadExceptions" lib/tutor/exercise_selector_provider.dart && flutter test test/tutor/durable_layers_no_agent_imports_test.dart test/tutor/l3_learned_letters_parity_test.dart</automated>
  </verify>
  <done>The selector drops illegal candidates, skips to the next legal node, logs loudly, and keeps lib/curriculum/ pure.</done>
</task>

<task type="auto">
  <name>Task 2: De-silence the controller's selection/clear catches (D-03) — preserve the mastery/star seam (D-02)</name>
  <files>lib/features/letter_unit/letter_unit_controller.dart</files>
  <read_first>
    - lib/features/letter_unit/letter_unit_controller.dart (the file being modified — the silent `catch (_) { return null; }` at `_selectNext` L471, the silent `catch (_) { return; }` at `markNodeCleared` L552, the silent `catch (_) { reps = 0; }` L568; the LOUD template `recordMasteryIfMet` L611–622; the mastery gate L602–661 + `_presentedExerciseIds` L677–695 — READ but do NOT change its behavior)
    - 25-CONTEXT.md D-03 (every firing logged loudly) + the star-reachability seam note (code_context L206–209)
    - 25-PATTERNS.md "letter_unit_controller.dart — L3 loud logging" (L280–312) + "Loud-degrade, never silent" (L335–338)
  </read_first>
  <action>
    Replace the three still-silent catches with loud degrades mirroring the L611–622
    template (name what failed, why, and the consequence): L471 `_selectNext` graph-load
    → `debugPrint('[LetterUnit] selection for "$_letterId" could not load the graph: $e —
    skipping this selection (the unit stays put, never a crash).')` then `return null`;
    L552 `markNodeCleared` graph-load → a loud debugPrint then `return`; L568 rep-read →
    a loud debugPrint then `reps = 0`. Preserve the mastery gate behavior EXACTLY
    (recordMasteryIfMet still fail-safe + already loud; `_presentedExerciseIds` unchanged)
    — this task only de-silences; it must not alter what grants the star (D-02
    reachability is proven by Task 3, not changed here). Keep `return`/`return null`/`reps
    = 0` fallbacks so behavior is unchanged except the logging.
  </action>
  <acceptance_criteria>
    - `grep -n 'catch (_)' lib/features/letter_unit/letter_unit_controller.dart` returns NO bare silent catch that returns false/null/void without a debugPrint at L471/L552/L568 (each now names id + cause + consequence)
    - `recordMasteryIfMet` and `_presentedExerciseIds` are behaviorally unchanged (diff shows only added logging elsewhere)
    - `flutter analyze lib/features/letter_unit/letter_unit_controller.dart` clean
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && test $(grep -c 'catch (_)' lib/features/letter_unit/letter_unit_controller.dart) -eq 0 && flutter analyze lib/features/letter_unit/letter_unit_controller.dart</automated>
  </verify>
  <done>The three silent selection/clear catches now log loudly; the mastery/star seam is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The live-path guard proof — seed an illegal card via the data path (criterion 3)</name>
  <files>test/features/letter_unit/l3_illegal_card_guard_test.dart</files>
  <behavior>
    - Seeding/injecting an illegal card (a graph node whose exercises.json `letters[]` reaches ahead) via the REAL data path never lets the walker present it: `controller.state.currentExerciseId` is NEVER the illegal id after a scored pass, and the rendered graph node is not it
    - The mastery star still fires: `recordMasteryIfMet()` returns true after the essential core is met (D-02 reachability)
    - The guard logs loudly on the skip (`L3 guard: … skipped`)
  </behavior>
  <read_first>
    - test/features/letter_unit/agent_pick_live_path_test.dart (the PRIMARY analog — copy `_pumpStarted` L151–196: in-memory Drift, `_SeededPositionRepo` L81–97, `curriculumGraphProvider` override L166–169, empty `ChildModelSnapshot` L171, `NoopTtsCoachSpeaker` L174; drive the pass via `WriteSurface.onResult` L210–212; the cursor + render assertions L216–233 / `_graphNode` L268–272)
    - test/features/letter_unit/mastery_progression_harness.dart (the SECONDARY analog — copy `_awaitPumping` dual-drain L118–155 and OBEY the one-case-per-file rule header L34–41: rootBundle stalls in a SECOND testWidgets → exactly ONE live case in this file; also its real-data loaders + `recordMasteryIfMet` proof pattern)
    - lib/features/letter_unit/widgets/write_surface.dart (`WriteSurface.onResult` — the scored-pass seam)
    - 25-PATTERNS.md "NEW: L3 live-path guard test" (L383–411) — the (a) seed / (b) never-present / (c) star-reachable / (d) loud-log requirement + the live-path mandate box
  </read_first>
  <action>
    Create test/features/letter_unit/l3_illegal_card_guard_test.dart with EXACTLY ONE
    `testWidgets` (one-case-per-file, per the harness rule). Build a graph fixture where
    an essential node's card demands an unseen letter (inject the illegal card via the
    real data path — the `curriculumGraphProvider` override returning a graph whose node
    resolves to an exercises.json card with a reach-ahead `letters[]`, plus a sibling
    LEGAL essential node so the star stays reachable). Pump the real `ExerciseScaffold`
    via a `_pumpStarted` copy; drive a scored PASS through `WriteSurface.onResult`; use
    `_awaitPumping` for any rootBundle-touching await. Assert: (a) `controller.state
    .currentExerciseId` is never the illegal id and the illegal node never renders
    (`_graphNode(illegal)` findsNothing); (b) after clearing the legal essentials,
    `recordMasteryIfMet()` returns true (D-02 star reachable); (c) the loud `L3 guard`
    line was produced (capture via `debugPrint` override or assert the skip landed on the
    walker's legal pick). NOT a selector-only unit test — it MUST pump the scaffold and
    drive the live seam (the Phase-15 dead wire shipped exactly because a unit test can't
    catch it).
  </action>
  <acceptance_criteria>
    - The file contains exactly one `testWidgets` (grep count == 1)
    - The test seeds the illegal card through the data path (curriculumGraphProvider override / real exercises.json card), not a hand-mocked selector return
    - It asserts `isNot(illegalId)` on the durable cursor AND `findsNothing` on the illegal rendered node AND `recordMasteryIfMet()` == true
    - `flutter test test/features/letter_unit/l3_illegal_card_guard_test.dart` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && test $(grep -c 'testWidgets(' test/features/letter_unit/l3_illegal_card_guard_test.dart) -eq 1 && flutter test test/features/letter_unit/l3_illegal_card_guard_test.dart</automated>
  </verify>
  <done>A single live-path case proves the illegal card is never presented, the star stays reachable, and the guard logs loudly.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Owner confirms the L3 degradation behavior (the roadmap's reserved L3 decision)</name>
  <files>(decision-only — no files modified; verdict recorded in 25-05-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md D-01/D-02/D-03 (SKIP behavior, star-reachability backstop, loud logging — the locked discuss-phase decision this gate finalizes)
    - the passing output of test/features/letter_unit/l3_illegal_card_guard_test.dart (Task 3 — the live-path proof the owner reviews)
  </read_first>
  <action>
    Present the demonstrated L3 behavior to the owner and record the verdict. Tasks 1-3
    implemented SKIP (D-01), a mandatory star-reachability backstop (D-02), and loud
    logging (D-03), proven by the live-path test. Per the roadmap this plan is
    autonomous:false — the owner reviews the demonstrated behavior before it locks: the
    passing live-path test output, plus (optionally) an on-device run showing an illegal
    card skipped, the star still earned, and the `L3 guard:` log line. Do NOT proceed to
    phase completion until the owner records a verdict; capture it verbatim in
    25-05-SUMMARY.md.
  </action>
  <human-check>
    Owner reviews the L3 degradation as built and replies with ONE of:
    - "accept-skip" — lock SKIP (D-01) as implemented. Simplest option (the owner's stated
      preference); star-reachability is proven and the loud log is in place. An illegal
      essential card with no sibling path relies on the D-02 backstop rather than
      substituting content.
    - a redirect instruction (e.g. substitute / tier-down) — only if the demonstrated
      behavior is unacceptable; note this re-opens the locked discuss-phase D-01 and adds
      scope. Capture the redirect verbatim.
  </human-check>
  <acceptance_criteria>
    - A verdict ("accept-skip" or a redirect instruction) is recorded verbatim in 25-05-SUMMARY.md
    - If "accept-skip": no further L3 change is made; D-01 is locked as implemented
    - If a redirect: the redirect instruction is captured as a follow-up scope item
  </acceptance_criteria>
  <verify>
    <human-check>Owner replies "accept-skip" (or a redirect instruction); the verdict is recorded in 25-05-SUMMARY.md.</human-check>
  </verify>
  <done>The owner has confirmed the L3 degradation behavior (SKIP accepted or redirect captured); the verdict is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore content → the child's screen | The device reads content Firestore-first; if bad data ships (past L2), the runtime selector is the LAST line before an illegal card reaches a child. |
| agent (network) selection → the rail | untrusted (T-15-05-T, pre-existing) — the agent's pick is already re-checked; the seen-letters filter is an additional narrowing on the SAME trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-T | Tampering | bad data shipping past L2 and reaching a child at runtime | mitigate | Task 1's seen-letters filter SKIPs any illegal candidate; Task 3 proves it on the live path (criterion 3) |
| T-25-05-D | DoS (progression) | a skip stranding the mastery star | mitigate | D-02 star-reachability backstop; Task 3 asserts `recordMasteryIfMet()` still fires |
| T-25-05-R | Repudiation | a silent guard firing hiding a data problem | mitigate | D-03 loud logging; Task 2 de-silences the three remaining catches |
| T-25-05-I | Info disclosure | the loud log leaking raw strokes or child identifiers | mitigate | the `L3 guard` line names only the exercise id + the demanded letter — no strokes, no child id (assert in the SUMMARY) |
</threat_model>

<verification>
- `flutter test test/features/letter_unit/l3_illegal_card_guard_test.dart` passes (criterion 3).
- `durable_layers_no_agent_imports_test.dart` green (lib/curriculum/ stayed pure).
- No bare silent `catch (_)` remains at the three controller sites.
- Owner accepted the SKIP disposition (or redirected) — recorded in the SUMMARY.
</verification>

<success_criteria>
Criterion 3: a live-path test seeds an illegal card via the data path and proves the walker never presents it, the star stays reachable, and the guard logs loudly; the owner has confirmed the L3 degradation behavior.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-05-SUMMARY.md` when done — record the owner's L3 decision verdict and confirm the log carries no child data.
</output>
