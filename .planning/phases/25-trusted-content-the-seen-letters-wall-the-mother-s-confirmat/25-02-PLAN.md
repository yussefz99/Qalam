---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - assets/curriculum/exercises.json
  - assets/curriculum/graphs/alif.json
  - assets/curriculum/graphs/baa.json
  - assets/curriculum/graphs/taa.json
  - assets/curriculum/graphs/thaa.json
  - assets/curriculum/curriculum_graph.json
autonomous: true
requirements: [QP-07, D-12, CUR-01]
must_haves:
  truths:
    - "After triage the audit gate exits 0 — ZERO unlabeled words and ZERO reach-ahead cards across every live letter (criterion 1)"
    - "Every re-pointed card carries an inline `_review` note naming the change so the mother's packet diff is machine-greppable (D-08/D-13)"
    - "No removed/skipped card strands an essential competency's only legal path — the mastery star stays reachable (D-02)"
    - "The new alif.writeLetter.fromPicture card is a valid enforced node (letters + criteria present, signedOff:false draft — D-15)"
  artifacts:
    - path: "assets/curriculum/exercises.json"
      provides: "re-pointed cards (each with _review) + the completed alif.writeLetter.fromPicture draft card"
      contains: "alif.writeLetter.fromPicture"
    - path: "assets/curriculum/graphs/alif.json"
      provides: "alif live graph with reach-ahead nodes removed (letter-level only) + the fromPicture node"
      contains: "alif.writeLetter.fromPicture"
    - path: "assets/curriculum/curriculum_graph.json"
      provides: "baa live graph kept byte-parity with graphs/baa.json after any node change"
  key_links:
    - from: "assets/curriculum/graphs/baa.json"
      to: "assets/curriculum/curriculum_graph.json"
      via: "byte-parity (graph_asset_parity_test.dart)"
      pattern: "signedOff"
    - from: "assets/curriculum/exercises.json card letters[]"
      to: "assets/curriculum/letters.json introOrder"
      via: "the seen-letters bar the audit gate enforces"
      pattern: "letters"
---

<objective>
Triage the 34-card audit worklist (the 2026-07-19 finding) to ZERO. Default D-07:
RE-POINT the card's word to one using only already-learned letters (keeps the
exercise); if no learned-letter word preserves the teaching point, REMOVE the card
from the live graph; keep a reach-ahead card ONLY as an existing baa exception (the 4
D-09 ids stay live for the mother's packet). Also complete the new letter-level alif
card (D-15). The OWNER triages now (relaying the model's drafts, D-08) so the build
goes clean immediately — the mother reviews the full diff later in her packet (Plan 06).

Purpose: make criterion 1 true (audit gate exits 0) without waiting on the mother
(D-12: the wall lands non-blocking).
Output: re-pointed/removed cards in exercises.json (each `_review`-noted), re-derived
graphs holding the D-02 star-reachability invariant, and the completed alif card.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
@tools/content/validation_report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the re-pointable cards (D-07 default) with _review notes</name>
  <files>assets/curriculum/exercises.json</files>
  <read_first>
    - tools/content/validation_report.md (THE worklist — regenerated by Plan 01; section 3 lists every reach-ahead live card by id + the unlearned letters)
    - assets/curriculum/letters.json (`introOrder`: alif 1, baa 2, taa 3, thaa 4 — the learned-set order the re-point must respect)
    - assets/curriculum/exercises.json (the cards to edit — grep each worklist id; do NOT read the whole 2100-line file, edit in place by id)
    - assets/curriculum/exercises.json L1078–1119 (`taa.writeLetter.fromPicture`) — the `_review` note convention (an inline string naming the change + "Mother confirms.")
    - 25-PATTERNS.md "exercises.json" (L188–218): the re-point pattern + the greppable `_review` convention; "Shared Patterns → Idempotent id-keyed content writes" (L347–352)
    - 25-CONTEXT.md D-07/D-08/D-09/D-10 (the disposition rules + the 4 baa exceptions that stay live)
  </read_first>
  <action>
    For each reach-ahead card in the worklist whose teaching point survives a
    learned-letter word: change the card's `expected` word/glyph (and any prompt text
    the child reads) to a word decomposing to letters with `introOrder ≤` the unit's
    introOrder, recompute its stored `letters[]` from the new word (mirror
    promote_letter's `decompose` → `_dedup_preserve`), and add an inline `_review`
    string naming the old→new word + "Mother confirms." — using the EXACT convention on
    taa.writeLetter.fromPicture L1081 so `grep '_review' exercises.json` yields the full
    packet diff (D-08/D-13). Do NOT touch the 4 D-09 baa exception cards
    (`baa.fillBlank.adjective`, `baa.transformWord.dual/.plural/.opposite`) — they stay
    live as-is for the mother's packet. Cards with no learned-letter word that preserves
    the teaching point are handled in Task 2 (REMOVE). Edit idempotently by id.
  </action>
  <acceptance_criteria>
    - Every re-pointed card's stored `letters[]` contains only letters with introOrder ≤ its unit's introOrder (the audit predicate returns `[]` for it)
    - Every re-pointed card gained an inline `_review` note in the taa L1081 style
    - `python -m content.validate` section 3 finding count strictly decreased vs the Plan-01 baseline
    - The 4 D-09 baa cards are unchanged (`git diff` shows no edit to those 4 ids)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python -c "import json; json.load(open('assets/curriculum/exercises.json')); print('json ok')" && cd tools && python -m content.validate | grep -i finding</automated>
  </verify>
  <done>Re-pointable cards use learned-letter words with correct letters[] + a _review note; findings dropped toward zero.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the non-re-pointable cards from the live graphs, holding star-reachability (D-02)</name>
  <files>assets/curriculum/graphs/alif.json, assets/curriculum/graphs/baa.json, assets/curriculum/graphs/taa.json, assets/curriculum/graphs/thaa.json, assets/curriculum/curriculum_graph.json</files>
  <read_first>
    - assets/curriculum/graphs/{alif,baa,taa,thaa}.json (the live per-letter graphs — node lists to prune)
    - assets/curriculum/curriculum_graph.json (baa canonical — MUST stay byte-parity with graphs/baa.json)
    - tools/content/promote_letter.py L310–348 (`--graph-only <letter>` re-derives a per-letter graph from a draft, forces signedOff:false, and guards every node id resolves to a live exercise — L329–339)
    - lib/curriculum/curriculum_graph.dart L176–177 (`essentialNodes` — the star core), L311–325 (`isLegalSelection`) — the invariant a removal must not strand (READ-ONLY, do not edit)
    - lib/curriculum/mastery_condition.dart L29–35 / L52–65 (`isMasteryMet` / `isMasteryMetForPresented`) — what "star reachable" means
    - 25-PATTERNS.md "graphs/*.json + curriculum_graph.json" (L221–243): the byte-parity + star-reachability invariants; the D-02 "removed essential card must leave its competency a legal reachable node" rule
    - test/curriculum/graph_asset_parity_test.dart (the parity guard that must stay green)
  </read_first>
  <action>
    For each worklist card that Task 1 could NOT re-point (no learned-letter word
    preserves the teaching point) — including the pre-existing reach-ahead alif cards
    (`alif.buildSentence.hear`, `alif.connectWord.baab`, `alif.transformWord.dual`,
    `alif.writeWord.*`; alif is now letter-level only) and any buildSentence card still
    in a live graph — remove its node from the letter's `graphs/<letter>.json` node
    list. Before removing an ESSENTIAL node, confirm its competency retains at least one
    other legal reachable node (D-02); if removal would strand the only path to an
    essential competency, DO NOT remove — flag it in the SUMMARY for the packet as a
    forced exception instead. Re-derive graphs via `promote_letter --graph-only <letter>`
    where a draft exists, else hand-edit the node list. Any baa node change MUST land in
    BOTH graphs/baa.json AND curriculum_graph.json (byte-parity). Leave the card configs
    in exercises.json (a config can stay unreferenced) — REMOVE means "not a live graph
    node", not "delete the config".
  </action>
  <acceptance_criteria>
    - `python -m content.validate --gate` (from tools/) exits 0 — ZERO reach-ahead findings and ZERO unlabeled live cards across all letters (criterion 1)
    - `flutter test test/curriculum/graph_asset_parity_test.dart` passes (baa byte-parity held)
    - No essential competency lost its only legal reachable node (SUMMARY records any forced exception explicitly, none silently dropped)
    - graphs/alif.json contains no reach-ahead node (alif letter-level only)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/tools && python -m content.validate --gate; echo "gate exit=$? (expected 0)"; cd /Users/mareekhalila/Documents/Qalam/qalam && flutter test test/curriculum/graph_asset_parity_test.dart</automated>
  </verify>
  <done>Audit gate exits 0; graphs pruned without stranding the star; baa parity holds.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the new alif.writeLetter.fromPicture draft card (D-15)</name>
  <files>assets/curriculum/exercises.json</files>
  <read_first>
    - assets/curriculum/exercises.json L2091–2123 (the PARTIAL alif.writeLetter.fromPicture card — has prompt/image/expected/feedback/_review but is MISSING `letters` and `criteria`)
    - assets/curriculum/exercises.json L1078–1119 (`taa.writeLetter.fromPicture` — the exact sibling shape: check `glyph+positionalForm`, signedOff:false, letters:["taa"], criteria the 5-stroke set)
    - tools/content/promote_letter.py L91–103 (`CRITERIA_BY_TYPE`: `writeLetter` → `["strokeCount","strokeOrder","shape","direction","dot"]`) and L189–213 (`enrich_exercise` — how letters[] + criteria + signedOff:false are stamped)
    - assets/curriculum/graphs/alif.json L56–60 (the fromPicture node already exists in alif's graph)
    - 25-CONTEXT.md D-15 (author as draft signedOff:false; promote on the mother's approval — that promotion is Plan 07, NOT here)
    - 25-PATTERNS.md "NEW: complete alif.writeLetter.fromPicture" (L357–362)
  </read_first>
  <action>
    Complete the existing partial alif.writeLetter.fromPicture card in exercises.json:
    add `"letters": ["alif"]` and `"criteria": ["strokeCount","strokeOrder","shape",
    "direction","dot"]` (the promote_letter `writeLetter` type map), and keep
    `"signedOff": false` (D-15 draft — do NOT set true; the mother promotes it live in
    Plan 07). Ensure the mnemonic word أسد (asad) lives in the image/say prompt part, NOT
    in a decomposable `kind:"text"` prompt part and NOT in `expected` — mirror the taa
    sibling exactly so the audit's `_arabic_strings_in_exercise` never decomposes أسد and
    the card stays legal with letters:["alif"]. alif stays letter-level only (no sentence
    work). Keep the existing `_review` note.
  </action>
  <acceptance_criteria>
    - The card has `letters:["alif"]`, the 5-item stroke `criteria`, and `signedOff:false`
    - `python -m content.validate --gate` still exits 0 (the alif card is legal — the audit does not flag أسد)
    - `flutter test test/curriculum/learned_letters_lint_test.dart` is NOT broken by the card (it becomes fully enforceable in Plan 03)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python -c "import json; e=[x for x in json.load(open('assets/curriculum/exercises.json'))['exercises'] if x['id']=='alif.writeLetter.fromPicture'][0]; assert e['letters']==['alif'], e.get('letters'); assert e['criteria']==['strokeCount','strokeOrder','shape','direction','dot'], e.get('criteria'); assert e['signedOff'] is False; print('alif card ok')"</automated>
  </verify>
  <done>The alif card is a valid enforced draft node (letters + criteria present, signedOff:false).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner triage → curriculum assets | The owner edits content that will be seeded to prod Firestore (Firestore-first → reaches children). The audit gate + the downstream L1/L2 refusals are the guards that a bad edit cannot ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-T | Tampering | a re-point that silently reduces a teaching point (a "v1"/"static" simplification) | mitigate | D-07 preserves the teaching point via a learned-letter word or REMOVE; the mother reviews the full re-point diff in Plan 06 and can reject |
| T-25-02-D | DoS (progression) | a removal stranding an essential competency's only path (unreachable star) | mitigate | Task 2 checks star-reachability before removal (D-02); Plan 05's live-path test proves the star still fires |
| T-25-02-I | Info disclosure | child data in content edits | accept | curriculum content carries no child identifiers or strokes |
</threat_model>

<verification>
- `python -m content.validate --gate` exits 0 (criterion 1).
- graph_asset_parity_test green (baa parity).
- alif card valid; every re-point carries a greppable `_review` note.
</verification>

<success_criteria>
Criterion 1 (content leg): ZERO unlabeled words + ZERO violating cards; the D-02 star-reachability invariant preserved; the alif draft card authored.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-02-SUMMARY.md` when done — list every re-pointed id (old→new word), every removed node, and any forced exception for the packet.
</output>
