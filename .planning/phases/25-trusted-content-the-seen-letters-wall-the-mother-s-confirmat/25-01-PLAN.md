---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/content/validate.py
  - tools/content/validation_report.md
  - tools/content/README.md
autonomous: true
requirements: [QP-07, D-12]
must_haves:
  truths:
    - "Running the audit gate exits non-zero while any live card is unlabeled or reaches ahead, and exits 0 once the worklist is triaged to zero (criterion 1)"
    - "The learned-letters reach-ahead check is a single reusable pure function that L2 (the seeder) can import — the wall's four layers refuse the SAME thing"
    - "The gate + seeder scope to LIVE GRAPH NODES only (via a shared live_graph_node_ids helper) and exempt the shared OWNER_APPROVED_EXCEPTIONS — dormant reach-ahead configs never trip it, matching L1's liveNodeIds scoping"
    - "validation_report.md reproduces the current 34-card worklist so the owner can triage from it"
  artifacts:
    - path: "tools/content/validate.py"
      provides: "unlearned_letters_for_exercise() shared predicate + live_graph_node_ids() + OWNER_APPROVED_EXCEPTIONS + a --gate build gate + an unlabeled-card check"
      contains: "def live_graph_node_ids"
    - path: "tools/content/validation_report.md"
      provides: "the regenerated live-exercise worklist"
      contains: "content demanding unlearned letters"
  key_links:
    - from: "tools/firebase/seed_curriculum_v2.py"
      to: "tools/content/validate.py::unlearned_letters_for_exercise"
      via: "python import (L2 reuses the L0 predicate — parity by construction)"
      pattern: "unlearned_letters_for_exercise"
---

<objective>
L0 — build the audit machinery that BOTH labels the content (every live card must
carry a correct `letters[]`) AND enforces the seen-letters bar as a reusable pure
predicate. This is the parity source of truth: L2 (the Python seeder) imports the
same predicate, and L1 (the Dart lint) mirrors it behaviorally — so all four wall
layers refuse identical content.

Purpose: satisfy criterion 1's "ZERO unlabeled words and ZERO violating cards" by
giving the build a single non-zero-exit gate, and give L2 a function to import so
the seeder and the audit cannot drift apart.
Output: an extended `validate.py` (shared predicate + `--gate` + label check), a
regenerated `validation_report.md` worklist, and a README note for the gate command.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared learned-letters predicate + a label check</name>
  <files>tools/content/validate.py</files>
  <read_first>
    - tools/content/validate.py (the file being modified — read `report_live_exercises` L193–228, `_arabic_strings_in_exercise` L176–190, `_base_members` L54–57, `load_intro_order` L48–51, path constants L30–41)
    - tools/content/arabic.py (`decompose`, `BASE_LETTERS` — the decomposition the predicate reuses)
    - 25-PATTERNS.md "validate.py — L0 / audit" (L84–122): the exact extraction target + the shared-predicate rule (`unlearned_letters_for_exercise`)
    - test/curriculum/learned_letters_lint_test.dart L127–135 (`unlearnedFor`) — the Dart predicate this Python one must match BEHAVIORALLY: it reads the card's STORED `letters[]`, not the decomposed display string
    - test/curriculum/learned_letters_lint_test.dart L55–93 (`_discoverUnitGraphs`) + L112–125 (`baaOwnerApprovedExceptions`) — the Dart source of truth for the live-node set and the 4 D-09 exception ids that L0 must MIRROR (`live_graph_node_ids` / `OWNER_APPROVED_EXCEPTIONS`)
    - assets/curriculum/graphs/*.json + assets/curriculum/curriculum_graph.json — the live graph node lists (node `exerciseId`s); 15 exercises.json configs are dormant (referenced by NO live node — e.g. `alif.buildSentence.hear`, the buildSentence pairs) and MUST NOT be gated
  </read_first>
  <action>
    Add a pure module-level function `unlearned_letters_for_exercise(ex, order)` to
    validate.py that returns `[(letter, order[letter]), …]` for every entry of the
    card's STORED `ex.get("letters", [])` whose `order[letter]` exceeds the unit's
    intro-order (unit = `ex["id"].split(".",1)[0]`); empty list == legal. This mirrors
    the Dart lint's `unlearnedFor` (which reads `e['letters']`) so L1/L2/L3 agree on
    stored `letters[]`. ALSO add `live_graph_node_ids()` returning the set of exercise
    ids referenced by a live graph node — the union of every `node["exerciseId"]` across
    `assets/curriculum/graphs/*.json` + `assets/curriculum/curriculum_graph.json`
    (mirroring the Dart lint's `_discoverUnitGraphs`/`liveNodeIds`; baa is byte-parity so
    dedup) — so the gate and the seeder scope to LIVE nodes and never flag the 15 dormant
    reach-ahead configs. ALSO add the module constant `OWNER_APPROVED_EXCEPTIONS` = the 4
    D-09 baa ids (`baa.fillBlank.adjective`, `baa.transformWord.dual/.plural/.opposite`),
    the single Python source both L0 and L2 read (mirror of the lint's
    `baaOwnerApprovedExceptions`). Keep the existing string-decomposition finding in
    `report_live_exercises` as a SEPARATE label-drift signal, and add a
    `unlabeled_cards(exercises, live_ids)` helper that flags any LIVE-NODE card whose
    `letters[]` is missing/empty OR diverges from `decompose(_text_for_display).letters`
    (the بطة/توت drift the module docstring already names) — this is criterion 1's
    "unlabeled words" leg, scoped to live nodes. Do NOT rename or move `load_intro_order`,
    `_base_members`, or the path constants — the seeder (Plan 04) imports
    `unlearned_letters_for_exercise`, `live_graph_node_ids`, `OWNER_APPROVED_EXCEPTIONS`,
    and `load_intro_order` by those names.
  </action>
  <acceptance_criteria>
    - `python -c "from tools.content.validate import unlearned_letters_for_exercise, load_intro_order"` imports without error (run from repo root with `PYTHONPATH=.`)
    - `unlearned_letters_for_exercise({"id":"baa.x","letters":["taa"]}, load_intro_order())` returns a non-empty list (taa introOrder 3 > baa 2); with `["alif","baa"]` returns `[]`
    - The function reads `ex["letters"]` (stored), NOT a re-decomposition of display text — grep shows `ex.get("letters"` inside the function body
    - `from tools.content.validate import live_graph_node_ids, OWNER_APPROVED_EXCEPTIONS` imports; `live_graph_node_ids()` returns a set EXCLUDING the dormant ids (`'alif.buildSentence.hear' not in live_graph_node_ids()`) and INCLUDING live ones (`'baa.fillBlank.adjective' in live_graph_node_ids()`); `OWNER_APPROVED_EXCEPTIONS == {the 4 D-09 baa ids}`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && PYTHONPATH=. python -c "from tools.content.validate import unlearned_letters_for_exercise as u, load_intro_order as o, live_graph_node_ids as lg, OWNER_APPROVED_EXCEPTIONS as ex; assert u({'id':'baa.x','letters':['taa']}, o()); assert not u({'id':'baa.x','letters':['alif','baa']}, o()); L=lg(); assert 'alif.buildSentence.hear' not in L and 'baa.fillBlank.adjective' in L; assert 'baa.fillBlank.adjective' in ex; print('ok')"</automated>
  </verify>
  <done>The shared predicate + label check exist as importable pure functions; the report still regenerates using them.</done>
</task>

<task type="auto">
  <name>Task 2: Add the --gate build gate and regenerate the worklist report</name>
  <files>tools/content/validate.py, tools/content/validation_report.md, tools/content/README.md</files>
  <read_first>
    - tools/content/validate.py `main()` L231–268 (the current exit path — exits non-zero only on a BLOCKING draft decomposition at L265–267; extend it)
    - tools/content/validation_report.md (the current 61-word-finding / ~34-card worklist to reproduce)
    - tools/content/README.md (where the gate command gets documented)
    - 25-PATTERNS.md L114–118 (the `--gate` / non-zero-exit-on-live-findings instruction)
  </read_first>
  <action>
    Add a `--gate` argparse flag to `main()`. When set, restrict the checks to LIVE
    GRAPH NODES (`ex["id"] in live_graph_node_ids()`) and exit non-zero if EITHER a
    live-node exercise that is NOT in `OWNER_APPROVED_EXCEPTIONS` reaches ahead (via
    `unlearned_letters_for_exercise`) OR any live-node card is unlabeled
    (`unlabeled_cards`). Dormant configs (the 15 not referenced by any live node — e.g.
    `alif.buildSentence.hear`, the buildSentence pairs) and the 4 D-09 exceptions are NOT
    gated — exactly what L1's `liveNodeIds`/`baaOwnerApprovedExceptions` scoping does, so
    L0 and L1 fail on the SAME set. Without `--gate`, keep today's behavior (report-only,
    exit non-zero only on a blocking draft decomposition) so existing runs are unaffected. Always regenerate
    `validation_report.md` via the existing `REPORT_MD.write_text(...)` path so the
    worklist reproduces. Do NOT triage or edit any content here — that is Plan 02; this
    task only surfaces + gates. Document `python -m content.validate --gate` in
    tools/content/README.md as the build gate criterion 1 references.
  </action>
  <acceptance_criteria>
    - Before Plan 02 triage: `python -m content.validate --gate` (from `tools/`) exits NON-ZERO because LIVE-NODE cards (taa/thaa reach-ahead nodes) still violate — and prints a finding count
    - A dormant reach-ahead config never trips the gate: after Plan 02, `--gate` exits 0 with `alif.buildSentence.hear` still present in exercises.json (proof the gate is live-node-scoped, not config-scoped)
    - The 4 D-09 exceptions do NOT trip the gate (exempted like L1/L2)
    - `python -m content.validate` (no flag) still exits 0 and rewrites validation_report.md; README.md documents the `--gate` command
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam/tools && python -m content.validate; echo "no-flag exit=$?"; python -m content.validate --gate; echo "gate exit=$? (expected non-zero pre-triage)"</automated>
  </verify>
  <done>The gate exits non-zero on the live worklist and 0 once content is clean; the report is regenerated; the command is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author/tooling → curriculum assets | validate.py only READS assets; it never writes content. Its output (the predicate + gate) is the parity source the write-path seeder (L2) trusts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-T | Tampering | the shared predicate diverging from the Dart lint | mitigate | Task 1 pins the predicate to read STORED `letters[]` (same input as `unlearnedFor`); Plan 03 adds a parity assertion |
| T-25-01-I | Info disclosure | the report contains child data | accept | validation_report.md contains only curriculum strings + intro orders — no child identifiers or strokes (verified: no child data reaches this tool) |
| T-25-01-SC | Tampering | supply chain (new packages) | accept | no new npm/pip/cargo installs this plan — validate.py uses stdlib + the existing content.arabic module only |
</threat_model>

<verification>
- `python -m content.validate --gate` exits non-zero pre-triage, 0 post-triage (Plan 02 proves the 0 case).
- The shared predicate is importable and behaviorally matches the Dart `unlearnedFor`.
- validation_report.md regenerated.
</verification>

<success_criteria>
Criterion 1 (partial): the audit machinery can report ZERO unlabeled + ZERO violating cards and gate the build on it. The predicate is the single definition L2 imports.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-01-SUMMARY.md` when done.
</output>
