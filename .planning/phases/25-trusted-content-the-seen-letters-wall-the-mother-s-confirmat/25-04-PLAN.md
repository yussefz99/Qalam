---
phase: 25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat
plan: 04
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - tools/firebase/seed_curriculum_v2.py
  - tools/firebase/test_seed_curriculum_v2.py
  - tools/firebase/README.md
autonomous: true
requirements: [QP-07, D-12]
must_haves:
  truths:
    - "The seeder refuses a crafted reach-ahead card BEFORE the first Firestore write — nothing the bundle lint would refuse can reach prod (criterion 2)"
    - "The refusal reuses the SAME predicate as the L0 audit (imported from validate.py) so L1/L2 refuse identically"
    - "A crafted-fixture pytest proves the refusal raises SystemExit with NO write having occurred"
  artifacts:
    - path: "tools/firebase/seed_curriculum_v2.py"
      provides: "_assert_learned_letters_legal fail-fast guard in the pre-write pass"
      contains: "_assert_learned_letters_legal"
    - path: "tools/firebase/test_seed_curriculum_v2.py"
      provides: "the crafted-fixture refusal test (no live DB, no service-account key)"
      contains: "SystemExit"
  key_links:
    - from: "tools/firebase/seed_curriculum_v2.py"
      to: "tools/content/validate.py::unlearned_letters_for_exercise"
      via: "python import — the wall's parity"
      pattern: "unlearned_letters_for_exercise"
    - from: "tools/firebase/seed_curriculum_v2.py::_assert_learned_letters_legal"
      to: "the pre-write validation pass (before doc(id).set)"
      via: "fail-fast raise SystemExit, nothing written"
      pattern: "raise SystemExit"
---

<objective>
L2 — close the Firestore-first bypass. The device reads curriculum content
Firestore-first (memory: letters-firestore-first), so a bad seed reaches the child even
when the bundle lint passes. `seed_curriculum_v2.py` validates only payload SHAPE
today; add a learned-letters refusal — a sibling of `_assert_firestore_legal` — that
refuses any reach-ahead card BEFORE the first write, reusing the L0 predicate so the
seeder and the audit refuse the SAME thing.

Purpose: criterion 2 — the seeder rejects a crafted violating fixture; nothing can reach
prod Firestore that the bundle lint would refuse.
Output: the new guard wired into the fail-fast pre-write pass + a crafted-fixture pytest
that needs no live database or service-account key.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-CONTEXT.md
@.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the learned-letters refusal to the pre-write pass (D-06)</name>
  <files>tools/firebase/seed_curriculum_v2.py, tools/firebase/README.md</files>
  <read_first>
    - tools/firebase/seed_curriculum_v2.py (the file being modified — `_assert_firestore_legal` L101–109, the pre-write validation pass L145–151, the writes L153–164, path constants L62–71)
    - tools/content/validate.py `unlearned_letters_for_exercise` + `load_intro_order` + `live_graph_node_ids` + `OWNER_APPROVED_EXCEPTIONS` (Plan 01 — ALL imported from content.validate; the single Python source of the predicate, the live-node set, and the 4 D-09 exception ids)
    - test/curriculum/learned_letters_lint_test.dart L55–93 (`_discoverUnitGraphs`/`liveNodeIds`) + L112–125 (`baaOwnerApprovedExceptions`) — L1's live-node scoping + exception set the seeder must match exactly
    - 25-PATTERNS.md "seed_curriculum_v2.py — L2 seeder refusal" (L125–160): the exact mirror shape + the D-06 shared-checker import note
    - 25-CONTEXT.md D-06 (add the learned-letters check so violating content is refused before the first write) + the Firestore-first rationale in code_context (L196–199)
  </read_first>
  <action>
    Add `_assert_learned_letters_legal(ex, order, live_ids)` mirroring
    `_assert_firestore_legal` (L101–109). It applies ONLY when `ex["id"] in live_ids`
    (a live graph node) — a dormant config (referenced by NO live graph node; 15 exist
    today, e.g. `baa.buildSentence.hear`) is seeded but NEVER refused, because it is never
    presented and L1 scopes the same way (`liveNodeIds`). For a live node: call
    `unlearned_letters_for_exercise(ex, order)`; if non-empty AND `ex["id"] not in
    OWNER_APPROVED_EXCEPTIONS`, `raise SystemExit("seed_curriculum_v2: doc '<id>' demands
    unseen letter(s) <…>; nothing was written.")`. IMPORT `unlearned_letters_for_exercise`,
    `load_intro_order`, `live_graph_node_ids`, and `OWNER_APPROVED_EXCEPTIONS` from
    content.validate (do NOT re-hardcode the exception ids — the single Python source is
    validate.py, keeping L0/L2 in exact parity). Compute `live_ids = live_graph_node_ids()`
    and `order = load_intro_order()` once. Wire the call into the pre-write pass (L145–151,
    inside the `for e in exercises` loop) so it runs BEFORE the first `doc(id).set(...)`
    write (L153) — fail-fast, no partial seed. Do NOT change the write logic or the CLI
    (dormant configs are STILL written — only their enforcement is skipped).
  </action>
  <acceptance_criteria>
    - `_assert_learned_letters_legal` raises SystemExit for a reach-ahead LIVE-NODE card, and is a no-op for a legal card, for the 4 D-09 exceptions, AND for a dormant reach-ahead config (not a live node)
    - It imports the exception set + live-node set from content.validate (grep shows `from` ... `import` ... `OWNER_APPROVED_EXCEPTIONS` and `live_graph_node_ids` — no re-hardcoded id list)
    - The call sits inside the pre-write validation pass, before any `db.collection(...).document(...).set(...)`
    - `seed(stub_db, None)` over the CURRENT (Plan-02-triaged) real content — which STILL contains the 15 dormant reach-ahead configs — does NOT raise and writes every doc to the stub (proof the seeder can complete `--all` and dormant configs pass through)
    - `python -m py_compile tools/firebase/seed_curriculum_v2.py` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && python -m py_compile tools/firebase/seed_curriculum_v2.py && grep -q "_assert_learned_letters_legal" tools/firebase/seed_curriculum_v2.py && grep -q "unlearned_letters_for_exercise" tools/firebase/seed_curriculum_v2.py && echo ok</automated>
  </verify>
  <done>The seeder refuses reach-ahead content before the first write, reusing the L0 predicate; real triaged content still seeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crafted-fixture refusal test — no live DB, no key</name>
  <files>tools/firebase/test_seed_curriculum_v2.py</files>
  <behavior>
    - A crafted illegal exercise that IS a live graph node (`{"id":"baa.x","type":"writeWord","letters":["taa"],…}`, with `baa.x` injected into the live-node set) raises SystemExit
    - The stub Firestore client records ZERO `.set()` calls when the refusal fires (fail-fast, no partial seed)
    - A DORMANT reach-ahead config (same reach-ahead letters[], but id NOT in the live-node set) does NOT raise — it is seeded normally (proof the refusal is live-node-scoped, matching L0/L1)
    - A crafted LEGAL card and each of the 4 D-09 exceptions are accepted (stub records writes)
    - The test touches no real service-account key and no live Firestore (mirrors test_roundtrip.py)
  </behavior>
  <read_first>
    - tools/content/test_promote_letter.py L47–61 (`restore_targets` fixture: snapshot + restore touched files; `_load`/`_read_bytes` helpers) — the primary analog
    - tools/firebase/test_roundtrip.py header L4–13 (proving a Firestore transform WITHOUT a live database or key) — the secondary analog
    - tools/firebase/seed_curriculum_v2.py `seed(db, letter)` L117–171 (the function under test; note it takes an injectable `db`)
    - 25-PATTERNS.md "NEW: seeder-refusal test" (L364–381): the fixture shape + the combine instruction
  </read_first>
  <action>
    Create tools/firebase/test_seed_curriculum_v2.py (pytest). Build a
    `_StubFirestore` whose `.collection(...).document(...).set(...)` chain appends to a
    `writes` list (no network). Cases: (a) a crafted ILLEGAL card that IS a live node —
    call `_assert_learned_letters_legal(card, order, live_ids={card_id})` directly (or seed
    a stub graph so `card_id` is a live node) — assert it raises `SystemExit` AND
    `stub.writes == []`; (b) a DORMANT reach-ahead config (same reach-ahead letters[], id
    NOT in `live_ids`) — assert NO raise (seeded); (c) a crafted LEGAL card — assert the
    write happened; (d) each of the 4 D-09 exceptions (in `OWNER_APPROVED_EXCEPTIONS`) —
    assert NOT refused. Prefer in-memory/injected inputs so no on-disk restore is needed;
    use the `restore_targets`-style fixture from test_promote_letter.py only if you mutate
    a file. No real key, no live Firestore.
  </action>
  <acceptance_criteria>
    - `pytest tools/firebase/test_seed_curriculum_v2.py -q` exits 0
    - The illegal LIVE-NODE case asserts BOTH `raises(SystemExit)` AND zero writes on the stub
    - The DORMANT reach-ahead case asserts NO raise (the guard is live-node-scoped, not config-scoped)
    - A legal-card case + a D-09-exception case assert the write DID happen (the guard is not over-broad)
    - The test imports no `firebase_admin.credentials.ApplicationDefault` live path (no key needed)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/Qalam/qalam && pytest tools/firebase/test_seed_curriculum_v2.py -q</automated>
  </verify>
  <done>The crafted violating fixture is proven refused before any write; a legal fixture is proven accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeder → prod Firestore (`graphs`/`exercises`/`units`) | THE prod-content write path. Firestore-first means bad content on prod reaches children directly. This is the genuinely security-relevant surface of the phase. |
| device → Firestore (client writes) | out of scope here — already deny-all via deployed rules (06.1-05: `allow write: if false`); content is written ONLY via this admin-SDK seeder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-T | Tampering | crafted/violating content bypassing the shape-only validator to reach prod Firestore | mitigate | Task 1's `_assert_learned_letters_legal` refuses reach-ahead content in the fail-fast pre-write pass, reusing the L0 predicate (criterion 2) |
| T-25-04-E | Elevation | client writing content directly (bypassing the seeder guard) | mitigate | deployed deny-all Firestore rules (06.1-05) — clients cannot write content; verified, not re-implemented here |
| T-25-04-I | Info disclosure | child PII/strokes in the seeded payload or the refusal message | accept/mitigate | curriculum docs carry no child data; the SystemExit message names only the doc id + the unseen letter (no child identifiers) |
| T-25-04-SC | Tampering | supply chain (new packages) | accept | no new pip installs — uses the existing firebase_admin + the content.validate module |
</threat_model>

<verification>
- `pytest tools/firebase/test_seed_curriculum_v2.py -q` passes (criterion 2).
- The refusal reuses `unlearned_letters_for_exercise` (import present).
- Real triaged content still seeds against a stub without raising.
</verification>

<success_criteria>
Criterion 2: the seeder rejects a crafted violating fixture before any write; nothing can reach prod Firestore that the bundle lint would refuse.
</success_criteria>

<output>
Create `.planning/phases/25-trusted-content-the-seen-letters-wall-the-mother-s-confirmat/25-04-SUMMARY.md` when done.
</output>
