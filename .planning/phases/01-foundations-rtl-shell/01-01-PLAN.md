---
phase: 01-foundations-rtl-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - l10n.yaml
  - lib/l10n/app_en.arb
  - assets/fonts/.gitkeep
  - assets/icons/.gitkeep
  - test/theme_test.dart
  - test/data/app_database_test.dart
  - test/direction_test.dart
  - test/numeral_isolation_test.dart
  - test/glyph_audit_golden_test.dart
  - test/orientation_test.dart
autonomous: true
requirements: [PLAT-02]
must_haves:
  truths:
    - "flutter pub get resolves the Phase-1 dependency set (Riverpod codegen, Drift, go_router, flutter_svg, flutter_localizations) with no version conflict"
    - "The four bundled font families (Noto Naskh Arabic, Cairo, Fredoka, Nunito) are declared in pubspec.yaml with TTFs present in assets/fonts/"
    - "gen-l10n is enabled with a single English ARB (app_en.arb) and no app_ar.arb"
    - "All six Wave-0 test files exist and FAIL (red) for the right reason — referencing not-yet-built lib/ symbols — establishing the validation scaffold"
    - "riverpod_lint is wired via analysis_server_plugin in analysis_options.yaml, NOT custom_lint"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Phase-1 dependency pins + bundled font + asset + gen-l10n declarations"
      contains: "flutter_riverpod"
    - path: "analysis_options.yaml"
      provides: "riverpod_lint via analysis_server_plugin"
      contains: "analysis_server_plugin"
    - path: "l10n.yaml"
      provides: "gen-l10n config pointing at lib/l10n/app_en.arb"
    - path: "lib/l10n/app_en.arb"
      provides: "English-only UI strings from the UI-SPEC copy table"
      contains: "Open Practice"
    - path: "test/glyph_audit_golden_test.dart"
      provides: "the D-12 risk-gate golden test scaffold (red until GlyphAuditScreen exists)"
    - path: "test/data/app_database_test.dart"
      provides: "in-memory Drift persist/read-across-restart test scaffold (D-09)"
  key_links:
    - from: "pubspec.yaml fonts: declaration"
      to: "TextStyle.fontFamily strings used in later theme work"
      via: "exact family-name match (Pitfall 3)"
      pattern: "Noto Naskh Arabic"
    - from: "pubspec.yaml generate: true + l10n.yaml"
      to: "AppLocalizations generated delegate"
      via: "Flutter gen-l10n build step"
      pattern: "generate:\\s*true"
---

<objective>
Stand up the Phase-1 foundation toolchain and the Wave-0 validation scaffold so every
later task in this phase has a real, failing test to turn green. This is the
test-first step of the Walking Skeleton: wire the verified dependency set, bundle the
four offline TTF fonts, enable the gen-l10n seam (English-only), configure
riverpod_lint correctly, and create the six failing test files that encode Phase 1's
acceptance criteria (theme tokens, Drift persistence, LTR/RTL direction, numeral
isolation, glyph-audit golden, landscape lock).

Purpose: Establish the validation contract (VALIDATION.md Wave-0) before any
production code exists, so the next plans implement against red tests — and lock the
exact package versions, font family strings, and analysis-plugin wiring that the rest
of the phase depends on.

Output: A resolvable pubspec, bundled fonts, gen-l10n config, correct analysis_options,
and six red test files under test/.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-rtl-shell/01-CONTEXT.md
@.planning/phases/01-foundations-rtl-shell/01-RESEARCH.md
@.planning/phases/01-foundations-rtl-shell/01-PATTERNS.md
@.planning/phases/01-foundations-rtl-shell/01-VALIDATION.md
@.planning/phases/01-foundations-rtl-shell/01-UI-SPEC.md
@CLAUDE.md
@pubspec.yaml
@android/app/src/main/AndroidManifest.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the verified dependency set, bundle fonts, enable gen-l10n + riverpod_lint plugin</name>
  <files>pubspec.yaml, analysis_options.yaml, l10n.yaml, lib/l10n/app_en.arb, assets/fonts/.gitkeep, assets/icons/.gitkeep</files>
  <read_first>
    - pubspec.yaml (the existing scaffold — extend, do not rewrite; preserve cupertino_icons + flutter_lints ^6.0.0)
    - .planning/phases/01-foundations-rtl-shell/01-RESEARCH.md §Standard Stack (verified versions) and §Fonts
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md "pubspec.yaml" section (exact dependency + fonts block shape) and "Riverpod-codegen wiring" shared pattern
    - docs/design/kit/project/SKILL.md (offline rule — NO google_fonts/CDN)
  </read_first>
  <action>
    Extend pubspec.yaml (do not rewrite) adding dependencies at the RESEARCH-verified pins:
    flutter_riverpod ^3.3.1, riverpod_annotation ^4.0.2, drift ^2.33.0,
    sqlite3_flutter_libs ^0.6.0, path_provider ^2.1.0, go_router ^17.2.3, flutter_svg ^2.3.0,
    and flutter_localizations from sdk. dev_dependencies: build_runner ^2.15.0,
    riverpod_generator ^4.0.3, riverpod_lint ^3.1.3, drift_dev ^2.33.0. Under flutter:
    set generate: true (gen-l10n, D-07); declare assets: assets/fonts/, assets/icons/;
    declare the four bundled font families with the EXACT family strings
    "Noto Naskh Arabic", "Cairo", "Fredoka", "Nunito" (the string must later match
    TextStyle.fontFamily exactly — Pitfall 3). Source the OFL static TTFs (Noto Naskh
    Arabic Regular; Cairo Regular + the ~600 logo weight; Fredoka Medium 500 + SemiBold
    600; Nunito Regular 400 + SemiBold 600 per D-03/D-04) into assets/fonts/ — Arabic is
    never bold so Noto Naskh Regular is sufficient. Add assets/fonts/.gitkeep and
    assets/icons/.gitkeep so the dirs exist. Create l10n.yaml pointing at
    lib/l10n/app_en.arb (arb-dir: lib/l10n, template-arb-file: app_en.arb,
    output-localization-file: app_localizations.dart). Create lib/l10n/app_en.arb with
    the UI-SPEC copy-table strings (Open Practice, Clear, Your Journey Starts Soon, Write
    Here, Settings, the database error string, the Clear-confirm strings) and an @@locale
    of en — NO app_ar.arb (Pitfall 4). Replace/create analysis_options.yaml to keep
    include: package:flutter_lints/flutter.yaml AND register riverpod_lint via the
    analysis_server_plugin mechanism (plugins: section), NOT custom_lint (RESEARCH
    State-of-Art). Run flutter pub get to confirm resolution.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exits 0 with no version-solve conflict.
    - `grep -v '^#' pubspec.yaml | grep -c flutter_riverpod` returns >= 1, and likewise for drift, go_router, riverpod_generator, drift_dev, flutter_localizations.
    - `grep -c 'family: Noto Naskh Arabic' pubspec.yaml` returns 1; Cairo, Fredoka, Nunito families likewise declared.
    - `grep -c 'generate: true' pubspec.yaml` returns 1.
    - assets/fonts/ contains at least NotoNaskhArabic-Regular.ttf, a Cairo TTF, a Fredoka TTF, a Nunito TTF (ls exits with the files present).
    - l10n.yaml exists and references app_en.arb; lib/l10n/app_en.arb contains the string "Open Practice"; no file named app_ar.arb exists.
    - analysis_options.yaml contains "analysis_server_plugin" and does NOT contain "custom_lint".
  </acceptance_criteria>
  <done>
    pubspec resolves; four font families bundled and declared with exact family strings;
    gen-l10n enabled English-only; riverpod_lint wired via analysis_server_plugin.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the six failing Wave-0 test files (the validation scaffold)</name>
  <files>test/theme_test.dart, test/data/app_database_test.dart, test/direction_test.dart, test/numeral_isolation_test.dart, test/glyph_audit_golden_test.dart, test/orientation_test.dart</files>
  <read_first>
    - .planning/phases/01-foundations-rtl-shell/01-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the test→behavior spec)
    - .planning/phases/01-foundations-rtl-shell/01-RESEARCH.md §Glyph-Audit Method (ZWJ form-forcing, PASS/FAIL) and §Western-Numeral Isolation (LRI/PDI)
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md "Test files (Wave 0)" table and "lib/dev/glyph_audit_screen.dart" section
    - test/widget_test.dart (existing boilerplate — reuse only the testWidgets/test harness skeleton; delete the counter body)
  </read_first>
  <action>
    Create six test files under test/ that encode Phase-1 acceptance and FAIL now
    (red) because the lib/ symbols they import do not yet exist — this is intentional
    (the next plans turn them green). (1) test/theme_test.dart — unit: import the
    theme and assert semantic tokens equal the CSS hex (primary == Color(0xFF168A8F),
    bg == Color(0xFFFAF6EE), reward == Color(0xFFF2A60C)); assert the QalamTheme
    ThemeExtension exposes reward/warnSoft/buttonShadow. (2) test/data/app_database_test.dart
    — unit: construct AppDatabase with NativeDatabase.memory(), write a key/value via the
    settings API, then simulate restart by reading it back through a second access and
    assert the value survives (D-09). (3) test/direction_test.dart — widget: pump a
    screen and assert Directionality.of(context) is TextDirection.ltr in chrome, and
    TextDirection.rtl only inside an ArabicText island (D-05). (4) test/numeral_isolation_test.dart
    — widget: render an ArabicText with a mixed Arabic+digit string and assert the
    rendered string contains literal U+0030–U+0039 digits LTR-isolated with LRI/PDI
    (U+2066/U+2069), never Eastern digits U+0660–U+0669 (D-06). (5) test/glyph_audit_golden_test.dart
    — golden: pump GlyphAuditScreen and matchesGoldenFile for the four-form grid
    (D-12 risk gate); mark the golden as TODO-baseline so it is red until the screen and
    baseline land. (6) test/orientation_test.dart — assert main() sets
    SystemChrome.setPreferredOrientations to landscapeLeft+landscapeRight and assert the
    AndroidManifest contains android:screenOrientation (D-10). Use package:qalam/...
    imports for the not-yet-existing symbols so failures are compile/import failures, the
    correct red. Do NOT stub the lib/ side here — that is the next plans' job.
  </action>
  <verify>
    <automated>flutter test 2>&1 | grep -Eq 'Error|failed|No such|not found' && echo RED_AS_EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist: test/theme_test.dart, test/data/app_database_test.dart, test/direction_test.dart, test/numeral_isolation_test.dart, test/glyph_audit_golden_test.dart, test/orientation_test.dart.
    - `flutter test` reports failures (red) — and the failures are import/missing-symbol failures referencing qalam lib/ symbols, NOT syntax errors in the test files themselves.
    - `grep -c '0xFF168A8F' test/theme_test.dart` returns >= 1 (asserts the real primary hex).
    - `grep -Ec '0x0660|0x0669|٠|٩' test/numeral_isolation_test.dart` returns >= 1 (the test references Eastern-digit codepoints it forbids).
    - test/glyph_audit_golden_test.dart references matchesGoldenFile and GlyphAuditScreen.
    - test/data/app_database_test.dart references NativeDatabase.memory.
  </acceptance_criteria>
  <done>
    Six red Wave-0 tests exist, each failing on a missing lib/ symbol (not a self-error),
    encoding the D-01/D-02, D-05, D-06, D-09, D-10, D-12 acceptance behaviors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | pub.dev packages pulled at `flutter pub get` — supply-chain surface |
| (no others) | Phase 1 has no network, no auth, no user input, no PII, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pub.dev installs (riverpod, drift, go_router, flutter_svg, etc.) | accept | All packages are first-party Flutter-team or mature ecosystem packages with official source repos (RESEARCH §Package Legitimacy Audit — none [ASSUMED]/[SUS]/[SLOP]); pins are RESEARCH-verified versions. No human-verify checkpoint required because no package is flagged. |
| T-01-01 | Information Disclosure | bundled assets / l10n strings | accept | No PII, no secrets, no keys in Phase 1 (tutor key is v2, server-only); fonts and English UI strings are public/non-sensitive. |
</threat_model>

<verification>
- `flutter pub get` resolves cleanly.
- `flutter test` is RED (expected at Wave 0) with import-level failures against qalam lib/ symbols.
- Font families declared with exact strings; gen-l10n English-only; analysis_server_plugin (not custom_lint).
</verification>

<success_criteria>
- The Phase-1 dependency set resolves and the four offline TTFs are bundled+declared.
- gen-l10n is wired English-only (app_en.arb, no app_ar.arb).
- riverpod_lint is configured via analysis_server_plugin.
- Six Wave-0 test files exist and fail for the right reason (missing lib/ symbols).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-rtl-shell/01-01-SUMMARY.md` when done.
</output>
