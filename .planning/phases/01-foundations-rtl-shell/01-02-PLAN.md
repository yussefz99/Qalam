---
phase: 01-foundations-rtl-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/theme/colors.dart
  - lib/theme/text_styles.dart
  - lib/theme/dimens.dart
  - lib/theme/brand_theme_ext.dart
  - lib/theme/app_theme.dart
  - lib/data/app_database.dart
  - lib/widgets/arabic_text.dart
  - lib/router/app_router.dart
  - lib/screens/home_screen.dart
  - lib/app.dart
  - lib/main.dart
  - android/app/src/main/AndroidManifest.xml
  - assets/logo.svg
autonomous: true
requirements: [PLAT-02]
must_haves:
  truths:
    - "The app launches on an Android tablet, locked to landscape at both the manifest and SystemChrome layers, with no global RTL"
    - "The Home screen renders the قلم logo, parchment background, and one real vocalized Arabic string through the ArabicText RTL island — proving connected-script rendering end-to-end"
    - "A trivial value written to the Drift DB is read back after a simulated restart (in-memory test green) and shown on Home, proving the persistence seam"
    - "Widgets read semantic theme tokens (primary/bg/reward) from the theme layer, never raw hex"
    - "App default direction is LTR (English chrome); only the ArabicText island is RTL"
  artifacts:
    - path: "lib/theme/colors.dart"
      provides: "semantic color tokens translated from colors_and_type.css"
      contains: "0xFF168A8F"
    - path: "lib/theme/brand_theme_ext.dart"
      provides: "QalamTheme ThemeExtension for reward/warnSoft/buttonShadow/motion"
      contains: "ThemeExtension"
    - path: "lib/data/app_database.dart"
      provides: "Drift @DriftDatabase with injectable executor + key/value settings"
      contains: "DriftDatabase"
    - path: "lib/widgets/arabic_text.dart"
      provides: "the RTL-island + Noto Naskh + numeral-isolation signature widget"
      contains: "TextDirection.rtl"
    - path: "lib/router/app_router.dart"
      provides: "go_router skeleton with /parent redirect seam"
      contains: "GoRouter"
    - path: "lib/main.dart"
      provides: "ProviderScope bootstrap + landscape SystemChrome lock"
      contains: "setPreferredOrientations"
  key_links:
    - from: "lib/widgets/arabic_text.dart"
      to: "Noto Naskh Arabic bundled font"
      via: "TextStyle.fontFamily exact match + Directionality(rtl)"
      pattern: "Noto Naskh Arabic"
    - from: "lib/screens/home_screen.dart"
      to: "lib/data/app_database.dart"
      via: "Riverpod provider reading the persisted settings value"
      pattern: "ref\\.watch"
    - from: "lib/main.dart"
      to: "android/app/src/main/AndroidManifest.xml"
      via: "belt-and-suspenders landscape lock (runtime + platform)"
      pattern: "landscapeLeft"
---

<objective>
Build the Walking Skeleton: the thinnest runnable end-to-end slice that proves every
foundation seam at once. After this plan a user can launch the Android-tablet app
(landscape-locked), see the قلم-branded Home screen on warm parchment, read one real
vocalized Arabic string rendered correctly through the ArabicText RTL island, and see a
value that was persisted to and read back from the local Drift database. This turns the
theme, direction, database, and orientation Wave-0 tests green.

Purpose: Prove the four foundation seams (design-token theme, RTL content island +
Arabic shaping, Drift persistence across restart, landscape lock) in one cohesive
running app — the architectural backbone every later phase extends. Sets the
canonical patterns for theme, state (Riverpod codegen), routing, and the ArabicText
widget for the first time.

Output: A runnable app with the theme layer, ArabicText widget, Drift DB, go_router
skeleton, landscape lock, and a Home screen wiring them together.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-rtl-shell/01-CONTEXT.md
@.planning/phases/01-foundations-rtl-shell/01-RESEARCH.md
@.planning/phases/01-foundations-rtl-shell/01-PATTERNS.md
@.planning/phases/01-foundations-rtl-shell/01-UI-SPEC.md
@.planning/phases/01-foundations-rtl-shell/01-SKELETON.md
@CLAUDE.md
@docs/design/kit/project/colors_and_type.css
@docs/design/kit/project/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theme layer — translate design-kit tokens to Dart (colors, type, dimens, ThemeExtension, app_theme)</name>
  <files>lib/theme/colors.dart, lib/theme/text_styles.dart, lib/theme/dimens.dart, lib/theme/brand_theme_ext.dart, lib/theme/app_theme.dart</files>
  <read_first>
    - docs/design/kit/project/colors_and_type.css (THE source of truth — lines 9–58 palette+semantic, 60–96 type scale, 98–153 spacing/radii/shadow/motion, 169–186 utilities)
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md sections "lib/theme/colors.dart", "text_styles.dart", "dimens.dart", "brand_theme_ext.dart", "app_theme.dart"
    - .planning/phases/01-foundations-rtl-shell/01-UI-SPEC.md (Color, Typography, Spacing Scale tables — the Phase-1 role subset)
    - test/theme_test.dart (the red test this turns green — match its expected token names/values)
  </read_first>
  <behavior>
    - test/theme_test.dart: QalamColors.primary == Color(0xFF168A8F), bg == Color(0xFFFAF6EE), reward == Color(0xFFF2A60C), warnSoft == Color(0xFFFF8A6B), fg == Color(0xFF222A2E).
    - QalamTheme ThemeExtension exposes reward, rewardTint, warnSoft, success, buttonShadow, and is retrievable via Theme.of(context).extension<QalamTheme>().
    - app_theme exposes scaffoldBackgroundColor == parchment (never 0xFFFFFFFF) and a textTheme wired to Fredoka/Nunito (D-04).
  </behavior>
  <action>
    Create lib/theme/colors.dart translating the CSS :root palette to private Color
    consts (_parchment 0xFFFAF6EE, _softAqua 0xFFEAF4F4, _inkTeal 0xFF168A8F, _deepInk
    0xFF0E5B5F, _goldInk 0xFFF2A60C, _leaf 0xFF3FB984, _coral 0xFFFF8A6B, _inkCharcoal
    0xFF222A2E, _slate 0xFF5C6B70) plus the tints (CSS L23–31), exposing a QalamColors
    class of SEMANTIC tokens widgets read (bg, surface, surfaceRaised 0xFFFFFFFF, border,
    fg, fgMuted, fgOnPrimary, primary, primaryPressed, primaryTint, reward, rewardTint,
    success, warnSoft). Create lib/theme/text_styles.dart with the English roles
    (Fredoka display/heading/button 500/600, Nunito body 18/400, label 16/600) and the
    Arabic roles (arBody: Noto Naskh Arabic, 26px, height 1.7, w400, letterSpacing 0;
    arDisplay: Cairo, 96px, height 1.1, w500; bundled families per D-03) — CRITICAL: never set letterSpacing on any
    Arabic style (Pitfall 2 / #71220); declare the full --fz scale even though only the
    UI-SPEC roles render this phase. Create lib/theme/dimens.dart for space1..space24,
    targetMin/Comfy/Large (64/72/96), radii sm..pill, shadows as List<BoxShadow>
    (shadowSm/Md/Lg) and the signature flat-bottom sticker buttonShadow (0 4px 0 deepInk),
    and motion (easeOutQuart Cubic(0.22,1,0.36,1); durFast 140 / durBase 220 / durSlow
    420 / durCheer 700 as Durations). Create lib/theme/brand_theme_ext.dart: an
    @immutable QalamTheme extends ThemeExtension<QalamTheme> carrying reward, rewardTint,
    warnSoft, warnSoftTint, success, successTint, buttonShadow, inkStroke, and motion
    tokens with copyWith + lerp. Create lib/theme/app_theme.dart: useMaterial3 true,
    scaffoldBackgroundColor = QalamColors.bg (parchment, never white — D-02), ColorScheme
    overridden to ink-teal primary, textTheme wired from text_styles (D-04), default
    fontFamily Nunito, extensions: [QalamTheme(...)]. Delete the deepPurple seed entirely.
    All tokens semantic — no raw hex leaks into widgets downstream (D-01/D-02).
  </action>
  <verify>
    <automated>flutter test test/theme_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/theme_test.dart` exits 0 (green).
    - `grep -c '0xFF168A8F' lib/theme/colors.dart` returns >= 1; 0xFFFAF6EE, 0xFFF2A60C, 0xFFFF8A6B likewise present.
    - lib/theme/brand_theme_ext.dart contains "extends ThemeExtension<QalamTheme>" and implements copyWith and lerp.
    - `grep -c 'letterSpacing' lib/theme/text_styles.dart` shows letterSpacing only as 0 / unset on Arabic styles (no nonzero value on an Arabic TextStyle).
    - lib/theme/app_theme.dart contains no occurrence of "deepPurple" and no literal 0xFFFFFFFF as scaffold background.
  </acceptance_criteria>
  <done>
    Theme layer exposes semantic tokens + QalamTheme extension matching the kit CSS;
    test/theme_test.dart is green; no deepPurple; Arabic styles carry no letterSpacing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drift DB (persist/read across restart) + ArabicText island + go_router skeleton + landscape lock</name>
  <files>lib/data/app_database.dart, lib/widgets/arabic_text.dart, lib/router/app_router.dart, lib/app.dart, lib/main.dart, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md sections "lib/data/app_database.dart", "lib/widgets/arabic_text.dart", "lib/router/app_router.dart", "lib/main.dart", "lib/app.dart", and the AndroidManifest section
    - .planning/phases/01-foundations-rtl-shell/01-RESEARCH.md §Western-Numeral Isolation (LRI/PDI technique), §Mixed-direction shell, §Code Examples (landscape lock, digit isolation)
    - test/data/app_database_test.dart, test/numeral_isolation_test.dart, test/direction_test.dart, test/orientation_test.dart (the red tests this turns green)
    - android/app/src/main/AndroidManifest.xml (the existing <activity> — add only android:screenOrientation; keep configChanges)
  </read_first>
  <behavior>
    - test/data/app_database_test.dart: write key="seedProof" value=v via in-memory AppDatabase, simulate restart, read back == v (D-09).
    - test/numeral_isolation_test.dart: ArabicText("سنة 2026") renders literal '2','0','2','6' wrapped in LRI(U+2066)…PDI(U+2069); contains no U+0660–U+0669 (D-06).
    - test/direction_test.dart: ambient Directionality is ltr in chrome; rtl only inside ArabicText (D-05).
    - test/orientation_test.dart: SystemChrome preferred orientations == [landscapeLeft, landscapeRight]; manifest has android:screenOrientation (D-10).
  </behavior>
  <action>
    Create lib/data/app_database.dart: a @DriftDatabase(tables:[AppSettings]) class with
    schemaVersion 1 and a constructor accepting an optional QueryExecutor (default opens a
    LazyDatabase over NativeDatabase at getApplicationDocumentsDirectory()/qalam.db via
    path_provider) so tests inject NativeDatabase.memory(). AppSettings = key TEXT PK,
    value TEXT — the trivial persist-proof; expose putSetting(key,value) and
    getSetting(key). Store NOTHING sensitive (RESEARCH §Security). Provide it via a
    @riverpod code-generated provider (Riverpod-only, no BLoC/GetX — D-11; part 'app_database.g.dart'). Create lib/widgets/arabic_text.dart —
    the signature widget bundling three concerns: (1) Directionality(textDirection:
    TextDirection.rtl) as the ONLY RTL island (D-05), (2) Text with arBody/arDisplay
    Noto Naskh style (letterSpacing 0), (3) a private _isolateDigits() that wraps any
    0–9 run in LRI U+2066 … PDI U+2069 (D-06) and optionally FontFeature.tabularFigures();
    NEVER intl.NumberFormat on ar (forbidden). Create lib/router/app_router.dart: a
    @riverpod GoRouter appRouter with initialLocation '/' and routes for '/' (HomeScreen),
    '/practice' (placeholder for plan 03), '/settings' (placeholder for plan 03), and a
    COMMENTED /parent redirect SEAM only — do not build the PIN gate (P9, D-08). Create
    lib/app.dart: QalamApp ConsumerWidget returning MaterialApp.router(routerConfig from
    appRouterProvider, theme: qalam app_theme, localizationsDelegates from generated
    AppLocalizations, supportedLocales const [Locale('en')] — NEVER add Locale('ar')
    (Pitfall 4), debugShowCheckedModeBanner false); NO global Directionality (D-05).
    Replace lib/main.dart: Future<void> main() that ensureInitialized, awaits
    SystemChrome.setPreferredOrientations([landscapeLeft, landscapeRight]) (D-10 runtime
    half), runApp(ProviderScope(child: QalamApp())). Modify AndroidManifest.xml: add
    android:screenOrientation="sensorLandscape" to the existing <activity> (D-10 platform
    half) — keep configChanges untouched. Run build_runner to generate the Drift + Riverpod
    code.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/data/app_database_test.dart test/numeral_isolation_test.dart test/direction_test.dart test/orientation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - build_runner generates app_database.g.dart and app_router.g.dart with no errors.
    - The four named test files all exit 0 (green).
    - lib/widgets/arabic_text.dart contains "TextDirection.rtl" and references the LRI/PDI codepoints (⁦ / ⁩) for digit isolation, and contains NO occurrence of "NumberFormat".
    - lib/router/app_router.dart contains "GoRouter" and a /parent seam comment but NO live PIN-gate redirect logic.
    - lib/main.dart contains "setPreferredOrientations" with landscapeLeft and landscapeRight; AndroidManifest.xml contains "android:screenOrientation".
    - lib/app.dart supportedLocales contains Locale('en') and does NOT contain Locale('ar').
  </acceptance_criteria>
  <done>
    Drift persists+reads a value across a simulated restart; ArabicText renders RTL with
    LTR-isolated Western digits; go_router skeleton + /parent seam in place; app boots LTR
    landscape-locked at both layers; the four tests are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Home screen — wire logo, theme, ArabicText, and the persisted DB value end-to-end</name>
  <files>lib/screens/home_screen.dart, assets/logo.svg</files>
  <read_first>
    - .planning/phases/01-foundations-rtl-shell/01-UI-SPEC.md (Screen Shells — Home row; Copywriting Contract — Home strings; Color reserved-for list)
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md "lib/screens/*.dart" section and "Brand assets" shared pattern
    - lib/widgets/arabic_text.dart, lib/data/app_database.dart, lib/theme/app_theme.dart (the pieces being wired)
    - docs/design/kit/project/assets/logo.svg and assets/ICONOGRAPHY.md (logo usage; no emoji, no pseudo-icons)
  </read_first>
  <behavior>
    - HomeScreen renders the قلم logo via flutter_svg, parchment background, a soft-aqua placeholder card with the "Your Journey Starts Soon" heading and its body copy (from gen-l10n, not hardcoded).
    - HomeScreen renders one real vocalized Arabic string (e.g. a curriculum-sample letter/word with tashkeel) through ArabicText — the on-screen proof of connected-script rendering.
    - HomeScreen reads a value written to the Drift DB on first build via the Riverpod provider and displays it (the persistence seam, visible), with NO running stars/totals/streaks (D-13).
  </behavior>
  <action>
    Copy docs/design/kit/project/assets/logo.svg into assets/ (declared in plan 01).
    Create lib/screens/home_screen.dart as a ConsumerWidget: parchment scaffold
    (QalamColors.bg), an app bar showing the قلم logo wordmark via flutter_svg (Cairo
    rendering), a centered soft-aqua placeholder card (radius-xl, shadowMd) containing the
    "Your Journey Starts Soon" heading + body copy pulled from AppLocalizations (D-07, no
    hardcoded strings), one ArabicText rendering a real vocalized Arabic sample string (a
    representative letter/word with tashkeel — the visible connected-script proof), and a
    single primary "Open Practice" CTA styled with the sticker buttonShadow that navigates
    to /practice. On first build, write a trivial value (e.g. key "skeletonProof",
    value a timestamp/sentinel) to the Drift DB via the provider, then read it back and
    show it small/muted to make the persistence seam visible. Use semantic tokens only
    (no raw hex); ink-teal reserved per the UI-SPEC list (CTA + app-bar accent). NO stars,
    totals, streaks, badges, emoji, or unicode pseudo-icons (D-13). Voice: second person,
    Title Case (UI-SPEC copy contract).
  </action>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` exits 0 (no errors).
    - `flutter test` exits 0 — all non-golden Wave-0 tests green (the glyph-audit golden remains pending until plan 03; if its baseline is absent the suite excludes it or it is the only known-red, documented in the summary).
    - lib/screens/home_screen.dart references ArabicText, flutter_svg/SvgPicture, and reads the DB provider via ref.watch.
    - `grep -Ec '⭐|✓|✗|🎉|Color\(0xFF' lib/screens/home_screen.dart` returns 0 (no emoji/pseudo-icons/raw hex).
    - lib/screens/home_screen.dart pulls heading/body via AppLocalizations (gen-l10n), not string literals for the copy-table entries.
    - assets/logo.svg exists.
  </acceptance_criteria>
  <done>
    Home screen runs end-to-end: قلم logo, parchment, a correctly-shaped vocalized Arabic
    string via ArabicText, the round-tripped Drift value shown, an Open Practice CTA — no
    gamification, no raw hex; analyze + the non-golden suite are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app ↔ local Drift DB (app-private storage) | The only persistence boundary; single-device, offline |
| (no network / auth / PII boundary) | Phase 1 has no network, no account, no child PII (profiles are P5), no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | Drift DB file (qalam.db) | mitigate | DB opens in getApplicationDocumentsDirectory() (app-private storage, not external/shared); Phase 1 persists only a trivial non-sensitive sentinel value — store nothing sensitive (carry-forward rule, RESEARCH §Security). |
| T-01-03 | Tampering | local SQLite at rest | accept | Single-device local-only app, no multi-user, no remote sync; on-device file tampering is out of scope for v1 (no PII to protect yet; real child-data controls land in P5). |
| T-01-04 | Information Disclosure | telemetry/logging | mitigate | Wire NO network and NO telemetry; do not log the persisted value or any device data (carry-forward "local-only ≠ privacy-handled"). |
</threat_model>

<verification>
- `dart run build_runner build` generates Drift + Riverpod code cleanly.
- `flutter analyze` is clean; the non-golden Wave-0 suite (theme, db, numeral, direction, orientation) is green.
- App boots LTR + landscape-locked; Home shows logo, a correctly-shaped Arabic string, and the round-tripped DB value.
</verification>

<success_criteria>
- The Walking Skeleton runs: landscape-locked Android-tablet app, theme from tokens, go_router skeleton, Drift persist/read across restart, one ArabicText RTL island rendering correct connected-script Arabic.
- D-01/D-02 (theme), D-05 (LTR app / RTL island), D-06 (Western numerals), D-09 (persistence), D-10 (landscape) tests are green.
- No raw hex, no gamification, no global RTL.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-rtl-shell/01-02-SUMMARY.md` when done.
</output>
