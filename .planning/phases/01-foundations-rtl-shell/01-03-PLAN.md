---
phase: 01-foundations-rtl-shell
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/dev/glyph_audit_screen.dart
  - test/glyph_audit_golden_test.dart
  - test/glyph_audit_golden_test.dart.golden
  - lib/screens/practice_screen.dart
  - lib/screens/settings_screen.dart
  - lib/router/app_router.dart
autonomous: false
requirements: [PLAT-02]
must_haves:
  truths:
    - "Noto Naskh Arabic shapes every representative curriculum letter in all four contextual forms (isolated/initial/medial/final) at 96px with no tofu, correct joining, and لا as a single ﻻ ligature — proven by the GlyphAuditScreen and gated by a golden test"
    - "A human has visually confirmed the first glyph-audit baseline against a known-good reference (the D-12 risk gate) before phase verification"
    - "The Practice screen renders smooth live freehand ink (deep-ink, round caps, smoothed) on a parchment canvas with a Clear action — the stylus spike, no scoring/guide"
    - "The Settings screen is a parchment placeholder carrying only a routing-seam comment for the future PIN-gated /parent branch (not built)"
    - "Western digits beside Arabic in the audit grid render 0–9 in correct LTR order"
  artifacts:
    - path: "lib/dev/glyph_audit_screen.dart"
      provides: "the D-12 four-form ZWJ audit grid at 96px"
      contains: "GlyphAuditScreen"
    - path: "test/glyph_audit_golden_test.dart.golden"
      provides: "the approved baseline golden image (regression gate)"
    - path: "lib/screens/practice_screen.dart"
      provides: "CustomPainter freehand ink spike + Clear"
      contains: "CustomPainter"
    - path: "lib/screens/settings_screen.dart"
      provides: "placeholder settings shell + /parent routing-seam comment"
      contains: "parent"
  key_links:
    - from: "lib/dev/glyph_audit_screen.dart"
      to: "bundled Noto Naskh Arabic font"
      via: "arDisplay style at 96px shaping the four ZWJ-forced forms"
      pattern: "200D|\\\\u200D|‍"
    - from: "test/glyph_audit_golden_test.dart"
      to: "lib/dev/glyph_audit_screen.dart"
      via: "matchesGoldenFile regression gate"
      pattern: "matchesGoldenFile"
    - from: "lib/screens/practice_screen.dart"
      to: "Listener/pointer events"
      via: "freehand stroke capture into a CustomPainter"
      pattern: "CustomPaint"
---

<objective>
Close the phase's one hard risk (R3 / D-12) and complete the shell. Build the debug
GlyphAuditScreen that renders the representative curriculum letter set in all four
contextual forms at 96px (using the ZWJ form-forcing technique), lock it behind a golden
regression test, and gate it with a human visual-PASS against a known-good reference —
proving Noto Naskh Arabic shapes connected-script Arabic correctly before any tracing
surface is built. Then finish the two remaining placeholder screens: the Practice
stylus-spike (live freehand ink) and the Settings shell (with only a /parent routing
seam).

Purpose: D-12 is the phase's risk gate — if the font mis-shapes a curriculum letter we
must know now (and switch to the documented Amiri fallback) before P3 builds the dotted
guide. The Practice ink spike de-risks the stylus rendering treatment; Settings leaves
the P9 parent seam without building it.

Output: GlyphAuditScreen + approved golden baseline, Practice ink screen, Settings
shell, and the human visual-PASS recorded.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-rtl-shell/01-CONTEXT.md
@.planning/phases/01-foundations-rtl-shell/01-RESEARCH.md
@.planning/phases/01-foundations-rtl-shell/01-PATTERNS.md
@.planning/phases/01-foundations-rtl-shell/01-VALIDATION.md
@.planning/phases/01-foundations-rtl-shell/01-UI-SPEC.md
@CLAUDE.md
@docs/design/kit/project/colors_and_type.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GlyphAuditScreen (four-form ZWJ grid) + golden regression gate — the D-12 risk gate</name>
  <files>lib/dev/glyph_audit_screen.dart, lib/router/app_router.dart, test/glyph_audit_golden_test.dart, test/glyph_audit_golden_test.dart.golden</files>
  <read_first>
    - .planning/phases/01-foundations-rtl-shell/01-RESEARCH.md §Glyph-Audit Method (representative letter set table, ZWJ form-forcing table, harness shape, PASS/FAIL criteria, Amiri fallback)
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md "lib/dev/glyph_audit_screen.dart" section
    - test/glyph_audit_golden_test.dart (the red golden test from plan 01 — turn it green with a baseline)
    - lib/widgets/arabic_text.dart, lib/theme/text_styles.dart (arDisplay 96px style the cells use)
  </read_first>
  <behavior>
    - GlyphAuditScreen renders a grid: rows = the representative set (ه, ع/غ, ك, ل, لا, ب/ت/ث, ج/ح/خ, س/ش, م, ي + a tashkeel row + a mixed Arabic+Western-digit row); columns = isolated/initial/medial/final.
    - Each form is produced by the ZWJ technique (isolated 'ه'; initial 'ه‍'; medial '‍ه‍'; final '‍ه') and rendered via Directionality(rtl) + arDisplay at 96px.
    - test/glyph_audit_golden_test.dart pumps GlyphAuditScreen and matchesGoldenFile against the committed baseline (regression gate).
  </behavior>
  <action>
    Create lib/dev/glyph_audit_screen.dart: a debug-only screen reachable from a dev route
    rendering the audit grid per RESEARCH §Glyph-Audit. Rows = the representative
    tricky-joiner set (RESEARCH table: ه ع/غ ك ل لا ب/ت/ث ج/ح/خ س/ش م ي), one row with
    tashkeel at line-height 2.0, and one row mixing Arabic + Western digits 0–9. Columns =
    the four contextual forms, each forced ONLY in this harness with Zero-Width Joiner
    U+200D (isolated bare; initial = ZWJ after; medial = ZWJ both sides; final = ZWJ
    before) — ZWJ is audit-harness-only, NEVER in production strings. Each cell:
    Directionality(rtl) + Text(form, style: arDisplay) at 96px (--fz-ar-display) so
    shaping is inspectable at child-facing size. Ensure the BUNDLED Noto Naskh is the
    shaping font (family string matches pubspec exactly; do a swap-test note in comments
    per Pitfall 3). Add a hidden/dev GoRoute '/dev/glyph-audit' in app_router.dart (debug
    seam, not in user nav). Generate the golden baseline
    (flutter test --update-goldens test/glyph_audit_golden_test.dart) and commit
    test/glyph_audit_golden_test.dart.golden so the golden becomes a regression gate.
    Record in the summary that the baseline is PROVISIONAL until the human visual-PASS in
    Task 3. If shaping fails any PASS criterion (tofu, wrong contextual form, لا not a
    single ﻻ, broken joins, clipped tashkeel), switch the bundled Arabic font to Amiri
    (documented fallback) and regenerate — note the switch in the summary.
  </action>
  <verify>
    <automated>flutter test test/glyph_audit_golden_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/glyph_audit_golden_test.dart` exits 0 against the committed baseline.
    - test/glyph_audit_golden_test.dart.golden exists (the baseline image).
    - lib/dev/glyph_audit_screen.dart references the ZWJ codepoint (U+200D / ‍) for form-forcing and renders cells via Directionality(rtl) + the 96px arDisplay style.
    - lib/dev/glyph_audit_screen.dart includes the لا (lam-alef) and a tashkeel row and a mixed Arabic+digit row.
    - app_router.dart contains a '/dev/glyph-audit' debug route and it is NOT surfaced in the user-facing nav.
  </acceptance_criteria>
  <done>
    GlyphAuditScreen renders the four-form ZWJ grid at 96px from the bundled font; the
    golden test passes against a committed (provisional) baseline; لا/tashkeel/digit rows
    present; dev route wired.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Practice stylus-spike (live freehand ink) + Settings placeholder shell</name>
  <files>lib/screens/practice_screen.dart, lib/screens/settings_screen.dart, lib/router/app_router.dart</files>
  <read_first>
    - .planning/phases/01-foundations-rtl-shell/01-UI-SPEC.md (Ink-Stroke Rendering table — stroke color/width/caps/join/smoothing/anti-alias; Screen Shells — Practice + Settings rows; Copywriting — Write Here / Clear / Clear-confirm / Settings strings)
    - .planning/phases/01-foundations-rtl-shell/01-PATTERNS.md "lib/screens/{home,practice,settings}_screen.dart" section
    - lib/theme/app_theme.dart, lib/theme/dimens.dart (canvas frame radius-xl, shadowMd; ink color deep-ink)
  </read_first>
  <behavior>
    - PracticeScreen captures freehand pointer input and paints smoothed strokes (deep-ink #0E5B5F, width 6px in 4–8 range, round cap, round join, quadratic/Catmull-Rom smoothing, isAntiAlias true) on a parchment canvas framed by a soft-aqua card.
    - A single Clear action clears the canvas, guarded by the "Clear your writing?" confirm (Clear / Keep Writing; coral-tinted, no red).
    - SettingsScreen is a parchment placeholder with the Settings copy and a code comment marking the /parent routing seam (no PIN gate).
  </behavior>
  <action>
    Create lib/screens/practice_screen.dart — the only interactive P1 screen and the
    stylus spike. A CustomPainter ink surface over a parchment canvas inset on a soft-aqua
    framed card (radius-xl 28, shadowMd). Capture pointer input (Listener/GestureDetector),
    accumulate sampled points per stroke, and paint smoothed paths: deep-ink #0E5B5F via
    QalamColors.primaryPressed, strokeWidth 6 (4–8 range), StrokeCap.round, StrokeJoin.round,
    quadratic/Catmull-Rom smoothing between points (not raw polylines), isAntiAlias true.
    Empty state heading "Write Here" + body from gen-l10n. A single Clear secondary action
    that shows the "Clear your writing?" confirm dialog (buttons Clear coral-tinted soft /
    Keep Writing default — no red, no red X) and clears on confirm. OUT OF SCOPE (do NOT
    build): dotted guide letter, stroke-order, scoring, failing-stroke highlight, star,
    confetti, palm/finger rejection (P3). Create lib/screens/settings_screen.dart — a
    parchment placeholder with the "Settings" heading + body and a list of placeholder
    rows; add a clear code comment marking the routing SEAM for the future PIN-gated
    /parent/* branch (P9) — do NOT build the PIN gate. Wire both into the existing
    /practice and /settings routes (replacing the plan-02 placeholders); the active nav
    item uses the ink-teal accent. All copy via gen-l10n; semantic tokens only; no emoji,
    no pseudo-icons, no gamification (D-13).
  </action>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` exits 0 and `flutter test` exits 0 (full suite green).
    - lib/screens/practice_screen.dart references CustomPaint/CustomPainter, StrokeCap.round, StrokeJoin.round, and isAntiAlias.
    - `grep -Ec 'Colors\.red|0xFFFF0000|✗' lib/screens/practice_screen.dart` returns 0 (no red, no red X).
    - lib/screens/practice_screen.dart contains NO reference to scoring/dottedGuide/star (out-of-scope items not built).
    - lib/screens/settings_screen.dart contains a comment referencing the /parent seam and NO PIN-gate logic.
    - `grep -Ec '⭐|✓|✗|🎉|Color\(0xFF' lib/screens/practice_screen.dart lib/screens/settings_screen.dart` returns 0.
  </acceptance_criteria>
  <done>
    Practice renders smooth on-brand freehand ink with a guarded Clear; Settings is a
    placeholder with only the /parent seam; full suite + analyze green; no out-of-scope
    features, no red, no gamification.
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Human visual-PASS of the glyph-audit baseline — the D-12 risk gate</name>
  <files>(verification-only — no files modified)</files>
  <read_first>
    - .planning/phases/01-foundations-rtl-shell/01-VALIDATION.md (Manual-Only Verifications — glyph-audit visual PASS instructions)
    - .planning/phases/01-foundations-rtl-shell/01-RESEARCH.md §Glyph-Audit Method PASS/FAIL criteria + Amiri fallback
  </read_first>
  <action>
    The glyph-audit golden baseline (Task 1) must be confirmed correct by a human against
    a known-good reference before phase verification — golden tests gate regressions but a
    human must confirm the FIRST baseline genuinely shows correct shaping. This is the
    D-12 risk gate and cannot be auto-approved (the perceptual judgment is the whole
    point). Launch the app on a tablet/emulator and open the dev route
    /dev/glyph-audit (or run the golden and open test/glyph_audit_golden_test.dart.golden).
  </action>
  <human-check>
    Open the GlyphAuditScreen (dev route /dev/glyph-audit) on a tablet/emulator, or open
    the committed golden image test/glyph_audit_golden_test.dart.golden. For each
    representative letter compare its four forms against a known-good reference (the
    design-kit specimen or the same string in a browser) and confirm ALL of:
    1. Every cell shows a glyph at 96px — no tofu / □ / missing-box.
    2. Each form is the correct contextual shape (initial connects left, final connects
       right, medial connects both).
    3. لا renders as the single ﻻ ligature, NOT ل + ا side by side.
    4. Connected sequences stay connected — no broken joins, no isolated glyphs mid-word.
    5. Tashkeel marks sit correctly above/below the base letter at line-height 2.0 — no
       overlap/clipping.
    6. Western digits beside Arabic render as 0–9 in correct LTR order.
    Reply "approved" if all six pass. If ANY fails, reply with the failing letter/form;
    the fix is to switch the bundled Arabic font to Amiri (documented fallback),
    regenerate the golden, and re-run this check.
  </human-check>
  <verify>
    <human-check>Human replies "approved" confirming the six PASS criteria; record PASS (and any Amiri switch) in the phase verification notes.</human-check>
  </verify>
  <done>
    A human has confirmed the glyph-audit baseline is genuinely correct (or triggered the
    Amiri fallback and re-confirmed), and the PASS is recorded — the D-12 risk gate is
    closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylus/pointer → Practice canvas | Local, in-memory stroke points; not persisted, not transmitted |
| (no network / auth / PII) | Phase 1 shell has no network, no account, no child PII, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | captured ink strokes (Practice spike) | mitigate | Strokes are held in-memory only and discarded on Clear/navigation; Phase 1 does NOT persist or log stroke data (carry-forward "no PII/stroke logging" rule; real handling is P3+). |
| T-01-06 | Information Disclosure | debug GlyphAuditScreen / dev route | accept | The /dev/glyph-audit route renders only public Arabic letters and Western digits — no sensitive data; it is a debug-only seam not surfaced in user nav. |
</threat_model>

<verification>
- `flutter test` is fully green including the glyph-audit golden against the committed baseline.
- The human visual-PASS of the glyph-audit baseline is recorded (D-12 gate closed; Amiri fallback noted if triggered).
- Practice renders smooth on-brand ink with a guarded Clear; Settings carries only the /parent seam.
</verification>

<success_criteria>
- D-12 proven: Noto Naskh shapes all four contextual forms correctly (no tofu, لا ligature, joins intact, tashkeel placed), golden-gated and human-confirmed.
- Practice stylus-spike renders smooth deep-ink freehand on parchment with Clear; no scoring/guide/star.
- Settings is a placeholder with only the P9 /parent routing seam.
- Full suite + analyze green; no red, no gamification, no out-of-scope features.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-rtl-shell/01-03-SUMMARY.md` when done.
</output>
