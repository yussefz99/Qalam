---
phase: 06-lesson-progression-home
plan: 08
type: execute
wave: 5
depends_on: [06-03, 06-07]
files_modified:
  - lib/core/strokes/stroke_normalization.dart
  - lib/dev/authoring_export.dart
  - lib/features/practice/widgets/stroke_order_animation.dart
  - lib/features/practice/widgets/ghost_comparison.dart
  - lib/features/practice/practice_screen.dart
  - lib/l10n/app_en.arb
  - test/features/practice/ghost_comparison_test.dart
  - test/features/practice/stroke_order_animation_test.dart
autonomous: true
requirements: [S1-09]
must_haves:
  truths:
    - "After a wobbly (failed) letter, the child can tap 'Watch the Difference' and see their stroke replayed beside Qalam's reference at half speed (D-21)"
    - "The child's stroke renders in coral (warnSoft), the reference in deep-ink — teaching moment, never error-shaming, never red"
    - "Stroke points live in widget State only — never in providers, never in the DB (T-03-01 holds; grep guard on practice_providers.dart stays 0)"
    - "Existing stroke-order animation and authoring export behave exactly as before (default-preserving parameterization)"
  artifacts:
    - path: "lib/features/practice/widgets/ghost_comparison.dart"
      provides: "Side-by-side half-speed replay panel (Yours / Qalam's)"
      min_lines: 60
    - path: "lib/core/strokes/stroke_normalization.dart"
      provides: "Shared combined-bbox normalization (extracted, not re-derived)"
  key_links:
    - from: "lib/features/practice/widgets/ghost_comparison.dart"
      to: "lib/features/practice/widgets/stroke_order_animation.dart"
      via: "parameterized duration/color replay machinery"
      pattern: "StrokeOrderAnimation"
    - from: "lib/features/practice/practice_screen.dart"
      to: "lib/features/practice/widgets/ghost_comparison.dart"
      via: "in-memory failing strokes from widget State"
      pattern: "GhostComparison"
---

<objective>
The slow-motion ghost comparison (D-21): when a stroke fails shape checks, the
ShowFix zone offers "Watch the Difference" — the child's stroke (coral, half
speed) beside the reference (deep-ink) — reusing the battle-tested
StrokeOrderAnimation machinery and the Phase-4 combined-bbox normalization.
In-memory strokes only; persistence of stroke points stays forbidden.

Purpose: D-21 ride-along strengthening the clean-rep loop that S1-09's pass rule
depends on — the child understands the named fix by SEEING the difference.
Output: ghost_comparison.dart, parameterized animation widget, shared
normalization, ShowFix trigger, tests proving the no-persistence invariant.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
@.planning/phases/06-lesson-progression-home/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared normalization + parameterize StrokeOrderAnimation (default-preserving)</name>
  <files>lib/core/strokes/stroke_normalization.dart, lib/dev/authoring_export.dart, lib/features/practice/widgets/stroke_order_animation.dart, test/features/practice/stroke_order_animation_test.dart</files>
  <read_first>
    - lib/dev/authoring_export.dart (normalizeToStrokeSpecs ~line 90 + _combinedBounds: the combined-bbox math that already solves the dot-position pitfall — extract, do not re-derive)
    - lib/features/practice/widgets/stroke_order_animation.dart (controller at ~49-77 with hardcoded QalamMotion.durWrite; ink Paint at ~145-151 with hardcoded QalamColors.inkStroke; StrokeSpec input shape)
    - test/features/practice/stroke_order_animation_test.dart (existing contract that must stay green)
  </read_first>
  <action>
    1. Create lib/core/strokes/stroke_normalization.dart (pure Dart): move the
    combined-bbox normalization so it can serve BOTH CapturedStroke (authoring)
    and raw `List&lt;List&lt;Offset&gt;&gt;` (the child's in-memory strokes) into
    0..1 StrokeSpecs — e.g., a core function over plain
    `List&lt;List&lt;List&lt;double&gt;&gt;&gt;` plus thin adapters.
    authoring_export.dart delegates to it; its public API and output values are
    unchanged (existing authoring/calibration tests must pass untouched).
    2. stroke_order_animation.dart: add optional constructor params `Duration?
    duration` (default QalamMotion.durWrite) and `Color? color` (default
    QalamColors.inkStroke), threaded to the controller and ink Paint. Defaults
    preserve current behavior exactly.
    3. Extend stroke_order_animation_test.dart: custom duration/color are applied;
    default construction unchanged.
  </action>
  <verify>
    <automated>flutter test test/features/practice/stroke_order_animation_test.dart test/features/authoring/ test/curriculum/ 2>/dev/null || flutter test test/features/practice/stroke_order_animation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing stroke_order_animation and authoring-related tests green without modification
    - lib/core/strokes/stroke_normalization.dart has no Flutter-widget imports (dart:ui Offset adapters allowed at the edge or in callers)
    - `grep -c "duration" lib/features/practice/widgets/stroke_order_animation.dart` shows the new param wired to the AnimationController
    - No duplicated bbox math: authoring_export's normalization body now delegates (grep its file for a single import of the new core file)
  </acceptance_criteria>
  <done>Replay machinery is reusable at any speed/color; normalization has one home.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GhostComparison widget + ShowFix trigger + no-persistence proof</name>
  <files>lib/features/practice/widgets/ghost_comparison.dart, lib/features/practice/practice_screen.dart, lib/l10n/app_en.arb, test/features/practice/ghost_comparison_test.dart</files>
  <read_first>
    - lib/features/practice/practice_screen.dart (_onLetterComplete ~88-116 where strokes exist and are discarded after scoring; _ActionRow showFix case ~970-990 with _GhostButton beside _PrimaryButton; the canvas card Stack hosting the ghost-cast overlay — the panel's home per RESEARCH Open Q2)
    - lib/features/practice/widgets/stroke_order_animation.dart (parameterized API from Task 1)
    - .planning/phases/06-lesson-progression-home/06-UI-SPEC.md (Ghost comparison copy + interaction contract: labels, half speed durWrite×2 = 2800ms linear, coral vs deep-ink, replayable)
    - lib/providers/practice_providers.dart (ANTI-PATTERN 3 GUARD — must remain untouched by this task)
  </read_first>
  <behavior>
    ghost_comparison_test.dart:
    - Renders two labeled panels: "Yours" and "Qalam's" (ghostCompareYours/
      ghostCompareQalams), title "Watch the difference." — never wrong/right wording
    - Child stroke animation configured with warnSoft color and durWrite×2
      duration, linear curve; reference with inkStroke
    - Replayable: a replay affordance restarts the animation
    - practice_screen: after a failing letter, ShowFix's action row shows "Watch
      the Difference" (ghostCompareButton) beside Show Me Again; tapping opens the
      panel fed from widget State; strokes cleared on retry/lesson change
  </behavior>
  <action>
    1. app_en.arb: add ghostCompareButton = "Watch the Difference",
    ghostCompareTitle = "Watch the difference.", ghostCompareYours = "Yours",
    ghostCompareQalams = "Qalam's" (UI-SPEC exact copy); flutter gen-l10n.
    2. ghost_comparison.dart: stateless-ish widget taking the child's normalized
    StrokeSpecs + the reference StrokeSpecs; side-by-side panels inside the canvas
    card Stack (Open Q2 recommendation), each a StrokeOrderAnimation — child:
    color QalamColors.warnSoft, duration QalamMotion.durWrite * 2 (2800ms), linear
    curve (even pen speed); reference: QalamColors.inkStroke, same duration.
    Labels at Label scale, title at Title scale. Replay restarts both.
    SECURITY header comment: strokes arrive from widget State, are never persisted.
    3. practice_screen.dart: retain the last FAILING letter's strokes in the
    State class only (set in _onLetterComplete when the result fails shape
    checks; cleared on retry, on pass, and on dispose — T-03-01 permits State
    retention, forbids persistence). Normalize via the shared core function. Add
    the ghost button to _ActionRow's showFix case beside Show Me Again
    (>= targetMin, >= 16px gap). Only show the button when failing strokes are
    held.
    4. No-persistence proof in the test file: assert
    `grep "List&lt;Offset&gt;" lib/providers/practice_providers.dart` count is 0
    via the existing convention (or a direct test that the controller state type
    carries no stroke fields), and that no repository/database API receives
    coordinate data from the ghost path (the panel takes data only from State).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/practice/ghost_comparison_test.dart test/features/practice/practice_screen_test.dart && grep -c "List<Offset>" lib/providers/practice_providers.dart | grep -qx "0" && echo "T-03-01 holds"</automated>
  </verify>
  <acceptance_criteria>
    - ghost_comparison_test.dart green: labels, colors (warnSoft child / inkStroke reference), 2800ms duration, replay
    - `grep -c "List<Offset>" lib/providers/practice_providers.dart` returns 0 (file untouched by this plan)
    - `grep -c "warnSoft" lib/features/practice/widgets/ghost_comparison.dart` >= 1 and `grep -c "Colors.red" lib/features/practice/widgets/ghost_comparison.dart` returns 0
    - practice_screen_test.dart stays green (no regression to 06-07's reconciled tests)
    - Ghost button only rendered in showFix with retained strokes (tested)
  </acceptance_criteria>
  <done>The child can watch their stroke beside Qalam's at half speed; stroke privacy invariant proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Child's raw stroke points → replay UI | The most privacy-sensitive data in the app transits widget State |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information disclosure (stroke-persistence creep, T-03-01) | ghost replay path | mitigate | Strokes retained in PracticeScreen State ONLY; cleared on retry/pass/dispose; grep guard on practice_providers.dart stays 0; no repository call carries coordinates; SECURITY header on the widget |
| T-06-09 | Gamification/shaming creep | comparison framing | mitigate | UI-SPEC copy contract: "Yours"/"Qalam's", never wrong-vs-right; coral not red |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/features/practice/ — green (minus documented environmental drift)
- Full suite green except documented font-drift goldens; device UAT (stylus replay feel) at the end-of-phase human gate
</verification>

<success_criteria>
- D-21 shipped exactly per UI-SPEC (copy, colors, half speed, replayable)
- T-03-01 demonstrably intact — no new persistence or provider surface for stroke points
- Zero behavioral change to existing animation/authoring consumers
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-08-SUMMARY.md` when done
</output>
