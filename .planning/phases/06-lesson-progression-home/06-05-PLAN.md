---
phase: 06-lesson-progression-home
plan: 05
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - lib/screens/home_screen.dart
  - test/screens/home_screen_test.dart
autonomous: true
requirements: [S1-01]
must_haves:
  truths:
    - "On launch the child lands on Home showing today's REAL lesson for the active child — live glyph + 'The Letter {name}' — with the whole card as the single Start and no navigation required (S1-01, D-08, D-12)"
    - "The day's letter renders as ink-fill progress: deep-ink opacity deepens per persisted clean rep; no visible rep numerals; never gold (D-09)"
    - "When every available lesson is passed, Home shows the calm all-mastered card whose tap goes to the Journey (D-11)"
    - "The card settles in like a prepared worksheet once per arrival; reduced-motion renders settled immediately (D-13)"
    - "Loading and error states degrade silently (blank glyph / startingLessonId fallback) — never a raw error to the child"
  artifacts:
    - path: "lib/screens/home_screen.dart"
      provides: "Live _TodaysLessonCard (scope guard → reader → layout), ink-fill, prepared-desk entrance, all-mastered variant"
      contains: "todayLessonProvider"
    - path: "test/screens/home_screen_test.dart"
      provides: "Live-card coverage + reconciled Test 4 (Journey navigates, Parent stays Coming soon)"
  key_links:
    - from: "lib/screens/home_screen.dart"
      to: "lib/providers/progression_providers.dart"
      via: "todayLessonProvider + cleanRepsForLetterProvider watch"
      pattern: "cleanRepsForLetterProvider"
    - from: "lib/screens/home_screen.dart"
      to: "/practice"
      via: "card tap with lesson query param"
      pattern: "/practice\\?lesson="
---

<objective>
Make the Home today-card live: real letter from todayLessonProvider (D-08), the
ink-fill progress rendering where the letter IS the progress (D-09), the
prepared-desk entrance (D-13), the dignified all-mastered end state (D-11), and
Home staying single-purpose — one clear Start, replay lives on the Journey only
(D-12). Closes S1-01's landing experience and reconciles the stale Test 4 debt.

Purpose: this is the screen the child sees every single day; S1-01 is the phase's
first success criterion.
Output: live home_screen.dart + extended/reconciled widget tests.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
@.planning/phases/06-lesson-progression-home/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live today-card — provider wiring, ink-fill, all-mastered variant, Test-4 reconciliation</name>
  <files>lib/screens/home_screen.dart, test/screens/home_screen_test.dart</files>
  <read_first>
    - lib/screens/home_screen.dart (the _GreetingHeader 3-layer scope-aware split — the prescribed pattern; the current _TodaysLessonCard layout to KEEP per D-08, key 'todaysLessonCard', hardcoded ArabicText('ا') and context.go('/practice'))
    - test/screens/home_screen_test.dart (existing harness + provider-override patterns; the stale Test 4 at line 204 asserting Journey nav is "Coming soon" — rewrite it, don't delete coverage)
    - lib/providers/progression_providers.dart (todayLessonProvider, cleanRepsForLetterProvider from 06-03)
    - .planning/phases/06-lesson-progression-home/06-UI-SPEC.md (binding color/copy/spacing contracts: ink-fill ramp formula, all-mastered copy, loading/error treatments, anti-gamification omissions)
    - lib/models/letter.dart (glyph char + name.display fields for the card)
  </read_first>
  <behavior>
    Widget tests (extend home_screen_test.dart, providers overridden):
    - Today = baa's lesson → card shows ب glyph and title "The Letter Baa"
      (homeLessonTitleFor); tap navigates to '/practice?lesson=lesson_02'
    - Ink-fill: cleanReps 1 of 3 → glyph Semantics label "1 of 3 clean reps"; NO
      Text widget on the card renders a bare rep numeral (D-09 replaces rep-dots)
    - All-mastered (todayLesson null): eyebrow "YOUR LETTERS", title "You've
      mastered all your letters.", body present; tap goes to '/journey' (D-11,
      D-12); copy is factual — no totals, no hype
    - Loading: blank glyph container + blank title, no spinner (UI-SPEC,
      _GreetingHeader degradation mirror); error: card still renders a Start
    - Reconciled Test 4: Journey nav item NAVIGATES (live since 03.1); Parent nav
      item still shows "Coming soon"
    - Exactly ONE today's-lesson card and no other practice CTA on Home (S1-01/D-12)
  </behavior>
  <action>
    Apply the _GreetingHeader 3-layer split to _TodaysLessonCard: scope guard
    (bare-harness fallback renders the static layout) → ConsumerWidget reader
    watching todayLessonProvider + cleanRepsForLetterProvider(letterId of today's
    lesson item) with full .when degradation → pure layout taking {glyph char,
    display name, int reps, int repsToAdvance, bool allMastered, String lessonId}.
    KEEP the existing card structure (D-08): same Row, glyph container, chevron
    pill, key 'todaysLessonCard'. Ink-fill (UI-SPEC prescriptive): glyph color =
    QalamColors.inkStroke.withValues(alpha: 0.25 + 0.75 * (reps /
    repsToAdvance).clamp(0,1)) — NOT gold, no animation, Semantics label via
    homeInkFillSemantics. Title uses homeLessonTitleFor(letterName) with the
    null-safe l10n ?? fallback idiom. Tap → context.go('/practice?lesson=$lessonId').
    All-mastered variant swaps eyebrow/title/body to the homeAllMastered* keys and
    taps to '/journey'. No draft-status markers anywhere (D-04); no star counts,
    tallies, or streak chrome (PLAT-03 binding omissions). Error state resolves the
    profile's startingLessonId lesson so the child always has a Start.
  </action>
  <verify>
    <automated>flutter test test/screens/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/screens/home_screen_test.dart fully green INCLUDING the rewritten Test 4 (zero remaining references to Journey "Coming soon")
    - `grep -c "QalamColors.reward" lib/screens/home_screen.dart` returns 0 (ink-fill never gold)
    - `grep -cE "context\.go\('/practice'\)" lib/screens/home_screen.dart` returns 0 (always parameterized)
    - Card key 'todaysLessonCard' still present (existing tests/UAT contract)
    - No hardcoded user-facing strings: new copy reads l10n keys with ?? fallbacks
  </acceptance_criteria>
  <done>Home shows the real today lesson with ink-fill progress and a single Start; stale Test 4 reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: Prepared-desk entrance (D-13) with reduced-motion support</name>
  <files>lib/screens/home_screen.dart, test/screens/home_screen_test.dart</files>
  <read_first>
    - lib/features/practice/widgets/mastery_celebration.dart (the AnimationController recipe at ~lines 56-79 — tokens, one-shot forward, tween shapes — adapt for the card)
    - lib/theme/dimens.dart (QalamMotion.easeOutQuart, durSlow 420ms, durBase 220ms)
    - lib/screens/home_screen.dart (the now-live card from Task 1)
  </read_first>
  <action>
    Add the prepared-desk entrance to the today-card (D-13, UI-SPEC Interaction
    contract): card slides up ~24px while fading in — easeOutQuart over durSlow
    (420ms); the letter glyph then fades up over durBase (220ms) after the card
    settles. Plays ONCE per arrival at Home (initState of the card's State — not
    on provider rebuilds; use a played-flag so data refreshes don't replay it).
    If MediaQuery.of(context).disableAnimations is true, skip the controllers and
    render fully settled immediately. Use only QalamMotion tokens — no custom
    curves or magic durations. Test: with disableAnimations true the card is at
    full opacity on first frame; with animations on, pumping durSlow + durBase
    settles it and a provider-triggered rebuild does not restart the entrance.
  </action>
  <verify>
    <automated>flutter test test/screens/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/screens/home_screen_test.dart green including the new entrance tests
    - `grep -c "disableAnimations" lib/screens/home_screen.dart` >= 1
    - `grep -cE "Duration\(milliseconds:" lib/screens/home_screen.dart` returns 0 for the new animation code (tokens only; pre-existing literals, if any, unchanged)
    - flutter analyze lib/screens/home_screen.dart clean
  </acceptance_criteria>
  <done>The lesson card settles in like a prepared worksheet, once, with reduced-motion respected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider data → child-facing screen | Error/loading states must never leak raw errors to a child |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information disclosure (raw error text to a child) | today-card .when(error:) branches | mitigate | Error branch degrades to startingLessonId lesson / settled layout; tested; UI-SPEC error contract |
| T-06-09 | Elevation of privilege (gamification creep violating PLAT-03) | Home surfaces | mitigate | Acceptance greps: no reward-gold on ink-fill, no rep numerals, no totals/streaks; D-04 no draft markers |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/screens/home_screen_test.dart — fully green (Test 4 reconciled)
- Full suite: known-failing set SHRINKS by the reconciled home test; font-drift goldens unchanged
- Device UAT (end-of-phase, human-gated): fresh launch → today's lesson card → single Start (VALIDATION manual-only)
</verification>

<success_criteria>
- S1-01 landing: launch → today's real lesson, one Start, zero navigation (automated where emulatable; device UAT at phase gate)
- D-08/D-09/D-11/D-12/D-13 all observable on the live card; anti-gamification invariants grep-enforced
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-05-SUMMARY.md` when done
</output>
