---
phase: 06-lesson-progression-home
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - lib/core/scoring/stroke_validation.dart
  - test/data/curriculum_repository_test.dart
autonomous: false
requirements: [S1-01, S1-09]
must_haves:
  truths:
    - "The 9 legitimate curl letters (jeem, haa_c, khaa, saad, daad, taa_h, ayn, ghayn, faa) load through the validator without throwing"
    - "A genuine closed-outline stroke (first≈last ≈ 0.0 over a long perimeter) is still rejected by the NOT-CLOSED guard"
    - "The owner has confirmed the 0.10 kClosedLoopEpsilon threshold before it ships"
  artifacts:
    - path: "lib/core/scoring/stroke_validation.dart"
      provides: "D-04 closed-loop guard with curl-admitting kClosedLoopEpsilon"
      contains: "kClosedLoopEpsilon = 0.10"
    - path: "test/data/curriculum_repository_test.dart"
      provides: "Load-time validator regression coverage incl. curl-letter admission"
  key_links:
    - from: "test/data/curriculum_repository_test.dart"
      to: "lib/core/scoring/stroke_validation.dart"
      via: "CurriculumRepository load path runs validateLetter over the shipped letters.json"
      pattern: "closed outline loop"
---

<objective>
Close Fix A from 06-FIXES.md: the D-04 closed-loop guard in `stroke_validation.dart`
false-positives on 9 legitimate curl letters (jeem, haa_c, khaa, saad, daad, taa_h, ayn,
ghayn, faa), flagging their correct open centerlines as closed outlines so they fail to
load and the app crashes on those letters. Lower `kClosedLoopEpsilon` from 0.30 to 0.10
(the tightest curl, taa_h, ends 0.121 from its start, so 0.10 admits all 9 while still
catching a true ≈0.0 closed outline), and document the curl-vs-outline distinction so the
threshold is not re-loosened by mistake later.

Purpose: Unblock loading of the 9 curl letters so they can be practiced on-device,
restoring the launch→today's-lesson→pass→unlock loop (S1-01, S1-09) for those letters.
Output: Updated `stroke_validation.dart` threshold + doc-comment; extended load-time
validator test coverage proving curl letters load and a true outline is still rejected.

This is a scoring/curriculum threshold change. Per CLAUDE.md ("propose, don't decide" on
pedagogy/curriculum) it carries an owner sign-off gate (`autonomous: false`).
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lesson-progression-home/06-FIXES.md
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@lib/core/scoring/stroke_validation.dart
@lib/models/letter.dart
@test/data/curriculum_repository_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lower kClosedLoopEpsilon to 0.10 and document curl-vs-outline</name>
  <files>lib/core/scoring/stroke_validation.dart, test/data/curriculum_repository_test.dart</files>
  <read_first>
    - .planning/phases/06-lesson-progression-home/06-FIXES.md (Fix A — root cause, the 9 letters with their first≈last / ratio numbers, the 0.30→0.10 rationale)
    - lib/core/scoring/stroke_validation.dart (the `kClosedLoopEpsilon` constant at line 32, its doc-comment lines 26-32, and the NOT-CLOSED check in `validateStroke` at lines 147-161 that consumes it)
    - test/data/curriculum_repository_test.dart (the `_loopLetterJson` fixture lines 10-34 — a true closed outline; the "SHIPPED letters.json passes the validator at load" test lines 277-289; the "closed-loop reference stroke makes load throw" test lines 291-310)
  </read_first>
  <behavior>
    - The shipped `assets/curriculum/letters.json` (which now holds the owner's draft strokes for the 9 curl letters) passes `validateLetter` at load — `getLetters()` resolves all 28 without throwing.
    - The existing `_loopLetterJson` fixture (a single stroke returning to its start over a long perimeter, first≈last ≈ 0.0) still trips NOT-CLOSED — load still throws a StateError whose message contains "closed outline loop".
    - A new regression test asserts each of the 9 curl letters (jeem, haa_c, khaa, saad, daad, taa_h, ayn, ghayn, faa) is present in the loaded set (proves they were not rejected at load).
  </behavior>
  <action>
    In `lib/core/scoring/stroke_validation.dart`, change the constant `kClosedLoopEpsilon`
    from `0.30` to `0.10`. Do NOT touch `kLoopLengthRatio` (stays 1.8) or `kCoordTolerance`.

    Rewrite the doc-comment immediately above `kClosedLoopEpsilon` (currently lines 26-32) to
    explain the curl-vs-outline distinction so it is not re-loosened later: a TRUE closed
    glyph outline returns to ≈0.0 from its start (alif's original outline was 0.2234); a
    LEGITIMATE curl letter (the pen genuinely loops back near the start over a winding path)
    ends 0.12–0.29 away. State that the tightest admitted curl is taa_h at 0.121 and that
    0.10 clears it with margin while still catching a ≈0.0 outline. Reference D-04 and the 9
    affected letters by name. Note the documented alternative for later (enclosed-area or
    two-near-parallel-passes discriminator) without implementing it now.

    In `test/data/curriculum_repository_test.dart`, add a test in the
    "load-time D-04 guard" group asserting the 9 curl letters all load: read the shipped
    `assets/curriculum/letters.json` off disk (mirror the existing shipped-asset test at
    lines 277-289), call `getLetters()`, and assert the loaded id set contains each of
    jeem, haa_c, khaa, saad, daad, taa_h, ayn, ghayn, faa. Leave the existing
    "closed-loop reference stroke makes load throw" test (lines 291-310) intact — it proves
    a genuine outline is still rejected at the lowered threshold.

    Do NOT modify `assets/curriculum/letters.json` stroke data and do NOT flip any
    `signedOff` flag — the curl-letter strokes are the owner's unsigned drafts, reviewed and
    signed off by the owner's mother, out of scope here.
  </action>
  <verify>
    <automated>flutter test test/data/curriculum_repository_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/scoring/stroke_validation.dart` contains `kClosedLoopEpsilon = 0.10` (grep, comments excluded: `grep -v '^[[:space:]]*//' lib/core/scoring/stroke_validation.dart | grep -c 'kClosedLoopEpsilon = 0.10'` ≥ 1).
    - `kLoopLengthRatio = 1.8` is unchanged.
    - The doc-comment above `kClosedLoopEpsilon` mentions both "curl" and "outline" and names taa_h's 0.121 margin (grep for "taa_h" and "curl" in the file).
    - `flutter test test/data/curriculum_repository_test.dart` exits 0.
    - jeem, haa_c, khaa, saad, daad, taa_h, ayn, ghayn, faa all appear in the loaded letter-id set (new test assertion) — none throws at load.
    - The "closed-loop reference stroke makes load throw" test still passes (a true ≈0.0 outline is still rejected).
  </acceptance_criteria>
  <done>
    kClosedLoopEpsilon is 0.10 with a curl-vs-outline doc-comment; the curriculum repository
    test (including the new 9-curl-letter admission test and the retained outline-rejection
    test) passes; letters.json stroke data and signedOff are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm each curl letter is a centerline, not an edge-trace</name>
  <files>test/data/curriculum_repository_test.dart</files>
  <read_first>
    - .planning/phases/06-lesson-progression-home/06-FIXES.md (Fix A "Confirm-before-shipping" paragraph — verify each of the 9 is a centerline (pen-tip path down the middle), not an edge-trace around the outline; if any is an edge-trace it needs re-authoring, not masking by the threshold)
    - lib/core/scoring/stroke_validation.dart (the `kLoopLengthRatio` doc-comment lines 33-40 — an outline traces the full perimeter so its path-length/bbox-diagonal ratio is ~2.3; a centerline curl sits 2.04–2.66 in this data, which is why the ratio gate alone can't separate them, hence the endpoint check)
    - lib/core/scoring/reference_path.dart (ReferencePath.resolve — the geometry every consumer reads)
  </read_first>
  <action>
    Add a diagnostic test in `test/data/curriculum_repository_test.dart` (a new group, e.g.
    "curl-letter centerline sanity (Fix A confirm-before-shipping)") that loads the shipped
    `assets/curriculum/letters.json` and, for each of the 9 curl letters, asserts the
    first reference (non-dot) stroke is an OPEN centerline rather than a closed edge-trace:
    assert its first→last endpoint distance is ≥ 0.10 (so it is admitted by the new
    threshold and is not a ≈0.0 return-to-start outline). Use the numbers in 06-FIXES.md as
    the expected lower bound per letter (taa_h is the tightest at 0.121). This is the
    automated half of the confirm-before-shipping check; it does NOT alter curriculum data.

    Do NOT attempt to re-author any stroke here. If a letter's first→last distance comes back
    below 0.10 (would indicate an edge-trace that the threshold should NOT mask), the test
    fails loudly — that letter then needs re-authoring, which is the owner's / owner's-mother's
    domain and is explicitly OUT OF SCOPE for this plan (surface it to the owner, do not patch
    the data).
  </action>
  <verify>
    <automated>flutter test test/data/curriculum_repository_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/data/curriculum_repository_test.dart` contains a test asserting each of jeem, haa_c, khaa, saad, daad, taa_h, ayn, ghayn, faa has a first reference stroke with first→last endpoint distance ≥ 0.10.
    - `flutter test test/data/curriculum_repository_test.dart` exits 0 (all 9 are centerlines admitted by 0.10; none is an edge-trace masked by the threshold).
    - No edit to `assets/curriculum/letters.json` and no `signedOff` change.
  </acceptance_criteria>
  <done>
    An automated centerline-sanity test proves each of the 9 curl letters is a genuine open
    centerline admitted by the 0.10 threshold (not an edge-trace being masked); any edge-trace
    would fail the test and is flagged for owner re-authoring rather than patched.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Owner sign-off on the 0.10 closed-loop threshold</name>
  <files>(none — owner-confirmation checkpoint; ships the Task 1 change in stroke_validation.dart)</files>
  <action>Pause for the owner to confirm the 0.10 `kClosedLoopEpsilon` threshold before it ships. Present the decision and options below; this is a curriculum/scoring threshold (CLAUDE.md: propose, don't decide). Tasks 1–2 have already proven automatically that all 9 curl letters are open centerlines admitted by 0.10. Record the owner's choice in the SUMMARY. Do NOT auto-approve — `workflow.auto_advance` does not apply to a curriculum-threshold sign-off.</action>
  <read_first>
    - .planning/phases/06-lesson-progression-home/06-FIXES.md (Fix A — the threshold rationale and the confirm-before-shipping requirement)
  </read_first>
  <decision>Confirm lowering the D-04 closed-loop guard `kClosedLoopEpsilon` from 0.30 to 0.10 before it ships.</decision>
  <context>
    The D-04 guard was tuned at 0.30 to catch the Phase-2 closed-outline alif bug. At 0.30 it
    also false-rejects 9 legitimate curl letters (jeem, haa_c, khaa, saad, daad, taa_h, ayn,
    ghayn, faa) whose pen genuinely loops back near the start, so the app crashes on them.
    Lowering to 0.10 admits all 9 (the tightest, taa_h, ends 0.121 from start) while still
    catching a genuine ≈0.0 closed outline. This is a scoring/curriculum threshold, which is
    the owner's call (CLAUDE.md: propose, don't decide on pedagogy). Tasks 1–2 have proven
    automatically that all 9 are open centerlines (not edge-traces) admitted by 0.10.
  </context>
  <options>
    <option id="confirm-0.10">
      <name>Confirm 0.10 (recommended)</name>
      <pros>Unblocks all 9 curl letters; still rejects true closed outlines; backed by per-letter centerline-sanity tests.</pros>
      <cons>Slightly narrower outline-detection margin than 0.30 (still ample: 0.121 tightest vs 0.10).</cons>
    </option>
    <option id="defer-smarter-discriminator">
      <name>Defer to a smarter discriminator</name>
      <pros>Could separate curl-vs-outline by enclosed-area instead of endpoint distance.</pros>
      <cons>More work, not needed now per 06-FIXES.md; leaves the 9 letters crashing until built.</cons>
    </option>
    <option id="re-author-flagged-letter">
      <name>Re-author a specific letter (only if Task 2 flagged an edge-trace)</name>
      <pros>Fixes a genuinely mis-authored stroke at the source rather than masking it.</pros>
      <cons>Owner's-mother sign-off work; only applies if Task 2's centerline-sanity test failed (it should not).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The owner has explicitly confirmed the 0.10 value (or selected an alternative), and the choice is recorded in the plan SUMMARY.
    - If `confirm-0.10` is selected, Tasks 1–2 are already green and the change ships.
  </acceptance_criteria>
  <resume-signal>Select: confirm-0.10, defer-smarter-discriminator, or re-author-flagged-letter</resume-signal>
  <verify>
    <human-check>The owner has explicitly confirmed the 0.10 threshold (or chosen an alternative); the decision is recorded in 06-09-SUMMARY.md.</human-check>
  </verify>
  <done>The owner has signed off on the 0.10 `kClosedLoopEpsilon` value (or selected an alternative), and the choice is recorded in the plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curriculum JSON → validator → scorer | Authored reference strokes (owner drafts) cross into the load path; the D-04 guard is the gate that rejects malformed/outline geometry before it reaches the scorer/animation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Tampering | `kClosedLoopEpsilon` re-loosened later by mistake | mitigate | Rewrite the doc-comment to explain curl-vs-outline and name taa_h's 0.121 margin so the value's rationale is self-documenting. |
| T-06-09-02 | Tampering | A genuine closed-outline stroke slips through the lowered threshold | mitigate | Retain the "closed-loop reference stroke makes load throw" test (≈0.0 outline still rejected at 0.10) + per-letter centerline-sanity test (Task 2). |
| T-06-09-03 | Repudiation | Threshold changed without owner authority over a curriculum decision | mitigate | `autonomous: false` + blocking owner-sign-off checkpoint (Task 3); choice recorded in SUMMARY. |

No package-manager installs in this plan — no T-{phase}-SC supply-chain checkpoint required.
</threat_model>

<verification>
- `flutter test test/data/curriculum_repository_test.dart` exits 0 (curl letters load; true outline still rejected; centerline sanity holds).
- `grep` confirms `kClosedLoopEpsilon = 0.10` and an unchanged `kLoopLengthRatio = 1.8`.
- Owner sign-off recorded (Task 3).
- Do NOT treat `test/glyph_audit_golden_test.dart` as part of this plan's gate — its failure is documented environmental Noto-Naskh font drift, never a regression, and must not be re-baked.
- `assets/curriculum/letters.json` and every `signedOff` flag are unmodified.
</verification>

<success_criteria>
The 9 curl letters (jeem, haa_c, khaa, saad, daad, taa_h, ayn, ghayn, faa) load through the
validator without throwing; a genuine ≈0.0 closed outline is still rejected; the threshold
change is documented and owner-confirmed; curriculum stroke data and sign-off flags are
untouched.
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-09-SUMMARY.md` when done, recording the
owner's sign-off decision on the 0.10 threshold.
</output>
