---
phase: 06-lesson-progression-home
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - lib/providers/progression_providers.dart
  - lib/router/app_router.dart
  - lib/features/practice/practice_screen.dart
  - lib/features/journey/journey_screen.dart
  - lib/l10n/app_en.arb
  - test/providers/progression_providers_test.dart
  - test/router/onboarding_gate_test.dart
autonomous: true
requirements: [S1-01, S1-09]
must_haves:
  truths:
    - "Recording a mastery makes the today-lesson provider recompute to the next lesson without any manual invalidation (S1-09 immediate-on-pass)"
    - "Today's lesson is computed for the ACTIVE child from profile.startingLessonId + mastered set + 28-lesson catalog (S1-01, D-06)"
    - "/practice?lesson=<id> opens that lesson with fresh widget State; null/unknown ids degrade silently to today's lesson — never a raw error to the child"
    - "The Phase-5 onboarding redirect gate is unaffected by query parameters"
  artifacts:
    - path: "lib/providers/progression_providers.dart"
      provides: "masteredLetterIdsProvider (StreamProvider), cleanRepsForLetterProvider (family), progressionProvider/todayLessonProvider"
      contains: "StreamProvider"
    - path: "lib/router/app_router.dart"
      provides: "/practice?lesson= and /journey?highlight= query-parameterized builders"
      contains: "queryParameters"
    - path: "test/providers/progression_providers_test.dart"
      provides: "Wave-0 stream-driven immediacy proof (S1-09)"
      min_lines: 60
  key_links:
    - from: "lib/providers/progression_providers.dart"
      to: "lib/data/app_database.dart"
      via: "watchMasteredLetterIds stream"
      pattern: "watchMasteredLetterIds"
    - from: "lib/providers/progression_providers.dart"
      to: "lib/models/lesson_progression.dart"
      via: "ProgressionSnapshot.compute"
      pattern: "ProgressionSnapshot"
    - from: "lib/router/app_router.dart"
      to: "lib/features/practice/practice_screen.dart"
      via: "PracticeScreen(key: ValueKey(lessonId), lessonId: ...)"
      pattern: "ValueKey"
---

<objective>
Wire the live progression state: stream-derived providers over the Wave-1 engine
and schema (D-06 today rule, generic D-02 unlock), and parameterize the existing
routes so every one of the 28 lessons is reachable (`/practice?lesson=`,
`/journey?highlight=`). After this plan, passing alif really unlocks baa in
provider state, and baa's practice screen is reachable by URL — the phase's
end-to-end spine, before the UI surfaces go live in Wave 3.

Purpose: S1-09's "immediate on pass" is won or lost here — drift streams, never
keepAlive caches (Pitfall 4). S1-01's "today's lesson for the active child" is
computed here.
Output: progression_providers.dart, parameterized router, lessonId-aware
PracticeScreen, Home ARB keys, provider integration tests.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — stream-driven immediacy provider tests</name>
  <files>test/providers/progression_providers_test.dart</files>
  <read_first>
    - test/data/app_database_test.dart (in-memory NativeDatabase + drift import-hide idiom)
    - lib/providers/profile_providers.dart (childProfileProvider override shape used in tests)
    - lib/data/curriculum_repository.dart (curriculumRepositoryProvider for overrides; getLessons)
    - lib/models/lesson_progression.dart (ProgressionSnapshot API from 06-01)
  </read_first>
  <behavior>
    New test file (RED: progression_providers.dart does not exist). Precedent-setting
    first stream-provider test — use ProviderContainer with overrides: in-memory
    AppDatabase (appDatabaseProvider), real CurriculumRepository over rootBundle
    (or a fixture repo), childProfileProvider overridden to a profile with
    startingLessonId 'lesson_01'. Cover:
    - Initial state: today resolves to lesson_01 (empty mastery).
    - S1-09 immediacy: await db.recordMastery(letterId: 'alif', cleanReps: 3) →
      masteredLetterIdsProvider emits {alif} → todayLesson recomputes to lesson_02
      WITHOUT any ref.invalidate call in the test (pump the container / listen to
      the provider).
    - D-06 entry point: profile with startingLessonId 'lesson_03' → today is
      lesson_03 with empty mastery; mastering an EARLIER skipped letter (alif)
      does not change today.
    - cleanRepsForLetterProvider('baa') emits the persisted count after
      db.setCleanReps and updates on overwrite.
    - All-mastered: with every lesson's letter mastered, todayLesson resolves null.
  </behavior>
  <action>
    Write the failing tests naming the provider API: `masteredLetterIdsProvider`
    (StreamProvider&lt;Set&lt;String&gt;&gt;), `cleanRepsForLetterProvider`
    (StreamProvider.family&lt;int, String&gt;), `progressionProvider`
    (FutureProvider&lt;ProgressionSnapshot&gt;), `todayLessonProvider`
    (FutureProvider&lt;Lesson?&gt;). Carry the Wave-0 RED-contract header comment.
  </action>
  <verify>
    <automated>flutter test test/providers/progression_providers_test.dart 2>&1 | grep -q "progression_providers.dart" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - File fails only on the missing package:qalam/providers/progression_providers.dart import
    - The immediacy test contains zero `invalidate` calls (grep the file: `grep -c "invalidate" test/providers/progression_providers_test.dart` returns 0)
    - D-06 entry-point and all-mastered cases present
  </acceptance_criteria>
  <done>RED provider contract committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — live progression providers (streams, never keepAlive)</name>
  <files>lib/providers/progression_providers.dart</files>
  <read_first>
    - test/providers/progression_providers_test.dart (the contract)
    - lib/providers/profile_providers.dart (hand-written-provider deviation pattern + imports/part shape; riverpod_generator 4.0.3 Drift-type bug)
    - lib/providers/journey_providers.dart (the keepAlive mock ANTI-pattern this plan must not copy; do NOT modify this file here — it retires in 06-06)
    - lib/data/app_database.dart (watch methods from 06-02)
  </read_first>
  <action>
    Create lib/providers/progression_providers.dart:
    - `masteredLetterIdsProvider = StreamProvider&lt;Set&lt;String&gt;&gt;((ref) =>
      ref.watch(appDatabaseProvider).watchMasteredLetterIds())` — hand-written
      (Pitfall 3 policy: hand-write anything in Drift's orbit; streams of plain
      Sets are safe either way, prefer hand-written for consistency with
      profile_providers.dart).
    - `cleanRepsForLetterProvider = StreamProvider.family&lt;int, String&gt;` over
      db.watchCleanReps(letterId).
    - `progressionProvider = FutureProvider&lt;ProgressionSnapshot&gt;`: awaits
      curriculum lessons (sorted by order), profile (childProfileProvider —
      null profile defensively → first lesson), and
      `ref.watch(masteredLetterIdsProvider.future)`; returns
      ProgressionSnapshot.compute(...). Watching the stream provider's future makes
      it recompute on every mastery emission — the no-invalidation guarantee.
    - `todayLessonProvider = FutureProvider&lt;Lesson?&gt;` deriving
      snapshot.today.
    - NO keepAlive on any of these (Pitfall 4 — a keepAlive live provider caches a
      stale "today"). File header comment documents the hand-written deviation and
      the never-keepAlive rule, mirroring profile_providers.dart's NOTE style.
  </action>
  <verify>
    <automated>flutter test test/providers/progression_providers_test.dart && flutter analyze lib/providers/progression_providers.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/providers/progression_providers_test.dart green
    - `grep -c "keepAlive" lib/providers/progression_providers.dart` returns 0
    - `grep -c "mockJourneyProgress" lib/providers/progression_providers.dart` returns 0 (mock untouched until 06-06)
    - flutter analyze clean for the new file
  </acceptance_criteria>
  <done>Pass → stream emission → today recomputes, proven by the Wave-0 tests.</done>
</task>

<task type="auto">
  <name>Task 3: Route query params + lessonId-aware PracticeScreen + Home ARB keys</name>
  <files>lib/router/app_router.dart, lib/features/practice/practice_screen.dart, lib/features/journey/journey_screen.dart, lib/l10n/app_en.arb, test/router/onboarding_gate_test.dart</files>
  <read_first>
    - lib/router/app_router.dart (hand-written GoRoute builders; the SYNCHRONOUS redirect gate comparing state.matchedLocation — must not be disturbed)
    - lib/features/practice/practice_screen.dart (static const _lessonId = 'lesson_01' and every reference to it; family-keyed controller watch)
    - lib/features/journey/journey_screen.dart (constructor — gains an inert highlightId field here; visuals/logic change in 06-06)
    - test/router/onboarding_gate_test.dart (gate tests to extend with the query-param assertion, RESEARCH A3)
    - lib/l10n/app_en.arb (existing key conventions + placeholder syntax, e.g. homeGreeting's {nickname})
  </read_first>
  <action>
    1. app_router.dart (RESEARCH Pattern 3): `/practice` builder reads
    `state.uri.queryParameters['lesson']` and constructs `PracticeScreen(key:
    ValueKey(lessonId), lessonId: lessonId)` — the ValueKey forces fresh widget
    State per lesson (Pitfall 5). `/journey` builder reads
    `queryParameters['highlight']` and passes `JourneyScreen(highlightId: ...)`.
    Redirect logic untouched — query params are chosen precisely because
    matchedLocation is path-only and the gate cannot see them.
    2. practice_screen.dart: delete `static const String _lessonId`; add `final
    String? lessonId` constructor param. Resolve the effective lesson id in
    State: if widget.lessonId is non-null AND resolves via
    CurriculumRepository.getLesson, use it; otherwise watch todayLessonProvider
    and use today's id (fallback chain ends at the profile's startingLessonId /
    'lesson_01') — V5 input validation by catalog allowlist, silent degradation,
    never a raw error to the child (UI-SPEC error contract). All former
    `PracticeScreen._lessonId` reads (controller family key, etc.) use the
    resolved id. While resolving, render the existing loading treatment.
    3. journey_screen.dart: add `final String? highlightId` constructor param
    (stored, intentionally unused until 06-06 — comment it as such). No other
    change to this file.
    4. app_en.arb: add the Home keys from the approved UI-SPEC Copywriting
    Contract (consumed by 06-05): `homeLessonTitleFor` = "The Letter {letterName}"
    (String placeholder), `homeAllMasteredEyebrow` = "YOUR LETTERS",
    `homeAllMasteredTitle` = "You've mastered all your letters.",
    `homeAllMasteredBody` = "Visit your journey to practice any letter again.",
    `homeInkFillSemantics` = "{n} of {total} clean reps" (two placeholders, a11y
    only). Run `flutter gen-l10n` (generated files are gitignored — Pitfall 9).
    5. onboarding_gate_test.dart: add an assertion that navigating to
    '/practice?lesson=lesson_02' with no profile still redirects to /onboarding,
    and with a profile does NOT redirect (A3: matchedLocation excludes query
    strings).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/router/ && flutter test test/features/practice/practice_screen_test.dart 2>&1 | tail -5 && flutter analyze lib/router/app_router.dart lib/features/practice/practice_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static const String _lessonId" lib/features/practice/practice_screen.dart` returns 0
    - `grep -c "ValueKey" lib/router/app_router.dart` >= 1
    - `grep -c "homeLessonTitleFor" lib/l10n/app_en.arb` returns 1
    - flutter test test/router/ green including the new query-param gate assertions
    - test/features/practice/practice_screen_test.dart has no NEW failures versus pre-plan state (lines 167/220 stale-debt failures remain documented for 06-07)
    - flutter test passes for all touched test files
  </acceptance_criteria>
  <done>Any lesson reachable by URL with allowlist degradation; gate intact; Home copy contract staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deep link / route restoration → route builders | `?lesson=` / `?highlight=` are externally influenceable strings entering navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering (malformed deep link, e.g. ?lesson=../../x) | /practice and /journey builders + PracticeScreen resolution | mitigate | ID-allowlist validation against the loaded curriculum catalog before use; unknown/missing → silent degrade to today's lesson; params never touch filesystem/SQL (drift parameterized queries only) |
| T-06-06 | Denial of service (redirect-gate bypass/loop via query strings) | Phase-5 onboarding redirect | mitigate | Gate compares matchedLocation (path-only); explicit gate test with query params added in Task 3 |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/providers/progression_providers_test.dart test/router/ — green
- Full suite: known-failing set unchanged (font-drift goldens + documented 03.1 stale debt)
</verification>

<success_criteria>
- recordMastery → providers recompute with zero invalidate calls (S1-09 immediacy proven by test)
- Today's lesson computed from active child's startingLessonId per D-06 (entry-point case tested)
- /practice?lesson=lesson_02 reaches baa's practice with fresh State; junk params degrade silently
- Onboarding gate provably unaffected by query parameters
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-03-SUMMARY.md` when done
</output>
