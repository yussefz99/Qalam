---
phase: 06-lesson-progression-home
plan: 06
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - lib/features/journey/journey_screen.dart
  - lib/providers/journey_providers.dart
  - test/features/journey/journey_screen_test.dart
autonomous: true
requirements: [S1-09]
must_haves:
  truths:
    - "Journey nodes light from LIVE mastery data — a mastered daal/raa/haa_c node turns complete (the 19/28 canonical-ID drift is fixed)"
    - "Locked lessons are visibly unavailable (inert locked/future nodes) until their prerequisite is passed (S1-09)"
    - "Skipped-but-unlocked earlier letters keep the future visual but are tappable into practice (D-05, D-07)"
    - "Arriving via /journey?highlight=<id>, the just-mastered node's gold star settles in before the pulse moves to the new current node (D-15)"
    - "No draft-status markers and no score treatments appear (D-04, PLAT-03)"
  artifacts:
    - path: "lib/features/journey/journey_screen.dart"
      provides: "Live-data journey with canonical curriculum IDs, D-07 tap rules, D-15 highlight arrival"
    - path: "test/features/journey/journey_screen_test.dart"
      provides: "Wave-0 widget coverage: canonical lighting, tap matrix, highlight"
      min_lines: 80
  key_links:
    - from: "lib/features/journey/journey_screen.dart"
      to: "lib/providers/progression_providers.dart"
      via: "progressionProvider snapshot (mastered/unlocked/today + lessonIdByLetterId)"
      pattern: "progressionProvider"
    - from: "lib/features/journey/journey_screen.dart"
      to: "/practice"
      via: "node tap with lesson param"
      pattern: "/practice\\?lesson="
---

<objective>
Take the Journey map live: replace the keepAlive mock with the stream-derived
progression snapshot, reconcile the screen's letter list to canonical
letters.json IDs (RESEARCH Pitfall 1 — 19 of 28 IDs currently diverge and would
silently never light), make skipped-but-unlocked nodes tappable (D-07), and land
the 03.1-deferred highlight arrival from the celebration (D-15). This is where
S1-09's "locked lessons are visibly unavailable" lives.

Purpose: the Journey is the phase's unlock-visibility surface and the only replay
path (D-12). Amends 03.1 D-08 minimally: the provider swaps and the node data
source is fixed to canonical IDs; layout and node visuals are unchanged.
Output: live journey_screen.dart, retired mock, new Wave-0 widget tests.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
@.planning/phases/06-lesson-progression-home/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — journey live-data widget tests</name>
  <files>test/features/journey/journey_screen_test.dart</files>
  <read_first>
    - lib/features/journey/journey_screen.dart (current structure: _kLetters hardcoded list with drifted IDs, mockJourneyProgressProvider consumption, node tap rules, JourneyNodeWidget states)
    - lib/models/journey_progress.dart (JourneyNodeState.compute semantics the screen uses)
    - test/screens/home_screen_test.dart (ConsumerWidget test harness + provider override conventions)
    - lib/providers/progression_providers.dart (progressionProvider/ProgressionSnapshot to override)
  </read_first>
  <behavior>
    New test file (RED: the screen still reads the mock and has drifted IDs). With
    progressionProvider overridden:
    - Canonical lighting: mastered = {'daal'} → the daal node renders complete
      (this FAILS against today's 'dal'-keyed list — the drift regression test)
    - Tap matrix (D-07/S1-09): complete node tap → '/practice?lesson=<its lesson>';
      current node tap → its lesson; skipped-but-unlocked node (future visual) IS
      tappable; a genuinely locked node (prerequisite unpassed, at/after start) is
      inert
    - Locked visibility: locked nodes render the existing locked/future treatment
      (visibly unavailable — S1-09 acceptance)
    - D-15: pumping JourneyScreen(highlightId: 'alif') shows the settling-star
      emphasis on the alif node (presence assertion, then settled after durCheer)
    - "N of 28" header reads live mastered count, plain information styling
  </behavior>
  <action>
    Write the failing tests with the Wave-0 RED header. Use canonical letters.json
    IDs throughout (daal, raa, haa_c, taa_h, zhaa, haa_f...) so the drift cannot
    re-enter via the tests.
  </action>
  <verify>
    <automated>flutter test test/features/journey/journey_screen_test.dart 2>&1 | grep -qE "(FAILED|Error|failed)" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with RED header; failures are the expected unimplemented behaviors (mock consumption / drifted IDs / missing highlight handling), not harness bugs
    - Tests reference only canonical letter IDs
    - The tap matrix covers all four node situations (complete, current, skipped-unlocked, locked)
  </acceptance_criteria>
  <done>RED journey contract committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — canonical IDs, live provider, D-07 taps; retire the mock</name>
  <files>lib/features/journey/journey_screen.dart, lib/providers/journey_providers.dart</files>
  <read_first>
    - lib/features/journey/journey_screen.dart (lines ~37-66 _kLetters; ~108-113 provider read; ~309-321 node tap wiring)
    - assets/curriculum/letters.json (canonical IDs + Arabic chars + display names — the single source of truth)
    - lib/data/curriculum_repository.dart (getLetters for deriving the node list)
    - lib/providers/journey_providers.dart (mockJourneyProgress doc comment promising this exact swap)
  </read_first>
  <action>
    1. Replace the screen's letter data source: derive the 28 nodes from
    CurriculumRepository.getLetters() (id, char, name.display) instead of
    _kLetters — kills the second source of truth (RESEARCH anti-pattern). If an
    async read complicates the layout path, an acceptable fallback is a
    canonical-ID static list regenerated from letters.json — but prefer the
    repository; either way every node id must equal a letters.json id.
    2. Swap mockJourneyProgressProvider for the live progression snapshot:
    masteredIds = snapshot.masteredLetterIds, currentId = today's letter ref (''
    when allMastered). Keep JourneyNodeState.compute and all node visuals
    unchanged (03.1 D-08 amended only by the data fix). Delete mockJourneyProgress
    from journey_providers.dart (or repoint the file to re-export the live
    provider) — no keepAlive static progress remains.
    3. Node taps: complete, current, AND skipped-but-unlocked (snapshot
    unlockedLessonIds contains the node's lesson, state == future) navigate
    `context.go('/practice?lesson=${snapshot.lessonIdByLetterId[letterId]}')`;
    locked nodes stay inert (S1-09 visible unavailability). Skipped nodes keep the
    future visual — no new visual state (D-07), no draft markers (D-04).
    4. The "N of 28" header reads live mastered count — quiet information, never
    gold, never celebrated (UI-SPEC).
    5. Mark ONLY the Task-1 D-15 highlight test `skip: 'GREEN in Task 3'` — the
    arrival animation lands in Task 3, which removes the skip. Every other Task-1
    test must pass un-skipped.
  </action>
  <verify>
    <automated>flutter test test/features/journey/journey_screen_test.dart && grep -rc "mockJourneyProgress" lib/ | grep -v ":0" | wc -l | grep -qx "0" && echo "mock retired"</automated>
  </verify>
  <acceptance_criteria>
    - Task-1 tests green; the D-15 highlight test carries `skip: 'GREEN in Task 3'` (the only skip in the file), so the file-level run passes
    - `grep -rn "mockJourneyProgress" lib/` returns no matches
    - `grep -c "'dal'\|'tah'\|'dhah'\|'ain'\|'ghain'\|'fa'\|'qaf'\|'kaf'\|'lam'\|'waw'\|'ya'" lib/features/journey/journey_screen.dart` returns 0 (no drifted IDs survive)
    - flutter analyze clean for both files
  </acceptance_criteria>
  <done>Journey lights from real mastery with canonical IDs; unlock visibility + replay taps correct.</done>
</task>

<task type="auto">
  <name>Task 3: D-15 highlight arrival — star settles on the just-mastered node</name>
  <files>lib/features/journey/journey_screen.dart, test/features/journey/journey_screen_test.dart</files>
  <read_first>
    - lib/features/practice/widgets/mastery_celebration.dart (~lines 56-79: the settling-star AnimationController recipe — easeSoftBack, durCheer 700ms, scale 0→1 + early-interval opacity)
    - lib/theme/dimens.dart (QalamMotion tokens)
    - lib/features/journey/journey_screen.dart (highlightId field added in 06-03, currently inert)
  </read_first>
  <action>
    Implement the highlight arrival (D-15, folds in the 03.1 deferral): when
    highlightId matches a node, that node's gold star badge plays the settling
    recipe once (scale 0→1 with QalamMotion.easeSoftBack over durCheer 700ms,
    opacity easing in over the first 40%), after which the existing pulse glow
    applies to the new current node. One-shot per arrival, no sound, gold only on
    the star (reward-exclusive color). Unknown/null highlightId → no-op (V5
    degradation). Respect MediaQuery.disableAnimations (render settled).
    Remove the Task-2 `skip: 'GREEN in Task 3'` marker from the D-15 test, turn
    it green, and add an unknown-id no-op test.
  </action>
  <verify>
    <automated>flutter test test/features/journey/journey_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All journey tests green including D-15 and the unknown-highlight no-op
    - `grep -c "GREEN in Task 3" test/features/journey/journey_screen_test.dart` returns 0 (Task-2 skip removed)
    - Animation uses QalamMotion.easeSoftBack + durCheer tokens (`grep -c "durCheer" lib/features/journey/journey_screen.dart` >= 1; no new Duration literals)
    - flutter test (full suite) known-failing set not grown
  </acceptance_criteria>
  <done>Celebration → journey arrival gives the mastered node its dignified moment, then attention moves forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?highlight= query param → journey animation | Externally influenceable string selects a node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering (malformed ?highlight=) | highlight arrival | mitigate | ID must match a rendered node (catalog allowlist); unknown → silent no-op, tested |
| T-06-09 | Gamification creep (PLAT-03) | journey header / node chrome | mitigate | "N of 28" stays plain information; gold confined to star badges; no draft markers (D-04); acceptance greps |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/features/journey/journey_screen_test.dart — fully green
- Full suite: known-failing set unchanged or shrinking
</verification>

<success_criteria>
- S1-09 "locked lessons visibly unavailable" observable and tested on the live map
- 19/28 ID drift eliminated with a regression test that would catch reintroduction
- D-05/D-07 skipped letters revisitable; D-15 highlight shipped; mock retired
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-06-SUMMARY.md` when done
</output>
