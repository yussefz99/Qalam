---
phase: 06-lesson-progression-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/app_database.dart
  - lib/data/progress_repository.dart
  - lib/data/drift_progress_repository.dart
  - lib/features/onboarding/onboarding_data.dart
  - test/data/app_database_test.dart
  - test/data/progress_repository_test.dart
  - test/features/onboarding/onboarding_data_test.dart
  - test/features/onboarding/onboarding_screen_test.dart
  - test/data/child_profile_repository_test.dart
  - test/screens/home_screen_test.dart
autonomous: true
requirements: [S1-09, S1-01]
must_haves:
  truths:
    - "Partial clean reps persist across an app restart in a new LetterReps table (D-10)"
    - "A v3 database upgrades to v4 without losing profiles, settings, or mastery rows"
    - "Stored startingLessonId values are lesson IDs after migration ('alif' rows rewritten to 'lesson_01'); new profiles get lesson IDs from the resolver"
    - "Mastered letter IDs and per-letter rep counts are observable as Drift streams that emit on write (S1-09 immediacy substrate)"
  artifacts:
    - path: "lib/data/app_database.dart"
      provides: "schemaVersion 4, LetterReps table, setCleanReps/getCleanReps, watchMasteredLetterIds, watchCleanReps"
      contains: "class LetterReps extends Table"
    - path: "lib/data/progress_repository.dart"
      provides: "Interface extended with rep persistence + watch streams, SECURITY-documented"
    - path: "lib/features/onboarding/onboarding_data.dart"
      provides: "gradeToStartingLessonId values in the lesson-id namespace"
      contains: "lesson_01"
  key_links:
    - from: "lib/data/drift_progress_repository.dart"
      to: "lib/data/app_database.dart"
      via: "thin delegation, no SQL in repository"
      pattern: "_db\\.(setCleanReps|watchMasteredLetterIds)"
    - from: "lib/data/app_database.dart"
      to: "child_profiles.starting_lesson_id"
      via: "v4 customStatement namespace rewrite"
      pattern: "lesson_01"
---

<objective>
Ship schema v4: the LetterReps table for persisted partial clean reps (D-10),
drift `.watch()` streams over mastery and reps (the substrate that makes S1-09's
"unlock is immediate on pass" possible without invalidation choreography), and
the startingLessonId letter-id → lesson-id namespace normalization (RESEARCH
Pitfall 2; the Phase-5 "Phase 6 decides" flag is hereby decided: lesson IDs).

Purpose: D-10/D-20 need durable rep counts; the live providers in plan 06-03 need
streams; the D-06 today rule needs startingLessonId to be comparable to lesson IDs.
Output: v4 AppDatabase with idempotent migration + data rewrite, extended
ProgressRepository, lesson-id resolver map, all proven by migration tests.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — v3→v4 migration + rep-persistence + stream tests</name>
  <files>test/data/app_database_test.dart</files>
  <read_first>
    - test/data/app_database_test.dart (the simulated-restart pattern over a shared NativeDatabase.memory() executor, the v2→v3 migration test shape, and the mandatory `import 'package:drift/drift.dart' hide isNull, isNotNull;` idiom)
    - lib/data/app_database.dart (current v3 schema, LetterMastery/ChildProfiles tables, migration strategy, recordMastery/isMastered accessors)
    - lib/data/app_database.g.dart (generated snake_case table/column names — needed to assert the customStatement rewrite target `child_profiles.starting_lesson_id`)
  </read_first>
  <behavior>
    Extend the existing test file (RED against not-yet-existing v4 API):
    - Migration survival: on a shared in-memory executor, create a DB, write an
      AppSettings row, a LetterMastery row (alif), and a ChildProfile with
      startingLessonId 'alif'; "restart" (fresh AppDatabase over the same
      executor); assert settings + mastery + profile survive AND the profile's
      startingLessonId now reads 'lesson_01' (v4 namespace rewrite). Idempotence:
      a second restart changes nothing.
    - Rep persistence (D-10): setCleanReps(letterId: 'baa', cleanReps: 2) →
      getCleanReps('baa') == 2; survives a simulated restart; setCleanReps(...,
      0) overwrites to 0 (the write-through reset shape Pitfall 7 needs);
      getCleanReps of an unknown letter returns 0.
    - Streams: watchMasteredLetterIds() emits a Set that gains 'alif' after
      recordMastery(letterId: 'alif', ...); watchCleanReps('baa') emits after
      setCleanReps. Use `expectLater`/stream matchers; first emission may be the
      current state.
  </behavior>
  <action>
    Write the failing tests only — no lib/ changes. Name the target API exactly:
    `setCleanReps({required String letterId, required int cleanReps})`,
    `Future&lt;int&gt; getCleanReps(String letterId)`,
    `Stream&lt;Set&lt;String&gt;&gt; watchMasteredLetterIds()`,
    `Stream&lt;int&gt; watchCleanReps(String letterId)` on AppDatabase.
  </action>
  <verify>
    <automated>flutter test test/data/app_database_test.dart 2>&1 | grep -qE "(setCleanReps|watchMasteredLetterIds|isn't defined|Error)" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - New tests fail to compile/pass only because the v4 API does not exist yet
    - All pre-existing tests in the file are untouched and still present
    - The migration test asserts BOTH data survival and the 'alif' → 'lesson_01' rewrite, plus idempotence on double restart
  </acceptance_criteria>
  <done>RED contract for schema v4 committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — schema v4: LetterReps table, idempotent migration, watch streams</name>
  <files>lib/data/app_database.dart</files>
  <read_first>
    - lib/data/app_database.dart (LetterMastery table shape WITH its SECURITY header comment to replicate; the `if (from &lt; N)` idempotent migration pattern; insertOnConflictUpdate/getSingleOrNull accessor shapes)
    - lib/data/app_database.g.dart (verify exact generated names `child_profiles` / `starting_lesson_id` before writing the customStatement SQL)
    - test/data/app_database_test.dart (the Task-1 contract)
  </read_first>
  <action>
    1. Add `class LetterReps extends Table` — TextColumn letterId (primary key),
    IntColumn cleanReps, DateTimeColumn updatedAt — with a SECURITY header comment
    matching LetterMastery's: only letterId/count/timestamp are stored, stroke
    points are NEVER persisted (T-03-01/T-06-01). Register in @DriftDatabase tables.
    2. Bump schemaVersion to 4. In onUpgrade add `if (from &lt; 4) { await
    m.createTable(letterReps); await customStatement("UPDATE child_profiles SET
    starting_lesson_id = 'lesson_01' WHERE starting_lesson_id = 'alif'"); }` —
    verify table/column spelling against app_database.g.dart first; keep the
    existing from&lt;2 / from&lt;3 guards untouched (idempotent pattern).
    3. Add accessors per the Task-1 API: setCleanReps via insertOnConflictUpdate
    (updatedAt = DateTime.now()), getCleanReps returning 0 when absent,
    watchMasteredLetterIds() as `select(letterMastery).watch().map((rows) =>
    rows.map((r) => r.letterId).toSet())` (RESEARCH Pattern 1 — first .watch() in
    the codebase, drift 2.31.0 verified), watchCleanReps(letterId) as a filtered
    watchSingleOrNull mapped to `row?.cleanReps ?? 0`.
    4. Run build_runner to regenerate app_database.g.dart.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/data/app_database_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/data/app_database_test.dart fully green (Task-1 tests included)
    - `grep -c "schemaVersion => 4" lib/data/app_database.dart` returns 1
    - LetterReps table declares exactly three columns: letterId, cleanReps, updatedAt (child-data minimization, T-06-01)
    - `grep -c "SECURITY" lib/data/app_database.dart` returns at least 2 (existing + new table header)
    - Existing v2→v3 tests still green (migration remains idempotent end-to-end)
  </acceptance_criteria>
  <done>v4 schema live with streams; migration test green including the namespace rewrite.</done>
</task>

<task type="auto">
  <name>Task 3: Repository extension + lesson-id namespace in the onboarding resolver</name>
  <files>lib/data/progress_repository.dart, lib/data/drift_progress_repository.dart, lib/features/onboarding/onboarding_data.dart, test/data/progress_repository_test.dart, test/features/onboarding/onboarding_data_test.dart, test/features/onboarding/onboarding_screen_test.dart, test/data/child_profile_repository_test.dart, test/screens/home_screen_test.dart</files>
  <read_first>
    - lib/data/progress_repository.dart (interface + SECURITY doc framing to extend)
    - lib/data/drift_progress_repository.dart (one-line delegation pattern; provider stays @Riverpod(keepAlive: true))
    - lib/features/onboarding/onboarding_data.dart (gradeToStartingLessonId map + resolveStartingLessonId + the explicit "Phase 6 decides" comment to resolve)
    - test/features/onboarding/onboarding_data_test.dart (expectations pinning 'alif' to update)
  </read_first>
  <action>
    1. ProgressRepository interface gains `setCleanReps`, `getCleanReps`,
    `watchMasteredLetterIds`, `watchCleanReps` with SECURITY doc comments (only
    letterId + int count persisted — never stroke points, T-03-01).
    DriftProgressRepository delegates one-line each to AppDatabase. recordMastery /
    isMastered unchanged (row-exists-equals-passed invariant preserved — RESEARCH
    "Alternatives Considered").
    2. onboarding_data.dart: change every gradeToStartingLessonId value from 'alif'
    to 'lesson_01' (all grades currently map to alif's lesson; per-grade entry
    points are Phase-7 content). Update the "Phase 6 decides" comment to state the
    decision: startingLessonId is a LESSON id (namespace decided this phase;
    migration rewrites legacy rows). Both the code edit AND the Task-2 migration
    are required — doing only one leaves mixed namespaces (RESEARCH Runtime State).
    3. Update every test pinning the old namespace: grep test/ for 'alif' in
    startingLessonId/gradeToStartingLessonId/resolveStartingLessonId contexts
    (known: onboarding_data_test.dart, onboarding_screen_test.dart,
    child_profile_repository_test.dart, home_screen_test.dart, plus the profile
    seed in app_database_test if Task 1 didn't already cover it) and change the
    expected value to 'lesson_01'. Do not touch letter-domain 'alif' usages
    (scorer/curriculum tests).
    4. Extend test/data/progress_repository_test.dart with a delegation check for
    one new method (setCleanReps roundtrip through the repository).
  </action>
  <verify>
    <automated>flutter test test/data/ test/features/onboarding/ test/screens/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'alif'" lib/features/onboarding/onboarding_data.dart` returns 0
    - `grep -c "lesson_01" lib/features/onboarding/onboarding_data.dart` >= 1
    - flutter test test/data/ test/features/onboarding/ passes
    - test/screens/home_screen_test.dart has no NEW failures versus its pre-plan state (the known-stale Test 4 at line 204 remains documented debt for plan 06-05 — do not fix it here, do not regress anything else)
    - `grep -c "List<Offset>" lib/data/progress_repository.dart lib/data/drift_progress_repository.dart` returns 0 for both files
  </acceptance_criteria>
  <done>Repository exposes rep persistence + streams; resolver and stored data agree on the lesson-id namespace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App code → child's local SQLite | New child-adjacent data lands in LetterReps |
| v3 on-device data → v4 migration | Existing profiles/mastery must survive the upgrade + rewrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure (child-data minimization) | LetterReps table | mitigate | Schema holds ONLY letterId + int cleanReps + updatedAt — same data class as LetterMastery, no PII, no coordinates; SECURITY header comment + acceptance grep enforce it |
| T-06-02 | Tampering/Integrity | v3→v4 migration + startingLessonId rewrite | mitigate | Idempotent `if (from < 4)` guard; simulated-restart test seeds v3-era rows and asserts survival + rewrite + double-restart idempotence |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/data/ test/features/onboarding/ — green
- Full suite: known-failing set unchanged (font-drift goldens + documented 03.1 stale debt only)
</verification>

<success_criteria>
- D-10 rep counts persist across restart, including overwrite-to-0
- v4 migration proven non-destructive and idempotent over real v3-shaped data
- One namespace: stored values, resolver map, and the D-06 comparison (plan 06-03) all speak lesson IDs
- Streams emit on mastery/rep writes — the S1-09 immediacy substrate exists
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-02-SUMMARY.md` when done
</output>
