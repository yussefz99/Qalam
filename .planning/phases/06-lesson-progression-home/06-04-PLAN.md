---
phase: 06-lesson-progression-home
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - lib/core/scoring/tolerances.dart
  - lib/core/scoring/letter_scorer.dart
  - lib/providers/practice_providers.dart
  - lib/features/practice/practice_screen.dart
  - test/core/scoring/letter_scorer_test.dart
  - test/features/practice/session_controller_test.dart
autonomous: true
requirements: [S1-09]
must_haves:
  truths:
    - "Rep N within a lesson scores against the ramp preset for the PERSISTED rep index — a child resuming at rep 2 scores at rep 2's preset (D-18, D-20)"
    - "The ramp is data (per-lesson toleranceRamp with the lessons.json global default), not code (D-19)"
    - "Clean-rep counts write through to LetterReps on every change, including reset to 0 on a miss (D-10, Pitfall 7)"
    - "A session resumed after restart primes its rep count from the DB"
    - "Default scoring behavior is unchanged when no override is passed (Phase-4 contract preserved)"
  artifacts:
    - path: "lib/core/scoring/tolerances.dart"
      provides: "public Tolerances.preset(String name) with unknown → normal"
      contains: "static Tolerances preset"
    - path: "lib/core/scoring/letter_scorer.dart"
      provides: "scoreLetter optional tolerances override, default-preserving"
      contains: "Tolerances? tolerances"
    - path: "lib/providers/practice_providers.dart"
      provides: "Persisted-rep seeding + write-through + ramp preset resolution on PracticeState"
  key_links:
    - from: "lib/providers/practice_providers.dart"
      to: "lib/data/progress_repository.dart"
      via: "getCleanReps seed + setCleanReps write-through"
      pattern: "setCleanReps"
    - from: "lib/features/practice/practice_screen.dart"
      to: "lib/core/scoring/letter_scorer.dart"
      via: "scoreLetter(..., tolerances: <ramp preset>)"
      pattern: "tolerances:"
---

<objective>
The scaffolding fade (D-18/D-19/D-20) and durable rep counts (D-10): the session
controller seeds clean reps from the persisted LetterReps row, writes every change
through (including the in-a-row reset to 0 on a miss), and resolves the tolerance
preset for the CURRENT persisted rep index from the data-driven ramp
([loose, normal, strict] default). scoreLetter gains an optional override so the
ramp preset reaches the scorer without moving scoring policy out of lib/core.

Purpose: S1-09's "pass" = the clean-reps-to-advance rule; this plan makes that rule
survive restarts and apply graded strictness. Invisible to the child (UI-SPEC:
scoring contract, not UI contract). ⚠ Pedagogy flags for the owner's mother (carry
into SUMMARY): D-10 cross-session persistence default and D-19 ramp semantics ship
as mechanisms; the rules are hers.
Output: ramp-aware controller + scorer override, fully unit-tested.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tolerances.preset accessor + scoreLetter override (default-preserving)</name>
  <files>lib/core/scoring/tolerances.dart, lib/core/scoring/letter_scorer.dart, test/core/scoring/letter_scorer_test.dart</files>
  <read_first>
    - lib/core/scoring/tolerances.dart (private _presets map: loose/normal/strict moving only maxCurvature 0.35/0.25/0.18; fromJson defensive idiom)
    - lib/core/scoring/letter_scorer.dart (scoreLetter signature with named recognizer param; internal `letter.tolerances ?? Tolerances.normal` resolution at ~line 61)
    - test/core/scoring/letter_scorer_test.dart (existing scorer test fixtures to extend)
  </read_first>
  <behavior>
    - Tolerances.preset('loose'/'normal'/'strict') returns the matching preset;
      preset('garbage') and preset('') return Tolerances.normal (defensive, never throws)
    - scoreLetter with tolerances: Tolerances.preset('strict') applies strict
      maxCurvature (a fixture stroke that passes loose fails strict, or assert via
      the resolved tolerance's effect)
    - scoreLetter WITHOUT the new param behaves byte-for-byte as before
      (letter.tolerances ?? normal) — existing tests stay green untouched
  </behavior>
  <action>
    tolerances.dart: add `static Tolerances preset(String name) =&gt; _presets[name]
    ?? normal;` (mirrors the fromJson unknown→normal idiom; D-18 consumes Phase-4
    presets unchanged). letter_scorer.dart: add optional named `Tolerances?
    tolerances` to scoreLetter; resolution becomes `tolerances ??
    letter.tolerances ?? Tolerances.normal`. No other scorer logic changes —
    scoring policy stays in lib/core (Architectural Responsibility Map).
  </action>
  <verify>
    <automated>flutter test test/core/scoring/</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/core/scoring/ fully green, including all pre-existing tests unmodified
    - New tests cover: known preset lookup, unknown-name fallback to normal, override-wins resolution order, no-override behavioral identity
    - `grep -c "Tolerances? tolerances" lib/core/scoring/letter_scorer.dart` >= 1
  </acceptance_criteria>
  <done>Ramp presets publicly resolvable; scorer override exists with Phase-4 default behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Controller rep persistence (seed + write-through) and ramp resolution; screen passes the preset</name>
  <files>lib/providers/practice_providers.dart, lib/features/practice/practice_screen.dart, test/features/practice/session_controller_test.dart</files>
  <read_first>
    - lib/providers/practice_providers.dart (ANTI-PATTERN 3 GUARD header — preserve verbatim; build()'s prime-then-update _loadLetter shape; _registerCleanRep's in-a-row reset `cleanReps: 0` on miss; _recordMastery best-effort try/swallow)
    - lib/features/practice/practice_screen.dart (_onLetterComplete where scoreLetter is invoked — the single line gaining the tolerances arg)
    - lib/data/progress_repository.dart (setCleanReps/getCleanReps from 06-02)
    - lib/data/curriculum_repository.dart (getDefaultToleranceRamp from 06-01; getLesson for toleranceRamp)
    - test/features/practice/session_controller_test.dart (existing controller-test harness/overrides to extend)
  </read_first>
  <behavior>
    Controller tests (extend session_controller_test.dart, in-memory DB overrides):
    - Seed (D-20): LetterReps has cleanReps 2 for the lesson's letter → after
      build/load, state.cleanReps == 2 and the resolved preset is ramp[2] ('strict'
      under the default ramp)
    - Write-through (D-10): each clean rep persists (getCleanReps reflects state);
      a miss resets BOTH state.cleanReps and the persisted row to 0 (Pitfall 7 —
      ink-fill may go lighter; the same-sitting question is the owner's mother's
      call, shipped default documented)
    - Ramp indexing (D-18): rep at index 0 → 'loose', 1 → 'normal', 2 → 'strict';
      index clamps at ramp.length - 1; lesson.toleranceRamp overrides the global
      default when present
    - Persistence failures are swallowed (best-effort pattern) — session continues
  </behavior>
  <action>
    practice_providers.dart: (1) in the async load path, read
    `getCleanReps(letterId)` and prime state.cleanReps from it (the _loadLetter
    prime-then-update shape); (2) on every cleanReps transition (increment in
    _registerCleanRep, reset-to-0 on miss) fire a best-effort
    `setCleanReps(letterId:, cleanReps:)` mirroring the _recordMastery try/swallow;
    (3) resolve the ramp once per load: `ramp = lesson.toleranceRamp ??
    await curriculumRepository.getDefaultToleranceRamp()`; expose the current
    preset on PracticeState as a new field (e.g. `String tolerancePreset`)
    recomputed as `ramp[min(cleanReps, ramp.length - 1)]` whenever cleanReps
    changes (D-20: index = persisted count, not sitting). Preserve the
    ANTI-PATTERN 3 GUARD header verbatim; introduce no List&lt;Offset&gt;.
    practice_screen.dart: in _onLetterComplete pass `tolerances:
    Tolerances.preset(state.tolerancePreset)` into the existing scoreLetter call —
    no other screen changes (celebration wiring belongs to 06-07). No UI exposes
    the preset (UI-SPEC: invisible to the child; no loose/strict labels anywhere).
  </action>
  <verify>
    <automated>flutter test test/features/practice/session_controller_test.dart && flutter test test/features/practice/practice_screen_test.dart 2>&1 | tail -5 && grep -c "List<Offset>" lib/providers/practice_providers.dart | grep -qx "0" && echo "guard holds"</automated>
  </verify>
  <acceptance_criteria>
    - session_controller_test.dart green with the four behavior groups above
    - `grep -c "List<Offset>" lib/providers/practice_providers.dart` returns 0 (Anti-Pattern 3 guard)
    - `grep -c "ANTI-PATTERN 3 GUARD" lib/providers/practice_providers.dart` returns 1 (header preserved)
    - No new strings/widgets expose preset names (grep lib/features/practice for "loose"/"strict" returns 0 outside imports/comments)
    - practice_screen_test.dart: no NEW failures versus pre-plan state (167/220 stale debt remains for 06-07)
  </acceptance_criteria>
  <done>Reps persist + reset through the DB; each rep scores at its data-driven preset; child sees nothing new.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session controller → LetterReps writes | New per-rep write path into child-local storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure (child-data minimization) | per-rep write path | mitigate | Writes carry only letterId + int count (interface enforces it); Anti-Pattern 3 grep guard stays 0 — no stroke data can transit the controller |
| T-06-07 | Tampering (malformed ramp data) | lesson.toleranceRamp / defaultToleranceRamp | mitigate | Unknown preset names resolve to Tolerances.normal (never throw); index clamped to ramp bounds |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/core/scoring/ test/features/practice/session_controller_test.dart — green
- Full suite: known-failing set unchanged
</verification>

<success_criteria>
- D-18/D-19/D-20 mechanism shipped: data-driven ramp, persisted-index addressing, owner's-mother sign-off flags recorded in SUMMARY
- D-10 write-through proven including miss-reset and restart-resume
- Phase-4 scoring contract untouched when no override is supplied
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-04-SUMMARY.md` when done
</output>
