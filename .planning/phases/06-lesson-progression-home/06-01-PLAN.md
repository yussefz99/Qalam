---
phase: 06-lesson-progression-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/lesson_progression_test.dart
  - assets/curriculum/lessons.json
  - lib/models/lesson.dart
  - lib/models/lesson_progression.dart
  - lib/data/curriculum_repository.dart
  - test/data/curriculum_repository_test.dart
  - test/models/lesson_test.dart
autonomous: true
requirements: [S1-01, S1-09]
must_haves:
  truths:
    - "lessons.json contains 28 lessons, one per letter in letters.json array order, each requiring the previous lesson (D-01)"
    - "A pure-Dart engine answers passed/unlocked/today for any lesson with no Flutter or DB imports (D-02, D-06)"
    - "Lessons earlier than startingLessonId compute as unlocked-but-not-mastered and are never 'today' (D-05, D-06)"
    - "When every lesson at or after startingLessonId is passed, todayLesson returns null — the all-mastered state (D-11)"
    - "DRAFT (signedOff: false) letters do not gate progression (D-03)"
  artifacts:
    - path: "lib/models/lesson_progression.dart"
      provides: "Pure-Dart progression engine: lessonPassed, lessonUnlocked, todayLesson, ProgressionSnapshot.compute"
      min_lines: 60
    - path: "assets/curriculum/lessons.json"
      provides: "28 lesson entries + defaultToleranceRamp"
      contains: "lesson_28"
    - path: "test/models/lesson_progression_test.dart"
      provides: "Wave-0 unit coverage of D-02/D-03/D-05/D-06/D-11 semantics"
      min_lines: 80
  key_links:
    - from: "lib/models/lesson_progression.dart"
      to: "lib/models/lesson.dart"
      via: "Lesson model inputs (items[].ref, unlock.requires)"
      pattern: "unlock\\.requires"
    - from: "assets/curriculum/lessons.json"
      to: "assets/curriculum/letters.json"
      via: "items[].ref uses canonical letter IDs"
      pattern: "haa_c"
---

<objective>
Author the full 28-lesson curriculum catalog (D-01, D-19 ramp data) and build the
pure-Dart progression engine that decides passed / unlocked / today (D-02, D-03,
D-05, D-06, D-11). This is the domain heart of S1-01 ("today's lesson") and S1-09
("unlocks only after passing"), test-first.

Purpose: every later plan (providers, Home, Journey, celebration) consumes this
engine and this catalog. Getting the semantics right here, in pure Dart with
exhaustive unit tests, means the UI plans are pure wiring.
Output: lessons.json with 28 entries, `lib/models/lesson_progression.dart`,
toleranceRamp parsing on the Lesson model, and green unit tests.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — progression-engine unit tests encoding the decided semantics</name>
  <files>test/models/lesson_progression_test.dart</files>
  <read_first>
    - test/models/lesson_test.dart (pure-model test pattern: inline data → asserts, no Flutter binding)
    - test/models/journey_progress_test.dart (existing model-test conventions)
    - lib/models/lesson.dart (Lesson/LessonItem/LessonUnlock shapes the engine consumes)
    - .planning/phases/06-lesson-progression-home/06-RESEARCH.md (Pattern 2 — the exact decided semantics)
  </read_first>
  <behavior>
    Tests target a not-yet-existing `package:qalam/models/lesson_progression.dart`
    (INTENTIONALLY RED — carry the Wave-0 RED-contract header comment from
    test/data/app_database_test.dart; do NOT stub lib/). Build small in-test Lesson
    fixtures (3-5 lessons) rather than loading assets. Cover:
    - D-02 generic unlock: a lesson is unlocked iff every lesson in unlock.requires[]
      is passed; empty requires[] = unlocked. Multi-prerequisite case included
      (requires: [a, b] stays locked until BOTH passed) to prove the engine is generic.
    - lessonPassed: passRule allItemsPassed — every items[] entry with type 'letter'
      has its ref in masteredLetterIds.
    - D-03: signedOff is NOT an input — a lesson whose letter is unsigned still
      passes/unlocks normally (the engine API must not even accept signedOff).
    - D-05: with startingLessonId = lesson_03, lessons 01-02 are unlocked (index <
      startIndex forces unlocked regardless of requires[]) but not passed.
    - D-06: todayLesson = first non-passed lesson AT OR AFTER startingLessonId;
      skipped earlier lessons are never returned as today; unknown startingLessonId
      defensively treated as index 0.
    - D-11: todayLesson returns null when all lessons at/after the start are passed.
    - Phase happy path: mastered = {} → today is lesson_01; mastered = {alif} →
      lesson_01 passed, lesson_02 unlocked AND today (S1-09 in domain terms).
  </behavior>
  <action>
    Write the RED test file only. Reference engine API to code against:
    `lessonPassed(Lesson, Set&lt;String&gt; masteredLetterIds)`,
    `lessonUnlocked(...)`, `todayLesson(List&lt;Lesson&gt; ordered, String
    startingLessonId, Set&lt;String&gt; mastered)`, and
    `ProgressionSnapshot.compute(ordered, startingLessonId, mastered)` returning
    `{Lesson? today, Set&lt;String&gt; masteredLetterIds, Set&lt;String&gt;
    unlockedLessonIds, Map&lt;String, String&gt; lessonIdByLetterId, bool
    allMastered}` (the journey screen needs unlockedLessonIds + the letter→lesson
    map in plan 06-06). Implements S1-01/S1-09 rules per D-02/D-03/D-05/D-06/D-11.
  </action>
  <verify>
    <automated>flutter test test/models/lesson_progression_test.dart 2>&1 | grep -q "lesson_progression.dart" && echo "RED as expected (missing import)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the Wave-0 RED header comment ("INTENTIONALLY RED at Wave 0")
    - Running it FAILS with a compile error on the missing package:qalam/models/lesson_progression.dart import (not on fixture bugs)
    - Test names mention each decided rule: at least one test each for D-02 (incl. multi-prerequisite), D-03, D-05, D-06 (incl. unknown-id fallback), D-11
    - No lib/ files created or modified by this task
  </acceptance_criteria>
  <done>RED test file committed; failure is the missing engine import only.</done>
</task>

<task type="auto">
  <name>Task 2: Expand lessons.json to 28 lessons + toleranceRamp data plumbing (D-01, D-19)</name>
  <files>assets/curriculum/lessons.json, lib/models/lesson.dart, lib/data/curriculum_repository.dart, test/data/curriculum_repository_test.dart, test/models/lesson_test.dart</files>
  <read_first>
    - assets/curriculum/lessons.json (current single-entry shape to replicate exactly)
    - assets/curriculum/letters.json (canonical IDs in array order — this IS the curriculum order; display names for titles)
    - lib/models/lesson.dart (Lesson.fromJson / LessonUnlock.fromJson defensive-parse idiom to extend)
    - lib/data/curriculum_repository.dart (_ensureLoaded parse path; getLessons/getLesson; load-guard behavior)
    - test/data/curriculum_repository_test.dart (existing test conventions to extend)
  </read_first>
  <action>
    1. lessons.json: keep top-level shape, add `"defaultToleranceRamp": ["loose",
    "normal", "strict"]` at the file level (D-19 — data the owner's mother can edit),
    and author lessons lesson_01..lesson_28 (zero-padded IDs), order 1..28, one per
    letter in the EXACT letters.json array order: alif, baa, taa, thaa, jeem, haa_c,
    khaa, daal, dhaal, raa, zaay, seen, sheen, saad, daad, taa_h, zhaa, ayn, ghayn,
    faa, qaaf, kaaf, laam, meem, noon, haa_f, waaw, yaa (D-01; canonical IDs —
    Pitfall 1's 19/28 drift list must NOT leak in here). Each entry: title.display
    "Lesson N — {Display}" using letters.json name.display (placeholder wording per
    CONTEXT discretion; content not code), items [{type: letter, ref: <canonical
    id>}], unlock {requires: [<previous lesson id>] (empty for lesson_01), passRule:
    "allItemsPassed"}. Do NOT add per-lesson toleranceRamp entries (global default
    suffices today; per-lesson override is supported by the parser below).
    2. lib/models/lesson.dart: Lesson gains optional `List&lt;String&gt;?
    toleranceRamp`, parsed defensively like LessonUnlock.fromJson (absent → null,
    never throw) (D-19).
    3. lib/data/curriculum_repository.dart: parse the file-level defaultToleranceRamp
    in _ensureLoaded and expose it (e.g., `Future&lt;List&lt;String&gt;&gt;
    getDefaultToleranceRamp()` defaulting to ['loose','normal','strict'] when
    absent). Consumed by plan 06-04.
    4. test/data/curriculum_repository_test.dart — the Pitfall-10 integrity gate:
    getLessons() returns exactly 28; every lesson.items[].ref is an id present in
    letters.json (load both via the repository); every unlock.requires[] entry
    resolves to a lesson id; orders are exactly 1..28 with no duplicates; lesson_01
    has empty requires and ref alif; lesson IDs are unique; defaultToleranceRamp
    parses as ['loose','normal','strict'].
    5. test/models/lesson_test.dart: toleranceRamp parse cases (present → list,
    absent → null).
  </action>
  <verify>
    <automated>flutter test test/data/curriculum_repository_test.dart test/models/lesson_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('assets/curriculum/lessons.json')); assert len(d['lessons'])==28; assert d['defaultToleranceRamp']==['loose','normal','strict']"` exits 0
    - lessons.json contains "haa_c", "daal", "taa_h", "zhaa", "haa_f" (canonical IDs, not journey-screen drift IDs like "tah"/"dhah")
    - Integrity test asserts every items[].ref resolves against letters.json and every requires[] resolves against lesson IDs
    - flutter test test/data/curriculum_repository_test.dart test/models/lesson_test.dart passes
    - Existing repository behavior unchanged (full pre-existing curriculum_repository tests still green)
  </acceptance_criteria>
  <done>28 lessons load through CurriculumRepository with integrity tests green; ramp data parses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement the pure-Dart progression engine</name>
  <files>lib/models/lesson_progression.dart</files>
  <read_first>
    - test/models/lesson_progression_test.dart (the contract written in Task 1 — implement to it, do not weaken it)
    - lib/models/journey_progress.dart (the canonical pure-Dart model file: header comment, static compute pattern, immutable snapshot pattern)
    - lib/models/lesson.dart (input types)
  </read_first>
  <action>
    Create lib/models/lesson_progression.dart implementing the API from Task 1.
    Rules (binding): pure Dart — no Flutter, no dart:ui, no imports from lib/data/
    or lib/features/ (models-purity convention from journey_progress.dart). Encode:
    D-02 generic requires[] evaluation; D-03 signedOff never consulted; D-05 `index
    &lt; startIndex → unlocked` explicitly (skipped lessons unlocked regardless of
    requires); D-06 today = first non-passed at/after startingLessonId, unknown id →
    index 0; D-11 today == null when exhausted; allMastered flag = (today == null).
    ProgressionSnapshot is an immutable const-constructible class mirroring
    JourneyProgress's shape conventions, including `lessonIdByLetterId` built from
    lesson items. Run the Task-1 tests until green; do not modify test expectations
    to make them pass.
  </action>
  <verify>
    <automated>flutter test test/models/lesson_progression_test.dart && flutter analyze lib/models/lesson_progression.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/models/lesson_progression_test.dart passes with zero skips
    - `grep -cE "import 'package:flutter|import 'dart:ui|lib/data/|lib/features/" lib/models/lesson_progression.dart` returns 0
    - `grep -c "signedOff" lib/models/lesson_progression.dart` returns 0 (D-03: not even referenced)
    - flutter analyze reports no issues for the new file
  </acceptance_criteria>
  <done>Engine green against the Wave-0 contract; purity rules hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lessons.json asset → parser | Bundled (trusted) data, but typo'd refs silently break practice (Pitfall 10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering/Integrity | lessons.json refs vs letters.json IDs | mitigate | Task-2 integrity test: every items[].ref and unlock.requires[] must resolve; runs on every suite execution |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- flutter test test/models/lesson_progression_test.dart test/data/curriculum_repository_test.dart test/models/lesson_test.dart — all green
- Full suite (`flutter test`) known-failing set not grown beyond documented font-drift goldens + stale 03.1 debt (reconciled in later plans)
</verification>

<success_criteria>
- 28 lessons authored with canonical letter refs and linear requires chain (D-01)
- Engine answers passed/unlocked/today exactly per D-02/D-03/D-05/D-06/D-11, proven by unit tests written before the implementation
- toleranceRamp is parseable data with a global default (D-19) — no code change needed for the owner's mother to edit it
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-01-SUMMARY.md` when done
</output>
