---
phase: 06-lesson-progression-home
plan: 07
type: execute
wave: 4
depends_on: [06-03, 06-04, 06-06]
files_modified:
  - lib/features/practice/widgets/mastery_celebration.dart
  - lib/features/practice/practice_screen.dart
  - lib/l10n/app_en.arb
  - test/features/practice/practice_screen_test.dart
  - test/features/practice/mastery_celebration_golden_test.dart
autonomous: true
requirements: [S1-09]
must_haves:
  truths:
    - "After mastering a letter, the celebration offers a primary 'Next Lesson' that goes straight into the newly unlocked letter's practice (D-14, S1-09 immediate unlock)"
    - "On the LAST lesson, the primary slot becomes 'See Journey' and Next Lesson is absent (D-16)"
    - "The celebration speaks the mastered letter — glyph and copy template on {letterName}, never 'alif' after mastering baa (Pitfall 6)"
    - "One warm tutor line appears: 'Go show your {letterName} to someone at home.' (D-17)"
    - "'See journey' navigates /journey?highlight={masteredLetterId} feeding D-15"
  artifacts:
    - path: "lib/features/practice/widgets/mastery_celebration.dart"
      provides: "Parameterized celebration: letter, onNextLesson, isLastLesson, tutor line; Back Home demoted to ghost"
      contains: "celebrationNextLesson"
    - path: "lib/l10n/app_en.arb"
      provides: "celebrationNextLesson, celebrationShowSomeone, templated celebration/praise keys"
  key_links:
    - from: "lib/features/practice/practice_screen.dart"
      to: "lib/providers/progression_providers.dart"
      via: "post-mastery todayLessonProvider read → Next Lesson target / isLastLesson"
      pattern: "todayLessonProvider"
    - from: "lib/features/practice/widgets/mastery_celebration.dart"
      to: "/journey"
      via: "See journey with highlight param"
      pattern: "/journey\\?highlight="
---

<objective>
Make the pass → unlock moment real on screen: the mastery celebration gains the
primary "Next Lesson" CTA into the newly unlocked letter (D-14), the last-lesson
"See Journey" variant (D-16), and the "show someone at home" tutor line (D-17);
all alif-hardcoded celebration/praise copy and the mastered glyph template on the
actual letter (Pitfall 6 — Phase 6 makes baa+ reachable). Reconciles the triply
stale "no See journey button" test debt and performs the ONE sanctioned deliberate
golden re-bake.

Purpose: S1-09's "unlock is immediate on pass" must be something the child can
ACT on in the same breath as the celebration.
Output: parameterized celebration + practice wiring + reconciled tests.
</objective>

## Phase Goal

**As a** child opening Qalam, **I want to** find today's lesson already prepared with one clear Start — and have the next lesson unlock the moment I pass this one, **so that** I always know what to practice without navigating.

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-lesson-progression-home/06-CONTEXT.md
@.planning/phases/06-lesson-progression-home/06-RESEARCH.md
@.planning/phases/06-lesson-progression-home/06-PATTERNS.md
@.planning/phases/06-lesson-progression-home/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterized MasteryCelebration — Next Lesson, last-lesson variant, tutor line</name>
  <files>lib/features/practice/widgets/mastery_celebration.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - lib/features/practice/widgets/mastery_celebration.dart (constructor + onBackHome; _BackHomeButton filled-teal treatment at ~366-397 — the recipe Next Lesson copies; ghost-link pattern at ~158-175; _MasteredGlyph hardcoded 'ا' at ~266-277; l10n ?? idiom)
    - lib/l10n/app_en.arb (practiceCelebrationLine/practicePraiseLine current values; placeholder syntax precedent from homeGreeting)
    - .planning/phases/06-lesson-progression-home/06-UI-SPEC.md (Mastery celebration additions table — exact copy, treatments, one-primary-per-screen rule)
    - docs/design/kit/project/SKILL.md (tutor voice rules for D-17 — warm, specific, never chatbot-cheerful)
  </read_first>
  <behavior>
    Widget-level expectations (asserted in Task 2's tests):
    - Default variant: primary filled-teal "Next Lesson" (targetComfy, button
      shadow), "Back Home" demoted to ghost/outlined, "See journey" ghost link
      present → exactly ONE primary on screen
    - isLastLesson: primary slot is "See Journey", Next Lesson absent, Back Home
      stays ghost (D-16 — no capstone screen)
    - Tutor line "Go show your baa to someone at home." at Body scale under the
      Arabic praise (D-17 — exactly one line)
    - Mastered glyph + heading speak the actual letter (ب / "You learned baa.")
  </behavior>
  <action>
    1. app_en.arb additions (UI-SPEC Copywriting Contract): `celebrationNextLesson`
    = "Next Lesson"; `celebrationShowSomeone` = "Go show your {letterName} to
    someone at home." ({letterName} String placeholder); a templated
    `practiceCelebrationLineFor` = "You learned {letterName}." and
    `practicePraiseLineFor` = templated generalization of the current alif praise
    line (generic {letterName} phrasing — per-letter coaching wording is Phase-7
    content, the owner's mother's domain; note the UI-SPEC "celebration heading
    unchanged" tension is resolved per RESEARCH A4: type role unchanged, copy must
    template). Keep the old static keys in place if other call sites still read
    them; remove them only if all usages migrate this plan. Run flutter gen-l10n.
    2. MasteryCelebration gains constructor params: required letter glyph char +
    romanized display name (or the Letter model), `VoidCallback? onNextLesson`,
    `bool isLastLesson`. Primary CTA copies the _BackHomeButton filled-teal recipe
    with celebrationNextLesson label; demote Back Home to the ghost treatment;
    "See journey" navigates context.go('/journey?highlight=&lt;masteredLetterId&gt;')
    (D-15 handoff). _MasteredGlyph renders the passed char via ArabicText (never
    hardcoded 'ا'). Tutor line uses celebrationShowSomeone(letterName), Body
    scale, under the Arabic praise. Anti-gamification: no star deltas, no totals
    (demo keys stay demo-only).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze lib/features/practice/widgets/mastery_celebration.dart && flutter test test/features/practice/mastery_celebration_golden_test.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "celebrationNextLesson\|celebrationShowSomeone" lib/l10n/app_en.arb` returns 2 keys present
    - `grep -c "'ا'" lib/features/practice/widgets/mastery_celebration.dart` returns 0 (glyph parameterized)
    - Widget compiles with the new required params; flutter analyze clean
    - Exactly one filled-teal primary in the default variant (code inspection: one QalamColors.primary Material button per variant)
  </acceptance_criteria>
  <done>Celebration widget speaks the mastered letter and offers the decided CTA set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Practice wiring + per-letter copy + test reconciliation + deliberate golden re-bake</name>
  <files>lib/features/practice/practice_screen.dart, test/features/practice/practice_screen_test.dart, test/features/practice/mastery_celebration_golden_test.dart</files>
  <read_first>
    - lib/features/practice/practice_screen.dart (celebrate-phase branch constructing MasteryCelebration; any remaining alif-specific Watch-phase heading/tip strings; the resolved lessonId from 06-03)
    - test/features/practice/practice_screen_test.dart (stale asserts at lines 167 and 220 — "no See journey button" — rewrite, don't delete coverage)
    - test/features/practice/mastery_celebration_golden_test.dart (stale assert at line 74 + the golden comparison; landmine: ONLY this deliberate D-14/D-17 change earns a re-bake — never re-bake for font drift)
    - lib/providers/progression_providers.dart (todayLessonProvider for next-lesson lookup)
  </read_first>
  <behavior>
    - Mastering lesson_01 then tapping "Next Lesson" navigates to
      '/practice?lesson=lesson_02' (the stream already advanced today — S1-09)
    - With all lessons passed, the celebration shows the See Journey primary
      (isLastLesson) and no Next Lesson (D-16)
    - Tutor line visible with the mastered letter's name (D-17)
    - Rewritten tests assert the "See journey" link EXISTS and carries the
      highlight param (reconciling 03.1 debt per D-14)
  </behavior>
  <action>
    1. practice_screen.dart celebrate branch: pass the mastered letter (glyph +
    display name) into MasteryCelebration; onNextLesson reads
    todayLessonProvider — post-mastery the stream has already recomputed, so
    today IS the newly unlocked lesson — and navigates
    context.go('/practice?lesson=${next.id}'); isLastLesson = (today resolves
    null) → D-16 variant; while the provider momentarily resolves, follow the
    existing loading degradation (never a raw error).
    2. Replace remaining alif-hardcoded user-facing strings in the practice
    surfaces (celebration line, praise line, Watch-phase heading/tip if
    alif-specific) with the templated keys from Task 1 — generic {letterName}
    phrasing only; flag per-letter wording to Phase 7 in the SUMMARY.
    3. Reconcile stale tests: practice_screen_test.dart:167,220 and
    mastery_celebration_golden_test.dart:74 now assert the button set decided in
    D-14/D-16 (See journey present with highlight param; Next Lesson primary;
    Back Home ghost). Add the new behavior tests from <behavior>.
    4. Golden: re-bake test/features/practice/goldens for mastery_celebration ONCE
    for this legitimate layout change (the landmine-sanctioned deliberate re-bake).
    Document in the SUMMARY that this baseline was baked on THIS machine and
    carries the known local-font-drift caveat (glyph_audit golden untouched).
  </action>
  <verify>
    <automated>flutter test test/features/practice/practice_screen_test.dart test/features/practice/mastery_celebration_golden_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Both test files fully green locally (stale 167/220/74 asserts gone; golden re-baked deliberately)
    - `grep -rn "You learned alif" lib/` returns no matches (templated)
    - Next-Lesson navigation test asserts the literal route '/practice?lesson=lesson_02'
    - Last-lesson variant test present and green
    - Full suite known-failing set SHRINKS (practice stale debt cleared); glyph_audit golden binary unchanged (`git diff --stat` shows no change to glyph_audit golden)
  </acceptance_criteria>
  <done>Pass → celebrate → straight into the next letter; copy is letter-true; 03.1 debt cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider-derived next-lesson id → navigation | Route construction from computed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Next-Lesson route construction | mitigate | Target id comes only from the curriculum-derived todayLessonProvider (catalog-internal, never user input); PracticeScreen re-validates on arrival (06-03 allowlist) |
| T-06-09 | Gamification creep (PLAT-03) | celebration additions | mitigate | No star deltas/totals; one dignified star (existing); tutor-voice copy reviewed against SKILL.md rules |
| T-06-SC | Tampering | package installs | accept (N/A) | Zero new packages this phase |
</threat_model>

<verification>
- flutter test test/features/practice/ — green except documented environmental drift on OTHER goldens
- Full suite: stale-debt set shrinks; no new failures
</verification>

<success_criteria>
- D-14/D-16/D-17 observable and tested; S1-09's unlock is actionable from the celebration itself
- Pitfall 6 closed: no surface praises alif after mastering another letter
- The only golden re-baked is the celebration, deliberately, with provenance documented
</success_criteria>

<output>
Create `.planning/phases/06-lesson-progression-home/06-07-SUMMARY.md` when done
</output>
