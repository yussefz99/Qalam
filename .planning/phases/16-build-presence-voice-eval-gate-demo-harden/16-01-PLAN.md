---
phase: 16-build-presence-voice-eval-gate-demo-harden
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/models.py
  - server/.env.example
  - server/README.md
  - server/pyproject.toml
  - test/tutor/tts_coach_speaker_test.dart
  - server/tests/test_eval/__init__.py
  - server/tests/test_eval/test_eval_harness.py
autonomous: true
requirements: [PRES-02, EVAL-01, EVAL-02]
must_haves:
  truths:
    - "All three tutor nodes (analyze/plan/coach) default to keyless Gemini-on-Vertex (the live baseline), with no Anthropic API key referenced in the deploy contract"
    - "The coach routing table accepts an env-swap to Claude-on-Vertex (anthropic_vertex provider, COACH_LOCATION) without a further code change"
    - "RED tests for the TTS segmenter and the eval harness exist and fail by missing-symbol before any implementation"
  artifacts:
    - path: "server/app/models.py"
      provides: "Keyless-Vertex default routing + a COACH_MODEL_PROVIDER==anthropic_vertex branch (ChatAnthropicVertex, COACH_LOCATION)"
      contains: "anthropic_vertex"
    - path: "test/tutor/tts_coach_speaker_test.dart"
      provides: "Failing pure-Dart segmentByScript + availability-degrade unit tests (RED, no device)"
      contains: "segmentByScript"
    - path: "server/tests/test_eval/test_eval_harness.py"
      provides: "Failing eval-harness contract test (RED) for per-dimension scoring over labeled cases"
      contains: "test_eval"
  key_links:
    - from: "server/app/models.py build_coach_model()"
      to: "langchain_google_vertexai.model_garden.ChatAnthropicVertex"
      via: "COACH_MODEL_PROVIDER == 'anthropic_vertex' branch"
      pattern: "anthropic_vertex"
---

<objective>
Lock the per-node model routing table to the **keyless Gemini-on-Vertex baseline that is already live in production** (D-02), add the env-swap branch for the eval-gated Claude-on-Vertex coach upgrade (D-03), strip the superseded Anthropic-API-key path from the deploy contract, and lay the Wave-0 RED contract (failing tests) for the two net-new behaviors this phase introduces: on-device mixed-script TTS (PRES-02) and the eval harness (EVAL-01/EVAL-02).

Purpose: Everything else in Phase 16 rides on a correct routing table and a Nyquist RED contract. The live `qalam-tutor` service already routes all three nodes through `google_vertexai`/`gemini-2.5-flash` keyless, but `models.py` / `.env.example` / `README.md` still describe the superseded `anthropic` + key-in-Secret-Manager path (16-RESEARCH "Runtime State Inventory" + "State of the Art"). This plan makes the source of truth match the deployed truth and makes the Claude coach a drop-in env swap — not a code change — gated on a human Model-Garden Enable + an eval win.
Output: A keyless-Vertex routing table with the `anthropic_vertex` coach branch, updated env/README docs, and two failing test files (TTS segmenter + eval harness) that pin the contracts the Wave-2 implementations must satisfy.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the keyless-Vertex routing table + add the anthropic_vertex coach branch (D-02/D-03)</name>
  <files>server/app/models.py, server/.env.example, server/README.md, server/pyproject.toml</files>
  <read_first>
    - server/app/models.py (the file being modified — the per-node routing table; build_coach_model lines 83-97, env defaults lines 28-41, _provider_kwargs lines 44-54)
    - server/.env.example (the deploy contract — the stale ANTHROPIC_API_KEY/anthropic block to remove)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-03 finding: Gemini 200 in us-central1, Claude 404 "no access"; Pattern 1 ChatAnthropicVertex code shape; "Runtime State Inventory" — live deploy uses google_vertexai keyless, COACH_TIMEOUT_SECONDS=12; the recommended env-driven routing table)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("server/app/models.py (EDIT)" section — the exact branch to add, keep-the-lazy-import convention, keep _provider_kwargs)
    - docs/architecture/ADR-015-v2-tutor-server-langgraph-agent.md (per-node routing rationale)
  </read_first>
  <action>
    In server/app/models.py: change the env DEFAULT strings so all three nodes default to the live keyless baseline — ANALYZE_MODEL_PROVIDER, PLAN_MODEL_PROVIDER, COACH_MODEL_PROVIDER all default to "google_vertexai" (not "anthropic"/"google_genai"); ANALYZE_MODEL / PLAN_MODEL / COACH_MODEL all default to "gemini-2.5-flash". Update the module docstring routing table (lines 7-17) to the keyless-Vertex truth. Add COACH_LOCATION = os.environ.get("COACH_LOCATION", "global"). In build_coach_model(), add an early branch: if COACH_MODEL_PROVIDER == "anthropic_vertex", import ChatAnthropicVertex from langchain_google_vertexai.model_garden and return ChatAnthropicVertex(model_name=COACH_MODEL, project=os.environ["GCP_PROJECT_ID"], location=COACH_LOCATION, temperature=COACH_TEMPERATURE, max_tokens=COACH_MAX_TOKENS) — keyless via runtime SA ADC, returned UNbound (coach.py still calls .bind_tools(ACTION_TOOLS, tool_choice="any")). Keep the lazy-import convention (import inside the function), keep _provider_kwargs unchanged (thinking_budget=0 stays google_vertexai-only). In server/.env.example: delete the ANTHROPIC_API_KEY and GOOGLE_API_KEY placeholder lines and the "Provider API keys" comment block (keyless ADC has no provider key — D-02); set COACH_MODEL=gemini-2.5-flash, COACH_MODEL_PROVIDER=google_vertexai, COACH_TIMEOUT_SECONDS=12 (the live value); add commented GATED-UPGRADE lines for COACH_MODEL=claude-haiku-4-5@20251001 / COACH_MODEL_PROVIDER=anthropic_vertex / COACH_LOCATION=global with a "only after a human Enables Claude in Model Garden + the eval picks it" note. In server/pyproject.toml: mark langchain-anthropic with an inline comment "# REMOVE candidate (D-02): superseded Anthropic-direct-key path; Claude-on-Vertex uses ChatAnthropicVertex from langchain-google-vertexai" (do NOT remove the dependency yet — confirm nothing imports it first; removal is verified in Task note). Update server/README.md model-routing section to the keyless-Vertex truth. Do NOT re-introduce any Anthropic API key anywhere (D-02; keyless ADC only).
  </action>
  <verify>
    <automated>cd server && uv run pytest -m code -q</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE ASSERTION: `grep -n 'anthropic_vertex' server/app/models.py` returns the new coach branch.
    - SOURCE ASSERTION: `grep -n 'COACH_LOCATION' server/app/models.py` returns the new env line defaulting to "global".
    - SOURCE ASSERTION: `grep -c 'google_vertexai' server/app/models.py` shows all three node provider defaults set to google_vertexai (≥3 occurrences across the default strings + docstring).
    - SOURCE ASSERTION: `grep -v '^#' server/.env.example | grep -c 'ANTHROPIC_API_KEY'` returns 0 (no active key line; only optionally-commented gated-upgrade lines remain).
    - BEHAVIOR ASSERTION: `cd server && uv run pytest -m code -q` exits 0 (existing model-free suite — test_endpoint monkeypatches build_coach_with_tools, so the routing default change must not break it).
  </acceptance_criteria>
  <done>All three nodes default to keyless google_vertexai/gemini-2.5-flash; the anthropic_vertex coach branch + COACH_LOCATION exist and are env-swappable; .env.example/README carry no active Anthropic key; the model-free server suite stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 RED — failing TTS segmenter test + eval-harness contract test (Nyquist)</name>
  <files>test/tutor/tts_coach_speaker_test.dart, server/tests/test_eval/__init__.py, server/tests/test_eval/test_eval_harness.py</files>
  <read_first>
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Pattern 2 segment-by-script: Arabic block U+0600–U+06FF + presentation forms vs the rest, isLanguageAvailable('ar') degrade; Validation Architecture "Phase Requirements → Test Map" — the exact test files/commands; "Wave 0 Gaps")
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("server/tests/test_eval/ (NEW)" section — mirror test_faithfulness.py + faithfulness_set.jsonl; pytest.mark.code marker convention; the two-leg harness shape)
    - lib/services/asset_audio_player.dart (the pure-resolver analog: static String? audioAssetFor + its unit-test style — segmentByScript is the analog)
    - server/app/faithfulness.py (the evaluate_faithfulness report shape {faithful,flagged,total,rate} the harness reuses for the model-free leg)
    - server/tests/fixtures/faithfulness_set.jsonl (the labeled-case JSONL shape: passed/mistakeId/expectedFix/coaching/label)
    - server/pyproject.toml ([tool.pytest.ini_options] markers — the `code` marker)
  </read_first>
  <behavior>
    - TTS test: segmentByScript("أحسنت — that curve is perfect") returns runs tagged ('ar', 'أحسنت') and ('en-US', '— that curve is perfect') in order (Arabic run separated from Latin run).
    - TTS test: segmentByScript on a pure-English line returns a single ('en-US', ...) run; pure-Arabic returns a single ('ar', ...) run; empty string returns an empty list.
    - TTS test: a speaker constructed with a stub reporting isLanguageAvailable('ar')==false SKIPS the Arabic run and still speaks the English run (graceful degrade, never throws) — assert the spoken English run was attempted and the Arabic run was not.
    - Eval test: a harness entrypoint scores a labeled set and returns per-dimension scores for all four 14-AI-SPEC §5 dimensions (faithfulness D1, names-fix D2, register D5, correct-Arabic) — the model-free faithfulness dimension reuses evaluate_faithfulness and is exactly 1.0 (100%) on the faithful fixture.
  </behavior>
  <action>
    Create test/tutor/tts_coach_speaker_test.dart importing package:qalam/tutor/tts_coach_speaker.dart (file does NOT exist yet — RED by missing symbol). Assert the segmentByScript behaviors above against the (not-yet-written) top-level/static segmentByScript function, and assert the graceful-degrade behavior against a TtsCoachSpeaker built with an injectable TTS-availability stub. Match the 16-01 RED contract naming so the Wave-2 implementer writes zero test edits: name the function segmentByScript and the class TtsCoachSpeaker. Create server/tests/test_eval/__init__.py (empty) and server/tests/test_eval/test_eval_harness.py importing the (not-yet-written) eval harness module under server/tests/test_eval/ — assert it returns a per-dimension score dict whose model-free faithfulness leg equals 1.0 on the all-faithful subset of fixtures/faithfulness_set.jsonl and is RED by missing-symbol until 16-03 builds the runner. Mark the model-free portion with pytestmark = pytest.mark.code; mark any Vertex-judge-touching assertion to be SKIPPED in the code leg (it is integration, run under `make eval`, not `-m code`). Do NOT implement segmentByScript / TtsCoachSpeaker / the eval runner here — only the failing tests (Nyquist Wave 0).
  </action>
  <verify>
    <automated>flutter test test/tutor/tts_coach_speaker_test.dart 2>&1 | grep -qiE "compilation|Error|failed|some tests failed" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE ASSERTION: `grep -n 'segmentByScript' test/tutor/tts_coach_speaker_test.dart` and `grep -n 'TtsCoachSpeaker' test/tutor/tts_coach_speaker_test.dart` both return matches (names match the Wave-2 contract).
    - BEHAVIOR ASSERTION: `flutter test test/tutor/tts_coach_speaker_test.dart` FAILS to compile / fails (RED by missing symbol — tts_coach_speaker.dart does not exist yet). This is the intended Wave-0 state.
    - SOURCE ASSERTION: `grep -n 'pytest.mark.code' server/tests/test_eval/test_eval_harness.py` confirms the model-free leg carries the `code` marker.
    - BEHAVIOR ASSERTION: `cd server && uv run pytest server/tests/test_eval/test_eval_harness.py -m code -q` errors/fails (RED by missing eval-runner symbol) — the intended Wave-0 state.
  </acceptance_criteria>
  <done>Both RED test files exist, name the exact symbols (segmentByScript, TtsCoachSpeaker, the eval runner) the Wave-2 plans will implement, and fail by missing-symbol; the eval test's model-free leg carries pytest.mark.code; the Vertex-judge assertion is skipped in the code leg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Cloud Run `/coach` | Untrusted client identity crosses; gated by Firebase ID token + App Check (unchanged this plan) |
| Cloud Run → Vertex AI | Server authenticates to Vertex keyless via runtime SA ADC (no provider key) |
| deploy contract (.env.example / Secret Manager) | The set of secrets the image is allowed to reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Information disclosure | Anthropic API key in deploy contract | mitigate | D-02: remove ANTHROPIC_API_KEY/GOOGLE_API_KEY from .env.example + README; keyless ADC only — no provider key in image/Secret-Manager/client. Verified by grep gate (no active key line). |
| T-16-01-02 | Tampering (grounding) | anthropic_vertex coach branch | accept (provider-independent guard) | coach.py G2/G3/G4 already rewrite a fail-advance / out-of-set / unauthored action to a grounded `say` regardless of provider; the new branch only changes the bound model, not the grounding guards. |
| T-16-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installed this plan (langchain-google-vertexai already carries ChatAnthropicVertex; langchain-anthropic is only annotated, not removed). The new client dep (flutter_tts) is gated by the blocking-human legitimacy checkpoint in plan 16-02. |
</threat_model>

<verification>
- `cd server && uv run pytest -m code -q` exits 0 (routing default change does not break the model-free suite).
- `grep -v '^#' server/.env.example | grep -c 'ANTHROPIC_API_KEY'` returns 0.
- `flutter test test/tutor/tts_coach_speaker_test.dart` is RED (missing symbol) — the intended Wave-0 state.
- `cd server && uv run pytest server/tests/test_eval/test_eval_harness.py -m code -q` is RED (missing eval runner) — the intended Wave-0 state.
</verification>

<success_criteria>
- All three nodes default to keyless google_vertexai/gemini-2.5-flash; Claude is a drop-in env swap (anthropic_vertex + COACH_LOCATION), not a code change.
- No active Anthropic API key in the deploy contract (D-02).
- The TTS segmenter and eval-harness RED contracts exist, name the exact Wave-2 symbols, and fail by missing-symbol (Nyquist Wave 0).
</success_criteria>

<output>
Create `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-01-SUMMARY.md` when done
</output>
