---
phase: 16-build-presence-voice-eval-gate-demo-harden
plan: 05
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - server/tests/test_eval/gold_set.jsonl
  - server/tests/test_eval/GOLD-SIGNOFF.md
  - server/tests/test_eval/CALIBRATION.md
  - server/tests/test_eval/run_judge.py
autonomous: false
requirements: [EVAL-01]
user_setup: []
must_haves:
  truths:
    - "The (verdict → ideal-coaching) gold examples are Claude-DRAFTED and then REVIEWED + SIGNED by the owner's mother (her register authority); nothing register-shaping ships unsigned"
    - "The Vertex LLM-judge is calibrated against the signed gold labels and reaches ≥0.7 correlation/agreement before it is trusted"
    - "The signed gold set carries the D-10 constraint: it must NOT train/fine-tune models without separate verifiable parental consent"
  artifacts:
    - path: "server/tests/test_eval/gold_set.jsonl"
      provides: "The mom-SIGNED (verdict → ideal-coaching) gold set (signed flag flipped true behind the human gate)"
      contains: "signed"
    - path: "server/tests/test_eval/GOLD-SIGNOFF.md"
      provides: "The sign-off record (who signed, when, scope) — same authority as the curriculum sign-off"
      contains: "sign"
    - path: "server/tests/test_eval/CALIBRATION.md"
      provides: "The LLM-judge calibration record (≥0.7 correlation vs the signed gold labels)"
      contains: "0.7"
  key_links:
    - from: "server/tests/test_eval/run_judge.py"
      to: "server/tests/test_eval/gold_set.jsonl (signed)"
      via: "calibration against the signed gold labels"
      pattern: "gold_set"
---

<objective>
Take the Claude-DRAFTED gold set from 16-03 through the owner's-mother sign-off gate (her register authority — the same authority as the curriculum sign-off; nothing register-shaping ships unsigned, D-09), then calibrate the Vertex LLM-judge against the SIGNED gold labels until it reaches ≥0.7 correlation/agreement before the register + correct-Arabic dimensions are trusted in the gate.

Purpose: The tutor's voice is the product's signature, and the register authority belongs to the owner's mother — not to a model or to research. The eval gate can only trust the LLM-judge for register/Arabic once it agrees with her signed gold. This closes EVAL-01's "scored against a labeled set" with a human-validated label source and a calibrated judge.
Output: A signed gold_set.jsonl, a GOLD-SIGNOFF.md record, and a CALIBRATION.md proving the judge reaches the correlation floor.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md
@docs/curriculum/national-curriculum-grade1.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner's-mother sign-off on the (verdict → ideal-coaching) gold set (D-09, register authority)</name>
  <read_first>
    - server/tests/test_eval/gold_set.jsonl (the Claude-DRAFTED examples from 16-03, each "signed": false / "drafted_by": "claude")
    - server/tests/test_eval/JUDGE_RUBRIC.md (the register + correct-Arabic rubric the gold encodes — from 16-03)
    - CLAUDE.md ("The tutor's voice" — warm, calm, specific; short sentences for a 5–10 year old; names the exact fix; a little Arabic welcome but guidance in the working language; never hype; this voice comes from the owner's mother)
    - docs/curriculum/national-curriculum-grade1.md (the curriculum the coaching must stay consistent with)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-09 — Claude DRAFTS, owner's mother REVIEWS + SIGNS, same gate as the curriculum sign-off; D-10 the no-training constraint)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md ("Manual-Only Verifications" — "Mom sign-off on the gold-set coaching examples")
    - the curriculum sign-off pattern from prior phases (the model-drafts/mother-signs gate — e.g. 15-07's tier-level sign-off record)
  </read_first>
  <what-built>Claude drafted the (verdict → ideal-coaching) gold examples covering baa's mistakes × pass/fail (16-03). They are register-shaping content and MUST be reviewed + signed by the owner's mother before the judge is calibrated against them — the same authority as the curriculum sign-off. Nothing register-shaping ships unsigned.</what-built>
  <how-to-verify>
    1. Present each drafted (verdict, learner-state) → ideal-coaching example to the owner's mother.
    2. She edits any line whose register/Arabic is off (warmth, specificity, child-appropriateness, correct Arabic), or approves it as-is.
    3. Record her edits back into gold_set.jsonl and flip each reviewed case "signed": true; record any case she rejects/removes.
    4. Write GOLD-SIGNOFF.md: who signed, the date, the scope (which baa mistakes × pass/fail), and the statement that the set is synthetic/authored non-PII and carries the D-10 no-training-without-consent constraint.
  </how-to-verify>
  <action>Blocking human checkpoint (gate="blocking-human", same authority as the curriculum sign-off). Present the drafted gold lines, capture the owner's-mother edits/approvals, flip "signed": true ONLY on reviewed cases, and write GOLD-SIGNOFF.md. HALT until she signs — do not calibrate the judge or trust the register/Arabic dimensions against unsigned gold. workflow.auto_advance is ignored for this sign-off gate.</action>
  <verify>
    <human-check>The owner's mother reviewed + signed the gold examples; the signed cases are flipped "signed": true and GOLD-SIGNOFF.md records the sign-off.</human-check>
  </verify>
  <acceptance_criteria>
    - SOURCE ASSERTION: `grep -c '"signed": true' server/tests/test_eval/gold_set.jsonl` ≥ 1 and matches the count recorded in GOLD-SIGNOFF.md.
    - ARTIFACT ASSERTION: `server/tests/test_eval/GOLD-SIGNOFF.md` exists and names the signer, date, scope, and the D-10 no-training constraint (`grep -niE 'sign|consent|train' GOLD-SIGNOFF.md` returns matches).
    - GATE ASSERTION: no judge calibration proceeded before the sign-off (this task halts Task 2).
  </acceptance_criteria>
  <resume-signal>Type "gold signed: N cases" (with the count) or describe the edits/rejections.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Calibrate the Vertex LLM-judge against the signed gold (≥0.7 correlation) — EVAL-01</name>
  <files>server/tests/test_eval/run_judge.py, server/tests/test_eval/CALIBRATION.md</files>
  <read_first>
    - server/tests/test_eval/run_judge.py (the Gemini judge runner from 16-03 — gemini-2.5-flash via google_vertexai keyless; judge != coach-under-test)
    - server/tests/test_eval/gold_set.jsonl (now SIGNED — the trusted label source)
    - server/tests/test_eval/JUDGE_RUBRIC.md (the rubric the judge scores against)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Open Q2 — start judge on gemini-2.5-flash, require ≥0.7 correlation with mom's labels before trusting it; escalate to gemini-2.5-pro only if correlation is weak; A5 the judge can be Gemini; coach-under-test ≠ judge to avoid self-grading bias)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md (Wave 0 Gaps — "LLM-judge calibration record (≥0.7 correlation vs mom labels) before the judge is trusted")
    - server/app/models.py (the keyless google_vertexai build posture the judge reuses)
  </read_first>
  <action>
    Run run_judge.py over the SIGNED gold_set.jsonl: have the gemini-2.5-flash judge score each signed case's coaching on register (D5) + correct-Arabic against JUDGE_RUBRIC.md, and compare the judge scores to the signed gold labels — compute a correlation/agreement figure. Write server/tests/test_eval/CALIBRATION.md recording: the judge model + region (gemini-2.5-flash, google_vertexai, keyless), the date, the per-dimension correlation/agreement vs the signed gold, and the PASS/FAIL of the ≥0.7 floor. If correlation < 0.7, tune the rubric/prompt in run_judge.py (or escalate the judge to gemini-2.5-pro per RESEARCH Open Q2) and re-run until ≥0.7; record the final configuration in CALIBRATION.md. Confirm the judge model is DISTINCT from the coach-under-test model (no self-grading). Set the register/Arabic threshold the gate (make eval, 16-03) uses, derived from the calibrated judge's behavior on the signed gold, and record it in CALIBRATION.md.
  </action>
  <verify>
    <automated>cd server && grep -qE '0\.[789]|0\.[7-9][0-9]|1\.0' tests/test_eval/CALIBRATION.md && grep -qi 'gemini-2.5' tests/test_eval/run_judge.py</automated>
  </verify>
  <acceptance_criteria>
    - ARTIFACT ASSERTION: `server/tests/test_eval/CALIBRATION.md` exists and records a correlation/agreement ≥ 0.7 vs the signed gold and the chosen register/Arabic threshold (`grep -niE 'correlation|agreement|0.7|threshold' CALIBRATION.md` returns matches).
    - SOURCE ASSERTION: the judge model is Gemini and distinct from the coach-under-test — `grep -ni 'gemini-2.5' server/tests/test_eval/run_judge.py` returns the judge model; CALIBRATION.md states judge != coach.
    - BEHAVIOR ASSERTION: re-running `cd server && uv run python tests/test_eval/run_judge.py` against the signed gold reproduces a correlation ≥ 0.7 (the recorded calibration is reproducible).
  </acceptance_criteria>
  <done>The LLM-judge is calibrated against the signed gold (≥0.7), is Gemini (distinct from the coach), and the register/Arabic gate threshold is recorded in CALIBRATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude draft → owner's-mother review | Register-shaping content must pass the human authority before it is trusted |
| signed gold labels → LLM-judge calibration | The judge is only trusted once it agrees with the human label source |
| gold set → models | The labeled set could be misused as training data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering (register integrity) | model-authored coaching shipping unsigned | mitigate | D-09 blocking-human sign-off: only "signed": true cases are trusted; GOLD-SIGNOFF.md records the owner's-mother authority; nothing register-shaping ships unsigned (gate halts Task 2 until signed). |
| T-16-05-02 | Repudiation/bias | an un-calibrated or self-grading judge | mitigate | ≥0.7 correlation vs signed gold required before the judge is trusted (CALIBRATION.md); judge = gemini-2.5-flash, distinct from the coach-under-test (no self-grading). |
| T-16-05-03 | Privacy/compliance | gold set used to train models | mitigate | D-10 / 14-AI-SPEC §1b: synthetic/authored non-PII; GOLD-SIGNOFF.md records the no-training-without-separate-verifiable-parental-consent constraint; Vertex no-training-use / request logging off. |
| T-16-05-SC | Tampering | npm/pip/cargo installs | accept | No new package (langchain-google-vertexai already carries the Vertex judge path). |
</threat_model>

<verification>
- `grep -c '"signed": true' server/tests/test_eval/gold_set.jsonl` ≥ 1 and matches GOLD-SIGNOFF.md.
- `server/tests/test_eval/CALIBRATION.md` records ≥0.7 correlation vs the signed gold + the gate threshold.
- The judge is Gemini, distinct from the coach-under-test (no self-grading).
- Re-running run_judge.py reproduces the ≥0.7 calibration.
</verification>

<success_criteria>
- The gold set is mom-SIGNED (her register authority); nothing register-shaping ships unsigned (D-09).
- The Vertex LLM-judge is calibrated to ≥0.7 vs the signed gold and is distinct from the coach (no self-grading).
- The D-10 no-training constraint is recorded.
</success_criteria>

<output>
Create `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-05-SUMMARY.md` when done
</output>
