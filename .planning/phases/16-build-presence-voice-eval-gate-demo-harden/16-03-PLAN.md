---
phase: 16-build-presence-voice-eval-gate-demo-harden
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - server/app/faithfulness.py
  - server/tests/test_faithfulness.py
  - server/tests/fixtures/faithfulness_set.jsonl
  - server/tests/test_eval/__init__.py
  - server/tests/test_eval/test_eval_harness.py
  - server/tests/test_eval/run_judge.py
  - server/tests/test_eval/run_eval.py
  - server/tests/test_eval/gold_set.jsonl
  - server/tests/test_eval/JUDGE_RUBRIC.md
  - server/Makefile
autonomous: true
requirements: [EVAL-01, EVAL-02]
must_haves:
  truths:
    - "The eval harness runs the coach over labeled (verdict, learner-state) cases and reports per-dimension scores for all four 14-AI-SPEC §5 dimensions"
    - "Faithfulness is a ZERO-TOLERANCE gate: any praise-on-fail or wrong-fix drops D1 below 100% and `make eval` exits non-zero"
    - "Register + correct-Arabic are scored by a Vertex LLM-judge (judge != coach-under-test) against a rubric, gating on a threshold"
  artifacts:
    - path: "server/Makefile"
      provides: "`make eval` target — the local pre-merge gate that exits non-zero below threshold"
      contains: "eval"
    - path: "server/tests/test_eval/run_judge.py"
      provides: "Vertex LLM-judge runner for register + correct-Arabic (gemini-2.5-flash judge)"
      contains: "judge"
    - path: "server/tests/test_eval/gold_set.jsonl"
      provides: "Claude-DRAFTED (verdict → ideal-coaching) gold examples awaiting mom sign-off (16-05)"
      contains: "coaching"
    - path: "server/app/faithfulness.py"
      provides: "Grown zero-tolerance model-free faithfulness check (the D-08 floor)"
      contains: "evaluate_faithfulness"
  key_links:
    - from: "server/Makefile (make eval)"
      to: "server/tests/test_eval/run_eval.py"
      via: "uv run python tests/test_eval/run_eval.py"
      pattern: "run_eval"
    - from: "server/tests/test_eval/run_eval.py"
      to: "server/app/faithfulness.py evaluate_faithfulness"
      via: "import + D1=100% zero-tolerance check"
      pattern: "evaluate_faithfulness"
---

<objective>
Promote the Phase-15 model-free faithfulness seed into the EVAL-01/EVAL-02 regression gate: grow `faithfulness.py` + its labeled set to cover every baa mistake × pass/fail as a ZERO-TOLERANCE check (D-08), build the `server/tests/test_eval/` harness that reports per-dimension scores for all four 14-AI-SPEC §5 dimensions (D-10), add the Vertex LLM-judge runner for register + correct-Arabic against a rubric (D-09), DRAFT the (verdict → ideal-coaching) gold set for mom sign-off, and wire `make eval` as the local documented pre-merge gate that exits non-zero below threshold (D-07).

Purpose: A guardrail that catches tutor-quality regressions before they ship — the grounding safety invariant (praise-on-fail / wrong-fix fails the build) plus a calibrated judge for the Arabic register that decides the coach voice. This is the reusable harness the roadmap assigned to Phase 13, promoted here, and the same harness the Claude-vs-Gemini bake-off (16-06) rides.
Output: A grown faithfulness gate, a per-dimension eval harness, a Vertex LLM-judge runner + rubric, a Claude-drafted gold-set file (signed in 16-05), and a `make eval` target that gates on D1=100% (faithfulness) and a register/Arabic threshold.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
@server/app/faithfulness.py
@server/tests/fixtures/faithfulness_set.jsonl
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grow the zero-tolerance faithfulness gate (every baa mistake × pass/fail) — D-08</name>
  <files>server/app/faithfulness.py, server/tests/test_faithfulness.py, server/tests/fixtures/faithfulness_set.jsonl</files>
  <read_first>
    - server/app/faithfulness.py (the 15-06 seed — _contradicts lines 49-67, _PRAISE lexicon lines 39-46 incl. أحسنت, evaluate_faithfulness lines 87-117 returning {faithful,flagged,total,rate}; the FLOOR-not-ceiling doc lines 13-28)
    - server/tests/test_faithfulness.py (pytestmark = pytest.mark.code line 27; _SET load lines 34-38; the assertion shape)
    - server/tests/fixtures/faithfulness_set.jsonl (the labeled-case JSONL: passed/mistakeId/expectedFix/coaching/label; labels faithful / adversarial_praise_on_fail / adversarial_wrong_fix)
    - lib/tutor/authored_fallback_brain.dart (the signed baa feedback keys — the mistakeIds the labeled set must cover: shallowBowl, noDot, hasTail, tooBig, lifted, ...)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("server/app/faithfulness.py (GROW)" — grow the labeled set, keep model-free, pytest.mark.code, .dockerignore excludes tests/)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-08 zero-tolerance; "Don't Hand-Roll" — grow the seed, do NOT build a new judge for faithfulness)
  </read_first>
  <behavior>
    - The labeled set covers every baa mistakeId in both a faithful and an adversarial (praise-on-fail AND wrong-fix) variant.
    - evaluate_faithfulness over the all-faithful subset returns rate == 1.0.
    - Any adversarial_praise_on_fail or adversarial_wrong_fix case is flagged (contradicts) — included so a regression that loosens _contradicts is caught.
    - A zero-tolerance helper / test asserts D1 == 100% over the curated FAITHFUL gate set (not the adversarial probes), and FAILS if a single contradiction appears.
  </behavior>
  <action>
    Extend server/tests/fixtures/faithfulness_set.jsonl with cases covering EVERY baa mistakeId present in the signed authored feedback (shallowBowl, noDot, hasTail, tooBig, lifted, and any others authored), each with: a faithful case, an adversarial_praise_on_fail case (a fail line containing a _PRAISE token), and an adversarial_wrong_fix case (a fail line omitting the expectedFix token). Add faithful PASS cases (warm praise on a clean pass — must NOT be flagged). Keep the JSONL keys exactly: passed, mistakeId, expectedFix, coaching, label. In faithfulness.py: if the _PRAISE lexicon misses any praise word the new fixtures use, widen it (keep أحسنت), but do NOT add a model — it stays deterministic/model-free (D-08). In test_faithfulness.py (pytestmark = pytest.mark.code): add a ZERO-TOLERANCE assertion that evaluate_faithfulness over the curated FAITHFUL gate cases yields rate == 1.0 (D1=100%), and an assertion that each adversarial label is correctly flagged. Keep tests/ out of the Docker image (.dockerignore already excludes it).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_faithfulness.py -m code -x -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `cd server && uv run pytest tests/test_faithfulness.py -m code -x -q` exits 0.
    - SOURCE ASSERTION: `grep -c 'adversarial_praise_on_fail' server/tests/fixtures/faithfulness_set.jsonl` ≥ 3 and `grep -c 'adversarial_wrong_fix' server/tests/fixtures/faithfulness_set.jsonl` ≥ 3 (every-mistake adversarial coverage).
    - SOURCE ASSERTION: `grep -c '"label": "faithful"' server/tests/fixtures/faithfulness_set.jsonl` exceeds the original seed count (the set grew).
  </acceptance_criteria>
  <done>The labeled set covers every baa mistake × pass/fail; the zero-tolerance D1=100% assertion passes on the faithful gate cases and adversarial probes are flagged; the check stays model-free.</done>
</task>

<task type="auto">
  <name>Task 2: Build the eval harness (per-dimension scores) + Vertex LLM-judge runner + gold-set DRAFT + rubric — turn the 16-01 eval RED test GREEN</name>
  <files>server/tests/test_eval/__init__.py, server/tests/test_eval/test_eval_harness.py, server/tests/test_eval/run_judge.py, server/tests/test_eval/run_eval.py, server/tests/test_eval/gold_set.jsonl, server/tests/test_eval/JUDGE_RUBRIC.md</files>
  <read_first>
    - server/tests/test_eval/test_eval_harness.py (the RED contract from 16-01 — the per-dimension score dict shape + the model-free leg == 1.0 assertion to satisfy)
    - server/app/faithfulness.py (evaluate_faithfulness — reuse for the model-free D1 leg)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md (§5 the four eval dimensions/rubrics: D1 never-contradicts-geometry, D2 names-the-specific-fix, D5 register-for-a-5-10-year-old, correct-Arabic; §1b no-training-on-child-transcripts)
    - server/app/models.py (the keyless-Vertex build pattern — the judge calls gemini-2.5-flash via the same google_vertexai posture; judge != coach-under-test)
    - server/tests/test_endpoint.py (the offline fake-model injection pattern — _FakeBoundCoach / _patch_coach — for the model-free leg)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("server/tests/test_eval/ (NEW)" — two legs in one harness; labeled-case JSONL format; coach-under-test != judge; offline fakes for the code leg; the mom-signed gold set is a SEPARATE JSONL)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-09/D-10; Validation Architecture Test Map — run_judge.py invoked by make eval; ≥0.7 correlation requirement; Open Q2 judge-model recommendation gemini-2.5-flash; A5 the judge can be Gemini)
  </read_first>
  <action>
    Create server/tests/test_eval/run_eval.py exposing the eval-harness entrypoint the 16-01 RED test imports — a function that scores a labeled set and returns a per-dimension dict {D1_faithfulness, D2_names_fix, D5_register, correct_arabic} for all four 14-AI-SPEC §5 dimensions: D1 reuses evaluate_faithfulness (model-free, zero-tolerance); D2 is the model-free expected-fix-token presence (reuse the faithfulness wrong-fix logic); D5 + correct_arabic come from the LLM-judge (skipped/None in the offline `code` leg, computed under `make eval`). Make test_eval_harness.py GREEN: the model-free legs (D1/D2) equal 1.0 on the all-faithful fixtures; mark the model-free assertions pytest.mark.code; mark the Vertex-judge assertion to be SKIPPED under `-m code`. Create server/tests/test_eval/run_judge.py: a Vertex LLM-judge runner that builds a Gemini judge (gemini-2.5-flash via google_vertexai keyless — NOT the coach-under-test model, to avoid self-grading bias, RESEARCH Open Q2) and scores register (D5) + correct-Arabic against JUDGE_RUBRIC.md, returning numeric scores in [0,1]; it reads the gold_set.jsonl and reports a correlation/agreement figure used for calibration. Create server/tests/test_eval/JUDGE_RUBRIC.md: the rubric prose for register-for-a-5-10-year-old (warm, calm, specific, short sentences, never hype, a little Arabic welcome) + correct-Arabic, anchored to CLAUDE.md "the tutor's voice" + docs/curriculum/national-curriculum-grade1.md. Create server/tests/test_eval/gold_set.jsonl: Claude-DRAFTS (verdict → ideal-coaching) examples covering baa's mistakes × pass/fail, each marked "signed": false with a "drafted_by": "claude" field — the mom-sign-off gate flips signed:true in 16-05; add a header comment / companion note stating D-10 (the labeled set / transcripts must NOT train/fine-tune models without separate verifiable parental consent; the gold set is synthetic/authored non-PII). Create server/tests/test_eval/__init__.py if not already present.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_eval/test_eval_harness.py -m code -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `cd server && uv run pytest tests/test_eval/test_eval_harness.py -m code -q` exits 0 (the 16-01 RED eval test is GREEN; model-free D1/D2 legs == 1.0).
    - SOURCE ASSERTION: `grep -n 'gemini-2.5-flash' server/tests/test_eval/run_judge.py` confirms the judge model is Gemini (not the coach-under-test); `grep -niE 'register|arabic' server/tests/test_eval/JUDGE_RUBRIC.md` returns the two judged dimensions.
    - SOURCE ASSERTION: `grep -c '"signed": false' server/tests/test_eval/gold_set.jsonl` ≥ 1 (drafted, awaiting mom sign-off) and `grep -niE 'consent|train' server/tests/test_eval/gold_set.jsonl server/tests/test_eval/run_eval.py` returns the D-10 no-training note.
    - SOURCE ASSERTION: `grep -niE 'D1|D2|D5|register|arabic|faithful' server/tests/test_eval/run_eval.py` confirms all four dimensions are scored/returned.
  </acceptance_criteria>
  <done>run_eval.py returns per-dimension scores for all four §5 dimensions; the model-free legs are GREEN under `-m code`; run_judge.py builds a Gemini judge (!= coach) against JUDGE_RUBRIC.md; gold_set.jsonl is drafted "signed": false with the D-10 no-training note.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `make eval` — the local pre-merge gate (exits non-zero below threshold) — D-07/D-08</name>
  <files>server/Makefile</files>
  <read_first>
    - server/pyproject.toml ([tool.pytest.ini_options] markers; [tool.uv] package = false lines 43-44 — the Makefile must not assume an installed package; use `uv run`)
    - server/tests/test_eval/run_eval.py (the entrypoint make eval wraps)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("server/Makefile (NEW)" — make eval wraps faithfulness (zero-tolerance) + Vertex LLM-judge; exits non-zero on D1<100% OR D5/D2 below threshold; the bake-off reuses the target by env-swap; use uv run)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-07 local pre-merge NOT CI; D-08 zero-tolerance; recommended routing-table env-swap for the bake-off)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md (Sampling Rate — `make eval` at wave merge + phase gate; the gate condition D1=100%, D5/D2 ≥ threshold)
  </read_first>
  <action>
    Create server/Makefile with an `eval` target that runs (via `uv run`): (1) the zero-tolerance model-free faithfulness gate (`uv run pytest tests/test_faithfulness.py tests/test_eval/test_eval_harness.py -m code -q`), then (2) the Vertex LLM-judge run (`uv run python tests/test_eval/run_judge.py`) — exiting NON-ZERO if D1 faithfulness < 100% (zero-tolerance, D-08) OR the register/Arabic threshold is not met (D-09 threshold). Document in a Makefile comment that this is the LOCAL documented pre-merge step, NOT CI (D-07), and that the Claude-vs-Gemini bake-off reuses the same target by env-swap (`COACH_MODEL_PROVIDER=anthropic_vertex COACH_LOCATION=global make eval` vs the Gemini run). Add a `.PHONY: eval` line. Keep `uv run` consistent with the project runner; do not assume the server package is installed.
  </action>
  <verify>
    <automated>cd server && grep -q '^eval:' Makefile && grep -q 'uv run' Makefile && make -n eval</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE ASSERTION: `grep -c '^eval:' server/Makefile` returns 1 and `grep -c '.PHONY: eval' server/Makefile` returns 1.
    - SOURCE ASSERTION: `grep -niE 'pre-merge|not ci|local' server/Makefile` returns the D-07 documentation comment; `grep -c 'uv run' server/Makefile` ≥ 1.
    - BEHAVIOR ASSERTION: `cd server && make -n eval` (dry run) prints the faithfulness + judge invocation without error (the gate wiring is well-formed; the live judge run is exercised at wave-merge after 16-05 calibration).
  </acceptance_criteria>
  <done>`make eval` wraps the zero-tolerance faithfulness gate + the Vertex LLM-judge and exits non-zero below threshold; it is documented as the local pre-merge step (not CI) and is bake-off-ready via env-swap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval gold set / labeled cases → models | The authored cases could be misused as training data |
| eval harness → Vertex AI (LLM-judge) | The judge call crosses to Vertex keyless via ADC |
| coach-under-test vs judge model | Self-grading bias if the same model both coaches and judges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Privacy/compliance | gold set / labeled transcripts used to train models | mitigate | D-10 / 14-AI-SPEC §1b: the gold set is synthetic/authored non-PII; the file + run_eval carry the explicit "no training/fine-tuning without separate verifiable parental consent" note; Vertex request-response logging stays off / no-training-use. |
| T-16-03-02 | Tampering (grounding) | a loosened faithfulness check letting praise-on-fail through | mitigate | D-08 zero-tolerance: `make eval` exits non-zero on D1 < 100%; adversarial_praise_on_fail / adversarial_wrong_fix probes in the labeled set fail the gate if _contradicts is weakened. |
| T-16-03-03 | Repudiation/bias | judge == coach (self-grading) | mitigate | run_judge.py uses gemini-2.5-flash as the judge, distinct from the coach-under-test model (RESEARCH Open Q2); calibration requires ≥0.7 correlation vs mom's gold labels before the judge is trusted (16-05). |
| T-16-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installed (langchain-google-vertexai already present carries the Vertex judge path; pytest already present). |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_faithfulness.py tests/test_eval/test_eval_harness.py -m code -q` GREEN.
- `cd server && make -n eval` prints a well-formed gate invocation.
- `grep -c '"signed": false' server/tests/test_eval/gold_set.jsonl` ≥ 1 (drafted, awaiting 16-05 sign-off).
- D-10 no-training note present in the gold-set / run_eval.
</verification>

<success_criteria>
- The eval harness reports per-dimension scores for all four 14-AI-SPEC §5 dimensions.
- Faithfulness is zero-tolerance (D1=100% or the gate fails); register/Arabic gate on a threshold via a Gemini judge (!= coach).
- `make eval` is the local pre-merge gate (not CI) and is bake-off-ready by env-swap.
- The gold set is drafted (signed:false) for mom sign-off in 16-05; the no-training constraint is recorded.
</success_criteria>

<output>
Create `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-03-SUMMARY.md` when done
</output>
