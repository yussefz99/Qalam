---
phase: 16-build-presence-voice-eval-gate-demo-harden
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - pubspec.yaml
  - android/app/src/main/AndroidManifest.xml
  - lib/tutor/tts_coach_speaker.dart
  - lib/providers/tts_providers.dart
  - test/tutor/tts_coach_speaker_test.dart
autonomous: false
requirements: [PRES-02]
user_setup: []
must_haves:
  truths:
    - "A spoken coach line is synthesized on-device via flutter_tts with no network round-trip"
    - "A mixed English+Arabic line is voiced per-script (Arabic run set to 'ar', English run to 'en-US') in order"
    - "When the Pixel lacks an Arabic voice the Arabic token is skipped and the English guidance still speaks — TTS never throws and never blocks the trace loop"
  artifacts:
    - path: "lib/tutor/tts_coach_speaker.dart"
      provides: "On-device coach-voice surface (segmentByScript + TtsCoachSpeaker) with never-block, silent-degrade posture"
      min_lines: 40
      contains: "segmentByScript"
    - path: "lib/providers/tts_providers.dart"
      provides: "ttsCoachSpeakerProvider (Provider + ref.onDispose) + NoopTtsCoachSpeaker for test override"
      contains: "ttsCoachSpeakerProvider"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "TTS_SERVICE <queries> intent so the engine is discoverable on Android 11+"
      contains: "TTS_SERVICE"
  key_links:
    - from: "lib/tutor/tts_coach_speaker.dart"
      to: "package:flutter_tts/flutter_tts.dart"
      via: "FlutterTts instance + per-segment setLanguage/speak"
      pattern: "FlutterTts"
    - from: "lib/providers/tts_providers.dart"
      to: "TtsCoachSpeaker.dispose"
      via: "ref.onDispose"
      pattern: "onDispose"
---

<objective>
Build the on-device coach-voice surface (PRES-02): the `flutter_tts` package (legitimacy-gated), the `TtsCoachSpeaker` service with the segment-by-script mixed-language pattern and graceful Arabic-voice degrade, its Riverpod provider, and the Android 11+ TTS discoverability manifest entry — turning the failing 16-01 RED tests GREEN.

Purpose: The tutor's voice is the product's signature. This slice makes coaching speakable on-device (lowest first-TTS latency, works in airplane mode, $0 — D-04) so a later plan can speak BOTH the live agent line AND the AuthoredFallback floor a beat after the visual. It must never block the trace loop (ADR-014: voice is display-only).
Output: A green `tts_coach_speaker_test.dart`, a disposable `TtsCoachSpeaker` behind a Riverpod provider, pinned `flutter_tts: ^4.2.5`, and the manifest `<queries> TTS_SERVICE` entry.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md
@lib/services/asset_audio_player.dart
@lib/providers/audio_providers.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate — verify flutter_tts before install (D-06)</name>
  <read_first>
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md ("Package Legitimacy Audit" table — flutter_tts 4.2.5, verified publisher eyedeadevelopment.com/dlutton, 1586 likes, 150/160 pts, ~267k/30d; "slopcheck does not cover pub.dev")
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("Package-legitimacy checkpoint (flutter_tts)" — copy the exact 07-02-PLAN T-07-02-01 task shape)
  </read_first>
  <what-built>Selected flutter_tts 4.2.5 for on-device coach TTS — to be verified for legitimacy before install per the project's Package Legitimacy Gate (slopcheck does NOT cover pub.dev/Dart; legitimacy rests on pub.dev signals + this blocking-human checkpoint, the identical basis used for audioplayers).</what-built>
  <how-to-verify>
    1. Confirm on pub.dev: verified publisher (eyedeadevelopment.com / dlutton), recent releases, high likes/pub-points (1586 likes, 150/160 pts, ~267k downloads/30d), Android support.
    2. Confirm the source repo is github.com/dlutton/flutter_tts and the latest version is 4.2.5 (2026-01-05).
    3. Approve the exact version to pin (4.2.5), or name an alternative.
  </how-to-verify>
  <action>Blocking human checkpoint (gate="blocking-human"). Present what-built, perform how-to-verify WITH the human, HALT until the resume-signal. Do not proceed autonomously past this gate (workflow.auto_advance is ignored for legitimacy checkpoints). Do not run `flutter pub add flutter_tts` before approval.</action>
  <verify>
    <human-check>Human confirms verified publisher + version on pub.dev and types the resume-signal.</human-check>
  </verify>
  <acceptance_criteria>
    - The human typed an approval naming the pinned version (or an alternative). Execution did not proceed past this gate autonomously.
  </acceptance_criteria>
  <resume-signal>Type "approved: flutter_tts@4.2.5" or name a different package/version.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install flutter_tts, add the TTS_SERVICE manifest query (Android 11+)</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - pubspec.yaml (line 71 audioplayers: ^6.5.0 — match the pin style)
    - android/app/src/main/AndroidManifest.xml (the existing <queries> block lines 40-46 — extend it, do not create a second <queries>)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Pitfall 5 — AndroidManifest <queries> TTS_SERVICE; minSdk 21 + Kotlin 1.9.10 already satisfied)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("AndroidManifest <queries> TTS discoverability" — the exact sibling <intent> to add)
  </read_first>
  <action>
    After the legitimacy approval, run `flutter pub add flutter_tts` (resolves 4.2.5) and confirm pubspec.yaml pins it as `flutter_tts: ^4.2.5` (style matching audioplayers: ^6.5.0). In android/app/src/main/AndroidManifest.xml, add a sibling <intent> INSIDE the existing <queries> element (do not add a second <queries>): an <intent> with <action android:name="android.intent.action.TTS_SERVICE" />. Do not touch minSdk or the Kotlin Gradle plugin (already ≥ the floor). Run a debug build to confirm the dependency links.
  </action>
  <verify>
    <automated>grep -n 'flutter_tts' pubspec.yaml && grep -n 'TTS_SERVICE' android/app/src/main/AndroidManifest.xml && flutter pub get</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE ASSERTION: `grep -c 'flutter_tts: \^4.2.5' pubspec.yaml` returns 1.
    - SOURCE ASSERTION: `grep -c 'TTS_SERVICE' android/app/src/main/AndroidManifest.xml` returns 1 and `grep -c '<queries>' android/app/src/main/AndroidManifest.xml` returns 1 (single queries element).
    - BEHAVIOR ASSERTION: `flutter pub get` exits 0 (flutter_tts 4.2.5 resolves cleanly against the lockstep set).
  </acceptance_criteria>
  <done>flutter_tts ^4.2.5 is pinned and resolved; the TTS_SERVICE intent is a sibling inside the single existing <queries> element.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build TtsCoachSpeaker (segment-by-script + graceful degrade) + ttsCoachSpeakerProvider — turn the 16-01 RED tests GREEN</name>
  <files>lib/tutor/tts_coach_speaker.dart, lib/providers/tts_providers.dart, test/tutor/tts_coach_speaker_test.dart</files>
  <read_first>
    - test/tutor/tts_coach_speaker_test.dart (the RED contract from 16-01 — the exact behaviors/symbols to satisfy: segmentByScript runs, degrade, never-throw)
    - lib/services/asset_audio_player.dart (the never-block analog: single reusable native handle, static pure resolver audioAssetFor, swallow-all-errors playLetter lines 96-116, dispose lines 118-125, the file-header house style lines 1-25)
    - lib/providers/audio_providers.dart (the provider analog: abstract interface class seam, Provider + ref.onDispose lines 31-35, NoopLetterAudioPlayer lines 17-21)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Pattern 2 segment-by-script: awaitSpeakCompletion(true) → isLanguageAvailable('ar') once → per-segment setLanguage('ar'|'en-US') + sequential speak(); skip Arabic run if !arOk; Arabic block U+0600–U+06FF + presentation forms; Anti-Patterns — never one setLanguage for the whole line, never block the loop)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("lib/tutor/tts_coach_speaker.dart (NEW)" — replicate the never-block posture, pure device-free resolver, single FlutterTts instance, dispose, provider wiring, NoopTtsCoachSpeaker)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md (the grounding invariant: TTS is display-only and can never imply a pass)
  </read_first>
  <behavior>
    - segmentByScript splits a line into ordered (locale, text) runs: Arabic-block runs → 'ar', everything else → 'en-US'; pure-English → one en-US run; pure-Arabic → one ar run; empty → [].
    - TtsCoachSpeaker.speak(line): awaitSpeakCompletion(true) once, check isLanguageAvailable('ar') once, then per run setLanguage + speak sequentially; if locale=='ar' && !arOk → skip that run (English still speaks).
    - speak() never throws and never blocks: any FlutterTts error is swallowed (mirrors asset_audio_player playLetter).
    - stop()/dispose() release the native handle; a cleared idle is silent.
  </behavior>
  <action>
    Create lib/tutor/tts_coach_speaker.dart with: a top-level pure function `List<(String locale, String text)> segmentByScript(String line)` that splits on the Arabic Unicode range (U+0600–U+06FF plus Arabic presentation forms U+FB50–U+FDFF / U+FE70–U+FEFF) versus the rest — device-free, unit-testable, the analog of audioAssetFor. Define an `abstract interface class CoachSpeaker { Future<void> speak(String line); Future<void> stop(); Future<void> dispose(); }` seam. Implement `class TtsCoachSpeaker implements CoachSpeaker` holding ONE reusable FlutterTts instance (inject for tests via constructor, default `FlutterTts()`); in speak(): await awaitSpeakCompletion(true), resolve arOk = (await isLanguageAvailable('ar')) == true once, iterate segmentByScript(line) runs — skip ar runs when !arOk, else setLanguage(locale) then speak(text); wrap the whole body in try/catch that SWALLOWS everything (never throw, never block — ADR-014). Make the availability check injectable so the RED test's stub (arOk=false) drives the degrade branch. Add a `class NoopTtsCoachSpeaker implements CoachSpeaker` (all no-ops) mirroring NoopLetterAudioPlayer. Write the project house-style file header (purpose, seam contract, NEVER-BLOCK POSTURE, degrade rules — like asset_audio_player.dart). Create lib/providers/tts_providers.dart with `final ttsCoachSpeakerProvider = Provider<CoachSpeaker>((ref) { final s = TtsCoachSpeaker(); ref.onDispose(s.dispose); return s; });` — Riverpod only (CLAUDE.md Decided; reject any non-Riverpod pattern). Make the 16-01 RED tests pass exactly as written (zero test edits beyond filling in any constructor-injection the RED contract left abstract).
  </action>
  <verify>
    <automated>flutter test test/tutor/tts_coach_speaker_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `flutter test test/tutor/tts_coach_speaker_test.dart` exits 0 (all 16-01 RED cases GREEN).
    - SOURCE ASSERTION: `grep -c 'try' lib/tutor/tts_coach_speaker.dart` ≥ 1 AND the speak() body catches and swallows (never rethrows) — never-block posture.
    - SOURCE ASSERTION: `grep -n 'ref.onDispose' lib/providers/tts_providers.dart` returns the disposal wiring; `grep -n 'NoopTtsCoachSpeaker' lib/tutor/tts_coach_speaker.dart` returns the test-override impl.
    - BEHAVIOR ASSERTION: `flutter analyze lib/tutor/tts_coach_speaker.dart lib/providers/tts_providers.dart` reports no errors.
  </acceptance_criteria>
  <done>segmentByScript + TtsCoachSpeaker + NoopTtsCoachSpeaker exist with the never-block/silent-degrade posture; ttsCoachSpeakerProvider disposes the native handle; the 16-01 RED tests are GREEN; Riverpod-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → app dependency tree | A new third-party native package (flutter_tts) enters the supply chain |
| coach TEXT → on-device TTS engine | Non-PII coach text is synthesized locally; no new network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-SC | Tampering | pub.dev install of flutter_tts | mitigate | Blocking-human package-legitimacy checkpoint (Task 1, gate="blocking-human") — verified publisher (eyedeadevelopment.com) + pinned ^4.2.5 on pub.dev BEFORE `flutter pub add`; mirrors audioplayers T-07-02-01. Never auto-approved. |
| T-16-02-01 | Information disclosure | coach text spoken on-device | accept | Only the non-PII derived coach text reaches the speaker (the server emits only derived facts → coach text; raw strokes/PII never cross — GROUND-02). TTS adds no network surface (on-device synthesis). |
| T-16-02-02 | Denial of service | a slow/missing TTS voice stalling the trace loop | mitigate | speak() swallows all errors, never awaits in a UI-blocking way, never throws (ADR-014 display-only); a missing Arabic voice is a silent skip — the instant on-screen verdict already rendered. |
</threat_model>

<verification>
- `flutter test test/tutor/tts_coach_speaker_test.dart` GREEN.
- `flutter analyze` clean on the two new files.
- `grep -c 'TTS_SERVICE' android/app/src/main/AndroidManifest.xml` returns 1 inside a single <queries>.
- `flutter pub get` resolves flutter_tts ^4.2.5.
</verification>

<success_criteria>
- The legitimacy gate was honored (blocking-human, not auto-approved).
- TtsCoachSpeaker voices a mixed en/ar line per-script and gracefully degrades when the Arabic voice is absent, never throwing or blocking (ADR-014 display-only).
- ttsCoachSpeakerProvider exists, Riverpod-only, disposes the native handle.
</success_criteria>

<output>
Create `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-02-SUMMARY.md` when done
</output>
