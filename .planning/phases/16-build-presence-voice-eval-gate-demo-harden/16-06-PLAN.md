---
phase: 16-build-presence-voice-eval-gate-demo-harden
plan: 06
type: execute
wave: 4
depends_on: ["16-04", "16-05"]
files_modified:
  - lib/demo/seeded_demo_state.dart
  - lib/main.dart
  - test/demo/seeded_demo_state_test.dart
  - docs/architecture/ADR-016-v2-per-node-model-verdict.md
  - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
autonomous: false
requirements: [DEMO-01]
user_setup: []
must_haves:
  truths:
    - "The baa AI-tutor path is demo-hardened end-to-end on the Pixel-Tablet build: Home/Journey → baa unit → mastery star, with no dead ends or stuck states"
    - "On any timeout/drop the AuthoredFallback floor takes over INVISIBLY — never a dead end or a hang on stage"
    - "A reliable seeded demo state fires the hero moment on cue: child wobbles → backward remediation re-surfaces an easier exercise → the tutor SPEAKS the specific fix → one quiet star at mastery"
    - "The per-node analyze/plan/coach model choices are finalized from the inline Gemini-vs-Claude bake-off and RECORDED; all-Gemini-keyless is the shippable default; the Claude coach is an eval-gated, human-Enable-gated upgrade"
    - "On-device Gemma stays deferred; the swappable TutorBrain seam keeps a future Gemma backend possible (TUTOR-04 met by the seam)"
  artifacts:
    - path: "lib/demo/seeded_demo_state.dart"
      provides: "A reliable demo seed (graph position + reps) that fires the wobble→remediation hero moment on cue"
      contains: "seedDemoState"
    - path: "docs/architecture/ADR-016-v2-per-node-model-verdict.md"
      provides: "The recorded per-node model verdict (SC#4) from the bake-off + the keyless-Vertex routing decision"
      contains: "coach"
  key_links:
    - from: "lib/demo/seeded_demo_state.dart"
      to: "lib/data/graph_position_repository.dart"
      via: "seed a wobble graph position + per-exercise reps"
      pattern: "graphPosition"
    - from: "docs/architecture/ADR-016-v2-per-node-model-verdict.md"
      to: "server/app/models.py routing table"
      via: "the finalized analyze/plan/coach model verdict"
      pattern: "gemini-2.5-flash"
---

<objective>
Demo-harden the baa AI-tutor path on the Pixel-Tablet build (DEMO-01): a reliable seeded demo state that fires the D-12 hero moment on cue (child wobbles → backward remediation re-surfaces an easier exercise → the tutor SPEAKS the specific fix → one quiet star at mastery), the AuthoredFallback floor proven as an INVISIBLE auto safety net on any timeout/drop (no dead ends, no hangs), and the full Home/Journey → baa unit → mastery-star path verified end-to-end on hardware. Then run the inline Gemini-vs-Claude coach bake-off on the calibrated eval harness and RECORD the finalized per-node model verdict (SC#4) in an ADR, with the Claude-coach upgrade gated on a human Model-Garden Enable — keeping all-Gemini-keyless as the shippable default.

Purpose: This is the finale of the v2.0 milestone — the demo must be bulletproof on stage for the technical Technion audience (the grounding guarantee is a deliberate second beat), and the per-node model choices must be settled by data, not assumption. The demo must ship and meet every Success Criterion on all-Gemini even if the Claude Enable never happens.
Output: A seeded-demo seam, the on-cue hero-moment verification on the Pixel Tablet, the bake-off result, and ADR-016 recording the per-node model verdict.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md
@docs/architecture/ADR-014-v2-tutor-agent-architecture.md
@.planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md
@lib/data/graph_position_repository.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reliable seeded demo state — the wobble → backward-remediation hero moment (D-12)</name>
  <files>lib/demo/seeded_demo_state.dart, lib/main.dart, test/demo/seeded_demo_state_test.dart</files>
  <read_first>
    - lib/data/graph_position_repository.dart (GraphPositionRepository — getPosition/setPosition (the durable cursor, D-08); LetterExerciseReps (per-essential-exercise clean-reps for isMasteryMet) — the state to seed)
    - lib/curriculum/curriculum_graph_walker.dart (remediateOneTier — the backward remediation the wobble triggers: first same-competency node one tier down ghayrManzur→manzur→manqul)
    - lib/tutor/exercise_selector_provider.dart (RouterExerciseSelector — pass→nextForward, fail→remediateOneTier; the selection switch the hero moment rides)
    - lib/features/letter_unit/letter_unit_controller.dart (the unit controller that resumes from the seeded position)
    - STATE.md (the existing demo-launch-flag quick task 260601-wa0 "boot app at /demo/home" + DEMO launch flag pattern — the precedent for a demo-only boot path)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md (D-12 hero moment + reliable seeded demo state; D-13 demo path Home/Journey → baa unit → mastery star)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Claude's Discretion — the seeded-demo-state mechanism is a debug/demo seed)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md (the star is the scorer's; the seed sets a STARTING state, never pre-awards a star)
  </read_first>
  <behavior>
    - seedDemoState writes a graph position + per-exercise reps for baa such that the next attempt is on a form the child is "wobbling" on (a medial/connected form), so a fail there triggers remediateOneTier → an easier (isolated/trace) exercise re-surfaces — the visible dynamism.
    - The seed is demo-only (behind a demo flag / debug, never a child-facing default boot) and is idempotent (re-running it resets to the same reliable starting state).
    - The seed sets reps such that completing the remediation + the form earns exactly ONE quiet star at mastery via isMasteryMet (the scorer/mastery rule owns the star — never pre-awarded by the seed).
  </behavior>
  <action>
    Create lib/demo/seeded_demo_state.dart with `Future<void> seedDemoState(GraphPositionRepository repo)` (inject the repo for tests): write a baa GraphPosition + LetterExerciseReps that places the child mid-unit on a form they are wobbling on so the next fail fires remediateOneTier (backward remediation), and sets the surrounding reps so that finishing the remediation + the form crosses isMasteryMet for exactly one quiet star. Make it idempotent (overwrite to the same state). Gate it behind the existing demo flag pattern (the 260601-wa0 demo launch flag / kDebugMode) in lib/main.dart so it ONLY runs on a demo boot, never the child-facing default — do NOT pre-award a star (the scorer owns it, ADR-014). Add test/demo/seeded_demo_state_test.dart with an in-memory AppDatabase + GraphPositionRepository asserting: (a) after seedDemoState the position is the wobble form, (b) re-running is idempotent (same state), (c) the seeded reps are below mastery (the star is still earned, not pre-set), (d) a fail at the seeded position resolves (via the walker) to an easier same-competency tier (backward remediation reachable).
  </action>
  <verify>
    <automated>flutter test test/demo/seeded_demo_state_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `flutter test test/demo/seeded_demo_state_test.dart` exits 0 — seed places the wobble form, is idempotent, leaves the star unearned, and a fail there reaches an easier tier.
    - SOURCE ASSERTION: `grep -n 'seedDemoState' lib/demo/seeded_demo_state.dart lib/main.dart` returns the seam + the demo-flag-gated call.
    - SOURCE ASSERTION: the seed is demo/debug-gated — `grep -nE 'kDebugMode|DEMO|String.fromEnvironment' lib/main.dart` shows the call is not on the default child boot path.
  </acceptance_criteria>
  <done>seedDemoState reliably + idempotently places the wobble state behind the demo flag (never the child default), the star stays the scorer's to earn, and a fail there reaches backward remediation; the test is GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-cue hero moment + no-dead-end demo-harden on the Pixel Tablet (DEMO-01/D-12/D-13) — autonomous:false, on hardware</name>
  <read_first>
    - lib/demo/seeded_demo_state.dart (the seed from Task 1)
    - lib/tutor/remote_agent_brain.dart (the degrade contract — every failure path returns the floor, never throws; the INVISIBLE safety net)
    - lib/tutor/coach_warmup.dart (the warm-up ping from 16-04 — masks cold-start)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the speak-the-line hook from 16-04 — the tutor SPEAKS the specific fix)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md (D-11 invisible auto safety net, never a dead end/hang; D-12 hero moment on cue, repeatably; D-13 demo path Home/Journey → baa unit → mastery star)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md ("Manual-Only Verifications" — "Seeded demo state fires backward-remediation on cue"; "Arabic Google-TTS voice presence on the stock Pixel Tablet")
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Pitfall 4 — confirm isLanguageAvailable('ar') on the device; degrade gracefully if absent)
  </read_first>
  <what-built>Tasks 1 (seed) + the 16-04 presence wiring (speak-the-line + warm-up) + the RemoteAgentBrain floor are all in. This task RUNS the demo on the Pixel Tablet and proves the hero moment fires on cue and there are no dead ends.</what-built>
  <how-to-verify>
    1. Build + run on the Pixel Tablet against the deployed qalam-tutor with the demo flag set (`--dart-define=TUTOR_BASE_URL=<service URL>` + the demo seed flag).
    2. Walk the demo path: Home/Journey → baa unit → trace. Confirm the warm-up ping masks cold-start (no long hang on the first /coach).
    3. Trigger the hero moment: wobble on the seeded form → confirm backward remediation re-surfaces an easier exercise → confirm the tutor SPEAKS the specific fix (Arabic token spoken if the voice is installed, else degrades to English-only with no gap) → continue to one quiet star at mastery.
    4. Confirm on `isLanguageAvailable('ar')` on this device and record the result.
    5. Fault-inject: enable airplane mode mid-flow → confirm the AuthoredFallback floor INVISIBLY takes over (a grounded spoken authored line, no dead end, no hang).
    6. Repeat the seeded flow to confirm it fires repeatably on cue. Record the run in a short device-UAT note.
  </how-to-verify>
  <action>Device verification (autonomous:false). HALT for the human to run the seeded demo on the Pixel Tablet and confirm: the hero moment fires on cue + repeatably, the floor takes over invisibly on a drop (no dead ends/hangs), the path Home/Journey → baa unit → one quiet star works end-to-end, and the Arabic voice availability is recorded. Do not mark DEMO-01 complete without the on-device confirmation.</action>
  <verify>
    <human-check>Human runs the seeded demo on the Pixel Tablet and confirms the hero moment fires on cue + repeatably, the floor takes over invisibly on a drop with no dead ends, and the full path reaches one quiet star.</human-check>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION (human-confirmed): the seeded wobble → backward remediation → spoken specific fix → one quiet star fires on cue and repeatably on the Pixel Tablet.
    - BEHAVIOR ASSERTION (human-confirmed): airplane-mode mid-flow degrades INVISIBLY to the AuthoredFallback floor — no dead end, no hang.
    - ARTIFACT ASSERTION: a device-UAT note records the run + the on-device `isLanguageAvailable('ar')` result.
  </acceptance_criteria>
  <resume-signal>Type "demo hardened" with the hero-moment + airplane-mode results, or describe what broke.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Model-Garden Enable for the Claude coach (D-03) — one-time human GCP console click (autonomous:false)</name>
  <read_first>
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (Pitfall 1 — Claude returns 404 "no access" until a human clicks Enable on the Claude Haiku 4.5 Model Garden card; the rawPredict 200 probe in "Code Examples"; Pitfall 2 — Claude region is global/us-east5, NOT us-central1; A1 — Technion credits may/may not cover partner-model billing)
    - server/app/models.py (the anthropic_vertex branch + COACH_LOCATION=global from 16-01 — the env-swap the Enable unlocks)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md ("Manual-Only Verifications" — "Model-Garden Enable for Claude Haiku 4.5 (if adopting the Claude coach)")
  </read_first>
  <what-built>The all-Gemini-keyless routing is the shippable default (16-01). The Claude coach is a gated UPGRADE: it needs a one-time human Enable on the Claude Haiku 4.5 Model Garden card (Claude returns 404 "no access" on the project today). This task is the Enable click + a 200 probe — required BEFORE the Claude leg of the bake-off (Task 4).</what-built>
  <how-to-verify>
    1. Owner opens Vertex AI → Model Garden → the Claude Haiku 4.5 card → Enable (accept Anthropic terms). One-time, self-serve, immediate.
    2. Verify with the rawPredict 200 probe from 16-RESEARCH "Code Examples" against region `us-east5` or `global` for `claude-haiku-4-5@20251001` (404 before Enable → 200 after).
    3. Confirm Technion credits cover partner-model billing (A1); if blocked, record that the demo ships all-Gemini (no demo impact).
  </how-to-verify>
  <action>Blocking human checkpoint (gate="blocking-human"). This is a one-time GCP console action separate from IAM. HALT until the human Enables Claude and confirms the rawPredict 200 probe (or reports it is blocked — in which case Task 4 runs Gemini-only and the demo ships all-Gemini, no demo impact). Do not run the Claude bake-off leg before a 200 probe. workflow.auto_advance is ignored for this gate.</action>
  <verify>
    <human-check>Human Enables Claude in Model Garden and reports the rawPredict 200 probe result (or "blocked").</human-check>
  </verify>
  <acceptance_criteria>
    - GATE ASSERTION: the human reported either a 200 rawPredict probe (Claude Enabled) OR "blocked: <reason>" (demo ships all-Gemini). Task 4's Claude leg only runs on a 200.
  </acceptance_criteria>
  <resume-signal>Type "claude enabled: 200" or "blocked: <reason> — ship all-Gemini".</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Coach bake-off (Gemini vs Claude, if Enabled) + record the per-node model verdict (SC#4)</name>
  <files>docs/architecture/ADR-016-v2-per-node-model-verdict.md, .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md</files>
  <read_first>
    - server/Makefile (the `make eval` target from 16-03 — the bake-off reuses it by env-swap)
    - server/tests/test_eval/run_judge.py + CALIBRATION.md (the calibrated judge from 16-05 — the bake-off is scored on register/Arabic + faithfulness)
    - server/app/models.py (the routing table + the anthropic_vertex coach branch from 16-01 — the env-swap COACH_MODEL_PROVIDER=anthropic_vertex COACH_LOCATION=global)
    - .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md (§4 the per-node routing table this verdict updates — NOTE §4's Anthropic-key path is SUPERSEDED by D-02; record the keyless-Vertex truth)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-02/D-03 routing lock; the recommended env-driven routing table; "best-Arabic model wins the coach voice"; A1 — if Claude can't be Enabled, ship all-Gemini)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md (SC#4 per-node model verdict recorded; on-device Gemma stays deferred; the swappable seam keeps TUTOR-04 possible)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md + ADR-015 (the ADR house style to mirror)
  </read_first>
  <action>
    Run the bake-off on the calibrated eval harness (16-05): score the coach on `gemini-2.5-flash` (us-central1, the live baseline) via `make eval`, and — ONLY if Task 3 reported a 200 (Claude Enabled) — score `claude-haiku-4-5@20251001` (region global) via `COACH_MODEL_PROVIDER=anthropic_vertex COACH_LOCATION=global make eval`, comparing the register/correct-Arabic + faithfulness scores. Write docs/architecture/ADR-016-v2-per-node-model-verdict.md recording: the FINALIZED per-node verdict — analyze = gemini-2.5-flash (us-central1, keyless), plan = gemini-2.5-flash (us-central1, keyless), coach = the bake-off winner (Gemini-2.5-flash by default; Claude-haiku-4-5@20251001/global ONLY if Enabled AND it won the Arabic-register dimension); the per-dimension bake-off scores; that all nodes are keyless Vertex (D-02, no Anthropic key — supersedes 14-AI-SPEC §4's anthropic-key path); that all-Gemini-keyless is the shippable default and the demo meets every Success Criterion on it even if the Enable never happened; and that on-device Gemma stays DEFERRED with the swappable TutorBrain seam keeping a future Gemma backend possible (TUTOR-04 met by the seam, not by shipping Gemma). Add a short ADDENDUM note to 14-AI-SPEC.md §4 pointing to ADR-016 as the finalized verdict (do not rewrite §4; append a dated pointer). If the deploy needs to change the live coach, note it is a `gcloud run deploy --set-env-vars` change (COACH_MODEL/COACH_MODEL_PROVIDER/COACH_LOCATION), not a code change.
  </action>
  <verify>
    <automated>cd server && make eval; test -f ../docs/architecture/ADR-016-v2-per-node-model-verdict.md && grep -qiE 'coach|gemini|keyless' ../docs/architecture/ADR-016-v2-per-node-model-verdict.md</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `cd server && make eval` exits 0 on the chosen coach (D1 faithfulness = 100%; register/Arabic ≥ the calibrated threshold from 16-05).
    - ARTIFACT ASSERTION: `docs/architecture/ADR-016-v2-per-node-model-verdict.md` exists and records the per-node verdict (analyze/plan/coach), the keyless-Vertex decision (no Anthropic key), all-Gemini as the shippable default, and Gemma-deferred-via-seam (`grep -niE 'analyze|plan|coach|keyless|gemma|all-gemini|default' ADR-016*.md` returns matches).
    - SOURCE ASSERTION: 14-AI-SPEC.md §4 carries a dated addendum pointer to ADR-016 (`grep -n 'ADR-016' .planning/phases/14-build-tutorbrain-spine-grounding-invariant/14-AI-SPEC.md` returns a match).
  </acceptance_criteria>
  <done>The bake-off ran on the calibrated harness; ADR-016 records the finalized per-node model verdict (all-Gemini-keyless default; Claude coach only if Enabled + eval-won), the keyless-Vertex posture, and Gemma-deferred-via-seam; 14-AI-SPEC §4 points to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo seed → durable Drift cursor | The demo seed writes the graph position; must never pre-award a star (the scorer owns it) |
| client → Cloud Run /coach (live demo) | The real online agent runs on stage; floor must invisibly cover any drop |
| GCP console (Model-Garden Enable) → Vertex partner-model access | A one-time human terms-acceptance gate, separate from IAM |
| coach bake-off → Vertex (Gemini + optional Claude) | Both legs run keyless via ADC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering (grounding) | the demo seed pre-awarding a star | mitigate | seedDemoState sets a STARTING state with reps BELOW mastery; the scorer/isMasteryMet owns the star (ADR-014); the seed-is-below-mastery assertion in the test enforces it. |
| T-16-06-02 | Denial of service | a cold-start or drop hanging the live demo on stage | mitigate | the 16-04 warm-up ping masks cold-start; RemoteAgentBrain degrades to the floor on any timeout/drop INVISIBLY (D-11); the airplane-mode fault-injection (Task 2) proves no dead end/hang. |
| T-16-06-03 | Information disclosure | the demo seed / instrumentation logging child PII | mitigate | the seed is demo/debug-gated (never a child-facing default boot) and writes only non-PII graph state; no raw strokes / nickname (GROUND-02). |
| T-16-06-04 | Tampering (supply-chain / grounding) | the Claude coach upgrade | mitigate | Claude is gated on a human Model-Garden Enable (Task 3, blocking-human) + winning the eval; coach.py G2/G3/G4 grounding guards are provider-independent; all-Gemini-keyless ships by default if the Enable is blocked. |
| T-16-06-05 | Information disclosure | re-introducing an Anthropic API key for the Claude coach | mitigate | D-02 keyless ADC only; ADR-016 records no Anthropic key anywhere; the anthropic_vertex branch uses runtime SA ADC, not a key. |
| T-16-06-SC | Tampering | npm/pip/cargo installs | accept | No new package (langchain-google-vertexai already carries ChatAnthropicVertex; the demo seed uses existing Drift/repos). |
</threat_model>

<verification>
- `flutter test test/demo/seeded_demo_state_test.dart` GREEN (wobble seed, idempotent, star unearned, remediation reachable).
- Human-confirmed: the hero moment fires on cue + repeatably on the Pixel Tablet; airplane-mode degrades invisibly; the full path reaches one quiet star.
- Human-confirmed: Claude Enabled (200 probe) OR blocked → ship all-Gemini.
- `cd server && make eval` GREEN on the chosen coach; `docs/architecture/ADR-016-v2-per-node-model-verdict.md` records the per-node verdict; 14-AI-SPEC §4 points to it.
</verification>

<success_criteria>
- The baa AI-tutor path is demo-hardened end-to-end on the Pixel-Tablet build with no dead ends; the floor takes over invisibly on any drop (DEMO-01).
- The hero moment fires on cue + repeatably (D-12).
- The per-node model verdict is finalized + recorded (SC#4); all-Gemini-keyless is the shippable default; the Claude coach is an eval-gated, human-Enable-gated upgrade.
- On-device Gemma stays deferred; the swappable seam keeps TUTOR-04 possible.
</success_criteria>

<output>
Create `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-06-SUMMARY.md` when done
</output>
