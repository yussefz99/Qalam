---
phase: 16-build-presence-voice-eval-gate-demo-harden
plan: 04
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - lib/features/letter_unit/widgets/exercise_scaffold.dart
  - lib/tutor/coach_warmup.dart
  - lib/features/letter_unit/letter_unit_screen.dart
  - test/tutor/coach_speak_hook_test.dart
  - test/tutor/coach_warmup_test.dart
autonomous: false
requirements: [PRES-01, PRES-02]
user_setup: []
must_haves:
  truths:
    - "The scorer verdict + star render instantly on-screen (local, silent); the spoken coach line plays a BEAT LATER on BOTH a clean pass and a miss"
    - "The spoken line voices BOTH the live agent line AND the AuthoredFallback floor (the floor speaks in airplane mode)"
    - "The instant stroke reflex never routes through the agent or TTS; a slow/missing voice never stalls the trace loop"
    - "A session-start warm-up GET /health ping is fired when a unit opens to mask Cloud Run cold-start"
    - "A written stroke→scorer→client→Cloud Run→model→render→first-TTS latency budget (warm + cold-start delta) is measured on the Pixel Tablet and recorded"
  artifacts:
    - path: "lib/features/letter_unit/widgets/exercise_scaffold.dart"
      provides: "The speak-the-line hook a beat after the visual, on pass + miss, voicing the resolved bubble text (agent line or floor)"
      contains: "ttsCoachSpeakerProvider"
    - path: "lib/tutor/coach_warmup.dart"
      provides: "warmUpCoach() — fire-and-forget GET /health at unit open (cold-start mask), never throws"
      contains: "/health"
    - path: ".planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-LATENCY-BUDGET.md"
      provides: "The written PRES-01 latency budget recorded from the on-device measurement (warm vs cold-start)"
      contains: "first-TTS"
  key_links:
    - from: "lib/features/letter_unit/widgets/exercise_scaffold.dart _onResult"
      to: "ttsCoachSpeakerProvider.speak"
      via: "fire-and-forget after tutorLineProvider.set, on pass + miss"
      pattern: "ttsCoachSpeakerProvider"
    - from: "lib/features/letter_unit/letter_unit_screen.dart"
      to: "GET {TUTOR_BASE_URL}/health"
      via: "warmUpCoach at unit open"
      pattern: "warmUpCoach"
---

<objective>
Make the tutor feel present (PRES-01/PRES-02): wire `TtsCoachSpeaker` into the exercise flow so the spoken coach line plays a BEAT AFTER the instant on-screen verdict, on BOTH a clean pass and a miss (the two clocks, D-05), voicing the resolved bubble text — the live agent line OR the AuthoredFallback floor (so the floor speaks in airplane mode, D-04); fire a session-start warm-up `GET /health` ping when a unit opens to mask Cloud Run cold-start (D-11); and measure + record the written stroke→scorer→client→Cloud Run→model→render→first-TTS latency budget on the Pixel Tablet, including the cold-start-vs-warm delta, with the millisecond stroke reflex never routing through the agent.

Purpose: Presence is the difference between "a chatbot replies" and "a patient teacher is sitting beside the child." The verdict must feel instant and the voice must arrive warmly a beat later — and it must hold offline. PRES-01's budget numbers come from the real device (ROADMAP hint=no), not a researched guess, so the measurement is an explicit autonomous:false on-hardware task.
Output: The speak-the-line hook in exercise_scaffold, a fire-and-forget warm-up pinger, the unit-open warm-up call, and `16-LATENCY-BUDGET.md` with the recorded device measurement.
</objective>

<execution_context>
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mareekhalila/Documents/Qalam/qalam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md
@.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md
@lib/features/letter_unit/widgets/exercise_scaffold.dart
@lib/tutor/tutor_providers.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Speak the coach line a beat after the visual, on pass + miss (D-05) — voice the agent line AND the floor (D-04)</name>
  <files>lib/features/letter_unit/widgets/exercise_scaffold.dart, test/tutor/coach_speak_hook_test.dart</files>
  <read_first>
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (the file being modified — _onResult lines 169-218: applyResult runs FIRST/unchanged at 171 (GROUND-01), then brain.next(facts).then sets tutorLineProvider at 206-217; _lineOf lines 221-225; _clear lines 229-235; initState clears stale line lines 152-159; _TutorColumn bubble text resolution where the floor line shows)
    - lib/tutor/tts_coach_speaker.dart (the CoachSpeaker seam + speak() — from 16-02; never-block, silent-degrade)
    - lib/providers/tts_providers.dart (ttsCoachSpeakerProvider — from 16-02)
    - lib/tutor/authored_fallback_brain.dart (the floor line resolution _resolveLine lines 39-53 — the floor IS the authored line; D-04 makes THIS line speak)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("exercise_scaffold.dart (EDIT)" — the exact hook point after set(line); fire-and-forget so a slow synth never stalls; speak the resolved bubble text (agent line OR floor) so the floor is voiced; on _clear stop in-flight TTS; guard if(!mounted))
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-05 two clocks; system diagram — TTS a BEAT after the visual, on BOTH pass and miss)
    - docs/architecture/ADR-014-v2-tutor-agent-architecture.md (grounding invariant: voice/TTS is display-only, can never imply a pass; the scorer owns pass/fail + star)
  </read_first>
  <behavior>
    - On a clean pass: applyResult runs first and the star/verdict render synchronously; AFTER the brain returns, the resolved line (agent praise, or floor feedback['pass'] on degrade) is spoken via the speaker — speak is fired, not awaited in a UI-blocking way.
    - On a miss: same ordering — verdict renders first, then the specific-fix line (agent line, or floor feedback[mistakeId]) is spoken.
    - The speaker is invoked with the SAME resolved text the bubble shows: prefer the agent line; when it is empty, voice the floor's authored line (so airplane-mode coaching speaks — D-04).
    - On _clear (Clear / Try-again): any in-flight TTS is stopped so a cleared idle is silent.
    - applyResult (GROUND-01) is untouched and still runs first; the speak hook can never flip a verdict.
  </behavior>
  <action>
    In exercise_scaffold.dart _onResult, INSIDE the existing brain.next(facts).then((decision){...}) after `ref.read(tutorLineProvider.notifier).set(...)` (around line 212): resolve the spoken text as the same bubble text the UI shows — the agent line when non-empty, else the floor's authored line for this verdict (mirror AuthoredFallbackBrain._resolveLine using widget.exercise.feedback: feedback['pass'] on a pass, feedback[mistakeId] on a known miss, the first non-'pass' line otherwise). Then fire-and-forget `ref.read(ttsCoachSpeakerProvider).speak(spokenText)` — do NOT await it in a way that blocks the build/UI (drop the future or use unawaited()); a slow or missing voice must never stall the child. Keep the existing if(!mounted) return guard (line 207) before reading providers. This must run on BOTH pass and miss (the .then fires for both). Do NOT move applyResult — it stays first and unchanged (GROUND-01 / the instant clock). In _clear (lines 229-235) and initState's stale-clear (lines 155-159), also call `ref.read(ttsCoachSpeakerProvider).stop()` so a cleared/fresh idle is silent. Add test/tutor/coach_speak_hook_test.dart: pump ExerciseScaffold with ProviderScope overrides — ttsCoachSpeakerProvider overridden with a recording fake CoachSpeaker, tutorBrainFactoryProvider overridden to return a brain yielding a known agent line, and a second case overridden to the AuthoredFallbackBrain floor — assert (a) the fake's speak() received the agent line on a pass and a miss, (b) on the floor case speak() received the authored floor line (D-04), (c) applyResult/verdict is recorded BEFORE speak (ordering), (d) _clear triggers stop(). Use the Noop/recording speaker pattern from 16-02 (NoopTtsCoachSpeaker as the base).
  </action>
  <verify>
    <automated>flutter test test/tutor/coach_speak_hook_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `flutter test test/tutor/coach_speak_hook_test.dart` exits 0 — speak() is called on pass AND miss, with the agent line when present and the floor line on degrade, AFTER the verdict, and stop() on clear.
    - SOURCE ASSERTION: `grep -n 'ttsCoachSpeakerProvider' lib/features/letter_unit/widgets/exercise_scaffold.dart` returns the speak hook + the stop() calls.
    - SOURCE ASSERTION: the speak call is fire-and-forget — `grep -nE 'unawaited\(|\.speak\(' lib/features/letter_unit/widgets/exercise_scaffold.dart` shows it is not `await`ed in the UI path (no `await ref.read(ttsCoachSpeakerProvider).speak`).
    - SOURCE ASSERTION: `applyResult` still appears BEFORE the speak hook in _onResult (GROUND-01 ordering preserved) — verify line order in the file.
  </acceptance_criteria>
  <done>The verdict renders instantly (applyResult first, unchanged); the resolved coach line (agent or floor) is spoken fire-and-forget a beat later on pass AND miss; clearing stops TTS; the floor speaks offline; the widget test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warm-up GET /health ping at unit open (cold-start mask) — D-11</name>
  <files>lib/tutor/coach_warmup.dart, lib/features/letter_unit/letter_unit_screen.dart, test/tutor/coach_warmup_test.dart</files>
  <read_first>
    - lib/tutor/tutor_providers.dart (tutorBaseUrlProvider lines 28-30 — the Cloud Run base URL from --dart-define=TUTOR_BASE_URL, empty by default; tutorHttpClientProvider lines 34-38 — the injectable client)
    - server/app/main.py (GET /health lines 61-68 — no auth, returns {"status":"ok"}; the header documents WHY it must be /health not /healthz — Google edge reserves /healthz)
    - lib/features/letter_unit/letter_unit_screen.dart (the unit-entry surface — where a unit opens; the warm-up fires here)
    - lib/tutor/remote_agent_brain.dart (the degrade contract — the warm-up never blocks; it is best-effort, mirrors the never-throw posture)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (D-11 warm-up ping; Architectural Responsibility Map "Warm-up ping" → client GET /health at session/unit start; the route is /health not /healthz)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-PATTERNS.md ("server/app/main.py (EDIT)" note — /health is already the warm-up path; the client warm-up targets THIS route)
  </read_first>
  <behavior>
    - warmUpCoach(client, baseUrl) issues a best-effort GET {baseUrl}/health when baseUrl is non-empty; on an empty baseUrl (offline/dev build with no define) it is a no-op.
    - It NEVER throws and NEVER blocks the unit-open path: any error (offline, DNS, timeout, non-200) is swallowed; the warm-up is fire-and-forget.
    - It is unauthenticated (the /health route carries no data and no auth — RESEARCH V4 access-control: intentionally unauthenticated).
  </behavior>
  <action>
    Create lib/tutor/coach_warmup.dart with a top-level `Future<void> warmUpCoach(http.Client client, String baseUrl)` (pure, injectable client for tests): if baseUrl is empty return immediately; else GET Uri.parse('$baseUrl/health') with a short timeout, wrapped in try/catch that SWALLOWS everything (best-effort cold-start mask, never blocks/throws). Add a file header documenting D-11 and that /health (not /healthz) is the route (Google edge reserves /healthz). In letter_unit_screen.dart, at unit open (the screen's initState / first build of the unit), call warmUpCoach fire-and-forget using ref.read(tutorHttpClientProvider) + ref.read(tutorBaseUrlProvider) — do not await, do not gate the screen on it. Add test/tutor/coach_warmup_test.dart with a MockClient asserting: (a) a non-empty baseUrl issues exactly one GET to {baseUrl}/health, (b) an empty baseUrl issues NO request, (c) a MockClient that throws causes warmUpCoach to complete normally (swallowed, never throws).
  </action>
  <verify>
    <automated>flutter test test/tutor/coach_warmup_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR ASSERTION: `flutter test test/tutor/coach_warmup_test.dart` exits 0 — GETs /health on a real baseUrl, no-ops on empty, swallows errors.
    - SOURCE ASSERTION: `grep -n '/health' lib/tutor/coach_warmup.dart` returns the route (and NOT /healthz).
    - SOURCE ASSERTION: `grep -n 'warmUpCoach' lib/features/letter_unit/letter_unit_screen.dart` returns the unit-open fire-and-forget call.
  </acceptance_criteria>
  <done>warmUpCoach best-effort-pings GET /health at unit open, no-ops offline, swallows all errors, and never blocks the unit-open path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Measure + record the PRES-01 latency budget on the Pixel Tablet (warm vs cold-start) — D-01 (autonomous:false, on hardware)</name>
  <read_first>
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-RESEARCH.md (A6 — latency NUMBERS are measured in-phase on the Pixel Tablet, not researched, by design; the system diagram — the segments to time; first-TTS latency + a possible silent TTS warm-up at unit open; cold-start-vs-warm delta)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-VALIDATION.md ("Manual-Only Verifications" — first row: record timestamps at scorer-verdict, /coach send, /coach receive, render, first-TTS; compare warm vs cold-start)
    - .planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-CONTEXT.md (D-01 the written budget; D-05 the ms stroke reflex stays local and never routes through the agent)
    - lib/features/letter_unit/widgets/exercise_scaffold.dart (where the instant verdict renders vs where the spoken line fires — confirm the reflex path is local)
  </read_first>
  <what-built>The speak-the-line presence hook (Task 1) + the warm-up ping (Task 2) are wired. This task INSTRUMENTS and RECORDS the real end-to-end latency on the Pixel Tablet. Add lightweight timestamp logging (kDebugMode/demo-flag only, never child-facing) at: stylus-up, scorer-verdict-rendered, /coach request sent, /coach response received, line rendered, first-TTS audio start. Run the seeded baa flow on the device.</what-built>
  <how-to-verify>
    1. Build + run the app on the Pixel Tablet against the deployed qalam-tutor (`--dart-define=TUTOR_BASE_URL=<service URL>`).
    2. Trigger a COLD path (first call after idle, min-instances=0): trace baa, record the per-segment timestamps; capture the cold-start delta.
    3. Trigger a WARM path (after the warm-up ping / a prior call): repeat and record.
    4. Confirm the instant stroke reflex (ink + scorer verdict) renders WITHOUT waiting on /coach or TTS (the ms reflex stays local — D-05).
    5. Write `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-LATENCY-BUDGET.md`: the acceptable delay per segment, the measured warm + cold numbers, the cold-start delta, the first-TTS start time, and an explicit statement that the local reflex never routes through the agent. Note whether a silent TTS warm-up at unit open is needed.
  </how-to-verify>
  <action>Device measurement (autonomous:false). Add the timestamp instrumentation (debug/demo-only), build to the Pixel Tablet, run the seeded flow, record warm + cold numbers, and WRITE 16-LATENCY-BUDGET.md. HALT for the human to run on hardware and confirm the recorded budget. Do not fabricate numbers — they must come from the device (A6).</action>
  <verify>
    <human-check>Human runs the seeded baa flow on the Pixel Tablet, confirms the instant reflex stays local, and confirms 16-LATENCY-BUDGET.md records the measured warm + cold-start numbers + first-TTS start.</human-check>
  </verify>
  <acceptance_criteria>
    - ARTIFACT ASSERTION: `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-LATENCY-BUDGET.md` exists and contains a per-segment budget, measured warm + cold numbers, the cold-start delta, and a first-TTS start time (`grep -niE 'cold|warm|first-TTS|budget' 16-LATENCY-BUDGET.md` returns matches).
    - BEHAVIOR ASSERTION (human-confirmed): the instant stroke reflex (ink + verdict + star) renders on-device without waiting on /coach or TTS.
  </acceptance_criteria>
  <resume-signal>Type "budget recorded" with the warm/cold first-TTS numbers, or describe what blocked the measurement.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Cloud Run `/coach` | Coach call (gated by ID token + App Check; unchanged) |
| client → Cloud Run `/health` (warm-up) | Intentionally unauthenticated; carries no data |
| coach TEXT → on-device TTS | Non-PII coach text synthesized locally a beat after the visual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering (grounding) | the speak-the-line hook | mitigate | applyResult (the scorer's verdict + star) runs FIRST and unchanged (GROUND-01); TTS is display-only (ADR-014) and fired a beat later — it can never flip a verdict; the hook only voices the already-resolved bubble text. |
| T-16-04-02 | Denial of service | a slow/missing TTS or a cold /coach stalling the trace loop | mitigate | speak() is fire-and-forget and swallows all errors (16-02); the instant reflex renders locally regardless; warmUpCoach is best-effort and never blocks; RemoteAgentBrain degrades to the floor on timeout. |
| T-16-04-03 | Access control | unauthenticated /health warm-up | accept | RESEARCH V4: /health is intentionally unauthenticated — no data, just a ping; the data-bearing /coach stays ID-token + App-Check gated. |
| T-16-04-04 | Information disclosure | timestamp instrumentation logging child data | mitigate | Instrumentation is debug/demo-flag only (never in a release child build) and logs only segment timings, no raw strokes / PII (GROUND-02). |
| T-16-04-SC | Tampering | npm/pip/cargo installs | accept | No new package (flutter_tts installed in 16-02; http already present). |
</threat_model>

<verification>
- `flutter test test/tutor/coach_speak_hook_test.dart` GREEN (speak on pass + miss, agent + floor, after verdict, stop on clear).
- `flutter test test/tutor/coach_warmup_test.dart` GREEN (GETs /health, no-ops offline, swallows errors).
- `16-LATENCY-BUDGET.md` exists with the device-measured warm + cold-start budget + first-TTS start (human-confirmed).
- `flutter analyze` clean on the touched files.
</verification>

<success_criteria>
- The verdict renders instantly; the coach line is spoken a beat later on pass AND miss, voicing the agent line or the floor (offline-capable) — D-05/D-04.
- The ms stroke reflex never routes through the agent or TTS.
- A warm-up GET /health fires at unit open to mask cold-start (D-11).
- The written PRES-01 latency budget (warm + cold delta + first-TTS) is measured on the Pixel Tablet and recorded.
</success_criteria>

<output>
Create `.planning/phases/16-build-presence-voice-eval-gate-demo-harden/16-04-SUMMARY.md` when done
</output>
